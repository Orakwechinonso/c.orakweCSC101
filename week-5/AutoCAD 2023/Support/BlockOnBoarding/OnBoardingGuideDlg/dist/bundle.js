/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./public/src/main.tsx");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./node_modules/@babel/runtime/helpers/esm/extends.js":
/*!************************************************************!*\
  !*** ./node_modules/@babel/runtime/helpers/esm/extends.js ***!
  \************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return _extends; });
function _extends() {
  _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

/***/ }),

/***/ "./node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js":
/*!*********************************************************************************!*\
  !*** ./node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js ***!
  \*********************************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return _objectWithoutPropertiesLoose; });
function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;

  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }

  return target;
}

/***/ }),

/***/ "./node_modules/@emotion/cache/dist/cache.browser.esm.js":
/*!***************************************************************!*\
  !*** ./node_modules/@emotion/cache/dist/cache.browser.esm.js ***!
  \***************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _emotion_sheet__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @emotion/sheet */ "./node_modules/@emotion/sheet/dist/sheet.browser.esm.js");
/* harmony import */ var _emotion_stylis__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @emotion/stylis */ "./node_modules/@emotion/stylis/dist/stylis.browser.esm.js");
/* harmony import */ var _emotion_weak_memoize__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @emotion/weak-memoize */ "./node_modules/@emotion/weak-memoize/dist/weak-memoize.browser.esm.js");




// https://github.com/thysultan/stylis.js/tree/master/plugins/rule-sheet
// inlined to avoid umd wrapper and peerDep warnings/installing stylis
// since we use stylis after closure compiler
var delimiter = '/*|*/';
var needle = delimiter + '}';

function toSheet(block) {
  if (block) {
    Sheet.current.insert(block + '}');
  }
}

var Sheet = {
  current: null
};
var ruleSheet = function ruleSheet(context, content, selectors, parents, line, column, length, ns, depth, at) {
  switch (context) {
    // property
    case 1:
      {
        switch (content.charCodeAt(0)) {
          case 64:
            {
              // @import
              Sheet.current.insert(content + ';');
              return '';
            }
          // charcode for l

          case 108:
            {
              // charcode for b
              // this ignores label
              if (content.charCodeAt(2) === 98) {
                return '';
              }
            }
        }

        break;
      }
    // selector

    case 2:
      {
        if (ns === 0) return content + delimiter;
        break;
      }
    // at-rule

    case 3:
      {
        switch (ns) {
          // @font-face, @page
          case 102:
          case 112:
            {
              Sheet.current.insert(selectors[0] + content);
              return '';
            }

          default:
            {
              return content + (at === 0 ? delimiter : '');
            }
        }
      }

    case -2:
      {
        content.split(needle).forEach(toSheet);
      }
  }
};

var createCache = function createCache(options) {
  if (options === undefined) options = {};
  var key = options.key || 'css';
  var stylisOptions;

  if (options.prefix !== undefined) {
    stylisOptions = {
      prefix: options.prefix
    };
  }

  var stylis = new _emotion_stylis__WEBPACK_IMPORTED_MODULE_1__["default"](stylisOptions);

  if (true) {
    // $FlowFixMe
    if (/[^a-z-]/.test(key)) {
      throw new Error("Emotion key must only contain lower case alphabetical characters and - but \"" + key + "\" was passed");
    }
  }

  var inserted = {}; // $FlowFixMe

  var container;

  {
    container = options.container || document.head;
    var nodes = document.querySelectorAll("style[data-emotion-" + key + "]");
    Array.prototype.forEach.call(nodes, function (node) {
      var attrib = node.getAttribute("data-emotion-" + key); // $FlowFixMe

      attrib.split(' ').forEach(function (id) {
        inserted[id] = true;
      });

      if (node.parentNode !== container) {
        container.appendChild(node);
      }
    });
  }

  var _insert;

  {
    stylis.use(options.stylisPlugins)(ruleSheet);

    _insert = function insert(selector, serialized, sheet, shouldCache) {
      var name = serialized.name;
      Sheet.current = sheet;

      if ( true && serialized.map !== undefined) {
        var map = serialized.map;
        Sheet.current = {
          insert: function insert(rule) {
            sheet.insert(rule + map);
          }
        };
      }

      stylis(selector, serialized.styles);

      if (shouldCache) {
        cache.inserted[name] = true;
      }
    };
  }

  if (true) {
    // https://esbench.com/bench/5bf7371a4cd7e6009ef61d0a
    var commentStart = /\/\*/g;
    var commentEnd = /\*\//g;
    stylis.use(function (context, content) {
      switch (context) {
        case -1:
          {
            while (commentStart.test(content)) {
              commentEnd.lastIndex = commentStart.lastIndex;

              if (commentEnd.test(content)) {
                commentStart.lastIndex = commentEnd.lastIndex;
                continue;
              }

              throw new Error('Your styles have an unterminated comment ("/*" without corresponding "*/").');
            }

            commentStart.lastIndex = 0;
            break;
          }
      }
    });
    stylis.use(function (context, content, selectors) {
      switch (context) {
        case -1:
          {
            var flag = 'emotion-disable-server-rendering-unsafe-selector-warning-please-do-not-use-this-the-warning-exists-for-a-reason';
            var unsafePseudoClasses = content.match(/(:first|:nth|:nth-last)-child/g);

            if (unsafePseudoClasses && cache.compat !== true) {
              unsafePseudoClasses.forEach(function (unsafePseudoClass) {
                var ignoreRegExp = new RegExp(unsafePseudoClass + ".*\\/\\* " + flag + " \\*\\/");
                var ignore = ignoreRegExp.test(content);

                if (unsafePseudoClass && !ignore) {
                  console.error("The pseudo class \"" + unsafePseudoClass + "\" is potentially unsafe when doing server-side rendering. Try changing it to \"" + unsafePseudoClass.split('-child')[0] + "-of-type\".");
                }
              });
            }

            break;
          }
      }
    });
  }

  var cache = {
    key: key,
    sheet: new _emotion_sheet__WEBPACK_IMPORTED_MODULE_0__["StyleSheet"]({
      key: key,
      container: container,
      nonce: options.nonce,
      speedy: options.speedy
    }),
    nonce: options.nonce,
    inserted: inserted,
    registered: {},
    insert: _insert
  };
  return cache;
};

/* harmony default export */ __webpack_exports__["default"] = (createCache);


/***/ }),

/***/ "./node_modules/@emotion/hash/dist/hash.browser.esm.js":
/*!*************************************************************!*\
  !*** ./node_modules/@emotion/hash/dist/hash.browser.esm.js ***!
  \*************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* eslint-disable */
// Inspired by https://github.com/garycourt/murmurhash-js
// Ported from https://github.com/aappleby/smhasher/blob/61a0530f28277f2e850bfc39600ce61d02b518de/src/MurmurHash2.cpp#L37-L86
function murmur2(str) {
  // 'm' and 'r' are mixing constants generated offline.
  // They're not really 'magic', they just happen to work well.
  // const m = 0x5bd1e995;
  // const r = 24;
  // Initialize the hash
  var h = 0; // Mix 4 bytes at a time into the hash

  var k,
      i = 0,
      len = str.length;

  for (; len >= 4; ++i, len -= 4) {
    k = str.charCodeAt(i) & 0xff | (str.charCodeAt(++i) & 0xff) << 8 | (str.charCodeAt(++i) & 0xff) << 16 | (str.charCodeAt(++i) & 0xff) << 24;
    k =
    /* Math.imul(k, m): */
    (k & 0xffff) * 0x5bd1e995 + ((k >>> 16) * 0xe995 << 16);
    k ^=
    /* k >>> r: */
    k >>> 24;
    h =
    /* Math.imul(k, m): */
    (k & 0xffff) * 0x5bd1e995 + ((k >>> 16) * 0xe995 << 16) ^
    /* Math.imul(h, m): */
    (h & 0xffff) * 0x5bd1e995 + ((h >>> 16) * 0xe995 << 16);
  } // Handle the last few bytes of the input array


  switch (len) {
    case 3:
      h ^= (str.charCodeAt(i + 2) & 0xff) << 16;

    case 2:
      h ^= (str.charCodeAt(i + 1) & 0xff) << 8;

    case 1:
      h ^= str.charCodeAt(i) & 0xff;
      h =
      /* Math.imul(h, m): */
      (h & 0xffff) * 0x5bd1e995 + ((h >>> 16) * 0xe995 << 16);
  } // Do a few final mixes of the hash to ensure the last few
  // bytes are well-incorporated.


  h ^= h >>> 13;
  h =
  /* Math.imul(h, m): */
  (h & 0xffff) * 0x5bd1e995 + ((h >>> 16) * 0xe995 << 16);
  return ((h ^ h >>> 15) >>> 0).toString(36);
}

/* harmony default export */ __webpack_exports__["default"] = (murmur2);


/***/ }),

/***/ "./node_modules/@emotion/memoize/dist/memoize.browser.esm.js":
/*!*******************************************************************!*\
  !*** ./node_modules/@emotion/memoize/dist/memoize.browser.esm.js ***!
  \*******************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
function memoize(fn) {
  var cache = {};
  return function (arg) {
    if (cache[arg] === undefined) cache[arg] = fn(arg);
    return cache[arg];
  };
}

/* harmony default export */ __webpack_exports__["default"] = (memoize);


/***/ }),

/***/ "./node_modules/@emotion/serialize/dist/serialize.browser.esm.js":
/*!***********************************************************************!*\
  !*** ./node_modules/@emotion/serialize/dist/serialize.browser.esm.js ***!
  \***********************************************************************/
/*! exports provided: serializeStyles */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "serializeStyles", function() { return serializeStyles; });
/* harmony import */ var _emotion_hash__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @emotion/hash */ "./node_modules/@emotion/hash/dist/hash.browser.esm.js");
/* harmony import */ var _emotion_unitless__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @emotion/unitless */ "./node_modules/@emotion/unitless/dist/unitless.browser.esm.js");
/* harmony import */ var _emotion_memoize__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @emotion/memoize */ "./node_modules/@emotion/memoize/dist/memoize.browser.esm.js");




var ILLEGAL_ESCAPE_SEQUENCE_ERROR = "You have illegal escape sequence in your template literal, most likely inside content's property value.\nBecause you write your CSS inside a JavaScript string you actually have to do double escaping, so for example \"content: '\\00d7';\" should become \"content: '\\\\00d7';\".\nYou can read more about this here:\nhttps://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals#ES2018_revision_of_illegal_escape_sequences";
var UNDEFINED_AS_OBJECT_KEY_ERROR = "You have passed in falsy value as style object's key (can happen when in example you pass unexported component as computed key).";
var hyphenateRegex = /[A-Z]|^ms/g;
var animationRegex = /_EMO_([^_]+?)_([^]*?)_EMO_/g;

var isCustomProperty = function isCustomProperty(property) {
  return property.charCodeAt(1) === 45;
};

var isProcessableValue = function isProcessableValue(value) {
  return value != null && typeof value !== 'boolean';
};

var processStyleName = Object(_emotion_memoize__WEBPACK_IMPORTED_MODULE_2__["default"])(function (styleName) {
  return isCustomProperty(styleName) ? styleName : styleName.replace(hyphenateRegex, '-$&').toLowerCase();
});

var processStyleValue = function processStyleValue(key, value) {
  switch (key) {
    case 'animation':
    case 'animationName':
      {
        if (typeof value === 'string') {
          return value.replace(animationRegex, function (match, p1, p2) {
            cursor = {
              name: p1,
              styles: p2,
              next: cursor
            };
            return p1;
          });
        }
      }
  }

  if (_emotion_unitless__WEBPACK_IMPORTED_MODULE_1__["default"][key] !== 1 && !isCustomProperty(key) && typeof value === 'number' && value !== 0) {
    return value + 'px';
  }

  return value;
};

if (true) {
  var contentValuePattern = /(attr|calc|counters?|url)\(/;
  var contentValues = ['normal', 'none', 'counter', 'open-quote', 'close-quote', 'no-open-quote', 'no-close-quote', 'initial', 'inherit', 'unset'];
  var oldProcessStyleValue = processStyleValue;
  var msPattern = /^-ms-/;
  var hyphenPattern = /-(.)/g;
  var hyphenatedCache = {};

  processStyleValue = function processStyleValue(key, value) {
    if (key === 'content') {
      if (typeof value !== 'string' || contentValues.indexOf(value) === -1 && !contentValuePattern.test(value) && (value.charAt(0) !== value.charAt(value.length - 1) || value.charAt(0) !== '"' && value.charAt(0) !== "'")) {
        console.error("You seem to be using a value for 'content' without quotes, try replacing it with `content: '\"" + value + "\"'`");
      }
    }

    var processed = oldProcessStyleValue(key, value);

    if (processed !== '' && !isCustomProperty(key) && key.indexOf('-') !== -1 && hyphenatedCache[key] === undefined) {
      hyphenatedCache[key] = true;
      console.error("Using kebab-case for css properties in objects is not supported. Did you mean " + key.replace(msPattern, 'ms-').replace(hyphenPattern, function (str, _char) {
        return _char.toUpperCase();
      }) + "?");
    }

    return processed;
  };
}

var shouldWarnAboutInterpolatingClassNameFromCss = true;

function handleInterpolation(mergedProps, registered, interpolation, couldBeSelectorInterpolation) {
  if (interpolation == null) {
    return '';
  }

  if (interpolation.__emotion_styles !== undefined) {
    if ( true && interpolation.toString() === 'NO_COMPONENT_SELECTOR') {
      throw new Error('Component selectors can only be used in conjunction with babel-plugin-emotion.');
    }

    return interpolation;
  }

  switch (typeof interpolation) {
    case 'boolean':
      {
        return '';
      }

    case 'object':
      {
        if (interpolation.anim === 1) {
          cursor = {
            name: interpolation.name,
            styles: interpolation.styles,
            next: cursor
          };
          return interpolation.name;
        }

        if (interpolation.styles !== undefined) {
          var next = interpolation.next;

          if (next !== undefined) {
            // not the most efficient thing ever but this is a pretty rare case
            // and there will be very few iterations of this generally
            while (next !== undefined) {
              cursor = {
                name: next.name,
                styles: next.styles,
                next: cursor
              };
              next = next.next;
            }
          }

          var styles = interpolation.styles + ";";

          if ( true && interpolation.map !== undefined) {
            styles += interpolation.map;
          }

          return styles;
        }

        return createStringFromObject(mergedProps, registered, interpolation);
      }

    case 'function':
      {
        if (mergedProps !== undefined) {
          var previousCursor = cursor;
          var result = interpolation(mergedProps);
          cursor = previousCursor;
          return handleInterpolation(mergedProps, registered, result, couldBeSelectorInterpolation);
        } else if (true) {
          console.error('Functions that are interpolated in css calls will be stringified.\n' + 'If you want to have a css call based on props, create a function that returns a css call like this\n' + 'let dynamicStyle = (props) => css`color: ${props.color}`\n' + 'It can be called directly with props or interpolated in a styled call like this\n' + "let SomeComponent = styled('div')`${dynamicStyle}`");
        }

        break;
      }

    case 'string':
      if (true) {
        var matched = [];
        var replaced = interpolation.replace(animationRegex, function (match, p1, p2) {
          var fakeVarName = "animation" + matched.length;
          matched.push("const " + fakeVarName + " = keyframes`" + p2.replace(/^@keyframes animation-\w+/, '') + "`");
          return "${" + fakeVarName + "}";
        });

        if (matched.length) {
          console.error('`keyframes` output got interpolated into plain string, please wrap it with `css`.\n\n' + 'Instead of doing this:\n\n' + [].concat(matched, ["`" + replaced + "`"]).join('\n') + '\n\nYou should wrap it with `css` like this:\n\n' + ("css`" + replaced + "`"));
        }
      }

      break;
  } // finalize string values (regular strings and functions interpolated into css calls)


  if (registered == null) {
    return interpolation;
  }

  var cached = registered[interpolation];

  if ( true && couldBeSelectorInterpolation && shouldWarnAboutInterpolatingClassNameFromCss && cached !== undefined) {
    console.error('Interpolating a className from css`` is not recommended and will cause problems with composition.\n' + 'Interpolating a className from css`` will be completely unsupported in a future major version of Emotion');
    shouldWarnAboutInterpolatingClassNameFromCss = false;
  }

  return cached !== undefined && !couldBeSelectorInterpolation ? cached : interpolation;
}

function createStringFromObject(mergedProps, registered, obj) {
  var string = '';

  if (Array.isArray(obj)) {
    for (var i = 0; i < obj.length; i++) {
      string += handleInterpolation(mergedProps, registered, obj[i], false);
    }
  } else {
    for (var _key in obj) {
      var value = obj[_key];

      if (typeof value !== 'object') {
        if (registered != null && registered[value] !== undefined) {
          string += _key + "{" + registered[value] + "}";
        } else if (isProcessableValue(value)) {
          string += processStyleName(_key) + ":" + processStyleValue(_key, value) + ";";
        }
      } else {
        if (_key === 'NO_COMPONENT_SELECTOR' && "development" !== 'production') {
          throw new Error('Component selectors can only be used in conjunction with babel-plugin-emotion.');
        }

        if (Array.isArray(value) && typeof value[0] === 'string' && (registered == null || registered[value[0]] === undefined)) {
          for (var _i = 0; _i < value.length; _i++) {
            if (isProcessableValue(value[_i])) {
              string += processStyleName(_key) + ":" + processStyleValue(_key, value[_i]) + ";";
            }
          }
        } else {
          var interpolated = handleInterpolation(mergedProps, registered, value, false);

          switch (_key) {
            case 'animation':
            case 'animationName':
              {
                string += processStyleName(_key) + ":" + interpolated + ";";
                break;
              }

            default:
              {
                if ( true && _key === 'undefined') {
                  console.error(UNDEFINED_AS_OBJECT_KEY_ERROR);
                }

                string += _key + "{" + interpolated + "}";
              }
          }
        }
      }
    }
  }

  return string;
}

var labelPattern = /label:\s*([^\s;\n{]+)\s*;/g;
var sourceMapPattern;

if (true) {
  sourceMapPattern = /\/\*#\ssourceMappingURL=data:application\/json;\S+\s+\*\//;
} // this is the cursor for keyframes
// keyframes are stored on the SerializedStyles object as a linked list


var cursor;
var serializeStyles = function serializeStyles(args, registered, mergedProps) {
  if (args.length === 1 && typeof args[0] === 'object' && args[0] !== null && args[0].styles !== undefined) {
    return args[0];
  }

  var stringMode = true;
  var styles = '';
  cursor = undefined;
  var strings = args[0];

  if (strings == null || strings.raw === undefined) {
    stringMode = false;
    styles += handleInterpolation(mergedProps, registered, strings, false);
  } else {
    if ( true && strings[0] === undefined) {
      console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR);
    }

    styles += strings[0];
  } // we start at 1 since we've already handled the first arg


  for (var i = 1; i < args.length; i++) {
    styles += handleInterpolation(mergedProps, registered, args[i], styles.charCodeAt(styles.length - 1) === 46);

    if (stringMode) {
      if ( true && strings[i] === undefined) {
        console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR);
      }

      styles += strings[i];
    }
  }

  var sourceMap;

  if (true) {
    styles = styles.replace(sourceMapPattern, function (match) {
      sourceMap = match;
      return '';
    });
  } // using a global regex with .exec is stateful so lastIndex has to be reset each time


  labelPattern.lastIndex = 0;
  var identifierName = '';
  var match; // https://esbench.com/bench/5b809c2cf2949800a0f61fb5

  while ((match = labelPattern.exec(styles)) !== null) {
    identifierName += '-' + // $FlowFixMe we know it's not null
    match[1];
  }

  var name = Object(_emotion_hash__WEBPACK_IMPORTED_MODULE_0__["default"])(styles) + identifierName;

  if (true) {
    // $FlowFixMe SerializedStyles type doesn't have toString property (and we don't want to add it)
    return {
      name: name,
      styles: styles,
      map: sourceMap,
      next: cursor,
      toString: function toString() {
        return "You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";
      }
    };
  }

  return {
    name: name,
    styles: styles,
    next: cursor
  };
};




/***/ }),

/***/ "./node_modules/@emotion/sheet/dist/sheet.browser.esm.js":
/*!***************************************************************!*\
  !*** ./node_modules/@emotion/sheet/dist/sheet.browser.esm.js ***!
  \***************************************************************/
/*! exports provided: StyleSheet */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "StyleSheet", function() { return StyleSheet; });
/*

Based off glamor's StyleSheet, thanks Sunil ❤️

high performance StyleSheet for css-in-js systems

- uses multiple style tags behind the scenes for millions of rules
- uses `insertRule` for appending in production for *much* faster performance

// usage

import { StyleSheet } from '@emotion/sheet'

let styleSheet = new StyleSheet({ key: '', container: document.head })

styleSheet.insert('#box { border: 1px solid red; }')
- appends a css rule into the stylesheet

styleSheet.flush()
- empties the stylesheet of all its contents

*/
// $FlowFixMe
function sheetForTag(tag) {
  if (tag.sheet) {
    // $FlowFixMe
    return tag.sheet;
  } // this weirdness brought to you by firefox

  /* istanbul ignore next */


  for (var i = 0; i < document.styleSheets.length; i++) {
    if (document.styleSheets[i].ownerNode === tag) {
      // $FlowFixMe
      return document.styleSheets[i];
    }
  }
}

function createStyleElement(options) {
  var tag = document.createElement('style');
  tag.setAttribute('data-emotion', options.key);

  if (options.nonce !== undefined) {
    tag.setAttribute('nonce', options.nonce);
  }

  tag.appendChild(document.createTextNode(''));
  return tag;
}

var StyleSheet =
/*#__PURE__*/
function () {
  function StyleSheet(options) {
    this.isSpeedy = options.speedy === undefined ? "development" === 'production' : options.speedy;
    this.tags = [];
    this.ctr = 0;
    this.nonce = options.nonce; // key is the value of the data-emotion attribute, it's used to identify different sheets

    this.key = options.key;
    this.container = options.container;
    this.before = null;
  }

  var _proto = StyleSheet.prototype;

  _proto.insert = function insert(rule) {
    // the max length is how many rules we have per style tag, it's 65000 in speedy mode
    // it's 1 in dev because we insert source maps that map a single rule to a location
    // and you can only have one source map per style tag
    if (this.ctr % (this.isSpeedy ? 65000 : 1) === 0) {
      var _tag = createStyleElement(this);

      var before;

      if (this.tags.length === 0) {
        before = this.before;
      } else {
        before = this.tags[this.tags.length - 1].nextSibling;
      }

      this.container.insertBefore(_tag, before);
      this.tags.push(_tag);
    }

    var tag = this.tags[this.tags.length - 1];

    if (this.isSpeedy) {
      var sheet = sheetForTag(tag);

      try {
        // this is a really hot path
        // we check the second character first because having "i"
        // as the second character will happen less often than
        // having "@" as the first character
        var isImportRule = rule.charCodeAt(1) === 105 && rule.charCodeAt(0) === 64; // this is the ultrafast version, works across browsers
        // the big drawback is that the css won't be editable in devtools

        sheet.insertRule(rule, // we need to insert @import rules before anything else
        // otherwise there will be an error
        // technically this means that the @import rules will
        // _usually_(not always since there could be multiple style tags)
        // be the first ones in prod and generally later in dev
        // this shouldn't really matter in the real world though
        // @import is generally only used for font faces from google fonts and etc.
        // so while this could be technically correct then it would be slower and larger
        // for a tiny bit of correctness that won't matter in the real world
        isImportRule ? 0 : sheet.cssRules.length);
      } catch (e) {
        if (true) {
          console.warn("There was a problem inserting the following rule: \"" + rule + "\"", e);
        }
      }
    } else {
      tag.appendChild(document.createTextNode(rule));
    }

    this.ctr++;
  };

  _proto.flush = function flush() {
    // $FlowFixMe
    this.tags.forEach(function (tag) {
      return tag.parentNode.removeChild(tag);
    });
    this.tags = [];
    this.ctr = 0;
  };

  return StyleSheet;
}();




/***/ }),

/***/ "./node_modules/@emotion/stylis/dist/stylis.browser.esm.js":
/*!*****************************************************************!*\
  !*** ./node_modules/@emotion/stylis/dist/stylis.browser.esm.js ***!
  \*****************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
function stylis_min (W) {
  function M(d, c, e, h, a) {
    for (var m = 0, b = 0, v = 0, n = 0, q, g, x = 0, K = 0, k, u = k = q = 0, l = 0, r = 0, I = 0, t = 0, B = e.length, J = B - 1, y, f = '', p = '', F = '', G = '', C; l < B;) {
      g = e.charCodeAt(l);
      l === J && 0 !== b + n + v + m && (0 !== b && (g = 47 === b ? 10 : 47), n = v = m = 0, B++, J++);

      if (0 === b + n + v + m) {
        if (l === J && (0 < r && (f = f.replace(N, '')), 0 < f.trim().length)) {
          switch (g) {
            case 32:
            case 9:
            case 59:
            case 13:
            case 10:
              break;

            default:
              f += e.charAt(l);
          }

          g = 59;
        }

        switch (g) {
          case 123:
            f = f.trim();
            q = f.charCodeAt(0);
            k = 1;

            for (t = ++l; l < B;) {
              switch (g = e.charCodeAt(l)) {
                case 123:
                  k++;
                  break;

                case 125:
                  k--;
                  break;

                case 47:
                  switch (g = e.charCodeAt(l + 1)) {
                    case 42:
                    case 47:
                      a: {
                        for (u = l + 1; u < J; ++u) {
                          switch (e.charCodeAt(u)) {
                            case 47:
                              if (42 === g && 42 === e.charCodeAt(u - 1) && l + 2 !== u) {
                                l = u + 1;
                                break a;
                              }

                              break;

                            case 10:
                              if (47 === g) {
                                l = u + 1;
                                break a;
                              }

                          }
                        }

                        l = u;
                      }

                  }

                  break;

                case 91:
                  g++;

                case 40:
                  g++;

                case 34:
                case 39:
                  for (; l++ < J && e.charCodeAt(l) !== g;) {
                  }

              }

              if (0 === k) break;
              l++;
            }

            k = e.substring(t, l);
            0 === q && (q = (f = f.replace(ca, '').trim()).charCodeAt(0));

            switch (q) {
              case 64:
                0 < r && (f = f.replace(N, ''));
                g = f.charCodeAt(1);

                switch (g) {
                  case 100:
                  case 109:
                  case 115:
                  case 45:
                    r = c;
                    break;

                  default:
                    r = O;
                }

                k = M(c, r, k, g, a + 1);
                t = k.length;
                0 < A && (r = X(O, f, I), C = H(3, k, r, c, D, z, t, g, a, h), f = r.join(''), void 0 !== C && 0 === (t = (k = C.trim()).length) && (g = 0, k = ''));
                if (0 < t) switch (g) {
                  case 115:
                    f = f.replace(da, ea);

                  case 100:
                  case 109:
                  case 45:
                    k = f + '{' + k + '}';
                    break;

                  case 107:
                    f = f.replace(fa, '$1 $2');
                    k = f + '{' + k + '}';
                    k = 1 === w || 2 === w && L('@' + k, 3) ? '@-webkit-' + k + '@' + k : '@' + k;
                    break;

                  default:
                    k = f + k, 112 === h && (k = (p += k, ''));
                } else k = '';
                break;

              default:
                k = M(c, X(c, f, I), k, h, a + 1);
            }

            F += k;
            k = I = r = u = q = 0;
            f = '';
            g = e.charCodeAt(++l);
            break;

          case 125:
          case 59:
            f = (0 < r ? f.replace(N, '') : f).trim();
            if (1 < (t = f.length)) switch (0 === u && (q = f.charCodeAt(0), 45 === q || 96 < q && 123 > q) && (t = (f = f.replace(' ', ':')).length), 0 < A && void 0 !== (C = H(1, f, c, d, D, z, p.length, h, a, h)) && 0 === (t = (f = C.trim()).length) && (f = '\x00\x00'), q = f.charCodeAt(0), g = f.charCodeAt(1), q) {
              case 0:
                break;

              case 64:
                if (105 === g || 99 === g) {
                  G += f + e.charAt(l);
                  break;
                }

              default:
                58 !== f.charCodeAt(t - 1) && (p += P(f, q, g, f.charCodeAt(2)));
            }
            I = r = u = q = 0;
            f = '';
            g = e.charCodeAt(++l);
        }
      }

      switch (g) {
        case 13:
        case 10:
          47 === b ? b = 0 : 0 === 1 + q && 107 !== h && 0 < f.length && (r = 1, f += '\x00');
          0 < A * Y && H(0, f, c, d, D, z, p.length, h, a, h);
          z = 1;
          D++;
          break;

        case 59:
        case 125:
          if (0 === b + n + v + m) {
            z++;
            break;
          }

        default:
          z++;
          y = e.charAt(l);

          switch (g) {
            case 9:
            case 32:
              if (0 === n + m + b) switch (x) {
                case 44:
                case 58:
                case 9:
                case 32:
                  y = '';
                  break;

                default:
                  32 !== g && (y = ' ');
              }
              break;

            case 0:
              y = '\\0';
              break;

            case 12:
              y = '\\f';
              break;

            case 11:
              y = '\\v';
              break;

            case 38:
              0 === n + b + m && (r = I = 1, y = '\f' + y);
              break;

            case 108:
              if (0 === n + b + m + E && 0 < u) switch (l - u) {
                case 2:
                  112 === x && 58 === e.charCodeAt(l - 3) && (E = x);

                case 8:
                  111 === K && (E = K);
              }
              break;

            case 58:
              0 === n + b + m && (u = l);
              break;

            case 44:
              0 === b + v + n + m && (r = 1, y += '\r');
              break;

            case 34:
            case 39:
              0 === b && (n = n === g ? 0 : 0 === n ? g : n);
              break;

            case 91:
              0 === n + b + v && m++;
              break;

            case 93:
              0 === n + b + v && m--;
              break;

            case 41:
              0 === n + b + m && v--;
              break;

            case 40:
              if (0 === n + b + m) {
                if (0 === q) switch (2 * x + 3 * K) {
                  case 533:
                    break;

                  default:
                    q = 1;
                }
                v++;
              }

              break;

            case 64:
              0 === b + v + n + m + u + k && (k = 1);
              break;

            case 42:
            case 47:
              if (!(0 < n + m + v)) switch (b) {
                case 0:
                  switch (2 * g + 3 * e.charCodeAt(l + 1)) {
                    case 235:
                      b = 47;
                      break;

                    case 220:
                      t = l, b = 42;
                  }

                  break;

                case 42:
                  47 === g && 42 === x && t + 2 !== l && (33 === e.charCodeAt(t + 2) && (p += e.substring(t, l + 1)), y = '', b = 0);
              }
          }

          0 === b && (f += y);
      }

      K = x;
      x = g;
      l++;
    }

    t = p.length;

    if (0 < t) {
      r = c;
      if (0 < A && (C = H(2, p, r, d, D, z, t, h, a, h), void 0 !== C && 0 === (p = C).length)) return G + p + F;
      p = r.join(',') + '{' + p + '}';

      if (0 !== w * E) {
        2 !== w || L(p, 2) || (E = 0);

        switch (E) {
          case 111:
            p = p.replace(ha, ':-moz-$1') + p;
            break;

          case 112:
            p = p.replace(Q, '::-webkit-input-$1') + p.replace(Q, '::-moz-$1') + p.replace(Q, ':-ms-input-$1') + p;
        }

        E = 0;
      }
    }

    return G + p + F;
  }

  function X(d, c, e) {
    var h = c.trim().split(ia);
    c = h;
    var a = h.length,
        m = d.length;

    switch (m) {
      case 0:
      case 1:
        var b = 0;

        for (d = 0 === m ? '' : d[0] + ' '; b < a; ++b) {
          c[b] = Z(d, c[b], e).trim();
        }

        break;

      default:
        var v = b = 0;

        for (c = []; b < a; ++b) {
          for (var n = 0; n < m; ++n) {
            c[v++] = Z(d[n] + ' ', h[b], e).trim();
          }
        }

    }

    return c;
  }

  function Z(d, c, e) {
    var h = c.charCodeAt(0);
    33 > h && (h = (c = c.trim()).charCodeAt(0));

    switch (h) {
      case 38:
        return c.replace(F, '$1' + d.trim());

      case 58:
        return d.trim() + c.replace(F, '$1' + d.trim());

      default:
        if (0 < 1 * e && 0 < c.indexOf('\f')) return c.replace(F, (58 === d.charCodeAt(0) ? '' : '$1') + d.trim());
    }

    return d + c;
  }

  function P(d, c, e, h) {
    var a = d + ';',
        m = 2 * c + 3 * e + 4 * h;

    if (944 === m) {
      d = a.indexOf(':', 9) + 1;
      var b = a.substring(d, a.length - 1).trim();
      b = a.substring(0, d).trim() + b + ';';
      return 1 === w || 2 === w && L(b, 1) ? '-webkit-' + b + b : b;
    }

    if (0 === w || 2 === w && !L(a, 1)) return a;

    switch (m) {
      case 1015:
        return 97 === a.charCodeAt(10) ? '-webkit-' + a + a : a;

      case 951:
        return 116 === a.charCodeAt(3) ? '-webkit-' + a + a : a;

      case 963:
        return 110 === a.charCodeAt(5) ? '-webkit-' + a + a : a;

      case 1009:
        if (100 !== a.charCodeAt(4)) break;

      case 969:
      case 942:
        return '-webkit-' + a + a;

      case 978:
        return '-webkit-' + a + '-moz-' + a + a;

      case 1019:
      case 983:
        return '-webkit-' + a + '-moz-' + a + '-ms-' + a + a;

      case 883:
        if (45 === a.charCodeAt(8)) return '-webkit-' + a + a;
        if (0 < a.indexOf('image-set(', 11)) return a.replace(ja, '$1-webkit-$2') + a;
        break;

      case 932:
        if (45 === a.charCodeAt(4)) switch (a.charCodeAt(5)) {
          case 103:
            return '-webkit-box-' + a.replace('-grow', '') + '-webkit-' + a + '-ms-' + a.replace('grow', 'positive') + a;

          case 115:
            return '-webkit-' + a + '-ms-' + a.replace('shrink', 'negative') + a;

          case 98:
            return '-webkit-' + a + '-ms-' + a.replace('basis', 'preferred-size') + a;
        }
        return '-webkit-' + a + '-ms-' + a + a;

      case 964:
        return '-webkit-' + a + '-ms-flex-' + a + a;

      case 1023:
        if (99 !== a.charCodeAt(8)) break;
        b = a.substring(a.indexOf(':', 15)).replace('flex-', '').replace('space-between', 'justify');
        return '-webkit-box-pack' + b + '-webkit-' + a + '-ms-flex-pack' + b + a;

      case 1005:
        return ka.test(a) ? a.replace(aa, ':-webkit-') + a.replace(aa, ':-moz-') + a : a;

      case 1e3:
        b = a.substring(13).trim();
        c = b.indexOf('-') + 1;

        switch (b.charCodeAt(0) + b.charCodeAt(c)) {
          case 226:
            b = a.replace(G, 'tb');
            break;

          case 232:
            b = a.replace(G, 'tb-rl');
            break;

          case 220:
            b = a.replace(G, 'lr');
            break;

          default:
            return a;
        }

        return '-webkit-' + a + '-ms-' + b + a;

      case 1017:
        if (-1 === a.indexOf('sticky', 9)) break;

      case 975:
        c = (a = d).length - 10;
        b = (33 === a.charCodeAt(c) ? a.substring(0, c) : a).substring(d.indexOf(':', 7) + 1).trim();

        switch (m = b.charCodeAt(0) + (b.charCodeAt(7) | 0)) {
          case 203:
            if (111 > b.charCodeAt(8)) break;

          case 115:
            a = a.replace(b, '-webkit-' + b) + ';' + a;
            break;

          case 207:
          case 102:
            a = a.replace(b, '-webkit-' + (102 < m ? 'inline-' : '') + 'box') + ';' + a.replace(b, '-webkit-' + b) + ';' + a.replace(b, '-ms-' + b + 'box') + ';' + a;
        }

        return a + ';';

      case 938:
        if (45 === a.charCodeAt(5)) switch (a.charCodeAt(6)) {
          case 105:
            return b = a.replace('-items', ''), '-webkit-' + a + '-webkit-box-' + b + '-ms-flex-' + b + a;

          case 115:
            return '-webkit-' + a + '-ms-flex-item-' + a.replace(ba, '') + a;

          default:
            return '-webkit-' + a + '-ms-flex-line-pack' + a.replace('align-content', '').replace(ba, '') + a;
        }
        break;

      case 973:
      case 989:
        if (45 !== a.charCodeAt(3) || 122 === a.charCodeAt(4)) break;

      case 931:
      case 953:
        if (!0 === la.test(d)) return 115 === (b = d.substring(d.indexOf(':') + 1)).charCodeAt(0) ? P(d.replace('stretch', 'fill-available'), c, e, h).replace(':fill-available', ':stretch') : a.replace(b, '-webkit-' + b) + a.replace(b, '-moz-' + b.replace('fill-', '')) + a;
        break;

      case 962:
        if (a = '-webkit-' + a + (102 === a.charCodeAt(5) ? '-ms-' + a : '') + a, 211 === e + h && 105 === a.charCodeAt(13) && 0 < a.indexOf('transform', 10)) return a.substring(0, a.indexOf(';', 27) + 1).replace(ma, '$1-webkit-$2') + a;
    }

    return a;
  }

  function L(d, c) {
    var e = d.indexOf(1 === c ? ':' : '{'),
        h = d.substring(0, 3 !== c ? e : 10);
    e = d.substring(e + 1, d.length - 1);
    return R(2 !== c ? h : h.replace(na, '$1'), e, c);
  }

  function ea(d, c) {
    var e = P(c, c.charCodeAt(0), c.charCodeAt(1), c.charCodeAt(2));
    return e !== c + ';' ? e.replace(oa, ' or ($1)').substring(4) : '(' + c + ')';
  }

  function H(d, c, e, h, a, m, b, v, n, q) {
    for (var g = 0, x = c, w; g < A; ++g) {
      switch (w = S[g].call(B, d, x, e, h, a, m, b, v, n, q)) {
        case void 0:
        case !1:
        case !0:
        case null:
          break;

        default:
          x = w;
      }
    }

    if (x !== c) return x;
  }

  function T(d) {
    switch (d) {
      case void 0:
      case null:
        A = S.length = 0;
        break;

      default:
        if ('function' === typeof d) S[A++] = d;else if ('object' === typeof d) for (var c = 0, e = d.length; c < e; ++c) {
          T(d[c]);
        } else Y = !!d | 0;
    }

    return T;
  }

  function U(d) {
    d = d.prefix;
    void 0 !== d && (R = null, d ? 'function' !== typeof d ? w = 1 : (w = 2, R = d) : w = 0);
    return U;
  }

  function B(d, c) {
    var e = d;
    33 > e.charCodeAt(0) && (e = e.trim());
    V = e;
    e = [V];

    if (0 < A) {
      var h = H(-1, c, e, e, D, z, 0, 0, 0, 0);
      void 0 !== h && 'string' === typeof h && (c = h);
    }

    var a = M(O, e, c, 0, 0);
    0 < A && (h = H(-2, a, e, e, D, z, a.length, 0, 0, 0), void 0 !== h && (a = h));
    V = '';
    E = 0;
    z = D = 1;
    return a;
  }

  var ca = /^\0+/g,
      N = /[\0\r\f]/g,
      aa = /: */g,
      ka = /zoo|gra/,
      ma = /([,: ])(transform)/g,
      ia = /,\r+?/g,
      F = /([\t\r\n ])*\f?&/g,
      fa = /@(k\w+)\s*(\S*)\s*/,
      Q = /::(place)/g,
      ha = /:(read-only)/g,
      G = /[svh]\w+-[tblr]{2}/,
      da = /\(\s*(.*)\s*\)/g,
      oa = /([\s\S]*?);/g,
      ba = /-self|flex-/g,
      na = /[^]*?(:[rp][el]a[\w-]+)[^]*/,
      la = /stretch|:\s*\w+\-(?:conte|avail)/,
      ja = /([^-])(image-set\()/,
      z = 1,
      D = 1,
      E = 0,
      w = 1,
      O = [],
      S = [],
      A = 0,
      R = null,
      Y = 0,
      V = '';
  B.use = T;
  B.set = U;
  void 0 !== W && U(W);
  return B;
}

/* harmony default export */ __webpack_exports__["default"] = (stylis_min);


/***/ }),

/***/ "./node_modules/@emotion/unitless/dist/unitless.browser.esm.js":
/*!*********************************************************************!*\
  !*** ./node_modules/@emotion/unitless/dist/unitless.browser.esm.js ***!
  \*********************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
var unitlessKeys = {
  animationIterationCount: 1,
  borderImageOutset: 1,
  borderImageSlice: 1,
  borderImageWidth: 1,
  boxFlex: 1,
  boxFlexGroup: 1,
  boxOrdinalGroup: 1,
  columnCount: 1,
  columns: 1,
  flex: 1,
  flexGrow: 1,
  flexPositive: 1,
  flexShrink: 1,
  flexNegative: 1,
  flexOrder: 1,
  gridRow: 1,
  gridRowEnd: 1,
  gridRowSpan: 1,
  gridRowStart: 1,
  gridColumn: 1,
  gridColumnEnd: 1,
  gridColumnSpan: 1,
  gridColumnStart: 1,
  msGridRow: 1,
  msGridRowSpan: 1,
  msGridColumn: 1,
  msGridColumnSpan: 1,
  fontWeight: 1,
  lineHeight: 1,
  opacity: 1,
  order: 1,
  orphans: 1,
  tabSize: 1,
  widows: 1,
  zIndex: 1,
  zoom: 1,
  WebkitLineClamp: 1,
  // SVG-related properties
  fillOpacity: 1,
  floodOpacity: 1,
  stopOpacity: 1,
  strokeDasharray: 1,
  strokeDashoffset: 1,
  strokeMiterlimit: 1,
  strokeOpacity: 1,
  strokeWidth: 1
};

/* harmony default export */ __webpack_exports__["default"] = (unitlessKeys);


/***/ }),

/***/ "./node_modules/@emotion/utils/dist/utils.browser.esm.js":
/*!***************************************************************!*\
  !*** ./node_modules/@emotion/utils/dist/utils.browser.esm.js ***!
  \***************************************************************/
/*! exports provided: getRegisteredStyles, insertStyles */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getRegisteredStyles", function() { return getRegisteredStyles; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "insertStyles", function() { return insertStyles; });
var isBrowser = "object" !== 'undefined';
function getRegisteredStyles(registered, registeredStyles, classNames) {
  var rawClassName = '';
  classNames.split(' ').forEach(function (className) {
    if (registered[className] !== undefined) {
      registeredStyles.push(registered[className]);
    } else {
      rawClassName += className + " ";
    }
  });
  return rawClassName;
}
var insertStyles = function insertStyles(cache, serialized, isStringTag) {
  var className = cache.key + "-" + serialized.name;

  if ( // we only need to add the styles to the registered cache if the
  // class name could be used further down
  // the tree but if it's a string tag, we know it won't
  // so we don't have to add it to registered cache.
  // this improves memory usage since we can avoid storing the whole style string
  (isStringTag === false || // we need to always store it if we're in compat mode and
  // in node since emotion-server relies on whether a style is in
  // the registered cache to know whether a style is global or not
  // also, note that this check will be dead code eliminated in the browser
  isBrowser === false && cache.compat !== undefined) && cache.registered[className] === undefined) {
    cache.registered[className] = serialized.styles;
  }

  if (cache.inserted[serialized.name] === undefined) {
    var current = serialized;

    do {
      var maybeStyles = cache.insert("." + className, current, cache.sheet, true);

      current = current.next;
    } while (current !== undefined);
  }
};




/***/ }),

/***/ "./node_modules/@emotion/weak-memoize/dist/weak-memoize.browser.esm.js":
/*!*****************************************************************************!*\
  !*** ./node_modules/@emotion/weak-memoize/dist/weak-memoize.browser.esm.js ***!
  \*****************************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
var weakMemoize = function weakMemoize(func) {
  // $FlowFixMe flow doesn't include all non-primitive types as allowed for weakmaps
  var cache = new WeakMap();
  return function (arg) {
    if (cache.has(arg)) {
      // $FlowFixMe
      return cache.get(arg);
    }

    var ret = func(arg);
    cache.set(arg, ret);
    return ret;
  };
};

/* harmony default export */ __webpack_exports__["default"] = (weakMemoize);


/***/ }),

/***/ "./node_modules/@hig/behaviors/build/index.es.js":
/*!*******************************************************!*\
  !*** ./node_modules/@hig/behaviors/build/index.es.js ***!
  \*******************************************************/
/*! exports provided: ControlBehavior, FocusBehavior, HoverBehavior, PressedBehavior */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ControlBehavior", function() { return ControlBehavior; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FocusBehavior", function() { return FocusBehavior; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "HoverBehavior", function() { return HoverBehavior; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PressedBehavior", function() { return PressedBehavior; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! prop-types */ "./node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_1__);



var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var FocusBehavior = function (_Component) {
  _inherits(FocusBehavior, _Component);

  function FocusBehavior(props) {
    _classCallCheck(this, FocusBehavior);

    // Binding in the constructor because performance
    var _this = _possibleConstructorReturn(this, (FocusBehavior.__proto__ || Object.getPrototypeOf(FocusBehavior)).call(this, props));

    _this.handleFocus = _this.handleFocus.bind(_this);
    _this.handleBlur = _this.handleBlur.bind(_this);

    _this.state = {
      hasFocus: false
    };
    return _this;
  }

  _createClass(FocusBehavior, [{
    key: "handleFocus",
    value: function handleFocus(event) {
      if (this.props.onFocus) {
        this.props.onFocus(event);
      }

      if (!event.defaultPrevented) {
        this.setState({ hasFocus: true });
      }
    }
  }, {
    key: "handleBlur",
    value: function handleBlur(event) {
      if (this.props.onBlur) {
        this.props.onBlur(event);
      }

      if (!event.defaultPrevented) {
        this.setState({ hasFocus: false });
      }
    }
  }, {
    key: "render",
    value: function render() {
      return this.props.children({
        hasFocus: this.state.hasFocus,
        onFocus: this.handleFocus,
        onBlur: this.handleBlur
      });
    }
  }]);

  return FocusBehavior;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

FocusBehavior.propTypes = {
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func.isRequired,
  onFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onBlur: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func
};
FocusBehavior.__docgenInfo = {
  "description": "",
  "displayName": "FocusBehavior",
  "props": {
    "children": {
      "type": {
        "name": "func"
      },
      "required": true,
      "description": ""
    },
    "onFocus": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onBlur": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    }
  }
};

var _createClass$1 = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck$1(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn$1(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits$1(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var HoverBehavior = function (_Component) {
  _inherits$1(HoverBehavior, _Component);

  function HoverBehavior(props) {
    _classCallCheck$1(this, HoverBehavior);

    // Binding in the constructor because performance
    var _this = _possibleConstructorReturn$1(this, (HoverBehavior.__proto__ || Object.getPrototypeOf(HoverBehavior)).call(this, props));

    _this.handleFocus = _this.handleFocus.bind(_this);
    _this.handleBlur = _this.handleBlur.bind(_this);

    _this.state = {
      hasHover: false
    };
    return _this;
  }

  _createClass$1(HoverBehavior, [{
    key: "handleFocus",
    value: function handleFocus(event) {
      if (this.props.onMouseEnter) {
        this.props.onMouseEnter(event);
      }

      if (!event.defaultPrevented) {
        this.setState({ hasHover: true });
      }
    }
  }, {
    key: "handleBlur",
    value: function handleBlur(event) {
      if (this.props.onMouseLeave) {
        this.props.onMouseLeave(event);
      }

      if (!event.defaultPrevented) {
        this.setState({ hasHover: false });
      }
    }
  }, {
    key: "render",
    value: function render() {
      return this.props.children({
        hasHover: this.state.hasHover,
        onMouseEnter: this.handleFocus,
        onMouseLeave: this.handleBlur
      });
    }
  }]);

  return HoverBehavior;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

HoverBehavior.propTypes = {
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func.isRequired,
  onMouseEnter: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func
};
HoverBehavior.__docgenInfo = {
  "description": "",
  "displayName": "HoverBehavior",
  "props": {
    "children": {
      "type": {
        "name": "func"
      },
      "required": true,
      "description": ""
    },
    "onMouseEnter": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    }
  }
};

var _createClass$2 = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck$2(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn$2(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits$2(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var PressedBehavior = function (_Component) {
  _inherits$2(PressedBehavior, _Component);

  function PressedBehavior(props) {
    _classCallCheck$2(this, PressedBehavior);

    // Binding in the constructor because performance
    var _this = _possibleConstructorReturn$2(this, (PressedBehavior.__proto__ || Object.getPrototypeOf(PressedBehavior)).call(this, props));

    _this.handleMouseDown = _this.handleMouseDown.bind(_this);
    _this.handleMouseUp = _this.handleMouseUp.bind(_this);
    _this.handleMouseLeave = _this.handleMouseLeave.bind(_this);

    _this.state = {
      isPressed: false
    };
    return _this;
  }

  _createClass$2(PressedBehavior, [{
    key: "handleMouseDown",
    value: function handleMouseDown(event) {
      if (this.props.onMouseDown) {
        this.props.onMouseDown(event);
      }

      if (!event.defaultPrevented) {
        this.setState({ isPressed: true });
      }
    }
  }, {
    key: "handleMouseLeave",
    value: function handleMouseLeave(event) {
      if (this.props.onMouseLeave) {
        this.props.onMouseLeave(event);
      }

      if (!event.defaultPrevented) {
        this.setState({ isPressed: false });
      }
    }
  }, {
    key: "handleMouseUp",
    value: function handleMouseUp(event) {
      if (this.props.onMouseUp) {
        this.props.onMouseUp(event);
      }

      if (!event.defaultPrevented) {
        this.setState({ isPressed: false });
      }
    }
  }, {
    key: "render",
    value: function render() {
      return this.props.children({
        isPressed: this.state.isPressed,
        onMouseDown: this.handleMouseDown,
        onMouseUp: this.handleMouseUp,
        onPressedMouseLeave: this.handleMouseLeave
      });
    }
  }]);

  return PressedBehavior;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

PressedBehavior.propTypes = {
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func.isRequired,
  onMouseDown: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseUp: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func
};
PressedBehavior.__docgenInfo = {
  "description": "",
  "displayName": "PressedBehavior",
  "props": {
    "children": {
      "type": {
        "name": "func"
      },
      "required": true,
      "description": ""
    },
    "onMouseDown": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseUp": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    }
  }
};

var _createClass$3 = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck$3(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn$3(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits$3(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ControlBehavior = function (_Component) {
  _inherits$3(ControlBehavior, _Component);

  function ControlBehavior() {
    _classCallCheck$3(this, ControlBehavior);

    return _possibleConstructorReturn$3(this, (ControlBehavior.__proto__ || Object.getPrototypeOf(ControlBehavior)).apply(this, arguments));
  }

  _createClass$3(ControlBehavior, [{
    key: "render",
    value: function render() {
      var _this2 = this;

      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
        HoverBehavior,
        {
          onMouseEnter: this.props.onMouseEnter,
          onMouseLeave: this.props.onMouseLeave
        },
        function (_ref) {
          var hasHover = _ref.hasHover,
              onMouseEnter = _ref.onMouseEnter,
              onMouseLeave = _ref.onMouseLeave;
          return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
            FocusBehavior,
            {
              onFocus: _this2.props.onFocus,
              onBlur: _this2.props.onBlur
            },
            function (_ref2) {
              var hasFocus = _ref2.hasFocus,
                  onFocus = _ref2.onFocus,
                  onBlur = _ref2.onBlur;
              return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
                PressedBehavior,
                {
                  onMouseDown: _this2.props.onMouseDown,
                  onMouseUp: _this2.props.onMouseUp,
                  onMouseLeave: onMouseLeave
                },
                function (_ref3) {
                  var isPressed = _ref3.isPressed,
                      onMouseDown = _ref3.onMouseDown,
                      onMouseUp = _ref3.onMouseUp,
                      onPressedMouseLeave = _ref3.onPressedMouseLeave;
                  return _this2.props.children({
                    hasHover: hasHover,
                    hasFocus: hasFocus,
                    isPressed: isPressed,
                    onFocus: onFocus,
                    onBlur: onBlur,
                    onMouseDown: onMouseDown,
                    onMouseEnter: onMouseEnter,
                    onMouseLeave: onPressedMouseLeave,
                    onMouseUp: onMouseUp
                  });
                }
              );
            }
          );
        }
      );
    }
  }]);

  return ControlBehavior;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

ControlBehavior.propTypes = {
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func.isRequired,
  onFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onBlur: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseDown: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseEnter: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseUp: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func
};
ControlBehavior.__docgenInfo = {
  "description": "",
  "displayName": "ControlBehavior",
  "props": {
    "children": {
      "type": {
        "name": "func"
      },
      "required": true,
      "description": ""
    },
    "onFocus": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onBlur": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseDown": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseEnter": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseUp": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    }
  }
};




/***/ }),

/***/ "./node_modules/@hig/button/build/index.es.js":
/*!****************************************************!*\
  !*** ./node_modules/@hig/button/build/index.es.js ***!
  \****************************************************/
/*! exports provided: default, targets, types, widths, availableTargets, availableTypes, availableWidths */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "targets", function() { return targets; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "types", function() { return types; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "widths", function() { return widths; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "availableTargets", function() { return availableTargets; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "availableTypes", function() { return availableTypes; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "availableWidths", function() { return availableWidths; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! prop-types */ "./node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var emotion__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! emotion */ "./node_modules/emotion/dist/emotion.esm.js");
/* harmony import */ var _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @hig/theme-context */ "./node_modules/@hig/theme-context/build/index.es.js");
/* harmony import */ var _hig_utils__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @hig/utils */ "./node_modules/@hig/utils/build/index.es.js");
/* harmony import */ var _hig_behaviors__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! @hig/behaviors */ "./node_modules/@hig/behaviors/build/index.es.js");







var targets = Object.freeze({
  SELF: "_self",
  BLANK: "_blank",
  PARENT: "_parent",
  TOP: "_top"
});

var types = Object.freeze({
  FLAT: "flat",
  OUTLINE: "outline",
  SOLID: "solid",
  /** @deprecated */
  PRIMARY: "primary",
  /** @deprecated */
  SECONDARY: "secondary"
});

var widths = Object.freeze({
  SHRINK: "shrink",
  GROW: "grow"
});

var availableTargets = Object.freeze(Object.values(targets));
var availableTypes = Object.freeze(Object.values(types));
var availableWidths = Object.freeze(Object.values(widths));

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function getButtonDefaultButtonRules(themeData) {
  return {
    borderWidth: themeData["button.borderWidth"],
    borderStyle: "solid",
    borderColor: "transparent",
    borderRadius: themeData["button.borderRadius"],
    boxSizing: "border-box",
    color: themeData["colorScheme.textColor"],
    cursor: "pointer",
    display: "inline-block",
    fontFamily: themeData["button.fontFamily"],
    fontSize: themeData["button.fontSize"],
    fontWeight: themeData["button.fontWeight"],
    lineHeight: themeData["button.lineHeight"],
    margin: "0",
    overflow: "hidden",
    padding: themeData["button.verticalPadding"] + "\n      " + themeData["button.horizontalPadding"],
    position: "relative",
    textAlign: "center",
    textDecoration: "none",
    textOverflow: "ellipsis",
    transitionDuration: "0.3s",
    transitionProperty: "box-shadow",
    transitionTimingFunction: "cubic-bezier(0.4, 0, 0.2, 1)",
    userSelect: "none",
    whiteSpace: "nowrap",
    MozOsxFontSmoothing: "grayscale",
    WebkitFontSmoothing: "antialiased"
  };
}

function getButtonRulesByType(type, themeData) {
  switch (type) {
    case types.SOLID:
    case types.PRIMARY:
      {
        return {
          background: themeData["button.solid.backgroundColor"],
          color: themeData["button.solid.textColor"],
          "&:visited": {
            color: themeData["button.solid.textColor"]
          },
          "svg *": {
            fill: themeData["button.solid.icon.color"]
          }
        };
      }
    case types.OUTLINE:
    case types.SECONDARY:
      {
        return {
          background: "none",
          borderColor: themeData["button.outline.borderColor"],
          color: themeData["button.outline.textColor"],
          "&:visited": {
            color: themeData["button.outline.textColor"]
          },
          "svg *": {
            fill: themeData["button.outline.icon.color"]
          }
        };
      }
    case types.FLAT:
      {
        return {
          background: "none",
          borderColor: themeData["button.flat.borderColor"],
          color: themeData["button.flat.textColor"],
          "&:visited": {
            color: themeData["button.flat.textColor"]
          },
          "svg *": {
            fill: themeData["button.flat.icon.color"]
          }
        };
      }
    default:
      return {};
  }
}

function getButtonRulesByDisabled(type, themeData) {
  return {
    opacity: themeData["component.disabled.opacity"],
    cursor: "default",
    pointerEvents: "none"
  };
}

function getButtonRulesByWidth(width) {
  return width === widths.GROW ? { width: "100%" } : {};
}

function getButtonHoverRulesByType(type, themeData) {
  switch (type) {
    case types.SOLID:
    case types.PRIMARY:
      return {
        background: themeData["button.solid.hover.backgroundColor"],
        boxShadow: "0 0 0 " + themeData["button.halo.width"] + " " + themeData["button.solid.hover.halo.color"]
      };
    case types.OUTLINE:
    case types.SECONDARY:
      return {
        borderColor: themeData["button.outline.hover.borderColor"],
        boxShadow: "0 0 0 " + themeData["button.halo.width"] + " " + themeData["button.outline.hover.halo.color"]
      };
    case types.FLAT:
      return {
        borderColor: themeData["button.flat.hover.borderColor"],
        boxShadow: "0 0 0 " + themeData["button.halo.width"] + " " + themeData["button.flat.hover.halo.color"]
      };
    default:
      return {};
  }
}

function getButtonFocusRulesByType(type, themeData) {
  switch (type) {
    case types.SOLID:
    case types.PRIMARY:
      return {
        background: themeData["button.solid.focus.backgroundColor"],
        boxShadow: "0 0 0 " + themeData["button.halo.width"] + " " + themeData["button.solid.focus.halo.color"],
        outline: "none"
      };
    case types.OUTLINE:
    case types.SECONDARY:
      return {
        background: themeData["button.outline.focus.backgroundColor"],
        borderColor: themeData["button.outline.focus.borderColor"],
        boxShadow: "0 0 0 " + themeData["button.halo.width"] + " " + themeData["button.outline.focus.halo.color"],
        color: themeData["button.outline.focus.textColor"],
        outline: "none",
        "svg *": {
          fill: themeData["button.outline.focus.icon.color"]
        }
      };
    case types.FLAT:
      return {
        background: themeData["button.flat.focus.backgroundColor"],
        borderColor: themeData["button.flat.focus.borderColor"],
        boxShadow: "0 0 0 " + themeData["button.halo.width"] + " " + themeData["button.flat.focus.halo.color"],
        color: themeData["button.flat.focus.textColor"],
        outline: "none",
        "svg *": {
          fill: themeData["button.flat.focus.icon.color"]
        }
      };
    default:
      return {};
  }
}

function getButtonPressedRulesByType(type, themeData) {
  switch (type) {
    case types.SOLID:
    case types.PRIMARY:
      return {
        boxShadow: "0 0 0 " + themeData["button.solid.pressed.haloWidth"] + " " + themeData["button.solid.pressed.halo.color"]
      };
    case types.OUTLINE:
    case types.SECONDARY:
      return {
        borderColor: themeData["button.outline.pressed.borderColor"],
        boxShadow: "0 0 0 " + themeData["button.outline.pressed.haloWidth"] + " " + themeData["button.outline.pressed.halo.color"]
      };
    case types.FLAT:
      return {
        boxShadow: "0 0 0 " + themeData["button.flat.pressed.haloWidth"] + " " + themeData["button.flat.pressed.halo.color"]
      };
    default:
      return {};
  }
}

function stylesheet(props, themeData, theme) {
  var disabled = props.disabled,
      hasFocus = props.hasFocus,
      hasHover = props.hasHover,
      isPressed = props.isPressed,
      type = props.type,
      width = props.width;

  var iconSize = theme.densityId === "medium-density" ? "24px" : "16px";

  return {
    button: _extends({}, getButtonDefaultButtonRules(themeData), getButtonRulesByType(type, themeData), disabled ? getButtonRulesByDisabled(type, themeData) : _extends({}, hasFocus ? getButtonFocusRulesByType(type, themeData) : {}, hasHover ? getButtonHoverRulesByType(type, themeData) : {}, isPressed ? getButtonPressedRulesByType(type, themeData) : {}), getButtonRulesByWidth(width)),
    icon: {
      display: "inline-block",
      position: "absolute",
      height: iconSize,
      marginRight: themeData["button.gutter"],
      top: "50%",
      transform: "translateY(-50%)"
    },
    iconText: {
      marginLeft: "calc(" + iconSize + " + " + themeData["button.gutter"] + ")"
    }
  };
}

var _extends$1 = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ButtonPresenter = function (_Component) {
  _inherits(ButtonPresenter, _Component);

  function ButtonPresenter() {
    _classCallCheck(this, ButtonPresenter);

    return _possibleConstructorReturn(this, (ButtonPresenter.__proto__ || Object.getPrototypeOf(ButtonPresenter)).apply(this, arguments));
  }

  _createClass(ButtonPresenter, [{
    key: "render",
    value: function render() {
      var _this2 = this;

      var _props = this.props,
          disabled = _props.disabled,
          hasFocus = _props.hasFocus,
          hasHover = _props.hasHover,
          isPressed = _props.isPressed,
          icon = _props.icon,
          link = _props.link,
          onBlur = _props.onBlur,
          onClick = _props.onClick,
          onFocus = _props.onFocus,
          onHover = _props.onHover,
          onMouseDown = _props.onMouseDown,
          onMouseEnter = _props.onMouseEnter,
          onMouseLeave = _props.onMouseLeave,
          onMouseUp = _props.onMouseUp,
          customStylesheet = _props.stylesheet,
          target = _props.target,
          title = _props.title,
          type = _props.type,
          width = _props.width,
          otherProps = _objectWithoutProperties(_props, ["disabled", "hasFocus", "hasHover", "isPressed", "icon", "link", "onBlur", "onClick", "onFocus", "onHover", "onMouseDown", "onMouseEnter", "onMouseLeave", "onMouseUp", "stylesheet", "target", "title", "type", "width"]);

      var className = otherProps.className;


      var href = link || undefined;
      var tabIndex = disabled ? "-1" : "0";
      var Wrapper = link ? "a" : "button";
      var wrapperTarget = link ? target : undefined;
      var iconClassName = Object(_hig_utils__WEBPACK_IMPORTED_MODULE_4__["createCustomClassNames"])(className, "icon");
      var iconTextClassName = Object(_hig_utils__WEBPACK_IMPORTED_MODULE_4__["createCustomClassNames"])(className, "icon-text");

      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
        _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__["ThemeContext"].Consumer,
        null,
        function (_ref) {
          var resolvedRoles = _ref.resolvedRoles,
              metadata = _ref.metadata;

          var styles = stylesheet({ disabled: disabled, hasFocus: hasFocus, hasHover: hasHover, isPressed: isPressed, type: type, width: width }, resolvedRoles, metadata);
          var cssStyles = customStylesheet ? customStylesheet(styles, _this2.props, resolvedRoles, metadata) : styles;

          return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
            Wrapper,
            _extends$1({}, otherProps, {
              className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(cssStyles.button), className),
              href: href,
              tabIndex: tabIndex,
              target: wrapperTarget,
              onBlur: onBlur,
              onClick: onClick,
              onFocus: onFocus,
              onMouseDown: onMouseDown,
              onMouseEnter: onMouseEnter,
              onMouseLeave: onMouseLeave,
              onMouseOver: onHover,
              onMouseUp: onMouseUp,
              disabled: disabled
            }),
            icon && react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
              "span",
              { className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(cssStyles.icon), iconClassName) },
              icon
            ),
            react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
              "span",
              {
                className: icon ? Object(emotion__WEBPACK_IMPORTED_MODULE_2__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(cssStyles.iconText), iconTextClassName) : ""
              },
              title
            )
          );
        }
      );
    }
  }]);

  return ButtonPresenter;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

ButtonPresenter.propTypes = {
  disabled: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  hasFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  hasHover: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  isPressed: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  icon: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.node,
  link: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string,
  onBlur: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onClick: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onHover: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseDown: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseEnter: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseUp: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  stylesheet: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  target: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableTargets),
  title: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string.isRequired,
  type: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableTypes),
  width: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableWidths)
};
ButtonPresenter.__docgenInfo = {
  "description": "",
  "displayName": "ButtonPresenter",
  "props": {
    "disabled": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "hasFocus": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "hasHover": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "isPressed": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "icon": {
      "type": {
        "name": "node"
      },
      "required": false,
      "description": ""
    },
    "link": {
      "type": {
        "name": "string"
      },
      "required": false,
      "description": ""
    },
    "onBlur": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onClick": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onFocus": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onHover": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseDown": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseEnter": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseUp": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "stylesheet": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "target": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableTargets"
      },
      "required": false,
      "description": ""
    },
    "title": {
      "type": {
        "name": "string"
      },
      "required": true,
      "description": ""
    },
    "type": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableTypes"
      },
      "required": false,
      "description": ""
    },
    "width": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableWidths"
      },
      "required": false,
      "description": ""
    }
  }
};

var _extends$2 = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass$1 = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _objectWithoutProperties$1(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function _classCallCheck$1(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn$1(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits$1(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Button = function (_Component) {
  _inherits$1(Button, _Component);

  function Button() {
    _classCallCheck$1(this, Button);

    return _possibleConstructorReturn$1(this, (Button.__proto__ || Object.getPrototypeOf(Button)).apply(this, arguments));
  }

  _createClass$1(Button, [{
    key: "render",
    value: function render() {
      var _props = this.props,
          disabled = _props.disabled,
          icon = _props.icon,
          link = _props.link,
          onBlur = _props.onBlur,
          onClick = _props.onClick,
          onFocus = _props.onFocus,
          onHover = _props.onHover,
          onMouseDown = _props.onMouseDown,
          onMouseEnter = _props.onMouseEnter,
          onMouseLeave = _props.onMouseLeave,
          onMouseUp = _props.onMouseUp,
          stylesheet = _props.stylesheet,
          target = _props.target,
          title = _props.title,
          type = _props.type,
          width = _props.width,
          otherProps = _objectWithoutProperties$1(_props, ["disabled", "icon", "link", "onBlur", "onClick", "onFocus", "onHover", "onMouseDown", "onMouseEnter", "onMouseLeave", "onMouseUp", "stylesheet", "target", "title", "type", "width"]);

      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
        _hig_behaviors__WEBPACK_IMPORTED_MODULE_5__["ControlBehavior"],
        {
          onBlur: onBlur,
          onFocus: onFocus,
          onMouseDown: onMouseDown,
          onMouseEnter: onMouseEnter,
          onMouseLeave: onMouseLeave,
          onMouseUp: onMouseUp
        },
        function (_ref) {
          var hasFocus = _ref.hasFocus,
              hasHover = _ref.hasHover,
              isPressed = _ref.isPressed,
              handleBlur = _ref.onBlur,
              handleFocus = _ref.onFocus,
              handleMouseDown = _ref.onMouseDown,
              handleMouseEnter = _ref.onMouseEnter,
              handleMouseLeave = _ref.onMouseLeave,
              handleMouseUp = _ref.onMouseUp;
          return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ButtonPresenter, _extends$2({
            disabled: disabled,
            hasFocus: hasFocus,
            hasHover: hasHover,
            isPressed: isPressed,
            icon: icon,
            link: link,
            onBlur: handleBlur,
            onClick: onClick,
            onFocus: handleFocus,
            onHover: onHover,
            onMouseDown: handleMouseDown,
            onMouseEnter: handleMouseEnter,
            onMouseLeave: handleMouseLeave,
            onMouseUp: handleMouseUp,
            stylesheet: stylesheet,
            target: target,
            title: title,
            type: type,
            width: width
          }, otherProps));
        }
      );
    }
  }]);

  return Button;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

Button.propTypes = {
  /**
   * Prevents user interaction with the button
   */
  disabled: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * A @hig/icon element
   */
  icon: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.node,
  /**
   * Sets the link of a button
   */
  link: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string,
  /**
   * Triggers blur when focus is moved away from icon
   */
  onBlur: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when you click the button
   */
  onClick: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when focus is moved to button
   */
  onFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when you hover over the button
   */
  onHover: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is pressed over the button
   */
  onMouseDown: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is over the button
   */
  onMouseEnter: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is no longer over the button
   */
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is no longer pressed over the button
   */
  onMouseUp: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Adds custom/overriding styles
   */
  stylesheet: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Specifies where to display the linked URL
   */
  target: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableTargets),
  /**
   * Sets the title of a button
   */
  title: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string.isRequired,
  /**
   * Specifies type of button
   */
  type: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableTypes),
  /**
   * Specifies width of button
   */
  width: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableWidths)
};
Button.defaultProps = {
  disabled: false,
  type: types.SOLID,
  width: widths.SHRINK
};
Button.__docgenInfo = {
  "description": "",
  "displayName": "Button",
  "props": {
    "disabled": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Prevents user interaction with the button",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    },
    "icon": {
      "type": {
        "name": "node"
      },
      "required": false,
      "description": "A @hig/icon element"
    },
    "link": {
      "type": {
        "name": "string"
      },
      "required": false,
      "description": "Sets the link of a button"
    },
    "onBlur": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers blur when focus is moved away from icon"
    },
    "onClick": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when you click the button"
    },
    "onFocus": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when focus is moved to button"
    },
    "onHover": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when you hover over the button"
    },
    "onMouseDown": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is pressed over the button"
    },
    "onMouseEnter": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is over the button"
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is no longer over the button"
    },
    "onMouseUp": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is no longer pressed over the button"
    },
    "stylesheet": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Adds custom/overriding styles"
    },
    "target": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableTargets"
      },
      "required": false,
      "description": "Specifies where to display the linked URL"
    },
    "title": {
      "type": {
        "name": "string"
      },
      "required": true,
      "description": "Sets the title of a button"
    },
    "type": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableTypes"
      },
      "required": false,
      "description": "Specifies type of button",
      "defaultValue": {
        "value": "types.SOLID",
        "computed": true
      }
    },
    "width": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableWidths"
      },
      "required": false,
      "description": "Specifies width of button",
      "defaultValue": {
        "value": "widths.SHRINK",
        "computed": true
      }
    }
  }
};

/* harmony default export */ __webpack_exports__["default"] = (Button);



/***/ }),

/***/ "./node_modules/@hig/checkbox/build/index.es.js":
/*!******************************************************!*\
  !*** ./node_modules/@hig/checkbox/build/index.es.js ***!
  \******************************************************/
/*! exports provided: default, CheckboxBehavior */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CheckboxBehavior", function() { return CheckboxBehavior; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! prop-types */ "./node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var emotion__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! emotion */ "./node_modules/emotion/dist/emotion.esm.js");
/* harmony import */ var _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @hig/theme-context */ "./node_modules/@hig/theme-context/build/index.es.js");
/* harmony import */ var _hig_icons__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @hig/icons */ "./node_modules/@hig/icons/build/index.es.js");
/* harmony import */ var _hig_behaviors__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! @hig/behaviors */ "./node_modules/@hig/behaviors/build/index.es.js");







var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function getCheckboxRulesByFocus(themeData) {
  return {
    borderColor: themeData["checkbox.focus.borderColor"],
    boxShadow: "0 0 0 " + themeData["checkbox.focus.halo.width"] + "\n      " + themeData["checkbox.focus.halo.color"]
  };
}

function getCheckboxRulesByHover(themeData) {
  return {
    borderColor: themeData["checkbox.borderColor"],
    boxShadow: "0 0 0 " + themeData["checkbox.hover.halo.width"] + " " + themeData["checkbox.hover.halo.color"]
  };
}

function getCheckboxRulesByPressed(themeData) {
  return {
    borderColor: themeData["checkbox.borderColor"],
    boxShadow: "0 0 0 " + themeData["checkbox.pressed.halo.width"] + " " + themeData["checkbox.pressed.halo.color"]
  };
}

function getCheckboxRulesByDisabled(themeData) {
  return {
    opacity: themeData["component.disabled.opacity"],
    boxShadow: "none",
    pointerEvents: "none"
  };
}

function getCheckboxRulesByChecked(themeData) {
  return {
    border: "none",
    backgroundColor: themeData["checkbox.checked.backgroundColor"]
  };
}

function getCheckboxRulesByIndeterminate(themeData) {
  return {
    border: "none",
    backgroundColor: themeData["checkbox.indeterminate.backgroundColor"]
  };
}

function getCheckboxRulesByLegacyTheme(themeData, props) {
  var checked = props.checked,
      disabled = props.disabled,
      hasFocus = props.hasFocus,
      indeterminate = props.indeterminate;


  return _extends({}, !checked && !hasFocus && !indeterminate ? {
    backgroundColor: themeData["checkbox.backgroundColor"]
  } : {}, hasFocus ? {
    backgroundColor: themeData["checkbox.backgroundColor"]
  } : {}, checked ? {
    backgroundColor: themeData["checkbox.checked.backgroundColor"],
    borderColor: themeData["checkbox.checked.borderColor"]
  } : {}, checked && hasFocus ? {
    backgroundColor: themeData["checkbox.focus.checked.backgroundColor"],
    borderColor: themeData["checkbox.focus.checked.borderColor"]
  } : {}, indeterminate ? {
    backgroundColor: themeData["checkbox.indeterminate.backgroundColor"],
    borderColor: themeData["checkbox.indeterminate.borderColor"]
  } : {}, indeterminate && hasFocus ? {
    borderColor: themeData["checkbox.focus.indeterminate.borderColor"]
  } : {}, disabled ? {
    borderStyle: "dotted",
    opacity: themeData["component.disabled.opacity"]
  } : {}, disabled && indeterminate ? {
    opacity: themeData["component.disabled.opacity"]
  } : {}, disabled && checked && !indeterminate ? {
    backgroundColor: themeData["checkbox.backgroundColor"],
    opacity: themeData["component.disabled.opacity"]
  } : {});
}

function stylesheet(props, themeData, theme) {
  var checked = props.checked,
      disabled = props.disabled,
      hasFocus = props.hasFocus,
      hasHover = props.hasHover,
      indeterminate = props.indeterminate,
      isPressed = props.isPressed;


  return {
    checkboxWrapper: {
      boxSizing: "border-box",
      display: "inline-block",
      position: "relative",
      height: themeData["checkbox.height"],
      width: themeData["checkbox.width"]
    },
    checkboxCheckWrapper: _extends({
      position: "relative",
      display: "block",
      boxSizing: "border-box",
      color: "transparent",
      border: themeData["checkbox.borderWidth"] + " solid\n        " + themeData["checkbox.borderColor"],
      backgroundColor: "transparent",
      textAlign: "center",
      fontSize: "25px",
      lineHeight: "14px",
      height: themeData["checkbox.height"],
      width: themeData["checkbox.width"],
      borderRadius: themeData["checkbox.borderRadius"],
      cursor: hasHover ? "pointer" : "default",
      transitionDuration: "0.3s, 0.3s",
      transitionProperty: "box-shadow, border",
      transitionTimingFunction: "cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1)",
      // eslint-disable-next-line prettier/prettier
      "svg": {
        position: "absolute",
        left: "50%",
        top: "50%",
        transform: "translateX(-50%) translateY(-50%)",
        pointerEvents: "none",
        "*": {
          fill: themeData["checkbox.iconColor"]
        }
      }
    }, hasFocus ? getCheckboxRulesByFocus(themeData) : {}, hasHover ? getCheckboxRulesByHover(themeData) : {}, isPressed ? getCheckboxRulesByPressed(themeData) : {}, disabled ? getCheckboxRulesByDisabled(themeData) : {}, checked ? getCheckboxRulesByChecked(themeData) : {}, indeterminate ? getCheckboxRulesByIndeterminate(themeData) : {}, theme === "hig-light" ? getCheckboxRulesByLegacyTheme(themeData, props) : {}),
    checkboxInput: {
      opacity: 0,
      position: "absolute",
      width: "100%",
      height: "100%",
      top: 0,
      left: 0,
      zIndex: 1,
      padding: 0,
      margin: 0
    },
    checkboxCheck: {
      display: checked && !indeterminate ? "block" : "none"
    },
    checkboxIndeterminate: {
      display: indeterminate ? "block" : "none"
    }
  };
}

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var CheckPresenter = function (_Component) {
  _inherits(CheckPresenter, _Component);

  function CheckPresenter() {
    _classCallCheck(this, CheckPresenter);

    return _possibleConstructorReturn(this, (CheckPresenter.__proto__ || Object.getPrototypeOf(CheckPresenter)).apply(this, arguments));
  }

  _createClass(CheckPresenter, [{
    key: "render",
    value: function render() {
      var _props = this.props,
          checked = _props.checked,
          disabled = _props.disabled,
          hasFocus = _props.hasFocus,
          hasHover = _props.hasHover,
          indeterminate = _props.indeterminate,
          isPressed = _props.isPressed;


      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
        _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__["ThemeContext"].Consumer,
        null,
        function (_ref) {
          var resolvedRoles = _ref.resolvedRoles;

          var styles = stylesheet({ checked: checked, disabled: disabled, hasFocus: hasFocus, hasHover: hasHover, indeterminate: indeterminate, isPressed: isPressed }, resolvedRoles);

          return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
            "span",
            { className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(styles.checkboxCheckWrapper) },
            react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
              "span",
              { className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(styles.checkboxCheck) },
              react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(_hig_icons__WEBPACK_IMPORTED_MODULE_4__["CheckmarkXsUI"], null)
            ),
            react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
              "span",
              { className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(styles.checkboxIndeterminate) },
              react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(_hig_icons__WEBPACK_IMPORTED_MODULE_4__["OperatorMinusXsUI"], null)
            )
          );
        }
      );
    }
  }]);

  return CheckPresenter;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

CheckPresenter.propTypes = {
  /**
   * Checks the checkbox
   */
  checked: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Prevents user actions on the checkbox
   */
  disabled: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Returns whether or not the checkbox is currently focused
   */
  hasFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Returns whether or not the checkbox is currently hovered
   */
  hasHover: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Returns whether or not the checkbox is currently pressed
   */
  isPressed: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Sets indeterminate state for checkbox
   */
  indeterminate: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool
};
CheckPresenter.defaultProps = {
  checked: false,
  disabled: false,
  indeterminate: false
};
CheckPresenter.__docgenInfo = {
  "description": "",
  "displayName": "CheckPresenter",
  "props": {
    "checked": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Checks the checkbox",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    },
    "disabled": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Prevents user actions on the checkbox",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    },
    "hasFocus": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Returns whether or not the checkbox is currently focused"
    },
    "hasHover": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Returns whether or not the checkbox is currently hovered"
    },
    "isPressed": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Returns whether or not the checkbox is currently pressed"
    },
    "indeterminate": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Sets indeterminate state for checkbox",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    }
  }
};

var _extends$1 = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass$1 = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function _classCallCheck$1(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn$1(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits$1(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var CheckboxPresenter = function (_Component) {
  _inherits$1(CheckboxPresenter, _Component);

  function CheckboxPresenter() {
    var _ref;

    var _temp, _this, _ret;

    _classCallCheck$1(this, CheckboxPresenter);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn$1(this, (_ref = CheckboxPresenter.__proto__ || Object.getPrototypeOf(CheckboxPresenter)).call.apply(_ref, [this].concat(args))), _this), _this.setIndeterminate = function (input) {
      if (input) {
        // Workaround for https://github.com/facebook/react/issues/1798
        // eslint-disable-next-line no-param-reassign
        input.indeterminate = _this.props.indeterminate;
      }
    }, _temp), _possibleConstructorReturn$1(_this, _ret);
  }

  _createClass$1(CheckboxPresenter, [{
    key: "render",
    value: function render() {
      var _this2 = this;

      var _props = this.props,
          checked = _props.checked,
          disabled = _props.disabled,
          hasFocus = _props.hasFocus,
          hasHover = _props.hasHover,
          indeterminate = _props.indeterminate,
          isPressed = _props.isPressed,
          onBlur = _props.onBlur,
          onChange = _props.onChange,
          onClick = _props.onClick,
          onMouseDown = _props.onMouseDown,
          onMouseEnter = _props.onMouseEnter,
          onMouseLeave = _props.onMouseLeave,
          onMouseUp = _props.onMouseUp,
          onFocus = _props.onFocus,
          otherProps = _objectWithoutProperties(_props, ["checked", "disabled", "hasFocus", "hasHover", "indeterminate", "isPressed", "onBlur", "onChange", "onClick", "onMouseDown", "onMouseEnter", "onMouseLeave", "onMouseUp", "onFocus"]);

      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
        _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__["ThemeContext"].Consumer,
        null,
        function (_ref2) {
          var resolvedRoles = _ref2.resolvedRoles;

          var styles = stylesheet({
            checked: checked,
            disabled: disabled,
            hasFocus: hasFocus,
            hasHover: hasHover,
            indeterminate: indeterminate,
            isPressed: isPressed
          }, resolvedRoles);

          return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
            "div",
            { className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(styles.checkboxWrapper) },
            react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("input", _extends$1({
              checked: checked,
              className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(styles.checkboxInput),
              disabled: disabled,
              onBlur: onBlur,
              onChange: onChange,
              onClick: onClick,
              onFocus: onFocus,
              onMouseDown: onMouseDown,
              onMouseEnter: onMouseEnter,
              onMouseLeave: onMouseLeave,
              onMouseUp: onMouseUp,
              ref: _this2.setIndeterminate,
              type: "checkbox"
            }, otherProps)),
            react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CheckPresenter, {
              checked: checked,
              disabled: disabled,
              hasFocus: hasFocus,
              hasHover: hasHover,
              indeterminate: indeterminate,
              isPressed: isPressed
            })
          );
        }
      );
    }
  }]);

  return CheckboxPresenter;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

CheckboxPresenter.propTypes = {
  /**
   * Checks the checkbox
   */
  checked: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Prevents user actions on the checkbox
   */
  disabled: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Returns whether or not the checkbox is currently focused
   */
  hasFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Returns whether or not the checkbox is currently hovered
   */
  hasHover: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Sets indeterminate state for checkbox
   */
  indeterminate: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Returns whether or not the checkbox is currently pressed
   */
  isPressed: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * The name of the checkbox as submitted with a form
   */
  name: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string,
  /**
   * Called when user moves focus from the field
   */
  onBlur: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Called when the checkbox is changed
   */
  onChange: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Called when user clicks on the checkbox
   */
  onClick: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Called when user puts focus on the field
   */
  onFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is pressed over the checkbox
   */
  onMouseDown: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is over the checkbox
   */
  onMouseEnter: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is no longer over the checkbox
   */
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is no longer pressed over the checkbox
   */
  onMouseUp: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Value submitted with a form if checked
   */
  value: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string
};
CheckboxPresenter.defaultProps = {
  checked: false,
  disabled: false,
  indeterminate: false,
  name: "checkbox",
  value: "value"
};
CheckboxPresenter.__docgenInfo = {
  "description": "",
  "displayName": "CheckboxPresenter",
  "props": {
    "checked": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Checks the checkbox",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    },
    "disabled": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Prevents user actions on the checkbox",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    },
    "hasFocus": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Returns whether or not the checkbox is currently focused"
    },
    "hasHover": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Returns whether or not the checkbox is currently hovered"
    },
    "indeterminate": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Sets indeterminate state for checkbox",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    },
    "isPressed": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Returns whether or not the checkbox is currently pressed"
    },
    "name": {
      "type": {
        "name": "string"
      },
      "required": false,
      "description": "The name of the checkbox as submitted with a form",
      "defaultValue": {
        "value": "\"checkbox\"",
        "computed": false
      }
    },
    "onBlur": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user moves focus from the field"
    },
    "onChange": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when the checkbox is changed"
    },
    "onClick": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user clicks on the checkbox"
    },
    "onFocus": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user puts focus on the field"
    },
    "onMouseDown": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is pressed over the checkbox"
    },
    "onMouseEnter": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is over the checkbox"
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is no longer over the checkbox"
    },
    "onMouseUp": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is no longer pressed over the checkbox"
    },
    "value": {
      "type": {
        "name": "string"
      },
      "required": false,
      "description": "Value submitted with a form if checked",
      "defaultValue": {
        "value": "\"value\"",
        "computed": false
      }
    }
  }
};

var _createClass$2 = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck$2(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn$2(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits$2(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/**
 * @typedef {Object} State
 * @property {boolean} checked
 */

var CheckboxBehavior = function (_Component) {
  _inherits$2(CheckboxBehavior, _Component);

  function CheckboxBehavior() {
    var _ref;

    var _temp, _this, _ret;

    _classCallCheck$2(this, CheckboxBehavior);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn$2(this, (_ref = CheckboxBehavior.__proto__ || Object.getPrototypeOf(CheckboxBehavior)).call.apply(_ref, [this].concat(args))), _this), _this.state = {
      checked: _this.props.defaultChecked
    }, _this.handleChange = function (event) {
      _this.setChecked(event.target.checked);
    }, _temp), _possibleConstructorReturn$2(_this, _ret);
  }

  /**
   * @type {State}
   */


  _createClass$2(CheckboxBehavior, [{
    key: "getChecked",


    /**
     * @returns {boolean}
     */
    value: function getChecked() {
      if (this.isControlled()) {
        return this.props.checked;
      }

      return this.state.checked;
    }

    /**
     * @param {boolean} checked
     */

  }, {
    key: "setChecked",
    value: function setChecked(checked) {
      var onChange = this.props.onChange;


      if (onChange) onChange(checked);

      if (!this.isControlled()) {
        this.setState({ checked: checked });
      }
    }
  }, {
    key: "isControlled",
    value: function isControlled() {
      return this.props.checked !== undefined;
    }

    /**
     * @param {UIEvent} event
     */

  }, {
    key: "render",
    value: function render() {
      var handleChange = this.handleChange;
      var handleClick = this.props.onClick;

      var checked = this.getChecked();

      return this.props.children({
        checked: checked,
        handleChange: handleChange,
        handleClick: handleClick
      });
    }
  }]);

  return CheckboxBehavior;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

CheckboxBehavior.propTypes = {
  /**
   * Checks the checkbox
   */
  checked: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Initially checks the checkbox, but allows user action to change it
   */
  defaultChecked: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Called when user clicks on the field
   */
  onClick: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Called when user changes the value of the field
   */
  onChange: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Render prop
   */
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func
};
CheckboxBehavior.defaultProps = {
  defaultChecked: false
};
CheckboxBehavior.__docgenInfo = {
  "description": "@typedef {Object} State\n@property {boolean} checked",
  "displayName": "CheckboxBehavior",
  "props": {
    "checked": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Checks the checkbox"
    },
    "defaultChecked": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Initially checks the checkbox, but allows user action to change it",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    },
    "onClick": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user clicks on the field"
    },
    "onChange": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user changes the value of the field"
    },
    "children": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Render prop"
    }
  }
};

var _extends$2 = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass$3 = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _objectWithoutProperties$1(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function _classCallCheck$3(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn$3(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits$3(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Checkbox = function (_Component) {
  _inherits$3(Checkbox, _Component);

  function Checkbox() {
    _classCallCheck$3(this, Checkbox);

    return _possibleConstructorReturn$3(this, (Checkbox.__proto__ || Object.getPrototypeOf(Checkbox)).apply(this, arguments));
  }

  _createClass$3(Checkbox, [{
    key: "render",
    value: function render() {
      var _props = this.props,
          controlledChecked = _props.checked,
          defaultChecked = _props.defaultChecked,
          disabled = _props.disabled,
          indeterminate = _props.indeterminate,
          onBlur = _props.onBlur,
          onChange = _props.onChange,
          onClick = _props.onClick,
          onFocus = _props.onFocus,
          onMouseDown = _props.onMouseDown,
          onMouseEnter = _props.onMouseEnter,
          onMouseLeave = _props.onMouseLeave,
          onMouseUp = _props.onMouseUp,
          otherProps = _objectWithoutProperties$1(_props, ["checked", "defaultChecked", "disabled", "indeterminate", "onBlur", "onChange", "onClick", "onFocus", "onMouseDown", "onMouseEnter", "onMouseLeave", "onMouseUp"]);

      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
        _hig_behaviors__WEBPACK_IMPORTED_MODULE_5__["ControlBehavior"],
        {
          onBlur: onBlur,
          onFocus: onFocus,
          onMouseDown: onMouseDown,
          onMouseEnter: onMouseEnter,
          onMouseLeave: onMouseLeave,
          onMouseUp: onMouseUp
        },
        function (_ref) {
          var hasFocus = _ref.hasFocus,
              hasHover = _ref.hasHover,
              isPressed = _ref.isPressed,
              handleBlur = _ref.onBlur,
              handleFocus = _ref.onFocus,
              handleMouseDown = _ref.onMouseDown,
              handleMouseEnter = _ref.onMouseEnter,
              handleMouseLeave = _ref.onMouseLeave,
              handleMouseUp = _ref.onMouseUp;
          return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
            CheckboxBehavior,
            {
              checked: controlledChecked,
              defaultChecked: defaultChecked,
              onChange: onChange,
              onClick: onClick
            },
            function (_ref2) {
              var checked = _ref2.checked,
                  handleChange = _ref2.handleChange,
                  handleClick = _ref2.handleClick;
              return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CheckboxPresenter, _extends$2({
                checked: checked,
                disabled: disabled,
                hasFocus: hasFocus,
                hasHover: hasHover,
                indeterminate: indeterminate,
                isPressed: isPressed,
                onBlur: handleBlur,
                onChange: handleChange,
                onClick: handleClick,
                onFocus: handleFocus,
                onMouseDown: handleMouseDown,
                onMouseEnter: handleMouseEnter,
                onMouseLeave: handleMouseLeave,
                onMouseUp: handleMouseUp
              }, otherProps));
            }
          );
        }
      );
    }
  }]);

  return Checkbox;
}(react__WEBPACK_IMPORTED_MODULE_0__["Component"]);

Checkbox.propTypes = {
  /**
   * Checks the checkbox
   */
  checked: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Initially checks the checkbox, but allows user action to change it
   */
  defaultChecked: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Prevents user actions on the checkbox
   */
  disabled: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Sets indeterminate state for checkbox
   */
  indeterminate: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * The name of the checkbox as submitted with a form
   */
  name: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string,
  /**
   * Called when user moves focus from the field
   */
  onBlur: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Called when user changes the value of the field
   */
  onChange: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Called when user clicks on the checkbox
   */
  onClick: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Called when user puts focus on the field
   */
  onFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is pressed over the checkbox
   */
  onMouseDown: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is over the checkbox
   */
  onMouseEnter: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is no longer over the checkbox
   */
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Triggers when the user's mouse is no longer pressed over the checkbox
   */
  onMouseUp: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Value submitted with a form if checked
   */
  value: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string
};
Checkbox.__docgenInfo = {
  "description": "",
  "displayName": "Checkbox",
  "props": {
    "checked": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Checks the checkbox"
    },
    "defaultChecked": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Initially checks the checkbox, but allows user action to change it"
    },
    "disabled": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Prevents user actions on the checkbox"
    },
    "indeterminate": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Sets indeterminate state for checkbox"
    },
    "name": {
      "type": {
        "name": "string"
      },
      "required": false,
      "description": "The name of the checkbox as submitted with a form"
    },
    "onBlur": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user moves focus from the field"
    },
    "onChange": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user changes the value of the field"
    },
    "onClick": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user clicks on the checkbox"
    },
    "onFocus": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Called when user puts focus on the field"
    },
    "onMouseDown": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is pressed over the checkbox"
    },
    "onMouseEnter": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is over the checkbox"
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is no longer over the checkbox"
    },
    "onMouseUp": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Triggers when the user's mouse is no longer pressed over the checkbox"
    },
    "value": {
      "type": {
        "name": "string"
      },
      "required": false,
      "description": "Value submitted with a form if checked"
    }
  }
};

/* harmony default export */ __webpack_exports__["default"] = (Checkbox);



/***/ }),

/***/ "./node_modules/@hig/icons/build/index.es.js":
/*!***************************************************!*\
  !*** ./node_modules/@hig/icons/build/index.es.js ***!
  \***************************************************/
/*! exports provided: AVAILABLE_NAMES, names, sets, sizes, AVAILABLE_SIZES, AddFolder24, AddMember24, AddPage24, Add24, Alert24, Archive24, AreaPost24, Attach24, Attachment24, Back24, BillStack24, Billing24, Bookmark24, Building24, Calendar24, CaretDown24, CaretLeft24, CaretRight24, CaretUp24, CartEmpty24, CartFull24, Chat24, Checklist24, Checkmark24, Clock24, ClosedCaptioning24, Cloud24, Collaboration24, CollapsePanel24, Comment24, Compare24, Complete24, Copy24, CtaArrowRight24, Dashboard24, DeviceDesktop24, DeviceMobile24, DeviceTablet24, Devices24, DocumentManagement24, Down24, Download24, EditProfile24, Edit24, Education24, Error24, ExpandPanel24, ExportMove24, ExternalLink24, External24, Favorite24, Featured24, Feedback24, Field24, FileAssembly24, FileAudio24, FileDocument24, FileGeneric24, FileImage24, FilePart24, FilePdf24, FilePresentation24, FileSpreadsheet24, FileVideo24, FileZip24, FilterTokens24, Filter24, Flag24, FolderOpen24, Folder24, Forums24, Forward24, Globe24, Graph24, Grid24, Help24, Hidden24, Hierarchy24, Home24, Import24, Inbox24, Info24, Item24, Link24, List24, Location24, Lock24, Mail24, ManageAccess24, Maximize24, Minimize24, MoreVertical24, More24, Mute24, NotEditable24, NotificationOff24, Notification24, OnlineViewers24, Paste24, PermissionGroup24, PermissionIndividual24, Photos24, Pin24, Placeholder24, PlaybackFastForward24, PlaybackGoToEnd24, PlaybackGoToStart24, PlaybackLoop24, PlaybackNext24, PlaybackPause24, PlaybackPlay24, PlaybackPrevious24, PlaybackRecord24, PlaybackRewind24, PlaybackShuffle24, PlaybackStop24, Print24, ProductsAndServices24, Profile24, ProjectManagement24, Properties24, Publish24, Quantities24, Recommend24, Redo24, Report24, Save24, Search24, Security24, SelectAll24, SelectNone24, Service24, Settings24, Share24, SocialBlog24, SocialFacebook24, SocialInstagram24, SocialLinkedin24, SocialTwitter24, SocialYoutube24, Sort24, Subscription24, Support24, SurveyEquipment24, Sync24, Tag24, Trash24, Unavailable24, Undo24, Unlink24, Unlock24, Unpin24, Up24, Upload24, UserInactive24, Visible24, Volume24, ZoomIn24, ZoomOut24, AddFolder16, AddMember16, AddPage16, Add16, Alert16, Archive16, AreaPost16, Attach16, Attachment16, Back16, BillStack16, Billing16, Bookmark16, Building16, Calendar16, CaretDown16, CaretLeft16, CaretRight16, CaretUp16, CartEmpty16, CartFull16, Chat16, Checklist16, Checkmark16, Clock16, ClosedCaptioning16, Cloud16, Collaboration16, CollapsePanel16, Comment16, Compare16, Complete16, Copy16, CtaArrowRight16, Dashboard16, DeviceDesktop16, DeviceMobile16, DeviceTablet16, Devices16, DocumentManagement16, Down16, Download16, EditProfile16, Edit16, Education16, Error16, ExpandPanel16, ExportMove16, ExternalLink16, External16, Favorite16, Featured16, Feedback16, Field16, FileAssembly16, FileAudio16, FileDocument16, FileGeneric16, FileImage16, FilePart16, FilePdf16, FilePresentation16, FileSpreadsheet16, FileVideo16, FileZip16, FilterTokens16, Filter16, Flag16, FolderOpen16, Folder16, Forums16, Forward16, Globe16, Graph16, Grid16, Help16, Hidden16, Hierarchy16, Home16, Import16, Inbox16, Info16, Item16, Link16, List16, Location16, Lock16, Mail16, ManageAccess16, Maximize16, Minimize16, MoreVertical16, More16, Mute16, NotEditable16, NotificationOff16, Notification16, OnlineViewers16, Paste16, PermissionGroup16, PermissionIndividual16, Photos16, Pin16, Placeholder16, PlaybackFastForward16, PlaybackGoToEnd16, PlaybackGoToStart16, PlaybackLoop16, PlaybackNext16, PlaybackPause16, PlaybackPlay16, PlaybackPrevious16, PlaybackRecord16, PlaybackRewind16, PlaybackShuffle16, PlaybackStop16, Print16, ProductsAndServices16, Profile16, ProjectManagement16, Properties16, Publish16, Quantities16, Recommend16, Redo16, Report16, Save16, Search16, Security16, SelectAll16, SelectNone16, Service16, Settings16, Share16, SocialBlog16, SocialFacebook16, SocialInstagram16, SocialLinkedin16, SocialTwitter16, SocialYoutube16, Sort16, Subscription16, Support16, SurveyEquipment16, Sync16, Tag16, Trash16, Unavailable16, Undo16, Unlink16, Unlock16, Unpin16, Up16, Upload16, UserInactive16, Visible16, Volume16, ZoomIn16, ZoomOut16, CaretDownMUI, CaretDownSUI, CaretDownXsUI, CaretLeftMUI, CaretLeftSUI, CaretLeftXsUI, CaretRightMUI, CaretRightSUI, CaretRightXsUI, CaretUpMUI, CaretUpSUI, CaretUpXsUI, CheckmarkSUI, CheckmarkXsUI, CloseLUI, CloseMUI, CloseSUI, CloseXsUI, HamburgerCloseUI, HamburgerDefaultUI, OperatorMinusSUI, OperatorMinusXsUI, OperatorPlusSUI, OperatorPlusXsUI */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AVAILABLE_NAMES", function() { return AVAILABLE_NAMES; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "names", function() { return names; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "sets", function() { return higIconSets; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "sizes", function() { return sizes; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AVAILABLE_SIZES", function() { return AVAILABLE_SIZES; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AddFolder24", function() { return AddFolder24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AddMember24", function() { return AddMember24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AddPage24", function() { return AddPage24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Add24", function() { return Add24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Alert24", function() { return Alert24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Archive24", function() { return Archive24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AreaPost24", function() { return AreaPost24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Attach24", function() { return Attach24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Attachment24", function() { return Attachment24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Back24", function() { return Back24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "BillStack24", function() { return BillStack24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Billing24", function() { return Billing24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Bookmark24", function() { return Bookmark24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Building24", function() { return Building24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Calendar24", function() { return Calendar24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretDown24", function() { return CaretDown24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretLeft24", function() { return CaretLeft24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretRight24", function() { return CaretRight24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretUp24", function() { return CaretUp24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CartEmpty24", function() { return CartEmpty24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CartFull24", function() { return CartFull24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Chat24", function() { return Chat24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Checklist24", function() { return Checklist24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Checkmark24", function() { return Checkmark24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Clock24", function() { return Clock24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ClosedCaptioning24", function() { return ClosedCaptioning24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Cloud24", function() { return Cloud24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Collaboration24", function() { return Collaboration24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CollapsePanel24", function() { return CollapsePanel24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Comment24", function() { return Comment24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Compare24", function() { return Compare24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Complete24", function() { return Complete24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Copy24", function() { return Copy24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CtaArrowRight24", function() { return CtaArrowRight24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Dashboard24", function() { return Dashboard24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DeviceDesktop24", function() { return DeviceDesktop24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DeviceMobile24", function() { return DeviceMobile24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DeviceTablet24", function() { return DeviceTablet24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Devices24", function() { return Devices24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DocumentManagement24", function() { return DocumentManagement24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Down24", function() { return Down24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Download24", function() { return Download24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "EditProfile24", function() { return EditProfile24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Edit24", function() { return Edit24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Education24", function() { return Education24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Error24", function() { return Error24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ExpandPanel24", function() { return ExpandPanel24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ExportMove24", function() { return ExportMove24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ExternalLink24", function() { return ExternalLink24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "External24", function() { return External24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Favorite24", function() { return Favorite24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Featured24", function() { return Featured24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Feedback24", function() { return Feedback24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Field24", function() { return Field24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileAssembly24", function() { return FileAssembly24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileAudio24", function() { return FileAudio24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileDocument24", function() { return FileDocument24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileGeneric24", function() { return FileGeneric24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileImage24", function() { return FileImage24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FilePart24", function() { return FilePart24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FilePdf24", function() { return FilePdf24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FilePresentation24", function() { return FilePresentation24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileSpreadsheet24", function() { return FileSpreadsheet24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileVideo24", function() { return FileVideo24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileZip24", function() { return FileZip24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FilterTokens24", function() { return FilterTokens24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Filter24", function() { return Filter24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Flag24", function() { return Flag24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FolderOpen24", function() { return FolderOpen24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Folder24", function() { return Folder24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Forums24", function() { return Forums24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Forward24", function() { return Forward24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Globe24", function() { return Globe24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Graph24", function() { return Graph24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Grid24", function() { return Grid24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Help24", function() { return Help24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Hidden24", function() { return Hidden24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Hierarchy24", function() { return Hierarchy24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Home24", function() { return Home24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Import24", function() { return Import24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Inbox24", function() { return Inbox24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Info24", function() { return Info24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Item24", function() { return Item24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Link24", function() { return Link24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "List24", function() { return List24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Location24", function() { return Location24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Lock24", function() { return Lock24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Mail24", function() { return Mail24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ManageAccess24", function() { return ManageAccess24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Maximize24", function() { return Maximize24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Minimize24", function() { return Minimize24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "MoreVertical24", function() { return MoreVertical24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "More24", function() { return More24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Mute24", function() { return Mute24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "NotEditable24", function() { return NotEditable24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "NotificationOff24", function() { return NotificationOff24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Notification24", function() { return Notification24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "OnlineViewers24", function() { return OnlineViewers24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Paste24", function() { return Paste24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PermissionGroup24", function() { return PermissionGroup24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PermissionIndividual24", function() { return PermissionIndividual24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Photos24", function() { return Photos24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Pin24", function() { return Pin24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Placeholder24", function() { return Placeholder24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackFastForward24", function() { return PlaybackFastForward24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackGoToEnd24", function() { return PlaybackGoToEnd24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackGoToStart24", function() { return PlaybackGoToStart24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackLoop24", function() { return PlaybackLoop24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackNext24", function() { return PlaybackNext24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackPause24", function() { return PlaybackPause24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackPlay24", function() { return PlaybackPlay24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackPrevious24", function() { return PlaybackPrevious24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackRecord24", function() { return PlaybackRecord24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackRewind24", function() { return PlaybackRewind24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackShuffle24", function() { return PlaybackShuffle24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackStop24", function() { return PlaybackStop24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Print24", function() { return Print24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ProductsAndServices24", function() { return ProductsAndServices24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Profile24", function() { return Profile24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ProjectManagement24", function() { return ProjectManagement24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Properties24", function() { return Properties24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Publish24", function() { return Publish24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Quantities24", function() { return Quantities24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Recommend24", function() { return Recommend24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Redo24", function() { return Redo24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Report24", function() { return Report24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Save24", function() { return Save24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Search24", function() { return Search24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Security24", function() { return Security24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SelectAll24", function() { return SelectAll24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SelectNone24", function() { return SelectNone24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Service24", function() { return Service24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Settings24", function() { return Settings24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Share24", function() { return Share24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialBlog24", function() { return SocialBlog24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialFacebook24", function() { return SocialFacebook24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialInstagram24", function() { return SocialInstagram24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialLinkedin24", function() { return SocialLinkedin24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialTwitter24", function() { return SocialTwitter24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialYoutube24", function() { return SocialYoutube24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Sort24", function() { return Sort24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Subscription24", function() { return Subscription24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Support24", function() { return Support24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SurveyEquipment24", function() { return SurveyEquipment24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Sync24", function() { return Sync24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Tag24", function() { return Tag24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Trash24", function() { return Trash24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Unavailable24", function() { return Unavailable24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Undo24", function() { return Undo24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Unlink24", function() { return Unlink24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Unlock24", function() { return Unlock24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Unpin24", function() { return Unpin24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Up24", function() { return Up24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Upload24", function() { return Upload24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "UserInactive24", function() { return UserInactive24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Visible24", function() { return Visible24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Volume24", function() { return Volume24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ZoomIn24", function() { return ZoomIn24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ZoomOut24", function() { return ZoomOut24; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AddFolder16", function() { return AddFolder16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AddMember16", function() { return AddMember16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AddPage16", function() { return AddPage16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Add16", function() { return Add16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Alert16", function() { return Alert16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Archive16", function() { return Archive16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "AreaPost16", function() { return AreaPost16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Attach16", function() { return Attach16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Attachment16", function() { return Attachment16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Back16", function() { return Back16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "BillStack16", function() { return BillStack16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Billing16", function() { return Billing16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Bookmark16", function() { return Bookmark16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Building16", function() { return Building16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Calendar16", function() { return Calendar16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretDown16", function() { return CaretDown16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretLeft16", function() { return CaretLeft16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretRight16", function() { return CaretRight16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretUp16", function() { return CaretUp16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CartEmpty16", function() { return CartEmpty16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CartFull16", function() { return CartFull16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Chat16", function() { return Chat16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Checklist16", function() { return Checklist16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Checkmark16", function() { return Checkmark16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Clock16", function() { return Clock16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ClosedCaptioning16", function() { return ClosedCaptioning16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Cloud16", function() { return Cloud16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Collaboration16", function() { return Collaboration16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CollapsePanel16", function() { return CollapsePanel16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Comment16", function() { return Comment16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Compare16", function() { return Compare16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Complete16", function() { return Complete16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Copy16", function() { return Copy16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CtaArrowRight16", function() { return CtaArrowRight16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Dashboard16", function() { return Dashboard16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DeviceDesktop16", function() { return DeviceDesktop16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DeviceMobile16", function() { return DeviceMobile16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DeviceTablet16", function() { return DeviceTablet16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Devices16", function() { return Devices16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DocumentManagement16", function() { return DocumentManagement16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Down16", function() { return Down16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Download16", function() { return Download16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "EditProfile16", function() { return EditProfile16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Edit16", function() { return Edit16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Education16", function() { return Education16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Error16", function() { return Error16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ExpandPanel16", function() { return ExpandPanel16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ExportMove16", function() { return ExportMove16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ExternalLink16", function() { return ExternalLink16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "External16", function() { return External16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Favorite16", function() { return Favorite16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Featured16", function() { return Featured16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Feedback16", function() { return Feedback16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Field16", function() { return Field16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileAssembly16", function() { return FileAssembly16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileAudio16", function() { return FileAudio16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileDocument16", function() { return FileDocument16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileGeneric16", function() { return FileGeneric16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileImage16", function() { return FileImage16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FilePart16", function() { return FilePart16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FilePdf16", function() { return FilePdf16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FilePresentation16", function() { return FilePresentation16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileSpreadsheet16", function() { return FileSpreadsheet16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileVideo16", function() { return FileVideo16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FileZip16", function() { return FileZip16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FilterTokens16", function() { return FilterTokens16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Filter16", function() { return Filter16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Flag16", function() { return Flag16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FolderOpen16", function() { return FolderOpen16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Folder16", function() { return Folder16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Forums16", function() { return Forums16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Forward16", function() { return Forward16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Globe16", function() { return Globe16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Graph16", function() { return Graph16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Grid16", function() { return Grid16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Help16", function() { return Help16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Hidden16", function() { return Hidden16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Hierarchy16", function() { return Hierarchy16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Home16", function() { return Home16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Import16", function() { return Import16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Inbox16", function() { return Inbox16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Info16", function() { return Info16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Item16", function() { return Item16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Link16", function() { return Link16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "List16", function() { return List16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Location16", function() { return Location16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Lock16", function() { return Lock16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Mail16", function() { return Mail16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ManageAccess16", function() { return ManageAccess16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Maximize16", function() { return Maximize16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Minimize16", function() { return Minimize16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "MoreVertical16", function() { return MoreVertical16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "More16", function() { return More16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Mute16", function() { return Mute16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "NotEditable16", function() { return NotEditable16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "NotificationOff16", function() { return NotificationOff16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Notification16", function() { return Notification16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "OnlineViewers16", function() { return OnlineViewers16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Paste16", function() { return Paste16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PermissionGroup16", function() { return PermissionGroup16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PermissionIndividual16", function() { return PermissionIndividual16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Photos16", function() { return Photos16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Pin16", function() { return Pin16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Placeholder16", function() { return Placeholder16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackFastForward16", function() { return PlaybackFastForward16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackGoToEnd16", function() { return PlaybackGoToEnd16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackGoToStart16", function() { return PlaybackGoToStart16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackLoop16", function() { return PlaybackLoop16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackNext16", function() { return PlaybackNext16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackPause16", function() { return PlaybackPause16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackPlay16", function() { return PlaybackPlay16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackPrevious16", function() { return PlaybackPrevious16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackRecord16", function() { return PlaybackRecord16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackRewind16", function() { return PlaybackRewind16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackShuffle16", function() { return PlaybackShuffle16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PlaybackStop16", function() { return PlaybackStop16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Print16", function() { return Print16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ProductsAndServices16", function() { return ProductsAndServices16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Profile16", function() { return Profile16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ProjectManagement16", function() { return ProjectManagement16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Properties16", function() { return Properties16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Publish16", function() { return Publish16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Quantities16", function() { return Quantities16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Recommend16", function() { return Recommend16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Redo16", function() { return Redo16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Report16", function() { return Report16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Save16", function() { return Save16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Search16", function() { return Search16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Security16", function() { return Security16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SelectAll16", function() { return SelectAll16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SelectNone16", function() { return SelectNone16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Service16", function() { return Service16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Settings16", function() { return Settings16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Share16", function() { return Share16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialBlog16", function() { return SocialBlog16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialFacebook16", function() { return SocialFacebook16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialInstagram16", function() { return SocialInstagram16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialLinkedin16", function() { return SocialLinkedin16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialTwitter16", function() { return SocialTwitter16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SocialYoutube16", function() { return SocialYoutube16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Sort16", function() { return Sort16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Subscription16", function() { return Subscription16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Support16", function() { return Support16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SurveyEquipment16", function() { return SurveyEquipment16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Sync16", function() { return Sync16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Tag16", function() { return Tag16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Trash16", function() { return Trash16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Unavailable16", function() { return Unavailable16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Undo16", function() { return Undo16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Unlink16", function() { return Unlink16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Unlock16", function() { return Unlock16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Unpin16", function() { return Unpin16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Up16", function() { return Up16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Upload16", function() { return Upload16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "UserInactive16", function() { return UserInactive16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Visible16", function() { return Visible16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Volume16", function() { return Volume16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ZoomIn16", function() { return ZoomIn16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ZoomOut16", function() { return ZoomOut16; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretDownMUI", function() { return CaretDownMUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretDownSUI", function() { return CaretDownSUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretDownXsUI", function() { return CaretDownXsUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretLeftMUI", function() { return CaretLeftMUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretLeftSUI", function() { return CaretLeftSUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretLeftXsUI", function() { return CaretLeftXsUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretRightMUI", function() { return CaretRightMUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretRightSUI", function() { return CaretRightSUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretRightXsUI", function() { return CaretRightXsUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretUpMUI", function() { return CaretUpMUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretUpSUI", function() { return CaretUpSUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CaretUpXsUI", function() { return CaretUpXsUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CheckmarkSUI", function() { return CheckmarkSUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CheckmarkXsUI", function() { return CheckmarkXsUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CloseLUI", function() { return CloseLUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CloseMUI", function() { return CloseMUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CloseSUI", function() { return CloseSUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CloseXsUI", function() { return CloseXsUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "HamburgerCloseUI", function() { return HamburgerCloseUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "HamburgerDefaultUI", function() { return HamburgerDefaultUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "OperatorMinusSUI", function() { return OperatorMinusSUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "OperatorMinusXsUI", function() { return OperatorMinusXsUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "OperatorPlusSUI", function() { return OperatorPlusSUI; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "OperatorPlusXsUI", function() { return OperatorPlusXsUI; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var emotion__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! emotion */ "./node_modules/emotion/dist/emotion.esm.js");
/* harmony import */ var _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @hig/theme-context */ "./node_modules/@hig/theme-context/build/index.es.js");




function stylesheet(props, themeData) {
  var color = props.color;

  var fillColor = color || themeData["colorScheme.iconColor"];

  return {
    fill: fillColor,
    "> *": {
      fill: fillColor
    }
  };
}

var _extends = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var OperatorPlusXsUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 7V4h2v3h3v2H9v3H7V9H4V7h3z" }));
});

var _extends$1 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$1(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var OperatorPlusSUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$1(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$1({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 7V2h2v5h5v2H9v5H7V9H2V7h5z" }));
});

var _extends$2 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$2(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var OperatorMinusXsUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$2(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$2({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 7h8v2H4z" }));
});

var _extends$3 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$3(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var OperatorMinusSUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$3(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$3({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 7h12v2H2V7z" }));
});

var _extends$4 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$4(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var HamburgerDefaultUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$4(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$4({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 2h16v2H0zm0 5h16v2H0zm0 5h16v2H0z" }));
});

var _extends$5 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$5(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var HamburgerCloseUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$5(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$5({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.8 6.4L14.2 0l1.4 1.4-6.4 6.4 6.5 6.5-1.4 1.4-6.5-6.5-6.4 6.4L0 14.2l6.4-6.4L0 1.4 1.4 0z" }));
});

var _extends$6 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$6(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CloseXsUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$6(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$6({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.4 5.4l-.8-.8L8 7.3 5.4 4.6l-.8.8L7.3 8l-2.7 2.6.8.8L8 8.7l2.6 2.7.8-.8L8.7 8l2.7-2.6z" }));
});

var _extends$7 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$7(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CloseSUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$7(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$7({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.4 3.4l-.8-.8L8 7.3 3.4 2.6l-.8.8L7.3 8l-4.7 4.6.8.8L8 8.7l4.6 4.7.8-.8L8.7 8l4.7-4.6z" }));
});

var _extends$8 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$8(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CloseMUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$8(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$8({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14.4 2.4l-.8-.8L8 7.3 2.4 1.6l-.8.8L7.3 8l-5.7 5.6.8.8L8 8.7l5.6 5.7.8-.8L8.7 8l5.7-5.6z" }));
});

var _extends$9 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$9(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CloseLUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$9(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$9({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15.4 1.4l-.8-.8L8 7.3 1.4.6l-.8.8L7.3 8 .6 14.6l.8.8L8 8.7l6.6 6.7.8-.8L8.7 8l6.7-6.6z" }));
});

var _extends$10 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$10(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CheckmarkXsUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$10(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$10({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4.2 8.6l1.4-1.5 1.7 1.5 3.6-4 1.5 1.3-5 5.5-3.2-2.8z" }));
});

var _extends$11 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$11(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CheckmarkSUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$11(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$11({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6.2 12L2 8.1l1.3-1.3 2.9 2.7L12.7 3 14 4.3 6.2 12z" }));
});

var _extends$12 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$12(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretUpXsUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$12(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$12({ xmlns: "http://www.w3.org/2000/svg", width: "5", height: "3", viewBox: "0 0 5 3" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2.5 0L5 3H0l2.5-3z" }));
});

var _extends$13 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$13(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretUpSUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$13(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$13({ xmlns: "http://www.w3.org/2000/svg", width: "7", height: "4", viewBox: "0 0 7 4" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3.5 0L7 4H0l3.5-4z" }));
});

var _extends$14 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$14(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretUpMUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$14(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$14({ xmlns: "http://www.w3.org/2000/svg", width: "10", height: "10", viewBox: "0 0 10 10" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8.71 6.78a.75.75 0 0 1-1.06 0L5 4.13 2.35 6.78a.75.75 0 0 1-1.06 0 .74.74 0 0 1 0-1.06l3.36-3.36a.5.5 0 0 1 .7 0l3.36 3.36a.74.74 0 0 1 0 1.06z" }));
});

var _extends$15 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$15(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretRightXsUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$15(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$15({ xmlns: "http://www.w3.org/2000/svg", width: "3", height: "5", viewBox: "0 0 3 5" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 2.5L0 5V0l3 2.5z" }));
});

var _extends$16 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$16(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretRightSUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$16(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$16({ xmlns: "http://www.w3.org/2000/svg", width: "4", height: "7", viewBox: "0 0 4 7" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 7V0l4 3.5L0 7z" }));
});

var _extends$17 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$17(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretRightMUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$17(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$17({ xmlns: "http://www.w3.org/2000/svg", width: "10", height: "10", viewBox: "0 0 10 10" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3.22 1.29a.75.75 0 0 0 0 1.06L5.87 5 3.22 7.65a.75.75 0 0 0 0 1.06.74.74 0 0 0 1.06 0l3.36-3.36a.5.5 0 0 0 0-.7L4.28 1.29a.74.74 0 0 0-1.06 0z" }));
});

var _extends$18 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$18(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretLeftXsUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$18(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$18({ xmlns: "http://www.w3.org/2000/svg", width: "3", height: "5", viewBox: "0 0 3 5" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 0v5L0 2.5 3 0z" }));
});

var _extends$19 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$19(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretLeftSUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$19(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$19({ xmlns: "http://www.w3.org/2000/svg", width: "4", height: "7", viewBox: "0 0 4 7" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 3.5L4 0v7L0 3.5z" }));
});

var _extends$20 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$20(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretLeftMUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$20(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$20({ xmlns: "http://www.w3.org/2000/svg", width: "10", height: "10", viewBox: "0 0 10 10" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6.78 8.71a.75.75 0 0 0 0-1.06L4.13 5l2.65-2.65a.75.75 0 0 0 0-1.06.74.74 0 0 0-1.06 0L2.36 4.65a.5.5 0 0 0 0 .7l3.36 3.36a.74.74 0 0 0 1.06 0z" }));
});

var _extends$21 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$21(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretDownXsUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$21(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$21({ xmlns: "http://www.w3.org/2000/svg", width: "5", height: "3", viewBox: "0 0 5 3" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 0h5L2.5 3 0 0z" }));
});

var _extends$22 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$22(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretDownSUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$22(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$22({ xmlns: "http://www.w3.org/2000/svg", width: "7", height: "4", viewBox: "0 0 7 4" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 0h7L3.5 4 0 0z" }));
});

var _extends$23 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$23(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretDownMUISVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$23(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$23({ xmlns: "http://www.w3.org/2000/svg", width: "10", height: "10", viewBox: "0 0 10 10" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8.71 3.22a.75.75 0 0 0-1.06 0L5 5.87 2.35 3.22a.75.75 0 0 0-1.06 0 .74.74 0 0 0 0 1.06l3.36 3.36a.5.5 0 0 0 .7 0l3.36-3.36a.74.74 0 0 0 0-1.06z" }));
});

var _extends$24 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$24(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ZoomOut16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$24(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$24({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.297 11.706l1.414-1.414 4.002 4.002-1.414 1.414zM6 0a6 6 0 1 0 6 6 6 6 0 0 0-6-6zm0 10.5A4.5 4.5 0 1 1 10.5 6 4.51 4.51 0 0 1 6 10.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 5h6v2H3z" }));
});

var _extends$25 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$25(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ZoomIn16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$25(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$25({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.297 11.706l1.414-1.414 4.002 4.002-1.414 1.414zM6 0a6 6 0 1 0 6 6 6 6 0 0 0-6-6zm0 10.5A4.5 4.5 0 1 1 10.5 6 4.51 4.51 0 0 1 6 10.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 3H5v2H3v2h2v2h2V7h2V5H7V3z" }));
});

var _extends$26 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$26(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Volume16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$26(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$26({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 12l5 4V-.08l-5 4V12zM0 4h3v8H0zm13.08-1.83l-.29.41a.3.3 0 0 0 0 .38 6.94 6.94 0 0 1 0 10.08.3.3 0 0 0 0 .38l.29.41a.21.21 0 0 0 .33 0 7.95 7.95 0 0 0 0-11.74.21.21 0 0 0-.33.08z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.07 10.53a.33.33 0 0 0 0 .39l.3.43a.19.19 0 0 0 .31 0 4.93 4.93 0 0 0 0-6.76.19.19 0 0 0-.31 0l-.3.43a.33.33 0 0 0 0 .39 3.91 3.91 0 0 1 0 5.06z" }));
});

var _extends$27 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$27(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Visible16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$27(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$27({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 8a3 3 0 1 1 3 3 3 3 0 0 1-3-3zM0 8a9.36 9.36 0 0 0 8 5 9.37 9.37 0 0 0 8-5 9.42 9.42 0 0 0-8-5 9.46 9.46 0 0 0-8 5zm6 0a2 2 0 1 0 2-2 2 2 0 0 0-2 2z" }));
});

var _extends$28 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$28(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var UserInactive16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$28(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$28({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 7c1.62 0 2.86-1.79 2.86-3.41A3.08 3.08 0 0 0 7 .44a2.93 2.93 0 0 0-2.87 3v.18C4.14 5.58 5.38 7 7 7zm-.25 5.5a4.76 4.76 0 0 1 3-4.41 5.58 5.58 0 0 0-2.38-.59h-.65C4.39 7.6 2.07 8.84 2 12.19V16h6.32a4.71 4.71 0 0 1-1.57-3.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.5 9a3.5 3.5 0 1 0 3.5 3.5A3.5 3.5 0 0 0 11.5 9zM9 13v-1h5v1z" }));
});

var _extends$29 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$29(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Upload16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$29(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$29({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6.14 12h3.69V6.75h2.96L7.99 0l-4.8 6.75h2.95V12z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 11v3H2v-3H0v5h16v-5h-2z" }));
});

var _extends$30 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$30(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Up16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$30(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$30({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M5.38 7.88h-3.5L7.56 0l5.69 7.88h-3.5V16H5.38V7.88z" }));
});

var _extends$31 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$31(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Unpin16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$31(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$31({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3.75 10.49L.32 15.54l5.04-3.45-1.61-1.6zm-.29-4.61h-.55L1.74 7.06l7.07 7.07 1.17-1.18v-.51L3.46 5.88zm3.93-1.12l3.69 3.74 1.71-1.7.97.97 2.12-2.13-5.66-5.65L8.1 2.11l.97.97-1.68 1.68zM2.4 1.13L1.26 2.25l12.28 12.36 1.12-1.05L2.4 1.13z" }));
});

var _extends$32 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$32(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Unlock16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$32(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$32({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.5 0A3.5 3.5 0 0 0 9 3.5V6H2v9h11V6h-2V3.5a1.5 1.5 0 0 1 3 0V6h2V3.5A3.5 3.5 0 0 0 12.5 0zM8 12H7V9h1z" }));
});

var _extends$33 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$33(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Unlink16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$33(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$33({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.12 2.76a1.51 1.51 0 0 1 2.12 0 1.49 1.49 0 0 1 0 2.12l-2.85 2.86 1.41 1.42 2.86-2.86a3.5 3.5 0 0 0-4.95-4.95L6.88 4.18l1.4 1.42zM4.77 13.37a1.5 1.5 0 0 1-2.12-2.12L5.5 8.39 4.09 7 1.24 9.84a3.5 3.5 0 0 0 5 4.95L9 12l-1.4-1.43zM1.26 2.53l12.28 12.36 1.12-1.05L2.4 1.41 1.26 2.53z" }));
});

var _extends$34 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$34(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Undo16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$34(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$34({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 3L0 8l8 5v-3h4a4.11 4.11 0 0 1 4 2.19v-.92C16 8.5 14.25 6 11.49 6H8V3z" }));
});

var _extends$35 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$35(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Unavailable16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$35(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$35({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 1a7 7 0 1 0 7 7 7 7 0 0 0-7-7zM3 9V7h10v2z" }));
});

var _extends$36 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$36(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Trash16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$36(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$36({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 2H6v-.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5zM5 2H2v2h12V2h-3V0H5zM3 5h10v11H3z" }));
});

var _extends$37 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$37(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Tag16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$37(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$37({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.69 1H1v6.92L8.29 15 15 8.25zM4.5 6A1.5 1.5 0 1 1 6 4.5 1.5 1.5 0 0 1 4.5 6z" }));
});

var _extends$38 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$38(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Sync16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$38(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$38({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 13a5.62 5.62 0 0 1-4.11-1.82L6 9.62l-6-2 .16 6.28 2.1-1.55A7.56 7.56 0 0 0 15 10.25l-1.9-.64A5.57 5.57 0 0 1 8 13zM8 .94a7.58 7.58 0 0 0-7 4.84l1.94.64a5.53 5.53 0 0 1 9.22-1.63L10 6.32l6 2L15.84 2l-2.1 1.55A7.62 7.62 0 0 0 8 .94z" }));
});

var _extends$39 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$39(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SurveyEquipment16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$39(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$39({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 2h-1v9h1v3h1a1 1 0 0 0 1-1V9zM4 14a1 1 0 0 0 1 1v1h6v-1a1 1 0 0 0 1-1v-2H4zm2-4h4a1 1 0 0 0 1-1V1a1 1 0 0 0-1-1H6a1 1 0 0 0-1 1v8a1 1 0 0 0 1 1zm2-7a2 2 0 1 1-2 2 2 2 0 0 1 2-2zM4 2H3L1 9v4a1 1 0 0 0 1 1h1v-3h1z" }));
});

var _extends$40 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$40(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Support16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$40(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$40({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 0a8 8 0 1 0 8 8 8 8 0 0 0-8-8zm4.28 12.11c-.3.38-1.18 1.38-2.11 1.14-3.48-.93-6.51-6.06-5.57-9.6.25-.93 1.55-1.2 2-1.26a.24.24 0 0 1 .25.14L7.86 5a.21.21 0 0 1-.09.26 4.14 4.14 0 0 1-1.88.64c-.46 1.73 1.32 4.72 3 5.16.11-.5 1.11-1.14 1.47-1.38a.23.23 0 0 1 .27.05l1.64 2.09a.26.26 0 0 1 .01.29z" }));
});

var _extends$41 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$41(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Subscription16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$41(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$41({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 3h2v2H3zm3 0h4v2H6zM3 6h2v2H3zm0 3h2v2H3zm5 0H6v2h1.1A4.84 4.84 0 0 1 8 9z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15.94 11.44l-1.24.11a2.3 2.3 0 0 1 .05.45 2.79 2.79 0 1 1-1.68-2.52l-.74 1.29 3.55-.33-1.47-3.25-.7 1.21A3.94 3.94 0 0 0 12 8a4 4 0 1 0 4 4 3.43 3.43 0 0 0-.06-.56z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 7.42V6H6v2h3a5.53 5.53 0 0 1 1-.58z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 12H2V2h9v5.1a5.05 5.05 0 0 1 2 0V0H0v15h8a4.92 4.92 0 0 1-1-3z" }));
});

var _extends$42 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$42(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Sort16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$42(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$42({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M13.54 9.38V3h-3.08v6.38H8L12 15l4-5.62h-2.46zM4 1L0 6.63h2.46V13h3.08V6.63H8L4 1z" }));
});

var _extends$43 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$43(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialYoutube16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$43(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$43({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15.67 3.87a2.11 2.11 0 0 0-1.42-1.51A44.44 44.44 0 0 0 8 2a44.44 44.44 0 0 0-6.25.36A2.11 2.11 0 0 0 .33 3.87 24.23 24.23 0 0 0 0 8a24.23 24.23 0 0 0 .33 4.13 2.11 2.11 0 0 0 1.42 1.51A44.44 44.44 0 0 0 8 14a44.44 44.44 0 0 0 6.25-.36 2.11 2.11 0 0 0 1.42-1.51A24.23 24.23 0 0 0 16 8a24.23 24.23 0 0 0-.33-4.13zM6 11V5l5 3z" }));
});

var _extends$44 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$44(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialTwitter16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$44(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$44({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 3.54a6.61 6.61 0 0 1-1.89.52 3.32 3.32 0 0 0 1.45-1.82 6.62 6.62 0 0 1-2.09.8 3.28 3.28 0 0 0-5.68 2.24 3.08 3.08 0 0 0 .09.72 9.32 9.32 0 0 1-6.77-3.4A3.29 3.29 0 0 0 2.13 7a3.29 3.29 0 0 1-1.49-.43 3.28 3.28 0 0 0 2.64 3.26 3.22 3.22 0 0 1-1.49.06 3.3 3.3 0 0 0 3.07 2.28 6.58 6.58 0 0 1-4.08 1.4 5.11 5.11 0 0 1-.78-.05A9.22 9.22 0 0 0 5 15a9.27 9.27 0 0 0 9.34-9.34v-.42A6.79 6.79 0 0 0 16 3.54z" }));
});

var _extends$45 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$45(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialLinkedin16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$45(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$45({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2.34 6h2.29v7H2.34zM2 3.5A1.5 1.5 0 1 1 3.5 5 1.5 1.5 0 0 1 2 3.5zM6 6h2v1c1.81-2 5-.9 5 1.12V13h-2.13V8.79c0-1.19-1.73-1.51-2.74-.19V13H6zM0 2v12a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H2a2 2 0 0 0-2 2z" }));
});

var _extends$46 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$46(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialInstagram16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$46(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$46({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 1.44c2.14 0 2.39 0 3.23.05a4.33 4.33 0 0 1 1.49.27 2.67 2.67 0 0 1 1.52 1.52 4.33 4.33 0 0 1 .27 1.49c0 .84.05 1.09.05 3.23s0 2.39-.05 3.23a4.33 4.33 0 0 1-.27 1.49 2.67 2.67 0 0 1-1.52 1.52 4.33 4.33 0 0 1-1.49.27c-.84 0-1.09.05-3.23.05s-2.39 0-3.23-.05a4.33 4.33 0 0 1-1.49-.27 2.67 2.67 0 0 1-1.52-1.52 4.33 4.33 0 0 1-.27-1.49c0-.84-.05-1.09-.05-3.23s0-2.39.05-3.23a4.33 4.33 0 0 1 .27-1.49 2.67 2.67 0 0 1 1.52-1.52 4.33 4.33 0 0 1 1.49-.27c.84 0 1.09-.05 3.23-.05M8 0C5.83 0 5.55 0 4.7.05a5.85 5.85 0 0 0-1.94.37A4 4 0 0 0 .42 2.76 5.85 5.85 0 0 0 .05 4.7C0 5.55 0 5.83 0 8s0 2.45.05 3.3a5.85 5.85 0 0 0 .37 1.94 4 4 0 0 0 2.34 2.34A5.85 5.85 0 0 0 4.7 16H8c2.17 0 2.44 0 3.3-.05a5.85 5.85 0 0 0 1.94-.37 4 4 0 0 0 2.34-2.34A5.85 5.85 0 0 0 16 11.3c0-.85.05-1.13.05-3.3S16 5.55 16 4.7a5.85 5.85 0 0 0-.37-1.94A4 4 0 0 0 13.24.42 5.85 5.85 0 0 0 11.3.05C10.44 0 10.17 0 8 0z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 3.89A4.11 4.11 0 1 0 12.11 8 4.11 4.11 0 0 0 8 3.89zm0 6.78A2.67 2.67 0 1 1 10.67 8 2.68 2.68 0 0 1 8 10.67z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "12.27", cy: "3.73", r: ".96" }));
});

var _extends$47 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$47(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialFacebook16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$47(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$47({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 0H1a1 1 0 0 0-1 1v14a1 1 0 0 0 1 1h8.07V9.61H7V7.13h2.07V5.29A3 3 0 0 1 11.76 2h.39a16.26 16.26 0 0 1 1.85.1v2.22h-1.27c-1 0-1.19.49-1.19 1.21v1.6h2.37l-.31 2.49h-2.06V16H15a1 1 0 0 0 1-1V1a1 1 0 0 0-1-1z" }));
});

var _extends$48 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$48(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialBlog16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$48(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$48({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "3", cy: "13", r: "2" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1.5 1H1v2h.5A11.5 11.5 0 0 1 13 14.5v.5h2v-.5A13.49 13.49 0 0 0 1.5 1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1.5 6H1v2a4.19 4.19 0 0 1 .5 0A6.5 6.5 0 0 1 8 14.5a4.19 4.19 0 0 1 0 .5h2v-.5A8.51 8.51 0 0 0 1.5 6z" }));
});

var _extends$49 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$49(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Share16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$49(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$49({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 2.5a2.37 2.37 0 0 0 .18.93L5 6.53A2.47 2.47 0 0 0 3.5 6a2.5 2.5 0 1 0 1.86 4.18l4.7 2.8a2.24 2.24 0 0 0-.06.52 2.5 2.5 0 1 0 2.5-2.5 2.45 2.45 0 0 0-1.86.84L6 9a2.87 2.87 0 0 0 0-.5 2.72 2.72 0 0 0-.18-1L11 4.47a2.5 2.5 0 1 0-1-2z" }));
});

var _extends$50 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$50(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Settings16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$50(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$50({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 9V6h-2.21a5.34 5.34 0 0 0-.49-1.18l1.56-1.56-2.12-2.12-1.56 1.56A5.34 5.34 0 0 0 9 2.21V0H6v2.21a5.34 5.34 0 0 0-1.18.49L3.26 1.14 1.14 3.26 2.7 4.82A5.34 5.34 0 0 0 2.21 6H0v3h2.21a5.34 5.34 0 0 0 .49 1.18l-1.56 1.56 2.12 2.12 1.56-1.56a5.34 5.34 0 0 0 1.18.49V15h3v-2.21a5.34 5.34 0 0 0 1.18-.49l1.56 1.56 2.12-2.12-1.56-1.56A5.34 5.34 0 0 0 12.79 9zm-5-1.5A2.48 2.48 0 1 1 7.52 5 2.5 2.5 0 0 1 10 7.5z" }));
});

var _extends$51 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$51(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Service16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$51(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$51({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 0a8 8 0 0 0-6.63 12.48L4.81 9a4 4 0 0 1 5.55-5l-2.8 2.79.1 1.55 1.55.1L12 5.64a4 4 0 0 1-5 5.55l-3.48 3.44A8 8 0 1 0 8 0z" }));
});

var _extends$52 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$52(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SelectNone16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$52(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$52({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 4v2h1v8H6v-1H4v3h12V4h-3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 0H0v12h12zm-2 10H2V2h8z" }));
});

var _extends$53 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$53(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SelectAll16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$53(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$53({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 4v9H4v3h12V4h-3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 0H0v12h12zM5 9.37L1.65 6 3 4.65l2 2 4-5L10.34 3z" }));
});

var _extends$54 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$54(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Security16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$54(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$54({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14.15 2.65A8.54 8.54 0 0 1 8 0a8.54 8.54 0 0 1-6.15 2.65H1C1 12.79 7.25 16 8 16s7-3.21 7-13.39c-.18.02-.56.04-.85.04z" }));
});

var _extends$55 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$55(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Search16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$55(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$55({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.297 11.706l1.414-1.414 4.002 4.002-1.414 1.414zM6 0a6 6 0 1 0 6 6 6 6 0 0 0-6-6zm0 10.5A4.5 4.5 0 1 1 10.5 6 4.51 4.51 0 0 1 6 10.5z" }));
});

var _extends$56 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$56(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Save16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$56(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$56({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 1H1v11l3 3h11zm-4 13h-1v-3H6v3H5v-4h6zm1-7H4V2h1v4h6V2h1z" }));
});

var _extends$57 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$57(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Report16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$57(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$57({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 0H1v16h14zM3 4h5v1H3zm10 10H3v-1h1V8h2v5h1v-3h2v3h1V6h2v7h1zm0-11H3V2h10z" }));
});

var _extends$58 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$58(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Redo16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$58(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$58({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 3v3H4.5C1.74 6 0 8.5 0 11.27v.92A4.11 4.11 0 0 1 4 10h4v3l8-5-8-5z" }));
});

var _extends$59 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$59(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Recommend16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$59(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$59({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.72 14.11s.07 0 .11-.11A1.26 1.26 0 0 0 14 12.75a1.23 1.23 0 0 0-.25-.75.25.25 0 0 1 0-.5 1.25 1.25 0 0 0 0-2.5.25.25 0 0 1-.25-.25.26.26 0 0 1 .27-.23A1.28 1.28 0 0 0 15 7.25 1.25 1.25 0 0 0 13.75 6h-5c0-1.44 1-2.56 1-4.95a1 1 0 1 0-2 0c0 2.24-4.17 5.24-4.17 6.8a1.08 1.08 0 0 1-.8.63 1 1 0 0 0-.72 1v3.91a1 1 0 0 0 .82 1 1.56 1.56 0 0 1 1 .72 4.52 4.52 0 0 0 2.78 1h4a2.1 2.1 0 0 0 2.06-2z" }));
});

var _extends$60 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$60(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Quantities16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$60(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$60({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 11h16v3H0zM8 0v6h3v4h2V6h3V0zm7 3H9V1h6zM1 15h14v1H1z" }));
});

var _extends$61 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$61(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Publish16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$61(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$61({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 12H1v4h14zm-3 3H4v-2h8z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M6.37 5.91H3.79L7.99 0l4.2 5.91H9.61V10H6.37V5.91z" }));
});

var _extends$62 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$62(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Properties16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$62(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$62({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 0H2v16h12zm-1 13h-2v1h-1v-1H3v-1h7v-1h1v1h2zm0-3H6v1H5v-1H3V9h2V8h1v1h7zm0-4H3V1h10z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 2h8v3H4z" }));
});

var _extends$63 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$63(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ProjectManagement16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$63(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$63({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6.61 9a5.51 5.51 0 0 1 1.74-2H2V6h7v.61A5.44 5.44 0 0 1 11 6V0H0v13h6.21a5.46 5.46 0 0 1 0-3H2V9zM2 3h7v1H2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15.37 9.1a3.59 3.59 0 0 0-1.47-1.47 4.57 4.57 0 0 0-6.27 6.27 3.59 3.59 0 0 0 1.47 1.47 4.57 4.57 0 0 0 6.27-6.27zM15 12h-4V8h1v3h3z" }));
});

var _extends$64 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$64(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Profile16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$64(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$64({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 0a8 8 0 1 0 8 8 8 8 0 0 0-8-8zm0 3.36a2.46 2.46 0 0 1 2.3 2.53A2.65 2.65 0 0 1 8 8.62a2.48 2.48 0 0 1-2.28-2.73A2.35 2.35 0 0 1 8 3.36zm4 10.38a7 7 0 0 1-8 0v-1A3.62 3.62 0 0 1 7.79 9h.52c1.94.08 3.69 1.09 3.69 4z" }));
});

var _extends$65 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$65(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ProductsAndServices16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$65(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$65({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.52 0L0 3.35v8.14L7.48 16 15 11.49V3.35zM14 4.44L7.92 8.12v6.47l-.42.25-.42-.25V8.12L1 4.43V4l.5-.23 6 3.65 6-3.64.5.22z" }));
});

var _extends$66 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$66(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Print16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$66(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$66({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 3v2H2V3H0v10h2v-2h12v2h2V3h-2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 0h10v4H3zm9 12v3H4v-3H3v4h10v-4h-1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 12h6v2H5z" }));
});

var _extends$67 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$67(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackStop16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$67(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$67({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 2h12v12H2z" }));
});

var _extends$68 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$68(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackShuffle16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$68(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$68({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3.8 5.4l1.33 1.77L6.38 5.5l-1-1.3A3 3 0 0 0 3 3H0v2h3a1 1 0 0 1 .8.4zM16 12l-4-3v2h-1a1 1 0 0 1-.8-.4L8.87 8.83 7.62 10.5l1 1.3A3 3 0 0 0 11 13h1v2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 4l-4-3v2h-1a3 3 0 0 0-2.4 1.2l-4.8 6.4a1 1 0 0 1-.8.4H0v2h3a3 3 0 0 0 2.4-1.2l4.8-6.4A1 1 0 0 1 11 5h1v2z", fillRule: "evenodd" }));
});

var _extends$69 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$69(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackRewind16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$69(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$69({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 8l8 5V3L8 8zM0 8l8 5V3L0 8z" }));
});

var _extends$70 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$70(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackRecord16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$70(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$70({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "8", cy: "8", r: "6" }));
});

var _extends$71 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$71(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackPrevious16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$71(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$71({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 8l9 5.73V2.27L4 8zM2 2v12h2V2H2z" }));
});

var _extends$72 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$72(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackPlay16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$72(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$72({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 8L3 15V1l11 7z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 8L3 15V1l11 7z" }));
});

var _extends$73 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$73(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackPause16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$73(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$73({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 2h4v12H3zm6 0h4v12H9z" }));
});

var _extends$74 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$74(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackNext16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$74(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$74({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 13.73L12 8 3 2.27v11.46zM12 2v12h2V2h-2z" }));
});

var _extends$75 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$75(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackLoop16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$75(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$75({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M9 4L5 1v2H3a3 3 0 0 0-3 3v5a3 3 0 0 0 3 3h1v-2H3a1 1 0 0 1-1-1V6a1 1 0 0 1 1-1h2v2zm4-1h-1v2h1a1 1 0 0 1 1 1v5a1 1 0 0 1-1 1h-2v-2l-4 3 4 3v-2h2a3 3 0 0 0 3-3V6a3 3 0 0 0-3-3z" }));
});

var _extends$76 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$76(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackGoToStart16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$76(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$76({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 3L9 7.38V3L2 7.38V2H0v12h2V8.63L9 13V8.63L16 13V3z" }));
});

var _extends$77 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$77(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackGoToEnd16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$77(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$77({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 2v5.38L7 3v4.38L0 3v10l7-4.37V13l7-4.37V14h2V2h-2z" }));
});

var _extends$78 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$78(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackFastForward16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$78(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$78({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 13l8-5-8-5v10zm16-5L8 3v10l8-5z" }));
});

var _extends$79 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$79(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Placeholder16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$79(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$79({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.3 12.58a7.22 7.22 0 0 1-2.38 1.79l.41.91a8 8 0 0 0 2.72-2l-.75-.65zM8.51 15a7.08 7.08 0 0 1-2.95-.42l-.35.94a8.05 8.05 0 0 0 3.37.48l-.07-1zm-5.09-1.7a7.36 7.36 0 0 1-1.79-2.38l-.9.41a7.92 7.92 0 0 0 2 2.72l.65-.75zM1 8.51a7.08 7.08 0 0 1 .42-2.95L.5 5.21A8.05 8.05 0 0 0 0 8.58l1-.07zm1.7-5.09a7.29 7.29 0 0 1 2.39-1.79l-.42-.9A8 8 0 0 0 2 2.77l.75.65zM7.49 1a7.08 7.08 0 0 1 2.95.42l.35-.92A8.05 8.05 0 0 0 7.42 0l.07 1zm5.09 1.7a7.16 7.16 0 0 1 1.79 2.39l.91-.42a8 8 0 0 0-2-2.72l-.65.75zM15 7.49a7.08 7.08 0 0 1-.42 2.95l.94.35A8.05 8.05 0 0 0 16 7.42l-1 .07z" }));
});

var _extends$80 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$80(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Pin16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$80(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$80({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.22.1L8.1 2.22l1 1L6.24 6H2.91L1.74 7.17l7.07 7.07L10 13.06V9.74l2.83-2.83 1 1 2.12-2.12zM3.75 10.6L.32 15.66l5.04-3.45-1.61-1.61z" }));
});

var _extends$81 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$81(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Photos16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$81(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$81({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 16h13V3H0zM9.5 5A1.5 1.5 0 1 1 8 6.5 1.5 1.5 0 0 1 9.5 5zM2 9.84l.29-.41a2 2 0 0 1 3.3 0l2.53 3.48a.28.28 0 0 0 .44 0l.95-1.17a1.23 1.23 0 0 1 1-.46 1.14 1.14 0 0 1 .52.13V14H2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 0H1v2h13v12h2V0h-2z" }));
});

var _extends$82 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$82(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PermissionIndividual16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$82(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$82({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.72 7.49C5.39 7.6 3.07 8.84 3 12.19V16h10v-3.5c0-3.64-2.2-4.9-4.63-5h-.65zM8 7c1.62 0 2.86-1.78 2.86-3.41A3.08 3.08 0 0 0 8 .44a2.93 2.93 0 0 0-2.86 3.17C5.14 5.58 6.38 7 8 7z" }));
});

var _extends$83 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$83(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PermissionGroup16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$83(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$83({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3.72 7.49C1.39 7.6.07 8.84 0 12.19V16h8v-3.5c0-3.64-1.2-4.9-3.63-5h-.65zM4 7c1.62 0 2.86-1.76 2.86-3.39A3.08 3.08 0 0 0 4 .44a2.93 2.93 0 0 0-2.86 3.17C1.14 5.58 2.38 7 4 7zm7.56 1.57c1.34 0 2.37-1.48 2.37-2.82a2.56 2.56 0 0 0-2.37-2.63 2.43 2.43 0 0 0-2.37 2.63 2.56 2.56 0 0 0 2.37 2.82zm.3.43h-.53a2.78 2.78 0 0 0-2.51 1.3A11.94 11.94 0 0 1 9 12.5V16h5.87v-2.9c0-3.01-.99-4.05-3.01-4.1z" }));
});

var _extends$84 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$84(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Paste16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$84(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$84({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 6h3V2H9v1H1V2H0v12h7V6zm9 4h-3V7l3 3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 11V7H8v9h8v-5h-4zM7.5 1H7V0H3v1h-.5a.5.5 0 0 0-.5.5V2h6v-.5a.5.5 0 0 0-.5-.5z" }));
});

var _extends$85 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$85(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var OnlineViewers16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$85(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$85({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3.29 11.25h2.42A10 10 0 0 1 7.25 10V7.75H2.76a13 13 0 0 0 .53 3.5zm-.53-4h4.49v-3.5h-4a13.43 13.43 0 0 0-.49 3.5zM7.75 0v3.25h3.81A9 9 0 0 0 10 .45 7.57 7.57 0 0 0 7.75 0zm-.5 3.25V0A7.57 7.57 0 0 0 5 .45a9 9 0 0 0-1.52 2.8zm-2.48 9.83a2.17 2.17 0 0 1 .46-1.33H3.45A9 9 0 0 0 5 14.55l.54.17-.19-.2a2.13 2.13 0 0 1-.58-1.44zm7.4-4.19a10.57 10.57 0 0 0 .07-1.14H7.75v1.91A6.59 6.59 0 0 1 11 8.77a6.37 6.37 0 0 1 1.17.12zm-.45-5.14h-4v3.5h4.49a13.43 13.43 0 0 0-.49-3.5zm1.02 3.5H15a7.45 7.45 0 0 0-1-3.5h-1.75a14 14 0 0 1 .49 3.5zm-9.99 4a14.42 14.42 0 0 1-.49-3.5H0a7.45 7.45 0 0 0 1 3.5zm0-7.5H1a7.45 7.45 0 0 0-1 3.5h2.26a14 14 0 0 1 .49-3.5zm.15 8H1.32A7.58 7.58 0 0 0 4 14.14a10.33 10.33 0 0 1-1.1-2.39zm9.84-4c0 .43 0 .85-.07 1.27a8 8 0 0 1 1.94.84A7.3 7.3 0 0 0 15 7.75zm-.64-4.5h1.58A7.54 7.54 0 0 0 11 .86a10.31 10.31 0 0 1 1.1 2.39zm-9.2 0A10.31 10.31 0 0 1 4 .86a7.54 7.54 0 0 0-2.68 2.39zm12.87 9.17c-1.31-1.49-5.26-4.55-9.52 0a.91.91 0 0 0-.23.62 1 1 0 0 0 .23.63A6.77 6.77 0 0 0 11.08 16a5.47 5.47 0 0 0 4.69-2.38A1 1 0 0 0 16 13a.94.94 0 0 0-.23-.58zM11 15a2 2 0 1 1 2-2 2 2 0 0 1-2 2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "11", cy: "13", r: "1" }));
});

var _extends$86 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$86(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Notification16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$86(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$86({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.84 8.77V6.52a4.17 4.17 0 0 0-3.1-4.18V.62A.62.62 0 0 0 8.12 0H6.88a.62.62 0 0 0-.62.62v1.67a4.26 4.26 0 0 0-3.1 4.23v2.25a3.26 3.26 0 0 1-2.16 3V13h13v-1.24a3.25 3.25 0 0 1-2.16-2.99zM7.5 16a2.25 2.25 0 0 0 2.09-2H5.41a2.25 2.25 0 0 0 2.09 2z" }));
});

var _extends$87 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$87(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var NotificationOff16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$87(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$87({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 9.5a3.58 3.58 0 0 1-.11-.73V6.52a4.17 4.17 0 0 0-3.15-4.17V.62A.62.62 0 0 0 8.12 0H6.88a.62.62 0 0 0-.62.62v1.67a3.87 3.87 0 0 0-1 .43zM3.23 5.76a4.77 4.77 0 0 0-.06.76v2.25a3.25 3.25 0 0 1-2.16 3V13h9.42zm-.81-4.5L1.28 2.38l12.28 12.36 1.12-1.05L2.42 1.26zM7.5 16a2.25 2.25 0 0 0 2.09-2H5.41a2.25 2.25 0 0 0 2.09 2z" }));
});

var _extends$88 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$88(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var NotEditable16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$88(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$88({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1.38 11.08l3.54 3.54L0 16l1.38-4.92zM13 0h-.31a1 1 0 0 0-.52.27l-2.09 2.11 3.54 3.54 2.09-2.09a1 1 0 0 0 .29-.52 1.94 1.94 0 0 0 0-.24V3a3 3 0 0 0-3-3zm-2 8.54l1.77-1.77-3.54-3.54-1.74 1.74L11 8.54zM4.96 7.5l-2.73 2.73 3.54 3.54 2.72-2.72L4.96 7.5zM2.4 1.24L1.26 2.36l12.28 12.36 1.12-1.05L2.4 1.24z" }));
});

var _extends$89 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$89(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Mute16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$89(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$89({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.7 8.03l1.68-1.61-.76-.75L13 7.33l-1.62-1.66-.76.75 1.68 1.61-1.68 1.62.76.75L13 8.73l1.62 1.67.76-.75-1.68-1.62zM4 12.02L9 16V0L4 3.98v8.04zM0 4h3v8H0z" }));
});

var _extends$90 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$90(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var More16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$90(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$90({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "1.5", cy: "8.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "7.5", cy: "8.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "13.5", cy: "8.5", r: "1.5" }));
});

var _extends$91 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$91(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var MoreVertical16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$91(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$91({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "7.5", cy: "14.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "7.5", cy: "8.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "7.5", cy: "2.5", r: "1.5" }));
});

var _extends$92 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$92(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Minimize16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$92(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$92({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 16h8V8H0zm2-6h4v4H2zm13.71-8.29L14.29.29l-3.52 3.52-1.76-1.77L8 8l5.96-1.01-1.77-1.76 3.52-3.52z", fillRule: "evenodd" }));
});

var _extends$93 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$93(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Maximize16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$93(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$93({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 1l1.77 1.77L8.59 6H0v10h10V7.41l3.22-3.22L15 6l1-6zM8 14H2V8h6z", fillRule: "evenodd" }));
});

var _extends$94 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$94(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ManageAccess16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$94(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$94({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 6.63c1.51 0 2.66-1.63 2.66-3.18A2.87 2.87 0 0 0 5 .5a2.72 2.72 0 0 0-2.66 3c0 1.79 1.15 3.13 2.66 3.13zm10 6.57v-1.4h-1a2.66 2.66 0 0 0-.23-.55l.73-.73-1-1-.73.73a2.66 2.66 0 0 0-.57-.25V9h-1.4v1a2.66 2.66 0 0 0-.55.23l-.73-.73-1 1 .73.73a2.66 2.66 0 0 0-.25.57H8v1.4h1a2.66 2.66 0 0 0 .23.55l-.73.73 1 1 .73-.73a2.66 2.66 0 0 0 .55.23v1h1.4V15a2.66 2.66 0 0 0 .55-.23l.73.73 1-1-.73-.73a2.66 2.66 0 0 0 .27-.57zm-3.49.47a1.17 1.17 0 1 1 1.16-1.17 1.18 1.18 0 0 1-1.16 1.17z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8.44 9.22A4.26 4.26 0 0 0 5 7.12a4.12 4.12 0 0 0-4 4.33V15h6.76a4.47 4.47 0 0 1 .68-5.78z" }));
});

var _extends$95 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$95(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Mail16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$95(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$95({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 11.8V3.7l-4 4zM.21 13H14.8l-4.55-4.55L7.4 11.3 4.66 8.56zM0 3.9v7.89l3.94-3.95zM0 2v.48l7.4 7.4L15 2.3V2z" }));
});

var _extends$96 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$96(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Lock16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$96(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$96({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 6v-.5a3.5 3.5 0 0 0-7 0V6H2v9h11V6zm-5-.5a1.5 1.5 0 0 1 3 0V6H6zM8 12H7V9h1z" }));
});

var _extends$97 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$97(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Location16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$97(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$97({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6.06 5A1.94 1.94 0 1 1 8 7a1.94 1.94 0 0 1-1.94-2zM3 5.05c0 2.79 2.55 6.74 5 11 2.47-4.24 5-8.16 5-10.95a5 5 0 1 0-10 0z" }));
});

var _extends$98 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$98(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var List16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$98(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$98({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("rect", { x: "4", y: "2", width: "11", height: "2", rx: ".5", ry: ".5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1 2h2v2H1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("rect", { x: "4", y: "7", width: "11", height: "2", rx: ".5", ry: ".5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1 7h2v2H1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("rect", { x: "4", y: "12", width: "11", height: "2", rx: ".5", ry: ".5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1 12h2v2H1z" }));
});

var _extends$99 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$99(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Link16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$99(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$99({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14.66 1.35a3.49 3.49 0 0 0-4.95 0L6 5.06a3.49 3.49 0 0 0 0 5l.19.16 1.23-1.28a.26.26 0 0 0 0-.35 1.51 1.51 0 0 1 0-2.12l3.71-3.71a1.51 1.51 0 0 1 2.12 0 1.49 1.49 0 0 1 0 2.12l-1.78 1.78A4.7 4.7 0 0 1 11.94 9l2.72-2.7a3.51 3.51 0 0 0 0-4.95z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M9.69 6L8.46 7.19a.26.26 0 0 0 0 .35 1.51 1.51 0 0 1 0 2.12l-3.7 3.71a1.51 1.51 0 0 1-2.12 0 1.49 1.49 0 0 1 0-2.12l1.77-1.78a4.7 4.7 0 0 1-.47-2.35L1.22 9.83a3.5 3.5 0 0 0 5 4.95l3.71-3.71a3.49 3.49 0 0 0 0-4.95z" }));
});

var _extends$100 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$100(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Item16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$100(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$100({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 0H0v16h15zm-3 13H3v-1h9zm0-3H3V9h9zm0-3H3V6h9zm0-3H3V3h9z" }));
});

var _extends$101 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$101(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Info16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$101(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$101({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 1a7 7 0 1 0 7 7 7 7 0 0 0-7-7zm1 11H7V7h2zM8 6.25A1.25 1.25 0 1 1 9.25 5 1.25 1.25 0 0 1 8 6.25z" }));
});

var _extends$102 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$102(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Inbox16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$102(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$102({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.82 11a3 3 0 0 1-5.64 0H1v5h14v-5zM1 0v10h1V3h12v7h1V0H1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 4v6h3a2 2 0 0 0 4 0h3V4z" }));
});

var _extends$103 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$103(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Import16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$103(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$103({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 0v4h4l-4-4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 0H3v4.76L9.66 9.5 3 14.24V15h13V5h-5V0z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M3.02 8.15V6l4.92 3.5L3.02 13v-2.15H0v-2.7h3.02z" }));
});

var _extends$104 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$104(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Home16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$104(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$104({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 5.59V2h-2v1.59l-3-3-8 8L1.41 10 2 9.41V15h4v-5h4v5h4V9.41l.59.59L16 8.59l-3-3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 11h2v4H7z" }));
});

var _extends$105 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$105(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Hierarchy16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$105(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$105({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 0v7h4v2H2v3H0v4h5v-4H3v-2h9v2h-2v4h5v-4h-2V9H8V7h4V0z" }));
});

var _extends$106 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$106(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Hidden16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$106(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$106({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5.61 10a3.13 3.13 0 0 1-.74-2.2l-2.6-2.59A9.74 9.74 0 0 0 0 8a9.37 9.37 0 0 0 8 5 8.09 8.09 0 0 0 1.83-.23l-1.66-1.66A3.12 3.12 0 0 1 5.61 10zm4.62-4.26a3.13 3.13 0 0 1 .91 2.5l2.58 2.57A9.57 9.57 0 0 0 16 8a9.46 9.46 0 0 0-8-5 8.25 8.25 0 0 0-1.85.23l1.6 1.61a3.12 3.12 0 0 1 2.48.9zM1.25 2.36l12.29 12.36 1.12-1.05L2.4 1.24 1.25 2.36z" }));
});

var _extends$107 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$107(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Help16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$107(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$107({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 0a8 8 0 1 0 8 8 8 8 0 0 0-8-8zm1 14H7v-2h2zm1-5.55c-.62.29-1 .5-1 1V11H7V8.92a1.88 1.88 0 0 1 .59-1.45c.46-.41 2.16-.91 2.16-1.88 0-.59-.3-1.26-1.52-1.26-1.48 0-2 2.33-2 2.33l-1.78-.41s.41-3.86 3.8-3.86a3.33 3.33 0 0 1 3.52 3.13A3.17 3.17 0 0 1 10 8.45z" }));
});

var _extends$108 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$108(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Grid16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$108(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$108({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 9H1v6h6zm0-8H1v6h6zm8 8H9v6h6zm0-8H9v6h6z" }));
});

var _extends$109 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$109(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Graph16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$109(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$109({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5.5 4.21l3 3L14.85.85l-.7-.7L8.5 5.79l-3-3L.15 8.15l.7.7L5.5 4.21zM0 12h3v4H0z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 8h3v8H4zm4 1h3v7H8zm4-3h3v10h-3z" }));
});

var _extends$110 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$110(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Globe16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$110(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$110({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2.76 7.25h4.49v-3.5h-4a13.43 13.43 0 0 0-.49 3.5zm4.49-4V0A7.57 7.57 0 0 0 5 .45a9 9 0 0 0-1.52 2.8zm-3.97 8h4v-3.5H2.76a13.43 13.43 0 0 0 .52 3.5zm8.82-8h1.58A7.54 7.54 0 0 0 11 .86a10.31 10.31 0 0 1 1.1 2.39zM7.25 15v-3.25H3.44A9 9 0 0 0 5 14.55a7.57 7.57 0 0 0 2.25.45zm5-11.25a14 14 0 0 1 .49 3.5H15a7.45 7.45 0 0 0-1-3.5zM10 .45A7.57 7.57 0 0 0 7.75 0v3.25h3.81A9 9 0 0 0 10 .45zm2.25 10.8H14a7.45 7.45 0 0 0 1-3.5h-2.26a14 14 0 0 1-.49 3.5zm-.01-3.5H7.75v3.5h4a13.43 13.43 0 0 0 .49-3.5zm-9.34 4H1.32A7.54 7.54 0 0 0 4 14.14a10.31 10.31 0 0 1-1.1-2.39zm-2.9-4a7.45 7.45 0 0 0 1 3.5h1.75a14 14 0 0 1-.49-3.5zm12.1 4a10.31 10.31 0 0 1-1.1 2.39 7.54 7.54 0 0 0 2.69-2.39zM1 3.75a7.45 7.45 0 0 0-1 3.5h2.26a14 14 0 0 1 .49-3.5zm6.75 8V15a7.57 7.57 0 0 0 2.25-.45 9 9 0 0 0 1.52-2.8zm3.97-8h-4v3.5h4.49a13.43 13.43 0 0 0-.49-3.5zm-8.82-.5A10.31 10.31 0 0 1 4 .86a7.54 7.54 0 0 0-2.68 2.39z" }));
});

var _extends$111 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$111(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Forward16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$111(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$111({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M8.13 10.63v3.5L16 8.44 8.13 2.75v3.5H0v4.38h8.13z" }));
});

var _extends$112 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$112(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Forums16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$112(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$112({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.29 8.81a2.05 2.05 0 0 0-1.29.42V14h4v-2c0-2.28-.78-3.11-2.31-3.18h-.4zM8.24 8h-.5C5.92 8.08 5.06 9 5 11.49V14h6v-2.23c0-2.71-.86-3.69-2.76-3.77zm5.26-.12a1.91 1.91 0 0 0 1.69-2 1.91 1.91 0 0 0-1.69-2 1.8 1.8 0 0 0-1.69 2 1.77 1.77 0 0 0 1.69 2zM0 12v2h4V9.23a2.05 2.05 0 0 0-1.29-.42h-.4C.78 8.87 0 9.7 0 12zm2.5-8.12a1.91 1.91 0 0 0-1.69 2 1.91 1.91 0 0 0 1.69 2 1.77 1.77 0 0 0 1.69-2 1.8 1.8 0 0 0-1.69-2zM8 6.75a2.32 2.32 0 0 0 2.06-2.45A2.32 2.32 0 0 0 8 1.85 2.21 2.21 0 0 0 5.94 4.3 2.17 2.17 0 0 0 8 6.75z" }));
});

var _extends$113 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$113(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Folder16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$113(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$113({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 2v2h6.34l1-1-1-1zm0 3v9h15V3H9L7 5z" }));
});

var _extends$114 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$114(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FolderOpen16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$114(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$114({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 5V4H8L6 2H0v9.76L3.38 5H12z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 6l-4 8h12l4-8H4z" }));
});

var _extends$115 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$115(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Flag16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$115(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$115({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.13 2.3a7 7 0 0 1-3.05.7 7.06 7.06 0 0 1-3-.67A3.56 3.56 0 0 0 4.54 2a3.49 3.49 0 0 0-2.39 1l4 6a3.45 3.45 0 0 1 2.42-1c1.48 0 2.32 1 4.54 1A7 7 0 0 0 16 8.38zM.88 2.85L0 3.4 7.82 15l.06.1L8 15l.74-.49z" }));
});

var _extends$116 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$116(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Filter16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$116(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$116({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 2h15v2L9 8.5V13l-3 2V8.5L0 3.89V2z" }));
});

var _extends$117 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$117(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FilterTokens16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$117(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$117({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 1v6h16V1zm4 4H2V3h2zM0 15h16V9H0zm2-4h2v2H2z" }));
});

var _extends$118 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$118(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileZip16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$118(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$118({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1 0v16h13V5h-4v3H8v1h1v1H8v1h1v1H8v1h1v1H8v1H6v-1h1v-1H6v-1h1v-1H6v-1h1V9H6V8H5V0z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 0v4h4zM7 4h1v2H7zM6 0v7h3V0z" }));
});

var _extends$119 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$119(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileVideo16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$119(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$119({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 6h3v4H3zm6 0h3v4H9z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 5V2H0v3h1v6H0v3h15v-3h-1V5zm-2-2h1v1h-1zm-2 0h1v1h-1zM9 3h1v1H9zM7 3h1v1H7zM5 3h1v1H5zM3 3h1v1H3zM1 3h1v1H1zm1 2h5v6H2zm0 8H1v-1h1zm2 0H3v-1h1zm2 0H5v-1h1zm2 0H7v-1h1zm2 0H9v-1h1zm2 0h-1v-1h1zm2 0h-1v-1h1zm-1-2H8V5h5z" }));
});

var _extends$120 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$120(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileSpreadsheet16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$120(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$120({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 2v13h16V2zm9 12H4v-2h5zm0-3H4V9h5zm0-3H4V6h5zm0-3H4V3h5zm6 9h-5v-2h5zm0-3h-5V9h5zm0-3h-5V6h5zm0-3h-5V3h5z", fill: "#87bc40" }));
});

var _extends$121 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$121(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FilePresentation16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$121(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$121({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14.5 1H.5a.5.5 0 0 0 0 1H1v9h4.33l-2.11 2.92a.5.5 0 1 0 .81.59L6.56 11H7v3.38a.57.57 0 0 0 .5.62.57.57 0 0 0 .5-.62V11h.44L11 14.51a.5.5 0 1 0 .81-.59L9.67 11H14V2h.5a.5.5 0 0 0 0-1zM13 10H2V2h11z", fill: "#faa21b" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fill: "#faa21b", d: "M3 3h9v6H3z" }));
});

var _extends$122 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$122(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FilePdf16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$122(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$122({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 0H1v16h13V4zM3 5h4v3H3zm6 7H3v-1h6zm3-2H3V9h9zm0-2H8V7h4zm0-2H8V5h4z", fill: "#ec4a41" }));
});

var _extends$123 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$123(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FilePart16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$123(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$123({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fill: "#ec4a41", d: "M11 8V3H5v5H0v5h16V8h-5z" }));
});

var _extends$124 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$124(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileImage16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$124(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$124({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1 1v14h14V1zm12 12H3V8.51l.4-.58a2.2 2.2 0 0 1 3.6 0l2.76 3.79a.3.3 0 0 0 .48 0l1-1.27a1.36 1.36 0 0 1 1.11-.45 1.29 1.29 0 0 1 .65.18zm-2-6a2 2 0 1 1 2-2 2 2 0 0 1-2 2z" }));
});

var _extends$125 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$125(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileGeneric16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$125(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$125({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1 0v16h13V5H9V0zm9 0v4h4z" }));
});

var _extends$126 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$126(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileDocument16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$126(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$126({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 0H1v16h13V4zM9 12H3v-1h6zm3-2H3V9h9zm0-2H3V7h9zm0-2H3V5h9z", fill: "#0696d7" }));
});

var _extends$127 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$127(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileAudio16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$127(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$127({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 12l5 4V-.08l-5 4V12zM0 4h3v8H0zm13.08-1.83l-.29.41a.3.3 0 0 0 0 .38 6.94 6.94 0 0 1 0 10.08.3.3 0 0 0 0 .38l.29.41a.21.21 0 0 0 .33 0 7.95 7.95 0 0 0 0-11.74.21.21 0 0 0-.33.08z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.07 10.53a.33.33 0 0 0 0 .39l.3.43a.19.19 0 0 0 .31 0 4.93 4.93 0 0 0 0-6.76.19.19 0 0 0-.31 0l-.3.43a.33.33 0 0 0 0 .39 3.91 3.91 0 0 1 0 5.06z" }));
});

var _extends$128 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$128(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileAssembly16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$128(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$128({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fill: "#ec4a41", d: "M12 3h4v4h-4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fill: "#ec4a41", d: "M11 8V3H5v5H0v5h16V8h-5z" }));
});

var _extends$129 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$129(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Field16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$129(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$129({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.48 11c-1.88 0-4.29.51-5.48 2l.75 1h9.5l.75-1c-1.19-1.49-3.62-2-5.52-2zM8 2.5a.5.5 0 0 0-1 0V3h1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 10.07a7.22 7.22 0 0 0-3-5.95V6h-1V4h1v-.5a.5.5 0 0 0-1 0A6.14 6.14 0 0 0 8 3v3H7V3a6.14 6.14 0 0 0-2 .51.5.5 0 0 0-1 0V4h1v2H4V4.13a7.24 7.24 0 0 0-3 5.94c0 .29-1 .93-1 .93v1h1.55c1.84-1.85 5-2 5.93-2s4.17.15 6 2H15v-1s-1-.64-1-.93z" }));
});

var _extends$130 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$130(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Feedback16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$130(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$130({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 8.58l1.42 3.58 4.83 3.38 1-1.14-3.7-3.1.24-.09-1.37-3.58zM6.91 11s.92-.46 1.23-.57c2.95-1.13 5.32-.78 7 1.77l.65-.25L11.29.31c-.21.09-.43.18-.65.25.47 3-1.05 4.85-4 6-.3.12-1.23.57-1.23.57zM.12 9.78l1 2.58 1-.36-1-2.6z" }));
});

var _extends$131 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$131(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Featured16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$131(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$131({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 10h2v2H7zm0-6h2v5H7zm1-4L6.46 2.77l-2.79-1.5.2 3.16-3.15.25L2.6 7.22.08 9.14l3 1.13L2 13.24l3.1-.65.69 3.09L8 13.45l2.25 2.22.69-3.09 3.1.65-1.09-3L16 9.1l-2.6-1.88 1.88-2.55-3.16-.25.2-3.16-2.78 1.51z" }));
});

var _extends$132 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$132(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Favorite16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$132(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$132({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 6H9.89L8 .19 6.11 6H0l4.94 3.59-1.88 5.82L8 11.81l4.94 3.6-1.88-5.82z" }));
});

var _extends$133 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$133(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var External16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$133(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$133({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.7 5.47L10.17 3H0v13h13V5.83L10.53 8.3 7.7 5.47z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.04 1.01l1.77 1.77-2.69 2.69 1.41 1.41 2.69-2.69 1.77 1.77 1-5.96-5.95 1.01z" }));
});

var _extends$134 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$134(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ExternalLink16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$134(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$134({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M10 11H4V5h2V3H2v10h10V9h-2v2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M7.23 2.96l1.59 1.58-3.47 3.47 1.64 1.64 3.47-3.47 1.58 1.59.98-5.79-5.79.98z" }));
});

var _extends$135 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$135(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ExportMove16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$135(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$135({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 6.18V5h-1.66L13 6.18zM9 0v4h4L9 0z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.02 14.94v-3.09H7v-4.7h3.02V5H8V0H0v15h13v-2.18l-2.98 2.12z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M11.02 8.15V6l4.92 3.5-4.92 3.5v-2.15H8v-2.7h3.02z" }));
});

var _extends$136 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$136(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ExpandPanel16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$136(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$136({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 15H0V1h7z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M11.02 6.15V4l4.92 3.5-4.92 3.5V8.85H8v-2.7h3.02z" }));
});

var _extends$137 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$137(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Error16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$137(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$137({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 0L0 8l8 8 8-8zm1 12H7v-2h2zM7 9V4h2v5z" }));
});

var _extends$138 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$138(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Education16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$138(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$138({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8.58 10.38a.55.55 0 0 1-.17-.05L4 8.54v2.05a.84.84 0 0 0 .14.41 7.68 7.68 0 0 0 4.36 2 7.64 7.64 0 0 0 4.36-2 .84.84 0 0 0 .14-.41V8.33l-4.24 2a.41.41 0 0 1-.18.05zM3 15V6.44l-1-.41V15h1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15.76 4.63l-7-3.49h-.14a.27.27 0 0 0-.13 0L.27 4.62A.39.39 0 0 0 0 5a.39.39 0 0 0 .21.38L2 6l5.54-2.46L9 4 3.46 6.65l5 2a.53.53 0 0 0 .17 0 .39.39 0 0 0 .18 0l4.24-2 2.78-1.29A.39.39 0 0 0 16 5a.41.41 0 0 0-.24-.37z" }));
});

var _extends$139 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$139(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Edit16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$139(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$139({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 16l4.92-1.38-3.54-3.54L0 16zm2.236-5.77l7-7 3.543 3.542-7 7zM13 0h-.31a1 1 0 0 0-.52.27l-2.09 2.11 3.54 3.54 2.09-2.09a1 1 0 0 0 .29-.52 1.94 1.94 0 0 0 0-.24V3a3 3 0 0 0-3-3z" }));
});

var _extends$140 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$140(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var EditProfile16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$140(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$140({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 7.29A3.91 3.91 0 0 0 0 11v4h5.24L6 12.4l1.94-1.94A3.94 3.94 0 0 0 4 7.29zm2.86 5.64L6 16l3.07-.86h.01l-2.22-2.22v.01zm.713-.71l4.1-4.102 2.214 2.213-4.101 4.102zM14.12 6h-.19a.62.62 0 0 0-.32.17l-1.23 1.24 2.21 2.21 1.23-1.23a.62.62 0 0 0 .18-.32v-.15A1.88 1.88 0 0 0 14.12 6z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("ellipse", { cx: "4", cy: "3.51", rx: "2.63", ry: "3" }));
});

var _extends$141 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$141(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Download16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$141(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$141({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M6.2 5.25H3.24L8.04 12l4.8-6.75H9.89V0H6.2v5.25z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 11v3H2v-3H0v5h16v-5h-2z" }));
});

var _extends$142 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$142(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Down16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$142(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$142({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M5.38 8.13h-3.5L7.56 16l5.69-7.87h-3.5V0H5.38v8.13z" }));
});

var _extends$143 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$143(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var DocumentManagement16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$143(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$143({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 8h4l-4-4v4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 14H9v-2h1v1h1V9H9V7H6v2H5V7H3v6h3v-1h1v2H2V6h7V4H0v12h14V9h-2v5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 0v3h8.41L15 7.59V12a1 1 0 0 0 1-1V0z" }));
});

var _extends$144 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$144(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Devices16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$144(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$144({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 0v5h1V1h12v10H8v2h8V0H2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 16h7V6H0zm1-9h5v7H1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 8h3v5H2zm12 2V2H4v3h4v5h6z" }));
});

var _extends$145 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$145(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var DeviceTablet16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$145(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$145({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 2v12h16V2zm13 11H1V3h12zm2-4h-1V7h1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 4h10v8H2z" }));
});

var _extends$146 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$146(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var DeviceMobile16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$146(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$146({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 2h6v10H5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 0v16h10V0zm6 15H7v-1h2zm3-2H4V1h8z" }));
});

var _extends$147 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$147(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var DeviceDesktop16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$147(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$147({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 12V1H0v11h6v2H2v1h12v-1h-4v-2zM1 11V2h14v9z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 3h12v7H2z" }));
});

var _extends$148 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$148(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Dashboard16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$148(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$148({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 2.15A7.51 7.51 0 0 0 .15 11 7.29 7.29 0 0 0 1 13.25a1 1 0 0 0 1.25.42l3-1.22A2.47 2.47 0 0 0 7 13.94a2.6 2.6 0 0 0 .56.06 2.52 2.52 0 0 0 2.28-1.55l3 1.23a1 1 0 0 0 1.16-.43 7.51 7.51 0 0 0-8-11.1zM4 11H1v-1h3zm.65-3.65l-2-2 .71-.71 2 2zm7-2.71l.71.71-.9.9a1.33 1.33 0 0 0-1-.46zM7 3h1v3H7zm2 8.86A1.5 1.5 0 0 1 7.23 13h-.07a1.46 1.46 0 0 1-1.07-1 1.51 1.51 0 0 1 .42-1.64l3.77-3.5a.29.29 0 0 1 .42 0 .3.3 0 0 1 .06.3S9.4 10.67 9 11.86zm2-.86v-1h3v1z" }));
});

var _extends$149 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$149(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CtaArrowRight16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$149(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$149({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 0a8 8 0 1 0 8 8 8 8 0 0 0-8-8zm.38 12V9.54H3V6.46h5.38V4L14 8z", fillRule: "evenodd" }));
});

var _extends$150 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$150(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Copy16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$150(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$150({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 3H7V0l3 3zM5 4h1V0H0v11h5V4zm11 4h-3V5l3 3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 9V5H6v11h10V9h-4z" }));
});

var _extends$151 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$151(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Complete16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$151(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$151({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 1a7 7 0 1 0 7 7 7 7 0 0 0-7-7zm-.87 10.9L3.86 9.15l.92-1.1 2.13 1.8L11 4.56l1.13.87z" }));
});

var _extends$152 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$152(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Compare16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$152(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$152({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M5.38 8L11 4 5.38 0v2.46H0v3.08h5.38V8zm5.25 2.46V8L5 12l5.63 4v-2.46H16v-3.08h-5.37z" }));
});

var _extends$153 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$153(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Comment16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$153(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$153({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 1v10h3v5l5-5h8V1zm9 7H3V7h6zm4-3H3V4h10z" }));
});

var _extends$154 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$154(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CollapsePanel16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$154(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$154({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 1h7v14H0z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M13 6V4L8.06 7.5 13 11V9h3V6h-3z" }));
});

var _extends$155 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$155(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Collaboration16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$155(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$155({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 4H0v12h12v-4H4V4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 0v4h8v8h4V0H4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 5h6v6H5z" }));
});

var _extends$156 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$156(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Cloud16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$156(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$156({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.5 8h-.05a4 4 0 1 0-7.31-3H6a3 3 0 0 0-3 3h-.5a2.5 2.5 0 0 0 0 5h11a2.5 2.5 0 0 0 0-5z" }));
});

var _extends$157 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$157(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ClosedCaptioning16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$157(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$157({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3.69 11.8c-.88 0-1.53-1-1.53-3.4S2.83 5 3.65 5 4.78 5.59 5 6.9l1.88-.55c-.35-2.08-1.34-3.12-3.2-3.12C1.41 3.23.11 5.1.11 8.42s1.36 5.16 3.57 5.16c1.9 0 2.89-1.2 3.3-3.33l-1.86-.58c-.26 1.38-.71 2.13-1.43 2.13zm9.4-2.13c-.25 1.38-.7 2.13-1.42 2.13-.88 0-1.53-1-1.53-3.4S10.8 5 11.63 5s1.12.59 1.37 1.9l1.89-.55c-.35-2.07-1.34-3.12-3.21-3.12-2.29 0-3.59 1.87-3.59 5.19s1.35 5.16 3.57 5.16c1.9 0 2.89-1.2 3.3-3.33z" }));
});

var _extends$158 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$158(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Clock16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$158(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$158({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 1a7 7 0 1 0 7 7 7 7 0 0 0-7-7zm4 8H7V3h1v5h4z" }));
});

var _extends$159 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$159(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Checkmark16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$159(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$159({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M.52 8.77L2.28 7l3.54 3.53 7.9-7.9 1.76 1.77-9.66 9.67-5.3-5.3z" }));
});

var _extends$160 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$160(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Checklist16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$160(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$160({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 3h1v1H4zm0 4h1v1H4zm0 4h1v1H4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1 0v15h14V0zm5 13H3v-3h3zm0-4H3V6h3zm0-4H3V2h3zm7 7H8v-1h5zm0-4H8V7h5zm0-4H8V3h5z" }));
});

var _extends$161 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$161(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Chat16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$161(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$161({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 5v4H5.41L5 9.41V13h6l3 3v-3h2V5h-4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 8h6V0H0v8h2v3l3-3z" }));
});

var _extends$162 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$162(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CartFull16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$162(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$162({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4.07 4l-.29-1.17-2.76-1.68-1.04 1.7L2.03 4.1 4 12h10l2-8H4.07z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "11.5", cy: "14.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "5.5", cy: "14.5", r: "1.5" }));
});

var _extends$163 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$163(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CartEmpty16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$163(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$163({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4.07 4l-.29-1.17L1 1.15 0 2.86 2 4.1 4 12h10l2-8zm8.37 6H5.56l-1-4h8.88z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "11.5", cy: "14.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "5.5", cy: "14.5", r: "1.5" }));
});

var _extends$164 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$164(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretUp16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$164(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$164({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1.71 10.03l6.3-6.3 6.3 6.3-1.77 1.77-4.53-4.54-4.54 4.54-1.76-1.77z" }));
});

var _extends$165 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$165(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretRight16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$165(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$165({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4.28 12.63l4.54-4.54-4.54-4.53 1.77-1.77 6.3 6.3-6.3 6.31-1.77-1.77z" }));
});

var _extends$166 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$166(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretLeft16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$166(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$166({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3.65 8.09l6.3-6.3 1.77 1.77-4.54 4.53 4.54 4.54-1.77 1.77-6.3-6.31z" }));
});

var _extends$167 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$167(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretDown16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$167(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$167({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1.69 6.13l1.77-1.76L7.99 8.9l4.54-4.53 1.76 1.76-6.3 6.31-6.3-6.31z" }));
});

var _extends$168 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$168(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Calendar16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$168(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$168({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M1 2v12h13V2zm4 11H2v-2h3zm0-3H2V8h3zm0-3H2V5h3zm4 6H6v-2h3zm0-3H6V8h3zm0-3H6V5h3zm4 6h-3v-2h3zm0-3h-3V8h3zm0-3h-3V5h3z" }));
});

var _extends$169 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$169(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Building16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$169(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$169({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 8V2h-1v14h2V8h-1zM3 16h3v-3h3v3h3V0H3zM8 2h2v2H8zm0 3h2v2H8zm0 3h2v2H8zM5 2h2v2H5zm0 3h2v2H5zm0 3h2v2H5zM1 8H0v8h2V2H1v6z" }));
});

var _extends$170 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$170(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Bookmark16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$170(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$170({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fill: "#231f20", d: "M12 16l-4.49-4L3 16V0h9v16z" }));
});

var _extends$171 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$171(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Billing16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$171(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$171({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 0H1v16h13zm-2 14H7v-1h5zm0-2H3V2h9z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 9h4v2H7zm0-3h4v2H7zM4 6h2v2H4zm0-3h2v2H4zm3 0h4v2H7zM4 9h2v2H4z" }));
});

var _extends$172 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$172(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var BillStack16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$172(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$172({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 2h4v12H6zm-6 9h5v1H0zm11 0h5v1h-5zM0 10h5V8H2V4h3V2H0v8zm11-8v2h3v4h-3v2h5V2h-5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 5h2v2H3zm8 0h2v2h-2zm0 8h5v1h-5zM0 13h5v1H0z" }));
});

var _extends$173 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$173(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Back16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$173(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$173({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M7.88 5.38v-3.5L0 7.56l7.88 5.69v-3.5H16V5.38H7.88z" }));
});

var _extends$174 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$174(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Attachment16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$174(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$174({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 3v3h3l-3-3zm4 3v9H2v1h13V6h-1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 7H9V3H6v4.5a1.5 1.5 0 0 1-3 0V5h1v2.5a.5.5 0 0 0 1 0V3H3V2a1 1 0 0 1 2 0v1h1V2a2 2 0 0 0-4 0v1H0v11h13z" }));
});

var _extends$175 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$175(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Attach16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$175(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$175({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 3v4h4l-4-4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 3H7v4.5a1.5 1.5 0 0 1-3 0V5h1v2.5a.5.5 0 0 0 1 0V3H4V2a1 1 0 0 1 2 0v1h1V2a2 2 0 0 0-4 0v1H0v12h16V8h-5z" }));
});

var _extends$176 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$176(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var AreaPost16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$176(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$176({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 0h2v2H0zm4 0h2v2H4zm4 0h2v2H8zM4 12h2v2H4zm8-12h2v2h-2zm0 4h2v2h-2zm4 7h-3V8h-2v3H8v2h3v3h2v-3h3v-2zM0 4h2v2H0zm0 4h2v2H0zm0 4h2v2H0z" }));
});

var _extends$177 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$177(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Archive16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$177(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$177({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M0 1h16v3H0zm1 14h14V5H1zm3-7h8v1H4z" }));
});

var _extends$178 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$178(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Alert16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$178(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$178({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 11h2v2H7zm0-6h2v5H7zm.73-3.86L0 14.72c-.12.19 0 .28.28.28h15.4c.23 0 .4-.09.28-.28L8.27 1.14a.33.33 0 0 0-.54 0z" }));
});

var _extends$179 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$179(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Add16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$179(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$179({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 1v5H1v3h5v5h3V9h5V6H9V1z" }));
});

var _extends$180 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$180(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var AddPage16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$180(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$180({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M9 0v4h4L9 0z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 11V9h3V5H8V0H0v15h8v-4h2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 10v2H9v2h2v2h2v-2h2v-2h-2v-2h-2z" }));
});

var _extends$181 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$181(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var AddMember16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$181(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$181({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 6.63c1.51 0 2.66-1.63 2.66-3.18A2.87 2.87 0 0 0 6 .5a2.72 2.72 0 0 0-2.66 3c0 1.79 1.15 3.13 2.66 3.13zm0 .49a4.12 4.12 0 0 0-4 4.33V15h6v-4h2a4.1 4.1 0 0 0-4-3.88z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 12v-2h-2v2H9v2h2v2h2v-2h2v-2h-2z" }));
});

var _extends$182 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$182(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var AddFolder16SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$182(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$182({ xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6.34 3l-1-1H0v2h5.34l1-1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M9 11h2V9h3V3H8L6 5H0v9h9v-3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 10v2h-2v2h2v2h2v-2h2v-2h-2v-2h-2z" }));
});

var _extends$183 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$183(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ZoomOut24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$183(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$183({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16.44 14.32l-.56.56-.46-.46A6.92 6.92 0 0 0 17 10a7 7 0 1 0-7 7 6.92 6.92 0 0 0 4.42-1.58l.46.46-.56.56 5.33 5.32 2.11-2.11zM10 16a6 6 0 1 1 6-6 6 6 0 0 1-6 6zm5.74.44l.7-.7 3.91 3.91-.7.7z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 9H7v2h6V9z" }));
});

var _extends$184 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$184(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ZoomIn24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$184(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$184({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16.44 14.32l-.56.56-.46-.46A6.92 6.92 0 0 0 17 10a7 7 0 1 0-7 7 6.92 6.92 0 0 0 4.42-1.58l.46.46-.56.56 5.33 5.32 2.11-2.11zM10 16a6 6 0 1 1 6-6 6 6 0 0 1-6 6zm5.74.44l.7-.7 3.91 3.91-.7.7z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 9h-2V7H9v2H7v2h2v2h2v-2h2V9z" }));
});

var _extends$185 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$185(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Volume24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$185(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$185({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 9H2v7h3l7 5V4zm0 6H3v-5h2zm6 4.06l-5-3.57v-6l5-3.57zm10-6.56a9.45 9.45 0 0 1-3.5 7.35l.62.8a10.5 10.5 0 0 0 0-16.3l-.62.8A9.45 9.45 0 0 1 21 12.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 12.5a6.5 6.5 0 0 1-2.33 5l.61.79a7.48 7.48 0 0 0 0-11.54l-.61.79A6.5 6.5 0 0 1 18 12.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 12.5a3.48 3.48 0 0 1-1.16 2.59l.6.79a4.44 4.44 0 0 0 0-6.76l-.6.79A3.48 3.48 0 0 1 15 12.5z" }));
});

var _extends$186 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$186(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Visible24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$186(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$186({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 5.42C5.38 5.42 2 12 2 12s3.17 6.6 10 6.6A10.9 10.9 0 0 0 22 12s-3.65-6.58-10-6.58zm0 12.18c-5.13 0-8-4.19-8.85-5.58C4 10.62 7 6.42 12 6.42s8 4.3 8.87 5.67A9.75 9.75 0 0 1 12 17.6z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 8a4 4 0 1 0 4 4 4 4 0 0 0-4-4zm0 7a3 3 0 1 1 3-3 3 3 0 0 1-3 3z" }));
});

var _extends$187 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$187(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var UserInactive24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$187(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$187({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.21 19H5v-4.5a4.49 4.49 0 0 1 8.85-1.1 5.58 5.58 0 0 1 .87-.64 5.49 5.49 0 0 0-3.31-3.41A4.2 4.2 0 0 0 13 6a3.78 3.78 0 0 0-3.5-4A3.78 3.78 0 0 0 6 6a4.2 4.2 0 0 0 1.59 3.35A5.49 5.49 0 0 0 4 14.5V20h8.61a5.09 5.09 0 0 1-.4-1zM7 6a2.79 2.79 0 0 1 2.5-3A2.79 2.79 0 0 1 12 6a2.79 2.79 0 0 1-2.5 3A2.79 2.79 0 0 1 7 6z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17.5 13a4.5 4.5 0 1 0 4.5 4.5 4.49 4.49 0 0 0-4.5-4.5zM15 18v-1h5v1z" }));
});

var _extends$188 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$188(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Upload24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$188(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$188({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.1 2L6 10h3v6h6v-6h3zM14 9v6h-4V9H8l4.06-5.33L16 9z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 16v2H7v-2H3v6h18v-6zm3 5H4v-4h2v2h12v-2h2z" }));
});

var _extends$189 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$189(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Up24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$189(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$189({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.5 2L4 12h4v10h7V12h4zm2.5 9v10H9V11H6l5.5-7.33L17 11z", fillRule: "evenodd" }));
});

var _extends$190 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$190(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Unpin24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$190(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$190({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.35 8.23l.7.7 2.1-2.1 4 4L15.07 13l.7.7 1.89-1.88 1.08 1.09L22 9.6 14.4 2l-3.26 3.26 1.09 1.08zm4.05-4.82l6.19 6.19-1.85 1.84-.52-.52-5-5-.61-.61zm-1 12.11v3.44l-.09.08-8.35-8.35.08-.09h3.44l-1-1H4.63l-1.09 1.09 4.42 4.42-4.67 5.58 5.58-4.68 4.44 4.45 1.09-1.09v-2.85l-1-1zM4.413 5.12l.707-.707L19.588 18.88l-.707.707z" }));
});

var _extends$191 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$191(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Unlock24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$191(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$191({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 2a5 5 0 0 0-5 5v3H5v11h14V10h-2V7a2 2 0 0 1 4 0v2h3V7a5 5 0 0 0-5-5zm-1 9v9H6v-9zm5-3h-1V7a3 3 0 0 0-6 0v3h-1V7a4 4 0 0 1 8 0z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 13h2v5h-2z" }));
});

var _extends$192 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$192(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Unlink24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$192(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$192({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.6 16.08l-3.69 3.69a4 4 0 0 1-5.68-5.67L7.34 11a5.42 5.42 0 0 0 .55 2.7l-2 2a1.72 1.72 0 0 0 2.44 2.43L12 14.46l-1-1-1 1a2 2 0 0 1-.23-.18A4 4 0 0 1 8.57 11L7.22 9.7l-.58.58-3.11 3.11a5 5 0 1 0 7.08 7.09l3.7-3.7zm-5.06-1.63A4 4 0 0 0 9 15a2.75 2.75 0 0 0 .26.22h.05l.25.19-2 2a.72.72 0 1 1-1-1zm1.86-6.52L14 4.38a4 4 0 0 1 5.67 0 4 4 0 0 1 0 5.67l-3.11 3.11a5.42 5.42 0 0 0-.55-2.7l2-2a1.72 1.72 0 0 0-1.22-2.96 1.67 1.67 0 0 0-1.21.5L12 9.55l1 1 .9-.89c.07.06.15.11.22.18a4 4 0 0 1 1.16 3l1.46 1.47.44-.44 3.12-3.11a5 5 0 0 0-3.51-8.56 5 5 0 0 0-3.54 1.47L9.7 7.22zm4.92 1.77a4 4 0 0 0-.46-.56 2.75 2.75 0 0 0-.26-.22l-.25-.2 2-2a.74.74 0 0 1 .51-.2.73.73 0 0 1 .51.2.71.71 0 0 1 .21.51.68.68 0 0 1-.21.51zM3.296 4.003l.707-.707 16.71 16.708-.708.708z" }));
});

var _extends$193 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$193(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Undo24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$193(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$193({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 17.17C22 12.36 18.73 8 13.57 8H12V4L2 11.5 12 19v-4h2.5c3.84 0 5.5 1 7.5 3.78v-1.61zM14.5 14H11v3l-7.33-5.5L11 6v3h2.57c4.36 0 6.9 3.45 7.34 7-1.53-1.33-3.35-2-6.41-2z" }));
});

var _extends$194 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$194(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Unavailable24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$194(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$194({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.5 2a9.5 9.5 0 1 0 9.5 9.5A9.5 9.5 0 0 0 11.5 2zm0 18a8.5 8.5 0 1 1 8.5-8.5 8.51 8.51 0 0 1-8.5 8.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 9v5h13V9zm12 4H6v-3h11z" }));
});

var _extends$195 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$195(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Trash24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$195(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$195({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 5h-3V3.13A1.08 1.08 0 0 0 15 2H8a1.11 1.11 0 0 0-1 1.13V5H4v4h1v13h13V9h1zm-4-2v2h-1V4H9v1H8V3zm2 18H6V9h11zm1-13H5V6h13z" }));
});

var _extends$196 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$196(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Tag24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$196(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$196({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 3H3v9l10 10 9-9zm-8 8.59V4h7.59l9 9L13 20.59z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.5 6A1.5 1.5 0 1 0 9 7.5 1.5 1.5 0 0 0 7.5 6zm0 2a.5.5 0 1 1 .5-.5.5.5 0 0 1-.5.5zm3.481 1.05l.708-.707 4.66 4.66-.708.707zm-1.146 4.126l.707-.707 3.168 3.168-.708.707z" }));
});

var _extends$197 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$197(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Sync24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$197(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$197({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 13.81l-.22-9.6-2.87 2.07A9 9 0 0 0 3.67 8.61l3.8 1.3A5 5 0 0 1 12 7a4.94 4.94 0 0 1 3.68 1.65l-2.82 2.06zM16.42 8A5.94 5.94 0 0 0 7 8.7L5.06 8a8 8 0 0 1 13.08-1.08l.6.72 2-1.48.15 6.24-5.92-2 2.2-1.6zm.11 6.08a5 5 0 0 1-8.23 1.27l2.84-2.08-9.09-3.1.22 9.6 2.79-2a9 9 0 0 0 15.28-2.35zM12 20a8 8 0 0 1-6.17-2.91l-.6-.73-2 1.47-.15-6.25L9 13.6l-2.18 1.6.74.82a6 6 0 0 0 9.44-.73l2 .71a8 8 0 0 1-7 4z" }));
});

var _extends$198 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$198(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SurveyEquipment24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$198(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$198({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 6a3 3 0 1 0 3 3 3 3 0 0 0-3-3zm0 5a2 2 0 1 1 2-2 2 2 0 0 1-2 2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20 13v-1l-.82-5.74A1.47 1.47 0 0 0 17.72 5H17V3a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v2h-.72a1.47 1.47 0 0 0-1.46 1.26L4 12v1H3v4h1v2a1 1 0 0 0 1 1h1v1h2v1h8v-1h2v-1h1a1 1 0 0 0 1-1v-2h1v-4zM8 3h2v1a1 1 0 0 0 1 1h2a1 1 0 0 0 1-1V3h2v11H8zm0 12h8v1H8zm-2 4H5v-6.93l.81-5.66A.47.47 0 0 1 6.28 6H7v10H6zm11 1H7v-3h10zm2-1h-1v-3h-1V6h.72a.47.47 0 0 1 .47.41l.81 5.66z" }));
});

var _extends$199 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$199(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Support24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$199(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$199({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 3a9 9 0 1 0 9 9 9 9 0 0 0-9-9zm0 17a8 8 0 1 1 8-8 8 8 0 0 1-8 8z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16.81 16.62c-.33.43-1.32 1.56-2.37 1.28-3.91-1-7.32-6.81-6.26-10.79.27-1.05 1.74-1.35 2.28-1.42a.28.28 0 0 1 .28.16l1.11 2.74a.25.25 0 0 1-.1.29 4.59 4.59 0 0 1-2.13.72c-.52 2 1.49 5.31 3.38 5.81.12-.53 1.22-1.25 1.66-1.52A.25.25 0 0 1 15 14l1.85 2.35a.27.27 0 0 1-.04.27z" }));
});

var _extends$200 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$200(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Subscription24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$200(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$200({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 17.46V17H8v-2h4.58a5.44 5.44 0 0 1 .63-1H8v-2h7v.53a5.8 5.8 0 0 1 1-.4V5H4v13h8a4.89 4.89 0 0 1 0-.54zM8 6h7v2H8zm0 3h7v2H8zm-1 8H5v-2h2zm0-3H5v-2h2zm0-3H5V9h2zm0-3H5V6h2z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21.94 16.82l-1.4.13a2.84 2.84 0 0 1 .05.51 3.09 3.09 0 1 1-3.09-3.12 3.05 3.05 0 0 1 1.21.26l-.84 1.46 4-.37L20.21 12l-.79 1.37a4.27 4.27 0 0 0-1.92-.45 4.66 4.66 0 1 0 4.44 3.9z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.62 20H3V3h14v8.94h1V2H2v19h11.27a5.53 5.53 0 0 1-.65-1z", fillRule: "evenodd" }));
});

var _extends$201 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$201(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Sort24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$201(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$201({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 14V5h-5v9h-3l5.5 7.68L22 14zm-6 1h2V6h3v9h2.05l-3.55 5z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.5 2.32L2 10h3v9h5v-9h3zM9 9v9H6V9H4l3.5-5 3.55 5z", fillRule: "evenodd" }));
});

var _extends$202 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$202(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialYoutube24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$202(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$202({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21.58 6.75A2.5 2.5 0 0 0 19.81 5C18.25 4.56 12 4.56 12 4.56s-6.25 0-7.81.42a2.51 2.51 0 0 0-1.77 1.77A25.91 25.91 0 0 0 2 11.56a25.91 25.91 0 0 0 .42 4.81 2.51 2.51 0 0 0 1.77 1.77c1.56.42 7.81.42 7.81.42s6.25 0 7.81-.42a2.51 2.51 0 0 0 1.77-1.77 25.91 25.91 0 0 0 .42-4.81 25.91 25.91 0 0 0-.42-4.81zM10 14.56v-6l5.2 3z" }));
});

var _extends$203 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$203(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialTwitter24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$203(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$203({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 5.93a8.41 8.41 0 0 1-2.36.63 4.06 4.06 0 0 0 1.8-2.23 8.21 8.21 0 0 1-2.61 1 4.12 4.12 0 0 0-7.1 2.73 4.06 4.06 0 0 0 .11.92 11.71 11.71 0 0 1-8.45-4.2 4 4 0 0 0-.56 2 4 4 0 0 0 1.83 3.35 4.17 4.17 0 0 1-1.86-.5v.07a4.05 4.05 0 0 0 3.29 4 4.28 4.28 0 0 1-1.85.07 4.1 4.1 0 0 0 3.83 2.8A8.35 8.35 0 0 1 3 18.25a8.39 8.39 0 0 1-1-.06A11.75 11.75 0 0 0 8.29 20 11.48 11.48 0 0 0 20 8.75V8a8.28 8.28 0 0 0 2-2.07z" }));
});

var _extends$204 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$204(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialLinkedin24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$204(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$204({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19.67 3H4.33A1.31 1.31 0 0 0 3 4.29V19.7A1.31 1.31 0 0 0 4.32 21h15.35A1.31 1.31 0 0 0 21 19.7V4.3A1.31 1.31 0 0 0 19.67 3zM8.34 18.34H5.67V9.75h2.67zM7 8.57A1.55 1.55 0 1 1 8.55 7 1.54 1.54 0 0 1 7 8.57zm11.33 9.76h-2.66v-4.17c0-1 0-2.28-1.39-2.28s-1.6 1.09-1.6 2.21v4.25H10V9.75h2.56v1.17a2.79 2.79 0 0 1 2.53-1.39c2.7 0 3.2 1.78 3.2 4.09z", fill: "#231f20" }));
});

var _extends$205 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$205(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialInstagram24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$205(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$205({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 3.8c2.67 0 3 0 4 .06a5.66 5.66 0 0 1 1.86.34 3.35 3.35 0 0 1 1.9 1.9 5.41 5.41 0 0 1 .38 1.9c0 1.05.06 1.37.06 4s0 3-.06 4a5.66 5.66 0 0 1-.34 1.86 3.35 3.35 0 0 1-1.9 1.9 5.66 5.66 0 0 1-1.9.34c-1 0-1.37.06-4 .06s-3 0-4-.06a5.55 5.55 0 0 1-1.94-.3 3.35 3.35 0 0 1-1.9-1.9 5.6 5.6 0 0 1-.3-1.9c0-1.05-.06-1.37-.06-4s0-3 .06-4a5.6 5.6 0 0 1 .34-1.9 3.35 3.35 0 0 1 1.9-1.9A5.6 5.6 0 0 1 8 3.86c1.05 0 1.37-.06 4-.06M12 2c-2.72 0-3.06 0-4.12.06a7.22 7.22 0 0 0-2.43.46 5.1 5.1 0 0 0-2.93 2.93 7.22 7.22 0 0 0-.46 2.43C2 8.94 2 9.28 2 12s0 3.06.06 4.12a7.22 7.22 0 0 0 .46 2.43 5.12 5.12 0 0 0 2.92 2.92 7.22 7.22 0 0 0 2.43.46C8.94 22 9.28 22 12 22s3.06 0 4.12-.06a7.22 7.22 0 0 0 2.43-.46 5.12 5.12 0 0 0 2.92-2.92 7.22 7.22 0 0 0 .46-2.43c0-1.07.06-1.41.06-4.12S22 9 21.93 7.89a7.22 7.22 0 0 0-.46-2.43 5.12 5.12 0 0 0-2.92-2.92 7.22 7.22 0 0 0-2.43-.46C15.06 2 14.72 2 12 2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 6.86A5.14 5.14 0 1 0 17.14 12 5.14 5.14 0 0 0 12 6.86zm0 8.47A3.33 3.33 0 1 1 15.33 12 3.33 3.33 0 0 1 12 15.33z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "17.34", cy: "6.66", r: "1.2" }));
});

var _extends$206 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$206(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialFacebook24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$206(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$206({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19.37 3H4.63A1.63 1.63 0 0 0 3 4.63v14.74A1.63 1.63 0 0 0 4.63 21h8.27v-7.2h-2.7v-2.7h2.7v-2c0-2.32 1.37-3.59 3.38-3.59a19 19 0 0 1 2 .11V8.4h-1.37c-1.09 0-1.31.54-1.31 1.32v1.38h2.67l-.35 2.7H15.6V21h3.77A1.63 1.63 0 0 0 21 19.37V4.63A1.63 1.63 0 0 0 19.37 3z" }));
});

var _extends$207 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$207(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SocialBlog24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$207(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$207({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16.24 8C13 4.72 8.45 3 3 3H2v4h1c8.16 0 13.78 5.63 14 14.08l.07.92H21v-1c.06-5.32-1.59-9.83-4.76-13zM18 21c-.23-9-6.26-15-15-15V4c10.43 0 17.11 6.57 17 17z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 9H2v4h1c4.88 0 7.8 2.91 8 8v.14l.23.82H15V21c0-5.78-3.77-12-12-12zm9 12c-.22-5.66-3.58-9-9-9v-2c7.11 0 11 5.2 11 11z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5.5 16A2.5 2.5 0 1 0 8 18.5 2.5 2.5 0 0 0 5.5 16zm0 4A1.5 1.5 0 1 1 7 18.5 1.5 1.5 0 0 1 5.5 20z" }));
});

var _extends$208 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$208(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Share24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$208(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$208({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 16a3 3 0 0 0-2.14.9L8.7 13.29a2.9 2.9 0 0 0 0-2.59l5.16-3.61a3.08 3.08 0 1 0-.56-.82L8.12 9.88a3 3 0 1 0 0 4.23l5.16 3.61A3 3 0 0 0 13 19a3 3 0 1 0 3-3zm0-13a2 2 0 1 1-2 2 2 2 0 0 1 2-2zM6 14a2 2 0 1 1 2-2 2 2 0 0 1-2 2zm10 7a2 2 0 1 1 2-2 2 2 0 0 1-2 2z" }));
});

var _extends$209 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$209(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Settings24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$209(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$209({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 7.67A4.33 4.33 0 1 0 16.33 12 4.34 4.34 0 0 0 12 7.67zm0 7.66A3.33 3.33 0 1 1 15.33 12 3.33 3.33 0 0 1 12 15.33z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 14v-4h-2.95a7.22 7.22 0 0 0-.65-1.57l2.09-2.09-2.83-2.83-2.09 2.09A7.22 7.22 0 0 0 14 5V2h-4v3a7.22 7.22 0 0 0-1.57.65L6.34 3.51 3.51 6.34 5.6 8.43A7.22 7.22 0 0 0 5 10H2v4h3a7.22 7.22 0 0 0 .65 1.57l-2.14 2.09 2.83 2.83 2.09-2.09a7.22 7.22 0 0 0 1.57.65V22h4v-2.95a7.22 7.22 0 0 0 1.57-.65l2.09 2.09 2.83-2.83-2.09-2.09a7.22 7.22 0 0 0 .65-1.57zm-2.93 3.66l-1.41 1.41-1.91-1.91-.66.37a7.07 7.07 0 0 1-1.36.56l-.73.2V21h-2v-2.71l-.73-.2a7.07 7.07 0 0 1-1.36-.56l-.66-.37-1.91 1.91-1.41-1.41 1.91-1.92-.37-.66a6.73 6.73 0 0 1-.56-1.35l-.2-.73H3v-2h2.71l.2-.73a6.73 6.73 0 0 1 .56-1.35l.37-.66-1.91-1.92 1.41-1.41 1.91 1.91.66-.37a7.07 7.07 0 0 1 1.36-.56l.73-.2V3h2v2.71l.73.2a7.07 7.07 0 0 1 1.36.56l.66.37 1.91-1.91 1.41 1.41-1.91 1.92.37.66a6.73 6.73 0 0 1 .56 1.35l.2.73H21v2h-2.71l-.2.73a6.73 6.73 0 0 1-.56 1.35l-.37.66z" }));
});

var _extends$210 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$210(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Service24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$210(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$210({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 2A10 10 0 0 0 2 12a9.88 9.88 0 0 0 1.62 5.43v.05c.18.26.36.53.56.78l4.92-4.92-.23-.61a4.11 4.11 0 0 1 .93-4.37 4.07 4.07 0 0 1 2.89-1.2 4.19 4.19 0 0 1 1.31.2l-2.86 2.86.14 2.4 2.44.22L16.64 10a4.11 4.11 0 0 1-5.36 5.14l-.61-.23-4.21 4.17-.72.72c.25.2.52.38.78.56h.05A9.88 9.88 0 0 0 12 22a10 10 0 0 0 0-20zm0 19a9 9 0 0 1-4.7-1.34L10.92 16a5.1 5.1 0 0 0 6-7.74l-3.5 3.51-1.12-.1-.07-1.09 3.51-3.5a5.1 5.1 0 0 0-7.74 6L4.34 16.7A9 9 0 1 1 12 21z" }));
});

var _extends$211 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$211(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SelectNone24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$211(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$211({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 7v1h2v12H8v-2H7v3h14V7h-3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 3H3v14h14zm-1 13H4V4h12z" }));
});

var _extends$212 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$212(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var SelectAll24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$212(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$212({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 7v1h2v12H8v-2H7v3h14V7z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 3H3v14h14zm-1 13H4V4h12z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.45 7.79l-.71-.71-4.24 4.25-1.92-1.91-.7.7 2.62 2.63 4.95-4.96z" }));
});

var _extends$213 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$213(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Security24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$213(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$213({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 6a9.84 9.84 0 0 1-7-3 9.84 9.84 0 0 1-7 3H4c0 11.39 7.14 15 8 15s8-3.61 8-15.06c-.2.06-.64.06-1 .06zm-7 14c-.78-.33-6.59-3.2-7-13a10.83 10.83 0 0 0 7-2.61A10.83 10.83 0 0 0 19 7c-.41 9.77-6.22 12.64-7 13z" }));
});

var _extends$214 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$214(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Search24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$214(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$214({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16.44 13.32l-.56.56-.46-.46A6.92 6.92 0 0 0 17 9a7 7 0 1 0-7 7 6.92 6.92 0 0 0 4.42-1.58l.46.46-.56.56 5.33 5.32 2.11-2.11zM10 15a6 6 0 1 1 6-6 6 6 0 0 1-6 6zm5.74.44l.7-.7 3.91 3.91-.7.7z" }));
});

var _extends$215 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$215(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Save24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$215(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$215({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 3H3v15l3 3h15V3zm-1 1v7H7V4zM7 20v-5h10v5zm13 0h-2v-6H6v5.59l-2-2V4h2v8h12V4h2z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 16h2v3H8z" }));
});

var _extends$216 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$216(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Report24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$216(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$216({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 2v20h17V2zm16 19H4V3h15z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 19h-1v-7h-3v7h-1v-5h-3v5H9v-9H6v9H5v1h13zM8 19H7v-8h1zm4 0h-1v-4h1zm4 0h-1v-6h1zM5 5h13v1H5zm0 2h6v1H5z" }));
});

var _extends$217 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$217(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Redo24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$217(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$217({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 11.5L12 4v4h-1.57C5.27 8 2 12.36 2 17.17v1.61C4 16 5.66 15 9.5 15H12v4zM9.5 14c-3.06 0-4.88.7-6.41 2.07.44-3.59 3-7 7.34-7H13V6l7.33 5.5L13 17v-3z" }));
});

var _extends$218 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$218(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Recommend24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$218(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$218({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20.68 11.35A2.4 2.4 0 0 0 18.26 9h-4.6c.06-.2.13-.42.21-.65a12.11 12.11 0 0 0 .8-4.12.89.89 0 0 0 0-.23 2.09 2.09 0 0 0-2.08-2h-.17a2.14 2.14 0 0 0-2.09 2.16c0 1.19-1.56 3.22-2.69 4.7a11.86 11.86 0 0 0-1.84 2.78 2.27 2.27 0 0 1-.66.41A2 2 0 0 0 4 13.81V18a2 2 0 0 0 1.6 2 2.15 2.15 0 0 1 .54.39q.18.16.36.3A6.24 6.24 0 0 0 10.24 22h4.53A3.37 3.37 0 0 0 18 19.67a2.34 2.34 0 0 0 1.59-2.24 2.31 2.31 0 0 0-.11-.69 2.39 2.39 0 0 0 .52-3.69 2.39 2.39 0 0 0 .68-1.7zm-2.43 1.94a1.38 1.38 0 0 1 0 2.76.25.25 0 0 0-.23.21c0 .17.07.16.22.35a1.4 1.4 0 0 1 .28.82 1.37 1.37 0 0 1-1.27 1.37.16.16 0 0 0-.12.05.22.22 0 0 0-.06.13 2.35 2.35 0 0 1-2.35 2h-4.48a5.19 5.19 0 0 1-3.14-1.04c-.38-.29-.78-.78-1.33-.87a1 1 0 0 1-.77-1v-4.26a1 1 0 0 1 .57-.81c.45-.22 1-.54 1.14-.93.57-1.63 4.62-5.42 4.62-7.87A1.14 1.14 0 0 1 12.44 3h.08a1.09 1.09 0 0 1 1.15 1 .28.28 0 0 1 0 .13c0 2.79-1.21 4.37-1.21 5.82h5.8a1.39 1.39 0 0 1 1.42 1.37 1.41 1.41 0 0 1-1.43 1.42.26.26 0 0 0 0 .52z" }));
});

var _extends$219 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$219(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Quantities24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$219(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$219({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 15h-5v-4h4V2H10v9h4v4H2v5h2v2h16v-2h2zm-11-5V3h9v7zm4 1h1v4h-1zm4 10H5v-1h14zm2-2H3v-3h18z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 5v1h-5V5h5m1-1h-7v3h7zm-2 4h2v1h-2z" }));
});

var _extends$220 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$220(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Publish24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$220(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$220({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.5 2.32L7 10h3v6h5v-6h3zM14 9v6h-3V9H9l3.5-5 3.55 5z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 17v5h18v-5zm17 4H4v-3h16z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 19h12v1H6z" }));
});

var _extends$221 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$221(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Properties24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$221(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$221({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20 2H3v20h17zm-1 19H4V3h15z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 10h13V4H5zm1-5h11v4H6zm7 9v2h3v-2h2v-1h-2v-2h-3v2H5v1zm1-2h1v3h-1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 18v2h3v-2h8v-1h-8v-2H7v2H5v1zm1-2h1v3H8z" }));
});

var _extends$222 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$222(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ProjectManagement24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$222(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$222({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 6h10v1H5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 6H5v1h10V6zM5 9h10v1H5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 9H5v1h10V9zm1.5 2a5.5 5.5 0 1 0 5.5 5.5 5.5 5.5 0 0 0-5.5-5.5zm0 10a4.5 4.5 0 1 1 4.5-4.5 4.49 4.49 0 0 1-4.5 4.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 16v-3h-1v4h4v-1h-3zm-6.82 2H3V3h14v7a6.72 6.72 0 0 1 1 .16V2H2v17h8.5a6.85 6.85 0 0 1-.32-1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.82 12H5v1h6a6.51 6.51 0 0 1 .82-1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.82 12H5v1h6a6.32 6.32 0 0 1 .79-1z" }));
});

var _extends$223 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$223(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Profile24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$223(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$223({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zM8.12 20.11a.4.4 0 0 1-.12-.23v-2.3c0-3.63 2.51-4.17 4-4.17s4 .54 4 4.17v2.47a8.88 8.88 0 0 1-7.88.06zM12 12c-1.2 0-2-1.29-2-2.5S10.8 7 12 7s2 1.29 2 2.5-.8 2.5-2 2.5zm4 8.05a8.91 8.91 0 0 0 1-.57v-1.9c0-2.71-1.32-4.49-3.59-5A3.7 3.7 0 0 0 15 9.5C15 7.82 13.85 6 12 6S9 7.82 9 9.5a3.7 3.7 0 0 0 1.59 3.08c-2.26.51-3.59 2.29-3.59 5v1.89a8.23 8.23 0 0 0 1.12.63 9 9 0 1 1 7.88-.05z" }));
});

var _extends$224 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$224(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ProductsAndServices24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$224(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$224({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20.42 5.89l-8-3.7a1 1 0 0 0-.84 0l-8 3.7a1 1 0 0 0-.58.9v8.91a1 1 0 0 0 .47.84l8 5a1 1 0 0 0 1.06 0l8-5a1 1 0 0 0 .47-.84V6.79a1 1 0 0 0-.58-.9zM20 15.56a.24.24 0 0 1-.12.21l-7.75 4.82a.24.24 0 0 1-.26 0l-7.75-4.82a.24.24 0 0 1-.12-.21V7a.24.24 0 0 1 .15-.22l7.75-3.63a.23.23 0 0 1 .2 0l7.75 3.58A.24.24 0 0 1 20 7z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18.93 7.4l-.39-.18a.08.08 0 0 0-.09 0l-6.45 4-6.45-4a.07.07 0 0 0-.09 0l-.39.18a.12.12 0 0 0-.07.11V8a.11.11 0 0 0 0 .08l6.46 4v7a.11.11 0 0 0 0 .08l.4.24a.09.09 0 0 0 .12 0l.4-.24a.11.11 0 0 0 0-.08v-7l6.46-4A.14.14 0 0 0 19 8v-.5a.12.12 0 0 0-.07-.1z" }));
});

var _extends$225 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$225(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Print24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$225(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$225({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 6V2H6v4H2v12h4v4h12v-4h4V6zM7 3h10v6H7zm10 18H7v-5h1v4h8v-4h1zm-8-5h6v3H9zm12 1h-3v-2H6v2H3V7h3v3h12V7h3z" }));
});

var _extends$226 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$226(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackStop24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$226(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$226({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 4v16h16V4zm15 15H5V5h14z" }));
});

var _extends$227 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$227(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackShuffle24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$227(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$227({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 15l-2.14-3L15 9v3l7-5-7-5v3a4.41 4.41 0 0 0-3.44 1.73l-1.23 1.72-.51-.72A4.36 4.36 0 0 0 6.35 6H2v4h4.35l1.43 2-1.43 2H2v4h4.35a4.36 4.36 0 0 0 3.47-1.73l.51-.72 1.23 1.72A4.41 4.41 0 0 0 15 19v3l7-5-7-5zM7.16 9.42A1 1 0 0 0 6.35 9H3V7h3.35A3.35 3.35 0 0 1 9 8.31l.71 1-1.31 1.83zM9 15.69A3.35 3.35 0 0 1 6.35 17H3v-2h3.35a1 1 0 0 0 .81-.42l5.22-7.26A3.33 3.33 0 0 1 15 6h1V3.94L20.28 7 16 10.06V8h-1a1 1 0 0 0-.81.42zm7-1.75L20.28 17 16 20.06V18h-1a3.33 3.33 0 0 1-2.66-1.32l-1.43-2 1.3-1.83 1.94 2.72A1 1 0 0 0 15 16h1z" }));
});

var _extends$228 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$228(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackRewind24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$228(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$228({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 12l10 7V5zm9 5.08L13.74 12 21 6.92zM2 12l10 7V5zm9 5.08L3.74 12 11 6.92z" }));
});

var _extends$229 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$229(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackRecord24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$229(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$229({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 4a8 8 0 1 0 8 8 8 8 0 0 0-8-8zm0 15a7 7 0 1 1 7-7 7 7 0 0 1-7 7z" }));
});

var _extends$230 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$230(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackPrevious24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$230(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$230({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 12l11 7V5zm10 5.18L8.86 12 17 6.82zM6 4v16h1V4H6z" }));
});

var _extends$231 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$231(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackPlay24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$231(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$231({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 3v18l14-9zm1 1.83L18.15 12 7 19.17z" }));
});

var _extends$232 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$232(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackPause24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$232(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$232({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 20h7V4H4zM5 5h5v14H5zm8-1v16h7V4zm6 15h-5V5h5z" }));
});

var _extends$233 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$233(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackNext24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$233(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$233({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 19l11-7L6 5zM7 6.82L15.14 12 7 17.18zM17 4v16h1V4h-1z" }));
});

var _extends$234 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$234(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackLoop24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$234(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$234({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 7L8 2v3H6a4 4 0 0 0-4 4v6a4 4 0 0 0 4 4h1v-4H6V9h2v3zM6 8a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1v2a3 3 0 0 1-3-3V9a3 3 0 0 1 3-3h3V3.94L13.28 7 9 10.06V8H6zm12-3h-1v4h1v6h-2v-3l-7 5 7 5v-3h2a4 4 0 0 0 4-4V9a4 4 0 0 0-4-4zm3 10a3 3 0 0 1-3 3h-3v2.06L10.72 17 15 13.94V16h3a1 1 0 0 0 1-1V9a1 1 0 0 0-1-1V6a3 3 0 0 1 3 3z" }));
});

var _extends$235 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$235(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackGoToStart24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$235(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$235({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 6l-9 5.4V6L3 12l10 6v-5.4l9 5.4zM12 16.23L4.94 12 12 7.77zm9 0L13.94 12 21 7.77zM2 4v16h1V4H2z" }));
});

var _extends$236 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$236(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackGoToEnd24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$236(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$236({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 11.4L2 6v12l9-5.4V18l10-6-10-6zm-8 4.83V7.77L10.06 12zm9-8.46L19.06 12 12 16.23zM21 4v16h1V4h-1z" }));
});

var _extends$237 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$237(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PlaybackFastForward24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$237(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$237({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 19l10-7L2 5zM3 6.92L10.26 12 3 17.08zM22 12L12 5v14zm-9-5.08L20.26 12 13 17.08z" }));
});

var _extends$238 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$238(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Placeholder24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$238(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$238({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20.11 6.17l-.81.59a9.07 9.07 0 0 1 1.33 2.7l1-.28a9.81 9.81 0 0 0-1.52-3.01zm-4.66-2.48A9 9 0 0 1 18 5.3l.66-.75a9.84 9.84 0 0 0-2.81-1.78zm4.49 12.55a9.08 9.08 0 0 1-1.85 2.37l.68.74a10.13 10.13 0 0 0 2.05-2.64zM2.09 10.75l1 .13A8.81 8.81 0 0 1 4 8l-.9-.45a10 10 0 0 0-1.01 3.2zM21 12a8.63 8.63 0 0 1-.34 2.42l1 .26A10.1 10.1 0 0 0 22 12v-.68l-1 .08zM9 3.52a8.6 8.6 0 0 1 4.55-.37l.17-1a9.75 9.75 0 0 0-5.05.41zM5 6.34a8.77 8.77 0 0 1 2.25-2l-.53-.86a9.92 9.92 0 0 0-2.5 2.2zm2.06 13.17a9.15 9.15 0 0 1-2.22-2.08L4 18a10 10 0 0 0 2.47 2.31zm9.45.26a8.81 8.81 0 0 1-2.8 1.06l.19 1a9.91 9.91 0 0 0 3.1-1.19zM3.83 15.76A9 9 0 0 1 3 12.83l-1 .09a10 10 0 0 0 .87 3.26zM11.74 22v-1a9 9 0 0 1-3-.59l-.36.94a9.88 9.88 0 0 0 3.36.65z" }));
});

var _extends$239 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$239(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Pin24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$239(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$239({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14.4 2l-3.26 3.26 1.09 1.08L9 9.6H4.63l-1.09 1.09L8 15.11l-4.71 5.58L8.87 16l4.44 4.45 1.09-1.09V15l3.26-3.26 1.08 1.09L22 9.6zm-1 17l-.09.08L5 10.69v-.09h4.39l4 4zm.5-4.84l-4-4 3.27-3.27 4 4zm4.32-3.2l-5-5-.61-.61 1.79-1.94 6.19 6.19-1.85 1.84z" }));
});

var _extends$240 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$240(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Photos24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$240(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$240({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 3v2h1V4h14v14h-1v1h2V3H5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.5 8a2.5 2.5 0 1 0 2.5 2.5A2.5 2.5 0 0 0 13.5 8zm0 4a1.5 1.5 0 1 1 1.5-1.5 1.5 1.5 0 0 1-1.5 1.5z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 6v16h16V6zm15 15H3v-3.38l2.43-3.09a1.63 1.63 0 0 1 2.63 0l.3.41 3 3.89a1.34 1.34 0 0 0 1.08.54 1.35 1.35 0 0 0 1-.48l1-1.22.18-.21a.66.66 0 0 1 .49-.22.63.63 0 0 1 .55.32L17 19.71zm-.43-3.93a1.63 1.63 0 0 0-1.43-.85 1.61 1.61 0 0 0-1.24.57l-1.21 1.43a.37.37 0 0 1-.57 0l-3.25-4.3a2.63 2.63 0 0 0-4.23 0L3 16V7h14v10.78z" }));
});

var _extends$241 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$241(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PermissionIndividual24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$241(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$241({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.33 10.54A3.9 3.9 0 0 0 15 7c0-2.43-1.29-4-3.35-4A3.69 3.69 0 0 0 8 6.72V7a3.94 3.94 0 0 0 1.87 3.54C7.35 11.17 6 13.33 6 17v4h11v-4c0-3.71-1.14-5.83-3.67-6.46zM9 7a2.72 2.72 0 0 1 2.42-3h.18C13.39 4 14 5.55 14 7s-.59 3-2.35 3A2.72 2.72 0 0 1 9 7.22 1.64 1.64 0 0 1 9 7zm7.08 13.08H7V17c0-5.11 2.67-5.71 4.63-5.71 3.12 0 4.47 1.76 4.47 5.71z" }));
});

var _extends$242 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$242(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var PermissionGroup24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$242(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$242({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M9.9 11.67a4.34 4.34 0 0 0 2-3.74 3.65 3.65 0 0 0-3.39-3.84A3.62 3.62 0 0 0 5 7.83v.09a4 4 0 0 0 2 3.72c-2.08.46-3 2.11-3 5.36v4h9v-3.7c0-3.3-.92-5.12-3.1-5.63zM6 7.92A2.61 2.61 0 0 1 8.4 5.1h.15a2.66 2.66 0 0 1 2.39 2.84c0 1.46-1 3.09-2.39 3.09A2.86 2.86 0 0 1 6 7.92zM5 20v-3c.08-3.6 1.22-4.37 3.1-4.45h.65c2 .09 3.25.94 3.25 4.84V20zm12.5-6.84a3.66 3.66 0 0 0 1.5-3.07 3 3 0 0 0-2.71-3.26h-.11a3.15 3.15 0 0 0-3.1 3.2v.07a3.52 3.52 0 0 0 1.63 3.05 3 3 0 0 0-1.07.52l.62.78a2.59 2.59 0 0 1 1.55-.45h.54C18 14 19 14.72 19 17.89V20h-5v1h6v-3.11c0-2.75-.79-4.24-2.5-4.73zm-3.42-3.07a2.15 2.15 0 0 1 2-2.27h.09A2.06 2.06 0 0 1 18 10.09c0 1.17-.74 2.47-1.82 2.47a2.27 2.27 0 0 1-2.1-2.45z" }));
});

var _extends$243 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$243(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Paste24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$243(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$243({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 19H3V5h1v2h9V5h1v3h1V4h-2a1 1 0 0 0-1-1h-1V2H6v1H5a1 1 0 0 0-1 1H2v16h8zM5 4h2V3h3v1h2v2H5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 9h-8v13h11V12zm2 12h-9V10h7v2h2z" }));
});

var _extends$244 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$244(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var OnlineViewers24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$244(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$244({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8.75 18.67A15.37 15.37 0 0 1 7.63 16h1.74c.24-.26.57-.61 1-1h-3A17 17 0 0 1 7 12h3.62v2.75c.3-.27.63-.55 1-.83V12h3.62v.19a3.84 3.84 0 0 1 .76-.08h.22V12h3a8.75 8.75 0 0 1-.22 1 7.17 7.17 0 0 1 .75.46 8 8 0 0 0 .25-2A8.76 8.76 0 0 0 11 3a8.76 8.76 0 0 0-9 8.5 8.76 8.76 0 0 0 9 8.5c-.43-.37-.81-.73-1.12-1.05a10 10 0 0 1-1.13-.28zM19.18 11h-3a16.78 16.78 0 0 0-.33-3h2.36a8.22 8.22 0 0 1 .97 3zm-1.64-4h-1.89a14.44 14.44 0 0 0-.79-2.12A7.72 7.72 0 0 1 17.54 7zm-5.91-3a5.53 5.53 0 0 1 1.87.33A15.37 15.37 0 0 1 14.62 7h-3zm3.23 4a16.14 16.14 0 0 1 .39 3h-3.62V8zm-4.24 3H7a17 17 0 0 1 .39-3h3.24zM8.75 4.33A5.53 5.53 0 0 1 10.62 4v3h-3a15.37 15.37 0 0 1 1.13-2.67zm-1.36.55A14.44 14.44 0 0 0 6.6 7H4.71a7.72 7.72 0 0 1 2.68-2.12zM4 8h2.36A16.78 16.78 0 0 0 6 11H3a8.22 8.22 0 0 1 1-3zm0 7a8.22 8.22 0 0 1-.93-3H6a16.78 16.78 0 0 0 .33 3zm.71 1H6.6a14.44 14.44 0 0 0 .79 2.12A7.72 7.72 0 0 1 4.71 16z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 13.36c-2.82 0-6 3.82-6 3.82S13.05 21 16 21s6-3.81 6-3.81-3.11-3.83-6-3.83zm0 5.82a2 2 0 1 1 2-2 2 2 0 0 1-2 2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "16", cy: "17.18", r: "1" }));
});

var _extends$245 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$245(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Notification24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$245(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$245({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 14.54V10a6 6 0 0 0-4-5.65V2h-4v2.35A6 6 0 0 0 6 10v4.54A5.4 5.4 0 0 0 3 19h6a3 3 0 0 0 6 0h6a5.4 5.4 0 0 0-3-4.46zM12 21a2 2 0 0 1-2-2h4a2 2 0 0 1-2 2zm-7.83-3a5.1 5.1 0 0 1 2.34-2.6l.49-.3V10a5 5 0 0 1 3.33-4.71l.67-.23V3h2v2.06l.67.23A5 5 0 0 1 17 10v5.1l.49.3a5.1 5.1 0 0 1 2.34 2.6z" }));
});

var _extends$246 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$246(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var NotificationOff24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$246(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$246({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10.33 5.29l.67-.23V3h2v2.06l.67.23A5 5 0 0 1 17 10v4.88l.58.58a5.48 5.48 0 0 1 2 2L21 18.87a5.47 5.47 0 0 0-3-4.33V10a6 6 0 0 0-4-5.65V2h-4v2.35a5.91 5.91 0 0 0-2.17 1.36l.71.71a5 5 0 0 1 1.79-1.13zM4.17 18a5.1 5.1 0 0 1 2.34-2.6l.49-.3V10a4.43 4.43 0 0 1 .07-.81l-.83-.83A5.91 5.91 0 0 0 6 10v4.54A5.4 5.4 0 0 0 3 19h6a3 3 0 0 0 6 0h1.88l-1-1zM12 21a2 2 0 0 1-2-2h4a2 2 0 0 1-2 2zm9-2.11l-.11.11H21v-.11zM4.413 5.12l.707-.707L19.588 18.88l-.707.707z" }));
});

var _extends$247 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$247(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var NotEditable24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$247(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$247({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17.62 3h-.34a1.16 1.16 0 0 0-.59.31L11.07 9l.71.71L15 6.47 17.53 9l-3.19 3.18.71.71 5.62-5.62a1.16 1.16 0 0 0 .33-.55 2.45 2.45 0 0 0 0-.27v-.07A3.38 3.38 0 0 0 17.62 3zM20 6.58l-1.77 1.76-2.56-2.57L17.4 4h.15A2.4 2.4 0 0 1 20 6.38v.2zM8.75 17.82l-2.57-2.57 3.48-3.47-.66-.71-4.4 4.39L3 21l5.54-1.55 4.39-4.4-.71-.71zm-.75.73l-3.57 1 1-3.57L8 18.53zM4.413 5.12l.707-.707L19.588 18.88l-.707.707z" }));
});

var _extends$248 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$248(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Mute24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$248(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$248({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 9H2v7h3l7 5V4zm0 6H3v-5h2zm6 4.06l-5-3.57v-6l5-3.57zm9.33-8.68l-.71-.71-2.12 2.12-2.12-2.12-.71.71 2.12 2.12-2.12 2.12.71.71 2.12-2.12 2.12 2.12.71-.71-2.12-2.12 2.12-2.12z" }));
});

var _extends$249 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$249(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var More24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$249(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$249({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18.5 10a2.5 2.5 0 1 0 2.5 2.5 2.5 2.5 0 0 0-2.5-2.5zm0 4a1.5 1.5 0 1 1 1.5-1.5 1.5 1.5 0 0 1-1.5 1.5zm-7-4a2.5 2.5 0 1 0 2.5 2.5 2.5 2.5 0 0 0-2.5-2.5zm0 4a1.5 1.5 0 1 1 1.5-1.5 1.5 1.5 0 0 1-1.5 1.5zm-7-4A2.5 2.5 0 1 0 7 12.5 2.5 2.5 0 0 0 4.5 10zm0 4A1.5 1.5 0 1 1 6 12.5 1.5 1.5 0 0 1 4.5 14z" }));
});

var _extends$250 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$250(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var MoreVertical24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$250(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$250({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.5 16a2.5 2.5 0 1 0 2.5 2.5 2.5 2.5 0 0 0-2.5-2.5zm0 4a1.5 1.5 0 1 1 1.5-1.5 1.5 1.5 0 0 1-1.5 1.5zm0-11a2.5 2.5 0 1 0 2.5 2.5A2.5 2.5 0 0 0 11.5 9zm0 4a1.5 1.5 0 1 1 1.5-1.5 1.5 1.5 0 0 1-1.5 1.5zm0-6A2.5 2.5 0 1 0 9 4.5 2.5 2.5 0 0 0 11.5 7zm0-4A1.5 1.5 0 1 1 10 4.5 1.5 1.5 0 0 1 11.5 3z" }));
});

var _extends$251 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$251(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Minimize24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$251(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$251({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M11 13.89V21H3v-8h7.13l.17-1H2v10h10v-8.28l-1 .17zm10.78-8.83L19 2.24l-4.29 4.24-2.12-2.12-1.42 8.48 8.49-1.41-2.12-2.12zm-4.2 5.7l-5.19.87.87-5.19 1.45 1.45L19 3.65l1.42 1.41-4.3 4.25z" }));
});

var _extends$252 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$252(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Maximize24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$252(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$252({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.53 4.41l2.12 2.12L11.18 10H2v12h12v-9.16l3.48-3.48 2.12 2.12L21 3zm6.4 5L17.48 8 13 12.42V21H3V11h8.59l4.47-4.47-1.45-1.45 5.18-.87z", fillRule: "evenodd" }));
});

var _extends$253 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$253(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ManageAccess24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$253(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$253({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20.43 18.33l.05-.18H22v-1.3h-1.52l-.05-.18a2.7 2.7 0 0 0-.27-.66l-.09-.16 1.07-1.07-.92-.92-1.07 1.07-.15-.09a2.7 2.7 0 0 0-.66-.27l-.18-.05V13h-1.3v1.52l-.18.05a2.7 2.7 0 0 0-.66.27l-.16.09-1.07-1.07-.92.92 1.07 1.07-.09.16a2.7 2.7 0 0 0-.27.66l-.05.18H13v1.3h1.52l.05.18a2.7 2.7 0 0 0 .27.66l.09.16-1.07 1.07.92.92 1.07-1.07.16.09a2.7 2.7 0 0 0 .66.27l.18.05V22h1.3v-1.52l.18-.05a2.7 2.7 0 0 0 .66-.27l.16-.09 1.07 1.07.92-.92-1.07-1.07.09-.16a2.7 2.7 0 0 0 .27-.66zm-2.93 1a1.79 1.79 0 1 1 1.79-1.79 1.79 1.79 0 0 1-1.79 1.75z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.21 19H5v-4.5a4.49 4.49 0 0 1 8.85-1.1 5.58 5.58 0 0 1 .87-.64 5.49 5.49 0 0 0-3.31-3.41A4.2 4.2 0 0 0 13 6a3.78 3.78 0 0 0-3.5-4A3.78 3.78 0 0 0 6 6a4.2 4.2 0 0 0 1.59 3.35A5.49 5.49 0 0 0 4 14.5V20h8.61a5.09 5.09 0 0 1-.4-1zM7 6a2.79 2.79 0 0 1 2.5-3A2.79 2.79 0 0 1 12 6a2.79 2.79 0 0 1-2.5 3A2.79 2.79 0 0 1 7 6z" }));
});

var _extends$254 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$254(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Mail24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$254(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$254({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 5H2v14h20zm-10 8L3.94 6H20zM3 6.46l5.79 5.05L3 17.29zm6.55 5.71l1.83 1.6a1 1 0 0 0 1.24 0l1.83-1.6L20.29 18H3.71zm5.66-.66l5.79-5v10.78z" }));
});

var _extends$255 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$255(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Lock24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$255(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$255({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 10V9A5 5 0 0 0 7 9v1H5v11h14V10zM8 9a4 4 0 0 1 8 0v1h-1V9a3 3 0 0 0-6 0v1H8zm6 0v1h-4V9a2 2 0 0 1 4 0zm4 11H6v-9h12z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 13h2v5h-2z" }));
});

var _extends$256 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$256(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Location24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$256(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$256({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 2a6.86 6.86 0 0 0-7 6.67 7.15 7.15 0 0 0 .4 2.42 81.08 81.08 0 0 0 4.82 8.07l1.34 2.13.44.71.42-.71c.69-1.16 1.66-2.7 2.62-4.24a64.33 64.33 0 0 0 3.53-5.93A6.8 6.8 0 0 0 19 8.67 6.86 6.86 0 0 0 12 2zm5.63 8.76c-.24.66-1.93 3.35-3.42 5.71-.8 1.28-1.6 2.54-2.24 3.59l-.88-1.4c-1.78-2.8-4.42-7-4.75-7.91A6.13 6.13 0 0 1 6 8.67 5.85 5.85 0 0 1 12 3a5.85 5.85 0 0 1 6 5.67 6 6 0 0 1-.37 2.09z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 6a3 3 0 1 0 3 3 3 3 0 0 0-3-3zm0 5a2 2 0 1 1 2-2 2 2 0 0 1-2 2z" }));
});

var _extends$257 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$257(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var List24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$257(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$257({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5.08 10h-3v4h3A.94.94 0 0 0 6 13v-2a.94.94 0 0 0-.88-1zM5 13H3v-2h2zm.08-10h-3v4h3A.94.94 0 0 0 6 6V4a.94.94 0 0 0-.88-1zM5 6H3V4h2zm.08 11h-3v4h3A.94.94 0 0 0 6 20v-2a.94.94 0 0 0-.88-1zM5 20H3v-2h2zM21 6H8V4h13zm.08-3H7v4h14.08A.94.94 0 0 0 22 6V4a.94.94 0 0 0-.88-1zM21 20H8v-2h13zm.08-3H7v4h14.08a.94.94 0 0 0 .92-1v-2a.94.94 0 0 0-.88-1zm0-7H7v4h14.08a.94.94 0 0 0 .92-1v-2a.94.94 0 0 0-.88-1zM21 13H8v-2h13z" }));
});

var _extends$258 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$258(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Link24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$258(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$258({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20.34 3.67a5 5 0 0 0-7.09 0L9 7.92a5.11 5.11 0 0 0-.52.63v-.12l-1.84 1.85-3.11 3.11a5 5 0 1 0 7.08 7.09l4.25-4.25a5.11 5.11 0 0 0 .52-.63v.12l1.85-1.85 3.12-3.11a5 5 0 0 0-.01-7.09zm-5.48 5.47a2.75 2.75 0 0 0-.26-.22l-.25-.2 2-2a.74.74 0 0 1 .51-.2.73.73 0 0 1 .51.2.71.71 0 0 1 .21.51.68.68 0 0 1-.21.51l-2 2a4 4 0 0 0-.51-.6zM9 15a2.75 2.75 0 0 0 .26.22h.05l.25.19-2 2a.72.72 0 1 1-1-1l2-2A4 4 0 0 0 9 15zm5.16.51l-4.25 4.26a4 4 0 0 1-5.68-5.67L7.34 11a5.42 5.42 0 0 0 .55 2.7l-2 2a1.72 1.72 0 0 0 2.44 2.43l4.25-4.25a1.72 1.72 0 0 0 0-2.43.28.28 0 0 1 0-.4l1.41-1.4c.07.06.15.11.22.18a4 4 0 0 1-.05 5.69zm5.47-5.47l-3.11 3.11a5.42 5.42 0 0 0-.55-2.7l2-2a1.72 1.72 0 0 0-1.18-2.95 1.67 1.67 0 0 0-1.21.5l-4.26 4.25a1.72 1.72 0 0 0 0 2.43.28.28 0 0 1 0 .4l-1.4 1.4a2 2 0 0 1-.23-.18 4 4 0 0 1 0-5.67L14 4.38a4 4 0 0 1 5.67 0 4 4 0 0 1-.04 5.67z" }));
});

var _extends$259 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$259(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Item24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$259(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$259({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20 21H4V3h16zM5 4v16h14V4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 7h10v1H7zm0 3h10v1H7zm0 3h10v1H7zm0 3h10v1H7z" }));
});

var _extends$260 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$260(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Info24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$260(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$260({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 3a9 9 0 1 0 9 9 9 9 0 0 0-9-9zm0 17a8 8 0 1 1 8-8 8 8 0 0 1-8 8z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13 12v-1h-3v1h1v4h-1v1h4v-1h-1v-4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "12", cy: "8", r: "1.25" }));
});

var _extends$261 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$261(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Inbox24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$261(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$261({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 2v20h18V2zm17 19H4v-5h5.18a3 3 0 0 0 5.64 0H20zM6 15V6h12v9h-4a2 2 0 0 1-4 0zm14 0h-1V5H5v10H4V3h16z" }));
});

var _extends$262 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$262(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Import24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$262(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$262({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 3H6v5h1V4h10v4h4v12H6v1h16V8zm1 4V5.41L19.59 7z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 14L7 9v3H2v4h5v3zM3 15v-2h5v-2l4.28 3L8 17v-2z", fillRule: "evenodd" }));
});

var _extends$263 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$263(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Home24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$263(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$263({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 9V4h-3v2l-4-4L2 12l1 2h1v7h16v-7h1l1-2zm-2-4h1v3h-1zm-3 15h-4v-2h1v-1h-1v-2h4zm5 0h-4v-6H9v6H5v-5.59l7-7 7 7zm1.38-7H19l-7-7-7 7H3.62l-.4-.8L12 3.41l8.78 8.79z" }));
});

var _extends$264 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$264(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Hierarchy24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$264(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$264({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21 16h-3v-3h-6v-2h6V2H5v9h6v2H5v3H2v6h7v-6H6v-2h11v2h-3v6h7zM6 3h11v7H6zm2 18H3v-4h5zm12 0h-5v-4h5z" }));
});

var _extends$265 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$265(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Hidden24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$265(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$265({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.23 9.11A3.25 3.25 0 0 1 12 9a3 3 0 0 1 3 3 3.25 3.25 0 0 1-.11.77l.79.79A3.89 3.89 0 0 0 16 12a4 4 0 0 0-4-4 3.89 3.89 0 0 0-1.56.32zm1.54 5.78A3.25 3.25 0 0 1 12 15a3 3 0 0 1-3-3 3.25 3.25 0 0 1 .11-.77l-.79-.79A3.89 3.89 0 0 0 8 12a4 4 0 0 0 4 4 3.89 3.89 0 0 0 1.56-.32zM3.296 4.003l.707-.707 16.71 16.708-.708.708z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M9.06 6.94A8.63 8.63 0 0 1 12 6.42c4.82 0 8 4.3 8.87 5.67a10.37 10.37 0 0 1-3 3.63l.7.71A11.52 11.52 0 0 0 22 12s-3.65-6.6-10-6.6a9.51 9.51 0 0 0-3.71.75zM15 17.16a10.34 10.34 0 0 1-3 .44c-5.13 0-8-4.19-8.85-5.58a13.91 13.91 0 0 1 3.19-3.56l-.72-.72A14.74 14.74 0 0 0 2 12s3.17 6.6 10 6.6a10.82 10.82 0 0 0 3.82-.66z" }));
});

var _extends$266 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$266(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Help24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$266(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$266({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 3a9 9 0 1 0 9 9 9 9 0 0 0-9-9zm0 17a8 8 0 1 1 8-8 8 8 0 0 1-8 8z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.35 6.33A3.15 3.15 0 0 0 9 9.23v.24l1.49.32v-.25c.23-1.19.83-1.77 1.83-1.77a1.77 1.77 0 0 1 1.15.38 1.18 1.18 0 0 1 .46 1 1.45 1.45 0 0 1-.48 1.08 6 6 0 0 1-.8.51 3.91 3.91 0 0 0-.77.5 1.55 1.55 0 0 0-.32.48 1.79 1.79 0 0 0-.17.6c0 .19-.08 1.44-.08 2.15v.25h1.5v-1.83a.89.89 0 0 1 .12-.55 2.73 2.73 0 0 1 .83-.55 2.91 2.91 0 0 0 1.85-2.61 2.66 2.66 0 0 0-.91-2.06 3.41 3.41 0 0 0-2.35-.79z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "12", cy: "17", r: "1" }));
});

var _extends$267 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$267(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Grid24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$267(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$267({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 11H3V3h8zM4 4v6h6V4zm17 7h-8V3h8zm-7-7v6h6V4zm-3 17H3v-8h8zm-7-7v6h6v-6zm17 7h-8v-8h8zm-7-7v6h6v-6z" }));
});

var _extends$268 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$268(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Graph24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$268(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$268({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2.88 14.83l-.76-.66 7.32-8.36 5 4 6.68-7.64.76.66-7.32 8.36-5-4-6.68 7.64zM18 9v13h4V9zm3 12h-2V10h2zm-8-7v8h4v-8zm3 7h-2v-6h2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 12v10h4V12zm3 9H9v-8h2zm-8-4v5h4v-5zm3 4H4v-3h2z" }));
});

var _extends$269 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$269(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Globe24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$269(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$269({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm8.94 9H18a16 16 0 0 0-.47-3h2.55a9 9 0 0 1 .86 3zm-1.46-4h-2.29a9.59 9.59 0 0 0-1.94-3.38A9.07 9.07 0 0 1 19.48 7zm-7 0V3.1C14 3.38 15.3 4.87 16.12 7zm3.95 1a13.94 13.94 0 0 1 .57 3h-4.5V8zM11.5 3.1V7H7.88C8.7 4.87 10 3.38 11.5 3.1zm0 4.9v3H7a13.85 13.85 0 0 1 .51-3zm0 4v3H7.31A14.41 14.41 0 0 1 7 12zm0 4v4.9c-1.71-.32-3.18-2.25-3.95-4.9zm1 4.9V16h3.95c-.77 2.65-2.24 4.58-3.95 4.9zm0-5.9v-3H17a14.41 14.41 0 0 1-.31 3zM8.75 3.62A9.59 9.59 0 0 0 6.81 7H4.52a9.07 9.07 0 0 1 4.23-3.38zM6.5 8a16.07 16.07 0 0 0-.5 3H3a8.86 8.86 0 0 1 1-3zM6 12a15.88 15.88 0 0 0 .28 3H3.53A8.78 8.78 0 0 1 3 12zm-2 4h2.5a10.38 10.38 0 0 0 2.22 4.37A9 9 0 0 1 4 16zm11.33 4.37A10.4 10.4 0 0 0 17.5 16h2.55a9 9 0 0 1-4.77 4.37zM20.47 15h-2.75a15.88 15.88 0 0 0 .28-3h3a8.78 8.78 0 0 1-.53 3z" }));
});

var _extends$270 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$270(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Forward24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$270(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$270({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 11.5L12 4v4H2v7h10v4zM3 14V9h10V6l7.33 5.5L13 17v-3z", fillRule: "evenodd" }));
});

var _extends$271 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$271(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Forums24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$271(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$271({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20 11.36A3.23 3.23 0 0 0 21 9a2.78 2.78 0 0 0-2.5-3A2.78 2.78 0 0 0 16 9a3.23 3.23 0 0 0 1 2.36 4 4 0 0 0-.56.35 5.08 5.08 0 0 1 .39 1A2.49 2.49 0 0 1 21 14.5V18h-4v1h5v-4.5a3.5 3.5 0 0 0-2-3.14zM18.5 11c-.81 0-1.5-.92-1.5-2s.69-2 1.5-2 1.5.92 1.5 2-.69 2-1.5 2zm-4.81-.61A3.69 3.69 0 0 0 15 7.5 3.28 3.28 0 0 0 12 4a3.28 3.28 0 0 0-3 3.5 3.69 3.69 0 0 0 1.31 2.89A4 4 0 0 0 8 14v5h8v-5a4 4 0 0 0-2.31-3.61zM10 7.5A2.3 2.3 0 0 1 12 5a2.3 2.3 0 0 1 2 2.5 2.3 2.3 0 0 1-2 2.5 2.3 2.3 0 0 1-2-2.5zM15 18H9v-4a3 3 0 0 1 6 0zm-8 0H3v-3.5a2.49 2.49 0 0 1 4.19-1.83 5.08 5.08 0 0 1 .39-1 4 4 0 0 0-.58-.31A3.23 3.23 0 0 0 8 9a2.78 2.78 0 0 0-2.5-3A2.78 2.78 0 0 0 3 9a3.23 3.23 0 0 0 1 2.36 3.5 3.5 0 0 0-2 3.14V19h5zM5.5 7C6.31 7 7 7.92 7 9s-.69 2-1.5 2S4 10.08 4 9s.69-2 1.5-2z" }));
});

var _extends$272 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$272(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Folder24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$272(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$272({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.79 6L9 4H2v16h19V6zM3 5h6l1.8 1.42L10 7H3zm17 14H3V8h7.26l1.52-1H20z" }));
});

var _extends$273 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$273(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FolderOpen24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$273(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$273({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18.36 20H2V4h6.65l3 2H20v3h1.69zM4.19 19h13.45l2.67-9H6.86zM3 5v14h.14l3-10H19V7h-7.65l-3-2z" }));
});

var _extends$274 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$274(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Flag24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$274(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$274({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 13.81a9.73 9.73 0 0 0 4-.88l-5.37-8.34a9.71 9.71 0 0 1-4.2 1 9.82 9.82 0 0 1-4.13-.9 4.72 4.72 0 0 0-2.12-.44A4.72 4.72 0 0 0 3.69 5l-.59-.92-.83.56L13.11 20.7l.83-.56-4.7-7a4.89 4.89 0 0 1 2.53-.72c2.03.03 3.23 1.4 6.23 1.39zm-9.81-2.18L4.25 5.78a3.72 3.72 0 0 1 1.93-.54 4 4 0 0 1 1.69.35 10.78 10.78 0 0 0 4.51 1 10.89 10.89 0 0 0 3.86-.73l4.24 6.59a8.9 8.9 0 0 1-2.47.37H18a8.81 8.81 0 0 1-3.38-.71 7.78 7.78 0 0 0-2.8-.64 5.78 5.78 0 0 0-3.11.89z" }));
});

var _extends$275 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$275(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Filter24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$275(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$275({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 4v2.33L10 12v8l4-2v-6l7-5.54V4zm17 2l-7 5.54v5.86l-2 1v-6.88L4 5.85V5h16z" }));
});

var _extends$276 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$276(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FilterTokens24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$276(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$276({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21 11H3V4h18zM4 5v5h16V5zm17 8H3v7h18zm-1 6H4v-5h16z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 6H5v3h3zM7 8H6V7h1zm1 7H5v3h3zm-1 2H6v-1h1z" }));
});

var _extends$277 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$277(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileZip24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$277(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$277({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 3H4v18h16V8zm1 2.41L17.59 7H16zM11 4h1v1h-1zm2 2v5h-3V6zm-2 7h-1v1h1v1h-1v1h1v1h-1v1h1v1h-1v1H5V4h5v1H9v7h2zm8 7h-7v-1h1v-1h-1v-1h1v-1h-1v-1h1v-1h-1v-1h1v-1h1V5h-1V4h2v4h4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 8h1v2h-1z" }));
});

var _extends$278 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$278(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileVideo24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$278(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$278({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21 7V4H2v3h1v10H2v3h19v-3h-1V7zm-2-2h1v1h-1zm-2 0h1v1h-1zm-2 0h1v1h-1zm-2 0h1v1h-1zm-2 0h1v1h-1zM9 5h1v1H9zM7 5h1v1H7zM5 5h1v1H5zM3 5h1v1H3zm1 14H3v-1h1zM4 7h7v10H4zm2 12H5v-1h1zm2 0H7v-1h1zm2 0H9v-1h1zm2 0h-1v-1h1zm2 0h-1v-1h1zm2 0h-1v-1h1zm2 0h-1v-1h1zm2 0h-1v-1h1zm-1-2h-7V7h7z" }));
});

var _extends$279 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$279(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileSpreadsheet24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$279(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$279({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20 4H4v16h16zM8 5h5v2H8zm0 3h5v2H8zm0 3h5v2H8zm0 3h5v2H8zm0 5v-2h5v2zm11 0h-5v-2h5zm0-3h-5v-2h5zm0-3h-5v-2h5zm0-3h-5V8h5zm0-3h-5V5h5z", fill: "#87bc40" }));
});

var _extends$280 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$280(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FilePresentation24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$280(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$280({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19.5 4h-16a.5.5 0 0 0 0 1H4v12h5.33l-2.11 2.92a.49.49 0 0 0 0 .7.51.51 0 0 0 .78-.05v-.07l2.56-3.5H11v3.38a.57.57 0 0 0 .5.63.57.57 0 0 0 .5-.62V17h.44L15 20.51a.5.5 0 0 0 .81-.58L13.67 17H19V5h.5a.5.5 0 0 0 0-1zM18 16H5V5h13z", fill: "#faa21b" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fill: "#faa21b", d: "M12 8h5v1h-5zm0 2h5v1h-5zm0 2h4v1h-4zM8.5 8a2.5 2.5 0 1 0 2.5 2.5A2.5 2.5 0 0 0 8.5 8zm0 4a1.5 1.5 0 1 1 1.5-1.5A1.5 1.5 0 0 1 8.5 12z" }));
});

var _extends$281 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$281(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FilePdf24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$281(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$281({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 3H4v18h16V8zm1 2.41L17.59 7H16zM19 20H5V4h10v4h4z", fill: "#ec4a41" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fill: "#ec4a41", d: "M7 13h10v1H7zm0 2h7v1H7zm5-4h5v1h-5zm0-2h5v1h-5zM7 9h4v3H7z" }));
});

var _extends$282 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$282(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FilePart24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$282(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$282({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20 11h-5V6H9v5H4v6h16zm-1 5H5v-4h5V7h4v5h5z", fill: "#ec4a41" }));
});

var _extends$283 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$283(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileImage24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$283(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$283({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 3v18h18V3zm17 17H4v-3.82l2.75-3.39A2 2 0 0 1 8.34 12a2 2 0 0 1 1.59.76l.3.4 3.35 4.31a1.41 1.41 0 0 0 2.17.06L17.13 16a.84.84 0 0 1 .65-.29.83.83 0 0 1 .73.41L20 18.48zm0-3.41l-.61-1a1.85 1.85 0 0 0-1.61-.92 1.89 1.89 0 0 0-1.4.62L15 16.87a.42.42 0 0 1-.63 0l-3.65-4.7a3 3 0 0 0-4.76 0L4 14.59V4h16z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15.5 11A2.5 2.5 0 1 0 13 8.5a2.5 2.5 0 0 0 2.5 2.5zm0-4A1.5 1.5 0 1 1 14 8.5 1.5 1.5 0 0 1 15.5 7z" }));
});

var _extends$284 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$284(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileGeneric24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$284(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$284({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 3H4v18h16V8zm1 2.41L17.59 7H16zM19 20H5V4h10v4h4z" }));
});

var _extends$285 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$285(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileDocument24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$285(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$285({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 3H4v18h16V8zm1 2.41L17.59 7H16zM19 20H5V4h10v4h4z", fill: "#0696d7" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fill: "#0696d7", d: "M7 13h10v1H7zm0 2h7v1H7zm0-4h10v1H7zm0-2h10v1H7z" }));
});

var _extends$286 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$286(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileAudio24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$286(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$286({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 9H2v7h3l7 5V4zm0 6H3v-5h2zm6 4.06l-5-3.57v-6l5-3.57zm10-6.56a9.45 9.45 0 0 1-3.5 7.35l.62.8a10.5 10.5 0 0 0 0-16.3l-.62.8A9.45 9.45 0 0 1 21 12.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 12.5a6.5 6.5 0 0 1-2.33 5l.61.79a7.48 7.48 0 0 0 0-11.54l-.61.79A6.5 6.5 0 0 1 18 12.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 12.5a3.48 3.48 0 0 1-1.16 2.59l.6.79a4.44 4.44 0 0 0 0-6.76l-.6.79A3.48 3.48 0 0 1 15 12.5z" }));
});

var _extends$287 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$287(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var FileAssembly24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$287(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$287({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20 6H9v5H4v6h16zm-1 10H5v-4h5V7h4v5h5zm0-5h-4V7h4z", fill: "#ec4a41" }));
});

var _extends$288 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$288(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Field24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$288(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$288({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20.83 15.82a4.42 4.42 0 0 1-1.25-2c0-4.7-3.36-8.54-7.58-8.83h-1c-4.22.29-7.58 4.13-7.58 8.83a4.42 4.42 0 0 1-1.25 2 .74.74 0 0 0-.17.43v.25a.5.5 0 0 0 .5.5H3l1.62 1.7a1 1 0 0 0 .72.31h12.29a1 1 0 0 0 .72-.31L20 17h.53a.5.5 0 0 0 .5-.5v-.25a.78.78 0 0 0-.2-.43zM17.63 18H5.37l-1-1v-.05C5.9 15.52 9 15 11.42 15s5.58.51 7.06 2l.05.05zM19 16c-2.2-1.85-6.26-2-7.49-2s-5.28.15-7.51 2h-.66a4.17 4.17 0 0 0 1.08-2.14A8 8 0 0 1 8 7v2.5a.5.5 0 0 0 1 0v-3a6.6 6.6 0 0 1 2-.5v2.5a.5.5 0 0 0 1 0V6a6.31 6.31 0 0 1 2 .49v3a.5.5 0 1 0 1 0V7a8 8 0 0 1 3.58 6.82A4.1 4.1 0 0 0 19.66 16z" }));
});

var _extends$289 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$289(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Feedback24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$289(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$289({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21.58 14.53c-.89-2.28-4.22-10-5-11.9a1 1 0 0 0-.58-.56.87.87 0 0 0-.45-.07 1 1 0 0 0-1 .79c-.55 3.38-1.75 4.47-2.88 5.2a7.34 7.34 0 0 1-1.46.64c-2.76.68-4.64 1.53-5.57 2.53a1.32 1.32 0 0 0-.49.93L3 13c-.73.59-.67 1.13-.49 1.47l1.11 2.33a1.16 1.16 0 0 0 1.08.74 1.78 1.78 0 0 0 .47-.07l1.45-.34c1 1.07 3.6 4.38 3.67 4.46.36.66 1.31.33 1.44.26l1.85-.64a1.15 1.15 0 0 0 .65-.86.84.84 0 0 0-.28-.74l-2.17-2.58a3.81 3.81 0 0 0 .52-.85.73.73 0 0 0-.06-.72.94.94 0 0 0-.81-.33l-1.27.2-.23-.33 1.78-.56a11.28 11.28 0 0 1 2.7-.3 8.93 8.93 0 0 1 5.59 1.6.82.82 0 0 0 .2.12 1 1 0 0 0 1.35-1.33zM5 16.52c-.3.08-.31.05-.41-.14l-1.15-2.33s0-.08.23-.25l.9-.73 1.14 2.67a4.62 4.62 0 0 0 .27.54zm6.46-3l-2 .64a1.18 1.18 0 0 0-.62.67.64.64 0 0 0 .09.54 7.88 7.88 0 0 0 .71.88.49.49 0 0 0 .44.16l1.12-.18a6.58 6.58 0 0 1-.41.53.5.5 0 0 0 0 .65l2.43 2.89-1.84.7a.41.41 0 0 1-.32 0c-.12-.16-3.05-3.84-3.85-4.66a3 3 0 0 1-.57-.91l-1.42-3.37.19-.21C6 11.26 7.3 10.39 10.5 9.6l.2-.06c.2.51.42 1.1.65 1.71s.54 1.45.8 2.09c-.45.08-.72.15-.73.15zm3-.34c-.45 0-.87 0-1.24.06-.27-.68-.58-1.51-.89-2.31-.23-.62-.46-1.22-.66-1.74a6 6 0 0 0 .64-.33C13.57 8 15 6.71 15.63 3c.81 1.92 4.1 9.55 4.94 11.89a9.9 9.9 0 0 0-6.16-1.74z" }));
});

var _extends$290 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$290(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Featured24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$290(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$290({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 13.66l-3.18-2.44L21.19 8l-4-.32.26-4-3.51 1.85L12 2l-1.94 3.53-3.52-1.91.26 4L2.81 8l2.37 3.25L2 13.66l3.74 1.44-1.38 3.79 3.91-.83.88 3.94L12 19.16 14.85 22l.88-3.94 3.91.83-1.38-3.79zm-7 3.22L14.27 20 12 17.75 9.73 20 9 16.88l-3.12.66 1.09-3L4 13.37l2.54-2-1.87-2.55 3.18-.25-.2-3.21 2.81 1.52L12 4.08l1.54 2.81 2.81-1.52-.21 3.21 3.18.25-1.89 2.59 2.54 2-3 1.15 1.09 3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 8h2v5h-2zm0 6h2v2h-2z" }));
});

var _extends$291 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$291(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Favorite24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$291(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$291({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 9.5h-7.64L12 2.24 9.64 9.5H2L8.18 14l-2.36 7.26L12 16.77l6.18 4.49L15.82 14zm-5.72 9.14L12 15.53l-4.28 3.11 1.64-5-4.28-3.14h5.29l1.63-5 1.63 5h5.29l-4.28 3.11z" }));
});

var _extends$292 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$292(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var External24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$292(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$292({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M17 20H4V6h9.76l.46-.46-.54-.54H3v16h15V10.24l-1 1V20z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M13.51 3.42l2.12 2.12L12.17 9 15 11.83l3.46-3.46 2.12 2.12L22 2zm6.37 4.95l-1.43-1.43L15 10.4 13.59 9l3.46-3.46-1.42-1.41 5.15-.91z", fillRule: "evenodd" }));
});

var _extends$293 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$293(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ExternalLink24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$293(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$293({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { fillRule: "evenodd", d: "M18 18H6V6h4V5H5v14h14v-5h-1v4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.88 5.05L14 7.17l-3.54 3.54 2.83 2.83L16.83 10 19 12.12l1.41-8.48zm6.36 5l-1.43-1.48-3.53 3.54-1.4-1.4 3.53-3.54L14 5.76l5.15-.91z", fillRule: "evenodd" }));
});

var _extends$294 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$294(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ExportMove24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$294(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$294({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 10.06V8l-5-5H2v18h16v-3.06l-1 .68V20H3V4h10v4h4v1.38zM14 7V5.41L15.59 7z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 14l-7-5v3h-5v4h5v3zm-11 1v-2h5v-2l4.28 3L16 17v-2z", fillRule: "evenodd" }));
});

var _extends$295 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$295(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ExpandPanel24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$295(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$295({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 12l-7-5v3h-4v4h4v3zm-10 1v-2h4V9l4.28 3L16 15v-2z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 3v18h8V3zm7 17H3V4h6z" }));
});

var _extends$296 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$296(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Error24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$296(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$296({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 2L2 12l10 10 10-10zM3.41 12L12 3.41 20.59 12 12 20.59z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.75 12.82h.5c.14 0 .25-.15.25-.33l.5-4.82c0-.19-.53-.67-.67-.67h-.66c-.14 0-.67.48-.67.67l.5 4.82c0 .18.11.33.25.33zM12 14a1 1 0 1 0 1 1 1 1 0 0 0-1-1z" }));
});

var _extends$297 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$297(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Education24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$297(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$297({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 4L2 9l3 1.5V20h3v-3.9a8.69 8.69 0 0 0 4 .9c3.31 0 6-1.57 6-3.5V11l4-2zM7 19H6v-8.5a.5.5 0 0 1 1 0zm10-5.5c0 1.18-2 2.5-5 2.5a7.15 7.15 0 0 1-4-1.06V12l4 2 5-2.5zm-5-.62l-4-2v-.38a1.62 1.62 0 0 0-.07-.44L12 9.49a.51.51 0 0 0 .43-.57.51.51 0 0 0-.57-.42l-4.7.66A1.46 1.46 0 0 0 6.5 9a1.48 1.48 0 0 0-1.16.56L4.21 9 12 5.12 19.74 9z" }));
});

var _extends$298 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$298(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Edit24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$298(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$298({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17.63 3h-.35a1.16 1.16 0 0 0-.59.31L4.55 15.46 3 21l5.54-1.55L20.67 7.31a1.16 1.16 0 0 0 .33-.59 2.45 2.45 0 0 0 0-.27v-.08A3.37 3.37 0 0 0 17.63 3zM15 6.47L17.53 9l-8.78 8.82-2.57-2.57zM4.44 19.56l1-3.57L8 18.53zM20 6.58l-1.77 1.76-2.56-2.57L17.4 4h.15A2.4 2.4 0 0 1 20 6.37v.21z" }));
});

var _extends$299 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$299(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var EditProfile24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$299(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$299({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12.86 18.93L12 22l3.07-.86h.01l-2.22-2.22v.01zm.718-.71l4.101-4.102 2.213 2.213-4.1 4.102zM20.12 12h-.19a.62.62 0 0 0-.32.17l-1.23 1.23 2.21 2.21 1.23-1.23a.62.62 0 0 0 .17-.32v-.15A1.88 1.88 0 0 0 20.12 12zM5 14.5a4.5 4.5 0 0 1 9 0v1.87l1-1v-.87a5.49 5.49 0 0 0-3.59-5.15A4.2 4.2 0 0 0 13 6a3.78 3.78 0 0 0-3.5-4A3.78 3.78 0 0 0 6 6a4.2 4.2 0 0 0 1.59 3.35A5.49 5.49 0 0 0 4 14.5V20h7.52l.28-1H5zM7 6a2.79 2.79 0 0 1 2.5-3A2.79 2.79 0 0 1 12 6a2.79 2.79 0 0 1-2.5 3A2.79 2.79 0 0 1 7 6z" }));
});

var _extends$300 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$300(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Download24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$300(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$300({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 8V2H9v6H6l6.1 8L18 8zm-2.92 6.33L8 9h2V3h4v6h2z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 16v2H7v-2H3v6h18v-6zm3 5H4v-4h2v2h12v-2h2z" }));
});

var _extends$301 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$301(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Down24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$301(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$301({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 12V2H8v10H4l7.5 10L19 12zm-9 1h3V3h5v10h3l-5.5 7.33z", fillRule: "evenodd" }));
});

var _extends$302 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$302(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var DocumentManagement24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$302(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$302({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 3v3h1V4h15v12h-1v1h2V3H5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 7H2v14h17v-9zm1 2.41L16.59 11H15zM18 20H3V8h11v4h4z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M9 13v-2h4v-1H4v8h7v-2h-1v1H5v-6h3v2h1zm8 0h-1v4h-2v-1h-1v2h4v-5z" }));
});

var _extends$303 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$303(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Devices24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$303(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$303({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15 16h7V3H4v4h1V4h16v11h-9v1h2v2h-2v1h7v-1h-4v-2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 8v14h9V8zm8 13H3v-2h7zm-7-3V9h7v9z" }));
});

var _extends$304 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$304(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var DeviceTablet24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$304(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$304({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 11h1v2h-1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 4v16h20V4zm1 15V5h14v14zm18 0h-3V5h3z" }));
});

var _extends$305 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$305(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var DeviceMobile24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$305(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$305({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 2v20h13V2zm12 19H6v-3h11zM6 17V3h11v14z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 19h3v1h-3z" }));
});

var _extends$306 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$306(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var DeviceDesktop24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$306(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$306({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 18h8V3H2v15h8v2H6v1h12v-1h-4v-2zm-1 2h-2v-2h2zM3 17V4h18v13z" }));
});

var _extends$307 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$307(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Dashboard24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$307(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$307({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.79 4A9.51 9.51 0 0 0 4.1 19.46L9.46 17a2.48 2.48 0 0 0 1.46 1 2.86 2.86 0 0 0 .58.06 2.46 2.46 0 0 0 2-1.05l5.37 2.51A9.41 9.41 0 0 0 21 13.55 9.61 9.61 0 0 0 11.79 4zM13 15.8a1.49 1.49 0 0 1-1.5 1.2 1.48 1.48 0 0 1-1.41-1 1.51 1.51 0 0 1 .42-1.64l4.11-3.49zm5.62 2.41L13.94 16l2.18-6.45 1-1-.71-.71-1.21 1.25-5.36 4.55A2.5 2.5 0 0 0 9.08 16l-4.66 2.21A8.5 8.5 0 0 1 11.5 5h.26A8.67 8.67 0 0 1 20 13.55a8.44 8.44 0 0 1-1.42 4.66z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 13h3v1H4zm12 0h3v1h-3zM5.847 8.547l.707-.707 2.122 2.121-.707.707zM11 6h1v3h-1z" }));
});

var _extends$308 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$308(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CtaArrowRight24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$308(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$308({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 19a9 9 0 1 1 9-9 9 9 0 0 1-9 9z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 10H6v4h5v3l7-5-7-5zm1-1l4.28 3L12 15v-2H7v-2h5z", fillRule: "evenodd" }));
});

var _extends$309 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$309(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Copy24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$309(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$309({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7 17H4V4h7v1h2l-2-2H3v15h4v-1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 6H8v15h13V11zm1 2.41L18.59 10H17zM20 20H9V7h7v4h4z" }));
});

var _extends$310 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$310(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Complete24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$310(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$310({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 3a9 9 0 1 0 9 9 9 9 0 0 0-9-9zm0 17a8 8 0 1 1 8-8 8 8 0 0 1-8 8z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16.29 8.73l-5.65 5.65-2.5-2.49-.71.71 3.21 3.2L17 9.43l-.71-.7z" }));
});

var _extends$311 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$311(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Compare24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$311(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$311({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M15.68 7.5L8 2v3H2v5h6v3zM3 9V6h6V4l5 3.5-5 3.55V9zm13 5v-3l-7.68 5.5L16 22v-3h6v-5zm5 4h-6v2.05l-5-3.55 5-3.5v2h6z", fillRule: "evenodd" }));
});

var _extends$312 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$312(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Comment24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$312(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$312({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 3v13h4v6l6-6h10V3zm19 12h-9.41L7 19.59V15H3V4h18z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 7h13v1H6zm0 4h8v1H6z" }));
});

var _extends$313 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$313(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CollapsePanel24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$313(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$313({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18 10V7l-7 5 7 5v-3h4v-4zm3 3h-4v2l-4.28-3L17 9v2h4z", fillRule: "evenodd" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 3v18h8V3zm7 17H3V4h6z" }));
});

var _extends$314 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$314(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Collaboration24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$314(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$314({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 9v7h8V8H8zm1 0h6v6H9z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21 3H8v4h1V4h11v11h-3v1h4zm-6 14v3H4V9h3V8H3v13h13v-4z" }));
});

var _extends$315 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$315(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Cloud24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$315(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$315({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19.55 13.06A5.08 5.08 0 0 0 20 11a5 5 0 0 0-9.67-1.76A4.18 4.18 0 0 0 9 9a4 4 0 0 0-4 4 3 3 0 0 0 0 6h14a3 3 0 0 0 .55-5.94zM19 18H5a2 2 0 0 1 0-4h1v-1a3 3 0 0 1 3-3 2.89 2.89 0 0 1 1 .19l.92.33.35-.92A4 4 0 0 1 19 11a4 4 0 0 1-.36 1.64l-.53 1.17 1.26.23A2 2 0 0 1 19 18z" }));
});

var _extends$316 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$316(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var ClosedCaptioning24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$316(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$316({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.85 12.87c-.17 1-.52 2.07-1.16 2.07-.79 0-1.28-1.32-1.28-3.44 0-2.54.67-3.44 1.23-3.44.25 0 .77 0 1.1 1.78l.11.54 3.15-.9-.08-.48c-.46-2.72-1.84-4-4.21-4C3.74 5 2 7.38 2 11.52S3.77 18 6.68 18C9 18 10.45 16.6 11 13.74v-.44l-3-1zM9.92 14c-.5 2.08-1.51 3-3.24 3C4.36 17 3 15 3 11.52 3 9.46 3.51 6 6.71 6c1.72 0 2.71.86 3.14 2.77l-1.24.35c-.26-1-.76-2.06-2-2.06-.67 0-2.24.44-2.24 4.44s1.43 4.44 2.28 4.44c1.3 0 1.81-1.41 2-2.33zM18 12.32l-.11.55c-.17 1-.52 2.07-1.16 2.07-.79 0-1.28-1.32-1.28-3.44 0-2.54.66-3.44 1.23-3.44.25 0 .77 0 1.1 1.78l.11.54 3.11-.9-.08-.48c-.46-2.72-1.84-4-4.21-4C13.74 5 12 7.38 12 11.52S13.77 18 16.68 18c2.32 0 3.77-1.4 4.32-4.26v-.44zM16.68 17C14.36 17 13 15 13 11.52 13 9.46 13.51 6 16.71 6c1.72 0 2.71.86 3.14 2.77l-1.24.35c-.26-1-.76-2.12-2-2.12-.67 0-2.24.5-2.24 4.5s1.47 4.5 2.32 4.5c1.3 0 1.81-1.47 2-2.39l1.2.37c-.47 2.08-1.48 3.02-3.21 3.02z" }));
});

var _extends$317 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$317(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Clock24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$317(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$317({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.5 3a8.5 8.5 0 1 0 8.5 8.5A8.51 8.51 0 0 0 11.5 3zm0 16a7.5 7.5 0 1 1 7.5-7.5 7.5 7.5 0 0 1-7.5 7.5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 11V6h-1v6h6v-1h-5z" }));
});

var _extends$318 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$318(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Checkmark24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$318(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$318({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17.23 5.55l-7.78 7.78-3.54-3.54-2.82 2.83 3.53 3.54L9.45 19l2.83-2.83 7.78-7.78zm-7.78 12l-4.95-5 1.41-1.41 3.54 3.53L17.23 7l1.41 1.42z" }));
});

var _extends$319 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$319(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Checklist24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$319(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$319({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 2v19h18V2zm17 18H4V3h16z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M6 5v3h3V5zm2 2H7V6h1zm3-1h7v1h-7zm-5 4v3h3v-3zm2 2H7v-1h1zm3-1h7v1h-7zm-5 4v3h3v-3zm2 2H7v-1h1zm3-1h7v1h-7z" }));
});

var _extends$320 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$320(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Chat24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$320(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$320({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 8v1h4v8h-3v2.59L15.41 17H9v-2.59l-1 1V18h7l4 4v-4h3V8h-5z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M2 3v10h3v4l4-4h7V3zm13 9H8.59L6 14.59V12H3V4h12z" }));
});

var _extends$321 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$321(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CartFull24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$321(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$321({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "7.5", cy: "20.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "16.5", cy: "20.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.9 15H18l3-8H6.87l-.36-1.8-4.1-1.88-.41.91 3.63 1.66 1.63 8.23L5 18h14v-1H6.73z" }));
});

var _extends$322 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$322(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CartEmpty24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$322(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$322({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "7.5", cy: "20.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("circle", { cx: "16.5", cy: "20.5", r: "1.5" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M7.9 15H18l3-8H6.87l-.36-1.8-4.1-1.88-.41.91 3.63 1.66 1.63 8.23L5 18h14v-1H6.73zm-.83-7h12.49l-2.23 6H8.25z" }));
});

var _extends$323 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$323(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretUp24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$323(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$323({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 4.1L2.1 14l3.54 3.54L12 11.17l6.36 6.37L21.9 14zm0 5.66l-6.36 6.36L3.51 14 12 5.51 20.49 14l-2.13 2.12z" }));
});

var _extends$324 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$324(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretRight24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$324(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$324({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M10 2.1L6.46 5.64 12.83 12l-6.37 6.36L10 21.9l9.9-9.9zM7.88 18.36L14.24 12 7.88 5.64 10 3.51 18.49 12 10 20.49z" }));
});

var _extends$325 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$325(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretLeft24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$325(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$325({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17.54 5.64L14 2.1 4.1 12l9.9 9.9 3.54-3.54L11.17 12zm-1.42 12.72L14 20.49 5.51 12 14 3.51l2.12 2.13L9.76 12z" }));
});

var _extends$326 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$326(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var CaretDown24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$326(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$326({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M18.36 6.46L12 12.83 5.64 6.46 2.1 10l9.9 9.9 9.9-9.9zM3.51 10l2.13-2.12L12 14.24l6.36-6.36L20.49 10 12 18.49z" }));
});

var _extends$327 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$327(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Calendar24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$327(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$327({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M4 4v16h16V4zm1 4h4v3H5zm0 4h4v3H5zm0 7v-3h4v3zm5 0v-3h4v3zm4-8h-4V8h4zm0 1v3h-4v-3zm5 7h-4v-3h4zm0-4h-4v-3h4zm0-4h-4V8h4zM5 7V5h14v2z" }));
});

var _extends$328 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$328(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Building24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$328(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$328({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21 10h-2V3h-2V2H7v1H5v7H3v12h18zM5 21H4V11h1zm2 0H6V4h1zm3 0v-4h4v4zm6 0h-1v-5H9v5H8V3h8zm2 0h-1V4h1zm2 0h-1V11h1z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M9 5h2v2H9zm4 0h2v2h-2zM9 8h2v2H9zm4 0h2v2h-2zm-4 3h2v2H9zm4 0h2v2h-2z" }));
});

var _extends$329 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$329(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Bookmark24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$329(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$329({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M17 3H7v18l5-4 5 4zm-1 15.92l-4-3.2-4 3.2V4h8z" }));
});

var _extends$330 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$330(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Billing24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$330(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$330({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 2H4v20h16V3a1 1 0 0 0-1-1zm0 19H5V3h14z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11 19h7v1h-7zm7-14H6v13h12zM9 17H7v-2h2zm0-3H7v-2h2zm0-3H7V9h2zm0-3H7V6h2zm8 9h-7v-2h7zm0-3h-7v-2h7zm0-3h-7V9h7zm0-3h-7V6h7z" }));
});

var _extends$331 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$331(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var BillStack24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$331(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$331({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M22 5H2v15h20zM9 19H3v-1h6zm0-2H3v-1h6zm0-4H5V8h4zm0-6H4v7h5v1H3V6h6zm5 3v9h-4V6h4zm1-2h4v5h-4zm6 11h-6v-1h6zm0-2h-6v-1h6zm0-2h-6v-1h5V7h-5V6h6z" }));
});

var _extends$332 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$332(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Back24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$332(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$332({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M12 8V4L2 11.5 12 19v-4h10V8zm9 6H11v3l-7.33-5.5L11 6v3h10z", fillRule: "evenodd" }));
});

var _extends$333 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$333(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Attachment24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$333(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$333({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M20 11v2h1v8H6v-1H5v2h17v-9l-2-2z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M14 5h-3v1h3v4h4v8H3V6h5V5H6v-.5a1.5 1.5 0 0 1 3 0V10a1 1 0 0 1-2 0V8H6v2a2 2 0 0 0 4 0V4.5a2.5 2.5 0 0 0-5 0V5H2v14h17v-9zm1 4V7.41L16.59 9z" }));
});

var _extends$334 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$334(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Attach24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$334(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$334({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M16 6h-4v1h4v4h4v9H4V7h5V6H7v-.5a1.5 1.5 0 0 1 3 0V12a1 1 0 0 1-2 0V9H7v3a2 2 0 0 0 4 0V5.5a2.5 2.5 0 0 0-5 0V6H3v15h18V11zm1 4V8.41L18.59 10z" }));
});

var _extends$335 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$335(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var AreaPost24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$335(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$335({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 9h1v4H3zm1-5h3V3H3v4h1V4zm5-1h4v1H9zM4 18v-3H3v4h4v-1H4zM15 3v1h3v3h1V3h-4zM9 18h3v1H9zm13-1h-3v-3h-2v3h-3v2h3v3h2v-3h3v-2zm-4-8h1v3h-1z" }));
});

var _extends$336 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$336(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Archive24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$336(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$336({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21 5H3v4h1v11h16V9h1zm-2 14H5V9h14zm1-11H4V6h16z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M8 12h8v1H8z" }));
});

var _extends$337 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$337(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Alert24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$337(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$337({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21.84 18.1L13.07 3.75a1.25 1.25 0 0 0-2.14 0L2.16 18.1A1.25 1.25 0 0 0 3.23 20h17.54a1.25 1.25 0 0 0 1.07-1.9zm-.85.77a.24.24 0 0 1-.22.13H3.23a.24.24 0 0 1-.23-.13.23.23 0 0 1 0-.25l8.79-14.35a.24.24 0 0 1 .42 0L21 18.62a.23.23 0 0 1 0 .25z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M11.75 13.82h.5c.14 0 .25-.15.25-.33l.5-4.82c0-.19-.53-.67-.67-.67h-.66c-.14 0-.67.48-.67.67l.5 4.82c0 .18.11.33.25.33zM12 15a1 1 0 1 0 1 1 1 1 0 0 0-1-1z" }));
});

var _extends$338 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$338(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var Add24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$338(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$338({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M21 10h-7V3h-4v7H3v4h7.13v7H14v-7h7zm-1 3h-7v7h-2v-7H4v-2h7V4h2v7h7z" }));
});

var _extends$339 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$339(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var AddPage24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$339(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$339({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 3h9v4h4v6h1V7l-5-5H2v17h11v-1H3zm10 1.41L14.59 6H13z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 17v-3h-2v3h-3v2h3v3h2v-3h3v-2h-3z" }));
});

var _extends$340 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$340(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var AddMember24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$340(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$340({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M5 14.5a4.5 4.5 0 0 1 9 0V16h1v-1.5a5.49 5.49 0 0 0-3.59-5.15A4.2 4.2 0 0 0 13 6a3.78 3.78 0 0 0-3.5-4A3.78 3.78 0 0 0 6 6a4.2 4.2 0 0 0 1.59 3.35A5.49 5.49 0 0 0 4 14.5V20h9v-1H5zM7 6a2.79 2.79 0 0 1 2.5-3A2.79 2.79 0 0 1 12 6a2.79 2.79 0 0 1-2.5 3A2.79 2.79 0 0 1 7 6z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 17v-3h-2v3h-3v2h3v3h2v-3h3v-2h-3z" }));
});

var _extends$341 = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }return target;
};

function _objectWithoutProperties$341(obj, keys) {
  var target = {};for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;target[i] = obj[i];
  }return target;
}
var AddFolder24SVG = (function (_ref) {
  var _ref$styles = _ref.styles,
      props = _objectWithoutProperties$341(_ref, ["styles"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("svg", _extends$341({ xmlns: "http://www.w3.org/2000/svg", width: "24", height: "24", viewBox: "0 0 24 24" }, props), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M3 8h7.26l1.52-1H20v9h1V6h-9.21L9 4H2v16h11v-1H3zm0-3h6l1.8 1.42L10 7H3z" }), react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("path", { d: "M19 17v-3h-2v3h-3v2h3v3h2v-3h3v-2h-3z" }));
});

var ADD_FOLDER = "AddFolder";
var ADD_MEMBER = "AddMember";
var ADD_PAGE = "AddPage";
var ADD = "Add";
var ALERT = "Alert";
var ARCHIVE = "Archive";
var AREA_POST = "AreaPost";
var ATTACH = "Attach";
var ATTACHMENT = "Attachment";
var BACK = "Back";
var BILL_STACK = "BillStack";
var BILLING = "Billing";
var BOOKMARK = "Bookmark";
var BUILDING = "Building";
var CALENDAR = "Calendar";
var CARET_DOWN = "CaretDown";
var CARET_LEFT = "CaretLeft";
var CARET_RIGHT = "CaretRight";
var CARET_UP = "CaretUp";
var CART_EMPTY = "CartEmpty";
var CART_FULL = "CartFull";
var CHAT = "Chat";
var CHECKLIST = "Checklist";
var CHECKMARK = "Checkmark";
var CLOCK = "Clock";
var CLOSED_CAPTIONING = "ClosedCaptioning";
var CLOUD = "Cloud";
var COLLABORATION = "Collaboration";
var COLLAPSE_PANEL = "CollapsePanel";
var COMMENT = "Comment";
var COMPARE = "Compare";
var COMPLETE = "Complete";
var COPY = "Copy";
var CTA_ARROW_RIGHT = "CtaArrowRight";
var DASHBOARD = "Dashboard";
var DEVICE_DESKTOP = "DeviceDesktop";
var DEVICE_MOBILE = "DeviceMobile";
var DEVICE_TABLET = "DeviceTablet";
var DEVICES = "Devices";
var DOCUMENT_MANAGEMENT = "DocumentManagement";
var DOWN = "Down";
var DOWNLOAD = "Download";
var EDIT_PROFILE = "EditProfile";
var EDIT = "Edit";
var EDUCATION = "Education";
var ERROR = "Error";
var EXPAND_PANEL = "ExpandPanel";
var EXPORT_MOVE = "ExportMove";
var EXTERNAL_LINK = "ExternalLink";
var EXTERNAL = "External";
var FAVORITE = "Favorite";
var FEATURED = "Featured";
var FEEDBACK = "Feedback";
var FIELD = "Field";
var FILE_ASSEMBLY = "FileAssembly";
var FILE_AUDIO = "FileAudio";
var FILE_DOCUMENT = "FileDocument";
var FILE_GENERIC = "FileGeneric";
var FILE_IMAGE = "FileImage";
var FILE_PART = "FilePart";
var FILE_PDF = "FilePdf";
var FILE_PRESENTATION = "FilePresentation";
var FILE_SPREADSHEET = "FileSpreadsheet";
var FILE_VIDEO = "FileVideo";
var FILE_ZIP = "FileZip";
var FILTER_TOKENS = "FilterTokens";
var FILTER = "Filter";
var FLAG = "Flag";
var FOLDER_OPEN = "FolderOpen";
var FOLDER = "Folder";
var FORUMS = "Forums";
var FORWARD = "Forward";
var GLOBE = "Globe";
var GRAPH = "Graph";
var GRID = "Grid";
var HELP = "Help";
var HIDDEN = "Hidden";
var HIERARCHY = "Hierarchy";
var HOME = "Home";
var IMPORT = "Import";
var INBOX = "Inbox";
var INFO = "Info";
var ITEM = "Item";
var LINK = "Link";
var LIST = "List";
var LOCATION = "Location";
var LOCK = "Lock";
var MAIL = "Mail";
var MANAGE_ACCESS = "ManageAccess";
var MAXIMIZE = "Maximize";
var MINIMIZE = "Minimize";
var MORE_VERTICAL = "MoreVertical";
var MORE = "More";
var MUTE = "Mute";
var NOT_EDITABLE = "NotEditable";
var NOTIFICATION_OFF = "NotificationOff";
var NOTIFICATION = "Notification";
var ONLINE_VIEWERS = "OnlineViewers";
var PASTE = "Paste";
var PERMISSION_GROUP = "PermissionGroup";
var PERMISSION_INDIVIDUAL = "PermissionIndividual";
var PHOTOS = "Photos";
var PIN = "Pin";
var PLACEHOLDER = "Placeholder";
var PLAYBACK_FAST_FORWARD = "PlaybackFastForward";
var PLAYBACK_GO_TO_END = "PlaybackGoToEnd";
var PLAYBACK_GO_TO_START = "PlaybackGoToStart";
var PLAYBACK_LOOP = "PlaybackLoop";
var PLAYBACK_NEXT = "PlaybackNext";
var PLAYBACK_PAUSE = "PlaybackPause";
var PLAYBACK_PLAY = "PlaybackPlay";
var PLAYBACK_PREVIOUS = "PlaybackPrevious";
var PLAYBACK_RECORD = "PlaybackRecord";
var PLAYBACK_REWIND = "PlaybackRewind";
var PLAYBACK_SHUFFLE = "PlaybackShuffle";
var PLAYBACK_STOP = "PlaybackStop";
var PRINT = "Print";
var PRODUCTS_AND_SERVICES = "ProductsAndServices";
var PROFILE = "Profile";
var PROJECT_MANAGEMENT = "ProjectManagement";
var PROPERTIES = "Properties";
var PUBLISH = "Publish";
var QUANTITIES = "Quantities";
var RECOMMEND = "Recommend";
var REDO = "Redo";
var REPORT = "Report";
var SAVE = "Save";
var SEARCH = "Search";
var SECURITY = "Security";
var SELECT_ALL = "SelectAll";
var SELECT_NONE = "SelectNone";
var SERVICE = "Service";
var SETTINGS = "Settings";
var SHARE = "Share";
var SOCIAL_BLOG = "SocialBlog";
var SOCIAL_FACEBOOK = "SocialFacebook";
var SOCIAL_INSTAGRAM = "SocialInstagram";
var SOCIAL_LINKEDIN = "SocialLinkedin";
var SOCIAL_TWITTER = "SocialTwitter";
var SOCIAL_YOUTUBE = "SocialYoutube";
var SORT = "Sort";
var SUBSCRIPTION = "Subscription";
var SUPPORT = "Support";
var SURVEY_EQUIPMENT = "SurveyEquipment";
var SYNC = "Sync";
var TAG = "Tag";
var TRASH = "Trash";
var UNAVAILABLE = "Unavailable";
var UNDO = "Undo";
var UNLINK = "Unlink";
var UNLOCK = "Unlock";
var UNPIN = "Unpin";
var UP = "Up";
var UPLOAD = "Upload";
var USER_INACTIVE = "UserInactive";
var VISIBLE = "Visible";
var VOLUME = "Volume";
var ZOOM_IN = "ZoomIn";
var ZOOM_OUT = "ZoomOut";
var CARET_DOWN_M = "CaretDownM";
var CARET_DOWN_S = "CaretDownS";
var CARET_DOWN_XS = "CaretDownXs";
var CARET_LEFT_M = "CaretLeftM";
var CARET_LEFT_S = "CaretLeftS";
var CARET_LEFT_XS = "CaretLeftXs";
var CARET_RIGHT_M = "CaretRightM";
var CARET_RIGHT_S = "CaretRightS";
var CARET_RIGHT_XS = "CaretRightXs";
var CARET_UP_M = "CaretUpM";
var CARET_UP_S = "CaretUpS";
var CARET_UP_XS = "CaretUpXs";
var CHECKMARK_S = "CheckmarkS";
var CHECKMARK_XS = "CheckmarkXs";
var CLOSE_L = "CloseL";
var CLOSE_M = "CloseM";
var CLOSE_S = "CloseS";
var CLOSE_XS = "CloseXs";
var HAMBURGER_CLOSE = "HamburgerClose";
var HAMBURGER_DEFAULT = "HamburgerDefault";
var OPERATOR_MINUS_S = "OperatorMinusS";
var OPERATOR_MINUS_XS = "OperatorMinusXs";
var OPERATOR_PLUS_S = "OperatorPlusS";
var OPERATOR_PLUS_XS = "OperatorPlusXs";
var names = {
	ADD_FOLDER: ADD_FOLDER,
	ADD_MEMBER: ADD_MEMBER,
	ADD_PAGE: ADD_PAGE,
	ADD: ADD,
	ALERT: ALERT,
	ARCHIVE: ARCHIVE,
	AREA_POST: AREA_POST,
	ATTACH: ATTACH,
	ATTACHMENT: ATTACHMENT,
	BACK: BACK,
	BILL_STACK: BILL_STACK,
	BILLING: BILLING,
	BOOKMARK: BOOKMARK,
	BUILDING: BUILDING,
	CALENDAR: CALENDAR,
	CARET_DOWN: CARET_DOWN,
	CARET_LEFT: CARET_LEFT,
	CARET_RIGHT: CARET_RIGHT,
	CARET_UP: CARET_UP,
	CART_EMPTY: CART_EMPTY,
	CART_FULL: CART_FULL,
	CHAT: CHAT,
	CHECKLIST: CHECKLIST,
	CHECKMARK: CHECKMARK,
	CLOCK: CLOCK,
	CLOSED_CAPTIONING: CLOSED_CAPTIONING,
	CLOUD: CLOUD,
	COLLABORATION: COLLABORATION,
	COLLAPSE_PANEL: COLLAPSE_PANEL,
	COMMENT: COMMENT,
	COMPARE: COMPARE,
	COMPLETE: COMPLETE,
	COPY: COPY,
	CTA_ARROW_RIGHT: CTA_ARROW_RIGHT,
	DASHBOARD: DASHBOARD,
	DEVICE_DESKTOP: DEVICE_DESKTOP,
	DEVICE_MOBILE: DEVICE_MOBILE,
	DEVICE_TABLET: DEVICE_TABLET,
	DEVICES: DEVICES,
	DOCUMENT_MANAGEMENT: DOCUMENT_MANAGEMENT,
	DOWN: DOWN,
	DOWNLOAD: DOWNLOAD,
	EDIT_PROFILE: EDIT_PROFILE,
	EDIT: EDIT,
	EDUCATION: EDUCATION,
	ERROR: ERROR,
	EXPAND_PANEL: EXPAND_PANEL,
	EXPORT_MOVE: EXPORT_MOVE,
	EXTERNAL_LINK: EXTERNAL_LINK,
	EXTERNAL: EXTERNAL,
	FAVORITE: FAVORITE,
	FEATURED: FEATURED,
	FEEDBACK: FEEDBACK,
	FIELD: FIELD,
	FILE_ASSEMBLY: FILE_ASSEMBLY,
	FILE_AUDIO: FILE_AUDIO,
	FILE_DOCUMENT: FILE_DOCUMENT,
	FILE_GENERIC: FILE_GENERIC,
	FILE_IMAGE: FILE_IMAGE,
	FILE_PART: FILE_PART,
	FILE_PDF: FILE_PDF,
	FILE_PRESENTATION: FILE_PRESENTATION,
	FILE_SPREADSHEET: FILE_SPREADSHEET,
	FILE_VIDEO: FILE_VIDEO,
	FILE_ZIP: FILE_ZIP,
	FILTER_TOKENS: FILTER_TOKENS,
	FILTER: FILTER,
	FLAG: FLAG,
	FOLDER_OPEN: FOLDER_OPEN,
	FOLDER: FOLDER,
	FORUMS: FORUMS,
	FORWARD: FORWARD,
	GLOBE: GLOBE,
	GRAPH: GRAPH,
	GRID: GRID,
	HELP: HELP,
	HIDDEN: HIDDEN,
	HIERARCHY: HIERARCHY,
	HOME: HOME,
	IMPORT: IMPORT,
	INBOX: INBOX,
	INFO: INFO,
	ITEM: ITEM,
	LINK: LINK,
	LIST: LIST,
	LOCATION: LOCATION,
	LOCK: LOCK,
	MAIL: MAIL,
	MANAGE_ACCESS: MANAGE_ACCESS,
	MAXIMIZE: MAXIMIZE,
	MINIMIZE: MINIMIZE,
	MORE_VERTICAL: MORE_VERTICAL,
	MORE: MORE,
	MUTE: MUTE,
	NOT_EDITABLE: NOT_EDITABLE,
	NOTIFICATION_OFF: NOTIFICATION_OFF,
	NOTIFICATION: NOTIFICATION,
	ONLINE_VIEWERS: ONLINE_VIEWERS,
	PASTE: PASTE,
	PERMISSION_GROUP: PERMISSION_GROUP,
	PERMISSION_INDIVIDUAL: PERMISSION_INDIVIDUAL,
	PHOTOS: PHOTOS,
	PIN: PIN,
	PLACEHOLDER: PLACEHOLDER,
	PLAYBACK_FAST_FORWARD: PLAYBACK_FAST_FORWARD,
	PLAYBACK_GO_TO_END: PLAYBACK_GO_TO_END,
	PLAYBACK_GO_TO_START: PLAYBACK_GO_TO_START,
	PLAYBACK_LOOP: PLAYBACK_LOOP,
	PLAYBACK_NEXT: PLAYBACK_NEXT,
	PLAYBACK_PAUSE: PLAYBACK_PAUSE,
	PLAYBACK_PLAY: PLAYBACK_PLAY,
	PLAYBACK_PREVIOUS: PLAYBACK_PREVIOUS,
	PLAYBACK_RECORD: PLAYBACK_RECORD,
	PLAYBACK_REWIND: PLAYBACK_REWIND,
	PLAYBACK_SHUFFLE: PLAYBACK_SHUFFLE,
	PLAYBACK_STOP: PLAYBACK_STOP,
	PRINT: PRINT,
	PRODUCTS_AND_SERVICES: PRODUCTS_AND_SERVICES,
	PROFILE: PROFILE,
	PROJECT_MANAGEMENT: PROJECT_MANAGEMENT,
	PROPERTIES: PROPERTIES,
	PUBLISH: PUBLISH,
	QUANTITIES: QUANTITIES,
	RECOMMEND: RECOMMEND,
	REDO: REDO,
	REPORT: REPORT,
	SAVE: SAVE,
	SEARCH: SEARCH,
	SECURITY: SECURITY,
	SELECT_ALL: SELECT_ALL,
	SELECT_NONE: SELECT_NONE,
	SERVICE: SERVICE,
	SETTINGS: SETTINGS,
	SHARE: SHARE,
	SOCIAL_BLOG: SOCIAL_BLOG,
	SOCIAL_FACEBOOK: SOCIAL_FACEBOOK,
	SOCIAL_INSTAGRAM: SOCIAL_INSTAGRAM,
	SOCIAL_LINKEDIN: SOCIAL_LINKEDIN,
	SOCIAL_TWITTER: SOCIAL_TWITTER,
	SOCIAL_YOUTUBE: SOCIAL_YOUTUBE,
	SORT: SORT,
	SUBSCRIPTION: SUBSCRIPTION,
	SUPPORT: SUPPORT,
	SURVEY_EQUIPMENT: SURVEY_EQUIPMENT,
	SYNC: SYNC,
	TAG: TAG,
	TRASH: TRASH,
	UNAVAILABLE: UNAVAILABLE,
	UNDO: UNDO,
	UNLINK: UNLINK,
	UNLOCK: UNLOCK,
	UNPIN: UNPIN,
	UP: UP,
	UPLOAD: UPLOAD,
	USER_INACTIVE: USER_INACTIVE,
	VISIBLE: VISIBLE,
	VOLUME: VOLUME,
	ZOOM_IN: ZOOM_IN,
	ZOOM_OUT: ZOOM_OUT,
	CARET_DOWN_M: CARET_DOWN_M,
	CARET_DOWN_S: CARET_DOWN_S,
	CARET_DOWN_XS: CARET_DOWN_XS,
	CARET_LEFT_M: CARET_LEFT_M,
	CARET_LEFT_S: CARET_LEFT_S,
	CARET_LEFT_XS: CARET_LEFT_XS,
	CARET_RIGHT_M: CARET_RIGHT_M,
	CARET_RIGHT_S: CARET_RIGHT_S,
	CARET_RIGHT_XS: CARET_RIGHT_XS,
	CARET_UP_M: CARET_UP_M,
	CARET_UP_S: CARET_UP_S,
	CARET_UP_XS: CARET_UP_XS,
	CHECKMARK_S: CHECKMARK_S,
	CHECKMARK_XS: CHECKMARK_XS,
	CLOSE_L: CLOSE_L,
	CLOSE_M: CLOSE_M,
	CLOSE_S: CLOSE_S,
	CLOSE_XS: CLOSE_XS,
	HAMBURGER_CLOSE: HAMBURGER_CLOSE,
	HAMBURGER_DEFAULT: HAMBURGER_DEFAULT,
	OPERATOR_MINUS_S: OPERATOR_MINUS_S,
	OPERATOR_MINUS_XS: OPERATOR_MINUS_XS,
	OPERATOR_PLUS_S: OPERATOR_PLUS_S,
	OPERATOR_PLUS_XS: OPERATOR_PLUS_XS
};

var higIconSets = [{ "title": "Regular", "dirName": "regular", "size": "24", "iconNames": ["AddFolder", "AddMember", "AddPage", "Add", "Alert", "Archive", "AreaPost", "Attach", "Attachment", "Back", "BillStack", "Billing", "Bookmark", "Building", "Calendar", "CaretDown", "CaretLeft", "CaretRight", "CaretUp", "CartEmpty", "CartFull", "Chat", "Checklist", "Checkmark", "Clock", "ClosedCaptioning", "Cloud", "Collaboration", "CollapsePanel", "Comment", "Compare", "Complete", "Copy", "CtaArrowRight", "Dashboard", "DeviceDesktop", "DeviceMobile", "DeviceTablet", "Devices", "DocumentManagement", "Down", "Download", "EditProfile", "Edit", "Education", "Error", "ExpandPanel", "ExportMove", "ExternalLink", "External", "Favorite", "Featured", "Feedback", "Field", "FileAssembly", "FileAudio", "FileDocument", "FileGeneric", "FileImage", "FilePart", "FilePdf", "FilePresentation", "FileSpreadsheet", "FileVideo", "FileZip", "FilterTokens", "Filter", "Flag", "FolderOpen", "Folder", "Forums", "Forward", "Globe", "Graph", "Grid", "Help", "Hidden", "Hierarchy", "Home", "Import", "Inbox", "Info", "Item", "Link", "List", "Location", "Lock", "Mail", "ManageAccess", "Maximize", "Minimize", "MoreVertical", "More", "Mute", "NotEditable", "NotificationOff", "Notification", "OnlineViewers", "Paste", "PermissionGroup", "PermissionIndividual", "Photos", "Pin", "Placeholder", "PlaybackFastForward", "PlaybackGoToEnd", "PlaybackGoToStart", "PlaybackLoop", "PlaybackNext", "PlaybackPause", "PlaybackPlay", "PlaybackPrevious", "PlaybackRecord", "PlaybackRewind", "PlaybackShuffle", "PlaybackStop", "Print", "ProductsAndServices", "Profile", "ProjectManagement", "Properties", "Publish", "Quantities", "Recommend", "Redo", "Report", "Save", "Search", "Security", "SelectAll", "SelectNone", "Service", "Settings", "Share", "SocialBlog", "SocialFacebook", "SocialInstagram", "SocialLinkedin", "SocialTwitter", "SocialYoutube", "Sort", "Subscription", "Support", "SurveyEquipment", "Sync", "Tag", "Trash", "Unavailable", "Undo", "Unlink", "Unlock", "Unpin", "Up", "Upload", "UserInactive", "Visible", "Volume", "ZoomIn", "ZoomOut"] }, { "title": "Information Dense", "dirName": "information-dense", "size": "16", "iconNames": ["AddFolder", "AddMember", "AddPage", "Add", "Alert", "Archive", "AreaPost", "Attach", "Attachment", "Back", "BillStack", "Billing", "Bookmark", "Building", "Calendar", "CaretDown", "CaretLeft", "CaretRight", "CaretUp", "CartEmpty", "CartFull", "Chat", "Checklist", "Checkmark", "Clock", "ClosedCaptioning", "Cloud", "Collaboration", "CollapsePanel", "Comment", "Compare", "Complete", "Copy", "CtaArrowRight", "Dashboard", "DeviceDesktop", "DeviceMobile", "DeviceTablet", "Devices", "DocumentManagement", "Down", "Download", "EditProfile", "Edit", "Education", "Error", "ExpandPanel", "ExportMove", "ExternalLink", "External", "Favorite", "Featured", "Feedback", "Field", "FileAssembly", "FileAudio", "FileDocument", "FileGeneric", "FileImage", "FilePart", "FilePdf", "FilePresentation", "FileSpreadsheet", "FileVideo", "FileZip", "FilterTokens", "Filter", "Flag", "FolderOpen", "Folder", "Forums", "Forward", "Globe", "Graph", "Grid", "Help", "Hidden", "Hierarchy", "Home", "Import", "Inbox", "Info", "Item", "Link", "List", "Location", "Lock", "Mail", "ManageAccess", "Maximize", "Minimize", "MoreVertical", "More", "Mute", "NotEditable", "NotificationOff", "Notification", "OnlineViewers", "Paste", "PermissionGroup", "PermissionIndividual", "Photos", "Pin", "Placeholder", "PlaybackFastForward", "PlaybackGoToEnd", "PlaybackGoToStart", "PlaybackLoop", "PlaybackNext", "PlaybackPause", "PlaybackPlay", "PlaybackPrevious", "PlaybackRecord", "PlaybackRewind", "PlaybackShuffle", "PlaybackStop", "Print", "ProductsAndServices", "Profile", "ProjectManagement", "Properties", "Publish", "Quantities", "Recommend", "Redo", "Report", "Save", "Search", "Security", "SelectAll", "SelectNone", "Service", "Settings", "Share", "SocialBlog", "SocialFacebook", "SocialInstagram", "SocialLinkedin", "SocialTwitter", "SocialYoutube", "Sort", "Subscription", "Support", "SurveyEquipment", "Sync", "Tag", "Trash", "Unavailable", "Undo", "Unlink", "Unlock", "Unpin", "Up", "Upload", "UserInactive", "Visible", "Volume", "ZoomIn", "ZoomOut"] }, { "title": "UI Controls", "dirName": "ui-controls", "size": "UI", "iconNames": ["CaretDownM", "CaretDownS", "CaretDownXs", "CaretLeftM", "CaretLeftS", "CaretLeftXs", "CaretRightM", "CaretRightS", "CaretRightXs", "CaretUpM", "CaretUpS", "CaretUpXs", "CheckmarkS", "CheckmarkXs", "CloseL", "CloseM", "CloseS", "CloseXs", "HamburgerClose", "HamburgerDefault", "OperatorMinusS", "OperatorMinusXs", "OperatorPlusS", "OperatorPlusXs"] }];

var sizes = Object.freeze({
  PX_24: "24",
  PX_16: "16",
  PX_UI: "UI"
});

var AVAILABLE_SIZES = Object.freeze(Object.values(sizes));

/* eslint-disable */

var AVAILABLE_NAMES = Object.freeze(Object.values(names));

var AddFolder24 = function AddFolder24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref) {
      var resolvedRoles = _ref.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(AddFolder24SVG, propsClone);
    }
  );
};

var AddMember24 = function AddMember24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref2) {
      var resolvedRoles = _ref2.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(AddMember24SVG, propsClone);
    }
  );
};

var AddPage24 = function AddPage24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref3) {
      var resolvedRoles = _ref3.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(AddPage24SVG, propsClone);
    }
  );
};

var Add24 = function Add24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref4) {
      var resolvedRoles = _ref4.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Add24SVG, propsClone);
    }
  );
};

var Alert24 = function Alert24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref5) {
      var resolvedRoles = _ref5.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Alert24SVG, propsClone);
    }
  );
};

var Archive24 = function Archive24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref6) {
      var resolvedRoles = _ref6.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Archive24SVG, propsClone);
    }
  );
};

var AreaPost24 = function AreaPost24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref7) {
      var resolvedRoles = _ref7.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(AreaPost24SVG, propsClone);
    }
  );
};

var Attach24 = function Attach24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref8) {
      var resolvedRoles = _ref8.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Attach24SVG, propsClone);
    }
  );
};

var Attachment24 = function Attachment24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref9) {
      var resolvedRoles = _ref9.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Attachment24SVG, propsClone);
    }
  );
};

var Back24 = function Back24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref10) {
      var resolvedRoles = _ref10.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Back24SVG, propsClone);
    }
  );
};

var BillStack24 = function BillStack24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref11) {
      var resolvedRoles = _ref11.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(BillStack24SVG, propsClone);
    }
  );
};

var Billing24 = function Billing24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref12) {
      var resolvedRoles = _ref12.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Billing24SVG, propsClone);
    }
  );
};

var Bookmark24 = function Bookmark24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref13) {
      var resolvedRoles = _ref13.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Bookmark24SVG, propsClone);
    }
  );
};

var Building24 = function Building24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref14) {
      var resolvedRoles = _ref14.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Building24SVG, propsClone);
    }
  );
};

var Calendar24 = function Calendar24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref15) {
      var resolvedRoles = _ref15.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Calendar24SVG, propsClone);
    }
  );
};

var CaretDown24 = function CaretDown24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref16) {
      var resolvedRoles = _ref16.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretDown24SVG, propsClone);
    }
  );
};

var CaretLeft24 = function CaretLeft24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref17) {
      var resolvedRoles = _ref17.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretLeft24SVG, propsClone);
    }
  );
};

var CaretRight24 = function CaretRight24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref18) {
      var resolvedRoles = _ref18.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretRight24SVG, propsClone);
    }
  );
};

var CaretUp24 = function CaretUp24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref19) {
      var resolvedRoles = _ref19.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretUp24SVG, propsClone);
    }
  );
};

var CartEmpty24 = function CartEmpty24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref20) {
      var resolvedRoles = _ref20.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CartEmpty24SVG, propsClone);
    }
  );
};

var CartFull24 = function CartFull24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref21) {
      var resolvedRoles = _ref21.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CartFull24SVG, propsClone);
    }
  );
};

var Chat24 = function Chat24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref22) {
      var resolvedRoles = _ref22.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Chat24SVG, propsClone);
    }
  );
};

var Checklist24 = function Checklist24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref23) {
      var resolvedRoles = _ref23.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Checklist24SVG, propsClone);
    }
  );
};

var Checkmark24 = function Checkmark24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref24) {
      var resolvedRoles = _ref24.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Checkmark24SVG, propsClone);
    }
  );
};

var Clock24 = function Clock24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref25) {
      var resolvedRoles = _ref25.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Clock24SVG, propsClone);
    }
  );
};

var ClosedCaptioning24 = function ClosedCaptioning24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref26) {
      var resolvedRoles = _ref26.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ClosedCaptioning24SVG, propsClone);
    }
  );
};

var Cloud24 = function Cloud24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref27) {
      var resolvedRoles = _ref27.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Cloud24SVG, propsClone);
    }
  );
};

var Collaboration24 = function Collaboration24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref28) {
      var resolvedRoles = _ref28.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Collaboration24SVG, propsClone);
    }
  );
};

var CollapsePanel24 = function CollapsePanel24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref29) {
      var resolvedRoles = _ref29.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CollapsePanel24SVG, propsClone);
    }
  );
};

var Comment24 = function Comment24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref30) {
      var resolvedRoles = _ref30.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Comment24SVG, propsClone);
    }
  );
};

var Compare24 = function Compare24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref31) {
      var resolvedRoles = _ref31.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Compare24SVG, propsClone);
    }
  );
};

var Complete24 = function Complete24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref32) {
      var resolvedRoles = _ref32.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Complete24SVG, propsClone);
    }
  );
};

var Copy24 = function Copy24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref33) {
      var resolvedRoles = _ref33.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Copy24SVG, propsClone);
    }
  );
};

var CtaArrowRight24 = function CtaArrowRight24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref34) {
      var resolvedRoles = _ref34.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CtaArrowRight24SVG, propsClone);
    }
  );
};

var Dashboard24 = function Dashboard24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref35) {
      var resolvedRoles = _ref35.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Dashboard24SVG, propsClone);
    }
  );
};

var DeviceDesktop24 = function DeviceDesktop24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref36) {
      var resolvedRoles = _ref36.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(DeviceDesktop24SVG, propsClone);
    }
  );
};

var DeviceMobile24 = function DeviceMobile24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref37) {
      var resolvedRoles = _ref37.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(DeviceMobile24SVG, propsClone);
    }
  );
};

var DeviceTablet24 = function DeviceTablet24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref38) {
      var resolvedRoles = _ref38.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(DeviceTablet24SVG, propsClone);
    }
  );
};

var Devices24 = function Devices24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref39) {
      var resolvedRoles = _ref39.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Devices24SVG, propsClone);
    }
  );
};

var DocumentManagement24 = function DocumentManagement24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref40) {
      var resolvedRoles = _ref40.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(DocumentManagement24SVG, propsClone);
    }
  );
};

var Down24 = function Down24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref41) {
      var resolvedRoles = _ref41.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Down24SVG, propsClone);
    }
  );
};

var Download24 = function Download24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref42) {
      var resolvedRoles = _ref42.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Download24SVG, propsClone);
    }
  );
};

var EditProfile24 = function EditProfile24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref43) {
      var resolvedRoles = _ref43.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(EditProfile24SVG, propsClone);
    }
  );
};

var Edit24 = function Edit24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref44) {
      var resolvedRoles = _ref44.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Edit24SVG, propsClone);
    }
  );
};

var Education24 = function Education24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref45) {
      var resolvedRoles = _ref45.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Education24SVG, propsClone);
    }
  );
};

var Error24 = function Error24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref46) {
      var resolvedRoles = _ref46.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Error24SVG, propsClone);
    }
  );
};

var ExpandPanel24 = function ExpandPanel24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref47) {
      var resolvedRoles = _ref47.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ExpandPanel24SVG, propsClone);
    }
  );
};

var ExportMove24 = function ExportMove24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref48) {
      var resolvedRoles = _ref48.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ExportMove24SVG, propsClone);
    }
  );
};

var ExternalLink24 = function ExternalLink24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref49) {
      var resolvedRoles = _ref49.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ExternalLink24SVG, propsClone);
    }
  );
};

var External24 = function External24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref50) {
      var resolvedRoles = _ref50.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(External24SVG, propsClone);
    }
  );
};

var Favorite24 = function Favorite24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref51) {
      var resolvedRoles = _ref51.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Favorite24SVG, propsClone);
    }
  );
};

var Featured24 = function Featured24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref52) {
      var resolvedRoles = _ref52.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Featured24SVG, propsClone);
    }
  );
};

var Feedback24 = function Feedback24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref53) {
      var resolvedRoles = _ref53.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Feedback24SVG, propsClone);
    }
  );
};

var Field24 = function Field24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref54) {
      var resolvedRoles = _ref54.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Field24SVG, propsClone);
    }
  );
};

var FileAssembly24 = function FileAssembly24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref55) {
      var resolvedRoles = _ref55.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileAssembly24SVG, propsClone);
    }
  );
};

var FileAudio24 = function FileAudio24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref56) {
      var resolvedRoles = _ref56.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileAudio24SVG, propsClone);
    }
  );
};

var FileDocument24 = function FileDocument24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref57) {
      var resolvedRoles = _ref57.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileDocument24SVG, propsClone);
    }
  );
};

var FileGeneric24 = function FileGeneric24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref58) {
      var resolvedRoles = _ref58.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileGeneric24SVG, propsClone);
    }
  );
};

var FileImage24 = function FileImage24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref59) {
      var resolvedRoles = _ref59.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileImage24SVG, propsClone);
    }
  );
};

var FilePart24 = function FilePart24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref60) {
      var resolvedRoles = _ref60.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FilePart24SVG, propsClone);
    }
  );
};

var FilePdf24 = function FilePdf24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref61) {
      var resolvedRoles = _ref61.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FilePdf24SVG, propsClone);
    }
  );
};

var FilePresentation24 = function FilePresentation24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref62) {
      var resolvedRoles = _ref62.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FilePresentation24SVG, propsClone);
    }
  );
};

var FileSpreadsheet24 = function FileSpreadsheet24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref63) {
      var resolvedRoles = _ref63.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileSpreadsheet24SVG, propsClone);
    }
  );
};

var FileVideo24 = function FileVideo24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref64) {
      var resolvedRoles = _ref64.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileVideo24SVG, propsClone);
    }
  );
};

var FileZip24 = function FileZip24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref65) {
      var resolvedRoles = _ref65.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileZip24SVG, propsClone);
    }
  );
};

var FilterTokens24 = function FilterTokens24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref66) {
      var resolvedRoles = _ref66.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FilterTokens24SVG, propsClone);
    }
  );
};

var Filter24 = function Filter24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref67) {
      var resolvedRoles = _ref67.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Filter24SVG, propsClone);
    }
  );
};

var Flag24 = function Flag24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref68) {
      var resolvedRoles = _ref68.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Flag24SVG, propsClone);
    }
  );
};

var FolderOpen24 = function FolderOpen24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref69) {
      var resolvedRoles = _ref69.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FolderOpen24SVG, propsClone);
    }
  );
};

var Folder24 = function Folder24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref70) {
      var resolvedRoles = _ref70.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Folder24SVG, propsClone);
    }
  );
};

var Forums24 = function Forums24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref71) {
      var resolvedRoles = _ref71.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Forums24SVG, propsClone);
    }
  );
};

var Forward24 = function Forward24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref72) {
      var resolvedRoles = _ref72.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Forward24SVG, propsClone);
    }
  );
};

var Globe24 = function Globe24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref73) {
      var resolvedRoles = _ref73.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Globe24SVG, propsClone);
    }
  );
};

var Graph24 = function Graph24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref74) {
      var resolvedRoles = _ref74.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Graph24SVG, propsClone);
    }
  );
};

var Grid24 = function Grid24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref75) {
      var resolvedRoles = _ref75.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Grid24SVG, propsClone);
    }
  );
};

var Help24 = function Help24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref76) {
      var resolvedRoles = _ref76.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Help24SVG, propsClone);
    }
  );
};

var Hidden24 = function Hidden24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref77) {
      var resolvedRoles = _ref77.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Hidden24SVG, propsClone);
    }
  );
};

var Hierarchy24 = function Hierarchy24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref78) {
      var resolvedRoles = _ref78.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Hierarchy24SVG, propsClone);
    }
  );
};

var Home24 = function Home24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref79) {
      var resolvedRoles = _ref79.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Home24SVG, propsClone);
    }
  );
};

var Import24 = function Import24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref80) {
      var resolvedRoles = _ref80.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Import24SVG, propsClone);
    }
  );
};

var Inbox24 = function Inbox24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref81) {
      var resolvedRoles = _ref81.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Inbox24SVG, propsClone);
    }
  );
};

var Info24 = function Info24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref82) {
      var resolvedRoles = _ref82.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Info24SVG, propsClone);
    }
  );
};

var Item24 = function Item24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref83) {
      var resolvedRoles = _ref83.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Item24SVG, propsClone);
    }
  );
};

var Link24 = function Link24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref84) {
      var resolvedRoles = _ref84.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Link24SVG, propsClone);
    }
  );
};

var List24 = function List24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref85) {
      var resolvedRoles = _ref85.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(List24SVG, propsClone);
    }
  );
};

var Location24 = function Location24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref86) {
      var resolvedRoles = _ref86.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Location24SVG, propsClone);
    }
  );
};

var Lock24 = function Lock24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref87) {
      var resolvedRoles = _ref87.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Lock24SVG, propsClone);
    }
  );
};

var Mail24 = function Mail24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref88) {
      var resolvedRoles = _ref88.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Mail24SVG, propsClone);
    }
  );
};

var ManageAccess24 = function ManageAccess24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref89) {
      var resolvedRoles = _ref89.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ManageAccess24SVG, propsClone);
    }
  );
};

var Maximize24 = function Maximize24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref90) {
      var resolvedRoles = _ref90.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Maximize24SVG, propsClone);
    }
  );
};

var Minimize24 = function Minimize24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref91) {
      var resolvedRoles = _ref91.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Minimize24SVG, propsClone);
    }
  );
};

var MoreVertical24 = function MoreVertical24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref92) {
      var resolvedRoles = _ref92.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(MoreVertical24SVG, propsClone);
    }
  );
};

var More24 = function More24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref93) {
      var resolvedRoles = _ref93.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(More24SVG, propsClone);
    }
  );
};

var Mute24 = function Mute24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref94) {
      var resolvedRoles = _ref94.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Mute24SVG, propsClone);
    }
  );
};

var NotEditable24 = function NotEditable24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref95) {
      var resolvedRoles = _ref95.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(NotEditable24SVG, propsClone);
    }
  );
};

var NotificationOff24 = function NotificationOff24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref96) {
      var resolvedRoles = _ref96.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(NotificationOff24SVG, propsClone);
    }
  );
};

var Notification24 = function Notification24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref97) {
      var resolvedRoles = _ref97.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Notification24SVG, propsClone);
    }
  );
};

var OnlineViewers24 = function OnlineViewers24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref98) {
      var resolvedRoles = _ref98.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(OnlineViewers24SVG, propsClone);
    }
  );
};

var Paste24 = function Paste24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref99) {
      var resolvedRoles = _ref99.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Paste24SVG, propsClone);
    }
  );
};

var PermissionGroup24 = function PermissionGroup24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref100) {
      var resolvedRoles = _ref100.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PermissionGroup24SVG, propsClone);
    }
  );
};

var PermissionIndividual24 = function PermissionIndividual24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref101) {
      var resolvedRoles = _ref101.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PermissionIndividual24SVG, propsClone);
    }
  );
};

var Photos24 = function Photos24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref102) {
      var resolvedRoles = _ref102.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Photos24SVG, propsClone);
    }
  );
};

var Pin24 = function Pin24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref103) {
      var resolvedRoles = _ref103.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Pin24SVG, propsClone);
    }
  );
};

var Placeholder24 = function Placeholder24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref104) {
      var resolvedRoles = _ref104.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Placeholder24SVG, propsClone);
    }
  );
};

var PlaybackFastForward24 = function PlaybackFastForward24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref105) {
      var resolvedRoles = _ref105.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackFastForward24SVG, propsClone);
    }
  );
};

var PlaybackGoToEnd24 = function PlaybackGoToEnd24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref106) {
      var resolvedRoles = _ref106.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackGoToEnd24SVG, propsClone);
    }
  );
};

var PlaybackGoToStart24 = function PlaybackGoToStart24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref107) {
      var resolvedRoles = _ref107.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackGoToStart24SVG, propsClone);
    }
  );
};

var PlaybackLoop24 = function PlaybackLoop24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref108) {
      var resolvedRoles = _ref108.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackLoop24SVG, propsClone);
    }
  );
};

var PlaybackNext24 = function PlaybackNext24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref109) {
      var resolvedRoles = _ref109.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackNext24SVG, propsClone);
    }
  );
};

var PlaybackPause24 = function PlaybackPause24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref110) {
      var resolvedRoles = _ref110.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackPause24SVG, propsClone);
    }
  );
};

var PlaybackPlay24 = function PlaybackPlay24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref111) {
      var resolvedRoles = _ref111.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackPlay24SVG, propsClone);
    }
  );
};

var PlaybackPrevious24 = function PlaybackPrevious24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref112) {
      var resolvedRoles = _ref112.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackPrevious24SVG, propsClone);
    }
  );
};

var PlaybackRecord24 = function PlaybackRecord24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref113) {
      var resolvedRoles = _ref113.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackRecord24SVG, propsClone);
    }
  );
};

var PlaybackRewind24 = function PlaybackRewind24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref114) {
      var resolvedRoles = _ref114.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackRewind24SVG, propsClone);
    }
  );
};

var PlaybackShuffle24 = function PlaybackShuffle24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref115) {
      var resolvedRoles = _ref115.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackShuffle24SVG, propsClone);
    }
  );
};

var PlaybackStop24 = function PlaybackStop24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref116) {
      var resolvedRoles = _ref116.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackStop24SVG, propsClone);
    }
  );
};

var Print24 = function Print24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref117) {
      var resolvedRoles = _ref117.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Print24SVG, propsClone);
    }
  );
};

var ProductsAndServices24 = function ProductsAndServices24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref118) {
      var resolvedRoles = _ref118.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ProductsAndServices24SVG, propsClone);
    }
  );
};

var Profile24 = function Profile24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref119) {
      var resolvedRoles = _ref119.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Profile24SVG, propsClone);
    }
  );
};

var ProjectManagement24 = function ProjectManagement24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref120) {
      var resolvedRoles = _ref120.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ProjectManagement24SVG, propsClone);
    }
  );
};

var Properties24 = function Properties24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref121) {
      var resolvedRoles = _ref121.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Properties24SVG, propsClone);
    }
  );
};

var Publish24 = function Publish24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref122) {
      var resolvedRoles = _ref122.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Publish24SVG, propsClone);
    }
  );
};

var Quantities24 = function Quantities24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref123) {
      var resolvedRoles = _ref123.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Quantities24SVG, propsClone);
    }
  );
};

var Recommend24 = function Recommend24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref124) {
      var resolvedRoles = _ref124.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Recommend24SVG, propsClone);
    }
  );
};

var Redo24 = function Redo24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref125) {
      var resolvedRoles = _ref125.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Redo24SVG, propsClone);
    }
  );
};

var Report24 = function Report24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref126) {
      var resolvedRoles = _ref126.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Report24SVG, propsClone);
    }
  );
};

var Save24 = function Save24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref127) {
      var resolvedRoles = _ref127.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Save24SVG, propsClone);
    }
  );
};

var Search24 = function Search24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref128) {
      var resolvedRoles = _ref128.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Search24SVG, propsClone);
    }
  );
};

var Security24 = function Security24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref129) {
      var resolvedRoles = _ref129.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Security24SVG, propsClone);
    }
  );
};

var SelectAll24 = function SelectAll24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref130) {
      var resolvedRoles = _ref130.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SelectAll24SVG, propsClone);
    }
  );
};

var SelectNone24 = function SelectNone24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref131) {
      var resolvedRoles = _ref131.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SelectNone24SVG, propsClone);
    }
  );
};

var Service24 = function Service24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref132) {
      var resolvedRoles = _ref132.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Service24SVG, propsClone);
    }
  );
};

var Settings24 = function Settings24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref133) {
      var resolvedRoles = _ref133.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Settings24SVG, propsClone);
    }
  );
};

var Share24 = function Share24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref134) {
      var resolvedRoles = _ref134.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Share24SVG, propsClone);
    }
  );
};

var SocialBlog24 = function SocialBlog24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref135) {
      var resolvedRoles = _ref135.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialBlog24SVG, propsClone);
    }
  );
};

var SocialFacebook24 = function SocialFacebook24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref136) {
      var resolvedRoles = _ref136.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialFacebook24SVG, propsClone);
    }
  );
};

var SocialInstagram24 = function SocialInstagram24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref137) {
      var resolvedRoles = _ref137.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialInstagram24SVG, propsClone);
    }
  );
};

var SocialLinkedin24 = function SocialLinkedin24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref138) {
      var resolvedRoles = _ref138.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialLinkedin24SVG, propsClone);
    }
  );
};

var SocialTwitter24 = function SocialTwitter24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref139) {
      var resolvedRoles = _ref139.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialTwitter24SVG, propsClone);
    }
  );
};

var SocialYoutube24 = function SocialYoutube24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref140) {
      var resolvedRoles = _ref140.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialYoutube24SVG, propsClone);
    }
  );
};

var Sort24 = function Sort24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref141) {
      var resolvedRoles = _ref141.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Sort24SVG, propsClone);
    }
  );
};

var Subscription24 = function Subscription24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref142) {
      var resolvedRoles = _ref142.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Subscription24SVG, propsClone);
    }
  );
};

var Support24 = function Support24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref143) {
      var resolvedRoles = _ref143.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Support24SVG, propsClone);
    }
  );
};

var SurveyEquipment24 = function SurveyEquipment24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref144) {
      var resolvedRoles = _ref144.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SurveyEquipment24SVG, propsClone);
    }
  );
};

var Sync24 = function Sync24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref145) {
      var resolvedRoles = _ref145.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Sync24SVG, propsClone);
    }
  );
};

var Tag24 = function Tag24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref146) {
      var resolvedRoles = _ref146.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Tag24SVG, propsClone);
    }
  );
};

var Trash24 = function Trash24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref147) {
      var resolvedRoles = _ref147.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Trash24SVG, propsClone);
    }
  );
};

var Unavailable24 = function Unavailable24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref148) {
      var resolvedRoles = _ref148.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Unavailable24SVG, propsClone);
    }
  );
};

var Undo24 = function Undo24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref149) {
      var resolvedRoles = _ref149.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Undo24SVG, propsClone);
    }
  );
};

var Unlink24 = function Unlink24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref150) {
      var resolvedRoles = _ref150.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Unlink24SVG, propsClone);
    }
  );
};

var Unlock24 = function Unlock24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref151) {
      var resolvedRoles = _ref151.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Unlock24SVG, propsClone);
    }
  );
};

var Unpin24 = function Unpin24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref152) {
      var resolvedRoles = _ref152.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Unpin24SVG, propsClone);
    }
  );
};

var Up24 = function Up24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref153) {
      var resolvedRoles = _ref153.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Up24SVG, propsClone);
    }
  );
};

var Upload24 = function Upload24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref154) {
      var resolvedRoles = _ref154.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Upload24SVG, propsClone);
    }
  );
};

var UserInactive24 = function UserInactive24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref155) {
      var resolvedRoles = _ref155.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(UserInactive24SVG, propsClone);
    }
  );
};

var Visible24 = function Visible24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref156) {
      var resolvedRoles = _ref156.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Visible24SVG, propsClone);
    }
  );
};

var Volume24 = function Volume24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref157) {
      var resolvedRoles = _ref157.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Volume24SVG, propsClone);
    }
  );
};

var ZoomIn24 = function ZoomIn24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref158) {
      var resolvedRoles = _ref158.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ZoomIn24SVG, propsClone);
    }
  );
};

var ZoomOut24 = function ZoomOut24(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref159) {
      var resolvedRoles = _ref159.resolvedRoles;

      var baseProps = {
        width: "24px",
        height: "24px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ZoomOut24SVG, propsClone);
    }
  );
};

var AddFolder16 = function AddFolder16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref160) {
      var resolvedRoles = _ref160.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(AddFolder16SVG, propsClone);
    }
  );
};

var AddMember16 = function AddMember16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref161) {
      var resolvedRoles = _ref161.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(AddMember16SVG, propsClone);
    }
  );
};

var AddPage16 = function AddPage16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref162) {
      var resolvedRoles = _ref162.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(AddPage16SVG, propsClone);
    }
  );
};

var Add16 = function Add16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref163) {
      var resolvedRoles = _ref163.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Add16SVG, propsClone);
    }
  );
};

var Alert16 = function Alert16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref164) {
      var resolvedRoles = _ref164.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Alert16SVG, propsClone);
    }
  );
};

var Archive16 = function Archive16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref165) {
      var resolvedRoles = _ref165.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Archive16SVG, propsClone);
    }
  );
};

var AreaPost16 = function AreaPost16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref166) {
      var resolvedRoles = _ref166.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(AreaPost16SVG, propsClone);
    }
  );
};

var Attach16 = function Attach16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref167) {
      var resolvedRoles = _ref167.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Attach16SVG, propsClone);
    }
  );
};

var Attachment16 = function Attachment16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref168) {
      var resolvedRoles = _ref168.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Attachment16SVG, propsClone);
    }
  );
};

var Back16 = function Back16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref169) {
      var resolvedRoles = _ref169.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Back16SVG, propsClone);
    }
  );
};

var BillStack16 = function BillStack16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref170) {
      var resolvedRoles = _ref170.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(BillStack16SVG, propsClone);
    }
  );
};

var Billing16 = function Billing16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref171) {
      var resolvedRoles = _ref171.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Billing16SVG, propsClone);
    }
  );
};

var Bookmark16 = function Bookmark16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref172) {
      var resolvedRoles = _ref172.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Bookmark16SVG, propsClone);
    }
  );
};

var Building16 = function Building16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref173) {
      var resolvedRoles = _ref173.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Building16SVG, propsClone);
    }
  );
};

var Calendar16 = function Calendar16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref174) {
      var resolvedRoles = _ref174.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Calendar16SVG, propsClone);
    }
  );
};

var CaretDown16 = function CaretDown16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref175) {
      var resolvedRoles = _ref175.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretDown16SVG, propsClone);
    }
  );
};

var CaretLeft16 = function CaretLeft16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref176) {
      var resolvedRoles = _ref176.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretLeft16SVG, propsClone);
    }
  );
};

var CaretRight16 = function CaretRight16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref177) {
      var resolvedRoles = _ref177.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretRight16SVG, propsClone);
    }
  );
};

var CaretUp16 = function CaretUp16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref178) {
      var resolvedRoles = _ref178.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretUp16SVG, propsClone);
    }
  );
};

var CartEmpty16 = function CartEmpty16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref179) {
      var resolvedRoles = _ref179.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CartEmpty16SVG, propsClone);
    }
  );
};

var CartFull16 = function CartFull16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref180) {
      var resolvedRoles = _ref180.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CartFull16SVG, propsClone);
    }
  );
};

var Chat16 = function Chat16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref181) {
      var resolvedRoles = _ref181.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Chat16SVG, propsClone);
    }
  );
};

var Checklist16 = function Checklist16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref182) {
      var resolvedRoles = _ref182.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Checklist16SVG, propsClone);
    }
  );
};

var Checkmark16 = function Checkmark16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref183) {
      var resolvedRoles = _ref183.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Checkmark16SVG, propsClone);
    }
  );
};

var Clock16 = function Clock16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref184) {
      var resolvedRoles = _ref184.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Clock16SVG, propsClone);
    }
  );
};

var ClosedCaptioning16 = function ClosedCaptioning16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref185) {
      var resolvedRoles = _ref185.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ClosedCaptioning16SVG, propsClone);
    }
  );
};

var Cloud16 = function Cloud16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref186) {
      var resolvedRoles = _ref186.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Cloud16SVG, propsClone);
    }
  );
};

var Collaboration16 = function Collaboration16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref187) {
      var resolvedRoles = _ref187.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Collaboration16SVG, propsClone);
    }
  );
};

var CollapsePanel16 = function CollapsePanel16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref188) {
      var resolvedRoles = _ref188.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CollapsePanel16SVG, propsClone);
    }
  );
};

var Comment16 = function Comment16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref189) {
      var resolvedRoles = _ref189.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Comment16SVG, propsClone);
    }
  );
};

var Compare16 = function Compare16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref190) {
      var resolvedRoles = _ref190.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Compare16SVG, propsClone);
    }
  );
};

var Complete16 = function Complete16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref191) {
      var resolvedRoles = _ref191.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Complete16SVG, propsClone);
    }
  );
};

var Copy16 = function Copy16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref192) {
      var resolvedRoles = _ref192.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Copy16SVG, propsClone);
    }
  );
};

var CtaArrowRight16 = function CtaArrowRight16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref193) {
      var resolvedRoles = _ref193.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CtaArrowRight16SVG, propsClone);
    }
  );
};

var Dashboard16 = function Dashboard16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref194) {
      var resolvedRoles = _ref194.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Dashboard16SVG, propsClone);
    }
  );
};

var DeviceDesktop16 = function DeviceDesktop16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref195) {
      var resolvedRoles = _ref195.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(DeviceDesktop16SVG, propsClone);
    }
  );
};

var DeviceMobile16 = function DeviceMobile16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref196) {
      var resolvedRoles = _ref196.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(DeviceMobile16SVG, propsClone);
    }
  );
};

var DeviceTablet16 = function DeviceTablet16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref197) {
      var resolvedRoles = _ref197.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(DeviceTablet16SVG, propsClone);
    }
  );
};

var Devices16 = function Devices16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref198) {
      var resolvedRoles = _ref198.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Devices16SVG, propsClone);
    }
  );
};

var DocumentManagement16 = function DocumentManagement16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref199) {
      var resolvedRoles = _ref199.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(DocumentManagement16SVG, propsClone);
    }
  );
};

var Down16 = function Down16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref200) {
      var resolvedRoles = _ref200.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Down16SVG, propsClone);
    }
  );
};

var Download16 = function Download16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref201) {
      var resolvedRoles = _ref201.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Download16SVG, propsClone);
    }
  );
};

var EditProfile16 = function EditProfile16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref202) {
      var resolvedRoles = _ref202.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(EditProfile16SVG, propsClone);
    }
  );
};

var Edit16 = function Edit16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref203) {
      var resolvedRoles = _ref203.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Edit16SVG, propsClone);
    }
  );
};

var Education16 = function Education16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref204) {
      var resolvedRoles = _ref204.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Education16SVG, propsClone);
    }
  );
};

var Error16 = function Error16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref205) {
      var resolvedRoles = _ref205.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Error16SVG, propsClone);
    }
  );
};

var ExpandPanel16 = function ExpandPanel16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref206) {
      var resolvedRoles = _ref206.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ExpandPanel16SVG, propsClone);
    }
  );
};

var ExportMove16 = function ExportMove16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref207) {
      var resolvedRoles = _ref207.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ExportMove16SVG, propsClone);
    }
  );
};

var ExternalLink16 = function ExternalLink16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref208) {
      var resolvedRoles = _ref208.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ExternalLink16SVG, propsClone);
    }
  );
};

var External16 = function External16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref209) {
      var resolvedRoles = _ref209.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(External16SVG, propsClone);
    }
  );
};

var Favorite16 = function Favorite16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref210) {
      var resolvedRoles = _ref210.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Favorite16SVG, propsClone);
    }
  );
};

var Featured16 = function Featured16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref211) {
      var resolvedRoles = _ref211.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Featured16SVG, propsClone);
    }
  );
};

var Feedback16 = function Feedback16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref212) {
      var resolvedRoles = _ref212.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Feedback16SVG, propsClone);
    }
  );
};

var Field16 = function Field16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref213) {
      var resolvedRoles = _ref213.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Field16SVG, propsClone);
    }
  );
};

var FileAssembly16 = function FileAssembly16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref214) {
      var resolvedRoles = _ref214.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileAssembly16SVG, propsClone);
    }
  );
};

var FileAudio16 = function FileAudio16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref215) {
      var resolvedRoles = _ref215.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileAudio16SVG, propsClone);
    }
  );
};

var FileDocument16 = function FileDocument16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref216) {
      var resolvedRoles = _ref216.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileDocument16SVG, propsClone);
    }
  );
};

var FileGeneric16 = function FileGeneric16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref217) {
      var resolvedRoles = _ref217.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileGeneric16SVG, propsClone);
    }
  );
};

var FileImage16 = function FileImage16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref218) {
      var resolvedRoles = _ref218.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileImage16SVG, propsClone);
    }
  );
};

var FilePart16 = function FilePart16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref219) {
      var resolvedRoles = _ref219.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FilePart16SVG, propsClone);
    }
  );
};

var FilePdf16 = function FilePdf16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref220) {
      var resolvedRoles = _ref220.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FilePdf16SVG, propsClone);
    }
  );
};

var FilePresentation16 = function FilePresentation16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref221) {
      var resolvedRoles = _ref221.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FilePresentation16SVG, propsClone);
    }
  );
};

var FileSpreadsheet16 = function FileSpreadsheet16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref222) {
      var resolvedRoles = _ref222.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileSpreadsheet16SVG, propsClone);
    }
  );
};

var FileVideo16 = function FileVideo16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref223) {
      var resolvedRoles = _ref223.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileVideo16SVG, propsClone);
    }
  );
};

var FileZip16 = function FileZip16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref224) {
      var resolvedRoles = _ref224.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FileZip16SVG, propsClone);
    }
  );
};

var FilterTokens16 = function FilterTokens16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref225) {
      var resolvedRoles = _ref225.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FilterTokens16SVG, propsClone);
    }
  );
};

var Filter16 = function Filter16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref226) {
      var resolvedRoles = _ref226.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Filter16SVG, propsClone);
    }
  );
};

var Flag16 = function Flag16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref227) {
      var resolvedRoles = _ref227.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Flag16SVG, propsClone);
    }
  );
};

var FolderOpen16 = function FolderOpen16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref228) {
      var resolvedRoles = _ref228.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(FolderOpen16SVG, propsClone);
    }
  );
};

var Folder16 = function Folder16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref229) {
      var resolvedRoles = _ref229.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Folder16SVG, propsClone);
    }
  );
};

var Forums16 = function Forums16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref230) {
      var resolvedRoles = _ref230.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Forums16SVG, propsClone);
    }
  );
};

var Forward16 = function Forward16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref231) {
      var resolvedRoles = _ref231.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Forward16SVG, propsClone);
    }
  );
};

var Globe16 = function Globe16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref232) {
      var resolvedRoles = _ref232.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Globe16SVG, propsClone);
    }
  );
};

var Graph16 = function Graph16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref233) {
      var resolvedRoles = _ref233.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Graph16SVG, propsClone);
    }
  );
};

var Grid16 = function Grid16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref234) {
      var resolvedRoles = _ref234.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Grid16SVG, propsClone);
    }
  );
};

var Help16 = function Help16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref235) {
      var resolvedRoles = _ref235.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Help16SVG, propsClone);
    }
  );
};

var Hidden16 = function Hidden16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref236) {
      var resolvedRoles = _ref236.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Hidden16SVG, propsClone);
    }
  );
};

var Hierarchy16 = function Hierarchy16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref237) {
      var resolvedRoles = _ref237.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Hierarchy16SVG, propsClone);
    }
  );
};

var Home16 = function Home16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref238) {
      var resolvedRoles = _ref238.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Home16SVG, propsClone);
    }
  );
};

var Import16 = function Import16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref239) {
      var resolvedRoles = _ref239.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Import16SVG, propsClone);
    }
  );
};

var Inbox16 = function Inbox16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref240) {
      var resolvedRoles = _ref240.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Inbox16SVG, propsClone);
    }
  );
};

var Info16 = function Info16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref241) {
      var resolvedRoles = _ref241.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Info16SVG, propsClone);
    }
  );
};

var Item16 = function Item16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref242) {
      var resolvedRoles = _ref242.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Item16SVG, propsClone);
    }
  );
};

var Link16 = function Link16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref243) {
      var resolvedRoles = _ref243.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Link16SVG, propsClone);
    }
  );
};

var List16 = function List16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref244) {
      var resolvedRoles = _ref244.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(List16SVG, propsClone);
    }
  );
};

var Location16 = function Location16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref245) {
      var resolvedRoles = _ref245.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Location16SVG, propsClone);
    }
  );
};

var Lock16 = function Lock16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref246) {
      var resolvedRoles = _ref246.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Lock16SVG, propsClone);
    }
  );
};

var Mail16 = function Mail16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref247) {
      var resolvedRoles = _ref247.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Mail16SVG, propsClone);
    }
  );
};

var ManageAccess16 = function ManageAccess16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref248) {
      var resolvedRoles = _ref248.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ManageAccess16SVG, propsClone);
    }
  );
};

var Maximize16 = function Maximize16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref249) {
      var resolvedRoles = _ref249.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Maximize16SVG, propsClone);
    }
  );
};

var Minimize16 = function Minimize16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref250) {
      var resolvedRoles = _ref250.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Minimize16SVG, propsClone);
    }
  );
};

var MoreVertical16 = function MoreVertical16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref251) {
      var resolvedRoles = _ref251.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(MoreVertical16SVG, propsClone);
    }
  );
};

var More16 = function More16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref252) {
      var resolvedRoles = _ref252.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(More16SVG, propsClone);
    }
  );
};

var Mute16 = function Mute16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref253) {
      var resolvedRoles = _ref253.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Mute16SVG, propsClone);
    }
  );
};

var NotEditable16 = function NotEditable16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref254) {
      var resolvedRoles = _ref254.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(NotEditable16SVG, propsClone);
    }
  );
};

var NotificationOff16 = function NotificationOff16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref255) {
      var resolvedRoles = _ref255.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(NotificationOff16SVG, propsClone);
    }
  );
};

var Notification16 = function Notification16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref256) {
      var resolvedRoles = _ref256.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Notification16SVG, propsClone);
    }
  );
};

var OnlineViewers16 = function OnlineViewers16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref257) {
      var resolvedRoles = _ref257.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(OnlineViewers16SVG, propsClone);
    }
  );
};

var Paste16 = function Paste16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref258) {
      var resolvedRoles = _ref258.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Paste16SVG, propsClone);
    }
  );
};

var PermissionGroup16 = function PermissionGroup16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref259) {
      var resolvedRoles = _ref259.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PermissionGroup16SVG, propsClone);
    }
  );
};

var PermissionIndividual16 = function PermissionIndividual16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref260) {
      var resolvedRoles = _ref260.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PermissionIndividual16SVG, propsClone);
    }
  );
};

var Photos16 = function Photos16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref261) {
      var resolvedRoles = _ref261.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Photos16SVG, propsClone);
    }
  );
};

var Pin16 = function Pin16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref262) {
      var resolvedRoles = _ref262.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Pin16SVG, propsClone);
    }
  );
};

var Placeholder16 = function Placeholder16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref263) {
      var resolvedRoles = _ref263.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Placeholder16SVG, propsClone);
    }
  );
};

var PlaybackFastForward16 = function PlaybackFastForward16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref264) {
      var resolvedRoles = _ref264.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackFastForward16SVG, propsClone);
    }
  );
};

var PlaybackGoToEnd16 = function PlaybackGoToEnd16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref265) {
      var resolvedRoles = _ref265.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackGoToEnd16SVG, propsClone);
    }
  );
};

var PlaybackGoToStart16 = function PlaybackGoToStart16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref266) {
      var resolvedRoles = _ref266.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackGoToStart16SVG, propsClone);
    }
  );
};

var PlaybackLoop16 = function PlaybackLoop16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref267) {
      var resolvedRoles = _ref267.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackLoop16SVG, propsClone);
    }
  );
};

var PlaybackNext16 = function PlaybackNext16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref268) {
      var resolvedRoles = _ref268.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackNext16SVG, propsClone);
    }
  );
};

var PlaybackPause16 = function PlaybackPause16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref269) {
      var resolvedRoles = _ref269.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackPause16SVG, propsClone);
    }
  );
};

var PlaybackPlay16 = function PlaybackPlay16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref270) {
      var resolvedRoles = _ref270.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackPlay16SVG, propsClone);
    }
  );
};

var PlaybackPrevious16 = function PlaybackPrevious16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref271) {
      var resolvedRoles = _ref271.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackPrevious16SVG, propsClone);
    }
  );
};

var PlaybackRecord16 = function PlaybackRecord16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref272) {
      var resolvedRoles = _ref272.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackRecord16SVG, propsClone);
    }
  );
};

var PlaybackRewind16 = function PlaybackRewind16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref273) {
      var resolvedRoles = _ref273.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackRewind16SVG, propsClone);
    }
  );
};

var PlaybackShuffle16 = function PlaybackShuffle16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref274) {
      var resolvedRoles = _ref274.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackShuffle16SVG, propsClone);
    }
  );
};

var PlaybackStop16 = function PlaybackStop16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref275) {
      var resolvedRoles = _ref275.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(PlaybackStop16SVG, propsClone);
    }
  );
};

var Print16 = function Print16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref276) {
      var resolvedRoles = _ref276.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Print16SVG, propsClone);
    }
  );
};

var ProductsAndServices16 = function ProductsAndServices16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref277) {
      var resolvedRoles = _ref277.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ProductsAndServices16SVG, propsClone);
    }
  );
};

var Profile16 = function Profile16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref278) {
      var resolvedRoles = _ref278.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Profile16SVG, propsClone);
    }
  );
};

var ProjectManagement16 = function ProjectManagement16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref279) {
      var resolvedRoles = _ref279.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ProjectManagement16SVG, propsClone);
    }
  );
};

var Properties16 = function Properties16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref280) {
      var resolvedRoles = _ref280.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Properties16SVG, propsClone);
    }
  );
};

var Publish16 = function Publish16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref281) {
      var resolvedRoles = _ref281.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Publish16SVG, propsClone);
    }
  );
};

var Quantities16 = function Quantities16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref282) {
      var resolvedRoles = _ref282.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Quantities16SVG, propsClone);
    }
  );
};

var Recommend16 = function Recommend16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref283) {
      var resolvedRoles = _ref283.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Recommend16SVG, propsClone);
    }
  );
};

var Redo16 = function Redo16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref284) {
      var resolvedRoles = _ref284.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Redo16SVG, propsClone);
    }
  );
};

var Report16 = function Report16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref285) {
      var resolvedRoles = _ref285.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Report16SVG, propsClone);
    }
  );
};

var Save16 = function Save16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref286) {
      var resolvedRoles = _ref286.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Save16SVG, propsClone);
    }
  );
};

var Search16 = function Search16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref287) {
      var resolvedRoles = _ref287.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Search16SVG, propsClone);
    }
  );
};

var Security16 = function Security16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref288) {
      var resolvedRoles = _ref288.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Security16SVG, propsClone);
    }
  );
};

var SelectAll16 = function SelectAll16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref289) {
      var resolvedRoles = _ref289.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SelectAll16SVG, propsClone);
    }
  );
};

var SelectNone16 = function SelectNone16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref290) {
      var resolvedRoles = _ref290.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SelectNone16SVG, propsClone);
    }
  );
};

var Service16 = function Service16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref291) {
      var resolvedRoles = _ref291.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Service16SVG, propsClone);
    }
  );
};

var Settings16 = function Settings16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref292) {
      var resolvedRoles = _ref292.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Settings16SVG, propsClone);
    }
  );
};

var Share16 = function Share16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref293) {
      var resolvedRoles = _ref293.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Share16SVG, propsClone);
    }
  );
};

var SocialBlog16 = function SocialBlog16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref294) {
      var resolvedRoles = _ref294.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialBlog16SVG, propsClone);
    }
  );
};

var SocialFacebook16 = function SocialFacebook16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref295) {
      var resolvedRoles = _ref295.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialFacebook16SVG, propsClone);
    }
  );
};

var SocialInstagram16 = function SocialInstagram16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref296) {
      var resolvedRoles = _ref296.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialInstagram16SVG, propsClone);
    }
  );
};

var SocialLinkedin16 = function SocialLinkedin16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref297) {
      var resolvedRoles = _ref297.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialLinkedin16SVG, propsClone);
    }
  );
};

var SocialTwitter16 = function SocialTwitter16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref298) {
      var resolvedRoles = _ref298.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialTwitter16SVG, propsClone);
    }
  );
};

var SocialYoutube16 = function SocialYoutube16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref299) {
      var resolvedRoles = _ref299.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SocialYoutube16SVG, propsClone);
    }
  );
};

var Sort16 = function Sort16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref300) {
      var resolvedRoles = _ref300.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Sort16SVG, propsClone);
    }
  );
};

var Subscription16 = function Subscription16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref301) {
      var resolvedRoles = _ref301.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Subscription16SVG, propsClone);
    }
  );
};

var Support16 = function Support16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref302) {
      var resolvedRoles = _ref302.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Support16SVG, propsClone);
    }
  );
};

var SurveyEquipment16 = function SurveyEquipment16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref303) {
      var resolvedRoles = _ref303.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(SurveyEquipment16SVG, propsClone);
    }
  );
};

var Sync16 = function Sync16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref304) {
      var resolvedRoles = _ref304.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Sync16SVG, propsClone);
    }
  );
};

var Tag16 = function Tag16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref305) {
      var resolvedRoles = _ref305.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Tag16SVG, propsClone);
    }
  );
};

var Trash16 = function Trash16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref306) {
      var resolvedRoles = _ref306.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Trash16SVG, propsClone);
    }
  );
};

var Unavailable16 = function Unavailable16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref307) {
      var resolvedRoles = _ref307.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Unavailable16SVG, propsClone);
    }
  );
};

var Undo16 = function Undo16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref308) {
      var resolvedRoles = _ref308.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Undo16SVG, propsClone);
    }
  );
};

var Unlink16 = function Unlink16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref309) {
      var resolvedRoles = _ref309.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Unlink16SVG, propsClone);
    }
  );
};

var Unlock16 = function Unlock16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref310) {
      var resolvedRoles = _ref310.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Unlock16SVG, propsClone);
    }
  );
};

var Unpin16 = function Unpin16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref311) {
      var resolvedRoles = _ref311.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Unpin16SVG, propsClone);
    }
  );
};

var Up16 = function Up16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref312) {
      var resolvedRoles = _ref312.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Up16SVG, propsClone);
    }
  );
};

var Upload16 = function Upload16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref313) {
      var resolvedRoles = _ref313.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Upload16SVG, propsClone);
    }
  );
};

var UserInactive16 = function UserInactive16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref314) {
      var resolvedRoles = _ref314.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(UserInactive16SVG, propsClone);
    }
  );
};

var Visible16 = function Visible16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref315) {
      var resolvedRoles = _ref315.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Visible16SVG, propsClone);
    }
  );
};

var Volume16 = function Volume16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref316) {
      var resolvedRoles = _ref316.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Volume16SVG, propsClone);
    }
  );
};

var ZoomIn16 = function ZoomIn16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref317) {
      var resolvedRoles = _ref317.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ZoomIn16SVG, propsClone);
    }
  );
};

var ZoomOut16 = function ZoomOut16(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref318) {
      var resolvedRoles = _ref318.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(ZoomOut16SVG, propsClone);
    }
  );
};

var CaretDownMUI = function CaretDownMUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref319) {
      var resolvedRoles = _ref319.resolvedRoles;

      var baseProps = {
        width: "10px",
        height: "10px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretDownMUISVG, propsClone);
    }
  );
};

var CaretDownSUI = function CaretDownSUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref320) {
      var resolvedRoles = _ref320.resolvedRoles;

      var baseProps = {
        width: "7px",
        height: "4px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretDownSUISVG, propsClone);
    }
  );
};

var CaretDownXsUI = function CaretDownXsUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref321) {
      var resolvedRoles = _ref321.resolvedRoles;

      var baseProps = {
        width: "5px",
        height: "3px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretDownXsUISVG, propsClone);
    }
  );
};

var CaretLeftMUI = function CaretLeftMUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref322) {
      var resolvedRoles = _ref322.resolvedRoles;

      var baseProps = {
        width: "10px",
        height: "10px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretLeftMUISVG, propsClone);
    }
  );
};

var CaretLeftSUI = function CaretLeftSUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref323) {
      var resolvedRoles = _ref323.resolvedRoles;

      var baseProps = {
        width: "4px",
        height: "7px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretLeftSUISVG, propsClone);
    }
  );
};

var CaretLeftXsUI = function CaretLeftXsUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref324) {
      var resolvedRoles = _ref324.resolvedRoles;

      var baseProps = {
        width: "3px",
        height: "5px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretLeftXsUISVG, propsClone);
    }
  );
};

var CaretRightMUI = function CaretRightMUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref325) {
      var resolvedRoles = _ref325.resolvedRoles;

      var baseProps = {
        width: "10px",
        height: "10px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretRightMUISVG, propsClone);
    }
  );
};

var CaretRightSUI = function CaretRightSUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref326) {
      var resolvedRoles = _ref326.resolvedRoles;

      var baseProps = {
        width: "4px",
        height: "7px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretRightSUISVG, propsClone);
    }
  );
};

var CaretRightXsUI = function CaretRightXsUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref327) {
      var resolvedRoles = _ref327.resolvedRoles;

      var baseProps = {
        width: "3px",
        height: "5px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretRightXsUISVG, propsClone);
    }
  );
};

var CaretUpMUI = function CaretUpMUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref328) {
      var resolvedRoles = _ref328.resolvedRoles;

      var baseProps = {
        width: "10px",
        height: "10px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretUpMUISVG, propsClone);
    }
  );
};

var CaretUpSUI = function CaretUpSUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref329) {
      var resolvedRoles = _ref329.resolvedRoles;

      var baseProps = {
        width: "7px",
        height: "4px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretUpSUISVG, propsClone);
    }
  );
};

var CaretUpXsUI = function CaretUpXsUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref330) {
      var resolvedRoles = _ref330.resolvedRoles;

      var baseProps = {
        width: "5px",
        height: "3px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CaretUpXsUISVG, propsClone);
    }
  );
};

var CheckmarkSUI = function CheckmarkSUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref331) {
      var resolvedRoles = _ref331.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CheckmarkSUISVG, propsClone);
    }
  );
};

var CheckmarkXsUI = function CheckmarkXsUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref332) {
      var resolvedRoles = _ref332.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CheckmarkXsUISVG, propsClone);
    }
  );
};

var CloseLUI = function CloseLUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref333) {
      var resolvedRoles = _ref333.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CloseLUISVG, propsClone);
    }
  );
};

var CloseMUI = function CloseMUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref334) {
      var resolvedRoles = _ref334.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CloseMUISVG, propsClone);
    }
  );
};

var CloseSUI = function CloseSUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref335) {
      var resolvedRoles = _ref335.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CloseSUISVG, propsClone);
    }
  );
};

var CloseXsUI = function CloseXsUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref336) {
      var resolvedRoles = _ref336.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(CloseXsUISVG, propsClone);
    }
  );
};

var HamburgerCloseUI = function HamburgerCloseUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref337) {
      var resolvedRoles = _ref337.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(HamburgerCloseUISVG, propsClone);
    }
  );
};

var HamburgerDefaultUI = function HamburgerDefaultUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref338) {
      var resolvedRoles = _ref338.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(HamburgerDefaultUISVG, propsClone);
    }
  );
};

var OperatorMinusSUI = function OperatorMinusSUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref339) {
      var resolvedRoles = _ref339.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(OperatorMinusSUISVG, propsClone);
    }
  );
};

var OperatorMinusXsUI = function OperatorMinusXsUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref340) {
      var resolvedRoles = _ref340.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(OperatorMinusXsUISVG, propsClone);
    }
  );
};

var OperatorPlusSUI = function OperatorPlusSUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref341) {
      var resolvedRoles = _ref341.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(OperatorPlusSUISVG, propsClone);
    }
  );
};

var OperatorPlusXsUI = function OperatorPlusXsUI(props) {
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_2__["ThemeContext"].Consumer,
    null,
    function (_ref342) {
      var resolvedRoles = _ref342.resolvedRoles;

      var baseProps = {
        width: "16px",
        height: "16px"
      };
      var propsClone = Object.assign(baseProps, props, { className: Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_1__["css"])(stylesheet(props, resolvedRoles)), props.className) });
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(OperatorPlusXsUISVG, propsClone);
    }
  );
};

/* eslint-enable */




/***/ }),

/***/ "./node_modules/@hig/input/build/index.es.js":
/*!***************************************************!*\
  !*** ./node_modules/@hig/input/build/index.es.js ***!
  \***************************************************/
/*! exports provided: default, variants, availableVariants, tagNames, availableTagNames */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "variants", function() { return variants; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "availableVariants", function() { return availableVariants; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "tagNames", function() { return tagNames; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "availableTagNames", function() { return availableTagNames; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! prop-types */ "./node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var emotion__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! emotion */ "./node_modules/emotion/dist/emotion.esm.js");
/* harmony import */ var _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @hig/theme-context */ "./node_modules/@hig/theme-context/build/index.es.js");
/* harmony import */ var _hig_behaviors__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @hig/behaviors */ "./node_modules/@hig/behaviors/build/index.es.js");






var variants = Object.freeze({
  LINE: "line",
  BOX: "box",
  PLAIN: "plain"
});

var availableVariants = Object.freeze(Object.values(variants));

var tagNames = Object.freeze({
  INPUT: "input",
  TEXTAREA: "textarea"
});

var availableTagNames = Object.freeze(Object.values(tagNames));

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function inputStyles(_ref, themeData) {
  var isDisabled = _ref.isDisabled,
      hasFocus = _ref.hasFocus,
      hasHover = _ref.hasHover;

  var defaults = {
    color: themeData["input.fontColor"],
    height: "calc(" + themeData["input.height"] + " - 2px)",
    transitionProperty: "color",
    transitionDuration: "0.3s"
  };

  if (isDisabled) {
    return _extends({}, defaults);
  }
  if (hasFocus) {
    return _extends({}, defaults, {
      transitionDuration: "0.1s"
    });
  }
  if (hasHover) {
    return _extends({}, defaults, {
      transitionDuration: "0.1s"
    });
  }
  return defaults;
}

function stylesheet(props, themeData) {
  return {
    input: _extends({
      backgroundColor: props.variant === variants.BOX ? themeData["input.box.backgroundColor"] : "transparent",
      boxSizing: "border-box",
      border: "none",
      appearance: "none",
      paddingTop: themeData["input.verticalPadding"],
      paddingRight: props.variant === variants.BOX ? themeData["input.boxType.horizontalPadding"] : themeData["input.horizontalPadding"],
      paddingBottom: themeData["input.verticalPadding"],
      paddingLeft: props.variant === variants.BOX ? themeData["input.boxType.horizontalPadding"] : themeData["input.horizontalPadding"],
      outline: "none",
      fontSize: themeData["density.fontSizes.medium"],
      fontFamily: themeData["input.fontFamily"],
      fontWeight: themeData["input.fontWeight"],
      lineHeight: themeData["input.lineHeight"],
      width: "100%",
      "&::placeholder": {
        color: themeData["input.placeholder.fontColor"]
      },
      "&::-ms-input-placeholder": {
        color: themeData["input.placeholder.fontColor"]
      },
      "::selection": {
        backgroundColor: themeData["input.highlightColor"]
      }
    }, inputStyles(props, themeData))
  };
}

var _extends$1 = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function InputPresenter(props) {
  var disabled = props.disabled,
      hasFocus = props.hasFocus,
      hasHover = props.hasHover,
      onBlur = props.onBlur,
      onFocus = props.onFocus,
      onMouseEnter = props.onMouseEnter,
      onMouseLeave = props.onMouseLeave,
      tagName = props.tagName,
      customStylesheet = props.stylesheet,
      variant = props.variant,
      otherProps = _objectWithoutProperties(props, ["disabled", "hasFocus", "hasHover", "onBlur", "onFocus", "onMouseEnter", "onMouseLeave", "tagName", "stylesheet", "variant"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__["ThemeContext"].Consumer,
    null,
    function (_ref) {
      var resolvedRoles = _ref.resolvedRoles,
          metadata = _ref.metadata;

      var styles = stylesheet({ isDisabled: disabled, hasFocus: hasFocus, hasHover: hasHover, variant: variant }, resolvedRoles);
      var cssStyles = customStylesheet ? customStylesheet(styles, props, resolvedRoles, metadata.colorSchemeId) : styles;

      var className = Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(cssStyles.input);
      var customClassName = otherProps.className;

      if (customClassName) {
        className = Object(emotion__WEBPACK_IMPORTED_MODULE_2__["cx"])(className, variant === variants.PLAIN ? customClassName : customClassName.split(" ").reduce(function (acc, cur) {
          return Object(emotion__WEBPACK_IMPORTED_MODULE_2__["cx"])(acc, cur.trim() + "__input");
        }, ""));
      }

      var tagNameKey = tagName.toUpperCase();
      var Element = tagNames[tagNameKey];

      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Element, _extends$1({}, otherProps, {
        className: className,
        disabled: disabled,
        onBlur: onBlur,
        onFocus: onFocus,
        onMouseEnter: onMouseEnter,
        onMouseLeave: onMouseLeave
      }));
    }
  );
}

InputPresenter.propTypes = {
  disabled: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  hasFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  hasHover: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  onBlur: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseEnter: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  stylesheet: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  tagName: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableTagNames),
  variant: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableVariants)
};

InputPresenter.defaultProps = {
  tagName: tagNames.INPUT
};
InputPresenter.__docgenInfo = {
  "description": "",
  "displayName": "InputPresenter",
  "props": {
    "disabled": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "hasFocus": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "hasHover": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "onBlur": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onFocus": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseEnter": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "stylesheet": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "tagName": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableTagNames"
      },
      "required": false,
      "description": "",
      "defaultValue": {
        "value": "tagNames.INPUT",
        "computed": true
      }
    },
    "variant": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableVariants"
      },
      "required": false,
      "description": ""
    }
  }
};

var _extends$2 = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function borderStyles(_ref, themeData) {
  var variant = _ref.variant;

  var defaults = {
    borderStyle: "solid",
    borderColor: "transparent",
    borderWidth: themeData["input.borderWidth"],
    borderBottomColor: themeData["input.borderBottomColor"]
  };

  return variant === variants.BOX ? _extends$2({}, defaults, {
    borderColor: themeData["input.borderColor"],
    borderBottomColor: themeData["input.box.borderBottomColor"]
  }) : defaults;
}

function borderBottomStyles(_ref2, themeData) {
  var isDisabled = _ref2.isDisabled,
      hasFocus = _ref2.hasFocus,
      hasHover = _ref2.hasHover,
      error = _ref2.error;

  var defaults = {
    borderBottomStyle: "solid",
    borderBottomWidth: themeData["input.borderBottomWidth"]
  };

  if (isDisabled) {
    return _extends$2({}, defaults, {
      opacity: themeData["input.disabled.opacity"]
    });
  }
  if (hasFocus) {
    return _extends$2({}, defaults, {
      borderBottomColor: themeData["input.focus.borderBottomColor"]
    });
  }
  if (hasHover) {
    return _extends$2({}, defaults, {
      borderBottomColor: themeData["input.hover.borderBottomColor"]
    });
  }
  if (error) {
    return _extends$2({}, defaults, {
      borderBottomColor: themeData["input.error.borderBottomColor"]
    });
  }
  return defaults;
}

function haloStyles(_ref3, themeData) {
  var isDisabled = _ref3.isDisabled,
      hasFocus = _ref3.hasFocus,
      hasHover = _ref3.hasHover;

  var defaults = {
    height: 0,
    backgroundColor: themeData["input.halo.color"],
    transitionProperty: "height, color",
    transitionDuration: "0.3s, 0.3s"
  };

  if (isDisabled) {
    return {};
  }
  if (hasFocus) {
    return {
      height: themeData["input.focus.halo.width"],
      backgroundColor: themeData["input.focus.halo.color"],
      transitionDuration: "0.1s, 0.1s"
    };
  }
  if (hasHover) {
    return _extends$2({}, defaults, {
      height: themeData["input.hover.halo.width"],
      transitionDuration: "0.1s, 0.1s"
    });
  }
  return defaults;
}

function stylesheet$1 (props, themeData) {
  return {
    wrapper: _extends$2({
      flexGrow: 2,
      position: "relative"
    }, borderStyles(props, themeData), borderBottomStyles(props, themeData)),
    halo: _extends$2({
      position: "absolute",
      top: "calc(100% + 1px)",
      left: "-1px",
      right: 0,
      width: "calc(100% + 2px)"
    }, haloStyles(props, themeData))
  };
}

function _objectWithoutProperties$1(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function InputHaloPresenter(props) {
  var children = props.children,
      hasFocus = props.hasFocus,
      hasHover = props.hasHover,
      isDisabled = props.isDisabled,
      customStylesheet = props.stylesheet,
      variant = props.variant,
      error = props.error,
      otherProps = _objectWithoutProperties$1(props, ["children", "hasFocus", "hasHover", "isDisabled", "stylesheet", "variant", "error"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__["ThemeContext"].Consumer,
    null,
    function (_ref) {
      var resolvedRoles = _ref.resolvedRoles,
          metadata = _ref.metadata;

      var styles = stylesheet$1({ isDisabled: isDisabled, hasFocus: hasFocus, hasHover: hasHover, variant: variant, error: error }, resolvedRoles);
      var cssStyles = customStylesheet ? customStylesheet(styles, props, resolvedRoles, metadata.colorSchemeId) : styles;

      var className = otherProps.className;


      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
        "div",
        { className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(cssStyles.wrapper), className) },
        children,
        react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement("div", {
          className: Object(emotion__WEBPACK_IMPORTED_MODULE_2__["cx"])(Object(emotion__WEBPACK_IMPORTED_MODULE_2__["css"])(cssStyles.halo), className ? className.split(" ").reduce(function (acc, cur) {
            return Object(emotion__WEBPACK_IMPORTED_MODULE_2__["cx"])(acc, cur.trim() + "__halo");
          }, "") : "")
        })
      );
    }
  );
}

InputHaloPresenter.propTypes = {
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.node,
  hasFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  hasHover: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  isDisabled: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  stylesheet: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  variant: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableVariants),
  error: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool
};
InputHaloPresenter.__docgenInfo = {
  "description": "",
  "displayName": "InputHaloPresenter",
  "props": {
    "children": {
      "type": {
        "name": "node"
      },
      "required": false,
      "description": ""
    },
    "hasFocus": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "hasHover": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "isDisabled": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    },
    "stylesheet": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": ""
    },
    "variant": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableVariants"
      },
      "required": false,
      "description": ""
    },
    "error": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": ""
    }
  }
};

var _extends$3 = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _objectWithoutProperties$2(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function Wrapper(props) {
  if (props.variant === variants.PLAIN) {
    return props.children;
  }

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(InputHaloPresenter, props);
}

Wrapper.propTypes = {
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.node,
  tagName: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableTagNames),
  variant: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableVariants)
};

function Input(props) {
  var onFocusProp = props.onFocus,
      onBlurProp = props.onBlur,
      onMouseEnterProp = props.onMouseEnter,
      onMouseLeaveProp = props.onMouseLeave,
      disabledProp = props.disabled,
      stylesheet = props.stylesheet,
      tagName = props.tagName,
      variant = props.variant,
      error = props.error,
      otherProps = _objectWithoutProperties$2(props, ["onFocus", "onBlur", "onMouseEnter", "onMouseLeave", "disabled", "stylesheet", "tagName", "variant", "error"]);

  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
    _hig_behaviors__WEBPACK_IMPORTED_MODULE_4__["ControlBehavior"],
    {
      onFocus: onFocusProp,
      onBlur: onBlurProp,
      onMouseEnter: onMouseEnterProp,
      onMouseLeave: onMouseLeaveProp
    },
    function (_ref) {
      var hasHover = _ref.hasHover,
          onMouseEnter = _ref.onMouseEnter,
          onMouseLeave = _ref.onMouseLeave,
          hasFocus = _ref.hasFocus,
          onFocus = _ref.onFocus,
          onBlur = _ref.onBlur;
      return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(
        Wrapper,
        _extends$3({
          isDisabled: disabledProp,
          hasFocus: hasFocus,
          hasHover: hasHover,
          stylesheet: stylesheet,
          tagName: tagName,
          variant: variant,
          error: error
        }, otherProps),
        react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(InputPresenter, _extends$3({
          disabled: disabledProp,
          hasFocus: hasFocus,
          hasHover: hasHover,
          onBlur: onBlur,
          onFocus: onFocus,
          onMouseEnter: onMouseEnter,
          onMouseLeave: onMouseLeave,
          stylesheet: stylesheet,
          tagName: tagName,
          variant: variant
        }, otherProps))
      );
    }
  );
}

Input.propTypes = {
  /**
   * The initial value of the control
   */
  defaultValue: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string,
  /**
   * Prevents the user from interacting with the input
   */
  disabled: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool,
  /**
   * Fired when an element has lost focus
   */
  onBlur: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Fired when an element has received focus
   */
  onFocus: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Fired when a pointing device is moved over the element
   */
  onMouseEnter: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Fired when the pointer of a pointing device is moved out of an element
   */
  onMouseLeave: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Adds custom/overriding styles
   */
  stylesheet: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func,
  /**
   * Renders the HTML element that is passed in
   */
  tagName: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableTagNames),
  /**
   * The value of the control
   */
  value: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.string,
  /**
   * The visual variant of the input
   */
  variant: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.oneOf(availableVariants),
  /**
   * Specify if the value of the input is wrong
   */
  error: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.bool
};

Input.defaultProps = {
  tagName: tagNames.INPUT,
  variant: variants.LINE,
  error: false
};
Input.__docgenInfo = {
  "description": "",
  "displayName": "Input",
  "props": {
    "defaultValue": {
      "type": {
        "name": "string"
      },
      "required": false,
      "description": "The initial value of the control"
    },
    "disabled": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Prevents the user from interacting with the input"
    },
    "onBlur": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Fired when an element has lost focus"
    },
    "onFocus": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Fired when an element has received focus"
    },
    "onMouseEnter": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Fired when a pointing device is moved over the element"
    },
    "onMouseLeave": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Fired when the pointer of a pointing device is moved out of an element"
    },
    "stylesheet": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "Adds custom/overriding styles"
    },
    "tagName": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableTagNames"
      },
      "required": false,
      "description": "Renders the HTML element that is passed in",
      "defaultValue": {
        "value": "tagNames.INPUT",
        "computed": true
      }
    },
    "value": {
      "type": {
        "name": "string"
      },
      "required": false,
      "description": "The value of the control"
    },
    "variant": {
      "type": {
        "name": "enum",
        "computed": true,
        "value": "availableVariants"
      },
      "required": false,
      "description": "The visual variant of the input",
      "defaultValue": {
        "value": "variants.LINE",
        "computed": true
      }
    },
    "error": {
      "type": {
        "name": "bool"
      },
      "required": false,
      "description": "Specify if the value of the input is wrong",
      "defaultValue": {
        "value": "false",
        "computed": false
      }
    }
  }
};

/* harmony default export */ __webpack_exports__["default"] = (Input);



/***/ }),

/***/ "./node_modules/@hig/theme-context/build/index.es.js":
/*!***********************************************************!*\
  !*** ./node_modules/@hig/theme-context/build/index.es.js ***!
  \***********************************************************/
/*! exports provided: default, ThemeContext */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ThemeContext", function() { return index; });
/* harmony import */ var create_react_context__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! create-react-context */ "./node_modules/create-react-context/lib/index.js");
/* harmony import */ var create_react_context__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(create_react_context__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _hig_theme_data_build_json_lightGrayMediumDensityTheme_theme_json__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @hig/theme-data/build/json/lightGrayMediumDensityTheme/theme.json */ "./node_modules/@hig/theme-data/build/json/lightGrayMediumDensityTheme/theme.json");
var _hig_theme_data_build_json_lightGrayMediumDensityTheme_theme_json__WEBPACK_IMPORTED_MODULE_1___namespace = /*#__PURE__*/__webpack_require__.t(/*! @hig/theme-data/build/json/lightGrayMediumDensityTheme/theme.json */ "./node_modules/@hig/theme-data/build/json/lightGrayMediumDensityTheme/theme.json", 1);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! prop-types */ "./node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_3__);





var _createReactContext = create_react_context__WEBPACK_IMPORTED_MODULE_0___default()(_hig_theme_data_build_json_lightGrayMediumDensityTheme_theme_json__WEBPACK_IMPORTED_MODULE_1__),
    Provider = _createReactContext.Provider,
    Consumer = _createReactContext.Consumer;

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

function isPresent(obj, prop) {
  return Object.prototype.hasOwnProperty.call(obj, prop);
}

function hasRoleFormat(prop) {
  return prop.search(/\./) > 0;
}

function resolvedRolesProxy(roles, theme) {
  if ((typeof roles === "undefined" ? "undefined" : _typeof(roles)) !== "object") return roles;

  return new Proxy(roles, {
    get: function get(obj, prop) {
      if (hasRoleFormat(prop) && !isPresent(obj, prop)) {
        /* eslint-disable-next-line no-console */
        console.error("Role " + prop.toString() + " does not exist");
        return obj[prop];
      }

      var hasRoleDeprecationInfo = isPresent(theme, "metadata") && isPresent(theme.metadata, "__deprecated__") && isPresent(theme.metadata.__deprecated__, prop);

      if (hasRoleDeprecationInfo) {
        var roleDeprecationInfo = theme.metadata.__deprecated__[prop];

        var equivalencyInfo = roleDeprecationInfo.equivalent ? " and has been renamed to " + roleDeprecationInfo.equivalent.toString() + "." : ".";

        /* eslint-disable-next-line no-console */
        console.warn("Role " + prop.toString() + " is deprecated" + equivalencyInfo);
      }

      return obj[prop];
    }
  });
}

function createThemeProxy(theme) {
  return new Proxy(theme, {
    get: function get(obj, prop) {
      var value = obj[prop];

      if (prop.toString() === "resolvedRoles") return resolvedRolesProxy(value, theme);

      if (!isPresent(obj, prop)) {
        /* eslint-disable-next-line no-console */
        console.error("Property " + prop.toString() + " does not exist.");
        return value;
      }

      if ((typeof value === "undefined" ? "undefined" : _typeof(value)) === "object" && value !== null) return createThemeProxy(value);

      return value;
    }
  });
}

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Consumer$1 = function (_Component) {
  _inherits(Consumer$$1, _Component);

  function Consumer$$1() {
    _classCallCheck(this, Consumer$$1);

    return _possibleConstructorReturn(this, (Consumer$$1.__proto__ || Object.getPrototypeOf(Consumer$$1)).apply(this, arguments));
  }

  _createClass(Consumer$$1, [{
    key: "render",
    value: function render() {
      var _this2 = this;

      return react__WEBPACK_IMPORTED_MODULE_2___default.a.createElement(
        Consumer,
        null,
        function (theme) {
          var isDebugging = "development" !== "production";
          var result = isDebugging ? createThemeProxy(theme) : theme;

          return _this2.props.children(result);
        }
      );
    }
  }]);

  return Consumer$$1;
}(react__WEBPACK_IMPORTED_MODULE_2__["Component"]);

Consumer$1.propTypes = {
  /** A theme provided to the consumer within */
  children: prop_types__WEBPACK_IMPORTED_MODULE_3___default.a.func
};
Consumer$1.__docgenInfo = {
  "description": "",
  "displayName": "Consumer",
  "props": {
    "children": {
      "type": {
        "name": "func"
      },
      "required": false,
      "description": "A theme provided to the consumer within"
    }
  }
};

var index = { Provider: Provider, Consumer: Consumer$1 };

/* harmony default export */ __webpack_exports__["default"] = (index);



/***/ }),

/***/ "./node_modules/@hig/theme-data/build/esm/lightGrayHighDensityTheme.js":
/*!*****************************************************************************!*\
  !*** ./node_modules/@hig/theme-data/build/esm/lightGrayHighDensityTheme.js ***!
  \*****************************************************************************/
/*! exports provided: metadata, resolvedRoles, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "metadata", function() { return metadata; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "resolvedRoles", function() { return resolvedRoles; });
const themeData = {
  "metadata": {
    "colorSchemeName": "Light gray",
    "colorSchemeId": "hig-light-gray",
    "className": "hig--light-gray-theme",
    "densityId": "high-density",
    "densityName": "High density",
    "name": "Light gray, High density",
    "fileName": "lightGrayHighDensityTheme",
    "id": "hig-light-gray-high-density",
    "__deprecated__": {
      "basics.colors.black": {
        "equivalent": "basics.colors.primary.black"
      },
      "basics.colors.white": {
        "equivalent": "basics.colors.primary.white"
      },
      "basics.colors.autodeskBlue100": {
        "equivalent": "basics.colors.primary.autodeskBlue.100"
      },
      "basics.colors.autodeskBlue200": {
        "equivalent": "basics.colors.primary.autodeskBlue.200"
      },
      "basics.colors.autodeskBlue300": {
        "equivalent": "basics.colors.primary.autodeskBlue.300"
      },
      "basics.colors.autodeskBlue400": {
        "equivalent": "basics.colors.primary.autodeskBlue.400"
      },
      "basics.colors.autodeskBlue500": {
        "equivalent": "basics.colors.primary.autodeskBlue.500"
      },
      "basics.colors.autodeskBlue600": {
        "equivalent": "basics.colors.primary.autodeskBlue.600"
      },
      "basics.colors.autodeskBlue700": {
        "equivalent": "basics.colors.primary.autodeskBlue.700"
      },
      "basics.colors.autodeskBlue800": {
        "equivalent": "basics.colors.primary.autodeskBlue.800"
      },
      "basics.colors.autodeskBlue900": {
        "equivalent": "basics.colors.primary.autodeskBlue.900"
      },
      "basics.colors.charcoal100": {
        "equivalent": "basics.colors.primary.charcoal.100"
      },
      "basics.colors.charcoal200": {
        "equivalent": "basics.colors.primary.charcoal.200"
      },
      "basics.colors.charcoal300": {
        "equivalent": "basics.colors.primary.charcoal.300"
      },
      "basics.colors.charcoal400": {
        "equivalent": "basics.colors.primary.charcoal.400"
      },
      "basics.colors.charcoal500": {
        "equivalent": "basics.colors.primary.charcoal.500"
      },
      "basics.colors.charcoal600": {
        "equivalent": "basics.colors.primary.charcoal.600"
      },
      "basics.colors.charcoal700": {
        "equivalent": "basics.colors.primary.charcoal.700"
      },
      "basics.colors.charcoal800": {
        "equivalent": "basics.colors.primary.charcoal.800"
      },
      "basics.colors.charcoal900": {
        "equivalent": "basics.colors.primary.charcoal.900"
      },
      "basics.colors.darkBlue100": {
        "equivalent": "basics.colors.secondary.darkBlue.100"
      },
      "basics.colors.darkBlue200": {
        "equivalent": "basics.colors.secondary.darkBlue.200"
      },
      "basics.colors.darkBlue300": {
        "equivalent": "basics.colors.secondary.darkBlue.300"
      },
      "basics.colors.darkBlue400": {
        "equivalent": "basics.colors.secondary.darkBlue.400"
      },
      "basics.colors.darkBlue500": {
        "equivalent": "basics.colors.secondary.darkBlue.500"
      },
      "basics.colors.darkBlue600": {
        "equivalent": "basics.colors.secondary.darkBlue.600"
      },
      "basics.colors.darkBlue700": {
        "equivalent": "basics.colors.secondary.darkBlue.700"
      },
      "basics.colors.darkBlue800": {
        "equivalent": "basics.colors.secondary.darkBlue.800"
      },
      "basics.colors.darkBlue900": {
        "equivalent": "basics.colors.secondary.darkBlue.900"
      },
      "basics.colors.green100": {
        "equivalent": "basics.colors.secondary.green.100"
      },
      "basics.colors.green200": {
        "equivalent": "basics.colors.secondary.green.200"
      },
      "basics.colors.green300": {
        "equivalent": "basics.colors.secondary.green.300"
      },
      "basics.colors.green400": {
        "equivalent": "basics.colors.secondary.green.400"
      },
      "basics.colors.green500": {
        "equivalent": "basics.colors.secondary.green.500"
      },
      "basics.colors.green600": {
        "equivalent": "basics.colors.secondary.green.600"
      },
      "basics.colors.green700": {
        "equivalent": "basics.colors.secondary.green.700"
      },
      "basics.colors.green800": {
        "equivalent": "basics.colors.secondary.green.800"
      },
      "basics.colors.green900": {
        "equivalent": "basics.colors.secondary.green.900"
      },
      "basics.colors.red100": {
        "equivalent": "basics.colors.secondary.red.100"
      },
      "basics.colors.red200": {
        "equivalent": "basics.colors.secondary.red.200"
      },
      "basics.colors.red300": {
        "equivalent": "basics.colors.secondary.red.300"
      },
      "basics.colors.red400": {
        "equivalent": "basics.colors.secondary.red.400"
      },
      "basics.colors.red500": {
        "equivalent": "basics.colors.secondary.red.500"
      },
      "basics.colors.red600": {
        "equivalent": "basics.colors.secondary.red.600"
      },
      "basics.colors.red700": {
        "equivalent": "basics.colors.secondary.red.700"
      },
      "basics.colors.red800": {
        "equivalent": "basics.colors.secondary.red.800"
      },
      "basics.colors.red900": {
        "equivalent": "basics.colors.secondary.red.900"
      },
      "basics.colors.turquoise100": {
        "equivalent": "basics.colors.secondary.turquoise.100"
      },
      "basics.colors.turquoise200": {
        "equivalent": "basics.colors.secondary.turquoise.200"
      },
      "basics.colors.turquoise300": {
        "equivalent": "basics.colors.secondary.turquoise.300"
      },
      "basics.colors.turquoise400": {
        "equivalent": "basics.colors.secondary.turquoise.400"
      },
      "basics.colors.turquoise500": {
        "equivalent": "basics.colors.secondary.turquoise.500"
      },
      "basics.colors.turquoise600": {
        "equivalent": "basics.colors.secondary.turquoise.600"
      },
      "basics.colors.turquoise700": {
        "equivalent": "basics.colors.secondary.turquoise.700"
      },
      "basics.colors.turquoise800": {
        "equivalent": "basics.colors.secondary.turquoise.800"
      },
      "basics.colors.turquoise900": {
        "equivalent": "basics.colors.secondary.turquoise.900"
      },
      "basics.colors.yellowOrange100": {
        "equivalent": "basics.colors.secondary.yellowOrange.100"
      },
      "basics.colors.yellowOrange200": {
        "equivalent": "basics.colors.secondary.yellowOrange.200"
      },
      "basics.colors.yellowOrange300": {
        "equivalent": "basics.colors.secondary.yellowOrange.300"
      },
      "basics.colors.yellowOrange400": {
        "equivalent": "basics.colors.secondary.yellowOrange.400"
      },
      "basics.colors.yellowOrange500": {
        "equivalent": "basics.colors.secondary.yellowOrange.500"
      },
      "basics.colors.yellowOrange600": {
        "equivalent": "basics.colors.secondary.yellowOrange.600"
      },
      "basics.colors.yellowOrange700": {
        "equivalent": "basics.colors.secondary.yellowOrange.700"
      },
      "basics.colors.yellowOrange800": {
        "equivalent": "basics.colors.secondary.yellowOrange.800"
      },
      "basics.colors.yellowOrange900": {
        "equivalent": "basics.colors.secondary.yellowOrange.900"
      },
      "basics.colors.pink100": {
        "equivalent": "basics.colors.tertiary.pink.100"
      },
      "basics.colors.pink200": {
        "equivalent": "basics.colors.tertiary.pink.200"
      },
      "basics.colors.pink300": {
        "equivalent": "basics.colors.tertiary.pink.300"
      },
      "basics.colors.pink400": {
        "equivalent": "basics.colors.tertiary.pink.400"
      },
      "basics.colors.pink500": {
        "equivalent": "basics.colors.tertiary.pink.500"
      },
      "basics.colors.pink600": {
        "equivalent": "basics.colors.tertiary.pink.600"
      },
      "basics.colors.pink700": {
        "equivalent": "basics.colors.tertiary.pink.700"
      },
      "basics.colors.pink800": {
        "equivalent": "basics.colors.tertiary.pink.800"
      },
      "basics.colors.pink900": {
        "equivalent": "basics.colors.tertiary.pink.900"
      },
      "basics.colors.purple100": {
        "equivalent": "basics.colors.tertiary.purple.100"
      },
      "basics.colors.purple200": {
        "equivalent": "basics.colors.tertiary.purple.200"
      },
      "basics.colors.purple300": {
        "equivalent": "basics.colors.tertiary.purple.300"
      },
      "basics.colors.purple400": {
        "equivalent": "basics.colors.tertiary.purple.400"
      },
      "basics.colors.purple500": {
        "equivalent": "basics.colors.tertiary.purple.500"
      },
      "basics.colors.purple600": {
        "equivalent": "basics.colors.tertiary.purple.600"
      },
      "basics.colors.purple700": {
        "equivalent": "basics.colors.tertiary.purple.700"
      },
      "basics.colors.purple800": {
        "equivalent": "basics.colors.tertiary.purple.800"
      },
      "basics.colors.purple900": {
        "equivalent": "basics.colors.tertiary.purple.900"
      },
      "basics.colors.salmon100": {
        "equivalent": "basics.colors.tertiary.salmon.100"
      },
      "basics.colors.salmon200": {
        "equivalent": "basics.colors.tertiary.salmon.200"
      },
      "basics.colors.salmon300": {
        "equivalent": "basics.colors.tertiary.salmon.300"
      },
      "basics.colors.salmon400": {
        "equivalent": "basics.colors.tertiary.salmon.400"
      },
      "basics.colors.salmon500": {
        "equivalent": "basics.colors.tertiary.salmon.500"
      },
      "basics.colors.salmon600": {
        "equivalent": "basics.colors.tertiary.salmon.600"
      },
      "basics.colors.salmon700": {
        "equivalent": "basics.colors.tertiary.salmon.700"
      },
      "basics.colors.salmon800": {
        "equivalent": "basics.colors.tertiary.salmon.800"
      },
      "basics.colors.salmon900": {
        "equivalent": "basics.colors.tertiary.salmon.900"
      },
      "basics.colors.slate100": {
        "equivalent": "basics.colors.tertiary.slate.100"
      },
      "basics.colors.slate200": {
        "equivalent": "basics.colors.tertiary.slate.200"
      },
      "basics.colors.slate300": {
        "equivalent": "basics.colors.tertiary.slate.300"
      },
      "basics.colors.slate400": {
        "equivalent": "basics.colors.tertiary.slate.400"
      },
      "basics.colors.slate500": {
        "equivalent": "basics.colors.tertiary.slate.500"
      },
      "basics.colors.slate600": {
        "equivalent": "basics.colors.tertiary.slate.600"
      },
      "basics.colors.slate700": {
        "equivalent": "basics.colors.tertiary.slate.700"
      },
      "basics.colors.slate800": {
        "equivalent": "basics.colors.tertiary.slate.800"
      },
      "basics.colors.slate900": {
        "equivalent": "basics.colors.tertiary.slate.900"
      },
      "basics.colors.surfaceLightGrayLevel100": {
        "equivalent": "basics.colors.surface.lightGray.100"
      },
      "basics.colors.surfaceLightGrayLevel200": {
        "equivalent": "basics.colors.surface.lightGray.200"
      },
      "basics.colors.surfaceLightGrayLevel250": {
        "equivalent": "basics.colors.surface.lightGray.250"
      },
      "basics.colors.surfaceLightGrayLevel300": {
        "equivalent": "basics.colors.surface.lightGray.300"
      },
      "basics.colors.surfaceLightGrayLevel350": {
        "equivalent": "basics.colors.surface.lightGray.350"
      },
      "basics.colors.surfaceDarkBlueLevel100": {
        "equivalent": "basics.colors.surface.darkBlue.100"
      },
      "basics.colors.surfaceDarkBlueLevel200": {
        "equivalent": "basics.colors.surface.darkBlue.200"
      },
      "basics.colors.surfaceDarkBlueLevel250": {
        "equivalent": "basics.colors.surface.darkBlue.250"
      },
      "basics.colors.surfaceDarkBlueLevel300": {
        "equivalent": "basics.colors.surface.darkBlue.300"
      },
      "basics.colors.surfaceDarkBlueLevel350": {
        "equivalent": "basics.colors.surface.darkBlue.350"
      },
      "basics.colors.surfaceDarkGrayLevel100": {
        "equivalent": "basics.colors.surface.darkGray.100"
      },
      "basics.colors.surfaceDarkGrayLevel200": {
        "equivalent": "basics.colors.surface.darkGray.200"
      },
      "basics.colors.surfaceDarkGrayLevel250": {
        "equivalent": "basics.colors.surface.darkGray.250"
      },
      "basics.colors.surfaceDarkGrayLevel300": {
        "equivalent": "basics.colors.surface.darkGray.300"
      },
      "basics.colors.surfaceDarkGrayLevel350": {
        "equivalent": "basics.colors.surface.darkGray.350"
      },
      "basics.colors.surfaceDarkBlueShadow": {
        "equivalent": "basics.colors.shadow.darkBlue"
      },
      "basics.colors.surfaceDarkGrayShadow": {
        "equivalent": "basics.colors.shadow.darkGray"
      },
      "basics.colors.surfaceLightGrayShadow": {
        "equivalent": "basics.colors.shadow.lightGray"
      },
      "basics.colors.textAgainstDark": {
        "equivalent": "basics.colors.text.againstDark"
      },
      "basics.colors.textAgainstLight": {
        "equivalent": "basics.colors.text.againstLight"
      },
      "basics.colors.iconLightGrayColor": {
        "equivalent": "basics.colors.icon.lightGray"
      },
      "basics.colors.iconDarkBlueColor": {
        "equivalent": "basics.colors.icon.darkBlue"
      },
      "basics.colors.iconDarkGrayColor": {
        "equivalent": "basics.colors.icon.darkGray"
      },
      "basics.colors.textLinkAgainstLight": {
        "equivalent": "basics.colors.textLink.primary.againstLight"
      },
      "basics.colors.textLinkAgainstDark": {
        "equivalent": "basics.colors.textLink.primary.againstDark"
      },
      "basics.colors.error": {
        "equivalent": "colorScheme.status.error"
      },
      "basics.colors.success": {
        "equivalent": "colorScheme.status.success"
      },
      "basics.colors.warning": {
        "equivalent": "colorScheme.status.warning"
      },
      "colorScheme.accentColor": {
        "equivalent": "colorScheme.reference.accent"
      },
      "colorScheme.baseColor": {
        "equivalent": "colorScheme.reference.base"
      },
      "colorScheme.surfaceLevel100Color": {
        "equivalent": "colorScheme.surface.level100"
      },
      "colorScheme.surfaceLevel200Color": {
        "equivalent": "colorScheme.surface.level200"
      },
      "colorScheme.surfaceLevel250Color": {
        "equivalent": "colorScheme.surface.level250"
      },
      "colorScheme.surfaceLevel300Color": {
        "equivalent": "colorScheme.surface.level300"
      },
      "colorScheme.surfaceLevel350Color": {
        "equivalent": "colorScheme.surface.level350"
      },
      "colorScheme.highShadowColor": {
        "equivalent": "colorScheme.shadow.high"
      },
      "colorScheme.lowShadowColor": {
        "equivalent": "colorScheme.shadow.low"
      },
      "colorScheme.textColor": {
        "equivalent": "colorScheme.text.default"
      },
      "colorScheme.textColorDim": {
        "equivalent": "colorScheme.text.dim"
      },
      "colorScheme.iconColor": {
        "equivalent": "colorScheme.icon.default"
      },
      "colorScheme.errorColor": {
        "equivalent": "colorScheme.status.error"
      },
      "colorScheme.successColor": {
        "equivalent": "colorScheme.status.success"
      },
      "colorScheme.warningColor": {
        "equivalent": "colorScheme.status.warning"
      },
      "colorScheme.infoColor": {
        "equivalent": "colorScheme.status.info"
      },
      "colorScheme.component.backgroundColor": {
        "equivalent": "colorScheme.surface.level"
      },
      "accordion.header.indicatorColor": {
        "equivalent": "accordion.header.default.indicatorColor"
      },
      "avatar.fontColorOnDark": {
        "equivalent": "avatar.darkBackground.fontColor"
      },
      "avatar.fontColorOnLight": {
        "equivalent": "avatar.lightBackground.fontColor"
      },
      "banner.info.iconBackground": {
        "equivalent": "banner.info.iconField.backgroundColor"
      },
      "banner.success.iconBackground": {
        "equivalent": "banner.success.iconField.backgroundColor"
      },
      "banner.warning.iconBackground": {
        "equivalent": "banner.warning.iconField.backgroundColor"
      },
      "banner.error.iconBackground": {
        "equivalent": "banner.error.iconField.backgroundColor"
      },
      "button.fontWeight": {
        "equivalent": "button.label.fontWeight"
      },
      "button.lineHeight": {
        "equivalent": "button.label.lineHeight"
      },
      "button.fontFamily": {
        "equivalent": "button.label.fontFamily"
      },
      "button.fontSize": {
        "equivalent": "button.label.fontSize"
      },
      "button.gutter": {
        "equivalent": "button.gutterWidth"
      },
      "button.horizontalPadding": {
        "equivalent": "button.paddingHorizontal"
      },
      "button.minimumWidth": {
        "equivalent": "button.minWidth"
      },
      "button.verticalPadding": {
        "equivalent": "button.paddingVertical"
      },
      "button.halo.width": {
        "equivalent": "button.haloWidth"
      },
      "button.halo.color": {
        "equivalent": "colorScheme.halo"
      },
      "button.hover.halo.width": {
        "equivalent": "button.hover.haloWidth"
      },
      "button.focus.halo.width": {
        "equivalent": "button.haloWidth"
      },
      "button.pressed.halo.width": {
        "equivalent": "button.pressed.haloWidth"
      },
      "button.focus.halo.color": {
        "equivalent": "button.focus.haloColor"
      },
      "button.pressed.halo.color": {
        "equivalent": "button.pressed.haloColor"
      },
      "button.outline.backgroundColor": {
        "equivalent": "button.outline.default.backgroundColor"
      },
      "button.outline.borderColor": {
        "equivalent": "button.outline.default.borderColor"
      },
      "button.outline.focus.halo.color": {
        "equivalent": "button.focus.haloColor"
      },
      "button.outline.focus.icon.color": {
        "equivalent": "button.outline.iconColor"
      },
      "button.outline.focus.textColor": {
        "equivalent": "button.outline.label.fontColor"
      },
      "button.outline.halo.color": {
        "equivalent": "colorScheme.halo"
      },
      "button.outline.hover.halo.color": {
        "equivalent": "button.hover.haloColor"
      },
      "button.outline.hover.icon.color": {
        "equivalent": "button.outline.iconColor"
      },
      "button.outline.hover.textColor": {
        "equivalent": "button.outline.label.fontColor"
      },
      "button.outline.icon.color": {
        "equivalent": "button.outline.iconColor"
      },
      "button.outline.pressed.halo.color": {
        "equivalent": "button.pressed.haloColor"
      },
      "button.outline.pressed.haloWidth": {
        "equivalent": "button.pressed.haloWidth"
      },
      "button.outline.pressed.icon.color": {
        "equivalent": "button.outline.iconColor"
      },
      "button.outline.pressed.textColor": {
        "equivalent": "button.outline.label.fontColor"
      },
      "button.outline.textColor": {
        "equivalent": "button.outline.label.fontColor"
      },
      "button.solid.backgroundColor": {
        "equivalent": "button.solid.default.backgroundColor"
      },
      "button.solid.borderColor": {
        "equivalent": "button.solid.default.borderColor"
      },
      "button.solid.halo.color": {
        "equivalent": "colorScheme.halo"
      },
      "button.solid.textColor": {
        "equivalent": "button.solid.label.fontColor"
      },
      "button.solid.icon.color": {
        "equivalent": "button.solid.iconColor"
      },
      "button.solid.hover.halo.color": {
        "equivalent": "button.hover.haloColor"
      },
      "button.solid.hover.textColor": {
        "equivalent": "button.solid.label.fontColor"
      },
      "button.solid.hover.icon.color": {
        "equivalent": "button.solid.iconColor"
      },
      "button.solid.focus.halo.color": {
        "equivalent": "button.focus.haloColor"
      },
      "button.solid.focus.textColor": {
        "equivalent": "button.solid.label.fontColor"
      },
      "button.solid.focus.icon.color": {
        "equivalent": "button.solid.iconColor"
      },
      "button.solid.pressed.haloWidth": {
        "equivalent": "button.pressed.haloWidth"
      },
      "button.solid.pressed.halo.color": {
        "equivalent": "button.pressed.haloColor"
      },
      "button.solid.pressed.textColor": {
        "equivalent": "button.solid.label.fontColor"
      },
      "button.solid.pressed.icon.color": {
        "equivalent": "button.solid.iconColor"
      },
      "button.flat.backgroundColor": {
        "equivalent": "button.flat.default.backgroundColor"
      },
      "button.flat.borderColor": {
        "equivalent": "button.flat.default.borderColor"
      },
      "button.flat.halo.color": {
        "equivalent": "colorScheme.halo"
      },
      "button.flat.textColor": {
        "equivalent": "button.flat.label.fontColor"
      },
      "button.flat.icon.color": {
        "equivalent": "button.flat.iconColor"
      },
      "button.flat.hover.halo.color": {
        "equivalent": "button.hover.haloColor"
      },
      "button.flat.hover.textColor": {
        "equivalent": "button.flat.label.fontColor"
      },
      "button.flat.hover.icon.color": {
        "equivalent": "button.flat.iconColor"
      },
      "button.flat.focus.halo.color": {
        "equivalent": "button.focus.haloColor"
      },
      "button.flat.focus.textColor": {
        "equivalent": "button.flat.label.fontColor"
      },
      "button.flat.focus.icon.color": {
        "equivalent": "button.flat.iconColor"
      },
      "button.flat.pressed.haloWidth": {
        "equivalent": "button.pressed.haloWidth"
      },
      "button.flat.pressed.halo.color": {
        "equivalent": "button.pressed.haloColor"
      },
      "button.flat.pressed.textColor": {
        "equivalent": "button.flat.label.fontColor"
      },
      "button.flat.pressed.icon.color": {
        "equivalent": "button.flat.iconColor"
      },
      "checkbox.halo.color": {
        "equivalent": "checkbox.hover.haloColor"
      },
      "checkbox.halo.width": {
        "equivalent": "checkbox.hover.haloWidth"
      },
      "checkbox.height": {
        "equivalent": "checkbox.minHeight"
      },
      "checkbox.iconColor": {
        "equivalent": "checkbox.indicatorColor"
      },
      "checkbox.width": {
        "equivalent": "checkbox.minWidth"
      },
      "checkbox.backgroundColor": {
        "equivalent": "checkbox.unchecked.default.backgroundColor"
      },
      "checkbox.borderColor": {
        "equivalent": "checkbox.unchecked.default.borderColor"
      },
      "checkbox.hover.borderColor": {
        "equivalent": "checkbox.unchecked.hover.borderColor"
      },
      "checkbox.hover.halo.width": {
        "equivalent": "checkbox.hover.haloWidth"
      },
      "checkbox.hover.halo.color": {
        "equivalent": "checkbox.hover.haloColor"
      },
      "checkbox.focus.borderColor": {
        "equivalent": "checkbox.unchecked.focus.borderColor"
      },
      "checkbox.focus.halo.color": {
        "equivalent": "checkbox.focus.haloColor"
      },
      "checkbox.focus.halo.width": {
        "equivalent": "checkbox.focus.haloWidth"
      },
      "checkbox.pressed.borderColor": {
        "equivalent": "checkbox.unchecked.pressed.borderColor"
      },
      "checkbox.pressed.halo.color": {
        "equivalent": "checkbox.pressed.haloColor"
      },
      "checkbox.pressed.halo.width": {
        "equivalent": "checkbox.pressed.haloWidth"
      },
      "checkbox.checked.borderColor": {
        "equivalent": "checkbox.checked.default.borderColor"
      },
      "checkbox.checked.iconColor": {
        "equivalent": "checkbox.indicatorColor"
      },
      "checkbox.checked.backgroundColor": {
        "equivalent": "checkbox.checked.default.backgroundColor"
      },
      "checkbox.indeterminate.borderColor": {
        "equivalent": "checkbox.checked.default.borderColor"
      },
      "checkbox.indeterminate.iconColor": {
        "equivalent": "checkbox.indicatorColor"
      },
      "checkbox.indeterminate.backgroundColor": {
        "equivalent": "checkbox.checked.default.backgroundColor"
      },
      "checkbox.focus.checked.backgroundColor": {
        "equivalent": "checkbox.checked.default.backgroundColor"
      },
      "checkbox.focus.checked.borderColor": {
        "equivalent": "checkbox.checked.default.borderColor"
      },
      "checkbox.focus.indeterminate.borderColor": {
        "equivalent": "checkbox.checked.default.borderColor"
      },
      "component.disabled.opacity": {
        "equivalent": "colorScheme.opacity.disabled"
      },
      "divider.width": {
        "equivalent": "divider.borderWidth"
      },
      "formField.instructionalText.fontSize": {
        "equivalent": "input.value.fontSize"
      },
      "formField.instructionalText.fontWeight": {
        "equivalent": "input.value.fontWeight"
      },
      "formField.instructionalText.fontFamily": {
        "equivalent": "input.value.fontFamily"
      },
      "formField.instructionalText.fontColor": {
        "equivalent": "input.value.placeholderFontColor"
      },
      "formField.instructionalText.lineHeight": {
        "equivalent": "basics.lineHeights.medium"
      },
      "formField.icon.width": {
        "equivalent": "basics.spacings"
      },
      "formField.icon.paddingRight": {
        "equivalent": "density.spacings.small"
      },
      "formField.icon.color": {
        "equivalent": "input.indicator.default"
      },
      "iconButton.focus.halo.color": {
        "equivalent": "iconButton.focus.haloColor"
      },
      "iconButton.focus.halo.width": {
        "equivalent": "iconButton.focus.haloWidth"
      },
      "iconButton.focus.iconColor": {
        "equivalent": "iconButton.dynamic.focus.iconColor"
      },
      "iconButton.focus.on.iconColor": {
        "equivalent": "iconButton.dynamic.on.focus.iconColor"
      },
      "iconButton.hover.iconColor": {
        "equivalent": "iconButton.dynamic.hover.iconColor"
      },
      "iconButton.hover.on.iconColor": {
        "equivalent": "iconButton.dynamic.on.hover.iconColor"
      },
      "iconButton.iconColor": {
        "equivalent": "iconButton.dynamic.default.iconColor"
      },
      "iconButton.on.iconColor": {
        "equivalent": "iconButton.dynamic.on.default.iconColor"
      },
      "iconButton.pressed.backgroundColor": {
        "equivalent": "iconButton.dynamic.pressed.100To250BackgroundColor"
      },
      "iconButton.pressed.iconColor": {
        "equivalent": "iconButton.dynamic.pressed.iconColor"
      },
      "iconButton.pressed.level100To250.backgroundColor": {
        "equivalent": "iconButton.dynamic.pressed.100To250BackgroundColor"
      },
      "iconButton.pressed.level300To350.backgroundColor": {
        "equivalent": "iconButton.dynamic.pressed.300To350BackgroundColor"
      },
      "iconButton.pressed.on.iconColor": {
        "equivalent": "iconButton.dynamic.on.pressed.iconColor"
      },
      "iconButton.static.hover.level100To250.backgroundColor": {
        "equivalent": "iconButton.static.hover.100To250BackgroundColor"
      },
      "iconButton.static.hover.level300To350.backgroundColor": {
        "equivalent": "iconButton.static.hover.300To350BackgroundColor"
      },
      "iconButton.static.on.backgroundColor": {
        "equivalent": "iconButton.static.on.default.backgroundColor"
      },
      "iconButton.static.on.borderColor": {
        "equivalent": "iconButton.static.on.default.borderColor"
      },
      "iconButton.static.on.hover.level100To250.backgroundColor": {
        "equivalent": "iconButton.static.on.hover.100To250BackgroundColor"
      },
      "iconButton.static.on.hover.level300To350.backgroundColor": {
        "equivalent": "iconButton.static.on.hover.300To350BackgroundColor"
      },
      "input.active.halo.color": {
        "equivalent": "input.focus.haloColor"
      },
      "input.borderBottomColor": {
        "equivalent": "input.line.default.borderBottomColor"
      },
      "input.active.halo.width": {
        "equivalent": "input.haloWidth"
      },
      "input.box.backgroundColor": {
        "equivalent": "input.box.default.backgroundColor"
      },
      "input.box.borderBottomColor": {
        "equivalent": "input.box.default.borderBottomColor"
      },
      "input.boxType.horizontalPadding": {
        "equivalent": "input.box.paddingHorizontal"
      },
      "input.focus.borderBottomColor": {
        "equivalent": "input.line.focus.borderBottomColor"
      },
      "input.focus.halo.color": {
        "equivalent": "input.focus.haloColor"
      },
      "input.focus.halo.width": {
        "equivalent": "input.haloWidth"
      },
      "input.fontColor": {
        "equivalent": "input.value.fontColor"
      },
      "input.fontFamily": {
        "equivalent": "input.value.fontFamily"
      },
      "input.fontSize": {
        "equivalent": "input.value.fontSize"
      },
      "input.fontWeight": {
        "equivalent": "input.value.fontWeight"
      },
      "input.halo.color": {
        "equivalent": "input.hover.haloColor"
      },
      "input.halo.width": {
        "equivalent": "input.haloWidth"
      },
      "input.height": {
        "equivalent": "input.minHeight"
      },
      "input.highlightColor": {
        "equivalent": "input.value.highlightColor"
      },
      "input.horizontalPadding": {
        "equivalent": "input.line.paddingHorizontal"
      },
      "input.hover.borderBottomColor": {
        "equivalent": "input.box.hover.borderBottomColor"
      },
      "input.hover.halo.width": {
        "equivalent": "input.haloWidth"
      },
      "input.lineHeight": {
        "equivalent": "input.value.lineHeight"
      },
      "input.placeholder.fontColor": {
        "equivalent": "input.value.placeholderFontColor"
      },
      "input.verticalPadding": {
        "equivalent": "input.paddingVertical"
      },
      "menu.item.backgroundColor": {
        "equivalent": "menu.item.default.backgroundColor"
      },
      "menu.item.active.backgroundColor": {
        "equivalent": "menu.item.pressed.backgroundColor"
      },
      "menu.item.horizontalPadding": {
        "equivalent": "menu.item.paddingHorizontal"
      },
      "menu.item.verticalPadding": {
        "equivalent": "menu.item.paddingVertical"
      },
      "modal.overlay.color": {
        "equivalent": "modal.overlayColor"
      },
      "modal.textColor": {
        "equivalent": "modal.fontColor"
      },
      "modal.body.height": {
        "equivalent": "modal.body.minHeight"
      },
      "modal.horizontalPadding": {
        "equivalent": "modal.paddingHorizontal"
      },
      "modal.header.borderColor": {
        "equivalent": "modal.header.borderBottomColor"
      },
      "modal.header.height": {
        "equivalent": "modal.header.minHeight"
      },
      "modal.window.backgroundColor": {
        "equivalent": "modal.shell.backgroundColor"
      },
      "modal.window.borderRadius": {
        "equivalent": "modal.shell.borderRadius"
      },
      "modal.window.height": {
        "equivalent": "modal.shell.minHeight"
      },
      "modal.window.width": {
        "equivalent": "modal.shell.minWidth"
      },
      "modal.window.shadowColor": {
        "equivalent": "modal.shell.shadowColor"
      },
      "progressBar.backgroundColor": {
        "equivalent": "progress.bar.backgroundColor"
      },
      "progressBar.highlightColor": {
        "equivalent": "progress.bar.highlightColor"
      },
      "progressBar.borderRadius": {
        "equivalent": "progress.bar.borderRadius"
      },
      "progressBar.height": {
        "equivalent": "progress.bar.minHeight"
      },
      "progressRing.backgroundColor": {
        "equivalent": "progress.ring.backgroundColor"
      },
      "progressRing.highlightColor1": {
        "equivalent": "progress.ring.highlightColor1"
      },
      "progressRing.highlightColor2": {
        "equivalent": "progress.ring.highlightColor2"
      },
      "skeletonItem.defaultHeight": {
        "equivalent": "skeletonItem.minHeight"
      },
      "slider.halo.width": {
        "equivalent": "slider.hover.haloWidth"
      },
      "slider.halo.color": {
        "equivalent": "slider.hover.haloColor"
      },
      "slider.thumb.width": {
        "equivalent": "slider.thumb.minWidth"
      },
      "slider.focused.halo.color": {
        "equivalent": "slider.focus.haloColor"
      },
      "slider.focused.halo.width": {
        "equivalent": "slider.focus.haloWidth"
      },
      "slider.focused.thumb.color": {
        "equivalent": "slider.thumb.backgroundColor"
      },
      "slider.hover.halo.color": {
        "equivalent": "slider.hover.haloColor"
      },
      "slider.hover.halo.width": {
        "equivalent": "slider.hover.haloWidth"
      },
      "slider.hover.thumb.color": {
        "equivalent": "slider.thumb.backgroundColor"
      },
      "slider.pressed.halo.color": {
        "equivalent": "slider.pressed.haloColor"
      },
      "slider.pressed.halo.width": {
        "equivalent": "slider.pressed.haloWidth"
      },
      "slider.pressed.thumb.color": {
        "equivalent": "slider.thumb.backgroundColor"
      },
      "slider.track.color": {
        "equivalent": "slider.track.backgroundColor"
      },
      "slider.track.width": {
        "equivalent": "slider.track.minHeight"
      },
      "slider.value.color": {
        "equivalent": "slider.value.backgroundColor"
      },
      "tabs.general.tab.fontFamily": {
        "equivalent": "tabs.label.fontFamily"
      },
      "tabs.general.tab.fontSize": {
        "equivalent": "tabs.label.fontSize"
      },
      "tabs.general.tab.fontWeight": {
        "equivalent": "tabs.label.inactive.fontWeight"
      },
      "tabs.general.tab.active.fontWeight": {
        "equivalent": "tabs.label.active.fontWeight"
      },
      "tabs.general.tab.color": {
        "equivalent": "tabs.label.fontColor"
      },
      "tabs.general.tab.lineHeight": {
        "equivalent": "tabs.label.active.lineHeight"
      },
      "tabs.general.halo.size": {
        "equivalent": "tabs.focus.haloWidth"
      },
      "tabs.general.halo.color": {
        "equivalent": "tabs.focus.haloColor"
      },
      "tabs.general.icon.size": {
        "equivalent": "tabs.iconSize"
      },
      "tabs.general.icon.color": {
        "equivalent": "tabs.iconColor"
      },
      "tabs.general.icon.gutter": {
        "equivalent": "tabs.iconGutter"
      },
      "tabs.general.closeButton.size": {
        "equivalent": "tabs.closeButton.minSize"
      },
      "tabs.general.closeButton.gutter": {
        "equivalent": "tabs.closeButton.gutter"
      },
      "tabs.general.closeButton.color": {
        "equivalent": "tabs.closeButton.default.iconColor"
      },
      "tabs.general.closeButton.hover.color": {
        "equivalent": "tabs.closeButton.hover.iconColor"
      },
      "tabs.general.closeButton.pressed.color": {
        "equivalent": "tabs.closeButton.pressed.iconColor"
      },
      "tabs.underline.halo.hover.color": {
        "equivalent": "tabs.underline.hover.haloColor"
      },
      "tabs.underline.halo.active.color": {
        "equivalent": "tabs.underline.active.haloColor"
      },
      "tabs.box.tab.horizontalPadding": {
        "equivalent": "tabs.box.tab.paddingHorizontal"
      },
      "tabs.box.tab.verticalPadding": {
        "equivalent": "tabs.box.tab.paddingVertical"
      },
      "tabs.box.divider.color": {
        "equivalent": "tabs.box.divider.borderColor"
      },
      "tabs.box.divider.width": {
        "equivalent": "tabs.box.dividerWidth"
      },
      "tabs.box.divider.height": {
        "equivalent": "tabs.box.dividerHeight"
      },
      "tabs.canvas.tab.horizontalPadding": {
        "equivalent": "tabs.canvas.tab.paddingHorizontal"
      },
      "tabs.canvas.tab.verticalPadding": {
        "equivalent": "tabs.canvas.tab.paddingVertical"
      },
      "tabs.canvas.divider.color": {
        "equivalent": "tabs.canvas.divider.borderColor"
      },
      "tabs.canvas.divider.width": {
        "equivalent": "tabs.canvas.dividerWidth"
      },
      "tabs.canvas.divider.height": {
        "equivalent": "tabs.canvas.dividerHeight"
      },
      "textLink.textColor": {
        "equivalent": "textLink.primary.default.fontColor"
      },
      "textLink.hover.textColor": {
        "equivalent": "textLink.primary.hover.fontColor"
      },
      "textLink.focus.textColor": {
        "equivalent": "textLink.primary.focus.fontColor"
      },
      "textLink.hover.underline.color": {
        "equivalent": "textLink.primary.hover.underlineColor"
      },
      "textLink.focus.halo.color": {
        "equivalent": "textLink.focus.haloColor"
      },
      "textLink.focus.halo.width": {
        "equivalent": "textLink.focus.haloWidth"
      },
      "tooltip.textColor": {
        "equivalent": "tooltip.title.fontColor"
      },
      "tooltip.borderColor": {
        "equivalent": "tooltip.backgroundColor"
      }
    }
  },
  "resolvedRoles": {
    "basics.borderRadii.none": "0",
    "basics.borderRadii.small": "0",
    "basics.borderRadii.medium": "2px",
    "basics.borderRadii.large": "4px",
    "basics.borderRadii.ellipse": "50%",
    "basics.borderRadii.pill": "1000em",
    "basics.borderWidths.none": "0",
    "basics.borderWidths.small": "1px",
    "basics.borderWidths.medium": "2px",
    "basics.borderWidths.large": "4px",
    "basics.colors.primary.black": "#000000",
    "basics.colors.primary.white": "#ffffff",
    "basics.colors.primary.autodeskBlue.100": "#cdeaf7",
    "basics.colors.primary.autodeskBlue.200": "#9bd5ef",
    "basics.colors.primary.autodeskBlue.300": "#6ac0e7",
    "basics.colors.primary.autodeskBlue.400": "#38abdf",
    "basics.colors.primary.autodeskBlue.500": "#0696d7",
    "basics.colors.primary.autodeskBlue.600": "#0686c2",
    "basics.colors.primary.autodeskBlue.700": "#006eaf",
    "basics.colors.primary.autodeskBlue.800": "#034b6c",
    "basics.colors.primary.autodeskBlue.900": "#022c40",
    "basics.colors.primary.charcoal.100": "#eeeeee",
    "basics.colors.primary.charcoal.200": "#dcdcdc",
    "basics.colors.primary.charcoal.300": "#cccccc",
    "basics.colors.primary.charcoal.400": "#bbbbbb",
    "basics.colors.primary.charcoal.500": "#999999",
    "basics.colors.primary.charcoal.600": "#808080",
    "basics.colors.primary.charcoal.700": "#666666",
    "basics.colors.primary.charcoal.800": "#4d4d4d",
    "basics.colors.primary.charcoal.900": "#3c3c3c",
    "basics.colors.secondary.darkBlue.100": "#e1ebf7",
    "basics.colors.secondary.darkBlue.200": "#d1deee",
    "basics.colors.secondary.darkBlue.300": "#a3bcdc",
    "basics.colors.secondary.darkBlue.400": "#749bcb",
    "basics.colors.secondary.darkBlue.500": "#4679b9",
    "basics.colors.secondary.darkBlue.600": "#1858a8",
    "basics.colors.secondary.darkBlue.700": "#12437f",
    "basics.colors.secondary.darkBlue.800": "#0e3666",
    "basics.colors.secondary.darkBlue.900": "#0c2c54",
    "basics.colors.secondary.green.100": "#e7f2d9",
    "basics.colors.secondary.green.200": "#cfe4b3",
    "basics.colors.secondary.green.300": "#b7d78c",
    "basics.colors.secondary.green.400": "#9fc966",
    "basics.colors.secondary.green.500": "#87b340",
    "basics.colors.secondary.green.600": "#658d30",
    "basics.colors.secondary.green.700": "#5d822c",
    "basics.colors.secondary.green.800": "#4c6b24",
    "basics.colors.secondary.green.900": "#445e20",
    "basics.colors.secondary.red.100": "#ffd9d9",
    "basics.colors.secondary.red.200": "#f8d3d3",
    "basics.colors.secondary.red.300": "#f1a7a7",
    "basics.colors.secondary.red.400": "#eb7a7a",
    "basics.colors.secondary.red.500": "#ec4a41",
    "basics.colors.secondary.red.600": "#dd2222",
    "basics.colors.secondary.red.700": "#a61a1a",
    "basics.colors.secondary.red.800": "#6f1111",
    "basics.colors.secondary.red.900": "#630f0f",
    "basics.colors.secondary.turquoise.100": "#d6f2ef",
    "basics.colors.secondary.turquoise.200": "#ade4de",
    "basics.colors.secondary.turquoise.300": "#84d7ce",
    "basics.colors.secondary.turquoise.400": "#5bc9bd",
    "basics.colors.secondary.turquoise.500": "#32bcad",
    "basics.colors.secondary.turquoise.600": "#23a597",
    "basics.colors.secondary.turquoise.700": "#268d82",
    "basics.colors.secondary.turquoise.800": "#20756c",
    "basics.colors.secondary.turquoise.900": "#195e57",
    "basics.colors.secondary.yellowOrange.100": "#feecd1",
    "basics.colors.secondary.yellowOrange.200": "#fddaa4",
    "basics.colors.secondary.yellowOrange.300": "#fcc776",
    "basics.colors.secondary.yellowOrange.400": "#fbb549",
    "basics.colors.secondary.yellowOrange.500": "#faa21b",
    "basics.colors.secondary.yellowOrange.600": "#bb7a14",
    "basics.colors.secondary.yellowOrange.700": "#916010",
    "basics.colors.secondary.yellowOrange.800": "#7d510e",
    "basics.colors.secondary.yellowOrange.900": "#523609",
    "basics.colors.tertiary.pink.100": "#ffebf5",
    "basics.colors.tertiary.pink.200": "#ffd6eb",
    "basics.colors.tertiary.pink.300": "#ffb8db",
    "basics.colors.tertiary.pink.400": "#ff8cc6",
    "basics.colors.tertiary.pink.500": "#fc56a9",
    "basics.colors.tertiary.pink.600": "#e84396",
    "basics.colors.tertiary.pink.700": "#c72877",
    "basics.colors.tertiary.pink.800": "#991f5c",
    "basics.colors.tertiary.pink.900": "#781848",
    "basics.colors.tertiary.purple.100": "#f4edfd",
    "basics.colors.tertiary.purple.200": "#eadcfd",
    "basics.colors.tertiary.purple.300": "#d5bcf7",
    "basics.colors.tertiary.purple.400": "#c5a1f8",
    "basics.colors.tertiary.purple.500": "#b385f2",
    "basics.colors.tertiary.purple.600": "#a76ef5",
    "basics.colors.tertiary.purple.700": "#8558c5",
    "basics.colors.tertiary.purple.800": "#5f3e8e",
    "basics.colors.tertiary.purple.900": "#482f6b",
    "basics.colors.tertiary.salmon.100": "#ffefec",
    "basics.colors.tertiary.salmon.200": "#fcded9",
    "basics.colors.tertiary.salmon.300": "#ffbeb3",
    "basics.colors.tertiary.salmon.400": "#ff9582",
    "basics.colors.tertiary.salmon.500": "#f26a52",
    "basics.colors.tertiary.salmon.600": "#db5942",
    "basics.colors.tertiary.salmon.700": "#a84a39",
    "basics.colors.tertiary.salmon.800": "#853729",
    "basics.colors.tertiary.salmon.900": "#66251a",
    "basics.colors.tertiary.slate.100": "#e9f0f7",
    "basics.colors.tertiary.slate.200": "#dce7f3",
    "basics.colors.tertiary.slate.300": "#c1cedc",
    "basics.colors.tertiary.slate.400": "#a7bacf",
    "basics.colors.tertiary.slate.500": "#7993b0",
    "basics.colors.tertiary.slate.600": "#6784a6",
    "basics.colors.tertiary.slate.700": "#4b6b8f",
    "basics.colors.tertiary.slate.800": "#354d67",
    "basics.colors.tertiary.slate.900": "#2c3e53",
    "basics.colors.surface.lightGray.100": "#ffffff",
    "basics.colors.surface.lightGray.200": "#f5f5f5",
    "basics.colors.surface.lightGray.250": "#eeeeee",
    "basics.colors.surface.lightGray.300": "#d9d9d9",
    "basics.colors.surface.lightGray.350": "#cccccc",
    "basics.colors.surface.darkBlue.100": "#454f61",
    "basics.colors.surface.darkBlue.200": "#3b4453",
    "basics.colors.surface.darkBlue.250": "#2e3440",
    "basics.colors.surface.darkBlue.300": "#222933",
    "basics.colors.surface.darkBlue.350": "#1a1f25",
    "basics.colors.surface.darkGray.100": "#535353",
    "basics.colors.surface.darkGray.200": "#474747",
    "basics.colors.surface.darkGray.250": "#373737",
    "basics.colors.surface.darkGray.300": "#2a2a2a",
    "basics.colors.surface.darkGray.350": "#202020",
    "basics.colors.shadow.darkBlue": "rgba(26, 31, 38, 0.5)",
    "basics.colors.shadow.darkGray": "rgba(0, 0, 0, 0.3)",
    "basics.colors.shadow.lightGray": "rgba(0, 0, 0, 0.2)",
    "basics.colors.text.againstDark": "#f5f5f5",
    "basics.colors.text.againstLight": "#3c3c3c",
    "basics.colors.textLink.primary.againstLight": "#006eaf",
    "basics.colors.textLink.primary.againstDark": "#6dd2ff",
    "basics.colors.textLink.secondary.againstLight": "#3c3c3c",
    "basics.colors.textLink.secondary.againstDark": "#f5f5f5",
    "basics.colors.icon.lightGray": "#808080",
    "basics.colors.icon.darkBlue": "#a2a6b0",
    "basics.colors.icon.darkGray": "#999999",
    "basics.colors.black": "#000000",
    "basics.colors.white": "#ffffff",
    "basics.colors.autodeskBlue100": "#cdeaf7",
    "basics.colors.autodeskBlue200": "#9bd5ef",
    "basics.colors.autodeskBlue300": "#6ac0e7",
    "basics.colors.autodeskBlue400": "#38abdf",
    "basics.colors.autodeskBlue500": "#0696d7",
    "basics.colors.autodeskBlue600": "#0686c2",
    "basics.colors.autodeskBlue700": "#006eaf",
    "basics.colors.autodeskBlue800": "#034b6c",
    "basics.colors.autodeskBlue900": "#022c40",
    "basics.colors.charcoal100": "#eeeeee",
    "basics.colors.charcoal200": "#dcdcdc",
    "basics.colors.charcoal300": "#cccccc",
    "basics.colors.charcoal400": "#bbbbbb",
    "basics.colors.charcoal500": "#999999",
    "basics.colors.charcoal600": "#808080",
    "basics.colors.charcoal700": "#666666",
    "basics.colors.charcoal800": "#4d4d4d",
    "basics.colors.charcoal900": "#3c3c3c",
    "basics.colors.darkBlue100": "#e1ebf7",
    "basics.colors.darkBlue200": "#d1deee",
    "basics.colors.darkBlue300": "#a3bcdc",
    "basics.colors.darkBlue400": "#749bcb",
    "basics.colors.darkBlue500": "#4679b9",
    "basics.colors.darkBlue600": "#1858a8",
    "basics.colors.darkBlue700": "#12437f",
    "basics.colors.darkBlue800": "#0e3666",
    "basics.colors.darkBlue900": "#0c2c54",
    "basics.colors.green100": "#e7f2d9",
    "basics.colors.green200": "#cfe4b3",
    "basics.colors.green300": "#b7d78c",
    "basics.colors.green400": "#9fc966",
    "basics.colors.green500": "#87b340",
    "basics.colors.green600": "#658d30",
    "basics.colors.green700": "#5d822c",
    "basics.colors.green800": "#4c6b24",
    "basics.colors.green900": "#445e20",
    "basics.colors.red100": "#ffd9d9",
    "basics.colors.red200": "#f8d3d3",
    "basics.colors.red300": "#f1a7a7",
    "basics.colors.red400": "#eb7a7a",
    "basics.colors.red500": "#ec4a41",
    "basics.colors.red600": "#dd2222",
    "basics.colors.red700": "#a61a1a",
    "basics.colors.red800": "#6f1111",
    "basics.colors.red900": "#630f0f",
    "basics.colors.turquoise100": "#d6f2ef",
    "basics.colors.turquoise200": "#ade4de",
    "basics.colors.turquoise300": "#84d7ce",
    "basics.colors.turquoise400": "#5bc9bd",
    "basics.colors.turquoise500": "#32bcad",
    "basics.colors.turquoise600": "#23a597",
    "basics.colors.turquoise700": "#268d82",
    "basics.colors.turquoise800": "#20756c",
    "basics.colors.turquoise900": "#195e57",
    "basics.colors.yellowOrange100": "#feecd1",
    "basics.colors.yellowOrange200": "#fddaa4",
    "basics.colors.yellowOrange300": "#fcc776",
    "basics.colors.yellowOrange400": "#fbb549",
    "basics.colors.yellowOrange500": "#faa21b",
    "basics.colors.yellowOrange600": "#bb7a14",
    "basics.colors.yellowOrange700": "#916010",
    "basics.colors.yellowOrange800": "#7d510e",
    "basics.colors.yellowOrange900": "#523609",
    "basics.colors.pink100": "#ffebf5",
    "basics.colors.pink200": "#ffd6eb",
    "basics.colors.pink300": "#ffb8db",
    "basics.colors.pink400": "#ff8cc6",
    "basics.colors.pink500": "#fc56a9",
    "basics.colors.pink600": "#e84396",
    "basics.colors.pink700": "#c72877",
    "basics.colors.pink800": "#991f5c",
    "basics.colors.pink900": "#781848",
    "basics.colors.purple100": "#f4edfd",
    "basics.colors.purple200": "#eadcfd",
    "basics.colors.purple300": "#d5bcf7",
    "basics.colors.purple400": "#c5a1f8",
    "basics.colors.purple500": "#b385f2",
    "basics.colors.purple600": "#a76ef5",
    "basics.colors.purple700": "#8558c5",
    "basics.colors.purple800": "#5f3e8e",
    "basics.colors.purple900": "#482f6b",
    "basics.colors.salmon100": "#ffefec",
    "basics.colors.salmon200": "#fcded9",
    "basics.colors.salmon300": "#ffbeb3",
    "basics.colors.salmon400": "#ff9582",
    "basics.colors.salmon500": "#f26a52",
    "basics.colors.salmon600": "#db5942",
    "basics.colors.salmon700": "#a84a39",
    "basics.colors.salmon800": "#853729",
    "basics.colors.salmon900": "#66251a",
    "basics.colors.slate100": "#e9f0f7",
    "basics.colors.slate200": "#dce7f3",
    "basics.colors.slate300": "#c1cedc",
    "basics.colors.slate400": "#a7bacf",
    "basics.colors.slate500": "#7993b0",
    "basics.colors.slate600": "#6784a6",
    "basics.colors.slate700": "#4b6b8f",
    "basics.colors.slate800": "#354d67",
    "basics.colors.slate900": "#2c3e53",
    "basics.colors.surfaceLightGrayLevel100": "#ffffff",
    "basics.colors.surfaceLightGrayLevel200": "#f5f5f5",
    "basics.colors.surfaceLightGrayLevel250": "#eeeeee",
    "basics.colors.surfaceLightGrayLevel300": "#d9d9d9",
    "basics.colors.surfaceLightGrayLevel350": "#cccccc",
    "basics.colors.surfaceDarkBlueLevel100": "#454f61",
    "basics.colors.surfaceDarkBlueLevel200": "#3b4453",
    "basics.colors.surfaceDarkBlueLevel250": "#2e3440",
    "basics.colors.surfaceDarkBlueLevel300": "#222933",
    "basics.colors.surfaceDarkBlueLevel350": "#1a1f25",
    "basics.colors.surfaceDarkGrayLevel100": "#535353",
    "basics.colors.surfaceDarkGrayLevel200": "#474747",
    "basics.colors.surfaceDarkGrayLevel250": "#373737",
    "basics.colors.surfaceDarkGrayLevel300": "#2a2a2a",
    "basics.colors.surfaceDarkGrayLevel350": "#202020",
    "basics.colors.surfaceDarkBlueShadow": "rgba(26, 31, 38, 0.5)",
    "basics.colors.surfaceDarkGrayShadow": "rgba(0, 0, 0, 0.3)",
    "basics.colors.surfaceLightGrayShadow": "rgba(0, 0, 0, 0.2)",
    "basics.colors.textAgainstDark": "#f5f5f5",
    "basics.colors.textAgainstLight": "#3c3c3c",
    "basics.colors.iconLightGrayColor": "#808080",
    "basics.colors.iconDarkBlueColor": "#a2a6b0",
    "basics.colors.iconDarkGrayColor": "#999999",
    "basics.colors.textLinkAgainstLight": "#006eaf",
    "basics.colors.textLinkAgainstDark": "#6dd2ff",
    "basics.colors.error": "#ec4a41",
    "basics.colors.success": "#87b340",
    "basics.colors.warning": "#faa21b",
    "basics.fontFamilies.main": "ArtifaktElement, sans-serif",
    "basics.fontFamilies.monospace": "'Source Code Pro', monospace",
    "basics.fontSizes.highSmall": "11px",
    "basics.fontSizes.highMedium": "12px",
    "basics.fontSizes.highLarge": "16px",
    "basics.fontSizes.highExtraLarge": "20px",
    "basics.fontSizes.highExtraExtraLarge": "24px",
    "basics.fontSizes.mediumSmall": "12px",
    "basics.fontSizes.mediumMedium": "14px",
    "basics.fontSizes.mediumLarge": "20px",
    "basics.fontSizes.mediumExtraLarge": "24px",
    "basics.fontSizes.mediumExtraExtraLarge": "28px",
    "basics.fontSizes.lowSmall": "14px",
    "basics.fontSizes.lowMedium": "16px",
    "basics.fontSizes.lowLarge": "22px",
    "basics.fontSizes.lowExtraLarge": "26px",
    "basics.fontSizes.lowExtraExtraLarge": "30px",
    "basics.fontWeights.regular": 400,
    "basics.fontWeights.medium": 600,
    "basics.fontWeights.bold": 700,
    "basics.lineHeights.small": "1.3",
    "basics.lineHeights.medium": "1.4",
    "basics.lineHeights.large": "1.5",
    "basics.lineHeights.highSmall": "1.25",
    "basics.lineHeights.highMedium": "1.272727273",
    "basics.lineHeights.highLarge": "1.3",
    "basics.lineHeights.highExtraLarge": "1.333333333",
    "basics.lineHeights.highExtraExtraLarge": "1.375",
    "basics.lineHeights.mediumSmall": "1.25",
    "basics.lineHeights.mediumMedium": "1.285714286",
    "basics.lineHeights.mediumLarge": "1.3",
    "basics.lineHeights.mediumExtraLarge": "1.428571429",
    "basics.lineHeights.mediumExtraExtraLarge": "1.5",
    "basics.shadows.lowBlur": "4px",
    "basics.shadows.highBlur": "16px",
    "basics.spacings.none": "0",
    "basics.spacings.highExtraExtraSmall": "2px",
    "basics.spacings.highExtraSmall": "4px",
    "basics.spacings.highSmall": "8px",
    "basics.spacings.highMedium": "12px",
    "basics.spacings.highLarge": "16px",
    "basics.spacings.highExtraLarge": "24px",
    "basics.spacings.highExtraExtraLarge": "32px",
    "basics.spacings.mediumExtraExtraSmall": "4px",
    "basics.spacings.mediumExtraSmall": "8px",
    "basics.spacings.mediumSmall": "12px",
    "basics.spacings.mediumMedium": "16px",
    "basics.spacings.mediumLarge": "24px",
    "basics.spacings.mediumExtraLarge": "32px",
    "basics.spacings.mediumExtraExtraLarge": "48px",
    "basics.spacings.lowExtraExtraSmall": "8px",
    "basics.spacings.lowExtraSmall": "12px",
    "basics.spacings.lowSmall": "16px",
    "basics.spacings.lowMedium": "24px",
    "basics.spacings.lowLarge": "32px",
    "basics.spacings.lowExtraLarge": "48px",
    "basics.spacings.lowExtraExtraLarge": "64px",
    "colorScheme.reference.accent": "#0696d7",
    "colorScheme.reference.base": "#808080",
    "colorScheme.surface.level100": "#ffffff",
    "colorScheme.surface.level200": "#f5f5f5",
    "colorScheme.surface.level250": "#eeeeee",
    "colorScheme.surface.level300": "#d9d9d9",
    "colorScheme.surface.level350": "#cccccc",
    "colorScheme.background.selected": "rgba(6, 150, 215, 0.15)",
    "colorScheme.background.on.default": "rgba(205, 234, 247, 0.6)",
    "colorScheme.background.on.hover": "rgba(155, 213, 239, 0.7)",
    "colorScheme.background.on.focus": "rgba(205, 234, 247, 0.6)",
    "colorScheme.background.on.pressed": "rgba(106, 192, 231, 0.7)",
    "colorScheme.background.empty.level100To250.hover": "rgba(128, 128, 128, 0.1)",
    "colorScheme.background.empty.level100To250.pressed": "rgba(128, 128, 128, 0.2)",
    "colorScheme.background.empty.level300To350.hover": "rgba(255, 255, 255, 0.25)",
    "colorScheme.background.empty.level300To350.pressed": "rgba(255, 255, 255, 0.35)",
    "colorScheme.background.filled.level100To250.default": "rgba(128, 128, 128, 0.15)",
    "colorScheme.background.filled.level100To250.hover": "rgba(128, 128, 128, 0.25)",
    "colorScheme.background.filled.level100To250.focus": "rgba(128, 128, 128, 0.15)",
    "colorScheme.background.filled.level100To250.pressed": "rgba(128, 128, 128, 0.35)",
    "colorScheme.background.filled.level300To350.default": "rgba(255, 255, 255, 0.5)",
    "colorScheme.background.filled.level300To350.hover": "rgba(255, 255, 255, 0.7)",
    "colorScheme.background.filled.level300To350.focus": "rgba(255, 255, 255, 0.5)",
    "colorScheme.background.filled.level300To350.pressed": "#ffffff",
    "colorScheme.background.transparent": "rgba(128, 128, 128, 0)",
    "colorScheme.border.base": "rgba(128, 128, 128, 0.5)",
    "colorScheme.border.accent": "#0696d7",
    "colorScheme.border.on": "rgba(6, 150, 215, 0.5)",
    "colorScheme.border.transparent": "rgba(128, 128, 128, 0)",
    "colorScheme.halo.hover": "rgba(128, 128, 128, 0.15)",
    "colorScheme.halo.focus": "rgba(6, 150, 215, 0.35)",
    "colorScheme.halo.pressed": "rgba(128, 128, 128, 0.25)",
    "colorScheme.shadow.high": "rgba(0, 0, 0, 0.2)",
    "colorScheme.shadow.low": "rgba(0, 0, 0, 0.2)",
    "colorScheme.text.default": "#3c3c3c",
    "colorScheme.text.dim": "rgba(60, 60, 60, 0.7)",
    "colorScheme.text.placeholder": "rgba(60, 60, 60, 0.4)",
    "colorScheme.text.active": "#0696d7",
    "colorScheme.icon.default": "#808080",
    "colorScheme.icon.hover": "#3c3c3c",
    "colorScheme.icon.focus": "#808080",
    "colorScheme.icon.pressed": "#3c3c3c",
    "colorScheme.icon.on.default": "#0696d7",
    "colorScheme.icon.on.hover": "#006eaf",
    "colorScheme.icon.on.focus": "#0696d7",
    "colorScheme.icon.on.pressed": "#006eaf",
    "colorScheme.indicator.default": "rgba(102, 102, 102, 0.7)",
    "colorScheme.indicator.hover": "#666666",
    "colorScheme.indicator.focus": "rgba(102, 102, 102, 0.7)",
    "colorScheme.indicator.pressed": "#0696d7",
    "colorScheme.status.error": "#ec4a41",
    "colorScheme.status.success": "#87b340",
    "colorScheme.status.warning": "#faa21b",
    "colorScheme.status.info": "#0696d7",
    "colorScheme.status.read": "rgba(128, 128, 128, 0.4)",
    "colorScheme.divider.lightweight": "rgba(60, 60, 60, 0.1)",
    "colorScheme.divider.heavyweight": "rgba(60, 60, 60, 0.25)",
    "colorScheme.opacity.mask": "#e84396",
    "colorScheme.opacity.disabled": 0.4,
    "colorScheme.accentColor": "#0696d7",
    "colorScheme.baseColor": "#808080",
    "colorScheme.surfaceLevel100Color": "#ffffff",
    "colorScheme.surfaceLevel200Color": "#f5f5f5",
    "colorScheme.surfaceLevel250Color": "#eeeeee",
    "colorScheme.surfaceLevel300Color": "#d9d9d9",
    "colorScheme.surfaceLevel350Color": "#cccccc",
    "colorScheme.highShadowColor": "rgba(0, 0, 0, 0.2)",
    "colorScheme.lowShadowColor": "rgba(0, 0, 0, 0.2)",
    "colorScheme.textColor": "#3c3c3c",
    "colorScheme.textColorDim": "rgba(60, 60, 60, 0.7)",
    "colorScheme.iconColor": "#808080",
    "colorScheme.errorColor": "#ec4a41",
    "colorScheme.successColor": "#87b340",
    "colorScheme.warningColor": "#faa21b",
    "colorScheme.infoColor": "#0696d7",
    "colorScheme.component.backgroundColor": "#ffffff",
    "density.spacings.extraExtraSmall": "2px",
    "density.spacings.extraSmall": "4px",
    "density.spacings.small": "8px",
    "density.spacings.medium": "12px",
    "density.spacings.large": "16px",
    "density.spacings.extraLarge": "24px",
    "density.spacings.extraExtraLarge": "32px",
    "density.fontSizes.small": "11px",
    "density.fontSizes.medium": "12px",
    "density.fontSizes.large": "16px",
    "density.fontSizes.extraLarge": "20px",
    "density.fontSizes.extraExtraLarge": "24px",
    "accordion.header.fontColor": "#3c3c3c",
    "accordion.header.fontFamily": "ArtifaktElement, sans-serif",
    "accordion.header.fontSize": "12px",
    "accordion.header.fontWeight": 700,
    "accordion.header.lineHeight": "1.428571429",
    "accordion.header.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "accordion.header.paddingHorizontal": "4px",
    "accordion.header.paddingVertical": "4px",
    "accordion.header.indicatorGutter": "4px",
    "accordion.header.indicatorSize": "12px",
    "accordion.header.default.indicatorColor": "rgba(102, 102, 102, 0.7)",
    "accordion.header.hover.indicatorColor": "#666666",
    "accordion.header.indicatorRight.paddingLeft": "8px",
    "accordion.header.indicatorRight.paddingRight": "12px",
    "accordion.header.indicatorColor": "rgba(102, 102, 102, 0.7)",
    "avatar.fontWeight": 400,
    "avatar.darkBackground.fontColor": "#ffffff",
    "avatar.lightBackground.fontColor": "#3c3c3c",
    "avatar.extraSmall.fontSize": "11px",
    "avatar.extraSmall.diameter": "16px",
    "avatar.small.fontSize": "12px",
    "avatar.small.diameter": "24px",
    "avatar.medium.fontSize": "14px",
    "avatar.medium.diameter": "32px",
    "avatar.large.fontSize": "24px",
    "avatar.large.diameter": "48px",
    "avatar.extraLarge.fontSize": "30px",
    "avatar.extraLarge.diameter": "64px",
    "avatar.color1.backgroundColor": "#6ac0e7",
    "avatar.color2.backgroundColor": "#1858a8",
    "avatar.color3.backgroundColor": "#5d822c",
    "avatar.color4.backgroundColor": "#ff9582",
    "avatar.color5.backgroundColor": "#7993b0",
    "avatar.color6.backgroundColor": "#5bc9bd",
    "avatar.color7.backgroundColor": "#faa21b",
    "avatar.color1.fontColor": "#3c3c3c",
    "avatar.color2.fontColor": "#ffffff",
    "avatar.color3.fontColor": "#ffffff",
    "avatar.color4.fontColor": "#3c3c3c",
    "avatar.color5.fontColor": "#ffffff",
    "avatar.color6.fontColor": "#3c3c3c",
    "avatar.color7.fontColor": "#3c3c3c",
    "avatar.fontColorOnDark": "#ffffff",
    "avatar.fontColorOnLight": "#3c3c3c",
    "avatarBundle.fontColor": "#3c3c3c",
    "avatarBundle.default.backgroundColor": "#eeeeee",
    "avatarBundle.hover.backgroundColor": "#eeeeee",
    "avatarBundle.pressed.backgroundColor": "#eeeeee",
    "avatarBundle.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "avatarBundle.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "avatarBundle.hover.haloWidth": "2px",
    "avatarBundle.pressed.haloWidth": "4px",
    "banner.error.backgroundColor": "#ffefec",
    "banner.error.borderColor": "#f1a7a7",
    "banner.error.iconField.backgroundColor": "#ec4a41",
    "banner.info.backgroundColor": "#cdeaf7",
    "banner.info.borderColor": "#9bd5ef",
    "banner.info.iconField.backgroundColor": "#0696d7",
    "banner.success.backgroundColor": "#e7f2d9",
    "banner.success.borderColor": "#b7d78c",
    "banner.success.iconField.backgroundColor": "#87b340",
    "banner.warning.backgroundColor": "#feecd1",
    "banner.warning.borderColor": "#fcc776",
    "banner.warning.iconField.backgroundColor": "#faa21b",
    "banner.info.iconBackground": "#0696d7",
    "banner.success.iconBackground": "#87b340",
    "banner.warning.iconBackground": "#faa21b",
    "banner.error.iconBackground": "#ec4a41",
    "breadcrumb.label.fontColor": "#3c3c3c",
    "breadcrumb.label.fontFamily": "ArtifaktElement, sans-serif",
    "breadcrumb.label.fontSize": "12px",
    "breadcrumb.label.default.fontWeight": 400,
    "breadcrumb.label.selected.fontWeight": 600,
    "breadcrumb.default.indicatorColor": "rgba(102, 102, 102, 0.7)",
    "breadcrumb.selected.indicatorColor": "#0696d7",
    "breadcrumb.divider.fontColor": "rgba(60, 60, 60, 0.4)",
    "breadcrumb.divider.fontWeight": 600,
    "button.borderRadius": "2px",
    "button.borderWidth": "1px",
    "button.minWidth": "10px",
    "button.paddingHorizontal": "12px",
    "button.paddingVertical": "4px",
    "button.gutterWidth": "4px",
    "button.label.fontFamily": "ArtifaktElement, sans-serif",
    "button.label.fontSize": "12px",
    "button.label.fontWeight": 600,
    "button.label.lineHeight": "14px",
    "button.haloWidth": "2px",
    "button.hover.haloWidth": "2px",
    "button.pressed.haloWidth": "4px",
    "button.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "button.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "button.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "button.solid.label.fontColor": "#ffffff",
    "button.solid.iconColor": "#ffffff",
    "button.solid.indicatorColor": "#ffffff",
    "button.solid.default.backgroundColor": "#0696d7",
    "button.solid.hover.backgroundColor": "#0696d7",
    "button.solid.focus.backgroundColor": "#0696d7",
    "button.solid.pressed.backgroundColor": "#0696d7",
    "button.solid.default.borderColor": "#0696d7",
    "button.solid.hover.borderColor": "#0696d7",
    "button.solid.focus.borderColor": "#0696d7",
    "button.solid.pressed.borderColor": "#0696d7",
    "button.outline.label.fontColor": "#3c3c3c",
    "button.outline.indicatorColor": "rgba(102, 102, 102, 0.7)",
    "button.outline.iconColor": "#808080",
    "button.outline.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.default.borderColor": "rgba(128, 128, 128, 0.5)",
    "button.outline.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "button.outline.focus.borderColor": "#0696d7",
    "button.outline.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "button.flat.label.fontColor": "#006eaf",
    "button.flat.iconColor": "#006eaf",
    "button.flat.indicatorColor": "#006eaf",
    "button.flat.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.default.borderColor": "rgba(128, 128, 128, 0)",
    "button.flat.hover.borderColor": "rgba(128, 128, 128, 0)",
    "button.flat.focus.borderColor": "rgba(128, 128, 128, 0)",
    "button.flat.pressed.borderColor": "rgba(128, 128, 128, 0)",
    "button.fontWeight": 600,
    "button.lineHeight": "14px",
    "button.fontFamily": "ArtifaktElement, sans-serif",
    "button.fontSize": "12px",
    "button.gutter": "4px",
    "button.horizontalPadding": "12px",
    "button.minimumWidth": "10px",
    "button.verticalPadding": "4px",
    "button.halo.width": "2px",
    "button.halo.color": "#808080",
    "button.hover.halo.width": "2px",
    "button.focus.halo.width": "2px",
    "button.pressed.halo.width": "4px",
    "button.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "button.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "button.outline.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.borderColor": "rgba(128, 128, 128, 0.5)",
    "button.outline.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "button.outline.focus.icon.color": "#808080",
    "button.outline.focus.textColor": "#3c3c3c",
    "button.outline.halo.color": "#808080",
    "button.outline.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "button.outline.hover.icon.color": "#808080",
    "button.outline.hover.textColor": "#3c3c3c",
    "button.outline.icon.color": "#808080",
    "button.outline.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "button.outline.pressed.haloWidth": "4px",
    "button.outline.pressed.icon.color": "#808080",
    "button.outline.pressed.textColor": "#3c3c3c",
    "button.outline.textColor": "#3c3c3c",
    "button.solid.backgroundColor": "#0696d7",
    "button.solid.borderColor": "#0696d7",
    "button.solid.halo.color": "#808080",
    "button.solid.textColor": "#ffffff",
    "button.solid.icon.color": "#ffffff",
    "button.solid.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "button.solid.hover.textColor": "#ffffff",
    "button.solid.hover.icon.color": "#ffffff",
    "button.solid.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "button.solid.focus.textColor": "#ffffff",
    "button.solid.focus.icon.color": "#ffffff",
    "button.solid.pressed.haloWidth": "4px",
    "button.solid.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "button.solid.pressed.textColor": "#ffffff",
    "button.solid.pressed.icon.color": "#ffffff",
    "button.flat.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.borderColor": "rgba(128, 128, 128, 0)",
    "button.flat.halo.color": "#808080",
    "button.flat.textColor": "#006eaf",
    "button.flat.icon.color": "#006eaf",
    "button.flat.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "button.flat.hover.textColor": "#006eaf",
    "button.flat.hover.icon.color": "#006eaf",
    "button.flat.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "button.flat.focus.textColor": "#006eaf",
    "button.flat.focus.icon.color": "#006eaf",
    "button.flat.pressed.haloWidth": "4px",
    "button.flat.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "button.flat.pressed.textColor": "#006eaf",
    "button.flat.pressed.icon.color": "#006eaf",
    "canvasFrame.label.fontColor": "#3c3c3c",
    "canvasFrame.label.fontFamily": "ArtifaktElement, sans-serif",
    "canvasFrame.label.fontSize": "12px",
    "canvasFrame.label.fontWeight": 600,
    "canvasFrame.label.marginLeft": "8px",
    "canvasFrame.border.borderColor": "#0696d7",
    "canvasFrame.border.borderWidth": "2px",
    "canvasFrame.controlPanel.backgroundColor": "#ffffff",
    "canvasFrame.controlPanel.paddingRight": "8px",
    "canvasFrame.controlPanel.paddingLeft": "4px",
    "canvasFrame.controlPanel.paddingVertical": "4px",
    "canvasFrame.closeIcon.marginLeft": "8px",
    "checkbox.borderWidth": "1px",
    "checkbox.borderRadius": "0",
    "checkbox.halo.color": "rgba(128, 128, 128, 0.15)",
    "checkbox.halo.width": "2px",
    "checkbox.minHeight": "12px",
    "checkbox.height": "12px",
    "checkbox.indicatorColor": "#ffffff",
    "checkbox.iconColor": "#ffffff",
    "checkbox.minWidth": "12px",
    "checkbox.width": "12px",
    "checkbox.unchecked.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.unchecked.default.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.unchecked.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.hover.halo.width": "2px",
    "checkbox.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "checkbox.unchecked.focus.borderColor": "#0696d7",
    "checkbox.focus.borderColor": "#0696d7",
    "checkbox.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "checkbox.focus.halo.width": "2px",
    "checkbox.unchecked.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "checkbox.pressed.halo.width": "4px",
    "checkbox.checked.default.borderColor": "#808080",
    "checkbox.checked.borderColor": "#808080",
    "checkbox.checked.iconColor": "#ffffff",
    "checkbox.checked.default.backgroundColor": "#808080",
    "checkbox.checked.backgroundColor": "#808080",
    "checkbox.indeterminate.borderColor": "#808080",
    "checkbox.indeterminate.iconColor": "#ffffff",
    "checkbox.indeterminate.backgroundColor": "#808080",
    "checkbox.focus.checked.backgroundColor": "#808080",
    "checkbox.focus.checked.borderColor": "#808080",
    "checkbox.focus.indeterminate.borderColor": "#808080",
    "checkbox.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "checkbox.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "checkbox.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "checkbox.focus.haloWidth": "2px",
    "checkbox.hover.haloWidth": "2px",
    "checkbox.pressed.haloWidth": "4px",
    "checkbox.unchecked.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.unchecked.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.unchecked.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.checked.hover.backgroundColor": "#808080",
    "checkbox.checked.hover.borderColor": "#808080",
    "checkbox.checked.pressed.backgroundColor": "#808080",
    "checkbox.checked.pressed.borderColor": "#808080",
    "component.disabled.opacity": "0.4",
    "datePicker.fontFamily": "ArtifaktElement, sans-serif",
    "datePicker.container.paddingHorizontal": "12px",
    "datePicker.container.paddingVertical": "16px",
    "datePicker.cell.minHeight": "36px",
    "datePicker.cell.minWidth": "36px",
    "datePicker.standardDate.fontColor": "#3c3c3c",
    "datePicker.standardDate.fontSize": "12px",
    "datePicker.standardDate.fontWeight": 400,
    "datePicker.selectedDate.fontWeight": 600,
    "datePicker.selectedDate.fontColor": "#ffffff",
    "datePicker.selectedDate.fontSize": "12px",
    "datePicker.notableDate.fontWeight": 400,
    "datePicker.notableDate.fontColor": "#0696d7",
    "datePicker.notableDate.fontSize": "12px",
    "datePicker.restrictedDate.fontWeight": 400,
    "datePicker.restrictedDate.fontColor": "rgba(60, 60, 60, 0.4)",
    "datePicker.restrictedDate.fontSize": "12px",
    "datePicker.day.fontWeight": 400,
    "datePicker.day.fontColor": "#3c3c3c",
    "datePicker.day.fontSize": "12px",
    "datePicker.header.fontWeight": 600,
    "datePicker.header.fontColor": "#3c3c3c",
    "datePicker.header.fontSize": "16px",
    "datePicker.header.paddingBottom": "16px",
    "datePicker.cell.borderColor": "#eeeeee",
    "datePicker.cell.borderWidth": "1px",
    "datePicker.standardDateCell.default.backgroundColor": "#ffffff",
    "datePicker.standardDateCell.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "datePicker.standardDateCell.focus.backgroundColor": "#ffffff",
    "datePicker.standardDateCell.focus.borderColor": "#0696d7",
    "datePicker.standardDateCell.focus.borderWidth": "1px",
    "datePicker.standardDateCell.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "datePicker.standardDateCell.focus.haloWidth": "2px",
    "datePicker.standardDateCell.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "datePicker.selectedDateCell.default.backgroundColor": "#0696d7",
    "datePicker.selectedDateCell.focus.backgroundColor": "#0696d7",
    "datePicker.selectedDateCell.focus.haloColor": "rgba(60, 60, 60, 0.25)",
    "datePicker.selectedDateCell.focus.haloWidth": "2px",
    "datePicker.rangeDateCell.default.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "datePicker.rangeDateCell.hover.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "datePicker.rangeDateCell.focus.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "datePicker.rangeDateCell.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "datePicker.rangeDateCell.focus.haloWidth": "2px",
    "datePicker.rangeDateCell.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "datePicker.currentDateCell.default.backgroundColor": "#ffffff",
    "datePicker.currentDateCell.default.borderColor": "#0696d7",
    "datePicker.currentDateCell.default.borderWidth": "1px",
    "datePicker.currentDateCell.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "datePicker.currentDateCell.hover.borderColor": "#0696d7",
    "datePicker.currentDateCell.hover.borderWidth": "1px",
    "datePicker.currentDateCell.focus.backgroundColor": "#ffffff",
    "datePicker.currentDateCell.focus.borderColor": "#0696d7",
    "datePicker.currentDateCell.focus.borderWidth": "1px",
    "datePicker.currentDateCell.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "datePicker.currentDateCell.focus.haloWidth": "2px",
    "datePicker.currentDateCell.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "datePicker.currentDateCell.pressed.borderColor": "#0696d7",
    "datePicker.currentDateCell.pressed.borderWidth": "1px",
    "divider.borderWidth": "1px",
    "divider.lightColor": "rgba(60, 60, 60, 0.1)",
    "divider.heavyColor": "rgba(60, 60, 60, 0.25)",
    "divider.width": "1px",
    "flyout.backgroundColor": "#ffffff",
    "flyout.borderColor": "rgba(128, 128, 128, 0)",
    "flyout.borderRadius": "4px",
    "flyout.shadowBlur": "16px",
    "flyout.shadowColor": "rgba(0, 0, 0, 0.2)",
    "flyout.nub.hypotenuseLength": "16px",
    "formField.instructionalText.fontSize": "12px",
    "formField.instructionalText.fontWeight": 600,
    "formField.instructionalText.fontFamily": "ArtifaktElement, sans-serif",
    "formField.instructionalText.fontColor": "rgba(60, 60, 60, 0.4)",
    "formField.instructionalText.lineHeight": "1.4",
    "formField.icon.width": "24px",
    "formField.icon.paddingRight": "8px",
    "formField.icon.color": "rgba(102, 102, 102, 0.7)",
    "iconButton.borderRadius": "2px",
    "iconButton.borderWidth": "0",
    "iconButton.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "iconButton.focus.haloWidth": "2px",
    "iconButton.dynamic.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.dynamic.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.dynamic.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.dynamic.default.iconColor": "#808080",
    "iconButton.dynamic.hover.iconColor": "#3c3c3c",
    "iconButton.dynamic.focus.iconColor": "#808080",
    "iconButton.dynamic.pressed.iconColor": "#3c3c3c",
    "iconButton.dynamic.on.default.iconColor": "#0696d7",
    "iconButton.dynamic.on.hover.iconColor": "#006eaf",
    "iconButton.dynamic.on.focus.iconColor": "#0696d7",
    "iconButton.dynamic.on.pressed.iconColor": "#006eaf",
    "iconButton.dynamic.pressed.100To250BackgroundColor": "rgba(128, 128, 128, 0.2)",
    "iconButton.dynamic.pressed.300To350BackgroundColor": "rgba(255, 255, 255, 0.35)",
    "iconButton.static.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.default.borderColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.hover.borderColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.focus.borderColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.pressed.borderColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.on.default.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "iconButton.static.on.hover.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "iconButton.static.on.focus.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "iconButton.static.on.pressed.backgroundColor": "rgba(106, 192, 231, 0.7)",
    "iconButton.static.on.default.borderColor": "rgba(6, 150, 215, 0.5)",
    "iconButton.static.on.hover.borderColor": "rgba(6, 150, 215, 0.5)",
    "iconButton.static.on.focus.borderColor": "#0696d7",
    "iconButton.static.on.pressed.borderColor": "rgba(6, 150, 215, 0.5)",
    "iconButton.static.hover.100To250BackgroundColor": "rgba(128, 128, 128, 0.1)",
    "iconButton.static.pressed.100To250BackgroundColor": "rgba(128, 128, 128, 0.2)",
    "iconButton.static.hover.300To350BackgroundColor": "rgba(255, 255, 255, 0.25)",
    "iconButton.static.pressed.300To350BackgroundColor": "rgba(255, 255, 255, 0.35)",
    "iconButton.static.on.hover.100To250BackgroundColor": "rgba(6, 150, 215, 0.35)",
    "iconButton.static.on.hover.300To350BackgroundColor": "rgba(6, 150, 215, 0.35)",
    "iconButton.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "iconButton.focus.halo.width": "2px",
    "iconButton.focus.iconColor": "#808080",
    "iconButton.focus.on.iconColor": "#0696d7",
    "iconButton.hover.iconColor": "#3c3c3c",
    "iconButton.hover.on.iconColor": "#006eaf",
    "iconButton.iconColor": "#808080",
    "iconButton.on.iconColor": "#0696d7",
    "iconButton.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "iconButton.pressed.iconColor": "#3c3c3c",
    "iconButton.pressed.level100To250.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "iconButton.pressed.level300To350.backgroundColor": "rgba(255, 255, 255, 0.35)",
    "iconButton.pressed.on.iconColor": "#006eaf",
    "iconButton.static.hover.level100To250.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "iconButton.static.hover.level300To350.backgroundColor": "rgba(255, 255, 255, 0.25)",
    "iconButton.static.on.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "iconButton.static.on.borderColor": "rgba(6, 150, 215, 0.5)",
    "iconButton.static.on.hover.level100To250.backgroundColor": "rgba(6, 150, 215, 0.35)",
    "iconButton.static.on.hover.level300To350.backgroundColor": "rgba(6, 150, 215, 0.35)",
    "input.borderWidth": "1px",
    "input.borderBottomWidth": "1px",
    "input.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.borderRadius": "0",
    "input.haloWidth": "4px",
    "input.minHeight": "24px",
    "input.paddingVertical": "4px",
    "input.gutterWidth": "4px",
    "input.disabled.opacity": 0.4,
    "input.error.borderBottomColor": "#ec4a41",
    "input.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "input.focus.haloColor": "rgba(6, 150, 215, 0.25)",
    "input.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "input.value.fontColor": "#3c3c3c",
    "input.value.fontFamily": "ArtifaktElement, sans-serif",
    "input.value.fontSize": "12px",
    "input.value.fontWeight": 600,
    "input.value.highlightColor": "rgba(128, 128, 128, 0.2)",
    "input.value.lineHeight": "1.4",
    "input.value.placeholderFontColor": "rgba(60, 60, 60, 0.4)",
    "input.indicator.default": "rgba(102, 102, 102, 0.7)",
    "input.indicator.focus": "rgba(102, 102, 102, 0.7)",
    "input.indicator.hover": "#666666",
    "input.indicator.pressed": "#0696d7",
    "input.line.paddingHorizontal": "0",
    "input.line.default.borderBottomColor": "rgba(128, 128, 128, 0.35)",
    "input.line.hover.borderBottomColor": "#808080",
    "input.line.focus.borderBottomColor": "#0696d7",
    "input.line.pressed.borderBottomColor": "rgba(128, 128, 128, 0.85)",
    "input.box.paddingHorizontal": "8px",
    "input.box.default.backgroundColor": "#ffffff",
    "input.box.hover.backgroundColor": "#ffffff",
    "input.box.focus.backgroundColor": "#ffffff",
    "input.box.pressed.backgroundColor": "#ffffff",
    "input.box.default.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.box.hover.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.box.focus.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.box.pressed.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.box.default.borderBottomColor": "rgba(255, 255, 255, 0.2)",
    "input.box.hover.borderBottomColor": "#808080",
    "input.box.focus.borderBottomColor": "#0696d7",
    "input.box.pressed.borderBottomColor": "rgba(128, 128, 128, 0.85)",
    "input.textArea.line.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.active.halo.color": "rgba(6, 150, 215, 0.25)",
    "input.borderBottomColor": "rgba(128, 128, 128, 0.35)",
    "input.active.halo.width": "4px",
    "input.box.backgroundColor": "#ffffff",
    "input.box.borderBottomColor": "rgba(255, 255, 255, 0.2)",
    "input.boxType.horizontalPadding": "8px",
    "input.focus.borderBottomColor": "#0696d7",
    "input.focus.halo.color": "rgba(6, 150, 215, 0.25)",
    "input.focus.halo.width": "4px",
    "input.fontColor": "#3c3c3c",
    "input.fontFamily": "ArtifaktElement, sans-serif",
    "input.fontSize": "12px",
    "input.fontWeight": 600,
    "input.halo.color": "rgba(128, 128, 128, 0.15)",
    "input.halo.width": "4px",
    "input.height": "24px",
    "input.highlightColor": "rgba(128, 128, 128, 0.2)",
    "input.horizontalPadding": "0",
    "input.hover.borderBottomColor": "#808080",
    "input.hover.halo.width": "4px",
    "input.lineHeight": "1.4",
    "input.placeholder.fontColor": "rgba(60, 60, 60, 0.4)",
    "input.verticalPadding": "4px",
    "label.fontFamily": "ArtifaktElement, sans-serif",
    "label.fontSize": "12px",
    "label.fontColor": "rgba(60, 60, 60, 0.9)",
    "label.hover.fontColor": "rgba(60, 60, 60, 0.9)",
    "label.fontWeight": 400,
    "label.lineHeight": "1.4",
    "label.top.fontSize": "12px",
    "menu.backgroundColor": "#ffffff",
    "menu.borderRadius": "0",
    "menu.topFlushBorderTopRadius": "0",
    "menu.item.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "menu.item.paddingHorizontal": "4px",
    "menu.item.paddingVertical": "12px",
    "menu.item.minHeight": "16px",
    "menu.item.gutterWidth": "12px",
    "menu.item.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "menu.item.focus.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "menu.item.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "menu.item.backgroundColor": "rgba(128, 128, 128, 0)",
    "menu.item.active.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "menu.item.horizontalPadding": "4px",
    "menu.item.verticalPadding": "12px",
    "modal.fontColor": "#3c3c3c",
    "modal.paddingHorizontal": "16px",
    "modal.overlayColor": "rgba(0, 0, 0, 0.5)",
    "modal.header.backgroundColor": "#ffffff",
    "modal.header.borderBottomColor": "rgba(60, 60, 60, 0.25)",
    "modal.header.minHeight": "45px",
    "modal.body.minHeight": "98px",
    "modal.shell.backgroundColor": "#ffffff",
    "modal.shell.borderRadius": "4px",
    "modal.shell.minHeight": "200px",
    "modal.shell.minWidth": "400px",
    "modal.shell.shadowColor": "rgba(0, 0, 0, 0.2)",
    "modal.overlay.color": "rgba(0, 0, 0, 0.5)",
    "modal.textColor": "#3c3c3c",
    "modal.body.height": "98px",
    "modal.horizontalPadding": "16px",
    "modal.header.borderColor": "rgba(60, 60, 60, 0.25)",
    "modal.header.height": "45px",
    "modal.window.backgroundColor": "#ffffff",
    "modal.window.borderRadius": "4px",
    "modal.window.height": "200px",
    "modal.window.width": "400px",
    "modal.window.shadowColor": "rgba(0, 0, 0, 0.2)",
    "notifications.borderRadius": "4px",
    "notifications.fontColor": "#3c3c3c",
    "notifications.fontFamily": "ArtifaktElement, sans-serif",
    "notifications.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "notifications.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "notifications.container.backgroundColor": "#ffffff",
    "notifications.container.borderColor": "rgba(128, 128, 128, 0)",
    "notifications.container.shadowBlur": "4px",
    "notifications.container.shadowColor": "rgba(0, 0, 0, 0.2)",
    "notifications.statusIndicator.width": "3px",
    "notifications.header.fontSize": "12px",
    "notifications.header.fontWeight": 700,
    "notifications.header.lineHeight": "20px",
    "notifications.toast.object.fontSize": "11px",
    "notifications.toast.object.fontWeight": 700,
    "notifications.toast.object.lineHeight": "16px",
    "notifications.toast.description.fontSize": "11px",
    "notifications.toast.description.fontWeight": 400,
    "notifications.toast.description.lineHeight": "16px",
    "progress.bar.backgroundColor": "rgba(0, 0, 0, 0.05)",
    "progress.bar.highlightColor": "#38abdf",
    "progress.bar.borderRadius": "2px",
    "progress.bar.minHeight": "4px",
    "progressBar.backgroundColor": "rgba(0, 0, 0, 0.05)",
    "progressBar.highlightColor": "#38abdf",
    "progressBar.borderRadius": "2px",
    "progressBar.height": "4px",
    "progress.ring.backgroundColor": "rgba(0, 0, 0, 0.05)",
    "progress.ring.highlightColor1": "#0686c2",
    "progress.ring.highlightColor2": "#38abdf",
    "progressRing.backgroundColor": "rgba(0, 0, 0, 0.05)",
    "progressRing.highlightColor1": "#0686c2",
    "progressRing.highlightColor2": "#38abdf",
    "scrollbar.radii": "1000em",
    "scrollbar.height": "5px",
    "scrollbar.default.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "scrollbar.pressed.backgroundColor": "rgba(128, 128, 128, 0.5)",
    "segmentedButton.borderRadius": "2px",
    "segmentedButton.borderWidth": "1px",
    "segmentedButton.gutterWidth": "4px",
    "segmentedButton.minWidth": "10px",
    "segmentedButton.paddingHorizontal": "12px",
    "segmentedButton.paddingVertical": "4px",
    "segmentedButton.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "segmentedButton.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "segmentedButton.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "segmentedButton.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "segmentedButton.default.borderColor": "rgba(128, 128, 128, 0.5)",
    "segmentedButton.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "segmentedButton.focus.borderColor": "#0696d7",
    "segmentedButton.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "segmentedButton.hover.haloWidth": "2px",
    "segmentedButton.focus.haloWidth": "2px",
    "segmentedButton.pressed.haloWidth": "4px",
    "segmentedButton.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "segmentedButton.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "segmentedButton.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "segmentedButton.default.iconColor": "#808080",
    "segmentedButton.active.iconColor": "#eeeeee",
    "segmentedButton.active.backgroundColor": "#808080",
    "segmentedButton.label.fontSize": "12px",
    "segmentedButton.label.default.fontColor": "#3c3c3c",
    "segmentedButton.label.default.fontWeight": 600,
    "segmentedButton.label.active.fontColor": "#f5f5f5",
    "segmentedButton.label.active.fontWeight": 700,
    "sideNav.fontFamily": "ArtifaktElement, sans-serif",
    "sideNav.container.backgroundColor": "#ffffff",
    "sideNav.account.fontColor": "#3c3c3c",
    "sideNav.account.fontSize": "11px",
    "sideNav.account.fontWeight": 500,
    "sideNav.account.lineHeight": "1.428571429",
    "sideNav.project.fontColor": "#3c3c3c",
    "sideNav.project.fontSize": "12px",
    "sideNav.project.fontWeight": 700,
    "sideNav.project.lineHeight": "1.428571429",
    "sideNav.module.default.fontColor": "#3c3c3c",
    "sideNav.module.default.fontSize": "12px",
    "sideNav.module.default.fontWeight": 700,
    "sideNav.module.default.lineHeight": "1.428571429",
    "sideNav.module.active.fontColor": "#0696d7",
    "sideNav.module.active.fontSize": "12px",
    "sideNav.module.active.fontWeight": 700,
    "sideNav.module.active.lineHeight": "1.428571429",
    "sideNav.subModule.default.fontColor": "#3c3c3c",
    "sideNav.subModule.default.fontSize": "12px",
    "sideNav.subModule.default.fontWeight": 400,
    "sideNav.subModule.default.lineHeight": "1.428571429",
    "sideNav.subModule.active.fontColor": "#0696d7",
    "sideNav.subModule.active.fontSize": "12px",
    "sideNav.subModule.active.fontWeight": 400,
    "sideNav.subModule.active.lineHeight": "1.428571429",
    "sideNav.legal.fontColor": "#3c3c3c",
    "sideNav.legal.fontSize": "11px",
    "sideNav.legal.fontWeight": 500,
    "sideNav.legal.lineHeight": "1.5",
    "sideNav.icon.defaultColor": "#808080",
    "sideNav.icon.activeColor": "#0696d7",
    "sideNav.activeIndicator.backgroundColor": "#0696d7",
    "skeletonItem.borderRadius": "2px",
    "skeletonItem.minHeight": "12px",
    "skeletonItem.backgroundColor": "rgba(0, 0, 0, 0.06)",
    "skeletonItem.highlightColor": "rgba(255, 255, 255, 0.3)",
    "skeletonItem.defaultHeight": "12px",
    "slider.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "slider.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "slider.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "slider.hover.haloWidth": "2px",
    "slider.focus.haloWidth": "2px",
    "slider.pressed.haloWidth": "4px",
    "slider.thumb.backgroundColor": "#808080",
    "slider.thumb.minWidth": "4px",
    "slider.thumb.minHeight": "16px",
    "slider.track.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "slider.track.minHeight": "1px",
    "slider.value.backgroundColor": "#808080",
    "slider.inputOverlay.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "slider.halo.width": 0,
    "slider.halo.color": "rgba(128, 128, 128, 0.15)",
    "slider.thumb.width": "4px",
    "slider.focused.halo.color": "rgba(6, 150, 215, 0.35)",
    "slider.focused.halo.width": "2px",
    "slider.focused.thumb.color": "#808080",
    "slider.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "slider.hover.halo.width": "2px",
    "slider.hover.thumb.color": "#808080",
    "slider.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "slider.pressed.halo.width": "4px",
    "slider.pressed.thumb.color": "#808080",
    "slider.track.color": "rgba(128, 128, 128, 0.2)",
    "slider.track.width": "1px",
    "slider.value.color": "#808080",
    "stepIndicator.fontFamily": "ArtifaktElement, sans-serif",
    "stepIndicator.borderWidth": "2px",
    "stepIndicator.minDiameter": "22px",
    "stepIndicator.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "stepIndicator.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "stepIndicator.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "stepIndicator.hover.haloWidth": "2px",
    "stepIndicator.focus.haloWidth": "2px",
    "stepIndicator.pressed.haloWidth": "4px",
    "stepIndicator.incomplete.backgroundColor": "rgba(128, 128, 128, 0)",
    "stepIndicator.incomplete.borderColor": "#dcdcdc",
    "stepIndicator.inProgress.backgroundColor": "rgba(128, 128, 128, 0)",
    "stepIndicator.inProgress.borderColor": "#0696d7",
    "stepIndicator.completed.backgroundColor": "#0696d7",
    "stepIndicator.completed.iconColor": "#ffffff",
    "stepIndicator.accordion.paddingHorizontal": "12px",
    "stepIndicator.accordion.paddingVertical": "16px",
    "stepIndicator.connector.minWidth": "2px",
    "stepIndicator.connector.default.backgroundColor": "#dcdcdc",
    "stepIndicator.connector.active.backgroundColor": "#0696d7",
    "stepIndicator.label.fontColor": "#3c3c3c",
    "stepIndicator.label.default.fontWeight": 400,
    "stepIndicator.label.horizontal.fontSize": "8px",
    "stepIndicator.label.horizontal.marginBottom": "8px",
    "stepIndicator.label.horizontal.marginTop": "8px",
    "stepIndicator.label.vertical.fontSize": "12px",
    "stepIndicator.label.vertical.marginLeft": "8px",
    "stepIndicator.label.inProgress.fontWeight": 700,
    "stepIndicator.number.fontSize": "12px",
    "stepIndicator.number.inProgress.fontColor": "#0696d7",
    "stepIndicator.number.inProgress.fontWeight": 700,
    "stepIndicator.number.incomplete.fontColor": "rgba(60, 60, 60, 0.4)",
    "stepIndicator.number.incomplete.fontWeight": 600,
    "table.fontColor": "#3c3c3c",
    "table.fontFamily": "ArtifaktElement, sans-serif",
    "table.fonSize": "12px",
    "table.lineHeight": "1.428571429",
    "table.title.fontWeight": 700,
    "table.item.fontWeight": 400,
    "table.row.borderBottomColor": "rgba(60, 60, 60, 0.1)",
    "table.row.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "table.row.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "table.row.selected.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "table.header.borderTopColor": "rgba(60, 60, 60, 0.1)",
    "table.header.borderBottomColor": "rgba(60, 60, 60, 0.25)",
    "tabs.focus.haloWidth": "2px",
    "tabs.focus.haloColor": "rgba(6, 150, 215, 0.5)",
    "tabs.label.fontColor": "#3c3c3c",
    "tabs.label.fontFamily": "ArtifaktElement, sans-serif",
    "tabs.label.fontSize": "12px",
    "tabs.label.inactive.fontWeight": 400,
    "tabs.label.active.fontWeight": 700,
    "tabs.label.active.lineHeight": "1.333333333",
    "tabs.iconSize": "16px",
    "tabs.iconColor": "#808080",
    "tabs.iconGutter": "8px",
    "tabs.closeButton.minSize": "16px",
    "tabs.closeButton.gutter": "8px",
    "tabs.closeButton.default.iconColor": "#808080",
    "tabs.closeButton.hover.iconColor": "#3c3c3c",
    "tabs.closeButton.pressed.iconColor": "#3c3c3c",
    "tabs.underline.wrapper.borderBottomColor": "rgba(128, 128, 128, 0.35)",
    "tabs.underline.wrapper.borderBottomWidth": "1px",
    "tabs.underline.tab.gutter": "16px",
    "tabs.underline.tab.paddingBottom": "2px",
    "tabs.underline.hover.haloColor": "rgba(128, 128, 128, 0.85)",
    "tabs.underline.active.haloColor": "#0696d7",
    "tabs.box.wrapper.backgroundColor": "rgba(128, 128, 128, 0)",
    "tabs.box.tab.paddingHorizontal": "8px",
    "tabs.box.tab.paddingVertical": "4px",
    "tabs.box.tab.active.backgroundColor": "#ffffff",
    "tabs.box.tab.hover.backgroundColor": "rgba(255, 255, 255, 0.5)",
    "tabs.box.divider.borderColor": "rgba(60, 60, 60, 0.25)",
    "tabs.box.dividerWidth": "1px",
    "tabs.box.dividerHeight": "12px",
    "tabs.canvas.wrapper.backgroundColor": "rgba(128, 128, 128, 0)",
    "tabs.canvas.tab.paddingHorizontal": "12px",
    "tabs.canvas.tab.paddingVertical": "4px",
    "tabs.canvas.tab.hover.backgroundColor": "rgba(245, 245, 245, 0.5)",
    "tabs.canvas.tab.active.backgroundColor": "#f5f5f5",
    "tabs.canvas.divider.borderColor": "rgba(60, 60, 60, 0.25)",
    "tabs.canvas.dividerWidth": "1px",
    "tabs.canvas.dividerHeight": "12px",
    "tabs.general.tab.fontFamily": "ArtifaktElement, sans-serif",
    "tabs.general.tab.fontSize": "12px",
    "tabs.general.tab.fontWeight": 400,
    "tabs.general.tab.active.fontWeight": 700,
    "tabs.general.tab.color": "#3c3c3c",
    "tabs.general.tab.lineHeight": "1.333333333",
    "tabs.general.halo.size": "2px",
    "tabs.general.halo.color": "rgba(6, 150, 215, 0.5)",
    "tabs.general.icon.size": "16px",
    "tabs.general.icon.color": "#808080",
    "tabs.general.icon.gutter": "8px",
    "tabs.general.closeButton.size": "16px",
    "tabs.general.closeButton.gutter": "8px",
    "tabs.general.closeButton.color": "#808080",
    "tabs.general.closeButton.hover.color": "#3c3c3c",
    "tabs.general.closeButton.pressed.color": "#3c3c3c",
    "tabs.underline.halo.hover.color": "rgba(128, 128, 128, 0.85)",
    "tabs.underline.halo.active.color": "#0696d7",
    "tabs.box.tab.horizontalPadding": "8px",
    "tabs.box.tab.verticalPadding": "4px",
    "tabs.box.divider.color": "rgba(60, 60, 60, 0.25)",
    "tabs.box.divider.width": "1px",
    "tabs.box.divider.height": "12px",
    "tabs.canvas.tab.horizontalPadding": "12px",
    "tabs.canvas.tab.verticalPadding": "4px",
    "tabs.canvas.divider.color": "rgba(60, 60, 60, 0.25)",
    "tabs.canvas.divider.width": "1px",
    "tabs.canvas.divider.height": "12px",
    "tag.borderWidth": "1px",
    "tag.borderRadii": "1000em",
    "tag.hover.haloWidth": "2px",
    "tag.focus.haloWidth": "4px",
    "tag.paddingLeft": "8px",
    "tag.paddingRight": "4px",
    "tag.paddingVertical": "4px",
    "tag.default.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "tag.hover.backgroundColor": "rgba(128, 128, 128, 0.25)",
    "tag.focus.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "tag.pressed.backgroundColor": "rgba(128, 128, 128, 0.35)",
    "tag.default.borderColor": "rgba(128, 128, 128, 0)",
    "tag.hover.borderColor": "rgba(128, 128, 128, 0)",
    "tag.focus.borderColor": "rgba(128, 128, 128, 0)",
    "tag.pressed.borderColor": "rgba(128, 128, 128, 0)",
    "tag.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "tag.on.default.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "tag.on.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "tag.on.focus.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "tag.on.pressed.backgroundColor": "rgba(128, 128, 128, 0.7)",
    "tag.on.default.borderColor": "rgba(6, 150, 215, 0.5)",
    "tag.on.hover.borderColor": "rgba(6, 150, 215, 0.5)",
    "tag.on.focus.borderColor": "#0696d7",
    "tag.on.pressed.borderColor": "rgba(6, 150, 215, 0.5)",
    "tag.on.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "tag.label.marginRight": "4px",
    "tag.label.fontColor": "#3c3c3c",
    "tag.label.fontFamily": "ArtifaktElement, sans-serif",
    "tag.label.fontSize": "12px",
    "tag.label.fontWeight": 600,
    "tag.closeButton.iconColor": "#808080",
    "textArea.minHeight": "250px",
    "textArea.borderColor": "rgba(128, 128, 128, 0.2)",
    "textLink.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "textLink.focus.haloWidth": "2px",
    "textLink.primary.default.fontColor": "#006eaf",
    "textLink.primary.hover.fontColor": "#006eaf",
    "textLink.primary.focus.fontColor": "#006eaf",
    "textLink.primary.hover.underlineColor": "#006eaf",
    "textLink.secondary.default.fontColor": "#3c3c3c",
    "textLink.secondary.hover.fontColor": "#3c3c3c",
    "textLink.secondary.focus.fontColor": "#3c3c3c",
    "textLink.secondary.hover.underlineColor": "#3c3c3c",
    "textLink.textColor": "#006eaf",
    "textLink.hover.textColor": "#006eaf",
    "textLink.focus.textColor": "#006eaf",
    "textLink.hover.underline.color": "#006eaf",
    "textLink.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "textLink.focus.halo.width": "2px",
    "tile.borderRadii": "0",
    "tile.borderWidth": "1px",
    "tile.haloWidth": "4px",
    "tile.padding": "8px",
    "tile.fontColor": "#3c3c3c",
    "tile.fontFamily": "ArtifaktElement, sans-serif",
    "tile.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "tile.thumbnail.marginRight": "8px",
    "tile.title.fontSize": "12px",
    "tile.title.fontWeight": 600,
    "tile.title.lineHeight": "1.333333333",
    "tile.title.marginBottom": "4px",
    "tile.subTitle.fontSize": "11px",
    "tile.subTitle.fontWeight": 400,
    "tile.subTitle.lineHeight": "1.272727273",
    "tile.default.level300To350BackgroundColor": "rgba(255, 255, 255, 0.5)",
    "tile.hover.level300To350BackgroundColor": "rgba(255, 255, 255, 0.7)",
    "tile.focus.level300To350BackgroundColor": "rgba(255, 255, 255, 0.5)",
    "tile.pressed.level300To350BackgroundColor": "rgba(255, 255, 255, 0.9)",
    "tile.selected.default.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "tile.selected.hover.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "tile.selected.focus.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "tile.selected.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "tile.selected.default.borderColor": "rgba(6, 150, 215, 0.5)",
    "tile.selected.hover.borderColor": "rgba(6, 150, 215, 0.5)",
    "tile.selected.focus.borderColor": "#0696d7",
    "tile.selected.pressed.borderColor": "rgba(6, 150, 215, 0.5)",
    "tile.filled.default.level100To250.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "tile.filled.hover.level100To250.backgroundColor": "rgba(128, 128, 128, 0.25)",
    "tile.filled.focus.level100To250.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "tile.filled.pressed.level100To250.backgroundColor": "rgba(128, 128, 128, 0.35)",
    "tile.filled.default.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.hover.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.focus.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.pressed.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.default.level300To350.backgroundColor": "rgba(255, 255, 255, 0.5)",
    "tile.filled.hover.level300To350.backgroundColor": "rgba(255, 255, 255, 0.7)",
    "tile.filled.focus.level300To350.backgroundColor": "rgba(255, 255, 255, 0.5)",
    "tile.filled.pressed.level300To350.backgroundColor": "#ffffff",
    "tile.filled.default.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.hover.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.focus.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.pressed.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.default.level100To250.backgroundColor": "rgba(128, 128, 128, 0)",
    "tile.empty.hover.level100To250.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "tile.empty.focus.level100To250.backgroundColor": "rgba(128, 128, 128, 0)",
    "tile.empty.pressed.level100To250.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "tile.empty.default.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.hover.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.focus.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.pressed.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.default.level300To350.backgroundColor": "rgba(128, 128, 128, 0)",
    "tile.empty.hover.level300To350.backgroundColor": "rgba(255, 255, 255, 0.25)",
    "tile.empty.focus.level300To350.backgroundColor": "rgba(128, 128, 128, 0)",
    "tile.empty.pressed.level300To350.backgroundColor": "rgba(255, 255, 255, 0.35)",
    "tile.empty.default.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.hover.level300To350.borderColor": "rgba(255, 255, 255, 0.25)",
    "tile.empty.focus.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.pressed.level300To350.borderColor": "rgba(255, 255, 255, 0.35)",
    "timestamp.fontColor": "rgba(60, 60, 60, 0.7)",
    "timestamp.fontFamily": "ArtifaktElement, sans-serif",
    "timestamp.fontSize": "11px",
    "timestamp.fontWeight": 400,
    "timestamp.lineHeight": "20px",
    "thumbnail.backgroundColor": "#eeeeee",
    "thumbnail.borderColor": "rgba(128, 128, 128, 0.5)",
    "toggle.borderRadii": "1000em",
    "toggle.borderWidth": "1px",
    "toggle.minHeight": "15px",
    "toggle.minWidth": "26px",
    "toggle.padding": "2px",
    "toggle.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "toggle.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "toggle.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "toggle.hover.haloWidth": "2px",
    "toggle.focus.haloWidth": "2px",
    "toggle.pressed.haloWidth": "4px",
    "toggle.off.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "toggle.off.default.borderColor": "rgba(128, 128, 128, 0.5)",
    "toggle.off.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "toggle.off.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "toggle.off.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "toggle.off.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "toggle.off.focus.borderColor": "#0696d7",
    "toggle.off.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "toggle.on.default.backgroundColor": "#0696d7",
    "toggle.on.default.borderColor": "#0696d7",
    "toggle.thumb.borderRadii": "50%",
    "toggle.thumb.minDiameter": "9px",
    "toggle.thumb.on.backgroundColor": "#ffffff",
    "toggle.thumb.off.backgroundColor": "#808080",
    "token.borderRadii": "0",
    "token.paddingLeft": "4px",
    "token.paddingRight": "2px",
    "token.paddingVertical": "2px",
    "token.default.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "token.hover.backgroundColor": "rgba(128, 128, 128, 0.25)",
    "token.pressed.backgroundColor": "rgba(128, 128, 128, 0.35)",
    "token.default.borderColor": "rgba(128, 128, 128, 0)",
    "token.hover.borderColor": "rgba(128, 128, 128, 0)",
    "token.pressed.borderColor": "rgba(128, 128, 128, 0)",
    "token.selected.default.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "token.selected.hover.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "token.selected.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "token.selected.default.borderColor": "rgba(6, 150, 215, 0.5)",
    "token.selected.hover.borderColor": "rgba(6, 150, 215, 0.5)",
    "token.selected.pressed.borderColor": "rgba(6, 150, 215, 0.5)",
    "token.warning.default.backgroundColor": "rgba(250, 162, 27, 0.15)",
    "token.warning.hover.backgroundColor": "rgba(250, 162, 27, 0.3)",
    "token.warning.iconColor": "#faa21b",
    "token.icon.marginRight": "4px",
    "token.label.fontColor": "#3c3c3c",
    "token.label.fontFamily": "ArtifaktElement, sans-serif",
    "token.label.fontSize": "12px",
    "token.label.fontWeight": 600,
    "token.label.marginRight": "2px",
    "token.closeButton.iconColor": "#808080",
    "tooltip.backgroundColor": "#535353",
    "tooltip.borderRadius": "2px",
    "tooltip.shadowBlur": "16px",
    "tooltip.shadowColor": "rgba(0, 0, 0, 0.2)",
    "tooltip.title.fontColor": "#f5f5f5",
    "tooltip.title.fontFamily": "ArtifaktElement, sans-serif",
    "tooltip.title.fontSize": "12px",
    "tooltip.title.fontWeight": 700,
    "tooltip.title.lineHeight": 1.333333333,
    "tooltip.nub.hypotenuseLength": "16px",
    "tooltip.description.fontColor": "#F5F5F5",
    "tooltip.description.fontFamily": "ArtifaktElement, sans-serif",
    "tooltip.description.fontSize": "12px",
    "tooltip.description.fontWeight": 400,
    "tooltip.description.lineHeight": 1.333333333,
    "tooltip.divider.borderBottomColor": "rgba(255, 255, 255, 0.1)",
    "tooltip.textColor": "#f5f5f5",
    "tooltip.borderColor": "#535353",
    "topNav.padding": "8px",
    "topNav.backgroundColor": "#ffffff",
    "topNav.borderColor": "rgba(60, 60, 60, 0.1)",
    "topNav.productLockupColor": "#000000",
    "topNav.hamburgetIcon.margin": "4px",
    "treeView.fontColor": "#3c3c3c",
    "treeView.fontFamily": "ArtifaktElement, sans-serif",
    "treeView.indicatorColor": "#3c3c3c",
    "treeView.icon.marginRight": "4px",
    "treeView.row.paddingHorizontal": "8px",
    "treeView.row.paddingVertical": "4px",
    "treeView.title.fontSize": "12px",
    "treeView.title.fontWeight": 600,
    "treeView.title.lineHeight": "1.333333333",
    "treeView.item.fontSize": "12px",
    "treeView.item.fontWeight": 600,
    "treeView.item.lineHeight": "1.333333333",
    "treeView.guideLine.backgroundColor": "rgba(128, 128, 128, 0.5)",
    "typography.body.fontFamily": "ArtifaktElement, sans-serif",
    "typography.body.fontWeight": 400,
    "typography.body.fontSize": "12px",
    "typography.body.lineHeight": "1.333333333",
    "typography.body.color": "#3c3c3c",
    "typography.caption.fontFamily": "ArtifaktElement, sans-serif",
    "typography.caption.fontWeight": 400,
    "typography.caption.fontSize": "11px",
    "typography.caption.lineHeight": "1.272727273",
    "typography.caption.color": "#3c3c3c",
    "typography.h1.fontFamily": "ArtifaktElement, sans-serif",
    "typography.h1.fontWeight": 400,
    "typography.h1.fontSize": "24px",
    "typography.h1.lineHeight": "1.25",
    "typography.h1.color": "#3c3c3c",
    "typography.h2.fontFamily": "ArtifaktElement, sans-serif",
    "typography.h2.fontWeight": 400,
    "typography.h2.fontSize": "20px",
    "typography.h2.lineHeight": "1.3",
    "typography.h2.color": "#3c3c3c",
    "typography.h3.fontFamily": "ArtifaktElement, sans-serif",
    "typography.h3.fontWeight": 600,
    "typography.h3.fontSize": "16px",
    "typography.h3.lineHeight": "1.375",
    "typography.h3.color": "#3c3c3c"
  }
};
const { metadata, resolvedRoles } = themeData;

/* harmony default export */ __webpack_exports__["default"] = (themeData);


/***/ }),

/***/ "./node_modules/@hig/theme-data/build/esm/lightGrayMediumDensityTheme.js":
/*!*******************************************************************************!*\
  !*** ./node_modules/@hig/theme-data/build/esm/lightGrayMediumDensityTheme.js ***!
  \*******************************************************************************/
/*! exports provided: metadata, resolvedRoles, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "metadata", function() { return metadata; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "resolvedRoles", function() { return resolvedRoles; });
const themeData = {
  "metadata": {
    "colorSchemeName": "Light gray",
    "colorSchemeId": "hig-light-gray",
    "className": "hig--light-gray-theme",
    "densityId": "medium-density",
    "densityName": "Medium density",
    "name": "Light gray, Medium density",
    "fileName": "lightGrayMediumDensityTheme",
    "id": "hig-light-gray-medium-density",
    "__deprecated__": {
      "basics.colors.black": {
        "equivalent": "basics.colors.primary.black"
      },
      "basics.colors.white": {
        "equivalent": "basics.colors.primary.white"
      },
      "basics.colors.autodeskBlue100": {
        "equivalent": "basics.colors.primary.autodeskBlue.100"
      },
      "basics.colors.autodeskBlue200": {
        "equivalent": "basics.colors.primary.autodeskBlue.200"
      },
      "basics.colors.autodeskBlue300": {
        "equivalent": "basics.colors.primary.autodeskBlue.300"
      },
      "basics.colors.autodeskBlue400": {
        "equivalent": "basics.colors.primary.autodeskBlue.400"
      },
      "basics.colors.autodeskBlue500": {
        "equivalent": "basics.colors.primary.autodeskBlue.500"
      },
      "basics.colors.autodeskBlue600": {
        "equivalent": "basics.colors.primary.autodeskBlue.600"
      },
      "basics.colors.autodeskBlue700": {
        "equivalent": "basics.colors.primary.autodeskBlue.700"
      },
      "basics.colors.autodeskBlue800": {
        "equivalent": "basics.colors.primary.autodeskBlue.800"
      },
      "basics.colors.autodeskBlue900": {
        "equivalent": "basics.colors.primary.autodeskBlue.900"
      },
      "basics.colors.charcoal100": {
        "equivalent": "basics.colors.primary.charcoal.100"
      },
      "basics.colors.charcoal200": {
        "equivalent": "basics.colors.primary.charcoal.200"
      },
      "basics.colors.charcoal300": {
        "equivalent": "basics.colors.primary.charcoal.300"
      },
      "basics.colors.charcoal400": {
        "equivalent": "basics.colors.primary.charcoal.400"
      },
      "basics.colors.charcoal500": {
        "equivalent": "basics.colors.primary.charcoal.500"
      },
      "basics.colors.charcoal600": {
        "equivalent": "basics.colors.primary.charcoal.600"
      },
      "basics.colors.charcoal700": {
        "equivalent": "basics.colors.primary.charcoal.700"
      },
      "basics.colors.charcoal800": {
        "equivalent": "basics.colors.primary.charcoal.800"
      },
      "basics.colors.charcoal900": {
        "equivalent": "basics.colors.primary.charcoal.900"
      },
      "basics.colors.darkBlue100": {
        "equivalent": "basics.colors.secondary.darkBlue.100"
      },
      "basics.colors.darkBlue200": {
        "equivalent": "basics.colors.secondary.darkBlue.200"
      },
      "basics.colors.darkBlue300": {
        "equivalent": "basics.colors.secondary.darkBlue.300"
      },
      "basics.colors.darkBlue400": {
        "equivalent": "basics.colors.secondary.darkBlue.400"
      },
      "basics.colors.darkBlue500": {
        "equivalent": "basics.colors.secondary.darkBlue.500"
      },
      "basics.colors.darkBlue600": {
        "equivalent": "basics.colors.secondary.darkBlue.600"
      },
      "basics.colors.darkBlue700": {
        "equivalent": "basics.colors.secondary.darkBlue.700"
      },
      "basics.colors.darkBlue800": {
        "equivalent": "basics.colors.secondary.darkBlue.800"
      },
      "basics.colors.darkBlue900": {
        "equivalent": "basics.colors.secondary.darkBlue.900"
      },
      "basics.colors.green100": {
        "equivalent": "basics.colors.secondary.green.100"
      },
      "basics.colors.green200": {
        "equivalent": "basics.colors.secondary.green.200"
      },
      "basics.colors.green300": {
        "equivalent": "basics.colors.secondary.green.300"
      },
      "basics.colors.green400": {
        "equivalent": "basics.colors.secondary.green.400"
      },
      "basics.colors.green500": {
        "equivalent": "basics.colors.secondary.green.500"
      },
      "basics.colors.green600": {
        "equivalent": "basics.colors.secondary.green.600"
      },
      "basics.colors.green700": {
        "equivalent": "basics.colors.secondary.green.700"
      },
      "basics.colors.green800": {
        "equivalent": "basics.colors.secondary.green.800"
      },
      "basics.colors.green900": {
        "equivalent": "basics.colors.secondary.green.900"
      },
      "basics.colors.red100": {
        "equivalent": "basics.colors.secondary.red.100"
      },
      "basics.colors.red200": {
        "equivalent": "basics.colors.secondary.red.200"
      },
      "basics.colors.red300": {
        "equivalent": "basics.colors.secondary.red.300"
      },
      "basics.colors.red400": {
        "equivalent": "basics.colors.secondary.red.400"
      },
      "basics.colors.red500": {
        "equivalent": "basics.colors.secondary.red.500"
      },
      "basics.colors.red600": {
        "equivalent": "basics.colors.secondary.red.600"
      },
      "basics.colors.red700": {
        "equivalent": "basics.colors.secondary.red.700"
      },
      "basics.colors.red800": {
        "equivalent": "basics.colors.secondary.red.800"
      },
      "basics.colors.red900": {
        "equivalent": "basics.colors.secondary.red.900"
      },
      "basics.colors.turquoise100": {
        "equivalent": "basics.colors.secondary.turquoise.100"
      },
      "basics.colors.turquoise200": {
        "equivalent": "basics.colors.secondary.turquoise.200"
      },
      "basics.colors.turquoise300": {
        "equivalent": "basics.colors.secondary.turquoise.300"
      },
      "basics.colors.turquoise400": {
        "equivalent": "basics.colors.secondary.turquoise.400"
      },
      "basics.colors.turquoise500": {
        "equivalent": "basics.colors.secondary.turquoise.500"
      },
      "basics.colors.turquoise600": {
        "equivalent": "basics.colors.secondary.turquoise.600"
      },
      "basics.colors.turquoise700": {
        "equivalent": "basics.colors.secondary.turquoise.700"
      },
      "basics.colors.turquoise800": {
        "equivalent": "basics.colors.secondary.turquoise.800"
      },
      "basics.colors.turquoise900": {
        "equivalent": "basics.colors.secondary.turquoise.900"
      },
      "basics.colors.yellowOrange100": {
        "equivalent": "basics.colors.secondary.yellowOrange.100"
      },
      "basics.colors.yellowOrange200": {
        "equivalent": "basics.colors.secondary.yellowOrange.200"
      },
      "basics.colors.yellowOrange300": {
        "equivalent": "basics.colors.secondary.yellowOrange.300"
      },
      "basics.colors.yellowOrange400": {
        "equivalent": "basics.colors.secondary.yellowOrange.400"
      },
      "basics.colors.yellowOrange500": {
        "equivalent": "basics.colors.secondary.yellowOrange.500"
      },
      "basics.colors.yellowOrange600": {
        "equivalent": "basics.colors.secondary.yellowOrange.600"
      },
      "basics.colors.yellowOrange700": {
        "equivalent": "basics.colors.secondary.yellowOrange.700"
      },
      "basics.colors.yellowOrange800": {
        "equivalent": "basics.colors.secondary.yellowOrange.800"
      },
      "basics.colors.yellowOrange900": {
        "equivalent": "basics.colors.secondary.yellowOrange.900"
      },
      "basics.colors.pink100": {
        "equivalent": "basics.colors.tertiary.pink.100"
      },
      "basics.colors.pink200": {
        "equivalent": "basics.colors.tertiary.pink.200"
      },
      "basics.colors.pink300": {
        "equivalent": "basics.colors.tertiary.pink.300"
      },
      "basics.colors.pink400": {
        "equivalent": "basics.colors.tertiary.pink.400"
      },
      "basics.colors.pink500": {
        "equivalent": "basics.colors.tertiary.pink.500"
      },
      "basics.colors.pink600": {
        "equivalent": "basics.colors.tertiary.pink.600"
      },
      "basics.colors.pink700": {
        "equivalent": "basics.colors.tertiary.pink.700"
      },
      "basics.colors.pink800": {
        "equivalent": "basics.colors.tertiary.pink.800"
      },
      "basics.colors.pink900": {
        "equivalent": "basics.colors.tertiary.pink.900"
      },
      "basics.colors.purple100": {
        "equivalent": "basics.colors.tertiary.purple.100"
      },
      "basics.colors.purple200": {
        "equivalent": "basics.colors.tertiary.purple.200"
      },
      "basics.colors.purple300": {
        "equivalent": "basics.colors.tertiary.purple.300"
      },
      "basics.colors.purple400": {
        "equivalent": "basics.colors.tertiary.purple.400"
      },
      "basics.colors.purple500": {
        "equivalent": "basics.colors.tertiary.purple.500"
      },
      "basics.colors.purple600": {
        "equivalent": "basics.colors.tertiary.purple.600"
      },
      "basics.colors.purple700": {
        "equivalent": "basics.colors.tertiary.purple.700"
      },
      "basics.colors.purple800": {
        "equivalent": "basics.colors.tertiary.purple.800"
      },
      "basics.colors.purple900": {
        "equivalent": "basics.colors.tertiary.purple.900"
      },
      "basics.colors.salmon100": {
        "equivalent": "basics.colors.tertiary.salmon.100"
      },
      "basics.colors.salmon200": {
        "equivalent": "basics.colors.tertiary.salmon.200"
      },
      "basics.colors.salmon300": {
        "equivalent": "basics.colors.tertiary.salmon.300"
      },
      "basics.colors.salmon400": {
        "equivalent": "basics.colors.tertiary.salmon.400"
      },
      "basics.colors.salmon500": {
        "equivalent": "basics.colors.tertiary.salmon.500"
      },
      "basics.colors.salmon600": {
        "equivalent": "basics.colors.tertiary.salmon.600"
      },
      "basics.colors.salmon700": {
        "equivalent": "basics.colors.tertiary.salmon.700"
      },
      "basics.colors.salmon800": {
        "equivalent": "basics.colors.tertiary.salmon.800"
      },
      "basics.colors.salmon900": {
        "equivalent": "basics.colors.tertiary.salmon.900"
      },
      "basics.colors.slate100": {
        "equivalent": "basics.colors.tertiary.slate.100"
      },
      "basics.colors.slate200": {
        "equivalent": "basics.colors.tertiary.slate.200"
      },
      "basics.colors.slate300": {
        "equivalent": "basics.colors.tertiary.slate.300"
      },
      "basics.colors.slate400": {
        "equivalent": "basics.colors.tertiary.slate.400"
      },
      "basics.colors.slate500": {
        "equivalent": "basics.colors.tertiary.slate.500"
      },
      "basics.colors.slate600": {
        "equivalent": "basics.colors.tertiary.slate.600"
      },
      "basics.colors.slate700": {
        "equivalent": "basics.colors.tertiary.slate.700"
      },
      "basics.colors.slate800": {
        "equivalent": "basics.colors.tertiary.slate.800"
      },
      "basics.colors.slate900": {
        "equivalent": "basics.colors.tertiary.slate.900"
      },
      "basics.colors.surfaceLightGrayLevel100": {
        "equivalent": "basics.colors.surface.lightGray.100"
      },
      "basics.colors.surfaceLightGrayLevel200": {
        "equivalent": "basics.colors.surface.lightGray.200"
      },
      "basics.colors.surfaceLightGrayLevel250": {
        "equivalent": "basics.colors.surface.lightGray.250"
      },
      "basics.colors.surfaceLightGrayLevel300": {
        "equivalent": "basics.colors.surface.lightGray.300"
      },
      "basics.colors.surfaceLightGrayLevel350": {
        "equivalent": "basics.colors.surface.lightGray.350"
      },
      "basics.colors.surfaceDarkBlueLevel100": {
        "equivalent": "basics.colors.surface.darkBlue.100"
      },
      "basics.colors.surfaceDarkBlueLevel200": {
        "equivalent": "basics.colors.surface.darkBlue.200"
      },
      "basics.colors.surfaceDarkBlueLevel250": {
        "equivalent": "basics.colors.surface.darkBlue.250"
      },
      "basics.colors.surfaceDarkBlueLevel300": {
        "equivalent": "basics.colors.surface.darkBlue.300"
      },
      "basics.colors.surfaceDarkBlueLevel350": {
        "equivalent": "basics.colors.surface.darkBlue.350"
      },
      "basics.colors.surfaceDarkGrayLevel100": {
        "equivalent": "basics.colors.surface.darkGray.100"
      },
      "basics.colors.surfaceDarkGrayLevel200": {
        "equivalent": "basics.colors.surface.darkGray.200"
      },
      "basics.colors.surfaceDarkGrayLevel250": {
        "equivalent": "basics.colors.surface.darkGray.250"
      },
      "basics.colors.surfaceDarkGrayLevel300": {
        "equivalent": "basics.colors.surface.darkGray.300"
      },
      "basics.colors.surfaceDarkGrayLevel350": {
        "equivalent": "basics.colors.surface.darkGray.350"
      },
      "basics.colors.surfaceDarkBlueShadow": {
        "equivalent": "basics.colors.shadow.darkBlue"
      },
      "basics.colors.surfaceDarkGrayShadow": {
        "equivalent": "basics.colors.shadow.darkGray"
      },
      "basics.colors.surfaceLightGrayShadow": {
        "equivalent": "basics.colors.shadow.lightGray"
      },
      "basics.colors.textAgainstDark": {
        "equivalent": "basics.colors.text.againstDark"
      },
      "basics.colors.textAgainstLight": {
        "equivalent": "basics.colors.text.againstLight"
      },
      "basics.colors.iconLightGrayColor": {
        "equivalent": "basics.colors.icon.lightGray"
      },
      "basics.colors.iconDarkBlueColor": {
        "equivalent": "basics.colors.icon.darkBlue"
      },
      "basics.colors.iconDarkGrayColor": {
        "equivalent": "basics.colors.icon.darkGray"
      },
      "basics.colors.textLinkAgainstLight": {
        "equivalent": "basics.colors.textLink.primary.againstLight"
      },
      "basics.colors.textLinkAgainstDark": {
        "equivalent": "basics.colors.textLink.primary.againstDark"
      },
      "basics.colors.error": {
        "equivalent": "colorScheme.status.error"
      },
      "basics.colors.success": {
        "equivalent": "colorScheme.status.success"
      },
      "basics.colors.warning": {
        "equivalent": "colorScheme.status.warning"
      },
      "colorScheme.accentColor": {
        "equivalent": "colorScheme.reference.accent"
      },
      "colorScheme.baseColor": {
        "equivalent": "colorScheme.reference.base"
      },
      "colorScheme.surfaceLevel100Color": {
        "equivalent": "colorScheme.surface.level100"
      },
      "colorScheme.surfaceLevel200Color": {
        "equivalent": "colorScheme.surface.level200"
      },
      "colorScheme.surfaceLevel250Color": {
        "equivalent": "colorScheme.surface.level250"
      },
      "colorScheme.surfaceLevel300Color": {
        "equivalent": "colorScheme.surface.level300"
      },
      "colorScheme.surfaceLevel350Color": {
        "equivalent": "colorScheme.surface.level350"
      },
      "colorScheme.highShadowColor": {
        "equivalent": "colorScheme.shadow.high"
      },
      "colorScheme.lowShadowColor": {
        "equivalent": "colorScheme.shadow.low"
      },
      "colorScheme.textColor": {
        "equivalent": "colorScheme.text.default"
      },
      "colorScheme.textColorDim": {
        "equivalent": "colorScheme.text.dim"
      },
      "colorScheme.iconColor": {
        "equivalent": "colorScheme.icon.default"
      },
      "colorScheme.errorColor": {
        "equivalent": "colorScheme.status.error"
      },
      "colorScheme.successColor": {
        "equivalent": "colorScheme.status.success"
      },
      "colorScheme.warningColor": {
        "equivalent": "colorScheme.status.warning"
      },
      "colorScheme.infoColor": {
        "equivalent": "colorScheme.status.info"
      },
      "colorScheme.component.backgroundColor": {
        "equivalent": "colorScheme.surface.level"
      },
      "accordion.header.indicatorColor": {
        "equivalent": "accordion.header.default.indicatorColor"
      },
      "avatar.fontColorOnDark": {
        "equivalent": "avatar.darkBackground.fontColor"
      },
      "avatar.fontColorOnLight": {
        "equivalent": "avatar.lightBackground.fontColor"
      },
      "banner.info.iconBackground": {
        "equivalent": "banner.info.iconField.backgroundColor"
      },
      "banner.success.iconBackground": {
        "equivalent": "banner.success.iconField.backgroundColor"
      },
      "banner.warning.iconBackground": {
        "equivalent": "banner.warning.iconField.backgroundColor"
      },
      "banner.error.iconBackground": {
        "equivalent": "banner.error.iconField.backgroundColor"
      },
      "button.fontWeight": {
        "equivalent": "button.label.fontWeight"
      },
      "button.lineHeight": {
        "equivalent": "button.label.lineHeight"
      },
      "button.fontFamily": {
        "equivalent": "button.label.fontFamily"
      },
      "button.fontSize": {
        "equivalent": "button.label.fontSize"
      },
      "button.gutter": {
        "equivalent": "button.gutterWidth"
      },
      "button.horizontalPadding": {
        "equivalent": "button.paddingHorizontal"
      },
      "button.minimumWidth": {
        "equivalent": "button.minWidth"
      },
      "button.verticalPadding": {
        "equivalent": "button.paddingVertical"
      },
      "button.halo.width": {
        "equivalent": "button.haloWidth"
      },
      "button.halo.color": {
        "equivalent": "colorScheme.halo"
      },
      "button.hover.halo.width": {
        "equivalent": "button.hover.haloWidth"
      },
      "button.focus.halo.width": {
        "equivalent": "button.haloWidth"
      },
      "button.pressed.halo.width": {
        "equivalent": "button.pressed.haloWidth"
      },
      "button.focus.halo.color": {
        "equivalent": "button.focus.haloColor"
      },
      "button.pressed.halo.color": {
        "equivalent": "button.pressed.haloColor"
      },
      "button.outline.backgroundColor": {
        "equivalent": "button.outline.default.backgroundColor"
      },
      "button.outline.borderColor": {
        "equivalent": "button.outline.default.borderColor"
      },
      "button.outline.focus.halo.color": {
        "equivalent": "button.focus.haloColor"
      },
      "button.outline.focus.icon.color": {
        "equivalent": "button.outline.iconColor"
      },
      "button.outline.focus.textColor": {
        "equivalent": "button.outline.label.fontColor"
      },
      "button.outline.halo.color": {
        "equivalent": "colorScheme.halo"
      },
      "button.outline.hover.halo.color": {
        "equivalent": "button.hover.haloColor"
      },
      "button.outline.hover.icon.color": {
        "equivalent": "button.outline.iconColor"
      },
      "button.outline.hover.textColor": {
        "equivalent": "button.outline.label.fontColor"
      },
      "button.outline.icon.color": {
        "equivalent": "button.outline.iconColor"
      },
      "button.outline.pressed.halo.color": {
        "equivalent": "button.pressed.haloColor"
      },
      "button.outline.pressed.haloWidth": {
        "equivalent": "button.pressed.haloWidth"
      },
      "button.outline.pressed.icon.color": {
        "equivalent": "button.outline.iconColor"
      },
      "button.outline.pressed.textColor": {
        "equivalent": "button.outline.label.fontColor"
      },
      "button.outline.textColor": {
        "equivalent": "button.outline.label.fontColor"
      },
      "button.solid.backgroundColor": {
        "equivalent": "button.solid.default.backgroundColor"
      },
      "button.solid.borderColor": {
        "equivalent": "button.solid.default.borderColor"
      },
      "button.solid.halo.color": {
        "equivalent": "colorScheme.halo"
      },
      "button.solid.textColor": {
        "equivalent": "button.solid.label.fontColor"
      },
      "button.solid.icon.color": {
        "equivalent": "button.solid.iconColor"
      },
      "button.solid.hover.halo.color": {
        "equivalent": "button.hover.haloColor"
      },
      "button.solid.hover.textColor": {
        "equivalent": "button.solid.label.fontColor"
      },
      "button.solid.hover.icon.color": {
        "equivalent": "button.solid.iconColor"
      },
      "button.solid.focus.halo.color": {
        "equivalent": "button.focus.haloColor"
      },
      "button.solid.focus.textColor": {
        "equivalent": "button.solid.label.fontColor"
      },
      "button.solid.focus.icon.color": {
        "equivalent": "button.solid.iconColor"
      },
      "button.solid.pressed.haloWidth": {
        "equivalent": "button.pressed.haloWidth"
      },
      "button.solid.pressed.halo.color": {
        "equivalent": "button.pressed.haloColor"
      },
      "button.solid.pressed.textColor": {
        "equivalent": "button.solid.label.fontColor"
      },
      "button.solid.pressed.icon.color": {
        "equivalent": "button.solid.iconColor"
      },
      "button.flat.backgroundColor": {
        "equivalent": "button.flat.default.backgroundColor"
      },
      "button.flat.borderColor": {
        "equivalent": "button.flat.default.borderColor"
      },
      "button.flat.halo.color": {
        "equivalent": "colorScheme.halo"
      },
      "button.flat.textColor": {
        "equivalent": "button.flat.label.fontColor"
      },
      "button.flat.icon.color": {
        "equivalent": "button.flat.iconColor"
      },
      "button.flat.hover.halo.color": {
        "equivalent": "button.hover.haloColor"
      },
      "button.flat.hover.textColor": {
        "equivalent": "button.flat.label.fontColor"
      },
      "button.flat.hover.icon.color": {
        "equivalent": "button.flat.iconColor"
      },
      "button.flat.focus.halo.color": {
        "equivalent": "button.focus.haloColor"
      },
      "button.flat.focus.textColor": {
        "equivalent": "button.flat.label.fontColor"
      },
      "button.flat.focus.icon.color": {
        "equivalent": "button.flat.iconColor"
      },
      "button.flat.pressed.haloWidth": {
        "equivalent": "button.pressed.haloWidth"
      },
      "button.flat.pressed.halo.color": {
        "equivalent": "button.pressed.haloColor"
      },
      "button.flat.pressed.textColor": {
        "equivalent": "button.flat.label.fontColor"
      },
      "button.flat.pressed.icon.color": {
        "equivalent": "button.flat.iconColor"
      },
      "checkbox.halo.color": {
        "equivalent": "checkbox.hover.haloColor"
      },
      "checkbox.halo.width": {
        "equivalent": "checkbox.hover.haloWidth"
      },
      "checkbox.height": {
        "equivalent": "checkbox.minHeight"
      },
      "checkbox.iconColor": {
        "equivalent": "checkbox.indicatorColor"
      },
      "checkbox.width": {
        "equivalent": "checkbox.minWidth"
      },
      "checkbox.backgroundColor": {
        "equivalent": "checkbox.unchecked.default.backgroundColor"
      },
      "checkbox.borderColor": {
        "equivalent": "checkbox.unchecked.default.borderColor"
      },
      "checkbox.hover.borderColor": {
        "equivalent": "checkbox.unchecked.hover.borderColor"
      },
      "checkbox.hover.halo.width": {
        "equivalent": "checkbox.hover.haloWidth"
      },
      "checkbox.hover.halo.color": {
        "equivalent": "checkbox.hover.haloColor"
      },
      "checkbox.focus.borderColor": {
        "equivalent": "checkbox.unchecked.focus.borderColor"
      },
      "checkbox.focus.halo.color": {
        "equivalent": "checkbox.focus.haloColor"
      },
      "checkbox.focus.halo.width": {
        "equivalent": "checkbox.focus.haloWidth"
      },
      "checkbox.pressed.borderColor": {
        "equivalent": "checkbox.unchecked.pressed.borderColor"
      },
      "checkbox.pressed.halo.color": {
        "equivalent": "checkbox.pressed.haloColor"
      },
      "checkbox.pressed.halo.width": {
        "equivalent": "checkbox.pressed.haloWidth"
      },
      "checkbox.checked.borderColor": {
        "equivalent": "checkbox.checked.default.borderColor"
      },
      "checkbox.checked.iconColor": {
        "equivalent": "checkbox.indicatorColor"
      },
      "checkbox.checked.backgroundColor": {
        "equivalent": "checkbox.checked.default.backgroundColor"
      },
      "checkbox.indeterminate.borderColor": {
        "equivalent": "checkbox.checked.default.borderColor"
      },
      "checkbox.indeterminate.iconColor": {
        "equivalent": "checkbox.indicatorColor"
      },
      "checkbox.indeterminate.backgroundColor": {
        "equivalent": "checkbox.checked.default.backgroundColor"
      },
      "checkbox.focus.checked.backgroundColor": {
        "equivalent": "checkbox.checked.default.backgroundColor"
      },
      "checkbox.focus.checked.borderColor": {
        "equivalent": "checkbox.checked.default.borderColor"
      },
      "checkbox.focus.indeterminate.borderColor": {
        "equivalent": "checkbox.checked.default.borderColor"
      },
      "component.disabled.opacity": {
        "equivalent": "colorScheme.opacity.disabled"
      },
      "divider.width": {
        "equivalent": "divider.borderWidth"
      },
      "formField.instructionalText.fontSize": {
        "equivalent": "input.value.fontSize"
      },
      "formField.instructionalText.fontWeight": {
        "equivalent": "input.value.fontWeight"
      },
      "formField.instructionalText.fontFamily": {
        "equivalent": "input.value.fontFamily"
      },
      "formField.instructionalText.fontColor": {
        "equivalent": "input.value.placeholderFontColor"
      },
      "formField.instructionalText.lineHeight": {
        "equivalent": "basics.lineHeights.medium"
      },
      "formField.icon.width": {
        "equivalent": "basics.spacings"
      },
      "formField.icon.paddingRight": {
        "equivalent": "density.spacings.small"
      },
      "formField.icon.color": {
        "equivalent": "input.indicator.default"
      },
      "iconButton.focus.halo.color": {
        "equivalent": "iconButton.focus.haloColor"
      },
      "iconButton.focus.halo.width": {
        "equivalent": "iconButton.focus.haloWidth"
      },
      "iconButton.focus.iconColor": {
        "equivalent": "iconButton.dynamic.focus.iconColor"
      },
      "iconButton.focus.on.iconColor": {
        "equivalent": "iconButton.dynamic.on.focus.iconColor"
      },
      "iconButton.hover.iconColor": {
        "equivalent": "iconButton.dynamic.hover.iconColor"
      },
      "iconButton.hover.on.iconColor": {
        "equivalent": "iconButton.dynamic.on.hover.iconColor"
      },
      "iconButton.iconColor": {
        "equivalent": "iconButton.dynamic.default.iconColor"
      },
      "iconButton.on.iconColor": {
        "equivalent": "iconButton.dynamic.on.default.iconColor"
      },
      "iconButton.pressed.backgroundColor": {
        "equivalent": "iconButton.dynamic.pressed.100To250BackgroundColor"
      },
      "iconButton.pressed.iconColor": {
        "equivalent": "iconButton.dynamic.pressed.iconColor"
      },
      "iconButton.pressed.level100To250.backgroundColor": {
        "equivalent": "iconButton.dynamic.pressed.100To250BackgroundColor"
      },
      "iconButton.pressed.level300To350.backgroundColor": {
        "equivalent": "iconButton.dynamic.pressed.300To350BackgroundColor"
      },
      "iconButton.pressed.on.iconColor": {
        "equivalent": "iconButton.dynamic.on.pressed.iconColor"
      },
      "iconButton.static.hover.level100To250.backgroundColor": {
        "equivalent": "iconButton.static.hover.100To250BackgroundColor"
      },
      "iconButton.static.hover.level300To350.backgroundColor": {
        "equivalent": "iconButton.static.hover.300To350BackgroundColor"
      },
      "iconButton.static.on.backgroundColor": {
        "equivalent": "iconButton.static.on.default.backgroundColor"
      },
      "iconButton.static.on.borderColor": {
        "equivalent": "iconButton.static.on.default.borderColor"
      },
      "iconButton.static.on.hover.level100To250.backgroundColor": {
        "equivalent": "iconButton.static.on.hover.100To250BackgroundColor"
      },
      "iconButton.static.on.hover.level300To350.backgroundColor": {
        "equivalent": "iconButton.static.on.hover.300To350BackgroundColor"
      },
      "input.active.halo.color": {
        "equivalent": "input.focus.haloColor"
      },
      "input.borderBottomColor": {
        "equivalent": "input.line.default.borderBottomColor"
      },
      "input.active.halo.width": {
        "equivalent": "input.haloWidth"
      },
      "input.box.backgroundColor": {
        "equivalent": "input.box.default.backgroundColor"
      },
      "input.box.borderBottomColor": {
        "equivalent": "input.box.default.borderBottomColor"
      },
      "input.boxType.horizontalPadding": {
        "equivalent": "input.box.paddingHorizontal"
      },
      "input.focus.borderBottomColor": {
        "equivalent": "input.line.focus.borderBottomColor"
      },
      "input.focus.halo.color": {
        "equivalent": "input.focus.haloColor"
      },
      "input.focus.halo.width": {
        "equivalent": "input.haloWidth"
      },
      "input.fontColor": {
        "equivalent": "input.value.fontColor"
      },
      "input.fontFamily": {
        "equivalent": "input.value.fontFamily"
      },
      "input.fontSize": {
        "equivalent": "input.value.fontSize"
      },
      "input.fontWeight": {
        "equivalent": "input.value.fontWeight"
      },
      "input.halo.color": {
        "equivalent": "input.hover.haloColor"
      },
      "input.halo.width": {
        "equivalent": "input.haloWidth"
      },
      "input.height": {
        "equivalent": "input.minHeight"
      },
      "input.highlightColor": {
        "equivalent": "input.value.highlightColor"
      },
      "input.horizontalPadding": {
        "equivalent": "input.line.paddingHorizontal"
      },
      "input.hover.borderBottomColor": {
        "equivalent": "input.box.hover.borderBottomColor"
      },
      "input.hover.halo.width": {
        "equivalent": "input.haloWidth"
      },
      "input.lineHeight": {
        "equivalent": "input.value.lineHeight"
      },
      "input.placeholder.fontColor": {
        "equivalent": "input.value.placeholderFontColor"
      },
      "input.verticalPadding": {
        "equivalent": "input.paddingVertical"
      },
      "menu.item.backgroundColor": {
        "equivalent": "menu.item.default.backgroundColor"
      },
      "menu.item.active.backgroundColor": {
        "equivalent": "menu.item.pressed.backgroundColor"
      },
      "menu.item.horizontalPadding": {
        "equivalent": "menu.item.paddingHorizontal"
      },
      "menu.item.verticalPadding": {
        "equivalent": "menu.item.paddingVertical"
      },
      "modal.overlay.color": {
        "equivalent": "modal.overlayColor"
      },
      "modal.textColor": {
        "equivalent": "modal.fontColor"
      },
      "modal.body.height": {
        "equivalent": "modal.body.minHeight"
      },
      "modal.horizontalPadding": {
        "equivalent": "modal.paddingHorizontal"
      },
      "modal.header.borderColor": {
        "equivalent": "modal.header.borderBottomColor"
      },
      "modal.header.height": {
        "equivalent": "modal.header.minHeight"
      },
      "modal.window.backgroundColor": {
        "equivalent": "modal.shell.backgroundColor"
      },
      "modal.window.borderRadius": {
        "equivalent": "modal.shell.borderRadius"
      },
      "modal.window.height": {
        "equivalent": "modal.shell.minHeight"
      },
      "modal.window.width": {
        "equivalent": "modal.shell.minWidth"
      },
      "modal.window.shadowColor": {
        "equivalent": "modal.shell.shadowColor"
      },
      "progressBar.backgroundColor": {
        "equivalent": "progress.bar.backgroundColor"
      },
      "progressBar.highlightColor": {
        "equivalent": "progress.bar.highlightColor"
      },
      "progressBar.borderRadius": {
        "equivalent": "progress.bar.borderRadius"
      },
      "progressBar.height": {
        "equivalent": "progress.bar.minHeight"
      },
      "progressRing.backgroundColor": {
        "equivalent": "progress.ring.backgroundColor"
      },
      "progressRing.highlightColor1": {
        "equivalent": "progress.ring.highlightColor1"
      },
      "progressRing.highlightColor2": {
        "equivalent": "progress.ring.highlightColor2"
      },
      "skeletonItem.defaultHeight": {
        "equivalent": "skeletonItem.minHeight"
      },
      "slider.halo.width": {
        "equivalent": "slider.hover.haloWidth"
      },
      "slider.halo.color": {
        "equivalent": "slider.hover.haloColor"
      },
      "slider.thumb.width": {
        "equivalent": "slider.thumb.minWidth"
      },
      "slider.focused.halo.color": {
        "equivalent": "slider.focus.haloColor"
      },
      "slider.focused.halo.width": {
        "equivalent": "slider.focus.haloWidth"
      },
      "slider.focused.thumb.color": {
        "equivalent": "slider.thumb.backgroundColor"
      },
      "slider.hover.halo.color": {
        "equivalent": "slider.hover.haloColor"
      },
      "slider.hover.halo.width": {
        "equivalent": "slider.hover.haloWidth"
      },
      "slider.hover.thumb.color": {
        "equivalent": "slider.thumb.backgroundColor"
      },
      "slider.pressed.halo.color": {
        "equivalent": "slider.pressed.haloColor"
      },
      "slider.pressed.halo.width": {
        "equivalent": "slider.pressed.haloWidth"
      },
      "slider.pressed.thumb.color": {
        "equivalent": "slider.thumb.backgroundColor"
      },
      "slider.track.color": {
        "equivalent": "slider.track.backgroundColor"
      },
      "slider.track.width": {
        "equivalent": "slider.track.minHeight"
      },
      "slider.value.color": {
        "equivalent": "slider.value.backgroundColor"
      },
      "tabs.general.tab.fontFamily": {
        "equivalent": "tabs.label.fontFamily"
      },
      "tabs.general.tab.fontSize": {
        "equivalent": "tabs.label.fontSize"
      },
      "tabs.general.tab.fontWeight": {
        "equivalent": "tabs.label.inactive.fontWeight"
      },
      "tabs.general.tab.active.fontWeight": {
        "equivalent": "tabs.label.active.fontWeight"
      },
      "tabs.general.tab.color": {
        "equivalent": "tabs.label.fontColor"
      },
      "tabs.general.tab.lineHeight": {
        "equivalent": "tabs.label.active.lineHeight"
      },
      "tabs.general.halo.size": {
        "equivalent": "tabs.focus.haloWidth"
      },
      "tabs.general.halo.color": {
        "equivalent": "tabs.focus.haloColor"
      },
      "tabs.general.icon.size": {
        "equivalent": "tabs.iconSize"
      },
      "tabs.general.icon.color": {
        "equivalent": "tabs.iconColor"
      },
      "tabs.general.icon.gutter": {
        "equivalent": "tabs.iconGutter"
      },
      "tabs.general.closeButton.size": {
        "equivalent": "tabs.closeButton.minSize"
      },
      "tabs.general.closeButton.gutter": {
        "equivalent": "tabs.closeButton.gutter"
      },
      "tabs.general.closeButton.color": {
        "equivalent": "tabs.closeButton.default.iconColor"
      },
      "tabs.general.closeButton.hover.color": {
        "equivalent": "tabs.closeButton.hover.iconColor"
      },
      "tabs.general.closeButton.pressed.color": {
        "equivalent": "tabs.closeButton.pressed.iconColor"
      },
      "tabs.underline.halo.hover.color": {
        "equivalent": "tabs.underline.hover.haloColor"
      },
      "tabs.underline.halo.active.color": {
        "equivalent": "tabs.underline.active.haloColor"
      },
      "tabs.box.tab.horizontalPadding": {
        "equivalent": "tabs.box.tab.paddingHorizontal"
      },
      "tabs.box.tab.verticalPadding": {
        "equivalent": "tabs.box.tab.paddingVertical"
      },
      "tabs.box.divider.color": {
        "equivalent": "tabs.box.divider.borderColor"
      },
      "tabs.box.divider.width": {
        "equivalent": "tabs.box.dividerWidth"
      },
      "tabs.box.divider.height": {
        "equivalent": "tabs.box.dividerHeight"
      },
      "tabs.canvas.tab.horizontalPadding": {
        "equivalent": "tabs.canvas.tab.paddingHorizontal"
      },
      "tabs.canvas.tab.verticalPadding": {
        "equivalent": "tabs.canvas.tab.paddingVertical"
      },
      "tabs.canvas.divider.color": {
        "equivalent": "tabs.canvas.divider.borderColor"
      },
      "tabs.canvas.divider.width": {
        "equivalent": "tabs.canvas.dividerWidth"
      },
      "tabs.canvas.divider.height": {
        "equivalent": "tabs.canvas.dividerHeight"
      },
      "textLink.textColor": {
        "equivalent": "textLink.primary.default.fontColor"
      },
      "textLink.hover.textColor": {
        "equivalent": "textLink.primary.hover.fontColor"
      },
      "textLink.focus.textColor": {
        "equivalent": "textLink.primary.focus.fontColor"
      },
      "textLink.hover.underline.color": {
        "equivalent": "textLink.primary.hover.underlineColor"
      },
      "textLink.focus.halo.color": {
        "equivalent": "textLink.focus.haloColor"
      },
      "textLink.focus.halo.width": {
        "equivalent": "textLink.focus.haloWidth"
      },
      "tooltip.textColor": {
        "equivalent": "tooltip.title.fontColor"
      },
      "tooltip.borderColor": {
        "equivalent": "tooltip.backgroundColor"
      }
    }
  },
  "resolvedRoles": {
    "basics.borderRadii.none": "0",
    "basics.borderRadii.small": "0",
    "basics.borderRadii.medium": "2px",
    "basics.borderRadii.large": "4px",
    "basics.borderRadii.ellipse": "50%",
    "basics.borderRadii.pill": "1000em",
    "basics.borderWidths.none": "0",
    "basics.borderWidths.small": "1px",
    "basics.borderWidths.medium": "2px",
    "basics.borderWidths.large": "4px",
    "basics.colors.primary.black": "#000000",
    "basics.colors.primary.white": "#ffffff",
    "basics.colors.primary.autodeskBlue.100": "#cdeaf7",
    "basics.colors.primary.autodeskBlue.200": "#9bd5ef",
    "basics.colors.primary.autodeskBlue.300": "#6ac0e7",
    "basics.colors.primary.autodeskBlue.400": "#38abdf",
    "basics.colors.primary.autodeskBlue.500": "#0696d7",
    "basics.colors.primary.autodeskBlue.600": "#0686c2",
    "basics.colors.primary.autodeskBlue.700": "#006eaf",
    "basics.colors.primary.autodeskBlue.800": "#034b6c",
    "basics.colors.primary.autodeskBlue.900": "#022c40",
    "basics.colors.primary.charcoal.100": "#eeeeee",
    "basics.colors.primary.charcoal.200": "#dcdcdc",
    "basics.colors.primary.charcoal.300": "#cccccc",
    "basics.colors.primary.charcoal.400": "#bbbbbb",
    "basics.colors.primary.charcoal.500": "#999999",
    "basics.colors.primary.charcoal.600": "#808080",
    "basics.colors.primary.charcoal.700": "#666666",
    "basics.colors.primary.charcoal.800": "#4d4d4d",
    "basics.colors.primary.charcoal.900": "#3c3c3c",
    "basics.colors.secondary.darkBlue.100": "#e1ebf7",
    "basics.colors.secondary.darkBlue.200": "#d1deee",
    "basics.colors.secondary.darkBlue.300": "#a3bcdc",
    "basics.colors.secondary.darkBlue.400": "#749bcb",
    "basics.colors.secondary.darkBlue.500": "#4679b9",
    "basics.colors.secondary.darkBlue.600": "#1858a8",
    "basics.colors.secondary.darkBlue.700": "#12437f",
    "basics.colors.secondary.darkBlue.800": "#0e3666",
    "basics.colors.secondary.darkBlue.900": "#0c2c54",
    "basics.colors.secondary.green.100": "#e7f2d9",
    "basics.colors.secondary.green.200": "#cfe4b3",
    "basics.colors.secondary.green.300": "#b7d78c",
    "basics.colors.secondary.green.400": "#9fc966",
    "basics.colors.secondary.green.500": "#87b340",
    "basics.colors.secondary.green.600": "#658d30",
    "basics.colors.secondary.green.700": "#5d822c",
    "basics.colors.secondary.green.800": "#4c6b24",
    "basics.colors.secondary.green.900": "#445e20",
    "basics.colors.secondary.red.100": "#ffd9d9",
    "basics.colors.secondary.red.200": "#f8d3d3",
    "basics.colors.secondary.red.300": "#f1a7a7",
    "basics.colors.secondary.red.400": "#eb7a7a",
    "basics.colors.secondary.red.500": "#ec4a41",
    "basics.colors.secondary.red.600": "#dd2222",
    "basics.colors.secondary.red.700": "#a61a1a",
    "basics.colors.secondary.red.800": "#6f1111",
    "basics.colors.secondary.red.900": "#630f0f",
    "basics.colors.secondary.turquoise.100": "#d6f2ef",
    "basics.colors.secondary.turquoise.200": "#ade4de",
    "basics.colors.secondary.turquoise.300": "#84d7ce",
    "basics.colors.secondary.turquoise.400": "#5bc9bd",
    "basics.colors.secondary.turquoise.500": "#32bcad",
    "basics.colors.secondary.turquoise.600": "#23a597",
    "basics.colors.secondary.turquoise.700": "#268d82",
    "basics.colors.secondary.turquoise.800": "#20756c",
    "basics.colors.secondary.turquoise.900": "#195e57",
    "basics.colors.secondary.yellowOrange.100": "#feecd1",
    "basics.colors.secondary.yellowOrange.200": "#fddaa4",
    "basics.colors.secondary.yellowOrange.300": "#fcc776",
    "basics.colors.secondary.yellowOrange.400": "#fbb549",
    "basics.colors.secondary.yellowOrange.500": "#faa21b",
    "basics.colors.secondary.yellowOrange.600": "#bb7a14",
    "basics.colors.secondary.yellowOrange.700": "#916010",
    "basics.colors.secondary.yellowOrange.800": "#7d510e",
    "basics.colors.secondary.yellowOrange.900": "#523609",
    "basics.colors.tertiary.pink.100": "#ffebf5",
    "basics.colors.tertiary.pink.200": "#ffd6eb",
    "basics.colors.tertiary.pink.300": "#ffb8db",
    "basics.colors.tertiary.pink.400": "#ff8cc6",
    "basics.colors.tertiary.pink.500": "#fc56a9",
    "basics.colors.tertiary.pink.600": "#e84396",
    "basics.colors.tertiary.pink.700": "#c72877",
    "basics.colors.tertiary.pink.800": "#991f5c",
    "basics.colors.tertiary.pink.900": "#781848",
    "basics.colors.tertiary.purple.100": "#f4edfd",
    "basics.colors.tertiary.purple.200": "#eadcfd",
    "basics.colors.tertiary.purple.300": "#d5bcf7",
    "basics.colors.tertiary.purple.400": "#c5a1f8",
    "basics.colors.tertiary.purple.500": "#b385f2",
    "basics.colors.tertiary.purple.600": "#a76ef5",
    "basics.colors.tertiary.purple.700": "#8558c5",
    "basics.colors.tertiary.purple.800": "#5f3e8e",
    "basics.colors.tertiary.purple.900": "#482f6b",
    "basics.colors.tertiary.salmon.100": "#ffefec",
    "basics.colors.tertiary.salmon.200": "#fcded9",
    "basics.colors.tertiary.salmon.300": "#ffbeb3",
    "basics.colors.tertiary.salmon.400": "#ff9582",
    "basics.colors.tertiary.salmon.500": "#f26a52",
    "basics.colors.tertiary.salmon.600": "#db5942",
    "basics.colors.tertiary.salmon.700": "#a84a39",
    "basics.colors.tertiary.salmon.800": "#853729",
    "basics.colors.tertiary.salmon.900": "#66251a",
    "basics.colors.tertiary.slate.100": "#e9f0f7",
    "basics.colors.tertiary.slate.200": "#dce7f3",
    "basics.colors.tertiary.slate.300": "#c1cedc",
    "basics.colors.tertiary.slate.400": "#a7bacf",
    "basics.colors.tertiary.slate.500": "#7993b0",
    "basics.colors.tertiary.slate.600": "#6784a6",
    "basics.colors.tertiary.slate.700": "#4b6b8f",
    "basics.colors.tertiary.slate.800": "#354d67",
    "basics.colors.tertiary.slate.900": "#2c3e53",
    "basics.colors.surface.lightGray.100": "#ffffff",
    "basics.colors.surface.lightGray.200": "#f5f5f5",
    "basics.colors.surface.lightGray.250": "#eeeeee",
    "basics.colors.surface.lightGray.300": "#d9d9d9",
    "basics.colors.surface.lightGray.350": "#cccccc",
    "basics.colors.surface.darkBlue.100": "#454f61",
    "basics.colors.surface.darkBlue.200": "#3b4453",
    "basics.colors.surface.darkBlue.250": "#2e3440",
    "basics.colors.surface.darkBlue.300": "#222933",
    "basics.colors.surface.darkBlue.350": "#1a1f25",
    "basics.colors.surface.darkGray.100": "#535353",
    "basics.colors.surface.darkGray.200": "#474747",
    "basics.colors.surface.darkGray.250": "#373737",
    "basics.colors.surface.darkGray.300": "#2a2a2a",
    "basics.colors.surface.darkGray.350": "#202020",
    "basics.colors.shadow.darkBlue": "rgba(26, 31, 38, 0.5)",
    "basics.colors.shadow.darkGray": "rgba(0, 0, 0, 0.3)",
    "basics.colors.shadow.lightGray": "rgba(0, 0, 0, 0.2)",
    "basics.colors.text.againstDark": "#f5f5f5",
    "basics.colors.text.againstLight": "#3c3c3c",
    "basics.colors.textLink.primary.againstLight": "#006eaf",
    "basics.colors.textLink.primary.againstDark": "#6dd2ff",
    "basics.colors.textLink.secondary.againstLight": "#3c3c3c",
    "basics.colors.textLink.secondary.againstDark": "#f5f5f5",
    "basics.colors.icon.lightGray": "#808080",
    "basics.colors.icon.darkBlue": "#a2a6b0",
    "basics.colors.icon.darkGray": "#999999",
    "basics.colors.black": "#000000",
    "basics.colors.white": "#ffffff",
    "basics.colors.autodeskBlue100": "#cdeaf7",
    "basics.colors.autodeskBlue200": "#9bd5ef",
    "basics.colors.autodeskBlue300": "#6ac0e7",
    "basics.colors.autodeskBlue400": "#38abdf",
    "basics.colors.autodeskBlue500": "#0696d7",
    "basics.colors.autodeskBlue600": "#0686c2",
    "basics.colors.autodeskBlue700": "#006eaf",
    "basics.colors.autodeskBlue800": "#034b6c",
    "basics.colors.autodeskBlue900": "#022c40",
    "basics.colors.charcoal100": "#eeeeee",
    "basics.colors.charcoal200": "#dcdcdc",
    "basics.colors.charcoal300": "#cccccc",
    "basics.colors.charcoal400": "#bbbbbb",
    "basics.colors.charcoal500": "#999999",
    "basics.colors.charcoal600": "#808080",
    "basics.colors.charcoal700": "#666666",
    "basics.colors.charcoal800": "#4d4d4d",
    "basics.colors.charcoal900": "#3c3c3c",
    "basics.colors.darkBlue100": "#e1ebf7",
    "basics.colors.darkBlue200": "#d1deee",
    "basics.colors.darkBlue300": "#a3bcdc",
    "basics.colors.darkBlue400": "#749bcb",
    "basics.colors.darkBlue500": "#4679b9",
    "basics.colors.darkBlue600": "#1858a8",
    "basics.colors.darkBlue700": "#12437f",
    "basics.colors.darkBlue800": "#0e3666",
    "basics.colors.darkBlue900": "#0c2c54",
    "basics.colors.green100": "#e7f2d9",
    "basics.colors.green200": "#cfe4b3",
    "basics.colors.green300": "#b7d78c",
    "basics.colors.green400": "#9fc966",
    "basics.colors.green500": "#87b340",
    "basics.colors.green600": "#658d30",
    "basics.colors.green700": "#5d822c",
    "basics.colors.green800": "#4c6b24",
    "basics.colors.green900": "#445e20",
    "basics.colors.red100": "#ffd9d9",
    "basics.colors.red200": "#f8d3d3",
    "basics.colors.red300": "#f1a7a7",
    "basics.colors.red400": "#eb7a7a",
    "basics.colors.red500": "#ec4a41",
    "basics.colors.red600": "#dd2222",
    "basics.colors.red700": "#a61a1a",
    "basics.colors.red800": "#6f1111",
    "basics.colors.red900": "#630f0f",
    "basics.colors.turquoise100": "#d6f2ef",
    "basics.colors.turquoise200": "#ade4de",
    "basics.colors.turquoise300": "#84d7ce",
    "basics.colors.turquoise400": "#5bc9bd",
    "basics.colors.turquoise500": "#32bcad",
    "basics.colors.turquoise600": "#23a597",
    "basics.colors.turquoise700": "#268d82",
    "basics.colors.turquoise800": "#20756c",
    "basics.colors.turquoise900": "#195e57",
    "basics.colors.yellowOrange100": "#feecd1",
    "basics.colors.yellowOrange200": "#fddaa4",
    "basics.colors.yellowOrange300": "#fcc776",
    "basics.colors.yellowOrange400": "#fbb549",
    "basics.colors.yellowOrange500": "#faa21b",
    "basics.colors.yellowOrange600": "#bb7a14",
    "basics.colors.yellowOrange700": "#916010",
    "basics.colors.yellowOrange800": "#7d510e",
    "basics.colors.yellowOrange900": "#523609",
    "basics.colors.pink100": "#ffebf5",
    "basics.colors.pink200": "#ffd6eb",
    "basics.colors.pink300": "#ffb8db",
    "basics.colors.pink400": "#ff8cc6",
    "basics.colors.pink500": "#fc56a9",
    "basics.colors.pink600": "#e84396",
    "basics.colors.pink700": "#c72877",
    "basics.colors.pink800": "#991f5c",
    "basics.colors.pink900": "#781848",
    "basics.colors.purple100": "#f4edfd",
    "basics.colors.purple200": "#eadcfd",
    "basics.colors.purple300": "#d5bcf7",
    "basics.colors.purple400": "#c5a1f8",
    "basics.colors.purple500": "#b385f2",
    "basics.colors.purple600": "#a76ef5",
    "basics.colors.purple700": "#8558c5",
    "basics.colors.purple800": "#5f3e8e",
    "basics.colors.purple900": "#482f6b",
    "basics.colors.salmon100": "#ffefec",
    "basics.colors.salmon200": "#fcded9",
    "basics.colors.salmon300": "#ffbeb3",
    "basics.colors.salmon400": "#ff9582",
    "basics.colors.salmon500": "#f26a52",
    "basics.colors.salmon600": "#db5942",
    "basics.colors.salmon700": "#a84a39",
    "basics.colors.salmon800": "#853729",
    "basics.colors.salmon900": "#66251a",
    "basics.colors.slate100": "#e9f0f7",
    "basics.colors.slate200": "#dce7f3",
    "basics.colors.slate300": "#c1cedc",
    "basics.colors.slate400": "#a7bacf",
    "basics.colors.slate500": "#7993b0",
    "basics.colors.slate600": "#6784a6",
    "basics.colors.slate700": "#4b6b8f",
    "basics.colors.slate800": "#354d67",
    "basics.colors.slate900": "#2c3e53",
    "basics.colors.surfaceLightGrayLevel100": "#ffffff",
    "basics.colors.surfaceLightGrayLevel200": "#f5f5f5",
    "basics.colors.surfaceLightGrayLevel250": "#eeeeee",
    "basics.colors.surfaceLightGrayLevel300": "#d9d9d9",
    "basics.colors.surfaceLightGrayLevel350": "#cccccc",
    "basics.colors.surfaceDarkBlueLevel100": "#454f61",
    "basics.colors.surfaceDarkBlueLevel200": "#3b4453",
    "basics.colors.surfaceDarkBlueLevel250": "#2e3440",
    "basics.colors.surfaceDarkBlueLevel300": "#222933",
    "basics.colors.surfaceDarkBlueLevel350": "#1a1f25",
    "basics.colors.surfaceDarkGrayLevel100": "#535353",
    "basics.colors.surfaceDarkGrayLevel200": "#474747",
    "basics.colors.surfaceDarkGrayLevel250": "#373737",
    "basics.colors.surfaceDarkGrayLevel300": "#2a2a2a",
    "basics.colors.surfaceDarkGrayLevel350": "#202020",
    "basics.colors.surfaceDarkBlueShadow": "rgba(26, 31, 38, 0.5)",
    "basics.colors.surfaceDarkGrayShadow": "rgba(0, 0, 0, 0.3)",
    "basics.colors.surfaceLightGrayShadow": "rgba(0, 0, 0, 0.2)",
    "basics.colors.textAgainstDark": "#f5f5f5",
    "basics.colors.textAgainstLight": "#3c3c3c",
    "basics.colors.iconLightGrayColor": "#808080",
    "basics.colors.iconDarkBlueColor": "#a2a6b0",
    "basics.colors.iconDarkGrayColor": "#999999",
    "basics.colors.textLinkAgainstLight": "#006eaf",
    "basics.colors.textLinkAgainstDark": "#6dd2ff",
    "basics.colors.error": "#ec4a41",
    "basics.colors.success": "#87b340",
    "basics.colors.warning": "#faa21b",
    "basics.fontFamilies.main": "ArtifaktElement, sans-serif",
    "basics.fontFamilies.monospace": "'Source Code Pro', monospace",
    "basics.fontSizes.highSmall": "11px",
    "basics.fontSizes.highMedium": "12px",
    "basics.fontSizes.highLarge": "16px",
    "basics.fontSizes.highExtraLarge": "20px",
    "basics.fontSizes.highExtraExtraLarge": "24px",
    "basics.fontSizes.mediumSmall": "12px",
    "basics.fontSizes.mediumMedium": "14px",
    "basics.fontSizes.mediumLarge": "20px",
    "basics.fontSizes.mediumExtraLarge": "24px",
    "basics.fontSizes.mediumExtraExtraLarge": "28px",
    "basics.fontSizes.lowSmall": "14px",
    "basics.fontSizes.lowMedium": "16px",
    "basics.fontSizes.lowLarge": "22px",
    "basics.fontSizes.lowExtraLarge": "26px",
    "basics.fontSizes.lowExtraExtraLarge": "30px",
    "basics.fontWeights.regular": 400,
    "basics.fontWeights.medium": 600,
    "basics.fontWeights.bold": 700,
    "basics.lineHeights.small": "1.3",
    "basics.lineHeights.medium": "1.4",
    "basics.lineHeights.large": "1.5",
    "basics.lineHeights.highSmall": "1.25",
    "basics.lineHeights.highMedium": "1.272727273",
    "basics.lineHeights.highLarge": "1.3",
    "basics.lineHeights.highExtraLarge": "1.333333333",
    "basics.lineHeights.highExtraExtraLarge": "1.375",
    "basics.lineHeights.mediumSmall": "1.25",
    "basics.lineHeights.mediumMedium": "1.285714286",
    "basics.lineHeights.mediumLarge": "1.3",
    "basics.lineHeights.mediumExtraLarge": "1.428571429",
    "basics.lineHeights.mediumExtraExtraLarge": "1.5",
    "basics.shadows.lowBlur": "4px",
    "basics.shadows.highBlur": "16px",
    "basics.spacings.none": "0",
    "basics.spacings.highExtraExtraSmall": "2px",
    "basics.spacings.highExtraSmall": "4px",
    "basics.spacings.highSmall": "8px",
    "basics.spacings.highMedium": "12px",
    "basics.spacings.highLarge": "16px",
    "basics.spacings.highExtraLarge": "24px",
    "basics.spacings.highExtraExtraLarge": "32px",
    "basics.spacings.mediumExtraExtraSmall": "4px",
    "basics.spacings.mediumExtraSmall": "8px",
    "basics.spacings.mediumSmall": "12px",
    "basics.spacings.mediumMedium": "16px",
    "basics.spacings.mediumLarge": "24px",
    "basics.spacings.mediumExtraLarge": "32px",
    "basics.spacings.mediumExtraExtraLarge": "48px",
    "basics.spacings.lowExtraExtraSmall": "8px",
    "basics.spacings.lowExtraSmall": "12px",
    "basics.spacings.lowSmall": "16px",
    "basics.spacings.lowMedium": "24px",
    "basics.spacings.lowLarge": "32px",
    "basics.spacings.lowExtraLarge": "48px",
    "basics.spacings.lowExtraExtraLarge": "64px",
    "colorScheme.reference.accent": "#0696d7",
    "colorScheme.reference.base": "#808080",
    "colorScheme.surface.level100": "#ffffff",
    "colorScheme.surface.level200": "#f5f5f5",
    "colorScheme.surface.level250": "#eeeeee",
    "colorScheme.surface.level300": "#d9d9d9",
    "colorScheme.surface.level350": "#cccccc",
    "colorScheme.background.selected": "rgba(6, 150, 215, 0.15)",
    "colorScheme.background.on.default": "rgba(205, 234, 247, 0.6)",
    "colorScheme.background.on.hover": "rgba(155, 213, 239, 0.7)",
    "colorScheme.background.on.focus": "rgba(205, 234, 247, 0.6)",
    "colorScheme.background.on.pressed": "rgba(106, 192, 231, 0.7)",
    "colorScheme.background.empty.level100To250.hover": "rgba(128, 128, 128, 0.1)",
    "colorScheme.background.empty.level100To250.pressed": "rgba(128, 128, 128, 0.2)",
    "colorScheme.background.empty.level300To350.hover": "rgba(255, 255, 255, 0.25)",
    "colorScheme.background.empty.level300To350.pressed": "rgba(255, 255, 255, 0.35)",
    "colorScheme.background.filled.level100To250.default": "rgba(128, 128, 128, 0.15)",
    "colorScheme.background.filled.level100To250.hover": "rgba(128, 128, 128, 0.25)",
    "colorScheme.background.filled.level100To250.focus": "rgba(128, 128, 128, 0.15)",
    "colorScheme.background.filled.level100To250.pressed": "rgba(128, 128, 128, 0.35)",
    "colorScheme.background.filled.level300To350.default": "rgba(255, 255, 255, 0.5)",
    "colorScheme.background.filled.level300To350.hover": "rgba(255, 255, 255, 0.7)",
    "colorScheme.background.filled.level300To350.focus": "rgba(255, 255, 255, 0.5)",
    "colorScheme.background.filled.level300To350.pressed": "#ffffff",
    "colorScheme.background.transparent": "rgba(128, 128, 128, 0)",
    "colorScheme.border.base": "rgba(128, 128, 128, 0.5)",
    "colorScheme.border.accent": "#0696d7",
    "colorScheme.border.on": "rgba(6, 150, 215, 0.5)",
    "colorScheme.border.transparent": "rgba(128, 128, 128, 0)",
    "colorScheme.halo.hover": "rgba(128, 128, 128, 0.15)",
    "colorScheme.halo.focus": "rgba(6, 150, 215, 0.35)",
    "colorScheme.halo.pressed": "rgba(128, 128, 128, 0.25)",
    "colorScheme.shadow.high": "rgba(0, 0, 0, 0.2)",
    "colorScheme.shadow.low": "rgba(0, 0, 0, 0.2)",
    "colorScheme.text.default": "#3c3c3c",
    "colorScheme.text.dim": "rgba(60, 60, 60, 0.7)",
    "colorScheme.text.placeholder": "rgba(60, 60, 60, 0.4)",
    "colorScheme.text.active": "#0696d7",
    "colorScheme.icon.default": "#808080",
    "colorScheme.icon.hover": "#3c3c3c",
    "colorScheme.icon.focus": "#808080",
    "colorScheme.icon.pressed": "#3c3c3c",
    "colorScheme.icon.on.default": "#0696d7",
    "colorScheme.icon.on.hover": "#006eaf",
    "colorScheme.icon.on.focus": "#0696d7",
    "colorScheme.icon.on.pressed": "#006eaf",
    "colorScheme.indicator.default": "rgba(102, 102, 102, 0.7)",
    "colorScheme.indicator.hover": "#666666",
    "colorScheme.indicator.focus": "rgba(102, 102, 102, 0.7)",
    "colorScheme.indicator.pressed": "#0696d7",
    "colorScheme.status.error": "#ec4a41",
    "colorScheme.status.success": "#87b340",
    "colorScheme.status.warning": "#faa21b",
    "colorScheme.status.info": "#0696d7",
    "colorScheme.status.read": "rgba(128, 128, 128, 0.4)",
    "colorScheme.divider.lightweight": "rgba(60, 60, 60, 0.1)",
    "colorScheme.divider.heavyweight": "rgba(60, 60, 60, 0.25)",
    "colorScheme.opacity.mask": "#e84396",
    "colorScheme.opacity.disabled": 0.4,
    "colorScheme.accentColor": "#0696d7",
    "colorScheme.baseColor": "#808080",
    "colorScheme.surfaceLevel100Color": "#ffffff",
    "colorScheme.surfaceLevel200Color": "#f5f5f5",
    "colorScheme.surfaceLevel250Color": "#eeeeee",
    "colorScheme.surfaceLevel300Color": "#d9d9d9",
    "colorScheme.surfaceLevel350Color": "#cccccc",
    "colorScheme.highShadowColor": "rgba(0, 0, 0, 0.2)",
    "colorScheme.lowShadowColor": "rgba(0, 0, 0, 0.2)",
    "colorScheme.textColor": "#3c3c3c",
    "colorScheme.textColorDim": "rgba(60, 60, 60, 0.7)",
    "colorScheme.iconColor": "#808080",
    "colorScheme.errorColor": "#ec4a41",
    "colorScheme.successColor": "#87b340",
    "colorScheme.warningColor": "#faa21b",
    "colorScheme.infoColor": "#0696d7",
    "colorScheme.component.backgroundColor": "#ffffff",
    "density.spacings.extraExtraSmall": "4px",
    "density.spacings.extraSmall": "8px",
    "density.spacings.small": "12px",
    "density.spacings.medium": "16px",
    "density.spacings.large": "24px",
    "density.spacings.extraLarge": "32px",
    "density.spacings.extraExtraLarge": "48px",
    "density.fontSizes.small": "12px",
    "density.fontSizes.medium": "14px",
    "density.fontSizes.large": "20px",
    "density.fontSizes.extraLarge": "24px",
    "density.fontSizes.extraExtraLarge": "28px",
    "accordion.header.fontColor": "#3c3c3c",
    "accordion.header.fontFamily": "ArtifaktElement, sans-serif",
    "accordion.header.fontSize": "14px",
    "accordion.header.fontWeight": 700,
    "accordion.header.lineHeight": "1.428571429",
    "accordion.header.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "accordion.header.paddingHorizontal": "8px",
    "accordion.header.paddingVertical": "8px",
    "accordion.header.indicatorGutter": "8px",
    "accordion.header.indicatorSize": "16px",
    "accordion.header.default.indicatorColor": "rgba(102, 102, 102, 0.7)",
    "accordion.header.hover.indicatorColor": "#666666",
    "accordion.header.indicatorRight.paddingLeft": "12px",
    "accordion.header.indicatorRight.paddingRight": "16px",
    "accordion.header.indicatorColor": "rgba(102, 102, 102, 0.7)",
    "avatar.fontWeight": 400,
    "avatar.darkBackground.fontColor": "#ffffff",
    "avatar.lightBackground.fontColor": "#3c3c3c",
    "avatar.extraSmall.fontSize": "11px",
    "avatar.extraSmall.diameter": "16px",
    "avatar.small.fontSize": "12px",
    "avatar.small.diameter": "24px",
    "avatar.medium.fontSize": "14px",
    "avatar.medium.diameter": "32px",
    "avatar.large.fontSize": "24px",
    "avatar.large.diameter": "48px",
    "avatar.extraLarge.fontSize": "30px",
    "avatar.extraLarge.diameter": "64px",
    "avatar.color1.backgroundColor": "#6ac0e7",
    "avatar.color2.backgroundColor": "#1858a8",
    "avatar.color3.backgroundColor": "#5d822c",
    "avatar.color4.backgroundColor": "#ff9582",
    "avatar.color5.backgroundColor": "#7993b0",
    "avatar.color6.backgroundColor": "#5bc9bd",
    "avatar.color7.backgroundColor": "#faa21b",
    "avatar.color1.fontColor": "#3c3c3c",
    "avatar.color2.fontColor": "#ffffff",
    "avatar.color3.fontColor": "#ffffff",
    "avatar.color4.fontColor": "#3c3c3c",
    "avatar.color5.fontColor": "#ffffff",
    "avatar.color6.fontColor": "#3c3c3c",
    "avatar.color7.fontColor": "#3c3c3c",
    "avatar.fontColorOnDark": "#ffffff",
    "avatar.fontColorOnLight": "#3c3c3c",
    "avatarBundle.fontColor": "#3c3c3c",
    "avatarBundle.default.backgroundColor": "#eeeeee",
    "avatarBundle.hover.backgroundColor": "#eeeeee",
    "avatarBundle.pressed.backgroundColor": "#eeeeee",
    "avatarBundle.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "avatarBundle.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "avatarBundle.hover.haloWidth": "2px",
    "avatarBundle.pressed.haloWidth": "4px",
    "banner.error.backgroundColor": "#ffefec",
    "banner.error.borderColor": "#f1a7a7",
    "banner.error.iconField.backgroundColor": "#ec4a41",
    "banner.info.backgroundColor": "#cdeaf7",
    "banner.info.borderColor": "#9bd5ef",
    "banner.info.iconField.backgroundColor": "#0696d7",
    "banner.success.backgroundColor": "#e7f2d9",
    "banner.success.borderColor": "#b7d78c",
    "banner.success.iconField.backgroundColor": "#87b340",
    "banner.warning.backgroundColor": "#feecd1",
    "banner.warning.borderColor": "#fcc776",
    "banner.warning.iconField.backgroundColor": "#faa21b",
    "banner.info.iconBackground": "#0696d7",
    "banner.success.iconBackground": "#87b340",
    "banner.warning.iconBackground": "#faa21b",
    "banner.error.iconBackground": "#ec4a41",
    "breadcrumb.label.fontColor": "#3c3c3c",
    "breadcrumb.label.fontFamily": "ArtifaktElement, sans-serif",
    "breadcrumb.label.fontSize": "14px",
    "breadcrumb.label.default.fontWeight": 400,
    "breadcrumb.label.selected.fontWeight": 600,
    "breadcrumb.default.indicatorColor": "rgba(102, 102, 102, 0.7)",
    "breadcrumb.selected.indicatorColor": "#0696d7",
    "breadcrumb.divider.fontColor": "rgba(60, 60, 60, 0.4)",
    "breadcrumb.divider.fontWeight": 600,
    "button.borderRadius": "2px",
    "button.borderWidth": "1px",
    "button.minWidth": "10px",
    "button.paddingHorizontal": "16px",
    "button.paddingVertical": "8px",
    "button.gutterWidth": "4px",
    "button.label.fontFamily": "ArtifaktElement, sans-serif",
    "button.label.fontSize": "14px",
    "button.label.fontWeight": 600,
    "button.label.lineHeight": "18px",
    "button.haloWidth": "2px",
    "button.hover.haloWidth": "2px",
    "button.pressed.haloWidth": "4px",
    "button.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "button.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "button.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "button.solid.label.fontColor": "#ffffff",
    "button.solid.iconColor": "#ffffff",
    "button.solid.indicatorColor": "#ffffff",
    "button.solid.default.backgroundColor": "#0696d7",
    "button.solid.hover.backgroundColor": "#0696d7",
    "button.solid.focus.backgroundColor": "#0696d7",
    "button.solid.pressed.backgroundColor": "#0696d7",
    "button.solid.default.borderColor": "#0696d7",
    "button.solid.hover.borderColor": "#0696d7",
    "button.solid.focus.borderColor": "#0696d7",
    "button.solid.pressed.borderColor": "#0696d7",
    "button.outline.label.fontColor": "#3c3c3c",
    "button.outline.indicatorColor": "rgba(102, 102, 102, 0.7)",
    "button.outline.iconColor": "#808080",
    "button.outline.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.default.borderColor": "rgba(128, 128, 128, 0.5)",
    "button.outline.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "button.outline.focus.borderColor": "#0696d7",
    "button.outline.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "button.flat.label.fontColor": "#006eaf",
    "button.flat.iconColor": "#006eaf",
    "button.flat.indicatorColor": "#006eaf",
    "button.flat.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.default.borderColor": "rgba(128, 128, 128, 0)",
    "button.flat.hover.borderColor": "rgba(128, 128, 128, 0)",
    "button.flat.focus.borderColor": "rgba(128, 128, 128, 0)",
    "button.flat.pressed.borderColor": "rgba(128, 128, 128, 0)",
    "button.fontWeight": 600,
    "button.lineHeight": "18px",
    "button.fontFamily": "ArtifaktElement, sans-serif",
    "button.fontSize": "14px",
    "button.gutter": "4px",
    "button.horizontalPadding": "16px",
    "button.minimumWidth": "10px",
    "button.verticalPadding": "8px",
    "button.halo.width": "2px",
    "button.halo.color": "#808080",
    "button.hover.halo.width": "2px",
    "button.focus.halo.width": "2px",
    "button.pressed.halo.width": "4px",
    "button.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "button.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "button.outline.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.outline.borderColor": "rgba(128, 128, 128, 0.5)",
    "button.outline.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "button.outline.focus.icon.color": "#808080",
    "button.outline.focus.textColor": "#3c3c3c",
    "button.outline.halo.color": "#808080",
    "button.outline.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "button.outline.hover.icon.color": "#808080",
    "button.outline.hover.textColor": "#3c3c3c",
    "button.outline.icon.color": "#808080",
    "button.outline.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "button.outline.pressed.haloWidth": "4px",
    "button.outline.pressed.icon.color": "#808080",
    "button.outline.pressed.textColor": "#3c3c3c",
    "button.outline.textColor": "#3c3c3c",
    "button.solid.backgroundColor": "#0696d7",
    "button.solid.borderColor": "#0696d7",
    "button.solid.halo.color": "#808080",
    "button.solid.textColor": "#ffffff",
    "button.solid.icon.color": "#ffffff",
    "button.solid.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "button.solid.hover.textColor": "#ffffff",
    "button.solid.hover.icon.color": "#ffffff",
    "button.solid.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "button.solid.focus.textColor": "#ffffff",
    "button.solid.focus.icon.color": "#ffffff",
    "button.solid.pressed.haloWidth": "4px",
    "button.solid.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "button.solid.pressed.textColor": "#ffffff",
    "button.solid.pressed.icon.color": "#ffffff",
    "button.flat.backgroundColor": "rgba(128, 128, 128, 0)",
    "button.flat.borderColor": "rgba(128, 128, 128, 0)",
    "button.flat.halo.color": "#808080",
    "button.flat.textColor": "#006eaf",
    "button.flat.icon.color": "#006eaf",
    "button.flat.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "button.flat.hover.textColor": "#006eaf",
    "button.flat.hover.icon.color": "#006eaf",
    "button.flat.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "button.flat.focus.textColor": "#006eaf",
    "button.flat.focus.icon.color": "#006eaf",
    "button.flat.pressed.haloWidth": "4px",
    "button.flat.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "button.flat.pressed.textColor": "#006eaf",
    "button.flat.pressed.icon.color": "#006eaf",
    "canvasFrame.label.fontColor": "#3c3c3c",
    "canvasFrame.label.fontFamily": "ArtifaktElement, sans-serif",
    "canvasFrame.label.fontSize": "14px",
    "canvasFrame.label.fontWeight": 600,
    "canvasFrame.label.marginLeft": "12px",
    "canvasFrame.border.borderColor": "#0696d7",
    "canvasFrame.border.borderWidth": "2px",
    "canvasFrame.controlPanel.backgroundColor": "#ffffff",
    "canvasFrame.controlPanel.paddingRight": "12px",
    "canvasFrame.controlPanel.paddingLeft": "8px",
    "canvasFrame.controlPanel.paddingVertical": "8px",
    "canvasFrame.closeIcon.marginLeft": "12px",
    "checkbox.borderWidth": "1px",
    "checkbox.borderRadius": "0",
    "checkbox.halo.color": "rgba(128, 128, 128, 0.15)",
    "checkbox.halo.width": "2px",
    "checkbox.minHeight": "16px",
    "checkbox.height": "16px",
    "checkbox.indicatorColor": "#ffffff",
    "checkbox.iconColor": "#ffffff",
    "checkbox.minWidth": "16px",
    "checkbox.width": "16px",
    "checkbox.unchecked.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.unchecked.default.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.unchecked.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.hover.halo.width": "2px",
    "checkbox.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "checkbox.unchecked.focus.borderColor": "#0696d7",
    "checkbox.focus.borderColor": "#0696d7",
    "checkbox.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "checkbox.focus.halo.width": "2px",
    "checkbox.unchecked.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "checkbox.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "checkbox.pressed.halo.width": "4px",
    "checkbox.checked.default.borderColor": "#808080",
    "checkbox.checked.borderColor": "#808080",
    "checkbox.checked.iconColor": "#ffffff",
    "checkbox.checked.default.backgroundColor": "#808080",
    "checkbox.checked.backgroundColor": "#808080",
    "checkbox.indeterminate.borderColor": "#808080",
    "checkbox.indeterminate.iconColor": "#ffffff",
    "checkbox.indeterminate.backgroundColor": "#808080",
    "checkbox.focus.checked.backgroundColor": "#808080",
    "checkbox.focus.checked.borderColor": "#808080",
    "checkbox.focus.indeterminate.borderColor": "#808080",
    "checkbox.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "checkbox.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "checkbox.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "checkbox.focus.haloWidth": "2px",
    "checkbox.hover.haloWidth": "2px",
    "checkbox.pressed.haloWidth": "4px",
    "checkbox.unchecked.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.unchecked.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.unchecked.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "checkbox.checked.hover.backgroundColor": "#808080",
    "checkbox.checked.hover.borderColor": "#808080",
    "checkbox.checked.pressed.backgroundColor": "#808080",
    "checkbox.checked.pressed.borderColor": "#808080",
    "component.disabled.opacity": "0.4",
    "datePicker.fontFamily": "ArtifaktElement, sans-serif",
    "datePicker.container.paddingHorizontal": "16px",
    "datePicker.container.paddingVertical": "24px",
    "datePicker.cell.minHeight": "36px",
    "datePicker.cell.minWidth": "36px",
    "datePicker.standardDate.fontColor": "#3c3c3c",
    "datePicker.standardDate.fontSize": "14px",
    "datePicker.standardDate.fontWeight": 400,
    "datePicker.selectedDate.fontWeight": 600,
    "datePicker.selectedDate.fontColor": "#ffffff",
    "datePicker.selectedDate.fontSize": "14px",
    "datePicker.notableDate.fontWeight": 400,
    "datePicker.notableDate.fontColor": "#0696d7",
    "datePicker.notableDate.fontSize": "14px",
    "datePicker.restrictedDate.fontWeight": 400,
    "datePicker.restrictedDate.fontColor": "rgba(60, 60, 60, 0.4)",
    "datePicker.restrictedDate.fontSize": "14px",
    "datePicker.day.fontWeight": 400,
    "datePicker.day.fontColor": "#3c3c3c",
    "datePicker.day.fontSize": "14px",
    "datePicker.header.fontWeight": 600,
    "datePicker.header.fontColor": "#3c3c3c",
    "datePicker.header.fontSize": "20px",
    "datePicker.header.paddingBottom": "24px",
    "datePicker.cell.borderColor": "#eeeeee",
    "datePicker.cell.borderWidth": "1px",
    "datePicker.standardDateCell.default.backgroundColor": "#ffffff",
    "datePicker.standardDateCell.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "datePicker.standardDateCell.focus.backgroundColor": "#ffffff",
    "datePicker.standardDateCell.focus.borderColor": "#0696d7",
    "datePicker.standardDateCell.focus.borderWidth": "1px",
    "datePicker.standardDateCell.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "datePicker.standardDateCell.focus.haloWidth": "2px",
    "datePicker.standardDateCell.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "datePicker.selectedDateCell.default.backgroundColor": "#0696d7",
    "datePicker.selectedDateCell.focus.backgroundColor": "#0696d7",
    "datePicker.selectedDateCell.focus.haloColor": "rgba(60, 60, 60, 0.25)",
    "datePicker.selectedDateCell.focus.haloWidth": "2px",
    "datePicker.rangeDateCell.default.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "datePicker.rangeDateCell.hover.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "datePicker.rangeDateCell.focus.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "datePicker.rangeDateCell.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "datePicker.rangeDateCell.focus.haloWidth": "2px",
    "datePicker.rangeDateCell.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "datePicker.currentDateCell.default.backgroundColor": "#ffffff",
    "datePicker.currentDateCell.default.borderColor": "#0696d7",
    "datePicker.currentDateCell.default.borderWidth": "1px",
    "datePicker.currentDateCell.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "datePicker.currentDateCell.hover.borderColor": "#0696d7",
    "datePicker.currentDateCell.hover.borderWidth": "1px",
    "datePicker.currentDateCell.focus.backgroundColor": "#ffffff",
    "datePicker.currentDateCell.focus.borderColor": "#0696d7",
    "datePicker.currentDateCell.focus.borderWidth": "1px",
    "datePicker.currentDateCell.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "datePicker.currentDateCell.focus.haloWidth": "2px",
    "datePicker.currentDateCell.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "datePicker.currentDateCell.pressed.borderColor": "#0696d7",
    "datePicker.currentDateCell.pressed.borderWidth": "1px",
    "divider.borderWidth": "1px",
    "divider.lightColor": "rgba(60, 60, 60, 0.1)",
    "divider.heavyColor": "rgba(60, 60, 60, 0.25)",
    "divider.width": "1px",
    "flyout.backgroundColor": "#ffffff",
    "flyout.borderColor": "rgba(128, 128, 128, 0)",
    "flyout.borderRadius": "4px",
    "flyout.shadowBlur": "16px",
    "flyout.shadowColor": "rgba(0, 0, 0, 0.2)",
    "flyout.nub.hypotenuseLength": "16px",
    "formField.instructionalText.fontSize": "14px",
    "formField.instructionalText.fontWeight": 600,
    "formField.instructionalText.fontFamily": "ArtifaktElement, sans-serif",
    "formField.instructionalText.fontColor": "rgba(60, 60, 60, 0.4)",
    "formField.instructionalText.lineHeight": "1.4",
    "formField.icon.width": "24px",
    "formField.icon.paddingRight": "12px",
    "formField.icon.color": "rgba(102, 102, 102, 0.7)",
    "iconButton.borderRadius": "2px",
    "iconButton.borderWidth": "0",
    "iconButton.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "iconButton.focus.haloWidth": "2px",
    "iconButton.dynamic.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.dynamic.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.dynamic.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.dynamic.default.iconColor": "#808080",
    "iconButton.dynamic.hover.iconColor": "#3c3c3c",
    "iconButton.dynamic.focus.iconColor": "#808080",
    "iconButton.dynamic.pressed.iconColor": "#3c3c3c",
    "iconButton.dynamic.on.default.iconColor": "#0696d7",
    "iconButton.dynamic.on.hover.iconColor": "#006eaf",
    "iconButton.dynamic.on.focus.iconColor": "#0696d7",
    "iconButton.dynamic.on.pressed.iconColor": "#006eaf",
    "iconButton.dynamic.pressed.100To250BackgroundColor": "rgba(128, 128, 128, 0.2)",
    "iconButton.dynamic.pressed.300To350BackgroundColor": "rgba(255, 255, 255, 0.35)",
    "iconButton.static.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.default.borderColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.hover.borderColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.focus.borderColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.pressed.borderColor": "rgba(128, 128, 128, 0)",
    "iconButton.static.on.default.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "iconButton.static.on.hover.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "iconButton.static.on.focus.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "iconButton.static.on.pressed.backgroundColor": "rgba(106, 192, 231, 0.7)",
    "iconButton.static.on.default.borderColor": "rgba(6, 150, 215, 0.5)",
    "iconButton.static.on.hover.borderColor": "rgba(6, 150, 215, 0.5)",
    "iconButton.static.on.focus.borderColor": "#0696d7",
    "iconButton.static.on.pressed.borderColor": "rgba(6, 150, 215, 0.5)",
    "iconButton.static.hover.100To250BackgroundColor": "rgba(128, 128, 128, 0.1)",
    "iconButton.static.pressed.100To250BackgroundColor": "rgba(128, 128, 128, 0.2)",
    "iconButton.static.hover.300To350BackgroundColor": "rgba(255, 255, 255, 0.25)",
    "iconButton.static.pressed.300To350BackgroundColor": "rgba(255, 255, 255, 0.35)",
    "iconButton.static.on.hover.100To250BackgroundColor": "rgba(6, 150, 215, 0.35)",
    "iconButton.static.on.hover.300To350BackgroundColor": "rgba(6, 150, 215, 0.35)",
    "iconButton.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "iconButton.focus.halo.width": "2px",
    "iconButton.focus.iconColor": "#808080",
    "iconButton.focus.on.iconColor": "#0696d7",
    "iconButton.hover.iconColor": "#3c3c3c",
    "iconButton.hover.on.iconColor": "#006eaf",
    "iconButton.iconColor": "#808080",
    "iconButton.on.iconColor": "#0696d7",
    "iconButton.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "iconButton.pressed.iconColor": "#3c3c3c",
    "iconButton.pressed.level100To250.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "iconButton.pressed.level300To350.backgroundColor": "rgba(255, 255, 255, 0.35)",
    "iconButton.pressed.on.iconColor": "#006eaf",
    "iconButton.static.hover.level100To250.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "iconButton.static.hover.level300To350.backgroundColor": "rgba(255, 255, 255, 0.25)",
    "iconButton.static.on.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "iconButton.static.on.borderColor": "rgba(6, 150, 215, 0.5)",
    "iconButton.static.on.hover.level100To250.backgroundColor": "rgba(6, 150, 215, 0.35)",
    "iconButton.static.on.hover.level300To350.backgroundColor": "rgba(6, 150, 215, 0.35)",
    "input.borderWidth": "1px",
    "input.borderBottomWidth": "1px",
    "input.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.borderRadius": "0",
    "input.haloWidth": "4px",
    "input.minHeight": "36px",
    "input.paddingVertical": "4px",
    "input.gutterWidth": "8px",
    "input.disabled.opacity": 0.4,
    "input.error.borderBottomColor": "#ec4a41",
    "input.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "input.focus.haloColor": "rgba(6, 150, 215, 0.25)",
    "input.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "input.value.fontColor": "#3c3c3c",
    "input.value.fontFamily": "ArtifaktElement, sans-serif",
    "input.value.fontSize": "14px",
    "input.value.fontWeight": 600,
    "input.value.highlightColor": "rgba(128, 128, 128, 0.2)",
    "input.value.lineHeight": "1.4",
    "input.value.placeholderFontColor": "rgba(60, 60, 60, 0.4)",
    "input.indicator.default": "rgba(102, 102, 102, 0.7)",
    "input.indicator.focus": "rgba(102, 102, 102, 0.7)",
    "input.indicator.hover": "#666666",
    "input.indicator.pressed": "#0696d7",
    "input.line.paddingHorizontal": "0",
    "input.line.default.borderBottomColor": "rgba(128, 128, 128, 0.35)",
    "input.line.hover.borderBottomColor": "#808080",
    "input.line.focus.borderBottomColor": "#0696d7",
    "input.line.pressed.borderBottomColor": "rgba(128, 128, 128, 0.85)",
    "input.box.paddingHorizontal": "12px",
    "input.box.default.backgroundColor": "#ffffff",
    "input.box.hover.backgroundColor": "#ffffff",
    "input.box.focus.backgroundColor": "#ffffff",
    "input.box.pressed.backgroundColor": "#ffffff",
    "input.box.default.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.box.hover.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.box.focus.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.box.pressed.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.box.default.borderBottomColor": "rgba(255, 255, 255, 0.2)",
    "input.box.hover.borderBottomColor": "#808080",
    "input.box.focus.borderBottomColor": "#0696d7",
    "input.box.pressed.borderBottomColor": "rgba(128, 128, 128, 0.85)",
    "input.textArea.line.borderColor": "rgba(128, 128, 128, 0.2)",
    "input.active.halo.color": "rgba(6, 150, 215, 0.25)",
    "input.borderBottomColor": "rgba(128, 128, 128, 0.35)",
    "input.active.halo.width": "4px",
    "input.box.backgroundColor": "#ffffff",
    "input.box.borderBottomColor": "rgba(255, 255, 255, 0.2)",
    "input.boxType.horizontalPadding": "12px",
    "input.focus.borderBottomColor": "#0696d7",
    "input.focus.halo.color": "rgba(6, 150, 215, 0.25)",
    "input.focus.halo.width": "4px",
    "input.fontColor": "#3c3c3c",
    "input.fontFamily": "ArtifaktElement, sans-serif",
    "input.fontSize": "14px",
    "input.fontWeight": 600,
    "input.halo.color": "rgba(128, 128, 128, 0.15)",
    "input.halo.width": "4px",
    "input.height": "36px",
    "input.highlightColor": "rgba(128, 128, 128, 0.2)",
    "input.horizontalPadding": "0",
    "input.hover.borderBottomColor": "#808080",
    "input.hover.halo.width": "4px",
    "input.lineHeight": "1.4",
    "input.placeholder.fontColor": "rgba(60, 60, 60, 0.4)",
    "input.verticalPadding": "4px",
    "label.fontFamily": "ArtifaktElement, sans-serif",
    "label.fontSize": "14px",
    "label.fontColor": "rgba(60, 60, 60, 0.9)",
    "label.hover.fontColor": "rgba(60, 60, 60, 0.9)",
    "label.fontWeight": 400,
    "label.lineHeight": "1.4",
    "label.top.fontSize": "12px",
    "menu.backgroundColor": "#ffffff",
    "menu.borderRadius": "0",
    "menu.topFlushBorderTopRadius": "0",
    "menu.item.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "menu.item.paddingHorizontal": "8px",
    "menu.item.paddingVertical": "12px",
    "menu.item.minHeight": "24px",
    "menu.item.gutterWidth": "12px",
    "menu.item.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "menu.item.focus.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "menu.item.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "menu.item.backgroundColor": "rgba(128, 128, 128, 0)",
    "menu.item.active.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "menu.item.horizontalPadding": "8px",
    "menu.item.verticalPadding": "12px",
    "modal.fontColor": "#3c3c3c",
    "modal.paddingHorizontal": "24px",
    "modal.overlayColor": "rgba(0, 0, 0, 0.5)",
    "modal.header.backgroundColor": "#ffffff",
    "modal.header.borderBottomColor": "rgba(60, 60, 60, 0.25)",
    "modal.header.minHeight": "60px",
    "modal.body.minHeight": "152px",
    "modal.shell.backgroundColor": "#ffffff",
    "modal.shell.borderRadius": "4px",
    "modal.shell.minHeight": "300px",
    "modal.shell.minWidth": "400px",
    "modal.shell.shadowColor": "rgba(0, 0, 0, 0.2)",
    "modal.overlay.color": "rgba(0, 0, 0, 0.5)",
    "modal.textColor": "#3c3c3c",
    "modal.body.height": "152px",
    "modal.horizontalPadding": "24px",
    "modal.header.borderColor": "rgba(60, 60, 60, 0.25)",
    "modal.header.height": "60px",
    "modal.window.backgroundColor": "#ffffff",
    "modal.window.borderRadius": "4px",
    "modal.window.height": "300px",
    "modal.window.width": "400px",
    "modal.window.shadowColor": "rgba(0, 0, 0, 0.2)",
    "notifications.borderRadius": "4px",
    "notifications.fontColor": "#3c3c3c",
    "notifications.fontFamily": "ArtifaktElement, sans-serif",
    "notifications.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "notifications.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "notifications.container.backgroundColor": "#ffffff",
    "notifications.container.borderColor": "rgba(128, 128, 128, 0)",
    "notifications.container.shadowBlur": "4px",
    "notifications.container.shadowColor": "rgba(0, 0, 0, 0.2)",
    "notifications.statusIndicator.width": "3px",
    "notifications.header.fontSize": "14px",
    "notifications.header.fontWeight": 700,
    "notifications.header.lineHeight": "20px",
    "notifications.toast.object.fontSize": "12px",
    "notifications.toast.object.fontWeight": 700,
    "notifications.toast.object.lineHeight": "16px",
    "notifications.toast.description.fontSize": "12px",
    "notifications.toast.description.fontWeight": 400,
    "notifications.toast.description.lineHeight": "16px",
    "progress.bar.backgroundColor": "rgba(0, 0, 0, 0.05)",
    "progress.bar.highlightColor": "#38abdf",
    "progress.bar.borderRadius": "2px",
    "progress.bar.minHeight": "4px",
    "progressBar.backgroundColor": "rgba(0, 0, 0, 0.05)",
    "progressBar.highlightColor": "#38abdf",
    "progressBar.borderRadius": "2px",
    "progressBar.height": "4px",
    "progress.ring.backgroundColor": "rgba(0, 0, 0, 0.05)",
    "progress.ring.highlightColor1": "#0686c2",
    "progress.ring.highlightColor2": "#38abdf",
    "progressRing.backgroundColor": "rgba(0, 0, 0, 0.05)",
    "progressRing.highlightColor1": "#0686c2",
    "progressRing.highlightColor2": "#38abdf",
    "scrollbar.radii": "1000em",
    "scrollbar.height": "5px",
    "scrollbar.default.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "scrollbar.pressed.backgroundColor": "rgba(128, 128, 128, 0.5)",
    "segmentedButton.borderRadius": "2px",
    "segmentedButton.borderWidth": "1px",
    "segmentedButton.gutterWidth": "4px",
    "segmentedButton.minWidth": "10px",
    "segmentedButton.paddingHorizontal": "16px",
    "segmentedButton.paddingVertical": "8px",
    "segmentedButton.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "segmentedButton.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "segmentedButton.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "segmentedButton.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "segmentedButton.default.borderColor": "rgba(128, 128, 128, 0.5)",
    "segmentedButton.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "segmentedButton.focus.borderColor": "#0696d7",
    "segmentedButton.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "segmentedButton.hover.haloWidth": "2px",
    "segmentedButton.focus.haloWidth": "2px",
    "segmentedButton.pressed.haloWidth": "4px",
    "segmentedButton.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "segmentedButton.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "segmentedButton.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "segmentedButton.default.iconColor": "#808080",
    "segmentedButton.active.iconColor": "#eeeeee",
    "segmentedButton.active.backgroundColor": "#808080",
    "segmentedButton.label.fontSize": "14px",
    "segmentedButton.label.default.fontColor": "#3c3c3c",
    "segmentedButton.label.default.fontWeight": 600,
    "segmentedButton.label.active.fontColor": "#f5f5f5",
    "segmentedButton.label.active.fontWeight": 700,
    "sideNav.fontFamily": "ArtifaktElement, sans-serif",
    "sideNav.container.backgroundColor": "#ffffff",
    "sideNav.account.fontColor": "#3c3c3c",
    "sideNav.account.fontSize": "12px",
    "sideNav.account.fontWeight": 500,
    "sideNav.account.lineHeight": "1.428571429",
    "sideNav.project.fontColor": "#3c3c3c",
    "sideNav.project.fontSize": "14px",
    "sideNav.project.fontWeight": 700,
    "sideNav.project.lineHeight": "1.428571429",
    "sideNav.module.default.fontColor": "#3c3c3c",
    "sideNav.module.default.fontSize": "14px",
    "sideNav.module.default.fontWeight": 700,
    "sideNav.module.default.lineHeight": "1.428571429",
    "sideNav.module.active.fontColor": "#0696d7",
    "sideNav.module.active.fontSize": "14px",
    "sideNav.module.active.fontWeight": 700,
    "sideNav.module.active.lineHeight": "1.428571429",
    "sideNav.subModule.default.fontColor": "#3c3c3c",
    "sideNav.subModule.default.fontSize": "14px",
    "sideNav.subModule.default.fontWeight": 400,
    "sideNav.subModule.default.lineHeight": "1.428571429",
    "sideNav.subModule.active.fontColor": "#0696d7",
    "sideNav.subModule.active.fontSize": "14px",
    "sideNav.subModule.active.fontWeight": 400,
    "sideNav.subModule.active.lineHeight": "1.428571429",
    "sideNav.legal.fontColor": "#3c3c3c",
    "sideNav.legal.fontSize": "12px",
    "sideNav.legal.fontWeight": 500,
    "sideNav.legal.lineHeight": "1.5",
    "sideNav.icon.defaultColor": "#808080",
    "sideNav.icon.activeColor": "#0696d7",
    "sideNav.activeIndicator.backgroundColor": "#0696d7",
    "skeletonItem.borderRadius": "2px",
    "skeletonItem.minHeight": "16px",
    "skeletonItem.backgroundColor": "rgba(0, 0, 0, 0.06)",
    "skeletonItem.highlightColor": "rgba(255, 255, 255, 0.3)",
    "skeletonItem.defaultHeight": "16px",
    "slider.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "slider.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "slider.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "slider.hover.haloWidth": "2px",
    "slider.focus.haloWidth": "2px",
    "slider.pressed.haloWidth": "4px",
    "slider.thumb.backgroundColor": "#808080",
    "slider.thumb.minWidth": "6px",
    "slider.thumb.minHeight": "20px",
    "slider.track.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "slider.track.minHeight": "2px",
    "slider.value.backgroundColor": "#808080",
    "slider.inputOverlay.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "slider.halo.width": 0,
    "slider.halo.color": "rgba(128, 128, 128, 0.15)",
    "slider.thumb.width": "6px",
    "slider.focused.halo.color": "rgba(6, 150, 215, 0.35)",
    "slider.focused.halo.width": "2px",
    "slider.focused.thumb.color": "#808080",
    "slider.hover.halo.color": "rgba(128, 128, 128, 0.15)",
    "slider.hover.halo.width": "2px",
    "slider.hover.thumb.color": "#808080",
    "slider.pressed.halo.color": "rgba(128, 128, 128, 0.25)",
    "slider.pressed.halo.width": "4px",
    "slider.pressed.thumb.color": "#808080",
    "slider.track.color": "rgba(128, 128, 128, 0.2)",
    "slider.track.width": "2px",
    "slider.value.color": "#808080",
    "stepIndicator.fontFamily": "ArtifaktElement, sans-serif",
    "stepIndicator.borderWidth": "2px",
    "stepIndicator.minDiameter": "22px",
    "stepIndicator.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "stepIndicator.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "stepIndicator.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "stepIndicator.hover.haloWidth": "2px",
    "stepIndicator.focus.haloWidth": "2px",
    "stepIndicator.pressed.haloWidth": "4px",
    "stepIndicator.incomplete.backgroundColor": "rgba(128, 128, 128, 0)",
    "stepIndicator.incomplete.borderColor": "#dcdcdc",
    "stepIndicator.inProgress.backgroundColor": "rgba(128, 128, 128, 0)",
    "stepIndicator.inProgress.borderColor": "#0696d7",
    "stepIndicator.completed.backgroundColor": "#0696d7",
    "stepIndicator.completed.iconColor": "#ffffff",
    "stepIndicator.accordion.paddingHorizontal": "16px",
    "stepIndicator.accordion.paddingVertical": "24px",
    "stepIndicator.connector.minWidth": "2px",
    "stepIndicator.connector.default.backgroundColor": "#dcdcdc",
    "stepIndicator.connector.active.backgroundColor": "#0696d7",
    "stepIndicator.label.fontColor": "#3c3c3c",
    "stepIndicator.label.default.fontWeight": 400,
    "stepIndicator.label.horizontal.fontSize": "12px",
    "stepIndicator.label.horizontal.marginBottom": "12px",
    "stepIndicator.label.horizontal.marginTop": "12px",
    "stepIndicator.label.vertical.fontSize": "14px",
    "stepIndicator.label.vertical.marginLeft": "12px",
    "stepIndicator.label.inProgress.fontWeight": 700,
    "stepIndicator.number.fontSize": "14px",
    "stepIndicator.number.inProgress.fontColor": "#0696d7",
    "stepIndicator.number.inProgress.fontWeight": 700,
    "stepIndicator.number.incomplete.fontColor": "rgba(60, 60, 60, 0.4)",
    "stepIndicator.number.incomplete.fontWeight": 600,
    "table.fontColor": "#3c3c3c",
    "table.fontFamily": "ArtifaktElement, sans-serif",
    "table.fonSize": "14px",
    "table.lineHeight": "1.428571429",
    "table.title.fontWeight": 700,
    "table.item.fontWeight": 400,
    "table.row.borderBottomColor": "rgba(60, 60, 60, 0.1)",
    "table.row.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "table.row.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "table.row.selected.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "table.header.borderTopColor": "rgba(60, 60, 60, 0.1)",
    "table.header.borderBottomColor": "rgba(60, 60, 60, 0.25)",
    "tabs.focus.haloWidth": "2px",
    "tabs.focus.haloColor": "rgba(6, 150, 215, 0.5)",
    "tabs.label.fontColor": "#3c3c3c",
    "tabs.label.fontFamily": "ArtifaktElement, sans-serif",
    "tabs.label.fontSize": "14px",
    "tabs.label.inactive.fontWeight": 400,
    "tabs.label.active.fontWeight": 700,
    "tabs.label.active.lineHeight": "1.428571429",
    "tabs.iconSize": "24px",
    "tabs.iconColor": "#808080",
    "tabs.iconGutter": "12px",
    "tabs.closeButton.minSize": "16px",
    "tabs.closeButton.gutter": "12px",
    "tabs.closeButton.default.iconColor": "#808080",
    "tabs.closeButton.hover.iconColor": "#3c3c3c",
    "tabs.closeButton.pressed.iconColor": "#3c3c3c",
    "tabs.underline.wrapper.borderBottomColor": "rgba(128, 128, 128, 0.35)",
    "tabs.underline.wrapper.borderBottomWidth": "1px",
    "tabs.underline.tab.gutter": "24px",
    "tabs.underline.tab.paddingBottom": "4px",
    "tabs.underline.hover.haloColor": "rgba(128, 128, 128, 0.85)",
    "tabs.underline.active.haloColor": "#0696d7",
    "tabs.box.wrapper.backgroundColor": "rgba(128, 128, 128, 0)",
    "tabs.box.tab.paddingHorizontal": "12px",
    "tabs.box.tab.paddingVertical": "8px",
    "tabs.box.tab.active.backgroundColor": "#ffffff",
    "tabs.box.tab.hover.backgroundColor": "rgba(255, 255, 255, 0.5)",
    "tabs.box.divider.borderColor": "rgba(60, 60, 60, 0.25)",
    "tabs.box.dividerWidth": "1px",
    "tabs.box.dividerHeight": "18px",
    "tabs.canvas.wrapper.backgroundColor": "rgba(128, 128, 128, 0)",
    "tabs.canvas.tab.paddingHorizontal": "16px",
    "tabs.canvas.tab.paddingVertical": "8px",
    "tabs.canvas.tab.hover.backgroundColor": "rgba(245, 245, 245, 0.5)",
    "tabs.canvas.tab.active.backgroundColor": "#f5f5f5",
    "tabs.canvas.divider.borderColor": "rgba(60, 60, 60, 0.25)",
    "tabs.canvas.dividerWidth": "1px",
    "tabs.canvas.dividerHeight": "18px",
    "tabs.general.tab.fontFamily": "ArtifaktElement, sans-serif",
    "tabs.general.tab.fontSize": "14px",
    "tabs.general.tab.fontWeight": 400,
    "tabs.general.tab.active.fontWeight": 700,
    "tabs.general.tab.color": "#3c3c3c",
    "tabs.general.tab.lineHeight": "1.428571429",
    "tabs.general.halo.size": "2px",
    "tabs.general.halo.color": "rgba(6, 150, 215, 0.5)",
    "tabs.general.icon.size": "24px",
    "tabs.general.icon.color": "#808080",
    "tabs.general.icon.gutter": "12px",
    "tabs.general.closeButton.size": "16px",
    "tabs.general.closeButton.gutter": "12px",
    "tabs.general.closeButton.color": "#808080",
    "tabs.general.closeButton.hover.color": "#3c3c3c",
    "tabs.general.closeButton.pressed.color": "#3c3c3c",
    "tabs.underline.halo.hover.color": "rgba(128, 128, 128, 0.85)",
    "tabs.underline.halo.active.color": "#0696d7",
    "tabs.box.tab.horizontalPadding": "12px",
    "tabs.box.tab.verticalPadding": "8px",
    "tabs.box.divider.color": "rgba(60, 60, 60, 0.25)",
    "tabs.box.divider.width": "1px",
    "tabs.box.divider.height": "18px",
    "tabs.canvas.tab.horizontalPadding": "16px",
    "tabs.canvas.tab.verticalPadding": "8px",
    "tabs.canvas.divider.color": "rgba(60, 60, 60, 0.25)",
    "tabs.canvas.divider.width": "1px",
    "tabs.canvas.divider.height": "18px",
    "tag.borderWidth": "1px",
    "tag.borderRadii": "1000em",
    "tag.hover.haloWidth": "2px",
    "tag.focus.haloWidth": "4px",
    "tag.paddingLeft": "12px",
    "tag.paddingRight": "8px",
    "tag.paddingVertical": "8px",
    "tag.default.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "tag.hover.backgroundColor": "rgba(128, 128, 128, 0.25)",
    "tag.focus.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "tag.pressed.backgroundColor": "rgba(128, 128, 128, 0.35)",
    "tag.default.borderColor": "rgba(128, 128, 128, 0)",
    "tag.hover.borderColor": "rgba(128, 128, 128, 0)",
    "tag.focus.borderColor": "rgba(128, 128, 128, 0)",
    "tag.pressed.borderColor": "rgba(128, 128, 128, 0)",
    "tag.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "tag.on.default.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "tag.on.hover.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "tag.on.focus.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "tag.on.pressed.backgroundColor": "rgba(128, 128, 128, 0.7)",
    "tag.on.default.borderColor": "rgba(6, 150, 215, 0.5)",
    "tag.on.hover.borderColor": "rgba(6, 150, 215, 0.5)",
    "tag.on.focus.borderColor": "#0696d7",
    "tag.on.pressed.borderColor": "rgba(6, 150, 215, 0.5)",
    "tag.on.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "tag.label.marginRight": "8px",
    "tag.label.fontColor": "#3c3c3c",
    "tag.label.fontFamily": "ArtifaktElement, sans-serif",
    "tag.label.fontSize": "14px",
    "tag.label.fontWeight": 600,
    "tag.closeButton.iconColor": "#808080",
    "textArea.minHeight": "250px",
    "textArea.borderColor": "rgba(128, 128, 128, 0.2)",
    "textLink.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "textLink.focus.haloWidth": "2px",
    "textLink.primary.default.fontColor": "#006eaf",
    "textLink.primary.hover.fontColor": "#006eaf",
    "textLink.primary.focus.fontColor": "#006eaf",
    "textLink.primary.hover.underlineColor": "#006eaf",
    "textLink.secondary.default.fontColor": "#3c3c3c",
    "textLink.secondary.hover.fontColor": "#3c3c3c",
    "textLink.secondary.focus.fontColor": "#3c3c3c",
    "textLink.secondary.hover.underlineColor": "#3c3c3c",
    "textLink.textColor": "#006eaf",
    "textLink.hover.textColor": "#006eaf",
    "textLink.focus.textColor": "#006eaf",
    "textLink.hover.underline.color": "#006eaf",
    "textLink.focus.halo.color": "rgba(6, 150, 215, 0.35)",
    "textLink.focus.halo.width": "2px",
    "tile.borderRadii": "0",
    "tile.borderWidth": "1px",
    "tile.haloWidth": "4px",
    "tile.padding": "12px",
    "tile.fontColor": "#3c3c3c",
    "tile.fontFamily": "ArtifaktElement, sans-serif",
    "tile.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "tile.thumbnail.marginRight": "12px",
    "tile.title.fontSize": "14px",
    "tile.title.fontWeight": 600,
    "tile.title.lineHeight": "1.333333333",
    "tile.title.marginBottom": "8px",
    "tile.subTitle.fontSize": "12px",
    "tile.subTitle.fontWeight": 400,
    "tile.subTitle.lineHeight": "1.272727273",
    "tile.default.level300To350BackgroundColor": "rgba(255, 255, 255, 0.5)",
    "tile.hover.level300To350BackgroundColor": "rgba(255, 255, 255, 0.7)",
    "tile.focus.level300To350BackgroundColor": "rgba(255, 255, 255, 0.5)",
    "tile.pressed.level300To350BackgroundColor": "rgba(255, 255, 255, 0.9)",
    "tile.selected.default.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "tile.selected.hover.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "tile.selected.focus.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "tile.selected.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "tile.selected.default.borderColor": "rgba(6, 150, 215, 0.5)",
    "tile.selected.hover.borderColor": "rgba(6, 150, 215, 0.5)",
    "tile.selected.focus.borderColor": "#0696d7",
    "tile.selected.pressed.borderColor": "rgba(6, 150, 215, 0.5)",
    "tile.filled.default.level100To250.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "tile.filled.hover.level100To250.backgroundColor": "rgba(128, 128, 128, 0.25)",
    "tile.filled.focus.level100To250.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "tile.filled.pressed.level100To250.backgroundColor": "rgba(128, 128, 128, 0.35)",
    "tile.filled.default.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.hover.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.focus.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.pressed.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.default.level300To350.backgroundColor": "rgba(255, 255, 255, 0.5)",
    "tile.filled.hover.level300To350.backgroundColor": "rgba(255, 255, 255, 0.7)",
    "tile.filled.focus.level300To350.backgroundColor": "rgba(255, 255, 255, 0.5)",
    "tile.filled.pressed.level300To350.backgroundColor": "#ffffff",
    "tile.filled.default.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.hover.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.focus.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.filled.pressed.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.default.level100To250.backgroundColor": "rgba(128, 128, 128, 0)",
    "tile.empty.hover.level100To250.backgroundColor": "rgba(128, 128, 128, 0.1)",
    "tile.empty.focus.level100To250.backgroundColor": "rgba(128, 128, 128, 0)",
    "tile.empty.pressed.level100To250.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "tile.empty.default.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.hover.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.focus.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.pressed.level100To250.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.default.level300To350.backgroundColor": "rgba(128, 128, 128, 0)",
    "tile.empty.hover.level300To350.backgroundColor": "rgba(255, 255, 255, 0.25)",
    "tile.empty.focus.level300To350.backgroundColor": "rgba(128, 128, 128, 0)",
    "tile.empty.pressed.level300To350.backgroundColor": "rgba(255, 255, 255, 0.35)",
    "tile.empty.default.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.hover.level300To350.borderColor": "rgba(255, 255, 255, 0.25)",
    "tile.empty.focus.level300To350.borderColor": "rgba(128, 128, 128, 0)",
    "tile.empty.pressed.level300To350.borderColor": "rgba(255, 255, 255, 0.35)",
    "timestamp.fontColor": "rgba(60, 60, 60, 0.7)",
    "timestamp.fontFamily": "ArtifaktElement, sans-serif",
    "timestamp.fontSize": "12px",
    "timestamp.fontWeight": 400,
    "timestamp.lineHeight": "20px",
    "thumbnail.backgroundColor": "#eeeeee",
    "thumbnail.borderColor": "rgba(128, 128, 128, 0.5)",
    "toggle.borderRadii": "1000em",
    "toggle.borderWidth": "1px",
    "toggle.minHeight": "20px",
    "toggle.minWidth": "37px",
    "toggle.padding": "4px",
    "toggle.hover.haloColor": "rgba(128, 128, 128, 0.15)",
    "toggle.focus.haloColor": "rgba(6, 150, 215, 0.35)",
    "toggle.pressed.haloColor": "rgba(128, 128, 128, 0.25)",
    "toggle.hover.haloWidth": "2px",
    "toggle.focus.haloWidth": "2px",
    "toggle.pressed.haloWidth": "4px",
    "toggle.off.default.backgroundColor": "rgba(128, 128, 128, 0)",
    "toggle.off.default.borderColor": "rgba(128, 128, 128, 0.5)",
    "toggle.off.hover.backgroundColor": "rgba(128, 128, 128, 0)",
    "toggle.off.focus.backgroundColor": "rgba(128, 128, 128, 0)",
    "toggle.off.pressed.backgroundColor": "rgba(128, 128, 128, 0)",
    "toggle.off.hover.borderColor": "rgba(128, 128, 128, 0.5)",
    "toggle.off.focus.borderColor": "#0696d7",
    "toggle.off.pressed.borderColor": "rgba(128, 128, 128, 0.5)",
    "toggle.on.default.backgroundColor": "#0696d7",
    "toggle.on.default.borderColor": "#0696d7",
    "toggle.thumb.borderRadii": "50%",
    "toggle.thumb.minDiameter": "12px",
    "toggle.thumb.on.backgroundColor": "#ffffff",
    "toggle.thumb.off.backgroundColor": "#808080",
    "token.borderRadii": "0",
    "token.paddingLeft": "8px",
    "token.paddingRight": "4px",
    "token.paddingVertical": "4px",
    "token.default.backgroundColor": "rgba(128, 128, 128, 0.15)",
    "token.hover.backgroundColor": "rgba(128, 128, 128, 0.25)",
    "token.pressed.backgroundColor": "rgba(128, 128, 128, 0.35)",
    "token.default.borderColor": "rgba(128, 128, 128, 0)",
    "token.hover.borderColor": "rgba(128, 128, 128, 0)",
    "token.pressed.borderColor": "rgba(128, 128, 128, 0)",
    "token.selected.default.backgroundColor": "rgba(205, 234, 247, 0.6)",
    "token.selected.hover.backgroundColor": "rgba(155, 213, 239, 0.7)",
    "token.selected.pressed.backgroundColor": "rgba(128, 128, 128, 0.2)",
    "token.selected.default.borderColor": "rgba(6, 150, 215, 0.5)",
    "token.selected.hover.borderColor": "rgba(6, 150, 215, 0.5)",
    "token.selected.pressed.borderColor": "rgba(6, 150, 215, 0.5)",
    "token.warning.default.backgroundColor": "rgba(250, 162, 27, 0.15)",
    "token.warning.hover.backgroundColor": "rgba(250, 162, 27, 0.3)",
    "token.warning.iconColor": "#faa21b",
    "token.icon.marginRight": "8px",
    "token.label.fontColor": "#3c3c3c",
    "token.label.fontFamily": "ArtifaktElement, sans-serif",
    "token.label.fontSize": "14px",
    "token.label.fontWeight": 600,
    "token.label.marginRight": "4px",
    "token.closeButton.iconColor": "#808080",
    "tooltip.backgroundColor": "#535353",
    "tooltip.borderRadius": "2px",
    "tooltip.shadowBlur": "16px",
    "tooltip.shadowColor": "rgba(0, 0, 0, 0.2)",
    "tooltip.title.fontColor": "#f5f5f5",
    "tooltip.title.fontFamily": "ArtifaktElement, sans-serif",
    "tooltip.title.fontSize": "12px",
    "tooltip.title.fontWeight": 700,
    "tooltip.title.lineHeight": 1.333333333,
    "tooltip.nub.hypotenuseLength": "16px",
    "tooltip.description.fontColor": "#F5F5F5",
    "tooltip.description.fontFamily": "ArtifaktElement, sans-serif",
    "tooltip.description.fontSize": "12px",
    "tooltip.description.fontWeight": 400,
    "tooltip.description.lineHeight": 1.333333333,
    "tooltip.divider.borderBottomColor": "rgba(255, 255, 255, 0.1)",
    "tooltip.textColor": "#f5f5f5",
    "tooltip.borderColor": "#535353",
    "topNav.padding": "12px",
    "topNav.backgroundColor": "#ffffff",
    "topNav.borderColor": "rgba(60, 60, 60, 0.1)",
    "topNav.productLockupColor": "#000000",
    "topNav.hamburgetIcon.margin": "8px",
    "treeView.fontColor": "#3c3c3c",
    "treeView.fontFamily": "ArtifaktElement, sans-serif",
    "treeView.indicatorColor": "#3c3c3c",
    "treeView.icon.marginRight": "8px",
    "treeView.row.paddingHorizontal": "12px",
    "treeView.row.paddingVertical": "8px",
    "treeView.title.fontSize": "14px",
    "treeView.title.fontWeight": 600,
    "treeView.title.lineHeight": "1.428571429",
    "treeView.item.fontSize": "14px",
    "treeView.item.fontWeight": 600,
    "treeView.item.lineHeight": "1.428571429",
    "treeView.guideLine.backgroundColor": "rgba(128, 128, 128, 0.5)",
    "typography.body.fontFamily": "ArtifaktElement, sans-serif",
    "typography.body.fontWeight": 400,
    "typography.body.fontSize": "14px",
    "typography.body.lineHeight": "1.428571429",
    "typography.body.color": "#3c3c3c",
    "typography.caption.fontFamily": "ArtifaktElement, sans-serif",
    "typography.caption.fontWeight": 400,
    "typography.caption.fontSize": "12px",
    "typography.caption.lineHeight": "1.5",
    "typography.caption.color": "#3c3c3c",
    "typography.h1.fontFamily": "ArtifaktElement, sans-serif",
    "typography.h1.fontWeight": 400,
    "typography.h1.fontSize": "28px",
    "typography.h1.lineHeight": "1.285714286",
    "typography.h1.color": "#3c3c3c",
    "typography.h2.fontFamily": "ArtifaktElement, sans-serif",
    "typography.h2.fontWeight": 400,
    "typography.h2.fontSize": "24px",
    "typography.h2.lineHeight": "1.25",
    "typography.h2.color": "#3c3c3c",
    "typography.h3.fontFamily": "ArtifaktElement, sans-serif",
    "typography.h3.fontWeight": 600,
    "typography.h3.fontSize": "20px",
    "typography.h3.lineHeight": "1.3",
    "typography.h3.color": "#3c3c3c"
  }
};
const { metadata, resolvedRoles } = themeData;

/* harmony default export */ __webpack_exports__["default"] = (themeData);


/***/ }),

/***/ "./node_modules/@hig/theme-data/build/json/lightGrayMediumDensityTheme/theme.json":
/*!****************************************************************************************!*\
  !*** ./node_modules/@hig/theme-data/build/json/lightGrayMediumDensityTheme/theme.json ***!
  \****************************************************************************************/
/*! exports provided: metadata, resolvedRoles, default */
/***/ (function(module) {

module.exports = JSON.parse("{\"metadata\":{\"colorSchemeName\":\"Light gray\",\"colorSchemeId\":\"hig-light-gray\",\"className\":\"hig--light-gray-theme\",\"densityId\":\"medium-density\",\"densityName\":\"Medium density\",\"name\":\"Light gray, Medium density\",\"fileName\":\"lightGrayMediumDensityTheme\",\"id\":\"hig-light-gray-medium-density\",\"__deprecated__\":{\"basics.colors.black\":{\"equivalent\":\"basics.colors.primary.black\"},\"basics.colors.white\":{\"equivalent\":\"basics.colors.primary.white\"},\"basics.colors.autodeskBlue100\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.100\"},\"basics.colors.autodeskBlue200\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.200\"},\"basics.colors.autodeskBlue300\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.300\"},\"basics.colors.autodeskBlue400\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.400\"},\"basics.colors.autodeskBlue500\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.500\"},\"basics.colors.autodeskBlue600\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.600\"},\"basics.colors.autodeskBlue700\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.700\"},\"basics.colors.autodeskBlue800\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.800\"},\"basics.colors.autodeskBlue900\":{\"equivalent\":\"basics.colors.primary.autodeskBlue.900\"},\"basics.colors.charcoal100\":{\"equivalent\":\"basics.colors.primary.charcoal.100\"},\"basics.colors.charcoal200\":{\"equivalent\":\"basics.colors.primary.charcoal.200\"},\"basics.colors.charcoal300\":{\"equivalent\":\"basics.colors.primary.charcoal.300\"},\"basics.colors.charcoal400\":{\"equivalent\":\"basics.colors.primary.charcoal.400\"},\"basics.colors.charcoal500\":{\"equivalent\":\"basics.colors.primary.charcoal.500\"},\"basics.colors.charcoal600\":{\"equivalent\":\"basics.colors.primary.charcoal.600\"},\"basics.colors.charcoal700\":{\"equivalent\":\"basics.colors.primary.charcoal.700\"},\"basics.colors.charcoal800\":{\"equivalent\":\"basics.colors.primary.charcoal.800\"},\"basics.colors.charcoal900\":{\"equivalent\":\"basics.colors.primary.charcoal.900\"},\"basics.colors.darkBlue100\":{\"equivalent\":\"basics.colors.secondary.darkBlue.100\"},\"basics.colors.darkBlue200\":{\"equivalent\":\"basics.colors.secondary.darkBlue.200\"},\"basics.colors.darkBlue300\":{\"equivalent\":\"basics.colors.secondary.darkBlue.300\"},\"basics.colors.darkBlue400\":{\"equivalent\":\"basics.colors.secondary.darkBlue.400\"},\"basics.colors.darkBlue500\":{\"equivalent\":\"basics.colors.secondary.darkBlue.500\"},\"basics.colors.darkBlue600\":{\"equivalent\":\"basics.colors.secondary.darkBlue.600\"},\"basics.colors.darkBlue700\":{\"equivalent\":\"basics.colors.secondary.darkBlue.700\"},\"basics.colors.darkBlue800\":{\"equivalent\":\"basics.colors.secondary.darkBlue.800\"},\"basics.colors.darkBlue900\":{\"equivalent\":\"basics.colors.secondary.darkBlue.900\"},\"basics.colors.green100\":{\"equivalent\":\"basics.colors.secondary.green.100\"},\"basics.colors.green200\":{\"equivalent\":\"basics.colors.secondary.green.200\"},\"basics.colors.green300\":{\"equivalent\":\"basics.colors.secondary.green.300\"},\"basics.colors.green400\":{\"equivalent\":\"basics.colors.secondary.green.400\"},\"basics.colors.green500\":{\"equivalent\":\"basics.colors.secondary.green.500\"},\"basics.colors.green600\":{\"equivalent\":\"basics.colors.secondary.green.600\"},\"basics.colors.green700\":{\"equivalent\":\"basics.colors.secondary.green.700\"},\"basics.colors.green800\":{\"equivalent\":\"basics.colors.secondary.green.800\"},\"basics.colors.green900\":{\"equivalent\":\"basics.colors.secondary.green.900\"},\"basics.colors.red100\":{\"equivalent\":\"basics.colors.secondary.red.100\"},\"basics.colors.red200\":{\"equivalent\":\"basics.colors.secondary.red.200\"},\"basics.colors.red300\":{\"equivalent\":\"basics.colors.secondary.red.300\"},\"basics.colors.red400\":{\"equivalent\":\"basics.colors.secondary.red.400\"},\"basics.colors.red500\":{\"equivalent\":\"basics.colors.secondary.red.500\"},\"basics.colors.red600\":{\"equivalent\":\"basics.colors.secondary.red.600\"},\"basics.colors.red700\":{\"equivalent\":\"basics.colors.secondary.red.700\"},\"basics.colors.red800\":{\"equivalent\":\"basics.colors.secondary.red.800\"},\"basics.colors.red900\":{\"equivalent\":\"basics.colors.secondary.red.900\"},\"basics.colors.turquoise100\":{\"equivalent\":\"basics.colors.secondary.turquoise.100\"},\"basics.colors.turquoise200\":{\"equivalent\":\"basics.colors.secondary.turquoise.200\"},\"basics.colors.turquoise300\":{\"equivalent\":\"basics.colors.secondary.turquoise.300\"},\"basics.colors.turquoise400\":{\"equivalent\":\"basics.colors.secondary.turquoise.400\"},\"basics.colors.turquoise500\":{\"equivalent\":\"basics.colors.secondary.turquoise.500\"},\"basics.colors.turquoise600\":{\"equivalent\":\"basics.colors.secondary.turquoise.600\"},\"basics.colors.turquoise700\":{\"equivalent\":\"basics.colors.secondary.turquoise.700\"},\"basics.colors.turquoise800\":{\"equivalent\":\"basics.colors.secondary.turquoise.800\"},\"basics.colors.turquoise900\":{\"equivalent\":\"basics.colors.secondary.turquoise.900\"},\"basics.colors.yellowOrange100\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.100\"},\"basics.colors.yellowOrange200\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.200\"},\"basics.colors.yellowOrange300\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.300\"},\"basics.colors.yellowOrange400\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.400\"},\"basics.colors.yellowOrange500\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.500\"},\"basics.colors.yellowOrange600\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.600\"},\"basics.colors.yellowOrange700\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.700\"},\"basics.colors.yellowOrange800\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.800\"},\"basics.colors.yellowOrange900\":{\"equivalent\":\"basics.colors.secondary.yellowOrange.900\"},\"basics.colors.pink100\":{\"equivalent\":\"basics.colors.tertiary.pink.100\"},\"basics.colors.pink200\":{\"equivalent\":\"basics.colors.tertiary.pink.200\"},\"basics.colors.pink300\":{\"equivalent\":\"basics.colors.tertiary.pink.300\"},\"basics.colors.pink400\":{\"equivalent\":\"basics.colors.tertiary.pink.400\"},\"basics.colors.pink500\":{\"equivalent\":\"basics.colors.tertiary.pink.500\"},\"basics.colors.pink600\":{\"equivalent\":\"basics.colors.tertiary.pink.600\"},\"basics.colors.pink700\":{\"equivalent\":\"basics.colors.tertiary.pink.700\"},\"basics.colors.pink800\":{\"equivalent\":\"basics.colors.tertiary.pink.800\"},\"basics.colors.pink900\":{\"equivalent\":\"basics.colors.tertiary.pink.900\"},\"basics.colors.purple100\":{\"equivalent\":\"basics.colors.tertiary.purple.100\"},\"basics.colors.purple200\":{\"equivalent\":\"basics.colors.tertiary.purple.200\"},\"basics.colors.purple300\":{\"equivalent\":\"basics.colors.tertiary.purple.300\"},\"basics.colors.purple400\":{\"equivalent\":\"basics.colors.tertiary.purple.400\"},\"basics.colors.purple500\":{\"equivalent\":\"basics.colors.tertiary.purple.500\"},\"basics.colors.purple600\":{\"equivalent\":\"basics.colors.tertiary.purple.600\"},\"basics.colors.purple700\":{\"equivalent\":\"basics.colors.tertiary.purple.700\"},\"basics.colors.purple800\":{\"equivalent\":\"basics.colors.tertiary.purple.800\"},\"basics.colors.purple900\":{\"equivalent\":\"basics.colors.tertiary.purple.900\"},\"basics.colors.salmon100\":{\"equivalent\":\"basics.colors.tertiary.salmon.100\"},\"basics.colors.salmon200\":{\"equivalent\":\"basics.colors.tertiary.salmon.200\"},\"basics.colors.salmon300\":{\"equivalent\":\"basics.colors.tertiary.salmon.300\"},\"basics.colors.salmon400\":{\"equivalent\":\"basics.colors.tertiary.salmon.400\"},\"basics.colors.salmon500\":{\"equivalent\":\"basics.colors.tertiary.salmon.500\"},\"basics.colors.salmon600\":{\"equivalent\":\"basics.colors.tertiary.salmon.600\"},\"basics.colors.salmon700\":{\"equivalent\":\"basics.colors.tertiary.salmon.700\"},\"basics.colors.salmon800\":{\"equivalent\":\"basics.colors.tertiary.salmon.800\"},\"basics.colors.salmon900\":{\"equivalent\":\"basics.colors.tertiary.salmon.900\"},\"basics.colors.slate100\":{\"equivalent\":\"basics.colors.tertiary.slate.100\"},\"basics.colors.slate200\":{\"equivalent\":\"basics.colors.tertiary.slate.200\"},\"basics.colors.slate300\":{\"equivalent\":\"basics.colors.tertiary.slate.300\"},\"basics.colors.slate400\":{\"equivalent\":\"basics.colors.tertiary.slate.400\"},\"basics.colors.slate500\":{\"equivalent\":\"basics.colors.tertiary.slate.500\"},\"basics.colors.slate600\":{\"equivalent\":\"basics.colors.tertiary.slate.600\"},\"basics.colors.slate700\":{\"equivalent\":\"basics.colors.tertiary.slate.700\"},\"basics.colors.slate800\":{\"equivalent\":\"basics.colors.tertiary.slate.800\"},\"basics.colors.slate900\":{\"equivalent\":\"basics.colors.tertiary.slate.900\"},\"basics.colors.surfaceLightGrayLevel100\":{\"equivalent\":\"basics.colors.surface.lightGray.100\"},\"basics.colors.surfaceLightGrayLevel200\":{\"equivalent\":\"basics.colors.surface.lightGray.200\"},\"basics.colors.surfaceLightGrayLevel250\":{\"equivalent\":\"basics.colors.surface.lightGray.250\"},\"basics.colors.surfaceLightGrayLevel300\":{\"equivalent\":\"basics.colors.surface.lightGray.300\"},\"basics.colors.surfaceLightGrayLevel350\":{\"equivalent\":\"basics.colors.surface.lightGray.350\"},\"basics.colors.surfaceDarkBlueLevel100\":{\"equivalent\":\"basics.colors.surface.darkBlue.100\"},\"basics.colors.surfaceDarkBlueLevel200\":{\"equivalent\":\"basics.colors.surface.darkBlue.200\"},\"basics.colors.surfaceDarkBlueLevel250\":{\"equivalent\":\"basics.colors.surface.darkBlue.250\"},\"basics.colors.surfaceDarkBlueLevel300\":{\"equivalent\":\"basics.colors.surface.darkBlue.300\"},\"basics.colors.surfaceDarkBlueLevel350\":{\"equivalent\":\"basics.colors.surface.darkBlue.350\"},\"basics.colors.surfaceDarkGrayLevel100\":{\"equivalent\":\"basics.colors.surface.darkGray.100\"},\"basics.colors.surfaceDarkGrayLevel200\":{\"equivalent\":\"basics.colors.surface.darkGray.200\"},\"basics.colors.surfaceDarkGrayLevel250\":{\"equivalent\":\"basics.colors.surface.darkGray.250\"},\"basics.colors.surfaceDarkGrayLevel300\":{\"equivalent\":\"basics.colors.surface.darkGray.300\"},\"basics.colors.surfaceDarkGrayLevel350\":{\"equivalent\":\"basics.colors.surface.darkGray.350\"},\"basics.colors.surfaceDarkBlueShadow\":{\"equivalent\":\"basics.colors.shadow.darkBlue\"},\"basics.colors.surfaceDarkGrayShadow\":{\"equivalent\":\"basics.colors.shadow.darkGray\"},\"basics.colors.surfaceLightGrayShadow\":{\"equivalent\":\"basics.colors.shadow.lightGray\"},\"basics.colors.textAgainstDark\":{\"equivalent\":\"basics.colors.text.againstDark\"},\"basics.colors.textAgainstLight\":{\"equivalent\":\"basics.colors.text.againstLight\"},\"basics.colors.iconLightGrayColor\":{\"equivalent\":\"basics.colors.icon.lightGray\"},\"basics.colors.iconDarkBlueColor\":{\"equivalent\":\"basics.colors.icon.darkBlue\"},\"basics.colors.iconDarkGrayColor\":{\"equivalent\":\"basics.colors.icon.darkGray\"},\"basics.colors.textLinkAgainstLight\":{\"equivalent\":\"basics.colors.textLink.primary.againstLight\"},\"basics.colors.textLinkAgainstDark\":{\"equivalent\":\"basics.colors.textLink.primary.againstDark\"},\"basics.colors.error\":{\"equivalent\":\"colorScheme.status.error\"},\"basics.colors.success\":{\"equivalent\":\"colorScheme.status.success\"},\"basics.colors.warning\":{\"equivalent\":\"colorScheme.status.warning\"},\"colorScheme.accentColor\":{\"equivalent\":\"colorScheme.reference.accent\"},\"colorScheme.baseColor\":{\"equivalent\":\"colorScheme.reference.base\"},\"colorScheme.surfaceLevel100Color\":{\"equivalent\":\"colorScheme.surface.level100\"},\"colorScheme.surfaceLevel200Color\":{\"equivalent\":\"colorScheme.surface.level200\"},\"colorScheme.surfaceLevel250Color\":{\"equivalent\":\"colorScheme.surface.level250\"},\"colorScheme.surfaceLevel300Color\":{\"equivalent\":\"colorScheme.surface.level300\"},\"colorScheme.surfaceLevel350Color\":{\"equivalent\":\"colorScheme.surface.level350\"},\"colorScheme.highShadowColor\":{\"equivalent\":\"colorScheme.shadow.high\"},\"colorScheme.lowShadowColor\":{\"equivalent\":\"colorScheme.shadow.low\"},\"colorScheme.textColor\":{\"equivalent\":\"colorScheme.text.default\"},\"colorScheme.textColorDim\":{\"equivalent\":\"colorScheme.text.dim\"},\"colorScheme.iconColor\":{\"equivalent\":\"colorScheme.icon.default\"},\"colorScheme.errorColor\":{\"equivalent\":\"colorScheme.status.error\"},\"colorScheme.successColor\":{\"equivalent\":\"colorScheme.status.success\"},\"colorScheme.warningColor\":{\"equivalent\":\"colorScheme.status.warning\"},\"colorScheme.infoColor\":{\"equivalent\":\"colorScheme.status.info\"},\"colorScheme.component.backgroundColor\":{\"equivalent\":\"colorScheme.surface.level\"},\"accordion.header.indicatorColor\":{\"equivalent\":\"accordion.header.default.indicatorColor\"},\"avatar.fontColorOnDark\":{\"equivalent\":\"avatar.darkBackground.fontColor\"},\"avatar.fontColorOnLight\":{\"equivalent\":\"avatar.lightBackground.fontColor\"},\"banner.info.iconBackground\":{\"equivalent\":\"banner.info.iconField.backgroundColor\"},\"banner.success.iconBackground\":{\"equivalent\":\"banner.success.iconField.backgroundColor\"},\"banner.warning.iconBackground\":{\"equivalent\":\"banner.warning.iconField.backgroundColor\"},\"banner.error.iconBackground\":{\"equivalent\":\"banner.error.iconField.backgroundColor\"},\"button.fontWeight\":{\"equivalent\":\"button.label.fontWeight\"},\"button.lineHeight\":{\"equivalent\":\"button.label.lineHeight\"},\"button.fontFamily\":{\"equivalent\":\"button.label.fontFamily\"},\"button.fontSize\":{\"equivalent\":\"button.label.fontSize\"},\"button.gutter\":{\"equivalent\":\"button.gutterWidth\"},\"button.horizontalPadding\":{\"equivalent\":\"button.paddingHorizontal\"},\"button.minimumWidth\":{\"equivalent\":\"button.minWidth\"},\"button.verticalPadding\":{\"equivalent\":\"button.paddingVertical\"},\"button.halo.width\":{\"equivalent\":\"button.haloWidth\"},\"button.halo.color\":{\"equivalent\":\"colorScheme.halo\"},\"button.hover.halo.width\":{\"equivalent\":\"button.hover.haloWidth\"},\"button.focus.halo.width\":{\"equivalent\":\"button.haloWidth\"},\"button.pressed.halo.width\":{\"equivalent\":\"button.pressed.haloWidth\"},\"button.focus.halo.color\":{\"equivalent\":\"button.focus.haloColor\"},\"button.pressed.halo.color\":{\"equivalent\":\"button.pressed.haloColor\"},\"button.outline.backgroundColor\":{\"equivalent\":\"button.outline.default.backgroundColor\"},\"button.outline.borderColor\":{\"equivalent\":\"button.outline.default.borderColor\"},\"button.outline.focus.halo.color\":{\"equivalent\":\"button.focus.haloColor\"},\"button.outline.focus.icon.color\":{\"equivalent\":\"button.outline.iconColor\"},\"button.outline.focus.textColor\":{\"equivalent\":\"button.outline.label.fontColor\"},\"button.outline.halo.color\":{\"equivalent\":\"colorScheme.halo\"},\"button.outline.hover.halo.color\":{\"equivalent\":\"button.hover.haloColor\"},\"button.outline.hover.icon.color\":{\"equivalent\":\"button.outline.iconColor\"},\"button.outline.hover.textColor\":{\"equivalent\":\"button.outline.label.fontColor\"},\"button.outline.icon.color\":{\"equivalent\":\"button.outline.iconColor\"},\"button.outline.pressed.halo.color\":{\"equivalent\":\"button.pressed.haloColor\"},\"button.outline.pressed.haloWidth\":{\"equivalent\":\"button.pressed.haloWidth\"},\"button.outline.pressed.icon.color\":{\"equivalent\":\"button.outline.iconColor\"},\"button.outline.pressed.textColor\":{\"equivalent\":\"button.outline.label.fontColor\"},\"button.outline.textColor\":{\"equivalent\":\"button.outline.label.fontColor\"},\"button.solid.backgroundColor\":{\"equivalent\":\"button.solid.default.backgroundColor\"},\"button.solid.borderColor\":{\"equivalent\":\"button.solid.default.borderColor\"},\"button.solid.halo.color\":{\"equivalent\":\"colorScheme.halo\"},\"button.solid.textColor\":{\"equivalent\":\"button.solid.label.fontColor\"},\"button.solid.icon.color\":{\"equivalent\":\"button.solid.iconColor\"},\"button.solid.hover.halo.color\":{\"equivalent\":\"button.hover.haloColor\"},\"button.solid.hover.textColor\":{\"equivalent\":\"button.solid.label.fontColor\"},\"button.solid.hover.icon.color\":{\"equivalent\":\"button.solid.iconColor\"},\"button.solid.focus.halo.color\":{\"equivalent\":\"button.focus.haloColor\"},\"button.solid.focus.textColor\":{\"equivalent\":\"button.solid.label.fontColor\"},\"button.solid.focus.icon.color\":{\"equivalent\":\"button.solid.iconColor\"},\"button.solid.pressed.haloWidth\":{\"equivalent\":\"button.pressed.haloWidth\"},\"button.solid.pressed.halo.color\":{\"equivalent\":\"button.pressed.haloColor\"},\"button.solid.pressed.textColor\":{\"equivalent\":\"button.solid.label.fontColor\"},\"button.solid.pressed.icon.color\":{\"equivalent\":\"button.solid.iconColor\"},\"button.flat.backgroundColor\":{\"equivalent\":\"button.flat.default.backgroundColor\"},\"button.flat.borderColor\":{\"equivalent\":\"button.flat.default.borderColor\"},\"button.flat.halo.color\":{\"equivalent\":\"colorScheme.halo\"},\"button.flat.textColor\":{\"equivalent\":\"button.flat.label.fontColor\"},\"button.flat.icon.color\":{\"equivalent\":\"button.flat.iconColor\"},\"button.flat.hover.halo.color\":{\"equivalent\":\"button.hover.haloColor\"},\"button.flat.hover.textColor\":{\"equivalent\":\"button.flat.label.fontColor\"},\"button.flat.hover.icon.color\":{\"equivalent\":\"button.flat.iconColor\"},\"button.flat.focus.halo.color\":{\"equivalent\":\"button.focus.haloColor\"},\"button.flat.focus.textColor\":{\"equivalent\":\"button.flat.label.fontColor\"},\"button.flat.focus.icon.color\":{\"equivalent\":\"button.flat.iconColor\"},\"button.flat.pressed.haloWidth\":{\"equivalent\":\"button.pressed.haloWidth\"},\"button.flat.pressed.halo.color\":{\"equivalent\":\"button.pressed.haloColor\"},\"button.flat.pressed.textColor\":{\"equivalent\":\"button.flat.label.fontColor\"},\"button.flat.pressed.icon.color\":{\"equivalent\":\"button.flat.iconColor\"},\"checkbox.halo.color\":{\"equivalent\":\"checkbox.hover.haloColor\"},\"checkbox.halo.width\":{\"equivalent\":\"checkbox.hover.haloWidth\"},\"checkbox.height\":{\"equivalent\":\"checkbox.minHeight\"},\"checkbox.iconColor\":{\"equivalent\":\"checkbox.indicatorColor\"},\"checkbox.width\":{\"equivalent\":\"checkbox.minWidth\"},\"checkbox.backgroundColor\":{\"equivalent\":\"checkbox.unchecked.default.backgroundColor\"},\"checkbox.borderColor\":{\"equivalent\":\"checkbox.unchecked.default.borderColor\"},\"checkbox.hover.borderColor\":{\"equivalent\":\"checkbox.unchecked.hover.borderColor\"},\"checkbox.hover.halo.width\":{\"equivalent\":\"checkbox.hover.haloWidth\"},\"checkbox.hover.halo.color\":{\"equivalent\":\"checkbox.hover.haloColor\"},\"checkbox.focus.borderColor\":{\"equivalent\":\"checkbox.unchecked.focus.borderColor\"},\"checkbox.focus.halo.color\":{\"equivalent\":\"checkbox.focus.haloColor\"},\"checkbox.focus.halo.width\":{\"equivalent\":\"checkbox.focus.haloWidth\"},\"checkbox.pressed.borderColor\":{\"equivalent\":\"checkbox.unchecked.pressed.borderColor\"},\"checkbox.pressed.halo.color\":{\"equivalent\":\"checkbox.pressed.haloColor\"},\"checkbox.pressed.halo.width\":{\"equivalent\":\"checkbox.pressed.haloWidth\"},\"checkbox.checked.borderColor\":{\"equivalent\":\"checkbox.checked.default.borderColor\"},\"checkbox.checked.iconColor\":{\"equivalent\":\"checkbox.indicatorColor\"},\"checkbox.checked.backgroundColor\":{\"equivalent\":\"checkbox.checked.default.backgroundColor\"},\"checkbox.indeterminate.borderColor\":{\"equivalent\":\"checkbox.checked.default.borderColor\"},\"checkbox.indeterminate.iconColor\":{\"equivalent\":\"checkbox.indicatorColor\"},\"checkbox.indeterminate.backgroundColor\":{\"equivalent\":\"checkbox.checked.default.backgroundColor\"},\"checkbox.focus.checked.backgroundColor\":{\"equivalent\":\"checkbox.checked.default.backgroundColor\"},\"checkbox.focus.checked.borderColor\":{\"equivalent\":\"checkbox.checked.default.borderColor\"},\"checkbox.focus.indeterminate.borderColor\":{\"equivalent\":\"checkbox.checked.default.borderColor\"},\"component.disabled.opacity\":{\"equivalent\":\"colorScheme.opacity.disabled\"},\"divider.width\":{\"equivalent\":\"divider.borderWidth\"},\"formField.instructionalText.fontSize\":{\"equivalent\":\"input.value.fontSize\"},\"formField.instructionalText.fontWeight\":{\"equivalent\":\"input.value.fontWeight\"},\"formField.instructionalText.fontFamily\":{\"equivalent\":\"input.value.fontFamily\"},\"formField.instructionalText.fontColor\":{\"equivalent\":\"input.value.placeholderFontColor\"},\"formField.instructionalText.lineHeight\":{\"equivalent\":\"basics.lineHeights.medium\"},\"formField.icon.width\":{\"equivalent\":\"basics.spacings\"},\"formField.icon.paddingRight\":{\"equivalent\":\"density.spacings.small\"},\"formField.icon.color\":{\"equivalent\":\"input.indicator.default\"},\"iconButton.focus.halo.color\":{\"equivalent\":\"iconButton.focus.haloColor\"},\"iconButton.focus.halo.width\":{\"equivalent\":\"iconButton.focus.haloWidth\"},\"iconButton.focus.iconColor\":{\"equivalent\":\"iconButton.dynamic.focus.iconColor\"},\"iconButton.focus.on.iconColor\":{\"equivalent\":\"iconButton.dynamic.on.focus.iconColor\"},\"iconButton.hover.iconColor\":{\"equivalent\":\"iconButton.dynamic.hover.iconColor\"},\"iconButton.hover.on.iconColor\":{\"equivalent\":\"iconButton.dynamic.on.hover.iconColor\"},\"iconButton.iconColor\":{\"equivalent\":\"iconButton.dynamic.default.iconColor\"},\"iconButton.on.iconColor\":{\"equivalent\":\"iconButton.dynamic.on.default.iconColor\"},\"iconButton.pressed.backgroundColor\":{\"equivalent\":\"iconButton.dynamic.pressed.100To250BackgroundColor\"},\"iconButton.pressed.iconColor\":{\"equivalent\":\"iconButton.dynamic.pressed.iconColor\"},\"iconButton.pressed.level100To250.backgroundColor\":{\"equivalent\":\"iconButton.dynamic.pressed.100To250BackgroundColor\"},\"iconButton.pressed.level300To350.backgroundColor\":{\"equivalent\":\"iconButton.dynamic.pressed.300To350BackgroundColor\"},\"iconButton.pressed.on.iconColor\":{\"equivalent\":\"iconButton.dynamic.on.pressed.iconColor\"},\"iconButton.static.hover.level100To250.backgroundColor\":{\"equivalent\":\"iconButton.static.hover.100To250BackgroundColor\"},\"iconButton.static.hover.level300To350.backgroundColor\":{\"equivalent\":\"iconButton.static.hover.300To350BackgroundColor\"},\"iconButton.static.on.backgroundColor\":{\"equivalent\":\"iconButton.static.on.default.backgroundColor\"},\"iconButton.static.on.borderColor\":{\"equivalent\":\"iconButton.static.on.default.borderColor\"},\"iconButton.static.on.hover.level100To250.backgroundColor\":{\"equivalent\":\"iconButton.static.on.hover.100To250BackgroundColor\"},\"iconButton.static.on.hover.level300To350.backgroundColor\":{\"equivalent\":\"iconButton.static.on.hover.300To350BackgroundColor\"},\"input.active.halo.color\":{\"equivalent\":\"input.focus.haloColor\"},\"input.borderBottomColor\":{\"equivalent\":\"input.line.default.borderBottomColor\"},\"input.active.halo.width\":{\"equivalent\":\"input.haloWidth\"},\"input.box.backgroundColor\":{\"equivalent\":\"input.box.default.backgroundColor\"},\"input.box.borderBottomColor\":{\"equivalent\":\"input.box.default.borderBottomColor\"},\"input.boxType.horizontalPadding\":{\"equivalent\":\"input.box.paddingHorizontal\"},\"input.focus.borderBottomColor\":{\"equivalent\":\"input.line.focus.borderBottomColor\"},\"input.focus.halo.color\":{\"equivalent\":\"input.focus.haloColor\"},\"input.focus.halo.width\":{\"equivalent\":\"input.haloWidth\"},\"input.fontColor\":{\"equivalent\":\"input.value.fontColor\"},\"input.fontFamily\":{\"equivalent\":\"input.value.fontFamily\"},\"input.fontSize\":{\"equivalent\":\"input.value.fontSize\"},\"input.fontWeight\":{\"equivalent\":\"input.value.fontWeight\"},\"input.halo.color\":{\"equivalent\":\"input.hover.haloColor\"},\"input.halo.width\":{\"equivalent\":\"input.haloWidth\"},\"input.height\":{\"equivalent\":\"input.minHeight\"},\"input.highlightColor\":{\"equivalent\":\"input.value.highlightColor\"},\"input.horizontalPadding\":{\"equivalent\":\"input.line.paddingHorizontal\"},\"input.hover.borderBottomColor\":{\"equivalent\":\"input.box.hover.borderBottomColor\"},\"input.hover.halo.width\":{\"equivalent\":\"input.haloWidth\"},\"input.lineHeight\":{\"equivalent\":\"input.value.lineHeight\"},\"input.placeholder.fontColor\":{\"equivalent\":\"input.value.placeholderFontColor\"},\"input.verticalPadding\":{\"equivalent\":\"input.paddingVertical\"},\"menu.item.backgroundColor\":{\"equivalent\":\"menu.item.default.backgroundColor\"},\"menu.item.active.backgroundColor\":{\"equivalent\":\"menu.item.pressed.backgroundColor\"},\"menu.item.horizontalPadding\":{\"equivalent\":\"menu.item.paddingHorizontal\"},\"menu.item.verticalPadding\":{\"equivalent\":\"menu.item.paddingVertical\"},\"modal.overlay.color\":{\"equivalent\":\"modal.overlayColor\"},\"modal.textColor\":{\"equivalent\":\"modal.fontColor\"},\"modal.body.height\":{\"equivalent\":\"modal.body.minHeight\"},\"modal.horizontalPadding\":{\"equivalent\":\"modal.paddingHorizontal\"},\"modal.header.borderColor\":{\"equivalent\":\"modal.header.borderBottomColor\"},\"modal.header.height\":{\"equivalent\":\"modal.header.minHeight\"},\"modal.window.backgroundColor\":{\"equivalent\":\"modal.shell.backgroundColor\"},\"modal.window.borderRadius\":{\"equivalent\":\"modal.shell.borderRadius\"},\"modal.window.height\":{\"equivalent\":\"modal.shell.minHeight\"},\"modal.window.width\":{\"equivalent\":\"modal.shell.minWidth\"},\"modal.window.shadowColor\":{\"equivalent\":\"modal.shell.shadowColor\"},\"progressBar.backgroundColor\":{\"equivalent\":\"progress.bar.backgroundColor\"},\"progressBar.highlightColor\":{\"equivalent\":\"progress.bar.highlightColor\"},\"progressBar.borderRadius\":{\"equivalent\":\"progress.bar.borderRadius\"},\"progressBar.height\":{\"equivalent\":\"progress.bar.minHeight\"},\"progressRing.backgroundColor\":{\"equivalent\":\"progress.ring.backgroundColor\"},\"progressRing.highlightColor1\":{\"equivalent\":\"progress.ring.highlightColor1\"},\"progressRing.highlightColor2\":{\"equivalent\":\"progress.ring.highlightColor2\"},\"skeletonItem.defaultHeight\":{\"equivalent\":\"skeletonItem.minHeight\"},\"slider.halo.width\":{\"equivalent\":\"slider.hover.haloWidth\"},\"slider.halo.color\":{\"equivalent\":\"slider.hover.haloColor\"},\"slider.thumb.width\":{\"equivalent\":\"slider.thumb.minWidth\"},\"slider.focused.halo.color\":{\"equivalent\":\"slider.focus.haloColor\"},\"slider.focused.halo.width\":{\"equivalent\":\"slider.focus.haloWidth\"},\"slider.focused.thumb.color\":{\"equivalent\":\"slider.thumb.backgroundColor\"},\"slider.hover.halo.color\":{\"equivalent\":\"slider.hover.haloColor\"},\"slider.hover.halo.width\":{\"equivalent\":\"slider.hover.haloWidth\"},\"slider.hover.thumb.color\":{\"equivalent\":\"slider.thumb.backgroundColor\"},\"slider.pressed.halo.color\":{\"equivalent\":\"slider.pressed.haloColor\"},\"slider.pressed.halo.width\":{\"equivalent\":\"slider.pressed.haloWidth\"},\"slider.pressed.thumb.color\":{\"equivalent\":\"slider.thumb.backgroundColor\"},\"slider.track.color\":{\"equivalent\":\"slider.track.backgroundColor\"},\"slider.track.width\":{\"equivalent\":\"slider.track.minHeight\"},\"slider.value.color\":{\"equivalent\":\"slider.value.backgroundColor\"},\"tabs.general.tab.fontFamily\":{\"equivalent\":\"tabs.label.fontFamily\"},\"tabs.general.tab.fontSize\":{\"equivalent\":\"tabs.label.fontSize\"},\"tabs.general.tab.fontWeight\":{\"equivalent\":\"tabs.label.inactive.fontWeight\"},\"tabs.general.tab.active.fontWeight\":{\"equivalent\":\"tabs.label.active.fontWeight\"},\"tabs.general.tab.color\":{\"equivalent\":\"tabs.label.fontColor\"},\"tabs.general.tab.lineHeight\":{\"equivalent\":\"tabs.label.active.lineHeight\"},\"tabs.general.halo.size\":{\"equivalent\":\"tabs.focus.haloWidth\"},\"tabs.general.halo.color\":{\"equivalent\":\"tabs.focus.haloColor\"},\"tabs.general.icon.size\":{\"equivalent\":\"tabs.iconSize\"},\"tabs.general.icon.color\":{\"equivalent\":\"tabs.iconColor\"},\"tabs.general.icon.gutter\":{\"equivalent\":\"tabs.iconGutter\"},\"tabs.general.closeButton.size\":{\"equivalent\":\"tabs.closeButton.minSize\"},\"tabs.general.closeButton.gutter\":{\"equivalent\":\"tabs.closeButton.gutter\"},\"tabs.general.closeButton.color\":{\"equivalent\":\"tabs.closeButton.default.iconColor\"},\"tabs.general.closeButton.hover.color\":{\"equivalent\":\"tabs.closeButton.hover.iconColor\"},\"tabs.general.closeButton.pressed.color\":{\"equivalent\":\"tabs.closeButton.pressed.iconColor\"},\"tabs.underline.halo.hover.color\":{\"equivalent\":\"tabs.underline.hover.haloColor\"},\"tabs.underline.halo.active.color\":{\"equivalent\":\"tabs.underline.active.haloColor\"},\"tabs.box.tab.horizontalPadding\":{\"equivalent\":\"tabs.box.tab.paddingHorizontal\"},\"tabs.box.tab.verticalPadding\":{\"equivalent\":\"tabs.box.tab.paddingVertical\"},\"tabs.box.divider.color\":{\"equivalent\":\"tabs.box.divider.borderColor\"},\"tabs.box.divider.width\":{\"equivalent\":\"tabs.box.dividerWidth\"},\"tabs.box.divider.height\":{\"equivalent\":\"tabs.box.dividerHeight\"},\"tabs.canvas.tab.horizontalPadding\":{\"equivalent\":\"tabs.canvas.tab.paddingHorizontal\"},\"tabs.canvas.tab.verticalPadding\":{\"equivalent\":\"tabs.canvas.tab.paddingVertical\"},\"tabs.canvas.divider.color\":{\"equivalent\":\"tabs.canvas.divider.borderColor\"},\"tabs.canvas.divider.width\":{\"equivalent\":\"tabs.canvas.dividerWidth\"},\"tabs.canvas.divider.height\":{\"equivalent\":\"tabs.canvas.dividerHeight\"},\"textLink.textColor\":{\"equivalent\":\"textLink.primary.default.fontColor\"},\"textLink.hover.textColor\":{\"equivalent\":\"textLink.primary.hover.fontColor\"},\"textLink.focus.textColor\":{\"equivalent\":\"textLink.primary.focus.fontColor\"},\"textLink.hover.underline.color\":{\"equivalent\":\"textLink.primary.hover.underlineColor\"},\"textLink.focus.halo.color\":{\"equivalent\":\"textLink.focus.haloColor\"},\"textLink.focus.halo.width\":{\"equivalent\":\"textLink.focus.haloWidth\"},\"tooltip.textColor\":{\"equivalent\":\"tooltip.title.fontColor\"},\"tooltip.borderColor\":{\"equivalent\":\"tooltip.backgroundColor\"}}},\"resolvedRoles\":{\"basics.borderRadii.none\":\"0\",\"basics.borderRadii.small\":\"0\",\"basics.borderRadii.medium\":\"2px\",\"basics.borderRadii.large\":\"4px\",\"basics.borderRadii.ellipse\":\"50%\",\"basics.borderRadii.pill\":\"1000em\",\"basics.borderWidths.none\":\"0\",\"basics.borderWidths.small\":\"1px\",\"basics.borderWidths.medium\":\"2px\",\"basics.borderWidths.large\":\"4px\",\"basics.colors.primary.black\":\"#000000\",\"basics.colors.primary.white\":\"#ffffff\",\"basics.colors.primary.autodeskBlue.100\":\"#cdeaf7\",\"basics.colors.primary.autodeskBlue.200\":\"#9bd5ef\",\"basics.colors.primary.autodeskBlue.300\":\"#6ac0e7\",\"basics.colors.primary.autodeskBlue.400\":\"#38abdf\",\"basics.colors.primary.autodeskBlue.500\":\"#0696d7\",\"basics.colors.primary.autodeskBlue.600\":\"#0686c2\",\"basics.colors.primary.autodeskBlue.700\":\"#006eaf\",\"basics.colors.primary.autodeskBlue.800\":\"#034b6c\",\"basics.colors.primary.autodeskBlue.900\":\"#022c40\",\"basics.colors.primary.charcoal.100\":\"#eeeeee\",\"basics.colors.primary.charcoal.200\":\"#dcdcdc\",\"basics.colors.primary.charcoal.300\":\"#cccccc\",\"basics.colors.primary.charcoal.400\":\"#bbbbbb\",\"basics.colors.primary.charcoal.500\":\"#999999\",\"basics.colors.primary.charcoal.600\":\"#808080\",\"basics.colors.primary.charcoal.700\":\"#666666\",\"basics.colors.primary.charcoal.800\":\"#4d4d4d\",\"basics.colors.primary.charcoal.900\":\"#3c3c3c\",\"basics.colors.secondary.darkBlue.100\":\"#e1ebf7\",\"basics.colors.secondary.darkBlue.200\":\"#d1deee\",\"basics.colors.secondary.darkBlue.300\":\"#a3bcdc\",\"basics.colors.secondary.darkBlue.400\":\"#749bcb\",\"basics.colors.secondary.darkBlue.500\":\"#4679b9\",\"basics.colors.secondary.darkBlue.600\":\"#1858a8\",\"basics.colors.secondary.darkBlue.700\":\"#12437f\",\"basics.colors.secondary.darkBlue.800\":\"#0e3666\",\"basics.colors.secondary.darkBlue.900\":\"#0c2c54\",\"basics.colors.secondary.green.100\":\"#e7f2d9\",\"basics.colors.secondary.green.200\":\"#cfe4b3\",\"basics.colors.secondary.green.300\":\"#b7d78c\",\"basics.colors.secondary.green.400\":\"#9fc966\",\"basics.colors.secondary.green.500\":\"#87b340\",\"basics.colors.secondary.green.600\":\"#658d30\",\"basics.colors.secondary.green.700\":\"#5d822c\",\"basics.colors.secondary.green.800\":\"#4c6b24\",\"basics.colors.secondary.green.900\":\"#445e20\",\"basics.colors.secondary.red.100\":\"#ffd9d9\",\"basics.colors.secondary.red.200\":\"#f8d3d3\",\"basics.colors.secondary.red.300\":\"#f1a7a7\",\"basics.colors.secondary.red.400\":\"#eb7a7a\",\"basics.colors.secondary.red.500\":\"#ec4a41\",\"basics.colors.secondary.red.600\":\"#dd2222\",\"basics.colors.secondary.red.700\":\"#a61a1a\",\"basics.colors.secondary.red.800\":\"#6f1111\",\"basics.colors.secondary.red.900\":\"#630f0f\",\"basics.colors.secondary.turquoise.100\":\"#d6f2ef\",\"basics.colors.secondary.turquoise.200\":\"#ade4de\",\"basics.colors.secondary.turquoise.300\":\"#84d7ce\",\"basics.colors.secondary.turquoise.400\":\"#5bc9bd\",\"basics.colors.secondary.turquoise.500\":\"#32bcad\",\"basics.colors.secondary.turquoise.600\":\"#23a597\",\"basics.colors.secondary.turquoise.700\":\"#268d82\",\"basics.colors.secondary.turquoise.800\":\"#20756c\",\"basics.colors.secondary.turquoise.900\":\"#195e57\",\"basics.colors.secondary.yellowOrange.100\":\"#feecd1\",\"basics.colors.secondary.yellowOrange.200\":\"#fddaa4\",\"basics.colors.secondary.yellowOrange.300\":\"#fcc776\",\"basics.colors.secondary.yellowOrange.400\":\"#fbb549\",\"basics.colors.secondary.yellowOrange.500\":\"#faa21b\",\"basics.colors.secondary.yellowOrange.600\":\"#bb7a14\",\"basics.colors.secondary.yellowOrange.700\":\"#916010\",\"basics.colors.secondary.yellowOrange.800\":\"#7d510e\",\"basics.colors.secondary.yellowOrange.900\":\"#523609\",\"basics.colors.tertiary.pink.100\":\"#ffebf5\",\"basics.colors.tertiary.pink.200\":\"#ffd6eb\",\"basics.colors.tertiary.pink.300\":\"#ffb8db\",\"basics.colors.tertiary.pink.400\":\"#ff8cc6\",\"basics.colors.tertiary.pink.500\":\"#fc56a9\",\"basics.colors.tertiary.pink.600\":\"#e84396\",\"basics.colors.tertiary.pink.700\":\"#c72877\",\"basics.colors.tertiary.pink.800\":\"#991f5c\",\"basics.colors.tertiary.pink.900\":\"#781848\",\"basics.colors.tertiary.purple.100\":\"#f4edfd\",\"basics.colors.tertiary.purple.200\":\"#eadcfd\",\"basics.colors.tertiary.purple.300\":\"#d5bcf7\",\"basics.colors.tertiary.purple.400\":\"#c5a1f8\",\"basics.colors.tertiary.purple.500\":\"#b385f2\",\"basics.colors.tertiary.purple.600\":\"#a76ef5\",\"basics.colors.tertiary.purple.700\":\"#8558c5\",\"basics.colors.tertiary.purple.800\":\"#5f3e8e\",\"basics.colors.tertiary.purple.900\":\"#482f6b\",\"basics.colors.tertiary.salmon.100\":\"#ffefec\",\"basics.colors.tertiary.salmon.200\":\"#fcded9\",\"basics.colors.tertiary.salmon.300\":\"#ffbeb3\",\"basics.colors.tertiary.salmon.400\":\"#ff9582\",\"basics.colors.tertiary.salmon.500\":\"#f26a52\",\"basics.colors.tertiary.salmon.600\":\"#db5942\",\"basics.colors.tertiary.salmon.700\":\"#a84a39\",\"basics.colors.tertiary.salmon.800\":\"#853729\",\"basics.colors.tertiary.salmon.900\":\"#66251a\",\"basics.colors.tertiary.slate.100\":\"#e9f0f7\",\"basics.colors.tertiary.slate.200\":\"#dce7f3\",\"basics.colors.tertiary.slate.300\":\"#c1cedc\",\"basics.colors.tertiary.slate.400\":\"#a7bacf\",\"basics.colors.tertiary.slate.500\":\"#7993b0\",\"basics.colors.tertiary.slate.600\":\"#6784a6\",\"basics.colors.tertiary.slate.700\":\"#4b6b8f\",\"basics.colors.tertiary.slate.800\":\"#354d67\",\"basics.colors.tertiary.slate.900\":\"#2c3e53\",\"basics.colors.surface.lightGray.100\":\"#ffffff\",\"basics.colors.surface.lightGray.200\":\"#f5f5f5\",\"basics.colors.surface.lightGray.250\":\"#eeeeee\",\"basics.colors.surface.lightGray.300\":\"#d9d9d9\",\"basics.colors.surface.lightGray.350\":\"#cccccc\",\"basics.colors.surface.darkBlue.100\":\"#454f61\",\"basics.colors.surface.darkBlue.200\":\"#3b4453\",\"basics.colors.surface.darkBlue.250\":\"#2e3440\",\"basics.colors.surface.darkBlue.300\":\"#222933\",\"basics.colors.surface.darkBlue.350\":\"#1a1f25\",\"basics.colors.surface.darkGray.100\":\"#535353\",\"basics.colors.surface.darkGray.200\":\"#474747\",\"basics.colors.surface.darkGray.250\":\"#373737\",\"basics.colors.surface.darkGray.300\":\"#2a2a2a\",\"basics.colors.surface.darkGray.350\":\"#202020\",\"basics.colors.shadow.darkBlue\":\"rgba(26, 31, 38, 0.5)\",\"basics.colors.shadow.darkGray\":\"rgba(0, 0, 0, 0.3)\",\"basics.colors.shadow.lightGray\":\"rgba(0, 0, 0, 0.2)\",\"basics.colors.text.againstDark\":\"#f5f5f5\",\"basics.colors.text.againstLight\":\"#3c3c3c\",\"basics.colors.textLink.primary.againstLight\":\"#006eaf\",\"basics.colors.textLink.primary.againstDark\":\"#6dd2ff\",\"basics.colors.textLink.secondary.againstLight\":\"#3c3c3c\",\"basics.colors.textLink.secondary.againstDark\":\"#f5f5f5\",\"basics.colors.icon.lightGray\":\"#808080\",\"basics.colors.icon.darkBlue\":\"#a2a6b0\",\"basics.colors.icon.darkGray\":\"#999999\",\"basics.colors.black\":\"#000000\",\"basics.colors.white\":\"#ffffff\",\"basics.colors.autodeskBlue100\":\"#cdeaf7\",\"basics.colors.autodeskBlue200\":\"#9bd5ef\",\"basics.colors.autodeskBlue300\":\"#6ac0e7\",\"basics.colors.autodeskBlue400\":\"#38abdf\",\"basics.colors.autodeskBlue500\":\"#0696d7\",\"basics.colors.autodeskBlue600\":\"#0686c2\",\"basics.colors.autodeskBlue700\":\"#006eaf\",\"basics.colors.autodeskBlue800\":\"#034b6c\",\"basics.colors.autodeskBlue900\":\"#022c40\",\"basics.colors.charcoal100\":\"#eeeeee\",\"basics.colors.charcoal200\":\"#dcdcdc\",\"basics.colors.charcoal300\":\"#cccccc\",\"basics.colors.charcoal400\":\"#bbbbbb\",\"basics.colors.charcoal500\":\"#999999\",\"basics.colors.charcoal600\":\"#808080\",\"basics.colors.charcoal700\":\"#666666\",\"basics.colors.charcoal800\":\"#4d4d4d\",\"basics.colors.charcoal900\":\"#3c3c3c\",\"basics.colors.darkBlue100\":\"#e1ebf7\",\"basics.colors.darkBlue200\":\"#d1deee\",\"basics.colors.darkBlue300\":\"#a3bcdc\",\"basics.colors.darkBlue400\":\"#749bcb\",\"basics.colors.darkBlue500\":\"#4679b9\",\"basics.colors.darkBlue600\":\"#1858a8\",\"basics.colors.darkBlue700\":\"#12437f\",\"basics.colors.darkBlue800\":\"#0e3666\",\"basics.colors.darkBlue900\":\"#0c2c54\",\"basics.colors.green100\":\"#e7f2d9\",\"basics.colors.green200\":\"#cfe4b3\",\"basics.colors.green300\":\"#b7d78c\",\"basics.colors.green400\":\"#9fc966\",\"basics.colors.green500\":\"#87b340\",\"basics.colors.green600\":\"#658d30\",\"basics.colors.green700\":\"#5d822c\",\"basics.colors.green800\":\"#4c6b24\",\"basics.colors.green900\":\"#445e20\",\"basics.colors.red100\":\"#ffd9d9\",\"basics.colors.red200\":\"#f8d3d3\",\"basics.colors.red300\":\"#f1a7a7\",\"basics.colors.red400\":\"#eb7a7a\",\"basics.colors.red500\":\"#ec4a41\",\"basics.colors.red600\":\"#dd2222\",\"basics.colors.red700\":\"#a61a1a\",\"basics.colors.red800\":\"#6f1111\",\"basics.colors.red900\":\"#630f0f\",\"basics.colors.turquoise100\":\"#d6f2ef\",\"basics.colors.turquoise200\":\"#ade4de\",\"basics.colors.turquoise300\":\"#84d7ce\",\"basics.colors.turquoise400\":\"#5bc9bd\",\"basics.colors.turquoise500\":\"#32bcad\",\"basics.colors.turquoise600\":\"#23a597\",\"basics.colors.turquoise700\":\"#268d82\",\"basics.colors.turquoise800\":\"#20756c\",\"basics.colors.turquoise900\":\"#195e57\",\"basics.colors.yellowOrange100\":\"#feecd1\",\"basics.colors.yellowOrange200\":\"#fddaa4\",\"basics.colors.yellowOrange300\":\"#fcc776\",\"basics.colors.yellowOrange400\":\"#fbb549\",\"basics.colors.yellowOrange500\":\"#faa21b\",\"basics.colors.yellowOrange600\":\"#bb7a14\",\"basics.colors.yellowOrange700\":\"#916010\",\"basics.colors.yellowOrange800\":\"#7d510e\",\"basics.colors.yellowOrange900\":\"#523609\",\"basics.colors.pink100\":\"#ffebf5\",\"basics.colors.pink200\":\"#ffd6eb\",\"basics.colors.pink300\":\"#ffb8db\",\"basics.colors.pink400\":\"#ff8cc6\",\"basics.colors.pink500\":\"#fc56a9\",\"basics.colors.pink600\":\"#e84396\",\"basics.colors.pink700\":\"#c72877\",\"basics.colors.pink800\":\"#991f5c\",\"basics.colors.pink900\":\"#781848\",\"basics.colors.purple100\":\"#f4edfd\",\"basics.colors.purple200\":\"#eadcfd\",\"basics.colors.purple300\":\"#d5bcf7\",\"basics.colors.purple400\":\"#c5a1f8\",\"basics.colors.purple500\":\"#b385f2\",\"basics.colors.purple600\":\"#a76ef5\",\"basics.colors.purple700\":\"#8558c5\",\"basics.colors.purple800\":\"#5f3e8e\",\"basics.colors.purple900\":\"#482f6b\",\"basics.colors.salmon100\":\"#ffefec\",\"basics.colors.salmon200\":\"#fcded9\",\"basics.colors.salmon300\":\"#ffbeb3\",\"basics.colors.salmon400\":\"#ff9582\",\"basics.colors.salmon500\":\"#f26a52\",\"basics.colors.salmon600\":\"#db5942\",\"basics.colors.salmon700\":\"#a84a39\",\"basics.colors.salmon800\":\"#853729\",\"basics.colors.salmon900\":\"#66251a\",\"basics.colors.slate100\":\"#e9f0f7\",\"basics.colors.slate200\":\"#dce7f3\",\"basics.colors.slate300\":\"#c1cedc\",\"basics.colors.slate400\":\"#a7bacf\",\"basics.colors.slate500\":\"#7993b0\",\"basics.colors.slate600\":\"#6784a6\",\"basics.colors.slate700\":\"#4b6b8f\",\"basics.colors.slate800\":\"#354d67\",\"basics.colors.slate900\":\"#2c3e53\",\"basics.colors.surfaceLightGrayLevel100\":\"#ffffff\",\"basics.colors.surfaceLightGrayLevel200\":\"#f5f5f5\",\"basics.colors.surfaceLightGrayLevel250\":\"#eeeeee\",\"basics.colors.surfaceLightGrayLevel300\":\"#d9d9d9\",\"basics.colors.surfaceLightGrayLevel350\":\"#cccccc\",\"basics.colors.surfaceDarkBlueLevel100\":\"#454f61\",\"basics.colors.surfaceDarkBlueLevel200\":\"#3b4453\",\"basics.colors.surfaceDarkBlueLevel250\":\"#2e3440\",\"basics.colors.surfaceDarkBlueLevel300\":\"#222933\",\"basics.colors.surfaceDarkBlueLevel350\":\"#1a1f25\",\"basics.colors.surfaceDarkGrayLevel100\":\"#535353\",\"basics.colors.surfaceDarkGrayLevel200\":\"#474747\",\"basics.colors.surfaceDarkGrayLevel250\":\"#373737\",\"basics.colors.surfaceDarkGrayLevel300\":\"#2a2a2a\",\"basics.colors.surfaceDarkGrayLevel350\":\"#202020\",\"basics.colors.surfaceDarkBlueShadow\":\"rgba(26, 31, 38, 0.5)\",\"basics.colors.surfaceDarkGrayShadow\":\"rgba(0, 0, 0, 0.3)\",\"basics.colors.surfaceLightGrayShadow\":\"rgba(0, 0, 0, 0.2)\",\"basics.colors.textAgainstDark\":\"#f5f5f5\",\"basics.colors.textAgainstLight\":\"#3c3c3c\",\"basics.colors.iconLightGrayColor\":\"#808080\",\"basics.colors.iconDarkBlueColor\":\"#a2a6b0\",\"basics.colors.iconDarkGrayColor\":\"#999999\",\"basics.colors.textLinkAgainstLight\":\"#006eaf\",\"basics.colors.textLinkAgainstDark\":\"#6dd2ff\",\"basics.colors.error\":\"#ec4a41\",\"basics.colors.success\":\"#87b340\",\"basics.colors.warning\":\"#faa21b\",\"basics.fontFamilies.main\":\"ArtifaktElement, sans-serif\",\"basics.fontFamilies.monospace\":\"'Source Code Pro', monospace\",\"basics.fontSizes.highSmall\":\"11px\",\"basics.fontSizes.highMedium\":\"12px\",\"basics.fontSizes.highLarge\":\"16px\",\"basics.fontSizes.highExtraLarge\":\"20px\",\"basics.fontSizes.highExtraExtraLarge\":\"24px\",\"basics.fontSizes.mediumSmall\":\"12px\",\"basics.fontSizes.mediumMedium\":\"14px\",\"basics.fontSizes.mediumLarge\":\"20px\",\"basics.fontSizes.mediumExtraLarge\":\"24px\",\"basics.fontSizes.mediumExtraExtraLarge\":\"28px\",\"basics.fontSizes.lowSmall\":\"14px\",\"basics.fontSizes.lowMedium\":\"16px\",\"basics.fontSizes.lowLarge\":\"22px\",\"basics.fontSizes.lowExtraLarge\":\"26px\",\"basics.fontSizes.lowExtraExtraLarge\":\"30px\",\"basics.fontWeights.regular\":400,\"basics.fontWeights.medium\":600,\"basics.fontWeights.bold\":700,\"basics.lineHeights.small\":\"1.3\",\"basics.lineHeights.medium\":\"1.4\",\"basics.lineHeights.large\":\"1.5\",\"basics.lineHeights.highSmall\":\"1.25\",\"basics.lineHeights.highMedium\":\"1.272727273\",\"basics.lineHeights.highLarge\":\"1.3\",\"basics.lineHeights.highExtraLarge\":\"1.333333333\",\"basics.lineHeights.highExtraExtraLarge\":\"1.375\",\"basics.lineHeights.mediumSmall\":\"1.25\",\"basics.lineHeights.mediumMedium\":\"1.285714286\",\"basics.lineHeights.mediumLarge\":\"1.3\",\"basics.lineHeights.mediumExtraLarge\":\"1.428571429\",\"basics.lineHeights.mediumExtraExtraLarge\":\"1.5\",\"basics.shadows.lowBlur\":\"4px\",\"basics.shadows.highBlur\":\"16px\",\"basics.spacings.none\":\"0\",\"basics.spacings.highExtraExtraSmall\":\"2px\",\"basics.spacings.highExtraSmall\":\"4px\",\"basics.spacings.highSmall\":\"8px\",\"basics.spacings.highMedium\":\"12px\",\"basics.spacings.highLarge\":\"16px\",\"basics.spacings.highExtraLarge\":\"24px\",\"basics.spacings.highExtraExtraLarge\":\"32px\",\"basics.spacings.mediumExtraExtraSmall\":\"4px\",\"basics.spacings.mediumExtraSmall\":\"8px\",\"basics.spacings.mediumSmall\":\"12px\",\"basics.spacings.mediumMedium\":\"16px\",\"basics.spacings.mediumLarge\":\"24px\",\"basics.spacings.mediumExtraLarge\":\"32px\",\"basics.spacings.mediumExtraExtraLarge\":\"48px\",\"basics.spacings.lowExtraExtraSmall\":\"8px\",\"basics.spacings.lowExtraSmall\":\"12px\",\"basics.spacings.lowSmall\":\"16px\",\"basics.spacings.lowMedium\":\"24px\",\"basics.spacings.lowLarge\":\"32px\",\"basics.spacings.lowExtraLarge\":\"48px\",\"basics.spacings.lowExtraExtraLarge\":\"64px\",\"colorScheme.reference.accent\":\"#0696d7\",\"colorScheme.reference.base\":\"#808080\",\"colorScheme.surface.level100\":\"#ffffff\",\"colorScheme.surface.level200\":\"#f5f5f5\",\"colorScheme.surface.level250\":\"#eeeeee\",\"colorScheme.surface.level300\":\"#d9d9d9\",\"colorScheme.surface.level350\":\"#cccccc\",\"colorScheme.background.selected\":\"rgba(6, 150, 215, 0.15)\",\"colorScheme.background.on.default\":\"rgba(205, 234, 247, 0.6)\",\"colorScheme.background.on.hover\":\"rgba(155, 213, 239, 0.7)\",\"colorScheme.background.on.focus\":\"rgba(205, 234, 247, 0.6)\",\"colorScheme.background.on.pressed\":\"rgba(106, 192, 231, 0.7)\",\"colorScheme.background.empty.level100To250.hover\":\"rgba(128, 128, 128, 0.1)\",\"colorScheme.background.empty.level100To250.pressed\":\"rgba(128, 128, 128, 0.2)\",\"colorScheme.background.empty.level300To350.hover\":\"rgba(255, 255, 255, 0.25)\",\"colorScheme.background.empty.level300To350.pressed\":\"rgba(255, 255, 255, 0.35)\",\"colorScheme.background.filled.level100To250.default\":\"rgba(128, 128, 128, 0.15)\",\"colorScheme.background.filled.level100To250.hover\":\"rgba(128, 128, 128, 0.25)\",\"colorScheme.background.filled.level100To250.focus\":\"rgba(128, 128, 128, 0.15)\",\"colorScheme.background.filled.level100To250.pressed\":\"rgba(128, 128, 128, 0.35)\",\"colorScheme.background.filled.level300To350.default\":\"rgba(255, 255, 255, 0.5)\",\"colorScheme.background.filled.level300To350.hover\":\"rgba(255, 255, 255, 0.7)\",\"colorScheme.background.filled.level300To350.focus\":\"rgba(255, 255, 255, 0.5)\",\"colorScheme.background.filled.level300To350.pressed\":\"#ffffff\",\"colorScheme.background.transparent\":\"rgba(128, 128, 128, 0)\",\"colorScheme.border.base\":\"rgba(128, 128, 128, 0.5)\",\"colorScheme.border.accent\":\"#0696d7\",\"colorScheme.border.on\":\"rgba(6, 150, 215, 0.5)\",\"colorScheme.border.transparent\":\"rgba(128, 128, 128, 0)\",\"colorScheme.halo.hover\":\"rgba(128, 128, 128, 0.15)\",\"colorScheme.halo.focus\":\"rgba(6, 150, 215, 0.35)\",\"colorScheme.halo.pressed\":\"rgba(128, 128, 128, 0.25)\",\"colorScheme.shadow.high\":\"rgba(0, 0, 0, 0.2)\",\"colorScheme.shadow.low\":\"rgba(0, 0, 0, 0.2)\",\"colorScheme.text.default\":\"#3c3c3c\",\"colorScheme.text.dim\":\"rgba(60, 60, 60, 0.7)\",\"colorScheme.text.placeholder\":\"rgba(60, 60, 60, 0.4)\",\"colorScheme.text.active\":\"#0696d7\",\"colorScheme.icon.default\":\"#808080\",\"colorScheme.icon.hover\":\"#3c3c3c\",\"colorScheme.icon.focus\":\"#808080\",\"colorScheme.icon.pressed\":\"#3c3c3c\",\"colorScheme.icon.on.default\":\"#0696d7\",\"colorScheme.icon.on.hover\":\"#006eaf\",\"colorScheme.icon.on.focus\":\"#0696d7\",\"colorScheme.icon.on.pressed\":\"#006eaf\",\"colorScheme.indicator.default\":\"rgba(102, 102, 102, 0.7)\",\"colorScheme.indicator.hover\":\"#666666\",\"colorScheme.indicator.focus\":\"rgba(102, 102, 102, 0.7)\",\"colorScheme.indicator.pressed\":\"#0696d7\",\"colorScheme.status.error\":\"#ec4a41\",\"colorScheme.status.success\":\"#87b340\",\"colorScheme.status.warning\":\"#faa21b\",\"colorScheme.status.info\":\"#0696d7\",\"colorScheme.status.read\":\"rgba(128, 128, 128, 0.4)\",\"colorScheme.divider.lightweight\":\"rgba(60, 60, 60, 0.1)\",\"colorScheme.divider.heavyweight\":\"rgba(60, 60, 60, 0.25)\",\"colorScheme.opacity.mask\":\"#e84396\",\"colorScheme.opacity.disabled\":0.4,\"colorScheme.accentColor\":\"#0696d7\",\"colorScheme.baseColor\":\"#808080\",\"colorScheme.surfaceLevel100Color\":\"#ffffff\",\"colorScheme.surfaceLevel200Color\":\"#f5f5f5\",\"colorScheme.surfaceLevel250Color\":\"#eeeeee\",\"colorScheme.surfaceLevel300Color\":\"#d9d9d9\",\"colorScheme.surfaceLevel350Color\":\"#cccccc\",\"colorScheme.highShadowColor\":\"rgba(0, 0, 0, 0.2)\",\"colorScheme.lowShadowColor\":\"rgba(0, 0, 0, 0.2)\",\"colorScheme.textColor\":\"#3c3c3c\",\"colorScheme.textColorDim\":\"rgba(60, 60, 60, 0.7)\",\"colorScheme.iconColor\":\"#808080\",\"colorScheme.errorColor\":\"#ec4a41\",\"colorScheme.successColor\":\"#87b340\",\"colorScheme.warningColor\":\"#faa21b\",\"colorScheme.infoColor\":\"#0696d7\",\"colorScheme.component.backgroundColor\":\"#ffffff\",\"density.spacings.extraExtraSmall\":\"4px\",\"density.spacings.extraSmall\":\"8px\",\"density.spacings.small\":\"12px\",\"density.spacings.medium\":\"16px\",\"density.spacings.large\":\"24px\",\"density.spacings.extraLarge\":\"32px\",\"density.spacings.extraExtraLarge\":\"48px\",\"density.fontSizes.small\":\"12px\",\"density.fontSizes.medium\":\"14px\",\"density.fontSizes.large\":\"20px\",\"density.fontSizes.extraLarge\":\"24px\",\"density.fontSizes.extraExtraLarge\":\"28px\",\"accordion.header.fontColor\":\"#3c3c3c\",\"accordion.header.fontFamily\":\"ArtifaktElement, sans-serif\",\"accordion.header.fontSize\":\"14px\",\"accordion.header.fontWeight\":700,\"accordion.header.lineHeight\":\"1.428571429\",\"accordion.header.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"accordion.header.paddingHorizontal\":\"8px\",\"accordion.header.paddingVertical\":\"8px\",\"accordion.header.indicatorGutter\":\"8px\",\"accordion.header.indicatorSize\":\"16px\",\"accordion.header.default.indicatorColor\":\"rgba(102, 102, 102, 0.7)\",\"accordion.header.hover.indicatorColor\":\"#666666\",\"accordion.header.indicatorRight.paddingLeft\":\"12px\",\"accordion.header.indicatorRight.paddingRight\":\"16px\",\"accordion.header.indicatorColor\":\"rgba(102, 102, 102, 0.7)\",\"avatar.fontWeight\":400,\"avatar.darkBackground.fontColor\":\"#ffffff\",\"avatar.lightBackground.fontColor\":\"#3c3c3c\",\"avatar.extraSmall.fontSize\":\"11px\",\"avatar.extraSmall.diameter\":\"16px\",\"avatar.small.fontSize\":\"12px\",\"avatar.small.diameter\":\"24px\",\"avatar.medium.fontSize\":\"14px\",\"avatar.medium.diameter\":\"32px\",\"avatar.large.fontSize\":\"24px\",\"avatar.large.diameter\":\"48px\",\"avatar.extraLarge.fontSize\":\"30px\",\"avatar.extraLarge.diameter\":\"64px\",\"avatar.color1.backgroundColor\":\"#6ac0e7\",\"avatar.color2.backgroundColor\":\"#1858a8\",\"avatar.color3.backgroundColor\":\"#5d822c\",\"avatar.color4.backgroundColor\":\"#ff9582\",\"avatar.color5.backgroundColor\":\"#7993b0\",\"avatar.color6.backgroundColor\":\"#5bc9bd\",\"avatar.color7.backgroundColor\":\"#faa21b\",\"avatar.color1.fontColor\":\"#3c3c3c\",\"avatar.color2.fontColor\":\"#ffffff\",\"avatar.color3.fontColor\":\"#ffffff\",\"avatar.color4.fontColor\":\"#3c3c3c\",\"avatar.color5.fontColor\":\"#ffffff\",\"avatar.color6.fontColor\":\"#3c3c3c\",\"avatar.color7.fontColor\":\"#3c3c3c\",\"avatar.fontColorOnDark\":\"#ffffff\",\"avatar.fontColorOnLight\":\"#3c3c3c\",\"avatarBundle.fontColor\":\"#3c3c3c\",\"avatarBundle.default.backgroundColor\":\"#eeeeee\",\"avatarBundle.hover.backgroundColor\":\"#eeeeee\",\"avatarBundle.pressed.backgroundColor\":\"#eeeeee\",\"avatarBundle.hover.haloColor\":\"rgba(128, 128, 128, 0.15)\",\"avatarBundle.pressed.haloColor\":\"rgba(128, 128, 128, 0.25)\",\"avatarBundle.hover.haloWidth\":\"2px\",\"avatarBundle.pressed.haloWidth\":\"4px\",\"banner.error.backgroundColor\":\"#ffefec\",\"banner.error.borderColor\":\"#f1a7a7\",\"banner.error.iconField.backgroundColor\":\"#ec4a41\",\"banner.info.backgroundColor\":\"#cdeaf7\",\"banner.info.borderColor\":\"#9bd5ef\",\"banner.info.iconField.backgroundColor\":\"#0696d7\",\"banner.success.backgroundColor\":\"#e7f2d9\",\"banner.success.borderColor\":\"#b7d78c\",\"banner.success.iconField.backgroundColor\":\"#87b340\",\"banner.warning.backgroundColor\":\"#feecd1\",\"banner.warning.borderColor\":\"#fcc776\",\"banner.warning.iconField.backgroundColor\":\"#faa21b\",\"banner.info.iconBackground\":\"#0696d7\",\"banner.success.iconBackground\":\"#87b340\",\"banner.warning.iconBackground\":\"#faa21b\",\"banner.error.iconBackground\":\"#ec4a41\",\"breadcrumb.label.fontColor\":\"#3c3c3c\",\"breadcrumb.label.fontFamily\":\"ArtifaktElement, sans-serif\",\"breadcrumb.label.fontSize\":\"14px\",\"breadcrumb.label.default.fontWeight\":400,\"breadcrumb.label.selected.fontWeight\":600,\"breadcrumb.default.indicatorColor\":\"rgba(102, 102, 102, 0.7)\",\"breadcrumb.selected.indicatorColor\":\"#0696d7\",\"breadcrumb.divider.fontColor\":\"rgba(60, 60, 60, 0.4)\",\"breadcrumb.divider.fontWeight\":600,\"button.borderRadius\":\"2px\",\"button.borderWidth\":\"1px\",\"button.minWidth\":\"10px\",\"button.paddingHorizontal\":\"16px\",\"button.paddingVertical\":\"8px\",\"button.gutterWidth\":\"4px\",\"button.label.fontFamily\":\"ArtifaktElement, sans-serif\",\"button.label.fontSize\":\"14px\",\"button.label.fontWeight\":600,\"button.label.lineHeight\":\"18px\",\"button.haloWidth\":\"2px\",\"button.hover.haloWidth\":\"2px\",\"button.pressed.haloWidth\":\"4px\",\"button.hover.haloColor\":\"rgba(128, 128, 128, 0.15)\",\"button.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"button.pressed.haloColor\":\"rgba(128, 128, 128, 0.25)\",\"button.solid.label.fontColor\":\"#ffffff\",\"button.solid.iconColor\":\"#ffffff\",\"button.solid.indicatorColor\":\"#ffffff\",\"button.solid.default.backgroundColor\":\"#0696d7\",\"button.solid.hover.backgroundColor\":\"#0696d7\",\"button.solid.focus.backgroundColor\":\"#0696d7\",\"button.solid.pressed.backgroundColor\":\"#0696d7\",\"button.solid.default.borderColor\":\"#0696d7\",\"button.solid.hover.borderColor\":\"#0696d7\",\"button.solid.focus.borderColor\":\"#0696d7\",\"button.solid.pressed.borderColor\":\"#0696d7\",\"button.outline.label.fontColor\":\"#3c3c3c\",\"button.outline.indicatorColor\":\"rgba(102, 102, 102, 0.7)\",\"button.outline.iconColor\":\"#808080\",\"button.outline.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.outline.hover.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.outline.focus.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.outline.pressed.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.outline.default.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"button.outline.hover.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"button.outline.focus.borderColor\":\"#0696d7\",\"button.outline.pressed.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"button.flat.label.fontColor\":\"#006eaf\",\"button.flat.iconColor\":\"#006eaf\",\"button.flat.indicatorColor\":\"#006eaf\",\"button.flat.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.hover.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.focus.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.pressed.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.default.borderColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.hover.borderColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.focus.borderColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.pressed.borderColor\":\"rgba(128, 128, 128, 0)\",\"button.fontWeight\":600,\"button.lineHeight\":\"18px\",\"button.fontFamily\":\"ArtifaktElement, sans-serif\",\"button.fontSize\":\"14px\",\"button.gutter\":\"4px\",\"button.horizontalPadding\":\"16px\",\"button.minimumWidth\":\"10px\",\"button.verticalPadding\":\"8px\",\"button.halo.width\":\"2px\",\"button.halo.color\":\"#808080\",\"button.hover.halo.width\":\"2px\",\"button.focus.halo.width\":\"2px\",\"button.pressed.halo.width\":\"4px\",\"button.focus.halo.color\":\"rgba(6, 150, 215, 0.35)\",\"button.pressed.halo.color\":\"rgba(128, 128, 128, 0.25)\",\"button.outline.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.outline.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"button.outline.focus.halo.color\":\"rgba(6, 150, 215, 0.35)\",\"button.outline.focus.icon.color\":\"#808080\",\"button.outline.focus.textColor\":\"#3c3c3c\",\"button.outline.halo.color\":\"#808080\",\"button.outline.hover.halo.color\":\"rgba(128, 128, 128, 0.15)\",\"button.outline.hover.icon.color\":\"#808080\",\"button.outline.hover.textColor\":\"#3c3c3c\",\"button.outline.icon.color\":\"#808080\",\"button.outline.pressed.halo.color\":\"rgba(128, 128, 128, 0.25)\",\"button.outline.pressed.haloWidth\":\"4px\",\"button.outline.pressed.icon.color\":\"#808080\",\"button.outline.pressed.textColor\":\"#3c3c3c\",\"button.outline.textColor\":\"#3c3c3c\",\"button.solid.backgroundColor\":\"#0696d7\",\"button.solid.borderColor\":\"#0696d7\",\"button.solid.halo.color\":\"#808080\",\"button.solid.textColor\":\"#ffffff\",\"button.solid.icon.color\":\"#ffffff\",\"button.solid.hover.halo.color\":\"rgba(128, 128, 128, 0.15)\",\"button.solid.hover.textColor\":\"#ffffff\",\"button.solid.hover.icon.color\":\"#ffffff\",\"button.solid.focus.halo.color\":\"rgba(6, 150, 215, 0.35)\",\"button.solid.focus.textColor\":\"#ffffff\",\"button.solid.focus.icon.color\":\"#ffffff\",\"button.solid.pressed.haloWidth\":\"4px\",\"button.solid.pressed.halo.color\":\"rgba(128, 128, 128, 0.25)\",\"button.solid.pressed.textColor\":\"#ffffff\",\"button.solid.pressed.icon.color\":\"#ffffff\",\"button.flat.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.borderColor\":\"rgba(128, 128, 128, 0)\",\"button.flat.halo.color\":\"#808080\",\"button.flat.textColor\":\"#006eaf\",\"button.flat.icon.color\":\"#006eaf\",\"button.flat.hover.halo.color\":\"rgba(128, 128, 128, 0.15)\",\"button.flat.hover.textColor\":\"#006eaf\",\"button.flat.hover.icon.color\":\"#006eaf\",\"button.flat.focus.halo.color\":\"rgba(6, 150, 215, 0.35)\",\"button.flat.focus.textColor\":\"#006eaf\",\"button.flat.focus.icon.color\":\"#006eaf\",\"button.flat.pressed.haloWidth\":\"4px\",\"button.flat.pressed.halo.color\":\"rgba(128, 128, 128, 0.25)\",\"button.flat.pressed.textColor\":\"#006eaf\",\"button.flat.pressed.icon.color\":\"#006eaf\",\"canvasFrame.label.fontColor\":\"#3c3c3c\",\"canvasFrame.label.fontFamily\":\"ArtifaktElement, sans-serif\",\"canvasFrame.label.fontSize\":\"14px\",\"canvasFrame.label.fontWeight\":600,\"canvasFrame.label.marginLeft\":\"12px\",\"canvasFrame.border.borderColor\":\"#0696d7\",\"canvasFrame.border.borderWidth\":\"2px\",\"canvasFrame.controlPanel.backgroundColor\":\"#ffffff\",\"canvasFrame.controlPanel.paddingRight\":\"12px\",\"canvasFrame.controlPanel.paddingLeft\":\"8px\",\"canvasFrame.controlPanel.paddingVertical\":\"8px\",\"canvasFrame.closeIcon.marginLeft\":\"12px\",\"checkbox.borderWidth\":\"1px\",\"checkbox.borderRadius\":\"0\",\"checkbox.halo.color\":\"rgba(128, 128, 128, 0.15)\",\"checkbox.halo.width\":\"2px\",\"checkbox.minHeight\":\"16px\",\"checkbox.height\":\"16px\",\"checkbox.indicatorColor\":\"#ffffff\",\"checkbox.iconColor\":\"#ffffff\",\"checkbox.minWidth\":\"16px\",\"checkbox.width\":\"16px\",\"checkbox.unchecked.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"checkbox.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"checkbox.unchecked.default.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"checkbox.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"checkbox.unchecked.hover.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"checkbox.hover.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"checkbox.hover.halo.width\":\"2px\",\"checkbox.hover.halo.color\":\"rgba(128, 128, 128, 0.15)\",\"checkbox.unchecked.focus.borderColor\":\"#0696d7\",\"checkbox.focus.borderColor\":\"#0696d7\",\"checkbox.focus.halo.color\":\"rgba(6, 150, 215, 0.35)\",\"checkbox.focus.halo.width\":\"2px\",\"checkbox.unchecked.pressed.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"checkbox.pressed.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"checkbox.pressed.halo.color\":\"rgba(128, 128, 128, 0.25)\",\"checkbox.pressed.halo.width\":\"4px\",\"checkbox.checked.default.borderColor\":\"#808080\",\"checkbox.checked.borderColor\":\"#808080\",\"checkbox.checked.iconColor\":\"#ffffff\",\"checkbox.checked.default.backgroundColor\":\"#808080\",\"checkbox.checked.backgroundColor\":\"#808080\",\"checkbox.indeterminate.borderColor\":\"#808080\",\"checkbox.indeterminate.iconColor\":\"#ffffff\",\"checkbox.indeterminate.backgroundColor\":\"#808080\",\"checkbox.focus.checked.backgroundColor\":\"#808080\",\"checkbox.focus.checked.borderColor\":\"#808080\",\"checkbox.focus.indeterminate.borderColor\":\"#808080\",\"checkbox.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"checkbox.hover.haloColor\":\"rgba(128, 128, 128, 0.15)\",\"checkbox.pressed.haloColor\":\"rgba(128, 128, 128, 0.25)\",\"checkbox.focus.haloWidth\":\"2px\",\"checkbox.hover.haloWidth\":\"2px\",\"checkbox.pressed.haloWidth\":\"4px\",\"checkbox.unchecked.hover.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"checkbox.unchecked.focus.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"checkbox.unchecked.pressed.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"checkbox.checked.hover.backgroundColor\":\"#808080\",\"checkbox.checked.hover.borderColor\":\"#808080\",\"checkbox.checked.pressed.backgroundColor\":\"#808080\",\"checkbox.checked.pressed.borderColor\":\"#808080\",\"component.disabled.opacity\":\"0.4\",\"datePicker.fontFamily\":\"ArtifaktElement, sans-serif\",\"datePicker.container.paddingHorizontal\":\"16px\",\"datePicker.container.paddingVertical\":\"24px\",\"datePicker.cell.minHeight\":\"36px\",\"datePicker.cell.minWidth\":\"36px\",\"datePicker.standardDate.fontColor\":\"#3c3c3c\",\"datePicker.standardDate.fontSize\":\"14px\",\"datePicker.standardDate.fontWeight\":400,\"datePicker.selectedDate.fontWeight\":600,\"datePicker.selectedDate.fontColor\":\"#ffffff\",\"datePicker.selectedDate.fontSize\":\"14px\",\"datePicker.notableDate.fontWeight\":400,\"datePicker.notableDate.fontColor\":\"#0696d7\",\"datePicker.notableDate.fontSize\":\"14px\",\"datePicker.restrictedDate.fontWeight\":400,\"datePicker.restrictedDate.fontColor\":\"rgba(60, 60, 60, 0.4)\",\"datePicker.restrictedDate.fontSize\":\"14px\",\"datePicker.day.fontWeight\":400,\"datePicker.day.fontColor\":\"#3c3c3c\",\"datePicker.day.fontSize\":\"14px\",\"datePicker.header.fontWeight\":600,\"datePicker.header.fontColor\":\"#3c3c3c\",\"datePicker.header.fontSize\":\"20px\",\"datePicker.header.paddingBottom\":\"24px\",\"datePicker.cell.borderColor\":\"#eeeeee\",\"datePicker.cell.borderWidth\":\"1px\",\"datePicker.standardDateCell.default.backgroundColor\":\"#ffffff\",\"datePicker.standardDateCell.hover.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"datePicker.standardDateCell.focus.backgroundColor\":\"#ffffff\",\"datePicker.standardDateCell.focus.borderColor\":\"#0696d7\",\"datePicker.standardDateCell.focus.borderWidth\":\"1px\",\"datePicker.standardDateCell.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"datePicker.standardDateCell.focus.haloWidth\":\"2px\",\"datePicker.standardDateCell.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"datePicker.selectedDateCell.default.backgroundColor\":\"#0696d7\",\"datePicker.selectedDateCell.focus.backgroundColor\":\"#0696d7\",\"datePicker.selectedDateCell.focus.haloColor\":\"rgba(60, 60, 60, 0.25)\",\"datePicker.selectedDateCell.focus.haloWidth\":\"2px\",\"datePicker.rangeDateCell.default.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"datePicker.rangeDateCell.hover.backgroundColor\":\"rgba(155, 213, 239, 0.7)\",\"datePicker.rangeDateCell.focus.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"datePicker.rangeDateCell.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"datePicker.rangeDateCell.focus.haloWidth\":\"2px\",\"datePicker.rangeDateCell.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"datePicker.currentDateCell.default.backgroundColor\":\"#ffffff\",\"datePicker.currentDateCell.default.borderColor\":\"#0696d7\",\"datePicker.currentDateCell.default.borderWidth\":\"1px\",\"datePicker.currentDateCell.hover.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"datePicker.currentDateCell.hover.borderColor\":\"#0696d7\",\"datePicker.currentDateCell.hover.borderWidth\":\"1px\",\"datePicker.currentDateCell.focus.backgroundColor\":\"#ffffff\",\"datePicker.currentDateCell.focus.borderColor\":\"#0696d7\",\"datePicker.currentDateCell.focus.borderWidth\":\"1px\",\"datePicker.currentDateCell.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"datePicker.currentDateCell.focus.haloWidth\":\"2px\",\"datePicker.currentDateCell.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"datePicker.currentDateCell.pressed.borderColor\":\"#0696d7\",\"datePicker.currentDateCell.pressed.borderWidth\":\"1px\",\"divider.borderWidth\":\"1px\",\"divider.lightColor\":\"rgba(60, 60, 60, 0.1)\",\"divider.heavyColor\":\"rgba(60, 60, 60, 0.25)\",\"divider.width\":\"1px\",\"flyout.backgroundColor\":\"#ffffff\",\"flyout.borderColor\":\"rgba(128, 128, 128, 0)\",\"flyout.borderRadius\":\"4px\",\"flyout.shadowBlur\":\"16px\",\"flyout.shadowColor\":\"rgba(0, 0, 0, 0.2)\",\"flyout.nub.hypotenuseLength\":\"16px\",\"formField.instructionalText.fontSize\":\"14px\",\"formField.instructionalText.fontWeight\":600,\"formField.instructionalText.fontFamily\":\"ArtifaktElement, sans-serif\",\"formField.instructionalText.fontColor\":\"rgba(60, 60, 60, 0.4)\",\"formField.instructionalText.lineHeight\":\"1.4\",\"formField.icon.width\":\"24px\",\"formField.icon.paddingRight\":\"12px\",\"formField.icon.color\":\"rgba(102, 102, 102, 0.7)\",\"iconButton.borderRadius\":\"2px\",\"iconButton.borderWidth\":\"0\",\"iconButton.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"iconButton.focus.haloWidth\":\"2px\",\"iconButton.dynamic.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.dynamic.hover.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.dynamic.focus.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.dynamic.default.iconColor\":\"#808080\",\"iconButton.dynamic.hover.iconColor\":\"#3c3c3c\",\"iconButton.dynamic.focus.iconColor\":\"#808080\",\"iconButton.dynamic.pressed.iconColor\":\"#3c3c3c\",\"iconButton.dynamic.on.default.iconColor\":\"#0696d7\",\"iconButton.dynamic.on.hover.iconColor\":\"#006eaf\",\"iconButton.dynamic.on.focus.iconColor\":\"#0696d7\",\"iconButton.dynamic.on.pressed.iconColor\":\"#006eaf\",\"iconButton.dynamic.pressed.100To250BackgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"iconButton.dynamic.pressed.300To350BackgroundColor\":\"rgba(255, 255, 255, 0.35)\",\"iconButton.static.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.static.focus.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.static.default.borderColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.static.hover.borderColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.static.focus.borderColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.static.pressed.borderColor\":\"rgba(128, 128, 128, 0)\",\"iconButton.static.on.default.backgroundColor\":\"rgba(155, 213, 239, 0.7)\",\"iconButton.static.on.hover.backgroundColor\":\"rgba(155, 213, 239, 0.7)\",\"iconButton.static.on.focus.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"iconButton.static.on.pressed.backgroundColor\":\"rgba(106, 192, 231, 0.7)\",\"iconButton.static.on.default.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"iconButton.static.on.hover.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"iconButton.static.on.focus.borderColor\":\"#0696d7\",\"iconButton.static.on.pressed.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"iconButton.static.hover.100To250BackgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"iconButton.static.pressed.100To250BackgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"iconButton.static.hover.300To350BackgroundColor\":\"rgba(255, 255, 255, 0.25)\",\"iconButton.static.pressed.300To350BackgroundColor\":\"rgba(255, 255, 255, 0.35)\",\"iconButton.static.on.hover.100To250BackgroundColor\":\"rgba(6, 150, 215, 0.35)\",\"iconButton.static.on.hover.300To350BackgroundColor\":\"rgba(6, 150, 215, 0.35)\",\"iconButton.focus.halo.color\":\"rgba(6, 150, 215, 0.35)\",\"iconButton.focus.halo.width\":\"2px\",\"iconButton.focus.iconColor\":\"#808080\",\"iconButton.focus.on.iconColor\":\"#0696d7\",\"iconButton.hover.iconColor\":\"#3c3c3c\",\"iconButton.hover.on.iconColor\":\"#006eaf\",\"iconButton.iconColor\":\"#808080\",\"iconButton.on.iconColor\":\"#0696d7\",\"iconButton.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"iconButton.pressed.iconColor\":\"#3c3c3c\",\"iconButton.pressed.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"iconButton.pressed.level300To350.backgroundColor\":\"rgba(255, 255, 255, 0.35)\",\"iconButton.pressed.on.iconColor\":\"#006eaf\",\"iconButton.static.hover.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"iconButton.static.hover.level300To350.backgroundColor\":\"rgba(255, 255, 255, 0.25)\",\"iconButton.static.on.backgroundColor\":\"rgba(155, 213, 239, 0.7)\",\"iconButton.static.on.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"iconButton.static.on.hover.level100To250.backgroundColor\":\"rgba(6, 150, 215, 0.35)\",\"iconButton.static.on.hover.level300To350.backgroundColor\":\"rgba(6, 150, 215, 0.35)\",\"input.borderWidth\":\"1px\",\"input.borderBottomWidth\":\"1px\",\"input.borderColor\":\"rgba(128, 128, 128, 0.2)\",\"input.borderRadius\":\"0\",\"input.haloWidth\":\"4px\",\"input.minHeight\":\"36px\",\"input.paddingVertical\":\"4px\",\"input.gutterWidth\":\"8px\",\"input.disabled.opacity\":0.4,\"input.error.borderBottomColor\":\"#ec4a41\",\"input.hover.haloColor\":\"rgba(128, 128, 128, 0.15)\",\"input.focus.haloColor\":\"rgba(6, 150, 215, 0.25)\",\"input.pressed.haloColor\":\"rgba(128, 128, 128, 0.25)\",\"input.value.fontColor\":\"#3c3c3c\",\"input.value.fontFamily\":\"ArtifaktElement, sans-serif\",\"input.value.fontSize\":\"14px\",\"input.value.fontWeight\":600,\"input.value.highlightColor\":\"rgba(128, 128, 128, 0.2)\",\"input.value.lineHeight\":\"1.4\",\"input.value.placeholderFontColor\":\"rgba(60, 60, 60, 0.4)\",\"input.indicator.default\":\"rgba(102, 102, 102, 0.7)\",\"input.indicator.focus\":\"rgba(102, 102, 102, 0.7)\",\"input.indicator.hover\":\"#666666\",\"input.indicator.pressed\":\"#0696d7\",\"input.line.paddingHorizontal\":\"0\",\"input.line.default.borderBottomColor\":\"rgba(128, 128, 128, 0.35)\",\"input.line.hover.borderBottomColor\":\"#808080\",\"input.line.focus.borderBottomColor\":\"#0696d7\",\"input.line.pressed.borderBottomColor\":\"rgba(128, 128, 128, 0.85)\",\"input.box.paddingHorizontal\":\"12px\",\"input.box.default.backgroundColor\":\"#ffffff\",\"input.box.hover.backgroundColor\":\"#ffffff\",\"input.box.focus.backgroundColor\":\"#ffffff\",\"input.box.pressed.backgroundColor\":\"#ffffff\",\"input.box.default.borderColor\":\"rgba(128, 128, 128, 0.2)\",\"input.box.hover.borderColor\":\"rgba(128, 128, 128, 0.2)\",\"input.box.focus.borderColor\":\"rgba(128, 128, 128, 0.2)\",\"input.box.pressed.borderColor\":\"rgba(128, 128, 128, 0.2)\",\"input.box.default.borderBottomColor\":\"rgba(255, 255, 255, 0.2)\",\"input.box.hover.borderBottomColor\":\"#808080\",\"input.box.focus.borderBottomColor\":\"#0696d7\",\"input.box.pressed.borderBottomColor\":\"rgba(128, 128, 128, 0.85)\",\"input.textArea.line.borderColor\":\"rgba(128, 128, 128, 0.2)\",\"input.active.halo.color\":\"rgba(6, 150, 215, 0.25)\",\"input.borderBottomColor\":\"rgba(128, 128, 128, 0.35)\",\"input.active.halo.width\":\"4px\",\"input.box.backgroundColor\":\"#ffffff\",\"input.box.borderBottomColor\":\"rgba(255, 255, 255, 0.2)\",\"input.boxType.horizontalPadding\":\"12px\",\"input.focus.borderBottomColor\":\"#0696d7\",\"input.focus.halo.color\":\"rgba(6, 150, 215, 0.25)\",\"input.focus.halo.width\":\"4px\",\"input.fontColor\":\"#3c3c3c\",\"input.fontFamily\":\"ArtifaktElement, sans-serif\",\"input.fontSize\":\"14px\",\"input.fontWeight\":600,\"input.halo.color\":\"rgba(128, 128, 128, 0.15)\",\"input.halo.width\":\"4px\",\"input.height\":\"36px\",\"input.highlightColor\":\"rgba(128, 128, 128, 0.2)\",\"input.horizontalPadding\":\"0\",\"input.hover.borderBottomColor\":\"#808080\",\"input.hover.halo.width\":\"4px\",\"input.lineHeight\":\"1.4\",\"input.placeholder.fontColor\":\"rgba(60, 60, 60, 0.4)\",\"input.verticalPadding\":\"4px\",\"label.fontFamily\":\"ArtifaktElement, sans-serif\",\"label.fontSize\":\"14px\",\"label.fontColor\":\"rgba(60, 60, 60, 0.9)\",\"label.hover.fontColor\":\"rgba(60, 60, 60, 0.9)\",\"label.fontWeight\":400,\"label.lineHeight\":\"1.4\",\"label.top.fontSize\":\"12px\",\"menu.backgroundColor\":\"#ffffff\",\"menu.borderRadius\":\"0\",\"menu.topFlushBorderTopRadius\":\"0\",\"menu.item.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"menu.item.paddingHorizontal\":\"8px\",\"menu.item.paddingVertical\":\"12px\",\"menu.item.minHeight\":\"24px\",\"menu.item.gutterWidth\":\"12px\",\"menu.item.hover.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"menu.item.focus.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"menu.item.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"menu.item.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"menu.item.active.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"menu.item.horizontalPadding\":\"8px\",\"menu.item.verticalPadding\":\"12px\",\"modal.fontColor\":\"#3c3c3c\",\"modal.paddingHorizontal\":\"24px\",\"modal.overlayColor\":\"rgba(0, 0, 0, 0.5)\",\"modal.header.backgroundColor\":\"#ffffff\",\"modal.header.borderBottomColor\":\"rgba(60, 60, 60, 0.25)\",\"modal.header.minHeight\":\"60px\",\"modal.body.minHeight\":\"152px\",\"modal.shell.backgroundColor\":\"#ffffff\",\"modal.shell.borderRadius\":\"4px\",\"modal.shell.minHeight\":\"300px\",\"modal.shell.minWidth\":\"400px\",\"modal.shell.shadowColor\":\"rgba(0, 0, 0, 0.2)\",\"modal.overlay.color\":\"rgba(0, 0, 0, 0.5)\",\"modal.textColor\":\"#3c3c3c\",\"modal.body.height\":\"152px\",\"modal.horizontalPadding\":\"24px\",\"modal.header.borderColor\":\"rgba(60, 60, 60, 0.25)\",\"modal.header.height\":\"60px\",\"modal.window.backgroundColor\":\"#ffffff\",\"modal.window.borderRadius\":\"4px\",\"modal.window.height\":\"300px\",\"modal.window.width\":\"400px\",\"modal.window.shadowColor\":\"rgba(0, 0, 0, 0.2)\",\"notifications.borderRadius\":\"4px\",\"notifications.fontColor\":\"#3c3c3c\",\"notifications.fontFamily\":\"ArtifaktElement, sans-serif\",\"notifications.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"notifications.hover.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"notifications.container.backgroundColor\":\"#ffffff\",\"notifications.container.borderColor\":\"rgba(128, 128, 128, 0)\",\"notifications.container.shadowBlur\":\"4px\",\"notifications.container.shadowColor\":\"rgba(0, 0, 0, 0.2)\",\"notifications.statusIndicator.width\":\"3px\",\"notifications.header.fontSize\":\"14px\",\"notifications.header.fontWeight\":700,\"notifications.header.lineHeight\":\"20px\",\"notifications.toast.object.fontSize\":\"12px\",\"notifications.toast.object.fontWeight\":700,\"notifications.toast.object.lineHeight\":\"16px\",\"notifications.toast.description.fontSize\":\"12px\",\"notifications.toast.description.fontWeight\":400,\"notifications.toast.description.lineHeight\":\"16px\",\"progress.bar.backgroundColor\":\"rgba(0, 0, 0, 0.05)\",\"progress.bar.highlightColor\":\"#38abdf\",\"progress.bar.borderRadius\":\"2px\",\"progress.bar.minHeight\":\"4px\",\"progressBar.backgroundColor\":\"rgba(0, 0, 0, 0.05)\",\"progressBar.highlightColor\":\"#38abdf\",\"progressBar.borderRadius\":\"2px\",\"progressBar.height\":\"4px\",\"progress.ring.backgroundColor\":\"rgba(0, 0, 0, 0.05)\",\"progress.ring.highlightColor1\":\"#0686c2\",\"progress.ring.highlightColor2\":\"#38abdf\",\"progressRing.backgroundColor\":\"rgba(0, 0, 0, 0.05)\",\"progressRing.highlightColor1\":\"#0686c2\",\"progressRing.highlightColor2\":\"#38abdf\",\"scrollbar.radii\":\"1000em\",\"scrollbar.height\":\"5px\",\"scrollbar.default.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"scrollbar.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.5)\",\"segmentedButton.borderRadius\":\"2px\",\"segmentedButton.borderWidth\":\"1px\",\"segmentedButton.gutterWidth\":\"4px\",\"segmentedButton.minWidth\":\"10px\",\"segmentedButton.paddingHorizontal\":\"16px\",\"segmentedButton.paddingVertical\":\"8px\",\"segmentedButton.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"segmentedButton.hover.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"segmentedButton.focus.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"segmentedButton.pressed.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"segmentedButton.default.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"segmentedButton.hover.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"segmentedButton.focus.borderColor\":\"#0696d7\",\"segmentedButton.pressed.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"segmentedButton.hover.haloWidth\":\"2px\",\"segmentedButton.focus.haloWidth\":\"2px\",\"segmentedButton.pressed.haloWidth\":\"4px\",\"segmentedButton.hover.haloColor\":\"rgba(128, 128, 128, 0.15)\",\"segmentedButton.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"segmentedButton.pressed.haloColor\":\"rgba(128, 128, 128, 0.25)\",\"segmentedButton.default.iconColor\":\"#808080\",\"segmentedButton.active.iconColor\":\"#eeeeee\",\"segmentedButton.active.backgroundColor\":\"#808080\",\"segmentedButton.label.fontSize\":\"14px\",\"segmentedButton.label.default.fontColor\":\"#3c3c3c\",\"segmentedButton.label.default.fontWeight\":600,\"segmentedButton.label.active.fontColor\":\"#f5f5f5\",\"segmentedButton.label.active.fontWeight\":700,\"sideNav.fontFamily\":\"ArtifaktElement, sans-serif\",\"sideNav.container.backgroundColor\":\"#ffffff\",\"sideNav.account.fontColor\":\"#3c3c3c\",\"sideNav.account.fontSize\":\"12px\",\"sideNav.account.fontWeight\":500,\"sideNav.account.lineHeight\":\"1.428571429\",\"sideNav.project.fontColor\":\"#3c3c3c\",\"sideNav.project.fontSize\":\"14px\",\"sideNav.project.fontWeight\":700,\"sideNav.project.lineHeight\":\"1.428571429\",\"sideNav.module.default.fontColor\":\"#3c3c3c\",\"sideNav.module.default.fontSize\":\"14px\",\"sideNav.module.default.fontWeight\":700,\"sideNav.module.default.lineHeight\":\"1.428571429\",\"sideNav.module.active.fontColor\":\"#0696d7\",\"sideNav.module.active.fontSize\":\"14px\",\"sideNav.module.active.fontWeight\":700,\"sideNav.module.active.lineHeight\":\"1.428571429\",\"sideNav.subModule.default.fontColor\":\"#3c3c3c\",\"sideNav.subModule.default.fontSize\":\"14px\",\"sideNav.subModule.default.fontWeight\":400,\"sideNav.subModule.default.lineHeight\":\"1.428571429\",\"sideNav.subModule.active.fontColor\":\"#0696d7\",\"sideNav.subModule.active.fontSize\":\"14px\",\"sideNav.subModule.active.fontWeight\":400,\"sideNav.subModule.active.lineHeight\":\"1.428571429\",\"sideNav.legal.fontColor\":\"#3c3c3c\",\"sideNav.legal.fontSize\":\"12px\",\"sideNav.legal.fontWeight\":500,\"sideNav.legal.lineHeight\":\"1.5\",\"sideNav.icon.defaultColor\":\"#808080\",\"sideNav.icon.activeColor\":\"#0696d7\",\"sideNav.activeIndicator.backgroundColor\":\"#0696d7\",\"skeletonItem.borderRadius\":\"2px\",\"skeletonItem.minHeight\":\"16px\",\"skeletonItem.backgroundColor\":\"rgba(0, 0, 0, 0.06)\",\"skeletonItem.highlightColor\":\"rgba(255, 255, 255, 0.3)\",\"skeletonItem.defaultHeight\":\"16px\",\"slider.hover.haloColor\":\"rgba(128, 128, 128, 0.15)\",\"slider.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"slider.pressed.haloColor\":\"rgba(128, 128, 128, 0.25)\",\"slider.hover.haloWidth\":\"2px\",\"slider.focus.haloWidth\":\"2px\",\"slider.pressed.haloWidth\":\"4px\",\"slider.thumb.backgroundColor\":\"#808080\",\"slider.thumb.minWidth\":\"6px\",\"slider.thumb.minHeight\":\"20px\",\"slider.track.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"slider.track.minHeight\":\"2px\",\"slider.value.backgroundColor\":\"#808080\",\"slider.inputOverlay.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"slider.halo.width\":0,\"slider.halo.color\":\"rgba(128, 128, 128, 0.15)\",\"slider.thumb.width\":\"6px\",\"slider.focused.halo.color\":\"rgba(6, 150, 215, 0.35)\",\"slider.focused.halo.width\":\"2px\",\"slider.focused.thumb.color\":\"#808080\",\"slider.hover.halo.color\":\"rgba(128, 128, 128, 0.15)\",\"slider.hover.halo.width\":\"2px\",\"slider.hover.thumb.color\":\"#808080\",\"slider.pressed.halo.color\":\"rgba(128, 128, 128, 0.25)\",\"slider.pressed.halo.width\":\"4px\",\"slider.pressed.thumb.color\":\"#808080\",\"slider.track.color\":\"rgba(128, 128, 128, 0.2)\",\"slider.track.width\":\"2px\",\"slider.value.color\":\"#808080\",\"stepIndicator.fontFamily\":\"ArtifaktElement, sans-serif\",\"stepIndicator.borderWidth\":\"2px\",\"stepIndicator.minDiameter\":\"22px\",\"stepIndicator.hover.haloColor\":\"rgba(128, 128, 128, 0.15)\",\"stepIndicator.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"stepIndicator.pressed.haloColor\":\"rgba(128, 128, 128, 0.25)\",\"stepIndicator.hover.haloWidth\":\"2px\",\"stepIndicator.focus.haloWidth\":\"2px\",\"stepIndicator.pressed.haloWidth\":\"4px\",\"stepIndicator.incomplete.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"stepIndicator.incomplete.borderColor\":\"#dcdcdc\",\"stepIndicator.inProgress.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"stepIndicator.inProgress.borderColor\":\"#0696d7\",\"stepIndicator.completed.backgroundColor\":\"#0696d7\",\"stepIndicator.completed.iconColor\":\"#ffffff\",\"stepIndicator.accordion.paddingHorizontal\":\"16px\",\"stepIndicator.accordion.paddingVertical\":\"24px\",\"stepIndicator.connector.minWidth\":\"2px\",\"stepIndicator.connector.default.backgroundColor\":\"#dcdcdc\",\"stepIndicator.connector.active.backgroundColor\":\"#0696d7\",\"stepIndicator.label.fontColor\":\"#3c3c3c\",\"stepIndicator.label.default.fontWeight\":400,\"stepIndicator.label.horizontal.fontSize\":\"12px\",\"stepIndicator.label.horizontal.marginBottom\":\"12px\",\"stepIndicator.label.horizontal.marginTop\":\"12px\",\"stepIndicator.label.vertical.fontSize\":\"14px\",\"stepIndicator.label.vertical.marginLeft\":\"12px\",\"stepIndicator.label.inProgress.fontWeight\":700,\"stepIndicator.number.fontSize\":\"14px\",\"stepIndicator.number.inProgress.fontColor\":\"#0696d7\",\"stepIndicator.number.inProgress.fontWeight\":700,\"stepIndicator.number.incomplete.fontColor\":\"rgba(60, 60, 60, 0.4)\",\"stepIndicator.number.incomplete.fontWeight\":600,\"table.fontColor\":\"#3c3c3c\",\"table.fontFamily\":\"ArtifaktElement, sans-serif\",\"table.fonSize\":\"14px\",\"table.lineHeight\":\"1.428571429\",\"table.title.fontWeight\":700,\"table.item.fontWeight\":400,\"table.row.borderBottomColor\":\"rgba(60, 60, 60, 0.1)\",\"table.row.hover.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"table.row.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"table.row.selected.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"table.header.borderTopColor\":\"rgba(60, 60, 60, 0.1)\",\"table.header.borderBottomColor\":\"rgba(60, 60, 60, 0.25)\",\"tabs.focus.haloWidth\":\"2px\",\"tabs.focus.haloColor\":\"rgba(6, 150, 215, 0.5)\",\"tabs.label.fontColor\":\"#3c3c3c\",\"tabs.label.fontFamily\":\"ArtifaktElement, sans-serif\",\"tabs.label.fontSize\":\"14px\",\"tabs.label.inactive.fontWeight\":400,\"tabs.label.active.fontWeight\":700,\"tabs.label.active.lineHeight\":\"1.428571429\",\"tabs.iconSize\":\"24px\",\"tabs.iconColor\":\"#808080\",\"tabs.iconGutter\":\"12px\",\"tabs.closeButton.minSize\":\"16px\",\"tabs.closeButton.gutter\":\"12px\",\"tabs.closeButton.default.iconColor\":\"#808080\",\"tabs.closeButton.hover.iconColor\":\"#3c3c3c\",\"tabs.closeButton.pressed.iconColor\":\"#3c3c3c\",\"tabs.underline.wrapper.borderBottomColor\":\"rgba(128, 128, 128, 0.35)\",\"tabs.underline.wrapper.borderBottomWidth\":\"1px\",\"tabs.underline.tab.gutter\":\"24px\",\"tabs.underline.tab.paddingBottom\":\"4px\",\"tabs.underline.hover.haloColor\":\"rgba(128, 128, 128, 0.85)\",\"tabs.underline.active.haloColor\":\"#0696d7\",\"tabs.box.wrapper.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"tabs.box.tab.paddingHorizontal\":\"12px\",\"tabs.box.tab.paddingVertical\":\"8px\",\"tabs.box.tab.active.backgroundColor\":\"#ffffff\",\"tabs.box.tab.hover.backgroundColor\":\"rgba(255, 255, 255, 0.5)\",\"tabs.box.divider.borderColor\":\"rgba(60, 60, 60, 0.25)\",\"tabs.box.dividerWidth\":\"1px\",\"tabs.box.dividerHeight\":\"18px\",\"tabs.canvas.wrapper.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"tabs.canvas.tab.paddingHorizontal\":\"16px\",\"tabs.canvas.tab.paddingVertical\":\"8px\",\"tabs.canvas.tab.hover.backgroundColor\":\"rgba(245, 245, 245, 0.5)\",\"tabs.canvas.tab.active.backgroundColor\":\"#f5f5f5\",\"tabs.canvas.divider.borderColor\":\"rgba(60, 60, 60, 0.25)\",\"tabs.canvas.dividerWidth\":\"1px\",\"tabs.canvas.dividerHeight\":\"18px\",\"tabs.general.tab.fontFamily\":\"ArtifaktElement, sans-serif\",\"tabs.general.tab.fontSize\":\"14px\",\"tabs.general.tab.fontWeight\":400,\"tabs.general.tab.active.fontWeight\":700,\"tabs.general.tab.color\":\"#3c3c3c\",\"tabs.general.tab.lineHeight\":\"1.428571429\",\"tabs.general.halo.size\":\"2px\",\"tabs.general.halo.color\":\"rgba(6, 150, 215, 0.5)\",\"tabs.general.icon.size\":\"24px\",\"tabs.general.icon.color\":\"#808080\",\"tabs.general.icon.gutter\":\"12px\",\"tabs.general.closeButton.size\":\"16px\",\"tabs.general.closeButton.gutter\":\"12px\",\"tabs.general.closeButton.color\":\"#808080\",\"tabs.general.closeButton.hover.color\":\"#3c3c3c\",\"tabs.general.closeButton.pressed.color\":\"#3c3c3c\",\"tabs.underline.halo.hover.color\":\"rgba(128, 128, 128, 0.85)\",\"tabs.underline.halo.active.color\":\"#0696d7\",\"tabs.box.tab.horizontalPadding\":\"12px\",\"tabs.box.tab.verticalPadding\":\"8px\",\"tabs.box.divider.color\":\"rgba(60, 60, 60, 0.25)\",\"tabs.box.divider.width\":\"1px\",\"tabs.box.divider.height\":\"18px\",\"tabs.canvas.tab.horizontalPadding\":\"16px\",\"tabs.canvas.tab.verticalPadding\":\"8px\",\"tabs.canvas.divider.color\":\"rgba(60, 60, 60, 0.25)\",\"tabs.canvas.divider.width\":\"1px\",\"tabs.canvas.divider.height\":\"18px\",\"tag.borderWidth\":\"1px\",\"tag.borderRadii\":\"1000em\",\"tag.hover.haloWidth\":\"2px\",\"tag.focus.haloWidth\":\"4px\",\"tag.paddingLeft\":\"12px\",\"tag.paddingRight\":\"8px\",\"tag.paddingVertical\":\"8px\",\"tag.default.backgroundColor\":\"rgba(128, 128, 128, 0.15)\",\"tag.hover.backgroundColor\":\"rgba(128, 128, 128, 0.25)\",\"tag.focus.backgroundColor\":\"rgba(128, 128, 128, 0.15)\",\"tag.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.35)\",\"tag.default.borderColor\":\"rgba(128, 128, 128, 0)\",\"tag.hover.borderColor\":\"rgba(128, 128, 128, 0)\",\"tag.focus.borderColor\":\"rgba(128, 128, 128, 0)\",\"tag.pressed.borderColor\":\"rgba(128, 128, 128, 0)\",\"tag.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"tag.on.default.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"tag.on.hover.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"tag.on.focus.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"tag.on.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.7)\",\"tag.on.default.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"tag.on.hover.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"tag.on.focus.borderColor\":\"#0696d7\",\"tag.on.pressed.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"tag.on.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"tag.label.marginRight\":\"8px\",\"tag.label.fontColor\":\"#3c3c3c\",\"tag.label.fontFamily\":\"ArtifaktElement, sans-serif\",\"tag.label.fontSize\":\"14px\",\"tag.label.fontWeight\":600,\"tag.closeButton.iconColor\":\"#808080\",\"textArea.minHeight\":\"250px\",\"textArea.borderColor\":\"rgba(128, 128, 128, 0.2)\",\"textLink.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"textLink.focus.haloWidth\":\"2px\",\"textLink.primary.default.fontColor\":\"#006eaf\",\"textLink.primary.hover.fontColor\":\"#006eaf\",\"textLink.primary.focus.fontColor\":\"#006eaf\",\"textLink.primary.hover.underlineColor\":\"#006eaf\",\"textLink.secondary.default.fontColor\":\"#3c3c3c\",\"textLink.secondary.hover.fontColor\":\"#3c3c3c\",\"textLink.secondary.focus.fontColor\":\"#3c3c3c\",\"textLink.secondary.hover.underlineColor\":\"#3c3c3c\",\"textLink.textColor\":\"#006eaf\",\"textLink.hover.textColor\":\"#006eaf\",\"textLink.focus.textColor\":\"#006eaf\",\"textLink.hover.underline.color\":\"#006eaf\",\"textLink.focus.halo.color\":\"rgba(6, 150, 215, 0.35)\",\"textLink.focus.halo.width\":\"2px\",\"tile.borderRadii\":\"0\",\"tile.borderWidth\":\"1px\",\"tile.haloWidth\":\"4px\",\"tile.padding\":\"12px\",\"tile.fontColor\":\"#3c3c3c\",\"tile.fontFamily\":\"ArtifaktElement, sans-serif\",\"tile.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"tile.thumbnail.marginRight\":\"12px\",\"tile.title.fontSize\":\"14px\",\"tile.title.fontWeight\":600,\"tile.title.lineHeight\":\"1.333333333\",\"tile.title.marginBottom\":\"8px\",\"tile.subTitle.fontSize\":\"12px\",\"tile.subTitle.fontWeight\":400,\"tile.subTitle.lineHeight\":\"1.272727273\",\"tile.default.level300To350BackgroundColor\":\"rgba(255, 255, 255, 0.5)\",\"tile.hover.level300To350BackgroundColor\":\"rgba(255, 255, 255, 0.7)\",\"tile.focus.level300To350BackgroundColor\":\"rgba(255, 255, 255, 0.5)\",\"tile.pressed.level300To350BackgroundColor\":\"rgba(255, 255, 255, 0.9)\",\"tile.selected.default.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"tile.selected.hover.backgroundColor\":\"rgba(155, 213, 239, 0.7)\",\"tile.selected.focus.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"tile.selected.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"tile.selected.default.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"tile.selected.hover.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"tile.selected.focus.borderColor\":\"#0696d7\",\"tile.selected.pressed.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"tile.filled.default.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0.15)\",\"tile.filled.hover.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0.25)\",\"tile.filled.focus.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0.15)\",\"tile.filled.pressed.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0.35)\",\"tile.filled.default.level100To250.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.filled.hover.level100To250.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.filled.focus.level100To250.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.filled.pressed.level100To250.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.filled.default.level300To350.backgroundColor\":\"rgba(255, 255, 255, 0.5)\",\"tile.filled.hover.level300To350.backgroundColor\":\"rgba(255, 255, 255, 0.7)\",\"tile.filled.focus.level300To350.backgroundColor\":\"rgba(255, 255, 255, 0.5)\",\"tile.filled.pressed.level300To350.backgroundColor\":\"#ffffff\",\"tile.filled.default.level300To350.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.filled.hover.level300To350.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.filled.focus.level300To350.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.filled.pressed.level300To350.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.default.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.hover.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0.1)\",\"tile.empty.focus.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.pressed.level100To250.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"tile.empty.default.level100To250.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.hover.level100To250.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.focus.level100To250.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.pressed.level100To250.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.default.level300To350.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.hover.level300To350.backgroundColor\":\"rgba(255, 255, 255, 0.25)\",\"tile.empty.focus.level300To350.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.pressed.level300To350.backgroundColor\":\"rgba(255, 255, 255, 0.35)\",\"tile.empty.default.level300To350.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.hover.level300To350.borderColor\":\"rgba(255, 255, 255, 0.25)\",\"tile.empty.focus.level300To350.borderColor\":\"rgba(128, 128, 128, 0)\",\"tile.empty.pressed.level300To350.borderColor\":\"rgba(255, 255, 255, 0.35)\",\"timestamp.fontColor\":\"rgba(60, 60, 60, 0.7)\",\"timestamp.fontFamily\":\"ArtifaktElement, sans-serif\",\"timestamp.fontSize\":\"12px\",\"timestamp.fontWeight\":400,\"timestamp.lineHeight\":\"20px\",\"thumbnail.backgroundColor\":\"#eeeeee\",\"thumbnail.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"toggle.borderRadii\":\"1000em\",\"toggle.borderWidth\":\"1px\",\"toggle.minHeight\":\"20px\",\"toggle.minWidth\":\"37px\",\"toggle.padding\":\"4px\",\"toggle.hover.haloColor\":\"rgba(128, 128, 128, 0.15)\",\"toggle.focus.haloColor\":\"rgba(6, 150, 215, 0.35)\",\"toggle.pressed.haloColor\":\"rgba(128, 128, 128, 0.25)\",\"toggle.hover.haloWidth\":\"2px\",\"toggle.focus.haloWidth\":\"2px\",\"toggle.pressed.haloWidth\":\"4px\",\"toggle.off.default.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"toggle.off.default.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"toggle.off.hover.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"toggle.off.focus.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"toggle.off.pressed.backgroundColor\":\"rgba(128, 128, 128, 0)\",\"toggle.off.hover.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"toggle.off.focus.borderColor\":\"#0696d7\",\"toggle.off.pressed.borderColor\":\"rgba(128, 128, 128, 0.5)\",\"toggle.on.default.backgroundColor\":\"#0696d7\",\"toggle.on.default.borderColor\":\"#0696d7\",\"toggle.thumb.borderRadii\":\"50%\",\"toggle.thumb.minDiameter\":\"12px\",\"toggle.thumb.on.backgroundColor\":\"#ffffff\",\"toggle.thumb.off.backgroundColor\":\"#808080\",\"token.borderRadii\":\"0\",\"token.paddingLeft\":\"8px\",\"token.paddingRight\":\"4px\",\"token.paddingVertical\":\"4px\",\"token.default.backgroundColor\":\"rgba(128, 128, 128, 0.15)\",\"token.hover.backgroundColor\":\"rgba(128, 128, 128, 0.25)\",\"token.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.35)\",\"token.default.borderColor\":\"rgba(128, 128, 128, 0)\",\"token.hover.borderColor\":\"rgba(128, 128, 128, 0)\",\"token.pressed.borderColor\":\"rgba(128, 128, 128, 0)\",\"token.selected.default.backgroundColor\":\"rgba(205, 234, 247, 0.6)\",\"token.selected.hover.backgroundColor\":\"rgba(155, 213, 239, 0.7)\",\"token.selected.pressed.backgroundColor\":\"rgba(128, 128, 128, 0.2)\",\"token.selected.default.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"token.selected.hover.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"token.selected.pressed.borderColor\":\"rgba(6, 150, 215, 0.5)\",\"token.warning.default.backgroundColor\":\"rgba(250, 162, 27, 0.15)\",\"token.warning.hover.backgroundColor\":\"rgba(250, 162, 27, 0.3)\",\"token.warning.iconColor\":\"#faa21b\",\"token.icon.marginRight\":\"8px\",\"token.label.fontColor\":\"#3c3c3c\",\"token.label.fontFamily\":\"ArtifaktElement, sans-serif\",\"token.label.fontSize\":\"14px\",\"token.label.fontWeight\":600,\"token.label.marginRight\":\"4px\",\"token.closeButton.iconColor\":\"#808080\",\"tooltip.backgroundColor\":\"#535353\",\"tooltip.borderRadius\":\"2px\",\"tooltip.shadowBlur\":\"16px\",\"tooltip.shadowColor\":\"rgba(0, 0, 0, 0.2)\",\"tooltip.title.fontColor\":\"#f5f5f5\",\"tooltip.title.fontFamily\":\"ArtifaktElement, sans-serif\",\"tooltip.title.fontSize\":\"12px\",\"tooltip.title.fontWeight\":700,\"tooltip.title.lineHeight\":1.333333333,\"tooltip.nub.hypotenuseLength\":\"16px\",\"tooltip.description.fontColor\":\"#F5F5F5\",\"tooltip.description.fontFamily\":\"ArtifaktElement, sans-serif\",\"tooltip.description.fontSize\":\"12px\",\"tooltip.description.fontWeight\":400,\"tooltip.description.lineHeight\":1.333333333,\"tooltip.divider.borderBottomColor\":\"rgba(255, 255, 255, 0.1)\",\"tooltip.textColor\":\"#f5f5f5\",\"tooltip.borderColor\":\"#535353\",\"topNav.padding\":\"12px\",\"topNav.backgroundColor\":\"#ffffff\",\"topNav.borderColor\":\"rgba(60, 60, 60, 0.1)\",\"topNav.productLockupColor\":\"#000000\",\"topNav.hamburgetIcon.margin\":\"8px\",\"treeView.fontColor\":\"#3c3c3c\",\"treeView.fontFamily\":\"ArtifaktElement, sans-serif\",\"treeView.indicatorColor\":\"#3c3c3c\",\"treeView.icon.marginRight\":\"8px\",\"treeView.row.paddingHorizontal\":\"12px\",\"treeView.row.paddingVertical\":\"8px\",\"treeView.title.fontSize\":\"14px\",\"treeView.title.fontWeight\":600,\"treeView.title.lineHeight\":\"1.428571429\",\"treeView.item.fontSize\":\"14px\",\"treeView.item.fontWeight\":600,\"treeView.item.lineHeight\":\"1.428571429\",\"treeView.guideLine.backgroundColor\":\"rgba(128, 128, 128, 0.5)\",\"typography.body.fontFamily\":\"ArtifaktElement, sans-serif\",\"typography.body.fontWeight\":400,\"typography.body.fontSize\":\"14px\",\"typography.body.lineHeight\":\"1.428571429\",\"typography.body.color\":\"#3c3c3c\",\"typography.caption.fontFamily\":\"ArtifaktElement, sans-serif\",\"typography.caption.fontWeight\":400,\"typography.caption.fontSize\":\"12px\",\"typography.caption.lineHeight\":\"1.5\",\"typography.caption.color\":\"#3c3c3c\",\"typography.h1.fontFamily\":\"ArtifaktElement, sans-serif\",\"typography.h1.fontWeight\":400,\"typography.h1.fontSize\":\"28px\",\"typography.h1.lineHeight\":\"1.285714286\",\"typography.h1.color\":\"#3c3c3c\",\"typography.h2.fontFamily\":\"ArtifaktElement, sans-serif\",\"typography.h2.fontWeight\":400,\"typography.h2.fontSize\":\"24px\",\"typography.h2.lineHeight\":\"1.25\",\"typography.h2.color\":\"#3c3c3c\",\"typography.h3.fontFamily\":\"ArtifaktElement, sans-serif\",\"typography.h3.fontWeight\":600,\"typography.h3.fontSize\":\"20px\",\"typography.h3.lineHeight\":\"1.3\",\"typography.h3.color\":\"#3c3c3c\"}}");

/***/ }),

/***/ "./node_modules/@hig/utils/build/index.es.js":
/*!***************************************************!*\
  !*** ./node_modules/@hig/utils/build/index.es.js ***!
  \***************************************************/
/*! exports provided: combineEventHandlers, memoizeCombineEventHandlers, createButtonEventHandlers, memoizeCreateButtonEventHandlers, generateId, createCustomClassNames */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "combineEventHandlers", function() { return combineEventHandlers; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "memoizeCombineEventHandlers", function() { return memoizeCombineEventHandlers; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createButtonEventHandlers", function() { return createButtonEventHandlers; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "memoizeCreateButtonEventHandlers", function() { return memoizeCreateButtonEventHandlers; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "generateId", function() { return generateId; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createCustomClassNames", function() { return createCustomClassNames; });
/* harmony import */ var lodash_memoize__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! lodash.memoize */ "./node_modules/lodash.memoize/index.js");
/* harmony import */ var lodash_memoize__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(lodash_memoize__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var emotion__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! emotion */ "./node_modules/emotion/dist/emotion.esm.js");



/**
 * @param {Array<function(...any): void>} handlers the event handler functions
 * @returns {function(...any): void} An event handler to add to an element
 */
function createAggregateEventHandler(handlers) {
  /**
   * This function needed to be moved out of `combineEventHandlers`
   * since babel doesn't handle nested functions using rest operators
   * properly.
   */
  return function aggregateEventHandler() {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    handlers.forEach(function (handler) {
      if (handler) handler.apply(undefined, args);
    });
  };
}

/**
 * Combines multiple event handlers into one
 * @param {...function(...any): void} handlers the event handler functions
 * @returns {function(...any): void} An event handler to add to an element
 */
function combineEventHandlers() {
  for (var _len2 = arguments.length, handlers = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    handlers[_key2] = arguments[_key2];
  }

  return createAggregateEventHandler(handlers);
}

function memoizeCombineEventHandlers() {
  return lodash_memoize__WEBPACK_IMPORTED_MODULE_0___default()(combineEventHandlers);
}

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

/**
 * @typedef {Object} ButtonEventHandlers
 * @property {function(MouseEvent, ...any): void} handleClick
 * @property {function(KeyboardEvent, ...any): void} handleKeyDown
 */

/**
 * @typedef {Object} Options
 * @property {boolean} [preventDefault]
 */

var KEYBOARD_INTERACTIONS = [" ", "Enter"];

/**
 * Create event handlers for native button behavior for non-button elements
 * @see https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/button_role
 *
 * @param {function(MouseEvent|KeyboardEvent, ...any): void} [handler] the event handler function
 * @param {Options} [options]
 * @returns {ButtonEventHandlers}
 */
function createButtonEventHandlers(handler) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

  if (!handler) return {};

  var _options$preventDefau = options.preventDefault,
      preventDefault = _options$preventDefau === undefined ? true : _options$preventDefau;


  return {
    handleClick: handler,
    handleKeyDown: function handleKeyDown(event) {
      var key = event.key;


      if (!KEYBOARD_INTERACTIONS.includes(key)) return;
      // Prevent space key default scrolling behavior
      if (preventDefault) event.preventDefault();

      for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
      }

      handler.apply(undefined, [event].concat(_toConsumableArray(args)));
    }
  };
}

function memoizeCreateButtonEventHandlers() {
  return lodash_memoize__WEBPACK_IMPORTED_MODULE_0___default()(createButtonEventHandlers);
}

/**
 * Generates a unique ID
 * @param {string} key
 * @returns {string} A generated id
 */
function generateId(key) {
  return key + "-" + Math.floor(Math.random() * 100000, 5);
}

/**
 * Returns a css className that combines what the user passes down and a string.
 * Ideally the string would map to the element's js style object's name.
 * @param {string} className
 * @param {string} appendedString
 * @returns {string} A string that looks like className__appendedString
 */

function createCustomClassNames(className, appendedString) {
  return className && className.split(" ").reduce(function (acc, cur) {
    return Object(emotion__WEBPACK_IMPORTED_MODULE_1__["cx"])(acc, cur.trim() + "__" + appendedString);
  }, "");
}

/* eslint-disable import/prefer-default-export */




/***/ }),

/***/ "./node_modules/aphrodite/es/chunk-febce46b.js":
/*!*****************************************************!*\
  !*** ./node_modules/aphrodite/es/chunk-febce46b.js ***!
  \*****************************************************/
/*! exports provided: a */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return makeExports; });
/* harmony import */ var string_hash__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! string-hash */ "./node_modules/string-hash/index.js");
/* harmony import */ var string_hash__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(string_hash__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var asap__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! asap */ "./node_modules/asap/browser-asap.js");
/* harmony import */ var asap__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(asap__WEBPACK_IMPORTED_MODULE_1__);



function _typeof(obj) {
  if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
    _typeof = function (obj) {
      return typeof obj;
    };
  } else {
    _typeof = function (obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
  }

  return _typeof(obj);
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}

function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};
    var ownKeys = Object.keys(source);

    if (typeof Object.getOwnPropertySymbols === 'function') {
      ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) {
        return Object.getOwnPropertyDescriptor(source, sym).enumerable;
      }));
    }

    ownKeys.forEach(function (key) {
      _defineProperty(target, key, source[key]);
    });
  }

  return target;
}

function _toConsumableArray(arr) {
  return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread();
}

function _arrayWithoutHoles(arr) {
  if (Array.isArray(arr)) {
    for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) arr2[i] = arr[i];

    return arr2;
  }
}

function _iterableToArray(iter) {
  if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter);
}

function _nonIterableSpread() {
  throw new TypeError("Invalid attempt to spread non-iterable instance");
}

/* @flow */
/* ::
type ObjectMap = { [id:string]: any };
*/

var UPPERCASE_RE = /([A-Z])/g;

var UPPERCASE_RE_TO_KEBAB = function UPPERCASE_RE_TO_KEBAB(match
/* : string */
) {
  return (
    /* : string */
    "-".concat(match.toLowerCase())
  );
};

var kebabifyStyleName = function kebabifyStyleName(string
/* : string */
)
/* : string */
{
  var result = string.replace(UPPERCASE_RE, UPPERCASE_RE_TO_KEBAB);

  if (result[0] === 'm' && result[1] === 's' && result[2] === '-') {
    return "-".concat(result);
  }

  return result;
};
/**
 * CSS properties which accept numbers but are not in units of "px".
 * Taken from React's CSSProperty.js
 */

var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridColumn: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,
  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};
/**
 * Taken from React's CSSProperty.js
 *
 * @param {string} prefix vendor-specific prefix, eg: Webkit
 * @param {string} key style name, eg: transitionDuration
 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
 * WebkitTransitionDuration
 */

function prefixKey(prefix, key) {
  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
}
/**
 * Support style names that may come passed in prefixed by adding permutations
 * of vendor prefixes.
 * Taken from React's CSSProperty.js
 */


var prefixes = ['Webkit', 'ms', 'Moz', 'O']; // Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
// infinite loop, because it iterates over the newly added props too.
// Taken from React's CSSProperty.js

Object.keys(isUnitlessNumber).forEach(function (prop) {
  prefixes.forEach(function (prefix) {
    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
  });
});
var stringifyValue = function stringifyValue(key
/* : string */
, prop
/* : any */
)
/* : string */
{
  if (typeof prop === "number") {
    if (isUnitlessNumber[key]) {
      return "" + prop;
    } else {
      return prop + "px";
    }
  } else {
    return '' + prop;
  }
};
var stringifyAndImportantifyValue = function stringifyAndImportantifyValue(key
/* : string */
, prop
/* : any */
) {
  return (
    /* : string */
    importantify(stringifyValue(key, prop))
  );
}; // Turn a string into a hash string of base-36 values (using letters and numbers)
// eslint-disable-next-line no-unused-vars

var hashString = function hashString(string
/* : string */
, key
/* : ?string */
) {
  return (
    /* string */
    string_hash__WEBPACK_IMPORTED_MODULE_0___default()(string).toString(36)
  );
}; // Hash a javascript object using JSON.stringify. This is very fast, about 3
// microseconds on my computer for a sample object:
// http://jsperf.com/test-hashfnv32a-hash/5
//
// Note that this uses JSON.stringify to stringify the objects so in order for
// this to produce consistent hashes browsers need to have a consistent
// ordering of objects. Ben Alpert says that Facebook depends on this, so we
// can probably depend on this too.

var hashObject = function hashObject(object
/* : ObjectMap */
) {
  return (
    /* : string */
    hashString(JSON.stringify(object))
  );
}; // Given a single style value string like the "b" from "a: b;", adds !important
// to generate "b !important".

var importantify = function importantify(string
/* : string */
) {
  return (
    /* : string */
    // Bracket string character access is very fast, and in the default case we
    // normally don't expect there to be "!important" at the end of the string
    // so we can use this simple check to take an optimized path. If there
    // happens to be a "!" in this position, we follow up with a more thorough
    // check.
    string[string.length - 10] === '!' && string.slice(-11) === ' !important' ? string : "".concat(string, " !important")
  );
};

/* @flow */
var MAP_EXISTS = typeof Map !== 'undefined';

var OrderedElements =
/*#__PURE__*/
function () {
  /* ::
  elements: {[string]: any};
  keyOrder: string[];
  */
  function OrderedElements() {
    this.elements = {};
    this.keyOrder = [];
  }

  var _proto = OrderedElements.prototype;

  _proto.forEach = function forEach(callback
  /* : (string, any) => void */
  ) {
    for (var i = 0; i < this.keyOrder.length; i++) {
      // (value, key) to match Map's API
      callback(this.elements[this.keyOrder[i]], this.keyOrder[i]);
    }
  };

  _proto.set = function set(key
  /* : string */
  , value
  /* : any */
  , shouldReorder
  /* : ?boolean */
  ) {
    if (!this.elements.hasOwnProperty(key)) {
      this.keyOrder.push(key);
    } else if (shouldReorder) {
      var index = this.keyOrder.indexOf(key);
      this.keyOrder.splice(index, 1);
      this.keyOrder.push(key);
    }

    if (value == null) {
      this.elements[key] = value;
      return;
    }

    if (MAP_EXISTS && value instanceof Map || value instanceof OrderedElements) {
      // We have found a nested Map, so we need to recurse so that all
      // of the nested objects and Maps are merged properly.
      var nested = this.elements.hasOwnProperty(key) ? this.elements[key] : new OrderedElements();
      value.forEach(function (value, key) {
        nested.set(key, value, shouldReorder);
      });
      this.elements[key] = nested;
      return;
    }

    if (!Array.isArray(value) && _typeof(value) === 'object') {
      // We have found a nested object, so we need to recurse so that all
      // of the nested objects and Maps are merged properly.
      var _nested = this.elements.hasOwnProperty(key) ? this.elements[key] : new OrderedElements();

      var keys = Object.keys(value);

      for (var i = 0; i < keys.length; i += 1) {
        _nested.set(keys[i], value[keys[i]], shouldReorder);
      }

      this.elements[key] = _nested;
      return;
    }

    this.elements[key] = value;
  };

  _proto.get = function get(key
  /* : string */
  )
  /* : any */
  {
    return this.elements[key];
  };

  _proto.has = function has(key
  /* : string */
  )
  /* : boolean */
  {
    return this.elements.hasOwnProperty(key);
  };

  _proto.addStyleType = function addStyleType(styleType
  /* : any */
  )
  /* : void */
  {
    var _this = this;

    if (MAP_EXISTS && styleType instanceof Map || styleType instanceof OrderedElements) {
      styleType.forEach(function (value, key) {
        _this.set(key, value, true);
      });
    } else {
      var keys = Object.keys(styleType);

      for (var i = 0; i < keys.length; i++) {
        this.set(keys[i], styleType[keys[i]], true);
      }
    }
  };

  return OrderedElements;
}();

function unwrapExports (x) {
	return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, 'default') ? x.default : x;
}

function createCommonjsModule(fn, module) {
	return module = { exports: {} }, fn(module, module.exports), module.exports;
}

function getCjsExportFromNamespace (n) {
	return n && n.default || n;
}

var capitalizeString_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = capitalizeString;
function capitalizeString(str) {
  return str.charAt(0).toUpperCase() + str.slice(1);
}
});

unwrapExports(capitalizeString_1);

var prefixProperty_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = prefixProperty;



var _capitalizeString2 = _interopRequireDefault(capitalizeString_1);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function prefixProperty(prefixProperties, property, style) {
  if (prefixProperties.hasOwnProperty(property)) {
    var newStyle = {};
    var requiredPrefixes = prefixProperties[property];
    var capitalizedProperty = (0, _capitalizeString2.default)(property);
    var keys = Object.keys(style);
    for (var i = 0; i < keys.length; i++) {
      var styleProperty = keys[i];
      if (styleProperty === property) {
        for (var j = 0; j < requiredPrefixes.length; j++) {
          newStyle[requiredPrefixes[j] + capitalizedProperty] = style[property];
        }
      }
      newStyle[styleProperty] = style[styleProperty];
    }
    return newStyle;
  }
  return style;
}
});

unwrapExports(prefixProperty_1);

var prefixValue_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = prefixValue;
function prefixValue(plugins, property, value, style, metaData) {
  for (var i = 0, len = plugins.length; i < len; ++i) {
    var processedValue = plugins[i](property, value, style, metaData);

    // we can stop processing if a value is returned
    // as all plugin criteria are unique
    if (processedValue) {
      return processedValue;
    }
  }
}
});

unwrapExports(prefixValue_1);

var addNewValuesOnly_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = addNewValuesOnly;
function addIfNew(list, value) {
  if (list.indexOf(value) === -1) {
    list.push(value);
  }
}

function addNewValuesOnly(list, values) {
  if (Array.isArray(values)) {
    for (var i = 0, len = values.length; i < len; ++i) {
      addIfNew(list, values[i]);
    }
  } else {
    addIfNew(list, values);
  }
}
});

unwrapExports(addNewValuesOnly_1);

var isObject_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isObject;
function isObject(value) {
  return value instanceof Object && !Array.isArray(value);
}
});

unwrapExports(isObject_1);

var createPrefixer_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createPrefixer;



var _prefixProperty2 = _interopRequireDefault(prefixProperty_1);



var _prefixValue2 = _interopRequireDefault(prefixValue_1);



var _addNewValuesOnly2 = _interopRequireDefault(addNewValuesOnly_1);



var _isObject2 = _interopRequireDefault(isObject_1);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createPrefixer(_ref) {
  var prefixMap = _ref.prefixMap,
      plugins = _ref.plugins;

  return function prefix(style) {
    for (var property in style) {
      var value = style[property];

      // handle nested objects
      if ((0, _isObject2.default)(value)) {
        style[property] = prefix(value);
        // handle array values
      } else if (Array.isArray(value)) {
        var combinedValue = [];

        for (var i = 0, len = value.length; i < len; ++i) {
          var processedValue = (0, _prefixValue2.default)(plugins, property, value[i], style, prefixMap);
          (0, _addNewValuesOnly2.default)(combinedValue, processedValue || value[i]);
        }

        // only modify the value if it was touched
        // by any plugin to prevent unnecessary mutations
        if (combinedValue.length > 0) {
          style[property] = combinedValue;
        }
      } else {
        var _processedValue = (0, _prefixValue2.default)(plugins, property, value, style, prefixMap);

        // only modify the value if it was touched
        // by any plugin to prevent unnecessary mutations
        if (_processedValue) {
          style[property] = _processedValue;
        }

        style = (0, _prefixProperty2.default)(prefixMap, property, style);
      }
    }

    return style;
  };
}
});

var createPrefixer = unwrapExports(createPrefixer_1);

var backgroundClip_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = backgroundClip;

// https://developer.mozilla.org/en-US/docs/Web/CSS/background-clip#Browser_compatibility
function backgroundClip(property, value) {
  if (typeof value === 'string' && value === 'text') {
    return ['-webkit-text', 'text'];
  }
}
});

var backgroundClip = unwrapExports(backgroundClip_1);

var isPrefixedValue_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isPrefixedValue;
var regex = /-webkit-|-moz-|-ms-/;

function isPrefixedValue(value) {
  return typeof value === 'string' && regex.test(value);
}
module.exports = exports['default'];
});

unwrapExports(isPrefixedValue_1);

var calc_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = calc;



var _isPrefixedValue2 = _interopRequireDefault(isPrefixedValue_1);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var prefixes = ['-webkit-', '-moz-', ''];
function calc(property, value) {
  if (typeof value === 'string' && !(0, _isPrefixedValue2.default)(value) && value.indexOf('calc(') > -1) {
    return prefixes.map(function (prefix) {
      return value.replace(/calc\(/g, prefix + 'calc(');
    });
  }
}
});

var calc = unwrapExports(calc_1);

var crossFade_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = crossFade;



var _isPrefixedValue2 = _interopRequireDefault(isPrefixedValue_1);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// http://caniuse.com/#search=cross-fade
var prefixes = ['-webkit-', ''];
function crossFade(property, value) {
  if (typeof value === 'string' && !(0, _isPrefixedValue2.default)(value) && value.indexOf('cross-fade(') > -1) {
    return prefixes.map(function (prefix) {
      return value.replace(/cross-fade\(/g, prefix + 'cross-fade(');
    });
  }
}
});

var crossFade = unwrapExports(crossFade_1);

var cursor_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = cursor;
var prefixes = ['-webkit-', '-moz-', ''];

var values = {
  'zoom-in': true,
  'zoom-out': true,
  grab: true,
  grabbing: true
};

function cursor(property, value) {
  if (property === 'cursor' && values.hasOwnProperty(value)) {
    return prefixes.map(function (prefix) {
      return prefix + value;
    });
  }
}
});

var cursor = unwrapExports(cursor_1);

var filter_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = filter;



var _isPrefixedValue2 = _interopRequireDefault(isPrefixedValue_1);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// http://caniuse.com/#feat=css-filter-function
var prefixes = ['-webkit-', ''];
function filter(property, value) {
  if (typeof value === 'string' && !(0, _isPrefixedValue2.default)(value) && value.indexOf('filter(') > -1) {
    return prefixes.map(function (prefix) {
      return value.replace(/filter\(/g, prefix + 'filter(');
    });
  }
}
});

var filter = unwrapExports(filter_1);

var flex_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = flex;
var values = {
  flex: ['-webkit-box', '-moz-box', '-ms-flexbox', '-webkit-flex', 'flex'],
  'inline-flex': ['-webkit-inline-box', '-moz-inline-box', '-ms-inline-flexbox', '-webkit-inline-flex', 'inline-flex']
};

function flex(property, value) {
  if (property === 'display' && values.hasOwnProperty(value)) {
    return values[value];
  }
}
});

var flex = unwrapExports(flex_1);

var flexboxIE_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = flexboxIE;
var alternativeValues = {
  'space-around': 'distribute',
  'space-between': 'justify',
  'flex-start': 'start',
  'flex-end': 'end'
};
var alternativeProps = {
  alignContent: 'msFlexLinePack',
  alignSelf: 'msFlexItemAlign',
  alignItems: 'msFlexAlign',
  justifyContent: 'msFlexPack',
  order: 'msFlexOrder',
  flexGrow: 'msFlexPositive',
  flexShrink: 'msFlexNegative',
  flexBasis: 'msFlexPreferredSize'
  // Full expanded syntax is flex-grow | flex-shrink | flex-basis.
};var flexShorthandMappings = {
  auto: '1 1 auto',
  inherit: 'inherit',
  initial: '0 1 auto',
  none: '0 0 auto',
  unset: 'unset'
};
var isUnitlessNumber = /^\d+(\.\d+)?$/;

function flexboxIE(property, value, style) {
  if (Object.prototype.hasOwnProperty.call(alternativeProps, property)) {
    style[alternativeProps[property]] = alternativeValues[value] || value;
  }
  if (property === 'flex') {
    // For certain values we can do straight mappings based on the spec
    // for the expansions.
    if (Object.prototype.hasOwnProperty.call(flexShorthandMappings, value)) {
      style.msFlex = flexShorthandMappings[value];
      return;
    }
    // Here we have no direct mapping, so we favor looking for a
    // unitless positive number as that will be the most common use-case.
    if (isUnitlessNumber.test(value)) {
      style.msFlex = value + ' 1 0%';
      return;
    }

    // The next thing we can look for is if there are multiple values.
    var flexValues = value.split(/\s/);
    // If we only have a single value that wasn't a positive unitless
    // or a pre-mapped value, then we can assume it is a unit value.
    switch (flexValues.length) {
      case 1:
        style.msFlex = '1 1 ' + value;
        return;
      case 2:
        // If we have 2 units, then we expect that the first will
        // always be a unitless number and represents flex-grow.
        // The second unit will represent flex-shrink for a unitless
        // value, or flex-basis otherwise.
        if (isUnitlessNumber.test(flexValues[1])) {
          style.msFlex = flexValues[0] + ' ' + flexValues[1] + ' 0%';
        } else {
          style.msFlex = flexValues[0] + ' 1 ' + flexValues[1];
        }
        return;
      default:
        style.msFlex = value;
    }
  }
}
});

var flexboxIE = unwrapExports(flexboxIE_1);

var flexboxOld_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = flexboxOld;
var alternativeValues = {
  'space-around': 'justify',
  'space-between': 'justify',
  'flex-start': 'start',
  'flex-end': 'end',
  'wrap-reverse': 'multiple',
  wrap: 'multiple'
};

var alternativeProps = {
  alignItems: 'WebkitBoxAlign',
  justifyContent: 'WebkitBoxPack',
  flexWrap: 'WebkitBoxLines',
  flexGrow: 'WebkitBoxFlex'
};

function flexboxOld(property, value, style) {
  if (property === 'flexDirection' && typeof value === 'string') {
    if (value.indexOf('column') > -1) {
      style.WebkitBoxOrient = 'vertical';
    } else {
      style.WebkitBoxOrient = 'horizontal';
    }
    if (value.indexOf('reverse') > -1) {
      style.WebkitBoxDirection = 'reverse';
    } else {
      style.WebkitBoxDirection = 'normal';
    }
  }
  if (alternativeProps.hasOwnProperty(property)) {
    style[alternativeProps[property]] = alternativeValues[value] || value;
  }
}
});

var flexboxOld = unwrapExports(flexboxOld_1);

var gradient_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = gradient;



var _isPrefixedValue2 = _interopRequireDefault(isPrefixedValue_1);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var prefixes = ['-webkit-', '-moz-', ''];

var values = /linear-gradient|radial-gradient|repeating-linear-gradient|repeating-radial-gradient/gi;

function gradient(property, value) {
  if (typeof value === 'string' && !(0, _isPrefixedValue2.default)(value) && values.test(value)) {
    return prefixes.map(function (prefix) {
      return value.replace(values, function (grad) {
        return prefix + grad;
      });
    });
  }
}
});

var gradient = unwrapExports(gradient_1);

var grid_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _slicedToArray = function () { function sliceIterator(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"]) _i["return"](); } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }();

exports.default = grid;
function isSimplePositionValue(value) {
  return typeof value === 'number' && !isNaN(value);
}

var alignmentValues = ['center', 'end', 'start', 'stretch'];

var displayValues = {
  'inline-grid': ['-ms-inline-grid', 'inline-grid'],
  grid: ['-ms-grid', 'grid']
};

var propertyConverters = {
  alignSelf: function alignSelf(value, style) {
    if (alignmentValues.indexOf(value) > -1) {
      style.msGridRowAlign = value;
    }
  },

  gridColumn: function gridColumn(value, style) {
    if (isSimplePositionValue(value)) {
      style.msGridColumn = value;
    } else {
      var _value$split$map = value.split('/').map(function (position) {
        return +position;
      }),
          _value$split$map2 = _slicedToArray(_value$split$map, 2),
          start = _value$split$map2[0],
          end = _value$split$map2[1];

      propertyConverters.gridColumnStart(start, style);
      propertyConverters.gridColumnEnd(end, style);
    }
  },

  gridColumnEnd: function gridColumnEnd(value, style) {
    var msGridColumn = style.msGridColumn;

    if (isSimplePositionValue(value) && isSimplePositionValue(msGridColumn)) {
      style.msGridColumnSpan = value - msGridColumn;
    }
  },

  gridColumnStart: function gridColumnStart(value, style) {
    if (isSimplePositionValue(value)) {
      style.msGridColumn = value;
    }
  },

  gridRow: function gridRow(value, style) {
    if (isSimplePositionValue(value)) {
      style.msGridRow = value;
    } else {
      var _value$split$map3 = value.split('/').map(function (position) {
        return +position;
      }),
          _value$split$map4 = _slicedToArray(_value$split$map3, 2),
          start = _value$split$map4[0],
          end = _value$split$map4[1];

      propertyConverters.gridRowStart(start, style);
      propertyConverters.gridRowEnd(end, style);
    }
  },

  gridRowEnd: function gridRowEnd(value, style) {
    var msGridRow = style.msGridRow;

    if (isSimplePositionValue(value) && isSimplePositionValue(msGridRow)) {
      style.msGridRowSpan = value - msGridRow;
    }
  },

  gridRowStart: function gridRowStart(value, style) {
    if (isSimplePositionValue(value)) {
      style.msGridRow = value;
    }
  },

  gridTemplateColumns: function gridTemplateColumns(value, style) {
    style.msGridColumns = value;
  },

  gridTemplateRows: function gridTemplateRows(value, style) {
    style.msGridRows = value;
  },

  justifySelf: function justifySelf(value, style) {
    if (alignmentValues.indexOf(value) > -1) {
      style.msGridColumnAlign = value;
    }
  }
};

function grid(property, value, style) {
  if (property === 'display' && value in displayValues) {
    return displayValues[value];
  }

  if (property in propertyConverters) {
    var propertyConverter = propertyConverters[property];
    propertyConverter(value, style);
  }
}
});

var grid = unwrapExports(grid_1);

var imageSet_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = imageSet;



var _isPrefixedValue2 = _interopRequireDefault(isPrefixedValue_1);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// http://caniuse.com/#feat=css-image-set
var prefixes = ['-webkit-', ''];
function imageSet(property, value) {
  if (typeof value === 'string' && !(0, _isPrefixedValue2.default)(value) && value.indexOf('image-set(') > -1) {
    return prefixes.map(function (prefix) {
      return value.replace(/image-set\(/g, prefix + 'image-set(');
    });
  }
}
});

var imageSet = unwrapExports(imageSet_1);

var logical_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = logical;
var alternativeProps = {
  marginBlockStart: ['WebkitMarginBefore'],
  marginBlockEnd: ['WebkitMarginAfter'],
  marginInlineStart: ['WebkitMarginStart', 'MozMarginStart'],
  marginInlineEnd: ['WebkitMarginEnd', 'MozMarginEnd'],
  paddingBlockStart: ['WebkitPaddingBefore'],
  paddingBlockEnd: ['WebkitPaddingAfter'],
  paddingInlineStart: ['WebkitPaddingStart', 'MozPaddingStart'],
  paddingInlineEnd: ['WebkitPaddingEnd', 'MozPaddingEnd'],
  borderBlockStart: ['WebkitBorderBefore'],
  borderBlockStartColor: ['WebkitBorderBeforeColor'],
  borderBlockStartStyle: ['WebkitBorderBeforeStyle'],
  borderBlockStartWidth: ['WebkitBorderBeforeWidth'],
  borderBlockEnd: ['WebkitBorderAfter'],
  borderBlockEndColor: ['WebkitBorderAfterColor'],
  borderBlockEndStyle: ['WebkitBorderAfterStyle'],
  borderBlockEndWidth: ['WebkitBorderAfterWidth'],
  borderInlineStart: ['WebkitBorderStart', 'MozBorderStart'],
  borderInlineStartColor: ['WebkitBorderStartColor', 'MozBorderStartColor'],
  borderInlineStartStyle: ['WebkitBorderStartStyle', 'MozBorderStartStyle'],
  borderInlineStartWidth: ['WebkitBorderStartWidth', 'MozBorderStartWidth'],
  borderInlineEnd: ['WebkitBorderEnd', 'MozBorderEnd'],
  borderInlineEndColor: ['WebkitBorderEndColor', 'MozBorderEndColor'],
  borderInlineEndStyle: ['WebkitBorderEndStyle', 'MozBorderEndStyle'],
  borderInlineEndWidth: ['WebkitBorderEndWidth', 'MozBorderEndWidth']
};

function logical(property, value, style) {
  if (Object.prototype.hasOwnProperty.call(alternativeProps, property)) {
    var alternativePropList = alternativeProps[property];
    for (var i = 0, len = alternativePropList.length; i < len; ++i) {
      style[alternativePropList[i]] = value;
    }
  }
}
});

var logical = unwrapExports(logical_1);

var position_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = position;
function position(property, value) {
  if (property === 'position' && value === 'sticky') {
    return ['-webkit-sticky', 'sticky'];
  }
}
});

var position = unwrapExports(position_1);

var sizing_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = sizing;
var prefixes = ['-webkit-', '-moz-', ''];

var properties = {
  maxHeight: true,
  maxWidth: true,
  width: true,
  height: true,
  columnWidth: true,
  minWidth: true,
  minHeight: true
};
var values = {
  'min-content': true,
  'max-content': true,
  'fill-available': true,
  'fit-content': true,
  'contain-floats': true
};

function sizing(property, value) {
  if (properties.hasOwnProperty(property) && values.hasOwnProperty(value)) {
    return prefixes.map(function (prefix) {
      return prefix + value;
    });
  }
}
});

var sizing = unwrapExports(sizing_1);

/* eslint-disable no-var, prefer-template */
var uppercasePattern = /[A-Z]/g;
var msPattern = /^ms-/;
var cache = {};

function toHyphenLower(match) {
  return '-' + match.toLowerCase()
}

function hyphenateStyleName(name) {
  if (cache.hasOwnProperty(name)) {
    return cache[name]
  }

  var hName = name.replace(uppercasePattern, toHyphenLower);
  return (cache[name] = msPattern.test(hName) ? '-' + hName : hName)
}

var hyphenateStyleName$1 = /*#__PURE__*/Object.freeze({
  default: hyphenateStyleName
});

var _hyphenateStyleName = getCjsExportFromNamespace(hyphenateStyleName$1);

var hyphenateProperty_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = hyphenateProperty;



var _hyphenateStyleName2 = _interopRequireDefault(_hyphenateStyleName);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function hyphenateProperty(property) {
  return (0, _hyphenateStyleName2.default)(property);
}
module.exports = exports['default'];
});

unwrapExports(hyphenateProperty_1);

var transition_1 = createCommonjsModule(function (module, exports) {

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = transition;



var _hyphenateProperty2 = _interopRequireDefault(hyphenateProperty_1);



var _isPrefixedValue2 = _interopRequireDefault(isPrefixedValue_1);



var _capitalizeString2 = _interopRequireDefault(capitalizeString_1);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var properties = {
  transition: true,
  transitionProperty: true,
  WebkitTransition: true,
  WebkitTransitionProperty: true,
  MozTransition: true,
  MozTransitionProperty: true
};


var prefixMapping = {
  Webkit: '-webkit-',
  Moz: '-moz-',
  ms: '-ms-'
};

function prefixValue(value, propertyPrefixMap) {
  if ((0, _isPrefixedValue2.default)(value)) {
    return value;
  }

  // only split multi values, not cubic beziers
  var multipleValues = value.split(/,(?![^()]*(?:\([^()]*\))?\))/g);

  for (var i = 0, len = multipleValues.length; i < len; ++i) {
    var singleValue = multipleValues[i];
    var values = [singleValue];
    for (var property in propertyPrefixMap) {
      var dashCaseProperty = (0, _hyphenateProperty2.default)(property);

      if (singleValue.indexOf(dashCaseProperty) > -1 && dashCaseProperty !== 'order') {
        var prefixes = propertyPrefixMap[property];
        for (var j = 0, pLen = prefixes.length; j < pLen; ++j) {
          // join all prefixes and create a new value
          values.unshift(singleValue.replace(dashCaseProperty, prefixMapping[prefixes[j]] + dashCaseProperty));
        }
      }
    }

    multipleValues[i] = values.join(',');
  }

  return multipleValues.join(',');
}

function transition(property, value, style, propertyPrefixMap) {
  // also check for already prefixed transitions
  if (typeof value === 'string' && properties.hasOwnProperty(property)) {
    var outputValue = prefixValue(value, propertyPrefixMap);
    // if the property is already prefixed
    var webkitOutput = outputValue.split(/,(?![^()]*(?:\([^()]*\))?\))/g).filter(function (val) {
      return !/-moz-|-ms-/.test(val);
    }).join(',');

    if (property.indexOf('Webkit') > -1) {
      return webkitOutput;
    }

    var mozOutput = outputValue.split(/,(?![^()]*(?:\([^()]*\))?\))/g).filter(function (val) {
      return !/-webkit-|-ms-/.test(val);
    }).join(',');

    if (property.indexOf('Moz') > -1) {
      return mozOutput;
    }

    style['Webkit' + (0, _capitalizeString2.default)(property)] = webkitOutput;
    style['Moz' + (0, _capitalizeString2.default)(property)] = mozOutput;
    return outputValue;
  }
}
});

var transition = unwrapExports(transition_1);

var w = ["Webkit"];
var m = ["Moz"];
var ms = ["ms"];
var wm = ["Webkit", "Moz"];
var wms = ["Webkit", "ms"];
var wmms = ["Webkit", "Moz", "ms"];
var staticData = {
  plugins: [backgroundClip, calc, crossFade, cursor, filter, flex, flexboxIE, flexboxOld, gradient, grid, imageSet, logical, position, sizing, transition],
  prefixMap: {
    "transform": wms,
    "transformOrigin": wms,
    "transformOriginX": wms,
    "transformOriginY": wms,
    "backfaceVisibility": w,
    "perspective": w,
    "perspectiveOrigin": w,
    "transformStyle": w,
    "transformOriginZ": w,
    "animation": w,
    "animationDelay": w,
    "animationDirection": w,
    "animationFillMode": w,
    "animationDuration": w,
    "animationIterationCount": w,
    "animationName": w,
    "animationPlayState": w,
    "animationTimingFunction": w,
    "appearance": wm,
    "userSelect": wmms,
    "fontKerning": w,
    "textEmphasisPosition": w,
    "textEmphasis": w,
    "textEmphasisStyle": w,
    "textEmphasisColor": w,
    "boxDecorationBreak": w,
    "clipPath": w,
    "maskImage": w,
    "maskMode": w,
    "maskRepeat": w,
    "maskPosition": w,
    "maskClip": w,
    "maskOrigin": w,
    "maskSize": w,
    "maskComposite": w,
    "mask": w,
    "maskBorderSource": w,
    "maskBorderMode": w,
    "maskBorderSlice": w,
    "maskBorderWidth": w,
    "maskBorderOutset": w,
    "maskBorderRepeat": w,
    "maskBorder": w,
    "maskType": w,
    "textDecorationStyle": wm,
    "textDecorationSkip": wm,
    "textDecorationLine": wm,
    "textDecorationColor": wm,
    "filter": w,
    "fontFeatureSettings": wm,
    "breakAfter": wmms,
    "breakBefore": wmms,
    "breakInside": wmms,
    "columnCount": wm,
    "columnFill": wm,
    "columnGap": wm,
    "columnRule": wm,
    "columnRuleColor": wm,
    "columnRuleStyle": wm,
    "columnRuleWidth": wm,
    "columns": wm,
    "columnSpan": wm,
    "columnWidth": wm,
    "writingMode": wms,
    "flex": wms,
    "flexBasis": w,
    "flexDirection": wms,
    "flexGrow": w,
    "flexFlow": wms,
    "flexShrink": w,
    "flexWrap": wms,
    "alignContent": w,
    "alignItems": w,
    "alignSelf": w,
    "justifyContent": w,
    "order": w,
    "transitionDelay": w,
    "transitionDuration": w,
    "transitionProperty": w,
    "transitionTimingFunction": w,
    "backdropFilter": w,
    "scrollSnapType": wms,
    "scrollSnapPointsX": wms,
    "scrollSnapPointsY": wms,
    "scrollSnapDestination": wms,
    "scrollSnapCoordinate": wms,
    "shapeImageThreshold": w,
    "shapeImageMargin": w,
    "shapeImageOutside": w,
    "hyphens": wmms,
    "flowInto": wms,
    "flowFrom": wms,
    "regionFragment": wms,
    "textOrientation": w,
    "boxSizing": m,
    "textAlignLast": m,
    "tabSize": m,
    "wrapFlow": ms,
    "wrapThrough": ms,
    "wrapMargin": ms,
    "touchAction": ms,
    "textSizeAdjust": wms,
    "borderImage": w,
    "borderImageOutset": w,
    "borderImageRepeat": w,
    "borderImageSlice": w,
    "borderImageSource": w,
    "borderImageWidth": w
  }
};

var prefixAll = createPrefixer(staticData);
/* ::
import type { SheetDefinition } from './index.js';
type StringHandlers = { [id:string]: Function };
type SelectorCallback = (selector: string) => string[];
export type SelectorHandler = (
    selector: string,
    baseSelector: string,
    callback: SelectorCallback
) => string[] | string | null;
*/

/**
 * `selectorHandlers` are functions which handle special selectors which act
 * differently than normal style definitions. These functions look at the
 * current selector and can generate CSS for the styles in their subtree by
 * calling the callback with a new selector.
 *
 * For example, when generating styles with a base selector of '.foo' and the
 * following styles object:
 *
 *   {
 *     ':nth-child(2n)': {
 *       ':hover': {
 *         color: 'red'
 *       }
 *     }
 *   }
 *
 * when we reach the ':hover' style, we would call our selector handlers like
 *
 *   handler(':hover', '.foo:nth-child(2n)', callback)
 *
 * Since our `pseudoSelectors` handles ':hover' styles, that handler would call
 * the callback like
 *
 *   callback('.foo:nth-child(2n):hover')
 *
 * to generate its subtree `{ color: 'red' }` styles with a
 * '.foo:nth-child(2n):hover' selector. The callback would return an array of CSS
 * rules like
 *
 *   ['.foo:nth-child(2n):hover{color:red !important;}']
 *
 * and the handler would then return that resulting CSS.
 *
 * `defaultSelectorHandlers` is the list of default handlers used in a call to
 * `generateCSS`.
 *
 * @name SelectorHandler
 * @function
 * @param {string} selector: The currently inspected selector. ':hover' in the
 *     example above.
 * @param {string} baseSelector: The selector of the parent styles.
 *     '.foo:nth-child(2n)' in the example above.
 * @param {function} generateSubtreeStyles: A function which can be called to
 *     generate CSS for the subtree of styles corresponding to the selector.
 *     Accepts a new baseSelector to use for generating those styles.
 * @returns {string[] | string | null} The generated CSS for this selector, or
 *     null if we don't handle this selector.
 */

var defaultSelectorHandlers
/* : SelectorHandler[] */
= [// Handle pseudo-selectors, like :hover and :nth-child(3n)
function pseudoSelectors(selector, baseSelector, generateSubtreeStyles) {
  if (selector[0] !== ":") {
    return null;
  }

  return generateSubtreeStyles(baseSelector + selector);
}, // Handle media queries (or font-faces)
function mediaQueries(selector, baseSelector, generateSubtreeStyles) {
  if (selector[0] !== "@") {
    return null;
  } // Generate the styles normally, and then wrap them in the media query.


  var generated = generateSubtreeStyles(baseSelector);
  return ["".concat(selector, "{").concat(generated.join(''), "}")];
}];
/**
 * Generate CSS for a selector and some styles.
 *
 * This function handles the media queries and pseudo selectors that can be used
 * in aphrodite styles.
 *
 * @param {string} selector: A base CSS selector for the styles to be generated
 *     with.
 * @param {Object} styleTypes: A list of properties of the return type of
 *     StyleSheet.create, e.g. [styles.red, styles.blue].
 * @param {Array.<SelectorHandler>} selectorHandlers: A list of selector
 *     handlers to use for handling special selectors. See
 *     `defaultSelectorHandlers`.
 * @param stringHandlers: See `generateCSSRuleset`
 * @param useImportant: See `generateCSSRuleset`
 *
 * To actually generate the CSS special-construct-less styles are passed to
 * `generateCSSRuleset`.
 *
 * For instance, a call to
 *
 *     generateCSS(".foo", [{
 *       color: "red",
 *       "@media screen": {
 *         height: 20,
 *         ":hover": {
 *           backgroundColor: "black"
 *         }
 *       },
 *       ":active": {
 *         fontWeight: "bold"
 *       }
 *     }], defaultSelectorHandlers);
 *
 * with the default `selectorHandlers` will make 5 calls to
 * `generateCSSRuleset`:
 *
 *     generateCSSRuleset(".foo", { color: "red" }, ...)
 *     generateCSSRuleset(".foo:active", { fontWeight: "bold" }, ...)
 *     // These 2 will be wrapped in @media screen {}
 *     generateCSSRuleset(".foo", { height: 20 }, ...)
 *     generateCSSRuleset(".foo:hover", { backgroundColor: "black" }, ...)
 */

var generateCSS = function generateCSS(selector
/* : string */
, styleTypes
/* : SheetDefinition[] */
, selectorHandlers
/* : SelectorHandler[] */
, stringHandlers
/* : StringHandlers */
, useImportant
/* : boolean */
)
/* : string[] */
{
  var merged = new OrderedElements();

  for (var i = 0; i < styleTypes.length; i++) {
    merged.addStyleType(styleTypes[i]);
  }

  var plainDeclarations = new OrderedElements();
  var generatedStyles = []; // TODO(emily): benchmark this to see if a plain for loop would be faster.

  merged.forEach(function (val, key) {
    // For each key, see if one of the selector handlers will handle these
    // styles.
    var foundHandler = selectorHandlers.some(function (handler) {
      var result = handler(key, selector, function (newSelector) {
        return generateCSS(newSelector, [val], selectorHandlers, stringHandlers, useImportant);
      });

      if (result != null) {
        // If the handler returned something, add it to the generated
        // CSS and stop looking for another handler.
        if (Array.isArray(result)) {
          generatedStyles.push.apply(generatedStyles, _toConsumableArray(result));
        } else {
          // eslint-disable-next-line
          console.warn('WARNING: Selector handlers should return an array of rules.' + 'Returning a string containing multiple rules is deprecated.', handler);
          generatedStyles.push("@media all {".concat(result, "}"));
        }

        return true;
      }
    }); // If none of the handlers handled it, add it to the list of plain
    // style declarations.

    if (!foundHandler) {
      plainDeclarations.set(key, val, true);
    }
  });
  var generatedRuleset = generateCSSRuleset(selector, plainDeclarations, stringHandlers, useImportant, selectorHandlers);

  if (generatedRuleset) {
    generatedStyles.unshift(generatedRuleset);
  }

  return generatedStyles;
};
/**
 * Helper method of generateCSSRuleset to facilitate custom handling of certain
 * CSS properties. Used for e.g. font families.
 *
 * See generateCSSRuleset for usage and documentation of paramater types.
 */

var runStringHandlers = function runStringHandlers(declarations
/* : OrderedElements */
, stringHandlers
/* : StringHandlers */
, selectorHandlers
/* : SelectorHandler[] */
)
/* : void */
{
  if (!stringHandlers) {
    return;
  }

  var stringHandlerKeys = Object.keys(stringHandlers);

  for (var i = 0; i < stringHandlerKeys.length; i++) {
    var key = stringHandlerKeys[i];

    if (declarations.has(key)) {
      // A declaration exists for this particular string handler, so we
      // need to let the string handler interpret the declaration first
      // before proceeding.
      //
      // TODO(emily): Pass in a callback which generates CSS, similar to
      // how our selector handlers work, instead of passing in
      // `selectorHandlers` and have them make calls to `generateCSS`
      // themselves. Right now, this is impractical because our string
      // handlers are very specialized and do complex things.
      declarations.set(key, stringHandlers[key](declarations.get(key), selectorHandlers), // Preserve order here, since we are really replacing an
      // unprocessed style with a processed style, not overriding an
      // earlier style
      false);
    }
  }
};

var transformRule = function transformRule(key
/* : string */
, value
/* : string */
, transformValue
/* : function */
) {
  return (
    /* : string */
    "".concat(kebabifyStyleName(key), ":").concat(transformValue(key, value), ";")
  );
};

var arrayToObjectKeysReducer = function arrayToObjectKeysReducer(acc, val) {
  acc[val] = true;
  return acc;
};
/**
 * Generate a CSS ruleset with the selector and containing the declarations.
 *
 * This function assumes that the given declarations don't contain any special
 * children (such as media queries, pseudo-selectors, or descendant styles).
 *
 * Note that this method does not deal with nesting used for e.g.
 * psuedo-selectors or media queries. That responsibility is left to  the
 * `generateCSS` function.
 *
 * @param {string} selector: the selector associated with the ruleset
 * @param {Object} declarations: a map from camelCased CSS property name to CSS
 *     property value.
 * @param {Object.<string, function>} stringHandlers: a map from camelCased CSS
 *     property name to a function which will map the given value to the value
 *     that is output.
 * @param {bool} useImportant: A boolean saying whether to append "!important"
 *     to each of the CSS declarations.
 * @returns {string} A string of raw CSS.
 *
 * Examples:
 *
 *    generateCSSRuleset(".blah", { color: "red" })
 *    -> ".blah{color: red !important;}"
 *    generateCSSRuleset(".blah", { color: "red" }, {}, false)
 *    -> ".blah{color: red}"
 *    generateCSSRuleset(".blah", { color: "red" }, {color: c => c.toUpperCase})
 *    -> ".blah{color: RED}"
 *    generateCSSRuleset(".blah:hover", { color: "red" })
 *    -> ".blah:hover{color: red}"
 */


var generateCSSRuleset = function generateCSSRuleset(selector
/* : string */
, declarations
/* : OrderedElements */
, stringHandlers
/* : StringHandlers */
, useImportant
/* : boolean */
, selectorHandlers
/* : SelectorHandler[] */
)
/* : string */
{
  // Mutates declarations
  runStringHandlers(declarations, stringHandlers, selectorHandlers);
  var originalElements = Object.keys(declarations.elements).reduce(arrayToObjectKeysReducer, Object.create(null)); // NOTE(emily): This mutates handledDeclarations.elements.

  var prefixedElements = prefixAll(declarations.elements);
  var elementNames = Object.keys(prefixedElements);

  if (elementNames.length !== declarations.keyOrder.length) {
    // There are some prefixed values, so we need to figure out how to sort
    // them.
    //
    // Loop through prefixedElements, looking for anything that is not in
    // sortOrder, which means it was added by prefixAll. This means that we
    // need to figure out where it should appear in the sortOrder.
    for (var i = 0; i < elementNames.length; i++) {
      if (!originalElements[elementNames[i]]) {
        // This element is not in the sortOrder, which means it is a prefixed
        // value that was added by prefixAll. Let's try to figure out where it
        // goes.
        var originalStyle = void 0;

        if (elementNames[i][0] === 'W') {
          // This is a Webkit-prefixed style, like "WebkitTransition". Let's
          // find its original style's sort order.
          originalStyle = elementNames[i][6].toLowerCase() + elementNames[i].slice(7);
        } else if (elementNames[i][1] === 'o') {
          // This is a Moz-prefixed style, like "MozTransition". We check
          // the second character to avoid colliding with Ms-prefixed
          // styles. Let's find its original style's sort order.
          originalStyle = elementNames[i][3].toLowerCase() + elementNames[i].slice(4);
        } else {
          // if (elementNames[i][1] === 's') {
          // This is a Ms-prefixed style, like "MsTransition".
          originalStyle = elementNames[i][2].toLowerCase() + elementNames[i].slice(3);
        }

        if (originalStyle && originalElements[originalStyle]) {
          var originalIndex = declarations.keyOrder.indexOf(originalStyle);
          declarations.keyOrder.splice(originalIndex, 0, elementNames[i]);
        } else {
          // We don't know what the original style was, so sort it to
          // top. This can happen for styles that are added that don't
          // have the same base name as the original style.
          declarations.keyOrder.unshift(elementNames[i]);
        }
      }
    }
  }

  var transformValue = useImportant === false ? stringifyValue : stringifyAndImportantifyValue;
  var rules = [];

  for (var _i = 0; _i < declarations.keyOrder.length; _i++) {
    var key = declarations.keyOrder[_i];
    var value = prefixedElements[key];

    if (Array.isArray(value)) {
      // inline-style-prefixer returns an array when there should be
      // multiple rules for the same key. Here we flatten to multiple
      // pairs with the same key.
      for (var j = 0; j < value.length; j++) {
        rules.push(transformRule(key, value[j], transformValue));
      }
    } else {
      rules.push(transformRule(key, value, transformValue));
    }
  }

  if (rules.length) {
    return "".concat(selector, "{").concat(rules.join(""), "}");
  } else {
    return "";
  }
};

/* ::
import type { SheetDefinition, SheetDefinitions } from './index.js';
import type { MaybeSheetDefinition } from './exports.js';
import type { SelectorHandler } from './generate.js';
*/
// The current <style> tag we are inserting into, or null if we haven't
// inserted anything yet. We could find this each time using
// `document.querySelector("style[data-aphrodite"])`, but holding onto it is
// faster.

var styleTag
/* : ?HTMLStyleElement */
= null; // Inject a set of rules into a <style> tag in the head of the document. This
// will automatically create a style tag and then continue to use it for
// multiple injections. It will also use a style tag with the `data-aphrodite`
// tag on it if that exists in the DOM. This could be used for e.g. reusing the
// same style tag that server-side rendering inserts.

var injectStyleTag = function injectStyleTag(cssRules
/* : string[] */
) {
  if (styleTag == null) {
    // Try to find a style tag with the `data-aphrodite` attribute first.
    styleTag = document.querySelector("style[data-aphrodite]")
    /* : any */
    ; // If that doesn't work, generate a new style tag.

    if (styleTag == null) {
      // Taken from
      // http://stackoverflow.com/questions/524696/how-to-create-a-style-tag-with-javascript
      var head = document.head || document.getElementsByTagName('head')[0];
      styleTag = document.createElement('style');
      styleTag.type = 'text/css';
      styleTag.setAttribute("data-aphrodite", "");
      head.appendChild(styleTag);
    }
  } // $FlowFixMe


  var sheet = styleTag.styleSheet || styleTag.sheet
  /* : any */
  ;

  if (sheet.insertRule) {
    var numRules = sheet.cssRules.length;
    cssRules.forEach(function (rule) {
      try {
        sheet.insertRule(rule, numRules);
        numRules += 1;
      } catch (e) {// The selector for this rule wasn't compatible with the browser
      }
    });
  } else {
    styleTag.innerText = (styleTag.innerText || '') + cssRules.join('');
  }
}; // Custom handlers for stringifying CSS values that have side effects
// (such as fontFamily, which can cause @font-face rules to be injected)


var stringHandlers = {
  // With fontFamily we look for objects that are passed in and interpret
  // them as @font-face rules that we need to inject. The value of fontFamily
  // can either be a string (as normal), an object (a single font face), or
  // an array of objects and strings.
  fontFamily: function fontFamily(val) {
    if (Array.isArray(val)) {
      var nameMap = {};
      val.forEach(function (v) {
        nameMap[fontFamily(v)] = true;
      });
      return Object.keys(nameMap).join(",");
    } else if (_typeof(val) === "object") {
      injectStyleOnce(val.src, "@font-face", [val], false);
      return "\"".concat(val.fontFamily, "\"");
    } else {
      return val;
    }
  },
  // With animationName we look for an object that contains keyframes and
  // inject them as an `@keyframes` block, returning a uniquely generated
  // name. The keyframes object should look like
  //  animationName: {
  //    from: {
  //      left: 0,
  //      top: 0,
  //    },
  //    '50%': {
  //      left: 15,
  //      top: 5,
  //    },
  //    to: {
  //      left: 20,
  //      top: 20,
  //    }
  //  }
  // TODO(emily): `stringHandlers` doesn't let us rename the key, so I have
  // to use `animationName` here. Improve that so we can call this
  // `animation` instead of `animationName`.
  animationName: function animationName(val, selectorHandlers) {
    if (Array.isArray(val)) {
      return val.map(function (v) {
        return animationName(v, selectorHandlers);
      }).join(",");
    } else if (_typeof(val) === "object") {
      // Generate a unique name based on the hash of the object. We can't
      // just use the hash because the name can't start with a number.
      // TODO(emily): this probably makes debugging hard, allow a custom
      // name?
      var name = "keyframe_".concat(hashObject(val)); // Since keyframes need 3 layers of nesting, we use `generateCSS` to
      // build the inner layers and wrap it in `@keyframes` ourselves.

      var finalVal = "@keyframes ".concat(name, "{"); // TODO see if we can find a way where checking for OrderedElements
      // here is not necessary. Alternatively, perhaps we should have a
      // utility method that can iterate over either a plain object, an
      // instance of OrderedElements, or a Map, and then use that here and
      // elsewhere.

      if (val instanceof OrderedElements) {
        val.forEach(function (valVal, valKey) {
          finalVal += generateCSS(valKey, [valVal], selectorHandlers, stringHandlers, false).join('');
        });
      } else {
        Object.keys(val).forEach(function (key) {
          finalVal += generateCSS(key, [val[key]], selectorHandlers, stringHandlers, false).join('');
        });
      }

      finalVal += '}';
      injectGeneratedCSSOnce(name, [finalVal]);
      return name;
    } else {
      return val;
    }
  }
}; // This is a map from Aphrodite's generated class names to `true` (acting as a
// set of class names)

var alreadyInjected = {}; // This is the buffer of styles which have not yet been flushed.

var injectionBuffer
/* : string[] */
= []; // A flag to tell if we are already buffering styles. This could happen either
// because we scheduled a flush call already, so newly added styles will
// already be flushed, or because we are statically buffering on the server.

var isBuffering = false;

var injectGeneratedCSSOnce = function injectGeneratedCSSOnce(key, generatedCSS) {
  var _injectionBuffer;

  if (alreadyInjected[key]) {
    return;
  }

  if (!isBuffering) {
    // We should never be automatically buffering on the server (or any
    // place without a document), so guard against that.
    if (typeof document === "undefined") {
      throw new Error("Cannot automatically buffer without a document");
    } // If we're not already buffering, schedule a call to flush the
    // current styles.


    isBuffering = true;
    asap__WEBPACK_IMPORTED_MODULE_1___default()(flushToStyleTag);
  }

  (_injectionBuffer = injectionBuffer).push.apply(_injectionBuffer, _toConsumableArray(generatedCSS));

  alreadyInjected[key] = true;
};

var injectStyleOnce = function injectStyleOnce(key
/* : string */
, selector
/* : string */
, definitions
/* : SheetDefinition[] */
, useImportant
/* : boolean */
) {
  var selectorHandlers
  /* : SelectorHandler[] */
  = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : [];

  if (alreadyInjected[key]) {
    return;
  }

  var generated = generateCSS(selector, definitions, selectorHandlers, stringHandlers, useImportant);
  injectGeneratedCSSOnce(key, generated);
};
var reset = function reset() {
  injectionBuffer = [];
  alreadyInjected = {};
  isBuffering = false;
  styleTag = null;
};
var resetInjectedStyle = function resetInjectedStyle(key
/* : string */
) {
  delete alreadyInjected[key];
};
var getBufferedStyles = function getBufferedStyles() {
  return injectionBuffer;
};
var startBuffering = function startBuffering() {
  if (isBuffering) {
    throw new Error("Cannot buffer while already buffering");
  }

  isBuffering = true;
};

var flushToArray = function flushToArray() {
  isBuffering = false;
  var ret = injectionBuffer;
  injectionBuffer = [];
  return ret;
};

var flushToString = function flushToString() {
  return flushToArray().join('');
};
var flushToStyleTag = function flushToStyleTag() {
  var cssRules = flushToArray();

  if (cssRules.length > 0) {
    injectStyleTag(cssRules);
  }
};
var getRenderedClassNames = function getRenderedClassNames()
/* : string[] */
{
  return Object.keys(alreadyInjected);
};
var addRenderedClassNames = function addRenderedClassNames(classNames
/* : string[] */
) {
  classNames.forEach(function (className) {
    alreadyInjected[className] = true;
  });
};

var isValidStyleDefinition = function isValidStyleDefinition(def
/* : Object */
) {
  return "_definition" in def && "_name" in def && "_len" in def;
};

var processStyleDefinitions = function processStyleDefinitions(styleDefinitions
/* : any[] */
, classNameBits
/* : string[] */
, definitionBits
/* : Object[] */
, length
/* : number */
)
/* : number */
{
  for (var i = 0; i < styleDefinitions.length; i += 1) {
    // Filter out falsy values from the input, to allow for
    // `css(a, test && c)`
    if (styleDefinitions[i]) {
      if (Array.isArray(styleDefinitions[i])) {
        // We've encountered an array, so let's recurse
        length += processStyleDefinitions(styleDefinitions[i], classNameBits, definitionBits, length);
      } else if (isValidStyleDefinition(styleDefinitions[i])) {
        classNameBits.push(styleDefinitions[i]._name);
        definitionBits.push(styleDefinitions[i]._definition);
        length += styleDefinitions[i]._len;
      } else {
        throw new Error("Invalid Style Definition: Styles should be defined using the StyleSheet.create method.");
      }
    }
  }

  return length;
};
/**
 * Inject styles associated with the passed style definition objects, and return
 * an associated CSS class name.
 *
 * @param {boolean} useImportant If true, will append !important to generated
 *     CSS output. e.g. {color: red} -> "color: red !important".
 * @param {(Object|Object[])[]} styleDefinitions style definition objects, or
 *     arbitrarily nested arrays of them, as returned as properties of the
 *     return value of StyleSheet.create().
 */


var injectAndGetClassName = function injectAndGetClassName(useImportant
/* : boolean */
, styleDefinitions
/* : MaybeSheetDefinition[] */
, selectorHandlers
/* : SelectorHandler[] */
)
/* : string */
{
  var classNameBits = [];
  var definitionBits = []; // Mutates classNameBits and definitionBits and returns a length which we
  // will append to the hash to decrease the chance of hash collisions.

  var length = processStyleDefinitions(styleDefinitions, classNameBits, definitionBits, 0); // Break if there aren't any valid styles.

  if (classNameBits.length === 0) {
    return "";
  }

  var className;

  if (false) {} else {
    className = classNameBits.join("-o_O-");
  }

  injectStyleOnce(className, ".".concat(className), definitionBits, useImportant, selectorHandlers);
  return className;
};

/* ::
import type { SelectorHandler } from './generate.js';
export type SheetDefinition = { [id:string]: any };
export type SheetDefinitions = SheetDefinition | SheetDefinition[];
type RenderFunction = () => string;
type Extension = {
    selectorHandler: SelectorHandler
};
export type MaybeSheetDefinition = SheetDefinition | false | null | void
*/

var unminifiedHashFn = function unminifiedHashFn(str
/* : string */
, key
/* : string */
) {
  return "".concat(key, "_").concat(hashString(str));
}; // StyleSheet.create is in a hot path so we want to keep as much logic out of it
// as possible. So, we figure out which hash function to use once, and only
// switch it out via minify() as necessary.
//
// This is in an exported function to make it easier to test.


var initialHashFn = function initialHashFn() {
  return  false ? undefined : unminifiedHashFn;
};
var hashFn = initialHashFn();
var StyleSheet = {
  create: function create(sheetDefinition
  /* : SheetDefinition */
  )
  /* : Object */
  {
    var mappedSheetDefinition = {};
    var keys = Object.keys(sheetDefinition);

    for (var i = 0; i < keys.length; i += 1) {
      var key = keys[i];
      var val = sheetDefinition[key];
      var stringVal = JSON.stringify(val);
      mappedSheetDefinition[key] = {
        _len: stringVal.length,
        _name: hashFn(stringVal, key),
        _definition: val
      };
    }

    return mappedSheetDefinition;
  },
  rehydrate: function rehydrate() {
    var renderedClassNames
    /* : string[] */
    = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
    addRenderedClassNames(renderedClassNames);
  }
};
/**
 * Utilities for using Aphrodite server-side.
 *
 * This can be minified out in client-only bundles by replacing `typeof window`
 * with `"object"`, e.g. via Webpack's DefinePlugin:
 *
 *   new webpack.DefinePlugin({
 *     "typeof window": JSON.stringify("object")
 *   })
 */

var StyleSheetServer = typeof window !== 'undefined' ? null : {
  renderStatic: function renderStatic(renderFunc
  /* : RenderFunction */
  ) {
    reset();
    startBuffering();
    var html = renderFunc();
    var cssContent = flushToString();
    return {
      html: html,
      css: {
        content: cssContent,
        renderedClassNames: getRenderedClassNames()
      }
    };
  }
};
/**
 * Utilities for using Aphrodite in tests.
 *
 * Not meant to be used in production.
 */

var StyleSheetTestUtils =  false ? undefined : {
  /**
  * Prevent styles from being injected into the DOM.
  *
  * This is useful in situations where you'd like to test rendering UI
  * components which use Aphrodite without any of the side-effects of
  * Aphrodite happening. Particularly useful for testing the output of
  * components when you have no DOM, e.g. testing in Node without a fake DOM.
  *
  * Should be paired with a subsequent call to
  * clearBufferAndResumeStyleInjection.
  */
  suppressStyleInjection: function suppressStyleInjection() {
    reset();
    startBuffering();
  },

  /**
  * Opposite method of preventStyleInject.
  */
  clearBufferAndResumeStyleInjection: function clearBufferAndResumeStyleInjection() {
    reset();
  },

  /**
  * Returns a string of buffered styles which have not been flushed
  *
  * @returns {string}  Buffer of styles which have not yet been flushed.
  */
  getBufferedStyles: function getBufferedStyles$1() {
    return getBufferedStyles();
  }
};
/**
 * Generate the Aphrodite API exports, with given `selectorHandlers` and
 * `useImportant` state.
 */

function makeExports(useImportant
/* : boolean */
) {
  var selectorHandlers
  /* : SelectorHandler[] */
  = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : defaultSelectorHandlers;
  return {
    StyleSheet: _objectSpread({}, StyleSheet, {
      /**
       * Returns a version of the exports of Aphrodite (i.e. an object
       * with `css` and `StyleSheet` properties) which have some
       * extensions included.
       *
       * @param {Array.<Object>} extensions: An array of extensions to
       *     add to this instance of Aphrodite. Each object should have a
       *     single property on it, defining which kind of extension to
       *     add.
       * @param {SelectorHandler} [extensions[].selectorHandler]: A
       *     selector handler extension. See `defaultSelectorHandlers` in
       *     generate.js.
       *
       * @returns {Object} An object containing the exports of the new
       *     instance of Aphrodite.
       */
      extend: function extend(extensions
      /* : Extension[] */
      ) {
        var extensionSelectorHandlers = extensions // Pull out extensions with a selectorHandler property
        .map(function (extension) {
          return extension.selectorHandler;
        }) // Remove nulls (i.e. extensions without a selectorHandler property).
        .filter(function (handler) {
          return handler;
        });
        return makeExports(useImportant, selectorHandlers.concat(extensionSelectorHandlers));
      }
    }),
    StyleSheetServer: StyleSheetServer,
    StyleSheetTestUtils: StyleSheetTestUtils,
    minify: function minify(shouldMinify
    /* : boolean */
    ) {
      hashFn = shouldMinify ? hashString : unminifiedHashFn;
    },
    css: function css()
    /* : MaybeSheetDefinition[] */
    {
      for (var _len = arguments.length, styleDefinitions = new Array(_len), _key = 0; _key < _len; _key++) {
        styleDefinitions[_key] = arguments[_key];
      }

      return injectAndGetClassName(useImportant, styleDefinitions, selectorHandlers);
    },
    flushToStyleTag: flushToStyleTag,
    injectAndGetClassName: injectAndGetClassName,
    defaultSelectorHandlers: defaultSelectorHandlers,
    reset: reset,
    resetInjectedStyle: resetInjectedStyle
  };
}




/***/ }),

/***/ "./node_modules/aphrodite/es/index.js":
/*!********************************************!*\
  !*** ./node_modules/aphrodite/es/index.js ***!
  \********************************************/
/*! exports provided: StyleSheet, StyleSheetServer, StyleSheetTestUtils, css, minify, flushToStyleTag, injectAndGetClassName, defaultSelectorHandlers, reset, resetInjectedStyle */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "StyleSheet", function() { return StyleSheet; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "StyleSheetServer", function() { return StyleSheetServer; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "StyleSheetTestUtils", function() { return StyleSheetTestUtils; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "css", function() { return css; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "minify", function() { return minify; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "flushToStyleTag", function() { return flushToStyleTag; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "injectAndGetClassName", function() { return injectAndGetClassName; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "defaultSelectorHandlers", function() { return defaultSelectorHandlers; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "reset", function() { return reset; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "resetInjectedStyle", function() { return resetInjectedStyle; });
/* harmony import */ var _chunk_febce46b_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./chunk-febce46b.js */ "./node_modules/aphrodite/es/chunk-febce46b.js");
/* harmony import */ var string_hash__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! string-hash */ "./node_modules/string-hash/index.js");
/* harmony import */ var string_hash__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(string_hash__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var asap__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! asap */ "./node_modules/asap/browser-asap.js");
/* harmony import */ var asap__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(asap__WEBPACK_IMPORTED_MODULE_2__);




var useImportant = true; // Add !important to all style definitions

var Aphrodite = Object(_chunk_febce46b_js__WEBPACK_IMPORTED_MODULE_0__["a"])(useImportant);
var StyleSheet = Aphrodite.StyleSheet,
    StyleSheetServer = Aphrodite.StyleSheetServer,
    StyleSheetTestUtils = Aphrodite.StyleSheetTestUtils,
    css = Aphrodite.css,
    minify = Aphrodite.minify,
    flushToStyleTag = Aphrodite.flushToStyleTag,
    injectAndGetClassName = Aphrodite.injectAndGetClassName,
    defaultSelectorHandlers = Aphrodite.defaultSelectorHandlers,
    reset = Aphrodite.reset,
    resetInjectedStyle = Aphrodite.resetInjectedStyle;




/***/ }),

/***/ "./node_modules/asap/browser-asap.js":
/*!*******************************************!*\
  !*** ./node_modules/asap/browser-asap.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


// rawAsap provides everything we need except exception management.
var rawAsap = __webpack_require__(/*! ./raw */ "./node_modules/asap/browser-raw.js");
// RawTasks are recycled to reduce GC churn.
var freeTasks = [];
// We queue errors to ensure they are thrown in right order (FIFO).
// Array-as-queue is good enough here, since we are just dealing with exceptions.
var pendingErrors = [];
var requestErrorThrow = rawAsap.makeRequestCallFromTimer(throwFirstError);

function throwFirstError() {
    if (pendingErrors.length) {
        throw pendingErrors.shift();
    }
}

/**
 * Calls a task as soon as possible after returning, in its own event, with priority
 * over other events like animation, reflow, and repaint. An error thrown from an
 * event will not interrupt, nor even substantially slow down the processing of
 * other events, but will be rather postponed to a lower priority event.
 * @param {{call}} task A callable object, typically a function that takes no
 * arguments.
 */
module.exports = asap;
function asap(task) {
    var rawTask;
    if (freeTasks.length) {
        rawTask = freeTasks.pop();
    } else {
        rawTask = new RawTask();
    }
    rawTask.task = task;
    rawAsap(rawTask);
}

// We wrap tasks with recyclable task objects.  A task object implements
// `call`, just like a function.
function RawTask() {
    this.task = null;
}

// The sole purpose of wrapping the task is to catch the exception and recycle
// the task object after its single use.
RawTask.prototype.call = function () {
    try {
        this.task.call();
    } catch (error) {
        if (asap.onerror) {
            // This hook exists purely for testing purposes.
            // Its name will be periodically randomized to break any code that
            // depends on its existence.
            asap.onerror(error);
        } else {
            // In a web browser, exceptions are not fatal. However, to avoid
            // slowing down the queue of pending tasks, we rethrow the error in a
            // lower priority turn.
            pendingErrors.push(error);
            requestErrorThrow();
        }
    } finally {
        this.task = null;
        freeTasks[freeTasks.length] = this;
    }
};


/***/ }),

/***/ "./node_modules/asap/browser-raw.js":
/*!******************************************!*\
  !*** ./node_modules/asap/browser-raw.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global) {

// Use the fastest means possible to execute a task in its own turn, with
// priority over other events including IO, animation, reflow, and redraw
// events in browsers.
//
// An exception thrown by a task will permanently interrupt the processing of
// subsequent tasks. The higher level `asap` function ensures that if an
// exception is thrown by a task, that the task queue will continue flushing as
// soon as possible, but if you use `rawAsap` directly, you are responsible to
// either ensure that no exceptions are thrown from your task, or to manually
// call `rawAsap.requestFlush` if an exception is thrown.
module.exports = rawAsap;
function rawAsap(task) {
    if (!queue.length) {
        requestFlush();
        flushing = true;
    }
    // Equivalent to push, but avoids a function call.
    queue[queue.length] = task;
}

var queue = [];
// Once a flush has been requested, no further calls to `requestFlush` are
// necessary until the next `flush` completes.
var flushing = false;
// `requestFlush` is an implementation-specific method that attempts to kick
// off a `flush` event as quickly as possible. `flush` will attempt to exhaust
// the event queue before yielding to the browser's own event loop.
var requestFlush;
// The position of the next task to execute in the task queue. This is
// preserved between calls to `flush` so that it can be resumed if
// a task throws an exception.
var index = 0;
// If a task schedules additional tasks recursively, the task queue can grow
// unbounded. To prevent memory exhaustion, the task queue will periodically
// truncate already-completed tasks.
var capacity = 1024;

// The flush function processes all tasks that have been scheduled with
// `rawAsap` unless and until one of those tasks throws an exception.
// If a task throws an exception, `flush` ensures that its state will remain
// consistent and will resume where it left off when called again.
// However, `flush` does not make any arrangements to be called again if an
// exception is thrown.
function flush() {
    while (index < queue.length) {
        var currentIndex = index;
        // Advance the index before calling the task. This ensures that we will
        // begin flushing on the next task the task throws an error.
        index = index + 1;
        queue[currentIndex].call();
        // Prevent leaking memory for long chains of recursive calls to `asap`.
        // If we call `asap` within tasks scheduled by `asap`, the queue will
        // grow, but to avoid an O(n) walk for every task we execute, we don't
        // shift tasks off the queue after they have been executed.
        // Instead, we periodically shift 1024 tasks off the queue.
        if (index > capacity) {
            // Manually shift all values starting at the index back to the
            // beginning of the queue.
            for (var scan = 0, newLength = queue.length - index; scan < newLength; scan++) {
                queue[scan] = queue[scan + index];
            }
            queue.length -= index;
            index = 0;
        }
    }
    queue.length = 0;
    index = 0;
    flushing = false;
}

// `requestFlush` is implemented using a strategy based on data collected from
// every available SauceLabs Selenium web driver worker at time of writing.
// https://docs.google.com/spreadsheets/d/1mG-5UYGup5qxGdEMWkhP6BWCz053NUb2E1QoUTU16uA/edit#gid=783724593

// Safari 6 and 6.1 for desktop, iPad, and iPhone are the only browsers that
// have WebKitMutationObserver but not un-prefixed MutationObserver.
// Must use `global` or `self` instead of `window` to work in both frames and web
// workers. `global` is a provision of Browserify, Mr, Mrs, or Mop.

/* globals self */
var scope = typeof global !== "undefined" ? global : self;
var BrowserMutationObserver = scope.MutationObserver || scope.WebKitMutationObserver;

// MutationObservers are desirable because they have high priority and work
// reliably everywhere they are implemented.
// They are implemented in all modern browsers.
//
// - Android 4-4.3
// - Chrome 26-34
// - Firefox 14-29
// - Internet Explorer 11
// - iPad Safari 6-7.1
// - iPhone Safari 7-7.1
// - Safari 6-7
if (typeof BrowserMutationObserver === "function") {
    requestFlush = makeRequestCallFromMutationObserver(flush);

// MessageChannels are desirable because they give direct access to the HTML
// task queue, are implemented in Internet Explorer 10, Safari 5.0-1, and Opera
// 11-12, and in web workers in many engines.
// Although message channels yield to any queued rendering and IO tasks, they
// would be better than imposing the 4ms delay of timers.
// However, they do not work reliably in Internet Explorer or Safari.

// Internet Explorer 10 is the only browser that has setImmediate but does
// not have MutationObservers.
// Although setImmediate yields to the browser's renderer, it would be
// preferrable to falling back to setTimeout since it does not have
// the minimum 4ms penalty.
// Unfortunately there appears to be a bug in Internet Explorer 10 Mobile (and
// Desktop to a lesser extent) that renders both setImmediate and
// MessageChannel useless for the purposes of ASAP.
// https://github.com/kriskowal/q/issues/396

// Timers are implemented universally.
// We fall back to timers in workers in most engines, and in foreground
// contexts in the following browsers.
// However, note that even this simple case requires nuances to operate in a
// broad spectrum of browsers.
//
// - Firefox 3-13
// - Internet Explorer 6-9
// - iPad Safari 4.3
// - Lynx 2.8.7
} else {
    requestFlush = makeRequestCallFromTimer(flush);
}

// `requestFlush` requests that the high priority event queue be flushed as
// soon as possible.
// This is useful to prevent an error thrown in a task from stalling the event
// queue if the exception handled by Node.js’s
// `process.on("uncaughtException")` or by a domain.
rawAsap.requestFlush = requestFlush;

// To request a high priority event, we induce a mutation observer by toggling
// the text of a text node between "1" and "-1".
function makeRequestCallFromMutationObserver(callback) {
    var toggle = 1;
    var observer = new BrowserMutationObserver(callback);
    var node = document.createTextNode("");
    observer.observe(node, {characterData: true});
    return function requestCall() {
        toggle = -toggle;
        node.data = toggle;
    };
}

// The message channel technique was discovered by Malte Ubl and was the
// original foundation for this library.
// http://www.nonblocking.io/2011/06/windownexttick.html

// Safari 6.0.5 (at least) intermittently fails to create message ports on a
// page's first load. Thankfully, this version of Safari supports
// MutationObservers, so we don't need to fall back in that case.

// function makeRequestCallFromMessageChannel(callback) {
//     var channel = new MessageChannel();
//     channel.port1.onmessage = callback;
//     return function requestCall() {
//         channel.port2.postMessage(0);
//     };
// }

// For reasons explained above, we are also unable to use `setImmediate`
// under any circumstances.
// Even if we were, there is another bug in Internet Explorer 10.
// It is not sufficient to assign `setImmediate` to `requestFlush` because
// `setImmediate` must be called *by name* and therefore must be wrapped in a
// closure.
// Never forget.

// function makeRequestCallFromSetImmediate(callback) {
//     return function requestCall() {
//         setImmediate(callback);
//     };
// }

// Safari 6.0 has a problem where timers will get lost while the user is
// scrolling. This problem does not impact ASAP because Safari 6.0 supports
// mutation observers, so that implementation is used instead.
// However, if we ever elect to use timers in Safari, the prevalent work-around
// is to add a scroll event listener that calls for a flush.

// `setTimeout` does not call the passed callback if the delay is less than
// approximately 7 in web workers in Firefox 8 through 18, and sometimes not
// even then.

function makeRequestCallFromTimer(callback) {
    return function requestCall() {
        // We dispatch a timeout with a specified delay of 0 for engines that
        // can reliably accommodate that request. This will usually be snapped
        // to a 4 milisecond delay, but once we're flushing, there's no delay
        // between events.
        var timeoutHandle = setTimeout(handleTimer, 0);
        // However, since this timer gets frequently dropped in Firefox
        // workers, we enlist an interval handle that will try to fire
        // an event 20 times per second until it succeeds.
        var intervalHandle = setInterval(handleTimer, 50);

        function handleTimer() {
            // Whichever timer succeeds will cancel both timers and
            // execute the callback.
            clearTimeout(timeoutHandle);
            clearInterval(intervalHandle);
            callback();
        }
    };
}

// This is for `asap.js` only.
// Its name will be periodically randomized to break any code that depends on
// its existence.
rawAsap.makeRequestCallFromTimer = makeRequestCallFromTimer;

// ASAP was originally a nextTick shim included in Q. This was factored out
// into this ASAP package. It was later adapted to RSVP which made further
// amendments. These decisions, particularly to marginalize MessageChannel and
// to capture the MutationObserver implementation in a closure, were integrated
// back into ASAP proper.
// https://github.com/tildeio/rsvp.js/blob/cddf7232546a9cf858524b75cde6f9edf72620a7/lib/rsvp/asap.js

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "./node_modules/core-js/modules/_a-function.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_a-function.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function (it) {
  if (typeof it != 'function') throw TypeError(it + ' is not a function!');
  return it;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_add-to-unscopables.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/modules/_add-to-unscopables.js ***!
  \*************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 22.1.3.31 Array.prototype[@@unscopables]
var UNSCOPABLES = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('unscopables');
var ArrayProto = Array.prototype;
if (ArrayProto[UNSCOPABLES] == undefined) __webpack_require__(/*! ./_hide */ "./node_modules/core-js/modules/_hide.js")(ArrayProto, UNSCOPABLES, {});
module.exports = function (key) {
  ArrayProto[UNSCOPABLES][key] = true;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_an-instance.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_an-instance.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function (it, Constructor, name, forbiddenField) {
  if (!(it instanceof Constructor) || (forbiddenField !== undefined && forbiddenField in it)) {
    throw TypeError(name + ': incorrect invocation!');
  } return it;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_an-object.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_an-object.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
module.exports = function (it) {
  if (!isObject(it)) throw TypeError(it + ' is not an object!');
  return it;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_array-includes.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/modules/_array-includes.js ***!
  \*********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// false -> Array#indexOf
// true  -> Array#includes
var toIObject = __webpack_require__(/*! ./_to-iobject */ "./node_modules/core-js/modules/_to-iobject.js");
var toLength = __webpack_require__(/*! ./_to-length */ "./node_modules/core-js/modules/_to-length.js");
var toAbsoluteIndex = __webpack_require__(/*! ./_to-absolute-index */ "./node_modules/core-js/modules/_to-absolute-index.js");
module.exports = function (IS_INCLUDES) {
  return function ($this, el, fromIndex) {
    var O = toIObject($this);
    var length = toLength(O.length);
    var index = toAbsoluteIndex(fromIndex, length);
    var value;
    // Array#includes uses SameValueZero equality algorithm
    // eslint-disable-next-line no-self-compare
    if (IS_INCLUDES && el != el) while (length > index) {
      value = O[index++];
      // eslint-disable-next-line no-self-compare
      if (value != value) return true;
    // Array#indexOf ignores holes, Array#includes - not
    } else for (;length > index; index++) if (IS_INCLUDES || index in O) {
      if (O[index] === el) return IS_INCLUDES || index || 0;
    } return !IS_INCLUDES && -1;
  };
};


/***/ }),

/***/ "./node_modules/core-js/modules/_classof.js":
/*!**************************************************!*\
  !*** ./node_modules/core-js/modules/_classof.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// getting tag from 19.1.3.6 Object.prototype.toString()
var cof = __webpack_require__(/*! ./_cof */ "./node_modules/core-js/modules/_cof.js");
var TAG = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('toStringTag');
// ES3 wrong here
var ARG = cof(function () { return arguments; }()) == 'Arguments';

// fallback for IE11 Script Access Denied error
var tryGet = function (it, key) {
  try {
    return it[key];
  } catch (e) { /* empty */ }
};

module.exports = function (it) {
  var O, T, B;
  return it === undefined ? 'Undefined' : it === null ? 'Null'
    // @@toStringTag case
    : typeof (T = tryGet(O = Object(it), TAG)) == 'string' ? T
    // builtinTag case
    : ARG ? cof(O)
    // ES3 arguments fallback
    : (B = cof(O)) == 'Object' && typeof O.callee == 'function' ? 'Arguments' : B;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_cof.js":
/*!**********************************************!*\
  !*** ./node_modules/core-js/modules/_cof.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

var toString = {}.toString;

module.exports = function (it) {
  return toString.call(it).slice(8, -1);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_core.js":
/*!***********************************************!*\
  !*** ./node_modules/core-js/modules/_core.js ***!
  \***********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

var core = module.exports = { version: '2.5.7' };
if (typeof __e == 'number') __e = core; // eslint-disable-line no-undef


/***/ }),

/***/ "./node_modules/core-js/modules/_create-property.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/modules/_create-property.js ***!
  \**********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var $defineProperty = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js");
var createDesc = __webpack_require__(/*! ./_property-desc */ "./node_modules/core-js/modules/_property-desc.js");

module.exports = function (object, index, value) {
  if (index in object) $defineProperty.f(object, index, createDesc(0, value));
  else object[index] = value;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_ctx.js":
/*!**********************************************!*\
  !*** ./node_modules/core-js/modules/_ctx.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// optional / simple context binding
var aFunction = __webpack_require__(/*! ./_a-function */ "./node_modules/core-js/modules/_a-function.js");
module.exports = function (fn, that, length) {
  aFunction(fn);
  if (that === undefined) return fn;
  switch (length) {
    case 1: return function (a) {
      return fn.call(that, a);
    };
    case 2: return function (a, b) {
      return fn.call(that, a, b);
    };
    case 3: return function (a, b, c) {
      return fn.call(that, a, b, c);
    };
  }
  return function (/* ...args */) {
    return fn.apply(that, arguments);
  };
};


/***/ }),

/***/ "./node_modules/core-js/modules/_defined.js":
/*!**************************************************!*\
  !*** ./node_modules/core-js/modules/_defined.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// 7.2.1 RequireObjectCoercible(argument)
module.exports = function (it) {
  if (it == undefined) throw TypeError("Can't call method on  " + it);
  return it;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_descriptors.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_descriptors.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// Thank's IE8 for his funny defineProperty
module.exports = !__webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js")(function () {
  return Object.defineProperty({}, 'a', { get: function () { return 7; } }).a != 7;
});


/***/ }),

/***/ "./node_modules/core-js/modules/_dom-create.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_dom-create.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
var document = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js").document;
// typeof document.createElement is 'object' in old IE
var is = isObject(document) && isObject(document.createElement);
module.exports = function (it) {
  return is ? document.createElement(it) : {};
};


/***/ }),

/***/ "./node_modules/core-js/modules/_enum-bug-keys.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/_enum-bug-keys.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// IE 8- don't enum bug keys
module.exports = (
  'constructor,hasOwnProperty,isPrototypeOf,propertyIsEnumerable,toLocaleString,toString,valueOf'
).split(',');


/***/ }),

/***/ "./node_modules/core-js/modules/_enum-keys.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_enum-keys.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// all enumerable object keys, includes symbols
var getKeys = __webpack_require__(/*! ./_object-keys */ "./node_modules/core-js/modules/_object-keys.js");
var gOPS = __webpack_require__(/*! ./_object-gops */ "./node_modules/core-js/modules/_object-gops.js");
var pIE = __webpack_require__(/*! ./_object-pie */ "./node_modules/core-js/modules/_object-pie.js");
module.exports = function (it) {
  var result = getKeys(it);
  var getSymbols = gOPS.f;
  if (getSymbols) {
    var symbols = getSymbols(it);
    var isEnum = pIE.f;
    var i = 0;
    var key;
    while (symbols.length > i) if (isEnum.call(it, key = symbols[i++])) result.push(key);
  } return result;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_export.js":
/*!*************************************************!*\
  !*** ./node_modules/core-js/modules/_export.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var core = __webpack_require__(/*! ./_core */ "./node_modules/core-js/modules/_core.js");
var hide = __webpack_require__(/*! ./_hide */ "./node_modules/core-js/modules/_hide.js");
var redefine = __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js");
var ctx = __webpack_require__(/*! ./_ctx */ "./node_modules/core-js/modules/_ctx.js");
var PROTOTYPE = 'prototype';

var $export = function (type, name, source) {
  var IS_FORCED = type & $export.F;
  var IS_GLOBAL = type & $export.G;
  var IS_STATIC = type & $export.S;
  var IS_PROTO = type & $export.P;
  var IS_BIND = type & $export.B;
  var target = IS_GLOBAL ? global : IS_STATIC ? global[name] || (global[name] = {}) : (global[name] || {})[PROTOTYPE];
  var exports = IS_GLOBAL ? core : core[name] || (core[name] = {});
  var expProto = exports[PROTOTYPE] || (exports[PROTOTYPE] = {});
  var key, own, out, exp;
  if (IS_GLOBAL) source = name;
  for (key in source) {
    // contains in native
    own = !IS_FORCED && target && target[key] !== undefined;
    // export native or passed
    out = (own ? target : source)[key];
    // bind timers to global for call from export context
    exp = IS_BIND && own ? ctx(out, global) : IS_PROTO && typeof out == 'function' ? ctx(Function.call, out) : out;
    // extend global
    if (target) redefine(target, key, out, type & $export.U);
    // export
    if (exports[key] != out) hide(exports, key, exp);
    if (IS_PROTO && expProto[key] != out) expProto[key] = out;
  }
};
global.core = core;
// type bitmap
$export.F = 1;   // forced
$export.G = 2;   // global
$export.S = 4;   // static
$export.P = 8;   // proto
$export.B = 16;  // bind
$export.W = 32;  // wrap
$export.U = 64;  // safe
$export.R = 128; // real proto method for `library`
module.exports = $export;


/***/ }),

/***/ "./node_modules/core-js/modules/_fails.js":
/*!************************************************!*\
  !*** ./node_modules/core-js/modules/_fails.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function (exec) {
  try {
    return !!exec();
  } catch (e) {
    return true;
  }
};


/***/ }),

/***/ "./node_modules/core-js/modules/_fix-re-wks.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_fix-re-wks.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var hide = __webpack_require__(/*! ./_hide */ "./node_modules/core-js/modules/_hide.js");
var redefine = __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js");
var fails = __webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js");
var defined = __webpack_require__(/*! ./_defined */ "./node_modules/core-js/modules/_defined.js");
var wks = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js");

module.exports = function (KEY, length, exec) {
  var SYMBOL = wks(KEY);
  var fns = exec(defined, SYMBOL, ''[KEY]);
  var strfn = fns[0];
  var rxfn = fns[1];
  if (fails(function () {
    var O = {};
    O[SYMBOL] = function () { return 7; };
    return ''[KEY](O) != 7;
  })) {
    redefine(String.prototype, KEY, strfn);
    hide(RegExp.prototype, SYMBOL, length == 2
      // 21.2.5.8 RegExp.prototype[@@replace](string, replaceValue)
      // 21.2.5.11 RegExp.prototype[@@split](string, limit)
      ? function (string, arg) { return rxfn.call(string, this, arg); }
      // 21.2.5.6 RegExp.prototype[@@match](string)
      // 21.2.5.9 RegExp.prototype[@@search](string)
      : function (string) { return rxfn.call(string, this); }
    );
  }
};


/***/ }),

/***/ "./node_modules/core-js/modules/_flags.js":
/*!************************************************!*\
  !*** ./node_modules/core-js/modules/_flags.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

// 21.2.5.3 get RegExp.prototype.flags
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
module.exports = function () {
  var that = anObject(this);
  var result = '';
  if (that.global) result += 'g';
  if (that.ignoreCase) result += 'i';
  if (that.multiline) result += 'm';
  if (that.unicode) result += 'u';
  if (that.sticky) result += 'y';
  return result;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_for-of.js":
/*!*************************************************!*\
  !*** ./node_modules/core-js/modules/_for-of.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var ctx = __webpack_require__(/*! ./_ctx */ "./node_modules/core-js/modules/_ctx.js");
var call = __webpack_require__(/*! ./_iter-call */ "./node_modules/core-js/modules/_iter-call.js");
var isArrayIter = __webpack_require__(/*! ./_is-array-iter */ "./node_modules/core-js/modules/_is-array-iter.js");
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var toLength = __webpack_require__(/*! ./_to-length */ "./node_modules/core-js/modules/_to-length.js");
var getIterFn = __webpack_require__(/*! ./core.get-iterator-method */ "./node_modules/core-js/modules/core.get-iterator-method.js");
var BREAK = {};
var RETURN = {};
var exports = module.exports = function (iterable, entries, fn, that, ITERATOR) {
  var iterFn = ITERATOR ? function () { return iterable; } : getIterFn(iterable);
  var f = ctx(fn, that, entries ? 2 : 1);
  var index = 0;
  var length, step, iterator, result;
  if (typeof iterFn != 'function') throw TypeError(iterable + ' is not iterable!');
  // fast case for arrays with default iterator
  if (isArrayIter(iterFn)) for (length = toLength(iterable.length); length > index; index++) {
    result = entries ? f(anObject(step = iterable[index])[0], step[1]) : f(iterable[index]);
    if (result === BREAK || result === RETURN) return result;
  } else for (iterator = iterFn.call(iterable); !(step = iterator.next()).done;) {
    result = call(iterator, f, step.value, entries);
    if (result === BREAK || result === RETURN) return result;
  }
};
exports.BREAK = BREAK;
exports.RETURN = RETURN;


/***/ }),

/***/ "./node_modules/core-js/modules/_global.js":
/*!*************************************************!*\
  !*** ./node_modules/core-js/modules/_global.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
var global = module.exports = typeof window != 'undefined' && window.Math == Math
  ? window : typeof self != 'undefined' && self.Math == Math ? self
  // eslint-disable-next-line no-new-func
  : Function('return this')();
if (typeof __g == 'number') __g = global; // eslint-disable-line no-undef


/***/ }),

/***/ "./node_modules/core-js/modules/_has.js":
/*!**********************************************!*\
  !*** ./node_modules/core-js/modules/_has.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

var hasOwnProperty = {}.hasOwnProperty;
module.exports = function (it, key) {
  return hasOwnProperty.call(it, key);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_hide.js":
/*!***********************************************!*\
  !*** ./node_modules/core-js/modules/_hide.js ***!
  \***********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var dP = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js");
var createDesc = __webpack_require__(/*! ./_property-desc */ "./node_modules/core-js/modules/_property-desc.js");
module.exports = __webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js") ? function (object, key, value) {
  return dP.f(object, key, createDesc(1, value));
} : function (object, key, value) {
  object[key] = value;
  return object;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_html.js":
/*!***********************************************!*\
  !*** ./node_modules/core-js/modules/_html.js ***!
  \***********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var document = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js").document;
module.exports = document && document.documentElement;


/***/ }),

/***/ "./node_modules/core-js/modules/_ie8-dom-define.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/modules/_ie8-dom-define.js ***!
  \*********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = !__webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js") && !__webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js")(function () {
  return Object.defineProperty(__webpack_require__(/*! ./_dom-create */ "./node_modules/core-js/modules/_dom-create.js")('div'), 'a', { get: function () { return 7; } }).a != 7;
});


/***/ }),

/***/ "./node_modules/core-js/modules/_inherit-if-required.js":
/*!**************************************************************!*\
  !*** ./node_modules/core-js/modules/_inherit-if-required.js ***!
  \**************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
var setPrototypeOf = __webpack_require__(/*! ./_set-proto */ "./node_modules/core-js/modules/_set-proto.js").set;
module.exports = function (that, target, C) {
  var S = target.constructor;
  var P;
  if (S !== C && typeof S == 'function' && (P = S.prototype) !== C.prototype && isObject(P) && setPrototypeOf) {
    setPrototypeOf(that, P);
  } return that;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_invoke.js":
/*!*************************************************!*\
  !*** ./node_modules/core-js/modules/_invoke.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// fast apply, http://jsperf.lnkit.com/fast-apply/5
module.exports = function (fn, args, that) {
  var un = that === undefined;
  switch (args.length) {
    case 0: return un ? fn()
                      : fn.call(that);
    case 1: return un ? fn(args[0])
                      : fn.call(that, args[0]);
    case 2: return un ? fn(args[0], args[1])
                      : fn.call(that, args[0], args[1]);
    case 3: return un ? fn(args[0], args[1], args[2])
                      : fn.call(that, args[0], args[1], args[2]);
    case 4: return un ? fn(args[0], args[1], args[2], args[3])
                      : fn.call(that, args[0], args[1], args[2], args[3]);
  } return fn.apply(that, args);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_iobject.js":
/*!**************************************************!*\
  !*** ./node_modules/core-js/modules/_iobject.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// fallback for non-array-like ES3 and non-enumerable old V8 strings
var cof = __webpack_require__(/*! ./_cof */ "./node_modules/core-js/modules/_cof.js");
// eslint-disable-next-line no-prototype-builtins
module.exports = Object('z').propertyIsEnumerable(0) ? Object : function (it) {
  return cof(it) == 'String' ? it.split('') : Object(it);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_is-array-iter.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/_is-array-iter.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// check on default Array iterator
var Iterators = __webpack_require__(/*! ./_iterators */ "./node_modules/core-js/modules/_iterators.js");
var ITERATOR = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('iterator');
var ArrayProto = Array.prototype;

module.exports = function (it) {
  return it !== undefined && (Iterators.Array === it || ArrayProto[ITERATOR] === it);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_is-array.js":
/*!***************************************************!*\
  !*** ./node_modules/core-js/modules/_is-array.js ***!
  \***************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 7.2.2 IsArray(argument)
var cof = __webpack_require__(/*! ./_cof */ "./node_modules/core-js/modules/_cof.js");
module.exports = Array.isArray || function isArray(arg) {
  return cof(arg) == 'Array';
};


/***/ }),

/***/ "./node_modules/core-js/modules/_is-object.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_is-object.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function (it) {
  return typeof it === 'object' ? it !== null : typeof it === 'function';
};


/***/ }),

/***/ "./node_modules/core-js/modules/_is-regexp.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_is-regexp.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 7.2.8 IsRegExp(argument)
var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
var cof = __webpack_require__(/*! ./_cof */ "./node_modules/core-js/modules/_cof.js");
var MATCH = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('match');
module.exports = function (it) {
  var isRegExp;
  return isObject(it) && ((isRegExp = it[MATCH]) !== undefined ? !!isRegExp : cof(it) == 'RegExp');
};


/***/ }),

/***/ "./node_modules/core-js/modules/_iter-call.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_iter-call.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// call something on iterator step with safe closing on error
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
module.exports = function (iterator, fn, value, entries) {
  try {
    return entries ? fn(anObject(value)[0], value[1]) : fn(value);
  // 7.4.6 IteratorClose(iterator, completion)
  } catch (e) {
    var ret = iterator['return'];
    if (ret !== undefined) anObject(ret.call(iterator));
    throw e;
  }
};


/***/ }),

/***/ "./node_modules/core-js/modules/_iter-create.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_iter-create.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var create = __webpack_require__(/*! ./_object-create */ "./node_modules/core-js/modules/_object-create.js");
var descriptor = __webpack_require__(/*! ./_property-desc */ "./node_modules/core-js/modules/_property-desc.js");
var setToStringTag = __webpack_require__(/*! ./_set-to-string-tag */ "./node_modules/core-js/modules/_set-to-string-tag.js");
var IteratorPrototype = {};

// 25.1.2.1.1 %IteratorPrototype%[@@iterator]()
__webpack_require__(/*! ./_hide */ "./node_modules/core-js/modules/_hide.js")(IteratorPrototype, __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('iterator'), function () { return this; });

module.exports = function (Constructor, NAME, next) {
  Constructor.prototype = create(IteratorPrototype, { next: descriptor(1, next) });
  setToStringTag(Constructor, NAME + ' Iterator');
};


/***/ }),

/***/ "./node_modules/core-js/modules/_iter-define.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_iter-define.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var LIBRARY = __webpack_require__(/*! ./_library */ "./node_modules/core-js/modules/_library.js");
var $export = __webpack_require__(/*! ./_export */ "./node_modules/core-js/modules/_export.js");
var redefine = __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js");
var hide = __webpack_require__(/*! ./_hide */ "./node_modules/core-js/modules/_hide.js");
var Iterators = __webpack_require__(/*! ./_iterators */ "./node_modules/core-js/modules/_iterators.js");
var $iterCreate = __webpack_require__(/*! ./_iter-create */ "./node_modules/core-js/modules/_iter-create.js");
var setToStringTag = __webpack_require__(/*! ./_set-to-string-tag */ "./node_modules/core-js/modules/_set-to-string-tag.js");
var getPrototypeOf = __webpack_require__(/*! ./_object-gpo */ "./node_modules/core-js/modules/_object-gpo.js");
var ITERATOR = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('iterator');
var BUGGY = !([].keys && 'next' in [].keys()); // Safari has buggy iterators w/o `next`
var FF_ITERATOR = '@@iterator';
var KEYS = 'keys';
var VALUES = 'values';

var returnThis = function () { return this; };

module.exports = function (Base, NAME, Constructor, next, DEFAULT, IS_SET, FORCED) {
  $iterCreate(Constructor, NAME, next);
  var getMethod = function (kind) {
    if (!BUGGY && kind in proto) return proto[kind];
    switch (kind) {
      case KEYS: return function keys() { return new Constructor(this, kind); };
      case VALUES: return function values() { return new Constructor(this, kind); };
    } return function entries() { return new Constructor(this, kind); };
  };
  var TAG = NAME + ' Iterator';
  var DEF_VALUES = DEFAULT == VALUES;
  var VALUES_BUG = false;
  var proto = Base.prototype;
  var $native = proto[ITERATOR] || proto[FF_ITERATOR] || DEFAULT && proto[DEFAULT];
  var $default = $native || getMethod(DEFAULT);
  var $entries = DEFAULT ? !DEF_VALUES ? $default : getMethod('entries') : undefined;
  var $anyNative = NAME == 'Array' ? proto.entries || $native : $native;
  var methods, key, IteratorPrototype;
  // Fix native
  if ($anyNative) {
    IteratorPrototype = getPrototypeOf($anyNative.call(new Base()));
    if (IteratorPrototype !== Object.prototype && IteratorPrototype.next) {
      // Set @@toStringTag to native iterators
      setToStringTag(IteratorPrototype, TAG, true);
      // fix for some old engines
      if (!LIBRARY && typeof IteratorPrototype[ITERATOR] != 'function') hide(IteratorPrototype, ITERATOR, returnThis);
    }
  }
  // fix Array#{values, @@iterator}.name in V8 / FF
  if (DEF_VALUES && $native && $native.name !== VALUES) {
    VALUES_BUG = true;
    $default = function values() { return $native.call(this); };
  }
  // Define iterator
  if ((!LIBRARY || FORCED) && (BUGGY || VALUES_BUG || !proto[ITERATOR])) {
    hide(proto, ITERATOR, $default);
  }
  // Plug for library
  Iterators[NAME] = $default;
  Iterators[TAG] = returnThis;
  if (DEFAULT) {
    methods = {
      values: DEF_VALUES ? $default : getMethod(VALUES),
      keys: IS_SET ? $default : getMethod(KEYS),
      entries: $entries
    };
    if (FORCED) for (key in methods) {
      if (!(key in proto)) redefine(proto, key, methods[key]);
    } else $export($export.P + $export.F * (BUGGY || VALUES_BUG), NAME, methods);
  }
  return methods;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_iter-detect.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_iter-detect.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var ITERATOR = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('iterator');
var SAFE_CLOSING = false;

try {
  var riter = [7][ITERATOR]();
  riter['return'] = function () { SAFE_CLOSING = true; };
  // eslint-disable-next-line no-throw-literal
  Array.from(riter, function () { throw 2; });
} catch (e) { /* empty */ }

module.exports = function (exec, skipClosing) {
  if (!skipClosing && !SAFE_CLOSING) return false;
  var safe = false;
  try {
    var arr = [7];
    var iter = arr[ITERATOR]();
    iter.next = function () { return { done: safe = true }; };
    arr[ITERATOR] = function () { return iter; };
    exec(arr);
  } catch (e) { /* empty */ }
  return safe;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_iter-step.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_iter-step.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function (done, value) {
  return { value: value, done: !!done };
};


/***/ }),

/***/ "./node_modules/core-js/modules/_iterators.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_iterators.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = {};


/***/ }),

/***/ "./node_modules/core-js/modules/_library.js":
/*!**************************************************!*\
  !*** ./node_modules/core-js/modules/_library.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = false;


/***/ }),

/***/ "./node_modules/core-js/modules/_meta.js":
/*!***********************************************!*\
  !*** ./node_modules/core-js/modules/_meta.js ***!
  \***********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var META = __webpack_require__(/*! ./_uid */ "./node_modules/core-js/modules/_uid.js")('meta');
var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
var has = __webpack_require__(/*! ./_has */ "./node_modules/core-js/modules/_has.js");
var setDesc = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js").f;
var id = 0;
var isExtensible = Object.isExtensible || function () {
  return true;
};
var FREEZE = !__webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js")(function () {
  return isExtensible(Object.preventExtensions({}));
});
var setMeta = function (it) {
  setDesc(it, META, { value: {
    i: 'O' + ++id, // object ID
    w: {}          // weak collections IDs
  } });
};
var fastKey = function (it, create) {
  // return primitive with prefix
  if (!isObject(it)) return typeof it == 'symbol' ? it : (typeof it == 'string' ? 'S' : 'P') + it;
  if (!has(it, META)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return 'F';
    // not necessary to add metadata
    if (!create) return 'E';
    // add missing metadata
    setMeta(it);
  // return object ID
  } return it[META].i;
};
var getWeak = function (it, create) {
  if (!has(it, META)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return true;
    // not necessary to add metadata
    if (!create) return false;
    // add missing metadata
    setMeta(it);
  // return hash weak collections IDs
  } return it[META].w;
};
// add metadata on freeze-family methods calling
var onFreeze = function (it) {
  if (FREEZE && meta.NEED && isExtensible(it) && !has(it, META)) setMeta(it);
  return it;
};
var meta = module.exports = {
  KEY: META,
  NEED: false,
  fastKey: fastKey,
  getWeak: getWeak,
  onFreeze: onFreeze
};


/***/ }),

/***/ "./node_modules/core-js/modules/_microtask.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_microtask.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var macrotask = __webpack_require__(/*! ./_task */ "./node_modules/core-js/modules/_task.js").set;
var Observer = global.MutationObserver || global.WebKitMutationObserver;
var process = global.process;
var Promise = global.Promise;
var isNode = __webpack_require__(/*! ./_cof */ "./node_modules/core-js/modules/_cof.js")(process) == 'process';

module.exports = function () {
  var head, last, notify;

  var flush = function () {
    var parent, fn;
    if (isNode && (parent = process.domain)) parent.exit();
    while (head) {
      fn = head.fn;
      head = head.next;
      try {
        fn();
      } catch (e) {
        if (head) notify();
        else last = undefined;
        throw e;
      }
    } last = undefined;
    if (parent) parent.enter();
  };

  // Node.js
  if (isNode) {
    notify = function () {
      process.nextTick(flush);
    };
  // browsers with MutationObserver, except iOS Safari - https://github.com/zloirock/core-js/issues/339
  } else if (Observer && !(global.navigator && global.navigator.standalone)) {
    var toggle = true;
    var node = document.createTextNode('');
    new Observer(flush).observe(node, { characterData: true }); // eslint-disable-line no-new
    notify = function () {
      node.data = toggle = !toggle;
    };
  // environments with maybe non-completely correct, but existent Promise
  } else if (Promise && Promise.resolve) {
    // Promise.resolve without an argument throws an error in LG WebOS 2
    var promise = Promise.resolve(undefined);
    notify = function () {
      promise.then(flush);
    };
  // for other environments - macrotask based on:
  // - setImmediate
  // - MessageChannel
  // - window.postMessag
  // - onreadystatechange
  // - setTimeout
  } else {
    notify = function () {
      // strange IE + webpack dev server bug - use .call(global)
      macrotask.call(global, flush);
    };
  }

  return function (fn) {
    var task = { fn: fn, next: undefined };
    if (last) last.next = task;
    if (!head) {
      head = task;
      notify();
    } last = task;
  };
};


/***/ }),

/***/ "./node_modules/core-js/modules/_new-promise-capability.js":
/*!*****************************************************************!*\
  !*** ./node_modules/core-js/modules/_new-promise-capability.js ***!
  \*****************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

// 25.4.1.5 NewPromiseCapability(C)
var aFunction = __webpack_require__(/*! ./_a-function */ "./node_modules/core-js/modules/_a-function.js");

function PromiseCapability(C) {
  var resolve, reject;
  this.promise = new C(function ($$resolve, $$reject) {
    if (resolve !== undefined || reject !== undefined) throw TypeError('Bad Promise constructor');
    resolve = $$resolve;
    reject = $$reject;
  });
  this.resolve = aFunction(resolve);
  this.reject = aFunction(reject);
}

module.exports.f = function (C) {
  return new PromiseCapability(C);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-create.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/_object-create.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.2 / 15.2.3.5 Object.create(O [, Properties])
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var dPs = __webpack_require__(/*! ./_object-dps */ "./node_modules/core-js/modules/_object-dps.js");
var enumBugKeys = __webpack_require__(/*! ./_enum-bug-keys */ "./node_modules/core-js/modules/_enum-bug-keys.js");
var IE_PROTO = __webpack_require__(/*! ./_shared-key */ "./node_modules/core-js/modules/_shared-key.js")('IE_PROTO');
var Empty = function () { /* empty */ };
var PROTOTYPE = 'prototype';

// Create object with fake `null` prototype: use iframe Object with cleared prototype
var createDict = function () {
  // Thrash, waste and sodomy: IE GC bug
  var iframe = __webpack_require__(/*! ./_dom-create */ "./node_modules/core-js/modules/_dom-create.js")('iframe');
  var i = enumBugKeys.length;
  var lt = '<';
  var gt = '>';
  var iframeDocument;
  iframe.style.display = 'none';
  __webpack_require__(/*! ./_html */ "./node_modules/core-js/modules/_html.js").appendChild(iframe);
  iframe.src = 'javascript:'; // eslint-disable-line no-script-url
  // createDict = iframe.contentWindow.Object;
  // html.removeChild(iframe);
  iframeDocument = iframe.contentWindow.document;
  iframeDocument.open();
  iframeDocument.write(lt + 'script' + gt + 'document.F=Object' + lt + '/script' + gt);
  iframeDocument.close();
  createDict = iframeDocument.F;
  while (i--) delete createDict[PROTOTYPE][enumBugKeys[i]];
  return createDict();
};

module.exports = Object.create || function create(O, Properties) {
  var result;
  if (O !== null) {
    Empty[PROTOTYPE] = anObject(O);
    result = new Empty();
    Empty[PROTOTYPE] = null;
    // add "__proto__" for Object.getPrototypeOf polyfill
    result[IE_PROTO] = O;
  } else result = createDict();
  return Properties === undefined ? result : dPs(result, Properties);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-dp.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_object-dp.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var IE8_DOM_DEFINE = __webpack_require__(/*! ./_ie8-dom-define */ "./node_modules/core-js/modules/_ie8-dom-define.js");
var toPrimitive = __webpack_require__(/*! ./_to-primitive */ "./node_modules/core-js/modules/_to-primitive.js");
var dP = Object.defineProperty;

exports.f = __webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js") ? Object.defineProperty : function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPrimitive(P, true);
  anObject(Attributes);
  if (IE8_DOM_DEFINE) try {
    return dP(O, P, Attributes);
  } catch (e) { /* empty */ }
  if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported!');
  if ('value' in Attributes) O[P] = Attributes.value;
  return O;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-dps.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_object-dps.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var dP = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js");
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var getKeys = __webpack_require__(/*! ./_object-keys */ "./node_modules/core-js/modules/_object-keys.js");

module.exports = __webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js") ? Object.defineProperties : function defineProperties(O, Properties) {
  anObject(O);
  var keys = getKeys(Properties);
  var length = keys.length;
  var i = 0;
  var P;
  while (length > i) dP.f(O, P = keys[i++], Properties[P]);
  return O;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-gopd.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_object-gopd.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var pIE = __webpack_require__(/*! ./_object-pie */ "./node_modules/core-js/modules/_object-pie.js");
var createDesc = __webpack_require__(/*! ./_property-desc */ "./node_modules/core-js/modules/_property-desc.js");
var toIObject = __webpack_require__(/*! ./_to-iobject */ "./node_modules/core-js/modules/_to-iobject.js");
var toPrimitive = __webpack_require__(/*! ./_to-primitive */ "./node_modules/core-js/modules/_to-primitive.js");
var has = __webpack_require__(/*! ./_has */ "./node_modules/core-js/modules/_has.js");
var IE8_DOM_DEFINE = __webpack_require__(/*! ./_ie8-dom-define */ "./node_modules/core-js/modules/_ie8-dom-define.js");
var gOPD = Object.getOwnPropertyDescriptor;

exports.f = __webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js") ? gOPD : function getOwnPropertyDescriptor(O, P) {
  O = toIObject(O);
  P = toPrimitive(P, true);
  if (IE8_DOM_DEFINE) try {
    return gOPD(O, P);
  } catch (e) { /* empty */ }
  if (has(O, P)) return createDesc(!pIE.f.call(O, P), O[P]);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-gopn-ext.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/modules/_object-gopn-ext.js ***!
  \**********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
var toIObject = __webpack_require__(/*! ./_to-iobject */ "./node_modules/core-js/modules/_to-iobject.js");
var gOPN = __webpack_require__(/*! ./_object-gopn */ "./node_modules/core-js/modules/_object-gopn.js").f;
var toString = {}.toString;

var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames
  ? Object.getOwnPropertyNames(window) : [];

var getWindowNames = function (it) {
  try {
    return gOPN(it);
  } catch (e) {
    return windowNames.slice();
  }
};

module.exports.f = function getOwnPropertyNames(it) {
  return windowNames && toString.call(it) == '[object Window]' ? getWindowNames(it) : gOPN(toIObject(it));
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-gopn.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_object-gopn.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.7 / 15.2.3.4 Object.getOwnPropertyNames(O)
var $keys = __webpack_require__(/*! ./_object-keys-internal */ "./node_modules/core-js/modules/_object-keys-internal.js");
var hiddenKeys = __webpack_require__(/*! ./_enum-bug-keys */ "./node_modules/core-js/modules/_enum-bug-keys.js").concat('length', 'prototype');

exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
  return $keys(O, hiddenKeys);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-gops.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_object-gops.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

exports.f = Object.getOwnPropertySymbols;


/***/ }),

/***/ "./node_modules/core-js/modules/_object-gpo.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_object-gpo.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.9 / 15.2.3.2 Object.getPrototypeOf(O)
var has = __webpack_require__(/*! ./_has */ "./node_modules/core-js/modules/_has.js");
var toObject = __webpack_require__(/*! ./_to-object */ "./node_modules/core-js/modules/_to-object.js");
var IE_PROTO = __webpack_require__(/*! ./_shared-key */ "./node_modules/core-js/modules/_shared-key.js")('IE_PROTO');
var ObjectProto = Object.prototype;

module.exports = Object.getPrototypeOf || function (O) {
  O = toObject(O);
  if (has(O, IE_PROTO)) return O[IE_PROTO];
  if (typeof O.constructor == 'function' && O instanceof O.constructor) {
    return O.constructor.prototype;
  } return O instanceof Object ? ObjectProto : null;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-keys-internal.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/modules/_object-keys-internal.js ***!
  \***************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var has = __webpack_require__(/*! ./_has */ "./node_modules/core-js/modules/_has.js");
var toIObject = __webpack_require__(/*! ./_to-iobject */ "./node_modules/core-js/modules/_to-iobject.js");
var arrayIndexOf = __webpack_require__(/*! ./_array-includes */ "./node_modules/core-js/modules/_array-includes.js")(false);
var IE_PROTO = __webpack_require__(/*! ./_shared-key */ "./node_modules/core-js/modules/_shared-key.js")('IE_PROTO');

module.exports = function (object, names) {
  var O = toIObject(object);
  var i = 0;
  var result = [];
  var key;
  for (key in O) if (key != IE_PROTO) has(O, key) && result.push(key);
  // Don't enum bug & hidden keys
  while (names.length > i) if (has(O, key = names[i++])) {
    ~arrayIndexOf(result, key) || result.push(key);
  }
  return result;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-keys.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_object-keys.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.14 / 15.2.3.14 Object.keys(O)
var $keys = __webpack_require__(/*! ./_object-keys-internal */ "./node_modules/core-js/modules/_object-keys-internal.js");
var enumBugKeys = __webpack_require__(/*! ./_enum-bug-keys */ "./node_modules/core-js/modules/_enum-bug-keys.js");

module.exports = Object.keys || function keys(O) {
  return $keys(O, enumBugKeys);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_object-pie.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_object-pie.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

exports.f = {}.propertyIsEnumerable;


/***/ }),

/***/ "./node_modules/core-js/modules/_object-sap.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_object-sap.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// most Object methods by ES6 should accept primitives
var $export = __webpack_require__(/*! ./_export */ "./node_modules/core-js/modules/_export.js");
var core = __webpack_require__(/*! ./_core */ "./node_modules/core-js/modules/_core.js");
var fails = __webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js");
module.exports = function (KEY, exec) {
  var fn = (core.Object || {})[KEY] || Object[KEY];
  var exp = {};
  exp[KEY] = exec(fn);
  $export($export.S + $export.F * fails(function () { fn(1); }), 'Object', exp);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_own-keys.js":
/*!***************************************************!*\
  !*** ./node_modules/core-js/modules/_own-keys.js ***!
  \***************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// all object keys, includes non-enumerable and symbols
var gOPN = __webpack_require__(/*! ./_object-gopn */ "./node_modules/core-js/modules/_object-gopn.js");
var gOPS = __webpack_require__(/*! ./_object-gops */ "./node_modules/core-js/modules/_object-gops.js");
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var Reflect = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js").Reflect;
module.exports = Reflect && Reflect.ownKeys || function ownKeys(it) {
  var keys = gOPN.f(anObject(it));
  var getSymbols = gOPS.f;
  return getSymbols ? keys.concat(getSymbols(it)) : keys;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_perform.js":
/*!**************************************************!*\
  !*** ./node_modules/core-js/modules/_perform.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function (exec) {
  try {
    return { e: false, v: exec() };
  } catch (e) {
    return { e: true, v: e };
  }
};


/***/ }),

/***/ "./node_modules/core-js/modules/_promise-resolve.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/modules/_promise-resolve.js ***!
  \**********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
var newPromiseCapability = __webpack_require__(/*! ./_new-promise-capability */ "./node_modules/core-js/modules/_new-promise-capability.js");

module.exports = function (C, x) {
  anObject(C);
  if (isObject(x) && x.constructor === C) return x;
  var promiseCapability = newPromiseCapability.f(C);
  var resolve = promiseCapability.resolve;
  resolve(x);
  return promiseCapability.promise;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_property-desc.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/_property-desc.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function (bitmap, value) {
  return {
    enumerable: !(bitmap & 1),
    configurable: !(bitmap & 2),
    writable: !(bitmap & 4),
    value: value
  };
};


/***/ }),

/***/ "./node_modules/core-js/modules/_redefine-all.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/modules/_redefine-all.js ***!
  \*******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var redefine = __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js");
module.exports = function (target, src, safe) {
  for (var key in src) redefine(target, key, src[key], safe);
  return target;
};


/***/ }),

/***/ "./node_modules/core-js/modules/_redefine.js":
/*!***************************************************!*\
  !*** ./node_modules/core-js/modules/_redefine.js ***!
  \***************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var hide = __webpack_require__(/*! ./_hide */ "./node_modules/core-js/modules/_hide.js");
var has = __webpack_require__(/*! ./_has */ "./node_modules/core-js/modules/_has.js");
var SRC = __webpack_require__(/*! ./_uid */ "./node_modules/core-js/modules/_uid.js")('src');
var TO_STRING = 'toString';
var $toString = Function[TO_STRING];
var TPL = ('' + $toString).split(TO_STRING);

__webpack_require__(/*! ./_core */ "./node_modules/core-js/modules/_core.js").inspectSource = function (it) {
  return $toString.call(it);
};

(module.exports = function (O, key, val, safe) {
  var isFunction = typeof val == 'function';
  if (isFunction) has(val, 'name') || hide(val, 'name', key);
  if (O[key] === val) return;
  if (isFunction) has(val, SRC) || hide(val, SRC, O[key] ? '' + O[key] : TPL.join(String(key)));
  if (O === global) {
    O[key] = val;
  } else if (!safe) {
    delete O[key];
    hide(O, key, val);
  } else if (O[key]) {
    O[key] = val;
  } else {
    hide(O, key, val);
  }
// add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
})(Function.prototype, TO_STRING, function toString() {
  return typeof this == 'function' && this[SRC] || $toString.call(this);
});


/***/ }),

/***/ "./node_modules/core-js/modules/_set-proto.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_set-proto.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// Works with __proto__ only. Old v8 can't work with null proto objects.
/* eslint-disable no-proto */
var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var check = function (O, proto) {
  anObject(O);
  if (!isObject(proto) && proto !== null) throw TypeError(proto + ": can't set as prototype!");
};
module.exports = {
  set: Object.setPrototypeOf || ('__proto__' in {} ? // eslint-disable-line
    function (test, buggy, set) {
      try {
        set = __webpack_require__(/*! ./_ctx */ "./node_modules/core-js/modules/_ctx.js")(Function.call, __webpack_require__(/*! ./_object-gopd */ "./node_modules/core-js/modules/_object-gopd.js").f(Object.prototype, '__proto__').set, 2);
        set(test, []);
        buggy = !(test instanceof Array);
      } catch (e) { buggy = true; }
      return function setPrototypeOf(O, proto) {
        check(O, proto);
        if (buggy) O.__proto__ = proto;
        else set(O, proto);
        return O;
      };
    }({}, false) : undefined),
  check: check
};


/***/ }),

/***/ "./node_modules/core-js/modules/_set-species.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_set-species.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var dP = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js");
var DESCRIPTORS = __webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js");
var SPECIES = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('species');

module.exports = function (KEY) {
  var C = global[KEY];
  if (DESCRIPTORS && C && !C[SPECIES]) dP.f(C, SPECIES, {
    configurable: true,
    get: function () { return this; }
  });
};


/***/ }),

/***/ "./node_modules/core-js/modules/_set-to-string-tag.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/_set-to-string-tag.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var def = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js").f;
var has = __webpack_require__(/*! ./_has */ "./node_modules/core-js/modules/_has.js");
var TAG = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('toStringTag');

module.exports = function (it, tag, stat) {
  if (it && !has(it = stat ? it : it.prototype, TAG)) def(it, TAG, { configurable: true, value: tag });
};


/***/ }),

/***/ "./node_modules/core-js/modules/_shared-key.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_shared-key.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var shared = __webpack_require__(/*! ./_shared */ "./node_modules/core-js/modules/_shared.js")('keys');
var uid = __webpack_require__(/*! ./_uid */ "./node_modules/core-js/modules/_uid.js");
module.exports = function (key) {
  return shared[key] || (shared[key] = uid(key));
};


/***/ }),

/***/ "./node_modules/core-js/modules/_shared.js":
/*!*************************************************!*\
  !*** ./node_modules/core-js/modules/_shared.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var core = __webpack_require__(/*! ./_core */ "./node_modules/core-js/modules/_core.js");
var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var SHARED = '__core-js_shared__';
var store = global[SHARED] || (global[SHARED] = {});

(module.exports = function (key, value) {
  return store[key] || (store[key] = value !== undefined ? value : {});
})('versions', []).push({
  version: core.version,
  mode: __webpack_require__(/*! ./_library */ "./node_modules/core-js/modules/_library.js") ? 'pure' : 'global',
  copyright: '© 2018 Denis Pushkarev (zloirock.ru)'
});


/***/ }),

/***/ "./node_modules/core-js/modules/_species-constructor.js":
/*!**************************************************************!*\
  !*** ./node_modules/core-js/modules/_species-constructor.js ***!
  \**************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 7.3.20 SpeciesConstructor(O, defaultConstructor)
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var aFunction = __webpack_require__(/*! ./_a-function */ "./node_modules/core-js/modules/_a-function.js");
var SPECIES = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('species');
module.exports = function (O, D) {
  var C = anObject(O).constructor;
  var S;
  return C === undefined || (S = anObject(C)[SPECIES]) == undefined ? D : aFunction(S);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_string-trim.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/_string-trim.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var $export = __webpack_require__(/*! ./_export */ "./node_modules/core-js/modules/_export.js");
var defined = __webpack_require__(/*! ./_defined */ "./node_modules/core-js/modules/_defined.js");
var fails = __webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js");
var spaces = __webpack_require__(/*! ./_string-ws */ "./node_modules/core-js/modules/_string-ws.js");
var space = '[' + spaces + ']';
var non = '\u200b\u0085';
var ltrim = RegExp('^' + space + space + '*');
var rtrim = RegExp(space + space + '*$');

var exporter = function (KEY, exec, ALIAS) {
  var exp = {};
  var FORCE = fails(function () {
    return !!spaces[KEY]() || non[KEY]() != non;
  });
  var fn = exp[KEY] = FORCE ? exec(trim) : spaces[KEY];
  if (ALIAS) exp[ALIAS] = fn;
  $export($export.P + $export.F * FORCE, 'String', exp);
};

// 1 -> String#trimLeft
// 2 -> String#trimRight
// 3 -> String#trim
var trim = exporter.trim = function (string, TYPE) {
  string = String(defined(string));
  if (TYPE & 1) string = string.replace(ltrim, '');
  if (TYPE & 2) string = string.replace(rtrim, '');
  return string;
};

module.exports = exporter;


/***/ }),

/***/ "./node_modules/core-js/modules/_string-ws.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_string-ws.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = '\x09\x0A\x0B\x0C\x0D\x20\xA0\u1680\u180E\u2000\u2001\u2002\u2003' +
  '\u2004\u2005\u2006\u2007\u2008\u2009\u200A\u202F\u205F\u3000\u2028\u2029\uFEFF';


/***/ }),

/***/ "./node_modules/core-js/modules/_task.js":
/*!***********************************************!*\
  !*** ./node_modules/core-js/modules/_task.js ***!
  \***********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var ctx = __webpack_require__(/*! ./_ctx */ "./node_modules/core-js/modules/_ctx.js");
var invoke = __webpack_require__(/*! ./_invoke */ "./node_modules/core-js/modules/_invoke.js");
var html = __webpack_require__(/*! ./_html */ "./node_modules/core-js/modules/_html.js");
var cel = __webpack_require__(/*! ./_dom-create */ "./node_modules/core-js/modules/_dom-create.js");
var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var process = global.process;
var setTask = global.setImmediate;
var clearTask = global.clearImmediate;
var MessageChannel = global.MessageChannel;
var Dispatch = global.Dispatch;
var counter = 0;
var queue = {};
var ONREADYSTATECHANGE = 'onreadystatechange';
var defer, channel, port;
var run = function () {
  var id = +this;
  // eslint-disable-next-line no-prototype-builtins
  if (queue.hasOwnProperty(id)) {
    var fn = queue[id];
    delete queue[id];
    fn();
  }
};
var listener = function (event) {
  run.call(event.data);
};
// Node.js 0.9+ & IE10+ has setImmediate, otherwise:
if (!setTask || !clearTask) {
  setTask = function setImmediate(fn) {
    var args = [];
    var i = 1;
    while (arguments.length > i) args.push(arguments[i++]);
    queue[++counter] = function () {
      // eslint-disable-next-line no-new-func
      invoke(typeof fn == 'function' ? fn : Function(fn), args);
    };
    defer(counter);
    return counter;
  };
  clearTask = function clearImmediate(id) {
    delete queue[id];
  };
  // Node.js 0.8-
  if (__webpack_require__(/*! ./_cof */ "./node_modules/core-js/modules/_cof.js")(process) == 'process') {
    defer = function (id) {
      process.nextTick(ctx(run, id, 1));
    };
  // Sphere (JS game engine) Dispatch API
  } else if (Dispatch && Dispatch.now) {
    defer = function (id) {
      Dispatch.now(ctx(run, id, 1));
    };
  // Browsers with MessageChannel, includes WebWorkers
  } else if (MessageChannel) {
    channel = new MessageChannel();
    port = channel.port2;
    channel.port1.onmessage = listener;
    defer = ctx(port.postMessage, port, 1);
  // Browsers with postMessage, skip WebWorkers
  // IE8 has postMessage, but it's sync & typeof its postMessage is 'object'
  } else if (global.addEventListener && typeof postMessage == 'function' && !global.importScripts) {
    defer = function (id) {
      global.postMessage(id + '', '*');
    };
    global.addEventListener('message', listener, false);
  // IE8-
  } else if (ONREADYSTATECHANGE in cel('script')) {
    defer = function (id) {
      html.appendChild(cel('script'))[ONREADYSTATECHANGE] = function () {
        html.removeChild(this);
        run.call(id);
      };
    };
  // Rest old browsers
  } else {
    defer = function (id) {
      setTimeout(ctx(run, id, 1), 0);
    };
  }
}
module.exports = {
  set: setTask,
  clear: clearTask
};


/***/ }),

/***/ "./node_modules/core-js/modules/_to-absolute-index.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/_to-absolute-index.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var toInteger = __webpack_require__(/*! ./_to-integer */ "./node_modules/core-js/modules/_to-integer.js");
var max = Math.max;
var min = Math.min;
module.exports = function (index, length) {
  index = toInteger(index);
  return index < 0 ? max(index + length, 0) : min(index, length);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_to-integer.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_to-integer.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// 7.1.4 ToInteger
var ceil = Math.ceil;
var floor = Math.floor;
module.exports = function (it) {
  return isNaN(it = +it) ? 0 : (it > 0 ? floor : ceil)(it);
};


/***/ }),

/***/ "./node_modules/core-js/modules/_to-iobject.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_to-iobject.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// to indexed object, toObject with fallback for non-array-like ES3 strings
var IObject = __webpack_require__(/*! ./_iobject */ "./node_modules/core-js/modules/_iobject.js");
var defined = __webpack_require__(/*! ./_defined */ "./node_modules/core-js/modules/_defined.js");
module.exports = function (it) {
  return IObject(defined(it));
};


/***/ }),

/***/ "./node_modules/core-js/modules/_to-length.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_to-length.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 7.1.15 ToLength
var toInteger = __webpack_require__(/*! ./_to-integer */ "./node_modules/core-js/modules/_to-integer.js");
var min = Math.min;
module.exports = function (it) {
  return it > 0 ? min(toInteger(it), 0x1fffffffffffff) : 0; // pow(2, 53) - 1 == 9007199254740991
};


/***/ }),

/***/ "./node_modules/core-js/modules/_to-object.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/_to-object.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 7.1.13 ToObject(argument)
var defined = __webpack_require__(/*! ./_defined */ "./node_modules/core-js/modules/_defined.js");
module.exports = function (it) {
  return Object(defined(it));
};


/***/ }),

/***/ "./node_modules/core-js/modules/_to-primitive.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/modules/_to-primitive.js ***!
  \*******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 7.1.1 ToPrimitive(input [, PreferredType])
var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
// instead of the ES6 spec version, we didn't implement @@toPrimitive case
// and the second argument - flag - preferred type is a string
module.exports = function (it, S) {
  if (!isObject(it)) return it;
  var fn, val;
  if (S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
  if (typeof (fn = it.valueOf) == 'function' && !isObject(val = fn.call(it))) return val;
  if (!S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
  throw TypeError("Can't convert object to primitive value");
};


/***/ }),

/***/ "./node_modules/core-js/modules/_uid.js":
/*!**********************************************!*\
  !*** ./node_modules/core-js/modules/_uid.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

var id = 0;
var px = Math.random();
module.exports = function (key) {
  return 'Symbol('.concat(key === undefined ? '' : key, ')_', (++id + px).toString(36));
};


/***/ }),

/***/ "./node_modules/core-js/modules/_user-agent.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_user-agent.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var navigator = global.navigator;

module.exports = navigator && navigator.userAgent || '';


/***/ }),

/***/ "./node_modules/core-js/modules/_wks-define.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/_wks-define.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var core = __webpack_require__(/*! ./_core */ "./node_modules/core-js/modules/_core.js");
var LIBRARY = __webpack_require__(/*! ./_library */ "./node_modules/core-js/modules/_library.js");
var wksExt = __webpack_require__(/*! ./_wks-ext */ "./node_modules/core-js/modules/_wks-ext.js");
var defineProperty = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js").f;
module.exports = function (name) {
  var $Symbol = core.Symbol || (core.Symbol = LIBRARY ? {} : global.Symbol || {});
  if (name.charAt(0) != '_' && !(name in $Symbol)) defineProperty($Symbol, name, { value: wksExt.f(name) });
};


/***/ }),

/***/ "./node_modules/core-js/modules/_wks-ext.js":
/*!**************************************************!*\
  !*** ./node_modules/core-js/modules/_wks-ext.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

exports.f = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js");


/***/ }),

/***/ "./node_modules/core-js/modules/_wks.js":
/*!**********************************************!*\
  !*** ./node_modules/core-js/modules/_wks.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var store = __webpack_require__(/*! ./_shared */ "./node_modules/core-js/modules/_shared.js")('wks');
var uid = __webpack_require__(/*! ./_uid */ "./node_modules/core-js/modules/_uid.js");
var Symbol = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js").Symbol;
var USE_SYMBOL = typeof Symbol == 'function';

var $exports = module.exports = function (name) {
  return store[name] || (store[name] =
    USE_SYMBOL && Symbol[name] || (USE_SYMBOL ? Symbol : uid)('Symbol.' + name));
};

$exports.store = store;


/***/ }),

/***/ "./node_modules/core-js/modules/core.get-iterator-method.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/modules/core.get-iterator-method.js ***!
  \******************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var classof = __webpack_require__(/*! ./_classof */ "./node_modules/core-js/modules/_classof.js");
var ITERATOR = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('iterator');
var Iterators = __webpack_require__(/*! ./_iterators */ "./node_modules/core-js/modules/_iterators.js");
module.exports = __webpack_require__(/*! ./_core */ "./node_modules/core-js/modules/_core.js").getIteratorMethod = function (it) {
  if (it != undefined) return it[ITERATOR]
    || it['@@iterator']
    || Iterators[classof(it)];
};


/***/ }),

/***/ "./node_modules/core-js/modules/es6.array.iterator.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/es6.array.iterator.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var addToUnscopables = __webpack_require__(/*! ./_add-to-unscopables */ "./node_modules/core-js/modules/_add-to-unscopables.js");
var step = __webpack_require__(/*! ./_iter-step */ "./node_modules/core-js/modules/_iter-step.js");
var Iterators = __webpack_require__(/*! ./_iterators */ "./node_modules/core-js/modules/_iterators.js");
var toIObject = __webpack_require__(/*! ./_to-iobject */ "./node_modules/core-js/modules/_to-iobject.js");

// 22.1.3.4 Array.prototype.entries()
// 22.1.3.13 Array.prototype.keys()
// 22.1.3.29 Array.prototype.values()
// 22.1.3.30 Array.prototype[@@iterator]()
module.exports = __webpack_require__(/*! ./_iter-define */ "./node_modules/core-js/modules/_iter-define.js")(Array, 'Array', function (iterated, kind) {
  this._t = toIObject(iterated); // target
  this._i = 0;                   // next index
  this._k = kind;                // kind
// 22.1.5.2.1 %ArrayIteratorPrototype%.next()
}, function () {
  var O = this._t;
  var kind = this._k;
  var index = this._i++;
  if (!O || index >= O.length) {
    this._t = undefined;
    return step(1);
  }
  if (kind == 'keys') return step(0, index);
  if (kind == 'values') return step(0, O[index]);
  return step(0, [index, O[index]]);
}, 'values');

// argumentsList[@@iterator] is %ArrayProto_values% (9.4.4.6, 9.4.4.7)
Iterators.Arguments = Iterators.Array;

addToUnscopables('keys');
addToUnscopables('values');
addToUnscopables('entries');


/***/ }),

/***/ "./node_modules/core-js/modules/es6.number.constructor.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/modules/es6.number.constructor.js ***!
  \****************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var has = __webpack_require__(/*! ./_has */ "./node_modules/core-js/modules/_has.js");
var cof = __webpack_require__(/*! ./_cof */ "./node_modules/core-js/modules/_cof.js");
var inheritIfRequired = __webpack_require__(/*! ./_inherit-if-required */ "./node_modules/core-js/modules/_inherit-if-required.js");
var toPrimitive = __webpack_require__(/*! ./_to-primitive */ "./node_modules/core-js/modules/_to-primitive.js");
var fails = __webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js");
var gOPN = __webpack_require__(/*! ./_object-gopn */ "./node_modules/core-js/modules/_object-gopn.js").f;
var gOPD = __webpack_require__(/*! ./_object-gopd */ "./node_modules/core-js/modules/_object-gopd.js").f;
var dP = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js").f;
var $trim = __webpack_require__(/*! ./_string-trim */ "./node_modules/core-js/modules/_string-trim.js").trim;
var NUMBER = 'Number';
var $Number = global[NUMBER];
var Base = $Number;
var proto = $Number.prototype;
// Opera ~12 has broken Object#toString
var BROKEN_COF = cof(__webpack_require__(/*! ./_object-create */ "./node_modules/core-js/modules/_object-create.js")(proto)) == NUMBER;
var TRIM = 'trim' in String.prototype;

// 7.1.3 ToNumber(argument)
var toNumber = function (argument) {
  var it = toPrimitive(argument, false);
  if (typeof it == 'string' && it.length > 2) {
    it = TRIM ? it.trim() : $trim(it, 3);
    var first = it.charCodeAt(0);
    var third, radix, maxCode;
    if (first === 43 || first === 45) {
      third = it.charCodeAt(2);
      if (third === 88 || third === 120) return NaN; // Number('+0x1') should be NaN, old V8 fix
    } else if (first === 48) {
      switch (it.charCodeAt(1)) {
        case 66: case 98: radix = 2; maxCode = 49; break; // fast equal /^0b[01]+$/i
        case 79: case 111: radix = 8; maxCode = 55; break; // fast equal /^0o[0-7]+$/i
        default: return +it;
      }
      for (var digits = it.slice(2), i = 0, l = digits.length, code; i < l; i++) {
        code = digits.charCodeAt(i);
        // parseInt parses a string to a first unavailable symbol
        // but ToNumber should return NaN if a string contains unavailable symbols
        if (code < 48 || code > maxCode) return NaN;
      } return parseInt(digits, radix);
    }
  } return +it;
};

if (!$Number(' 0o1') || !$Number('0b1') || $Number('+0x1')) {
  $Number = function Number(value) {
    var it = arguments.length < 1 ? 0 : value;
    var that = this;
    return that instanceof $Number
      // check on 1..constructor(foo) case
      && (BROKEN_COF ? fails(function () { proto.valueOf.call(that); }) : cof(that) != NUMBER)
        ? inheritIfRequired(new Base(toNumber(it)), that, $Number) : toNumber(it);
  };
  for (var keys = __webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js") ? gOPN(Base) : (
    // ES3:
    'MAX_VALUE,MIN_VALUE,NaN,NEGATIVE_INFINITY,POSITIVE_INFINITY,' +
    // ES6 (in case, if modules with ES6 Number statics required before):
    'EPSILON,isFinite,isInteger,isNaN,isSafeInteger,MAX_SAFE_INTEGER,' +
    'MIN_SAFE_INTEGER,parseFloat,parseInt,isInteger'
  ).split(','), j = 0, key; keys.length > j; j++) {
    if (has(Base, key = keys[j]) && !has($Number, key)) {
      dP($Number, key, gOPD(Base, key));
    }
  }
  $Number.prototype = proto;
  proto.constructor = $Number;
  __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js")(global, NUMBER, $Number);
}


/***/ }),

/***/ "./node_modules/core-js/modules/es6.number.is-nan.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es6.number.is-nan.js ***!
  \***********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 20.1.2.4 Number.isNaN(number)
var $export = __webpack_require__(/*! ./_export */ "./node_modules/core-js/modules/_export.js");

$export($export.S, 'Number', {
  isNaN: function isNaN(number) {
    // eslint-disable-next-line no-self-compare
    return number != number;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es6.object.keys.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/modules/es6.object.keys.js ***!
  \*********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// 19.1.2.14 Object.keys(O)
var toObject = __webpack_require__(/*! ./_to-object */ "./node_modules/core-js/modules/_to-object.js");
var $keys = __webpack_require__(/*! ./_object-keys */ "./node_modules/core-js/modules/_object-keys.js");

__webpack_require__(/*! ./_object-sap */ "./node_modules/core-js/modules/_object-sap.js")('keys', function () {
  return function keys(it) {
    return $keys(toObject(it));
  };
});


/***/ }),

/***/ "./node_modules/core-js/modules/es6.object.to-string.js":
/*!**************************************************************!*\
  !*** ./node_modules/core-js/modules/es6.object.to-string.js ***!
  \**************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

// 19.1.3.6 Object.prototype.toString()
var classof = __webpack_require__(/*! ./_classof */ "./node_modules/core-js/modules/_classof.js");
var test = {};
test[__webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('toStringTag')] = 'z';
if (test + '' != '[object z]') {
  __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js")(Object.prototype, 'toString', function toString() {
    return '[object ' + classof(this) + ']';
  }, true);
}


/***/ }),

/***/ "./node_modules/core-js/modules/es6.promise.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/es6.promise.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var LIBRARY = __webpack_require__(/*! ./_library */ "./node_modules/core-js/modules/_library.js");
var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var ctx = __webpack_require__(/*! ./_ctx */ "./node_modules/core-js/modules/_ctx.js");
var classof = __webpack_require__(/*! ./_classof */ "./node_modules/core-js/modules/_classof.js");
var $export = __webpack_require__(/*! ./_export */ "./node_modules/core-js/modules/_export.js");
var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
var aFunction = __webpack_require__(/*! ./_a-function */ "./node_modules/core-js/modules/_a-function.js");
var anInstance = __webpack_require__(/*! ./_an-instance */ "./node_modules/core-js/modules/_an-instance.js");
var forOf = __webpack_require__(/*! ./_for-of */ "./node_modules/core-js/modules/_for-of.js");
var speciesConstructor = __webpack_require__(/*! ./_species-constructor */ "./node_modules/core-js/modules/_species-constructor.js");
var task = __webpack_require__(/*! ./_task */ "./node_modules/core-js/modules/_task.js").set;
var microtask = __webpack_require__(/*! ./_microtask */ "./node_modules/core-js/modules/_microtask.js")();
var newPromiseCapabilityModule = __webpack_require__(/*! ./_new-promise-capability */ "./node_modules/core-js/modules/_new-promise-capability.js");
var perform = __webpack_require__(/*! ./_perform */ "./node_modules/core-js/modules/_perform.js");
var userAgent = __webpack_require__(/*! ./_user-agent */ "./node_modules/core-js/modules/_user-agent.js");
var promiseResolve = __webpack_require__(/*! ./_promise-resolve */ "./node_modules/core-js/modules/_promise-resolve.js");
var PROMISE = 'Promise';
var TypeError = global.TypeError;
var process = global.process;
var versions = process && process.versions;
var v8 = versions && versions.v8 || '';
var $Promise = global[PROMISE];
var isNode = classof(process) == 'process';
var empty = function () { /* empty */ };
var Internal, newGenericPromiseCapability, OwnPromiseCapability, Wrapper;
var newPromiseCapability = newGenericPromiseCapability = newPromiseCapabilityModule.f;

var USE_NATIVE = !!function () {
  try {
    // correct subclassing with @@species support
    var promise = $Promise.resolve(1);
    var FakePromise = (promise.constructor = {})[__webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('species')] = function (exec) {
      exec(empty, empty);
    };
    // unhandled rejections tracking support, NodeJS Promise without it fails @@species test
    return (isNode || typeof PromiseRejectionEvent == 'function')
      && promise.then(empty) instanceof FakePromise
      // v8 6.6 (Node 10 and Chrome 66) have a bug with resolving custom thenables
      // https://bugs.chromium.org/p/chromium/issues/detail?id=830565
      // we can't detect it synchronously, so just check versions
      && v8.indexOf('6.6') !== 0
      && userAgent.indexOf('Chrome/66') === -1;
  } catch (e) { /* empty */ }
}();

// helpers
var isThenable = function (it) {
  var then;
  return isObject(it) && typeof (then = it.then) == 'function' ? then : false;
};
var notify = function (promise, isReject) {
  if (promise._n) return;
  promise._n = true;
  var chain = promise._c;
  microtask(function () {
    var value = promise._v;
    var ok = promise._s == 1;
    var i = 0;
    var run = function (reaction) {
      var handler = ok ? reaction.ok : reaction.fail;
      var resolve = reaction.resolve;
      var reject = reaction.reject;
      var domain = reaction.domain;
      var result, then, exited;
      try {
        if (handler) {
          if (!ok) {
            if (promise._h == 2) onHandleUnhandled(promise);
            promise._h = 1;
          }
          if (handler === true) result = value;
          else {
            if (domain) domain.enter();
            result = handler(value); // may throw
            if (domain) {
              domain.exit();
              exited = true;
            }
          }
          if (result === reaction.promise) {
            reject(TypeError('Promise-chain cycle'));
          } else if (then = isThenable(result)) {
            then.call(result, resolve, reject);
          } else resolve(result);
        } else reject(value);
      } catch (e) {
        if (domain && !exited) domain.exit();
        reject(e);
      }
    };
    while (chain.length > i) run(chain[i++]); // variable length - can't use forEach
    promise._c = [];
    promise._n = false;
    if (isReject && !promise._h) onUnhandled(promise);
  });
};
var onUnhandled = function (promise) {
  task.call(global, function () {
    var value = promise._v;
    var unhandled = isUnhandled(promise);
    var result, handler, console;
    if (unhandled) {
      result = perform(function () {
        if (isNode) {
          process.emit('unhandledRejection', value, promise);
        } else if (handler = global.onunhandledrejection) {
          handler({ promise: promise, reason: value });
        } else if ((console = global.console) && console.error) {
          console.error('Unhandled promise rejection', value);
        }
      });
      // Browsers should not trigger `rejectionHandled` event if it was handled here, NodeJS - should
      promise._h = isNode || isUnhandled(promise) ? 2 : 1;
    } promise._a = undefined;
    if (unhandled && result.e) throw result.v;
  });
};
var isUnhandled = function (promise) {
  return promise._h !== 1 && (promise._a || promise._c).length === 0;
};
var onHandleUnhandled = function (promise) {
  task.call(global, function () {
    var handler;
    if (isNode) {
      process.emit('rejectionHandled', promise);
    } else if (handler = global.onrejectionhandled) {
      handler({ promise: promise, reason: promise._v });
    }
  });
};
var $reject = function (value) {
  var promise = this;
  if (promise._d) return;
  promise._d = true;
  promise = promise._w || promise; // unwrap
  promise._v = value;
  promise._s = 2;
  if (!promise._a) promise._a = promise._c.slice();
  notify(promise, true);
};
var $resolve = function (value) {
  var promise = this;
  var then;
  if (promise._d) return;
  promise._d = true;
  promise = promise._w || promise; // unwrap
  try {
    if (promise === value) throw TypeError("Promise can't be resolved itself");
    if (then = isThenable(value)) {
      microtask(function () {
        var wrapper = { _w: promise, _d: false }; // wrap
        try {
          then.call(value, ctx($resolve, wrapper, 1), ctx($reject, wrapper, 1));
        } catch (e) {
          $reject.call(wrapper, e);
        }
      });
    } else {
      promise._v = value;
      promise._s = 1;
      notify(promise, false);
    }
  } catch (e) {
    $reject.call({ _w: promise, _d: false }, e); // wrap
  }
};

// constructor polyfill
if (!USE_NATIVE) {
  // 25.4.3.1 Promise(executor)
  $Promise = function Promise(executor) {
    anInstance(this, $Promise, PROMISE, '_h');
    aFunction(executor);
    Internal.call(this);
    try {
      executor(ctx($resolve, this, 1), ctx($reject, this, 1));
    } catch (err) {
      $reject.call(this, err);
    }
  };
  // eslint-disable-next-line no-unused-vars
  Internal = function Promise(executor) {
    this._c = [];             // <- awaiting reactions
    this._a = undefined;      // <- checked in isUnhandled reactions
    this._s = 0;              // <- state
    this._d = false;          // <- done
    this._v = undefined;      // <- value
    this._h = 0;              // <- rejection state, 0 - default, 1 - handled, 2 - unhandled
    this._n = false;          // <- notify
  };
  Internal.prototype = __webpack_require__(/*! ./_redefine-all */ "./node_modules/core-js/modules/_redefine-all.js")($Promise.prototype, {
    // 25.4.5.3 Promise.prototype.then(onFulfilled, onRejected)
    then: function then(onFulfilled, onRejected) {
      var reaction = newPromiseCapability(speciesConstructor(this, $Promise));
      reaction.ok = typeof onFulfilled == 'function' ? onFulfilled : true;
      reaction.fail = typeof onRejected == 'function' && onRejected;
      reaction.domain = isNode ? process.domain : undefined;
      this._c.push(reaction);
      if (this._a) this._a.push(reaction);
      if (this._s) notify(this, false);
      return reaction.promise;
    },
    // 25.4.5.1 Promise.prototype.catch(onRejected)
    'catch': function (onRejected) {
      return this.then(undefined, onRejected);
    }
  });
  OwnPromiseCapability = function () {
    var promise = new Internal();
    this.promise = promise;
    this.resolve = ctx($resolve, promise, 1);
    this.reject = ctx($reject, promise, 1);
  };
  newPromiseCapabilityModule.f = newPromiseCapability = function (C) {
    return C === $Promise || C === Wrapper
      ? new OwnPromiseCapability(C)
      : newGenericPromiseCapability(C);
  };
}

$export($export.G + $export.W + $export.F * !USE_NATIVE, { Promise: $Promise });
__webpack_require__(/*! ./_set-to-string-tag */ "./node_modules/core-js/modules/_set-to-string-tag.js")($Promise, PROMISE);
__webpack_require__(/*! ./_set-species */ "./node_modules/core-js/modules/_set-species.js")(PROMISE);
Wrapper = __webpack_require__(/*! ./_core */ "./node_modules/core-js/modules/_core.js")[PROMISE];

// statics
$export($export.S + $export.F * !USE_NATIVE, PROMISE, {
  // 25.4.4.5 Promise.reject(r)
  reject: function reject(r) {
    var capability = newPromiseCapability(this);
    var $$reject = capability.reject;
    $$reject(r);
    return capability.promise;
  }
});
$export($export.S + $export.F * (LIBRARY || !USE_NATIVE), PROMISE, {
  // 25.4.4.6 Promise.resolve(x)
  resolve: function resolve(x) {
    return promiseResolve(LIBRARY && this === Wrapper ? $Promise : this, x);
  }
});
$export($export.S + $export.F * !(USE_NATIVE && __webpack_require__(/*! ./_iter-detect */ "./node_modules/core-js/modules/_iter-detect.js")(function (iter) {
  $Promise.all(iter)['catch'](empty);
})), PROMISE, {
  // 25.4.4.1 Promise.all(iterable)
  all: function all(iterable) {
    var C = this;
    var capability = newPromiseCapability(C);
    var resolve = capability.resolve;
    var reject = capability.reject;
    var result = perform(function () {
      var values = [];
      var index = 0;
      var remaining = 1;
      forOf(iterable, false, function (promise) {
        var $index = index++;
        var alreadyCalled = false;
        values.push(undefined);
        remaining++;
        C.resolve(promise).then(function (value) {
          if (alreadyCalled) return;
          alreadyCalled = true;
          values[$index] = value;
          --remaining || resolve(values);
        }, reject);
      });
      --remaining || resolve(values);
    });
    if (result.e) reject(result.v);
    return capability.promise;
  },
  // 25.4.4.4 Promise.race(iterable)
  race: function race(iterable) {
    var C = this;
    var capability = newPromiseCapability(C);
    var reject = capability.reject;
    var result = perform(function () {
      forOf(iterable, false, function (promise) {
        C.resolve(promise).then(capability.resolve, reject);
      });
    });
    if (result.e) reject(result.v);
    return capability.promise;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es6.regexp.constructor.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/modules/es6.regexp.constructor.js ***!
  \****************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var inheritIfRequired = __webpack_require__(/*! ./_inherit-if-required */ "./node_modules/core-js/modules/_inherit-if-required.js");
var dP = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js").f;
var gOPN = __webpack_require__(/*! ./_object-gopn */ "./node_modules/core-js/modules/_object-gopn.js").f;
var isRegExp = __webpack_require__(/*! ./_is-regexp */ "./node_modules/core-js/modules/_is-regexp.js");
var $flags = __webpack_require__(/*! ./_flags */ "./node_modules/core-js/modules/_flags.js");
var $RegExp = global.RegExp;
var Base = $RegExp;
var proto = $RegExp.prototype;
var re1 = /a/g;
var re2 = /a/g;
// "new" creates a new object, old webkit buggy here
var CORRECT_NEW = new $RegExp(re1) !== re1;

if (__webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js") && (!CORRECT_NEW || __webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js")(function () {
  re2[__webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js")('match')] = false;
  // RegExp constructor can alter flags and IsRegExp works correct with @@match
  return $RegExp(re1) != re1 || $RegExp(re2) == re2 || $RegExp(re1, 'i') != '/a/i';
}))) {
  $RegExp = function RegExp(p, f) {
    var tiRE = this instanceof $RegExp;
    var piRE = isRegExp(p);
    var fiU = f === undefined;
    return !tiRE && piRE && p.constructor === $RegExp && fiU ? p
      : inheritIfRequired(CORRECT_NEW
        ? new Base(piRE && !fiU ? p.source : p, f)
        : Base((piRE = p instanceof $RegExp) ? p.source : p, piRE && fiU ? $flags.call(p) : f)
      , tiRE ? this : proto, $RegExp);
  };
  var proxy = function (key) {
    key in $RegExp || dP($RegExp, key, {
      configurable: true,
      get: function () { return Base[key]; },
      set: function (it) { Base[key] = it; }
    });
  };
  for (var keys = gOPN(Base), i = 0; keys.length > i;) proxy(keys[i++]);
  proto.constructor = $RegExp;
  $RegExp.prototype = proto;
  __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js")(global, 'RegExp', $RegExp);
}

__webpack_require__(/*! ./_set-species */ "./node_modules/core-js/modules/_set-species.js")('RegExp');


/***/ }),

/***/ "./node_modules/core-js/modules/es6.regexp.replace.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/es6.regexp.replace.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// @@replace logic
__webpack_require__(/*! ./_fix-re-wks */ "./node_modules/core-js/modules/_fix-re-wks.js")('replace', 2, function (defined, REPLACE, $replace) {
  // 21.1.3.14 String.prototype.replace(searchValue, replaceValue)
  return [function replace(searchValue, replaceValue) {
    'use strict';
    var O = defined(this);
    var fn = searchValue == undefined ? undefined : searchValue[REPLACE];
    return fn !== undefined
      ? fn.call(searchValue, O, replaceValue)
      : $replace.call(String(O), searchValue, replaceValue);
  }, $replace];
});


/***/ }),

/***/ "./node_modules/core-js/modules/es6.regexp.search.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es6.regexp.search.js ***!
  \***********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// @@search logic
__webpack_require__(/*! ./_fix-re-wks */ "./node_modules/core-js/modules/_fix-re-wks.js")('search', 1, function (defined, SEARCH, $search) {
  // 21.1.3.15 String.prototype.search(regexp)
  return [function search(regexp) {
    'use strict';
    var O = defined(this);
    var fn = regexp == undefined ? undefined : regexp[SEARCH];
    return fn !== undefined ? fn.call(regexp, O) : new RegExp(regexp)[SEARCH](String(O));
  }, $search];
});


/***/ }),

/***/ "./node_modules/core-js/modules/es6.symbol.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/es6.symbol.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

// ECMAScript 6 symbols shim
var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var has = __webpack_require__(/*! ./_has */ "./node_modules/core-js/modules/_has.js");
var DESCRIPTORS = __webpack_require__(/*! ./_descriptors */ "./node_modules/core-js/modules/_descriptors.js");
var $export = __webpack_require__(/*! ./_export */ "./node_modules/core-js/modules/_export.js");
var redefine = __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js");
var META = __webpack_require__(/*! ./_meta */ "./node_modules/core-js/modules/_meta.js").KEY;
var $fails = __webpack_require__(/*! ./_fails */ "./node_modules/core-js/modules/_fails.js");
var shared = __webpack_require__(/*! ./_shared */ "./node_modules/core-js/modules/_shared.js");
var setToStringTag = __webpack_require__(/*! ./_set-to-string-tag */ "./node_modules/core-js/modules/_set-to-string-tag.js");
var uid = __webpack_require__(/*! ./_uid */ "./node_modules/core-js/modules/_uid.js");
var wks = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js");
var wksExt = __webpack_require__(/*! ./_wks-ext */ "./node_modules/core-js/modules/_wks-ext.js");
var wksDefine = __webpack_require__(/*! ./_wks-define */ "./node_modules/core-js/modules/_wks-define.js");
var enumKeys = __webpack_require__(/*! ./_enum-keys */ "./node_modules/core-js/modules/_enum-keys.js");
var isArray = __webpack_require__(/*! ./_is-array */ "./node_modules/core-js/modules/_is-array.js");
var anObject = __webpack_require__(/*! ./_an-object */ "./node_modules/core-js/modules/_an-object.js");
var isObject = __webpack_require__(/*! ./_is-object */ "./node_modules/core-js/modules/_is-object.js");
var toIObject = __webpack_require__(/*! ./_to-iobject */ "./node_modules/core-js/modules/_to-iobject.js");
var toPrimitive = __webpack_require__(/*! ./_to-primitive */ "./node_modules/core-js/modules/_to-primitive.js");
var createDesc = __webpack_require__(/*! ./_property-desc */ "./node_modules/core-js/modules/_property-desc.js");
var _create = __webpack_require__(/*! ./_object-create */ "./node_modules/core-js/modules/_object-create.js");
var gOPNExt = __webpack_require__(/*! ./_object-gopn-ext */ "./node_modules/core-js/modules/_object-gopn-ext.js");
var $GOPD = __webpack_require__(/*! ./_object-gopd */ "./node_modules/core-js/modules/_object-gopd.js");
var $DP = __webpack_require__(/*! ./_object-dp */ "./node_modules/core-js/modules/_object-dp.js");
var $keys = __webpack_require__(/*! ./_object-keys */ "./node_modules/core-js/modules/_object-keys.js");
var gOPD = $GOPD.f;
var dP = $DP.f;
var gOPN = gOPNExt.f;
var $Symbol = global.Symbol;
var $JSON = global.JSON;
var _stringify = $JSON && $JSON.stringify;
var PROTOTYPE = 'prototype';
var HIDDEN = wks('_hidden');
var TO_PRIMITIVE = wks('toPrimitive');
var isEnum = {}.propertyIsEnumerable;
var SymbolRegistry = shared('symbol-registry');
var AllSymbols = shared('symbols');
var OPSymbols = shared('op-symbols');
var ObjectProto = Object[PROTOTYPE];
var USE_NATIVE = typeof $Symbol == 'function';
var QObject = global.QObject;
// Don't use setters in Qt Script, https://github.com/zloirock/core-js/issues/173
var setter = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild;

// fallback for old Android, https://code.google.com/p/v8/issues/detail?id=687
var setSymbolDesc = DESCRIPTORS && $fails(function () {
  return _create(dP({}, 'a', {
    get: function () { return dP(this, 'a', { value: 7 }).a; }
  })).a != 7;
}) ? function (it, key, D) {
  var protoDesc = gOPD(ObjectProto, key);
  if (protoDesc) delete ObjectProto[key];
  dP(it, key, D);
  if (protoDesc && it !== ObjectProto) dP(ObjectProto, key, protoDesc);
} : dP;

var wrap = function (tag) {
  var sym = AllSymbols[tag] = _create($Symbol[PROTOTYPE]);
  sym._k = tag;
  return sym;
};

var isSymbol = USE_NATIVE && typeof $Symbol.iterator == 'symbol' ? function (it) {
  return typeof it == 'symbol';
} : function (it) {
  return it instanceof $Symbol;
};

var $defineProperty = function defineProperty(it, key, D) {
  if (it === ObjectProto) $defineProperty(OPSymbols, key, D);
  anObject(it);
  key = toPrimitive(key, true);
  anObject(D);
  if (has(AllSymbols, key)) {
    if (!D.enumerable) {
      if (!has(it, HIDDEN)) dP(it, HIDDEN, createDesc(1, {}));
      it[HIDDEN][key] = true;
    } else {
      if (has(it, HIDDEN) && it[HIDDEN][key]) it[HIDDEN][key] = false;
      D = _create(D, { enumerable: createDesc(0, false) });
    } return setSymbolDesc(it, key, D);
  } return dP(it, key, D);
};
var $defineProperties = function defineProperties(it, P) {
  anObject(it);
  var keys = enumKeys(P = toIObject(P));
  var i = 0;
  var l = keys.length;
  var key;
  while (l > i) $defineProperty(it, key = keys[i++], P[key]);
  return it;
};
var $create = function create(it, P) {
  return P === undefined ? _create(it) : $defineProperties(_create(it), P);
};
var $propertyIsEnumerable = function propertyIsEnumerable(key) {
  var E = isEnum.call(this, key = toPrimitive(key, true));
  if (this === ObjectProto && has(AllSymbols, key) && !has(OPSymbols, key)) return false;
  return E || !has(this, key) || !has(AllSymbols, key) || has(this, HIDDEN) && this[HIDDEN][key] ? E : true;
};
var $getOwnPropertyDescriptor = function getOwnPropertyDescriptor(it, key) {
  it = toIObject(it);
  key = toPrimitive(key, true);
  if (it === ObjectProto && has(AllSymbols, key) && !has(OPSymbols, key)) return;
  var D = gOPD(it, key);
  if (D && has(AllSymbols, key) && !(has(it, HIDDEN) && it[HIDDEN][key])) D.enumerable = true;
  return D;
};
var $getOwnPropertyNames = function getOwnPropertyNames(it) {
  var names = gOPN(toIObject(it));
  var result = [];
  var i = 0;
  var key;
  while (names.length > i) {
    if (!has(AllSymbols, key = names[i++]) && key != HIDDEN && key != META) result.push(key);
  } return result;
};
var $getOwnPropertySymbols = function getOwnPropertySymbols(it) {
  var IS_OP = it === ObjectProto;
  var names = gOPN(IS_OP ? OPSymbols : toIObject(it));
  var result = [];
  var i = 0;
  var key;
  while (names.length > i) {
    if (has(AllSymbols, key = names[i++]) && (IS_OP ? has(ObjectProto, key) : true)) result.push(AllSymbols[key]);
  } return result;
};

// 19.4.1.1 Symbol([description])
if (!USE_NATIVE) {
  $Symbol = function Symbol() {
    if (this instanceof $Symbol) throw TypeError('Symbol is not a constructor!');
    var tag = uid(arguments.length > 0 ? arguments[0] : undefined);
    var $set = function (value) {
      if (this === ObjectProto) $set.call(OPSymbols, value);
      if (has(this, HIDDEN) && has(this[HIDDEN], tag)) this[HIDDEN][tag] = false;
      setSymbolDesc(this, tag, createDesc(1, value));
    };
    if (DESCRIPTORS && setter) setSymbolDesc(ObjectProto, tag, { configurable: true, set: $set });
    return wrap(tag);
  };
  redefine($Symbol[PROTOTYPE], 'toString', function toString() {
    return this._k;
  });

  $GOPD.f = $getOwnPropertyDescriptor;
  $DP.f = $defineProperty;
  __webpack_require__(/*! ./_object-gopn */ "./node_modules/core-js/modules/_object-gopn.js").f = gOPNExt.f = $getOwnPropertyNames;
  __webpack_require__(/*! ./_object-pie */ "./node_modules/core-js/modules/_object-pie.js").f = $propertyIsEnumerable;
  __webpack_require__(/*! ./_object-gops */ "./node_modules/core-js/modules/_object-gops.js").f = $getOwnPropertySymbols;

  if (DESCRIPTORS && !__webpack_require__(/*! ./_library */ "./node_modules/core-js/modules/_library.js")) {
    redefine(ObjectProto, 'propertyIsEnumerable', $propertyIsEnumerable, true);
  }

  wksExt.f = function (name) {
    return wrap(wks(name));
  };
}

$export($export.G + $export.W + $export.F * !USE_NATIVE, { Symbol: $Symbol });

for (var es6Symbols = (
  // 19.4.2.2, 19.4.2.3, 19.4.2.4, 19.4.2.6, 19.4.2.8, 19.4.2.9, 19.4.2.10, 19.4.2.11, 19.4.2.12, 19.4.2.13, 19.4.2.14
  'hasInstance,isConcatSpreadable,iterator,match,replace,search,species,split,toPrimitive,toStringTag,unscopables'
).split(','), j = 0; es6Symbols.length > j;)wks(es6Symbols[j++]);

for (var wellKnownSymbols = $keys(wks.store), k = 0; wellKnownSymbols.length > k;) wksDefine(wellKnownSymbols[k++]);

$export($export.S + $export.F * !USE_NATIVE, 'Symbol', {
  // 19.4.2.1 Symbol.for(key)
  'for': function (key) {
    return has(SymbolRegistry, key += '')
      ? SymbolRegistry[key]
      : SymbolRegistry[key] = $Symbol(key);
  },
  // 19.4.2.5 Symbol.keyFor(sym)
  keyFor: function keyFor(sym) {
    if (!isSymbol(sym)) throw TypeError(sym + ' is not a symbol!');
    for (var key in SymbolRegistry) if (SymbolRegistry[key] === sym) return key;
  },
  useSetter: function () { setter = true; },
  useSimple: function () { setter = false; }
});

$export($export.S + $export.F * !USE_NATIVE, 'Object', {
  // 19.1.2.2 Object.create(O [, Properties])
  create: $create,
  // 19.1.2.4 Object.defineProperty(O, P, Attributes)
  defineProperty: $defineProperty,
  // 19.1.2.3 Object.defineProperties(O, Properties)
  defineProperties: $defineProperties,
  // 19.1.2.6 Object.getOwnPropertyDescriptor(O, P)
  getOwnPropertyDescriptor: $getOwnPropertyDescriptor,
  // 19.1.2.7 Object.getOwnPropertyNames(O)
  getOwnPropertyNames: $getOwnPropertyNames,
  // 19.1.2.8 Object.getOwnPropertySymbols(O)
  getOwnPropertySymbols: $getOwnPropertySymbols
});

// 24.3.2 JSON.stringify(value [, replacer [, space]])
$JSON && $export($export.S + $export.F * (!USE_NATIVE || $fails(function () {
  var S = $Symbol();
  // MS Edge converts symbol values to JSON as {}
  // WebKit converts symbol values to JSON as null
  // V8 throws on boxed symbols
  return _stringify([S]) != '[null]' || _stringify({ a: S }) != '{}' || _stringify(Object(S)) != '{}';
})), 'JSON', {
  stringify: function stringify(it) {
    var args = [it];
    var i = 1;
    var replacer, $replacer;
    while (arguments.length > i) args.push(arguments[i++]);
    $replacer = replacer = args[1];
    if (!isObject(replacer) && it === undefined || isSymbol(it)) return; // IE8 returns string on undefined
    if (!isArray(replacer)) replacer = function (key, value) {
      if (typeof $replacer == 'function') value = $replacer.call(this, key, value);
      if (!isSymbol(value)) return value;
    };
    args[1] = replacer;
    return _stringify.apply($JSON, args);
  }
});

// 19.4.3.4 Symbol.prototype[@@toPrimitive](hint)
$Symbol[PROTOTYPE][TO_PRIMITIVE] || __webpack_require__(/*! ./_hide */ "./node_modules/core-js/modules/_hide.js")($Symbol[PROTOTYPE], TO_PRIMITIVE, $Symbol[PROTOTYPE].valueOf);
// 19.4.3.5 Symbol.prototype[@@toStringTag]
setToStringTag($Symbol, 'Symbol');
// 20.2.1.9 Math[@@toStringTag]
setToStringTag(Math, 'Math', true);
// 24.3.3 JSON[@@toStringTag]
setToStringTag(global.JSON, 'JSON', true);


/***/ }),

/***/ "./node_modules/core-js/modules/es7.array.includes.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/es7.array.includes.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

// https://github.com/tc39/Array.prototype.includes
var $export = __webpack_require__(/*! ./_export */ "./node_modules/core-js/modules/_export.js");
var $includes = __webpack_require__(/*! ./_array-includes */ "./node_modules/core-js/modules/_array-includes.js")(true);

$export($export.P, 'Array', {
  includes: function includes(el /* , fromIndex = 0 */) {
    return $includes(this, el, arguments.length > 1 ? arguments[1] : undefined);
  }
});

__webpack_require__(/*! ./_add-to-unscopables */ "./node_modules/core-js/modules/_add-to-unscopables.js")('includes');


/***/ }),

/***/ "./node_modules/core-js/modules/es7.object.get-own-property-descriptors.js":
/*!*********************************************************************************!*\
  !*** ./node_modules/core-js/modules/es7.object.get-own-property-descriptors.js ***!
  \*********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// https://github.com/tc39/proposal-object-getownpropertydescriptors
var $export = __webpack_require__(/*! ./_export */ "./node_modules/core-js/modules/_export.js");
var ownKeys = __webpack_require__(/*! ./_own-keys */ "./node_modules/core-js/modules/_own-keys.js");
var toIObject = __webpack_require__(/*! ./_to-iobject */ "./node_modules/core-js/modules/_to-iobject.js");
var gOPD = __webpack_require__(/*! ./_object-gopd */ "./node_modules/core-js/modules/_object-gopd.js");
var createProperty = __webpack_require__(/*! ./_create-property */ "./node_modules/core-js/modules/_create-property.js");

$export($export.S, 'Object', {
  getOwnPropertyDescriptors: function getOwnPropertyDescriptors(object) {
    var O = toIObject(object);
    var getDesc = gOPD.f;
    var keys = ownKeys(O);
    var result = {};
    var i = 0;
    var key, desc;
    while (keys.length > i) {
      desc = getDesc(O, key = keys[i++]);
      if (desc !== undefined) createProperty(result, key, desc);
    }
    return result;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es7.symbol.async-iterator.js":
/*!*******************************************************************!*\
  !*** ./node_modules/core-js/modules/es7.symbol.async-iterator.js ***!
  \*******************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(/*! ./_wks-define */ "./node_modules/core-js/modules/_wks-define.js")('asyncIterator');


/***/ }),

/***/ "./node_modules/core-js/modules/web.dom.iterable.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/modules/web.dom.iterable.js ***!
  \**********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var $iterators = __webpack_require__(/*! ./es6.array.iterator */ "./node_modules/core-js/modules/es6.array.iterator.js");
var getKeys = __webpack_require__(/*! ./_object-keys */ "./node_modules/core-js/modules/_object-keys.js");
var redefine = __webpack_require__(/*! ./_redefine */ "./node_modules/core-js/modules/_redefine.js");
var global = __webpack_require__(/*! ./_global */ "./node_modules/core-js/modules/_global.js");
var hide = __webpack_require__(/*! ./_hide */ "./node_modules/core-js/modules/_hide.js");
var Iterators = __webpack_require__(/*! ./_iterators */ "./node_modules/core-js/modules/_iterators.js");
var wks = __webpack_require__(/*! ./_wks */ "./node_modules/core-js/modules/_wks.js");
var ITERATOR = wks('iterator');
var TO_STRING_TAG = wks('toStringTag');
var ArrayValues = Iterators.Array;

var DOMIterables = {
  CSSRuleList: true, // TODO: Not spec compliant, should be false.
  CSSStyleDeclaration: false,
  CSSValueList: false,
  ClientRectList: false,
  DOMRectList: false,
  DOMStringList: false,
  DOMTokenList: true,
  DataTransferItemList: false,
  FileList: false,
  HTMLAllCollection: false,
  HTMLCollection: false,
  HTMLFormElement: false,
  HTMLSelectElement: false,
  MediaList: true, // TODO: Not spec compliant, should be false.
  MimeTypeArray: false,
  NamedNodeMap: false,
  NodeList: true,
  PaintRequestList: false,
  Plugin: false,
  PluginArray: false,
  SVGLengthList: false,
  SVGNumberList: false,
  SVGPathSegList: false,
  SVGPointList: false,
  SVGStringList: false,
  SVGTransformList: false,
  SourceBufferList: false,
  StyleSheetList: true, // TODO: Not spec compliant, should be false.
  TextTrackCueList: false,
  TextTrackList: false,
  TouchList: false
};

for (var collections = getKeys(DOMIterables), i = 0; i < collections.length; i++) {
  var NAME = collections[i];
  var explicit = DOMIterables[NAME];
  var Collection = global[NAME];
  var proto = Collection && Collection.prototype;
  var key;
  if (proto) {
    if (!proto[ITERATOR]) hide(proto, ITERATOR, ArrayValues);
    if (!proto[TO_STRING_TAG]) hide(proto, TO_STRING_TAG, NAME);
    Iterators[NAME] = ArrayValues;
    if (explicit) for (key in $iterators) if (!proto[key]) redefine(proto, key, $iterators[key], true);
  }
}


/***/ }),

/***/ "./node_modules/create-emotion/dist/create-emotion.browser.esm.js":
/*!************************************************************************!*\
  !*** ./node_modules/create-emotion/dist/create-emotion.browser.esm.js ***!
  \************************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _emotion_cache__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @emotion/cache */ "./node_modules/@emotion/cache/dist/cache.browser.esm.js");
/* harmony import */ var _emotion_serialize__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @emotion/serialize */ "./node_modules/@emotion/serialize/dist/serialize.browser.esm.js");
/* harmony import */ var _emotion_utils__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @emotion/utils */ "./node_modules/@emotion/utils/dist/utils.browser.esm.js");




function insertWithoutScoping(cache, serialized) {
  if (cache.inserted[serialized.name] === undefined) {
    return cache.insert('', serialized, cache.sheet, true);
  }
}

function merge(registered, css, className) {
  var registeredStyles = [];
  var rawClassName = Object(_emotion_utils__WEBPACK_IMPORTED_MODULE_2__["getRegisteredStyles"])(registered, registeredStyles, className);

  if (registeredStyles.length < 2) {
    return className;
  }

  return rawClassName + css(registeredStyles);
}

var createEmotion = function createEmotion(options) {
  var cache = Object(_emotion_cache__WEBPACK_IMPORTED_MODULE_0__["default"])(options); // $FlowFixMe

  cache.sheet.speedy = function (value) {
    if ( true && this.ctr !== 0) {
      throw new Error('speedy must be changed before any rules are inserted');
    }

    this.isSpeedy = value;
  };

  cache.compat = true;

  var css = function css() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    var serialized = Object(_emotion_serialize__WEBPACK_IMPORTED_MODULE_1__["serializeStyles"])(args, cache.registered, undefined);
    Object(_emotion_utils__WEBPACK_IMPORTED_MODULE_2__["insertStyles"])(cache, serialized, false);
    return cache.key + "-" + serialized.name;
  };

  var keyframes = function keyframes() {
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }

    var serialized = Object(_emotion_serialize__WEBPACK_IMPORTED_MODULE_1__["serializeStyles"])(args, cache.registered);
    var animation = "animation-" + serialized.name;
    insertWithoutScoping(cache, {
      name: serialized.name,
      styles: "@keyframes " + animation + "{" + serialized.styles + "}"
    });
    return animation;
  };

  var injectGlobal = function injectGlobal() {
    for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
      args[_key3] = arguments[_key3];
    }

    var serialized = Object(_emotion_serialize__WEBPACK_IMPORTED_MODULE_1__["serializeStyles"])(args, cache.registered);
    insertWithoutScoping(cache, serialized);
  };

  var cx = function cx() {
    for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
      args[_key4] = arguments[_key4];
    }

    return merge(cache.registered, css, classnames(args));
  };

  return {
    css: css,
    cx: cx,
    injectGlobal: injectGlobal,
    keyframes: keyframes,
    hydrate: function hydrate(ids) {
      ids.forEach(function (key) {
        cache.inserted[key] = true;
      });
    },
    flush: function flush() {
      cache.registered = {};
      cache.inserted = {};
      cache.sheet.flush();
    },
    // $FlowFixMe
    sheet: cache.sheet,
    cache: cache,
    getRegisteredStyles: _emotion_utils__WEBPACK_IMPORTED_MODULE_2__["getRegisteredStyles"].bind(null, cache.registered),
    merge: merge.bind(null, cache.registered, css)
  };
};

var classnames = function classnames(args) {
  var cls = '';

  for (var i = 0; i < args.length; i++) {
    var arg = args[i];
    if (arg == null) continue;
    var toAdd = void 0;

    switch (typeof arg) {
      case 'boolean':
        break;

      case 'object':
        {
          if (Array.isArray(arg)) {
            toAdd = classnames(arg);
          } else {
            toAdd = '';

            for (var k in arg) {
              if (arg[k] && k) {
                toAdd && (toAdd += ' ');
                toAdd += k;
              }
            }
          }

          break;
        }

      default:
        {
          toAdd = arg;
        }
    }

    if (toAdd) {
      cls && (cls += ' ');
      cls += toAdd;
    }
  }

  return cls;
};

/* harmony default export */ __webpack_exports__["default"] = (createEmotion);


/***/ }),

/***/ "./node_modules/create-react-context/lib/implementation.js":
/*!*****************************************************************!*\
  !*** ./node_modules/create-react-context/lib/implementation.js ***!
  \*****************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _react = __webpack_require__(/*! react */ "./node_modules/react/index.js");

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(/*! prop-types */ "./node_modules/prop-types/index.js");

var _propTypes2 = _interopRequireDefault(_propTypes);

var _gud = __webpack_require__(/*! gud */ "./node_modules/gud/index.js");

var _gud2 = _interopRequireDefault(_gud);

var _warning = __webpack_require__(/*! fbjs/lib/warning */ "./node_modules/fbjs/lib/warning.js");

var _warning2 = _interopRequireDefault(_warning);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var MAX_SIGNED_31_BIT_INT = 1073741823;

// Inlined Object.is polyfill.
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
function objectIs(x, y) {
  if (x === y) {
    return x !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}

function createEventEmitter(value) {
  var handlers = [];
  return {
    on: function on(handler) {
      handlers.push(handler);
    },
    off: function off(handler) {
      handlers = handlers.filter(function (h) {
        return h !== handler;
      });
    },
    get: function get() {
      return value;
    },
    set: function set(newValue, changedBits) {
      value = newValue;
      handlers.forEach(function (handler) {
        return handler(value, changedBits);
      });
    }
  };
}

function onlyChild(children) {
  return Array.isArray(children) ? children[0] : children;
}

function createReactContext(defaultValue, calculateChangedBits) {
  var _Provider$childContex, _Consumer$contextType;

  var contextProp = '__create-react-context-' + (0, _gud2.default)() + '__';

  var Provider = function (_Component) {
    _inherits(Provider, _Component);

    function Provider() {
      var _temp, _this, _ret;

      _classCallCheck(this, Provider);

      for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }

      return _ret = (_temp = (_this = _possibleConstructorReturn(this, _Component.call.apply(_Component, [this].concat(args))), _this), _this.emitter = createEventEmitter(_this.props.value), _temp), _possibleConstructorReturn(_this, _ret);
    }

    Provider.prototype.getChildContext = function getChildContext() {
      var _ref;

      return _ref = {}, _ref[contextProp] = this.emitter, _ref;
    };

    Provider.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
      if (this.props.value !== nextProps.value) {
        var oldValue = this.props.value;
        var newValue = nextProps.value;
        var changedBits = void 0;

        if (objectIs(oldValue, newValue)) {
          changedBits = 0; // No change
        } else {
          changedBits = typeof calculateChangedBits === 'function' ? calculateChangedBits(oldValue, newValue) : MAX_SIGNED_31_BIT_INT;
          if (true) {
            (0, _warning2.default)((changedBits & MAX_SIGNED_31_BIT_INT) === changedBits, 'calculateChangedBits: Expected the return value to be a ' + '31-bit integer. Instead received: %s', changedBits);
          }

          changedBits |= 0;

          if (changedBits !== 0) {
            this.emitter.set(nextProps.value, changedBits);
          }
        }
      }
    };

    Provider.prototype.render = function render() {
      return this.props.children;
    };

    return Provider;
  }(_react.Component);

  Provider.childContextTypes = (_Provider$childContex = {}, _Provider$childContex[contextProp] = _propTypes2.default.object.isRequired, _Provider$childContex);

  var Consumer = function (_Component2) {
    _inherits(Consumer, _Component2);

    function Consumer() {
      var _temp2, _this2, _ret2;

      _classCallCheck(this, Consumer);

      for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }

      return _ret2 = (_temp2 = (_this2 = _possibleConstructorReturn(this, _Component2.call.apply(_Component2, [this].concat(args))), _this2), _this2.state = {
        value: _this2.getValue()
      }, _this2.onUpdate = function (newValue, changedBits) {
        var observedBits = _this2.observedBits | 0;
        if ((observedBits & changedBits) !== 0) {
          _this2.setState({ value: _this2.getValue() });
        }
      }, _temp2), _possibleConstructorReturn(_this2, _ret2);
    }

    Consumer.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
      var observedBits = nextProps.observedBits;

      this.observedBits = observedBits === undefined || observedBits === null ? MAX_SIGNED_31_BIT_INT // Subscribe to all changes by default
      : observedBits;
    };

    Consumer.prototype.componentDidMount = function componentDidMount() {
      if (this.context[contextProp]) {
        this.context[contextProp].on(this.onUpdate);
      }
      var observedBits = this.props.observedBits;

      this.observedBits = observedBits === undefined || observedBits === null ? MAX_SIGNED_31_BIT_INT // Subscribe to all changes by default
      : observedBits;
    };

    Consumer.prototype.componentWillUnmount = function componentWillUnmount() {
      if (this.context[contextProp]) {
        this.context[contextProp].off(this.onUpdate);
      }
    };

    Consumer.prototype.getValue = function getValue() {
      if (this.context[contextProp]) {
        return this.context[contextProp].get();
      } else {
        return defaultValue;
      }
    };

    Consumer.prototype.render = function render() {
      return onlyChild(this.props.children)(this.state.value);
    };

    return Consumer;
  }(_react.Component);

  Consumer.contextTypes = (_Consumer$contextType = {}, _Consumer$contextType[contextProp] = _propTypes2.default.object, _Consumer$contextType);


  return {
    Provider: Provider,
    Consumer: Consumer
  };
}

exports.default = createReactContext;
module.exports = exports['default'];

/***/ }),

/***/ "./node_modules/create-react-context/lib/index.js":
/*!********************************************************!*\
  !*** ./node_modules/create-react-context/lib/index.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _react = __webpack_require__(/*! react */ "./node_modules/react/index.js");

var _react2 = _interopRequireDefault(_react);

var _implementation = __webpack_require__(/*! ./implementation */ "./node_modules/create-react-context/lib/implementation.js");

var _implementation2 = _interopRequireDefault(_implementation);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = _react2.default.createContext || _implementation2.default;
module.exports = exports['default'];

/***/ }),

/***/ "./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./public/src/components/guideDialog.scss":
/*!*****************************************************************************************************************************!*\
  !*** ./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./public/src/components/guideDialog.scss ***!
  \*****************************************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(/*! ../../../node_modules/css-loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js");
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.i, "ul.ul-guide-info {\n  list-style-type: none;\n  margin: 1.5rem auto;\n  min-width: 30rem;\n  padding-inline-start: 2rem; }\n\nli.li-img-style {\n  padding: 0 1rem;\n  overflow: auto; }\n\nli.li-img-style h3 {\n  margin-bottom: 0px; }\n\nli.li-img-style img {\n  float: left;\n  margin: 0 1.5rem 0 0; }\n\nli.li-img-style p {\n  margin-top: 0px; }\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ "./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./public/src/components/syncsettingDialog.scss":
/*!***********************************************************************************************************************************!*\
  !*** ./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./public/src/components/syncsettingDialog.scss ***!
  \***********************************************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(/*! ../../../node_modules/css-loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js");
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.i, ".tooltip {\n  position: absolute;\n  background-color: #F1F2F7;\n  border: 1px solid #818181;\n  padding: 6px 12px;\n  font-family: inherit;\n  font-size: 1em;\n  max-width: 350px;\n  color: #000;\n  z-index: 99; }\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ "./node_modules/css-loader/dist/runtime/api.js":
/*!*****************************************************!*\
  !*** ./node_modules/css-loader/dist/runtime/api.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
// css base code, injected by the css-loader
// eslint-disable-next-line func-names
module.exports = function (useSourceMap) {
  var list = []; // return the list of modules as css string

  list.toString = function toString() {
    return this.map(function (item) {
      var content = cssWithMappingToString(item, useSourceMap);

      if (item[2]) {
        return "@media ".concat(item[2], " {").concat(content, "}");
      }

      return content;
    }).join('');
  }; // import a list of modules into the list
  // eslint-disable-next-line func-names


  list.i = function (modules, mediaQuery, dedupe) {
    if (typeof modules === 'string') {
      // eslint-disable-next-line no-param-reassign
      modules = [[null, modules, '']];
    }

    var alreadyImportedModules = {};

    if (dedupe) {
      for (var i = 0; i < this.length; i++) {
        // eslint-disable-next-line prefer-destructuring
        var id = this[i][0];

        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }

    for (var _i = 0; _i < modules.length; _i++) {
      var item = [].concat(modules[_i]);

      if (dedupe && alreadyImportedModules[item[0]]) {
        // eslint-disable-next-line no-continue
        continue;
      }

      if (mediaQuery) {
        if (!item[2]) {
          item[2] = mediaQuery;
        } else {
          item[2] = "".concat(mediaQuery, " and ").concat(item[2]);
        }
      }

      list.push(item);
    }
  };

  return list;
};

function cssWithMappingToString(item, useSourceMap) {
  var content = item[1] || ''; // eslint-disable-next-line prefer-destructuring

  var cssMapping = item[3];

  if (!cssMapping) {
    return content;
  }

  if (useSourceMap && typeof btoa === 'function') {
    var sourceMapping = toComment(cssMapping);
    var sourceURLs = cssMapping.sources.map(function (source) {
      return "/*# sourceURL=".concat(cssMapping.sourceRoot || '').concat(source, " */");
    });
    return [content].concat(sourceURLs).concat([sourceMapping]).join('\n');
  }

  return [content].join('\n');
} // Adapted from convert-source-map (MIT)


function toComment(sourceMap) {
  // eslint-disable-next-line no-undef
  var base64 = btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap))));
  var data = "sourceMappingURL=data:application/json;charset=utf-8;base64,".concat(base64);
  return "/*# ".concat(data, " */");
}

/***/ }),

/***/ "./node_modules/emotion/dist/emotion.esm.js":
/*!**************************************************!*\
  !*** ./node_modules/emotion/dist/emotion.esm.js ***!
  \**************************************************/
/*! exports provided: cache, css, cx, flush, getRegisteredStyles, hydrate, injectGlobal, keyframes, merge, sheet */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "cache", function() { return cache; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "css", function() { return css; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "cx", function() { return cx; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "flush", function() { return flush; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getRegisteredStyles", function() { return getRegisteredStyles; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "hydrate", function() { return hydrate; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "injectGlobal", function() { return injectGlobal; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "keyframes", function() { return keyframes; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "merge", function() { return merge; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "sheet", function() { return sheet; });
/* harmony import */ var create_emotion__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! create-emotion */ "./node_modules/create-emotion/dist/create-emotion.browser.esm.js");


var _createEmotion = Object(create_emotion__WEBPACK_IMPORTED_MODULE_0__["default"])(),
    flush = _createEmotion.flush,
    hydrate = _createEmotion.hydrate,
    cx = _createEmotion.cx,
    merge = _createEmotion.merge,
    getRegisteredStyles = _createEmotion.getRegisteredStyles,
    injectGlobal = _createEmotion.injectGlobal,
    keyframes = _createEmotion.keyframes,
    css = _createEmotion.css,
    sheet = _createEmotion.sheet,
    cache = _createEmotion.cache;




/***/ }),

/***/ "./node_modules/fbjs/lib/emptyFunction.js":
/*!************************************************!*\
  !*** ./node_modules/fbjs/lib/emptyFunction.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 */

function makeEmptyFunction(arg) {
  return function () {
    return arg;
  };
}

/**
 * This function accepts and discards inputs; it has no side effects. This is
 * primarily useful idiomatically for overridable function endpoints which
 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
 */
var emptyFunction = function emptyFunction() {};

emptyFunction.thatReturns = makeEmptyFunction;
emptyFunction.thatReturnsFalse = makeEmptyFunction(false);
emptyFunction.thatReturnsTrue = makeEmptyFunction(true);
emptyFunction.thatReturnsNull = makeEmptyFunction(null);
emptyFunction.thatReturnsThis = function () {
  return this;
};
emptyFunction.thatReturnsArgument = function (arg) {
  return arg;
};

module.exports = emptyFunction;

/***/ }),

/***/ "./node_modules/fbjs/lib/warning.js":
/*!******************************************!*\
  !*** ./node_modules/fbjs/lib/warning.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var emptyFunction = __webpack_require__(/*! ./emptyFunction */ "./node_modules/fbjs/lib/emptyFunction.js");

/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = emptyFunction;

if (true) {
  var printWarning = function printWarning(format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  warning = function warning(condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }

    if (format.indexOf('Failed Composite propType: ') === 0) {
      return; // Ignore CompositeComponent proptype check.
    }

    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

module.exports = warning;

/***/ }),

/***/ "./node_modules/gud/index.js":
/*!***********************************!*\
  !*** ./node_modules/gud/index.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global) {// @flow


var key = '__global_unique_id__';

module.exports = function() {
  return global[key] = (global[key] || 0) + 1;
};

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "./node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js":
/*!**********************************************************************************!*\
  !*** ./node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js ***!
  \**********************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var reactIs = __webpack_require__(/*! react-is */ "./node_modules/react-is/index.js");

/**
 * Copyright 2015, Yahoo! Inc.
 * Copyrights licensed under the New BSD License. See the accompanying LICENSE file for terms.
 */
var REACT_STATICS = {
  childContextTypes: true,
  contextType: true,
  contextTypes: true,
  defaultProps: true,
  displayName: true,
  getDefaultProps: true,
  getDerivedStateFromError: true,
  getDerivedStateFromProps: true,
  mixins: true,
  propTypes: true,
  type: true
};
var KNOWN_STATICS = {
  name: true,
  length: true,
  prototype: true,
  caller: true,
  callee: true,
  arguments: true,
  arity: true
};
var FORWARD_REF_STATICS = {
  '$$typeof': true,
  render: true,
  defaultProps: true,
  displayName: true,
  propTypes: true
};
var MEMO_STATICS = {
  '$$typeof': true,
  compare: true,
  defaultProps: true,
  displayName: true,
  propTypes: true,
  type: true
};
var TYPE_STATICS = {};
TYPE_STATICS[reactIs.ForwardRef] = FORWARD_REF_STATICS;
TYPE_STATICS[reactIs.Memo] = MEMO_STATICS;

function getStatics(component) {
  // React v16.11 and below
  if (reactIs.isMemo(component)) {
    return MEMO_STATICS;
  } // React v16.12 and above


  return TYPE_STATICS[component['$$typeof']] || REACT_STATICS;
}

var defineProperty = Object.defineProperty;
var getOwnPropertyNames = Object.getOwnPropertyNames;
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var getPrototypeOf = Object.getPrototypeOf;
var objectPrototype = Object.prototype;
function hoistNonReactStatics(targetComponent, sourceComponent, blacklist) {
  if (typeof sourceComponent !== 'string') {
    // don't hoist over string (html) components
    if (objectPrototype) {
      var inheritedComponent = getPrototypeOf(sourceComponent);

      if (inheritedComponent && inheritedComponent !== objectPrototype) {
        hoistNonReactStatics(targetComponent, inheritedComponent, blacklist);
      }
    }

    var keys = getOwnPropertyNames(sourceComponent);

    if (getOwnPropertySymbols) {
      keys = keys.concat(getOwnPropertySymbols(sourceComponent));
    }

    var targetStatics = getStatics(targetComponent);
    var sourceStatics = getStatics(sourceComponent);

    for (var i = 0; i < keys.length; ++i) {
      var key = keys[i];

      if (!KNOWN_STATICS[key] && !(blacklist && blacklist[key]) && !(sourceStatics && sourceStatics[key]) && !(targetStatics && targetStatics[key])) {
        var descriptor = getOwnPropertyDescriptor(sourceComponent, key);

        try {
          // Avoid failures from read-only properties
          defineProperty(targetComponent, key, descriptor);
        } catch (e) {}
      }
    }
  }

  return targetComponent;
}

module.exports = hoistNonReactStatics;


/***/ }),

/***/ "./node_modules/lodash.memoize/index.js":
/*!**********************************************!*\
  !*** ./node_modules/lodash.memoize/index.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {/**
 * lodash (Custom Build) <https://lodash.com/>
 * Build: `lodash modularize exports="npm" -o ./`
 * Copyright jQuery Foundation and other contributors <https://jquery.org/>
 * Released under MIT license <https://lodash.com/license>
 * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
 * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 */

/** Used as the `TypeError` message for "Functions" methods. */
var FUNC_ERROR_TEXT = 'Expected a function';

/** Used to stand-in for `undefined` hash values. */
var HASH_UNDEFINED = '__lodash_hash_undefined__';

/** `Object#toString` result references. */
var funcTag = '[object Function]',
    genTag = '[object GeneratorFunction]';

/**
 * Used to match `RegExp`
 * [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).
 */
var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;

/** Used to detect host constructors (Safari). */
var reIsHostCtor = /^\[object .+?Constructor\]$/;

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

/**
 * Gets the value at `key` of `object`.
 *
 * @private
 * @param {Object} [object] The object to query.
 * @param {string} key The key of the property to get.
 * @returns {*} Returns the property value.
 */
function getValue(object, key) {
  return object == null ? undefined : object[key];
}

/**
 * Checks if `value` is a host object in IE < 9.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a host object, else `false`.
 */
function isHostObject(value) {
  // Many host objects are `Object` objects that can coerce to strings
  // despite having improperly defined `toString` methods.
  var result = false;
  if (value != null && typeof value.toString != 'function') {
    try {
      result = !!(value + '');
    } catch (e) {}
  }
  return result;
}

/** Used for built-in method references. */
var arrayProto = Array.prototype,
    funcProto = Function.prototype,
    objectProto = Object.prototype;

/** Used to detect overreaching core-js shims. */
var coreJsData = root['__core-js_shared__'];

/** Used to detect methods masquerading as native. */
var maskSrcKey = (function() {
  var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || '');
  return uid ? ('Symbol(src)_1.' + uid) : '';
}());

/** Used to resolve the decompiled source of functions. */
var funcToString = funcProto.toString;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var objectToString = objectProto.toString;

/** Used to detect if a method is native. */
var reIsNative = RegExp('^' +
  funcToString.call(hasOwnProperty).replace(reRegExpChar, '\\$&')
  .replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, '$1.*?') + '$'
);

/** Built-in value references. */
var splice = arrayProto.splice;

/* Built-in method references that are verified to be native. */
var Map = getNative(root, 'Map'),
    nativeCreate = getNative(Object, 'create');

/**
 * Creates a hash object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Hash(entries) {
  var index = -1,
      length = entries ? entries.length : 0;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the hash.
 *
 * @private
 * @name clear
 * @memberOf Hash
 */
function hashClear() {
  this.__data__ = nativeCreate ? nativeCreate(null) : {};
}

/**
 * Removes `key` and its value from the hash.
 *
 * @private
 * @name delete
 * @memberOf Hash
 * @param {Object} hash The hash to modify.
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function hashDelete(key) {
  return this.has(key) && delete this.__data__[key];
}

/**
 * Gets the hash value for `key`.
 *
 * @private
 * @name get
 * @memberOf Hash
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function hashGet(key) {
  var data = this.__data__;
  if (nativeCreate) {
    var result = data[key];
    return result === HASH_UNDEFINED ? undefined : result;
  }
  return hasOwnProperty.call(data, key) ? data[key] : undefined;
}

/**
 * Checks if a hash value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Hash
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function hashHas(key) {
  var data = this.__data__;
  return nativeCreate ? data[key] !== undefined : hasOwnProperty.call(data, key);
}

/**
 * Sets the hash `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Hash
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the hash instance.
 */
function hashSet(key, value) {
  var data = this.__data__;
  data[key] = (nativeCreate && value === undefined) ? HASH_UNDEFINED : value;
  return this;
}

// Add methods to `Hash`.
Hash.prototype.clear = hashClear;
Hash.prototype['delete'] = hashDelete;
Hash.prototype.get = hashGet;
Hash.prototype.has = hashHas;
Hash.prototype.set = hashSet;

/**
 * Creates an list cache object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function ListCache(entries) {
  var index = -1,
      length = entries ? entries.length : 0;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the list cache.
 *
 * @private
 * @name clear
 * @memberOf ListCache
 */
function listCacheClear() {
  this.__data__ = [];
}

/**
 * Removes `key` and its value from the list cache.
 *
 * @private
 * @name delete
 * @memberOf ListCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function listCacheDelete(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    return false;
  }
  var lastIndex = data.length - 1;
  if (index == lastIndex) {
    data.pop();
  } else {
    splice.call(data, index, 1);
  }
  return true;
}

/**
 * Gets the list cache value for `key`.
 *
 * @private
 * @name get
 * @memberOf ListCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function listCacheGet(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  return index < 0 ? undefined : data[index][1];
}

/**
 * Checks if a list cache value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf ListCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function listCacheHas(key) {
  return assocIndexOf(this.__data__, key) > -1;
}

/**
 * Sets the list cache `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf ListCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the list cache instance.
 */
function listCacheSet(key, value) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    data.push([key, value]);
  } else {
    data[index][1] = value;
  }
  return this;
}

// Add methods to `ListCache`.
ListCache.prototype.clear = listCacheClear;
ListCache.prototype['delete'] = listCacheDelete;
ListCache.prototype.get = listCacheGet;
ListCache.prototype.has = listCacheHas;
ListCache.prototype.set = listCacheSet;

/**
 * Creates a map cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function MapCache(entries) {
  var index = -1,
      length = entries ? entries.length : 0;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the map.
 *
 * @private
 * @name clear
 * @memberOf MapCache
 */
function mapCacheClear() {
  this.__data__ = {
    'hash': new Hash,
    'map': new (Map || ListCache),
    'string': new Hash
  };
}

/**
 * Removes `key` and its value from the map.
 *
 * @private
 * @name delete
 * @memberOf MapCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function mapCacheDelete(key) {
  return getMapData(this, key)['delete'](key);
}

/**
 * Gets the map value for `key`.
 *
 * @private
 * @name get
 * @memberOf MapCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function mapCacheGet(key) {
  return getMapData(this, key).get(key);
}

/**
 * Checks if a map value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf MapCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function mapCacheHas(key) {
  return getMapData(this, key).has(key);
}

/**
 * Sets the map `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf MapCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the map cache instance.
 */
function mapCacheSet(key, value) {
  getMapData(this, key).set(key, value);
  return this;
}

// Add methods to `MapCache`.
MapCache.prototype.clear = mapCacheClear;
MapCache.prototype['delete'] = mapCacheDelete;
MapCache.prototype.get = mapCacheGet;
MapCache.prototype.has = mapCacheHas;
MapCache.prototype.set = mapCacheSet;

/**
 * Gets the index at which the `key` is found in `array` of key-value pairs.
 *
 * @private
 * @param {Array} array The array to inspect.
 * @param {*} key The key to search for.
 * @returns {number} Returns the index of the matched value, else `-1`.
 */
function assocIndexOf(array, key) {
  var length = array.length;
  while (length--) {
    if (eq(array[length][0], key)) {
      return length;
    }
  }
  return -1;
}

/**
 * The base implementation of `_.isNative` without bad shim checks.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a native function,
 *  else `false`.
 */
function baseIsNative(value) {
  if (!isObject(value) || isMasked(value)) {
    return false;
  }
  var pattern = (isFunction(value) || isHostObject(value)) ? reIsNative : reIsHostCtor;
  return pattern.test(toSource(value));
}

/**
 * Gets the data for `map`.
 *
 * @private
 * @param {Object} map The map to query.
 * @param {string} key The reference key.
 * @returns {*} Returns the map data.
 */
function getMapData(map, key) {
  var data = map.__data__;
  return isKeyable(key)
    ? data[typeof key == 'string' ? 'string' : 'hash']
    : data.map;
}

/**
 * Gets the native function at `key` of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {string} key The key of the method to get.
 * @returns {*} Returns the function if it's native, else `undefined`.
 */
function getNative(object, key) {
  var value = getValue(object, key);
  return baseIsNative(value) ? value : undefined;
}

/**
 * Checks if `value` is suitable for use as unique object key.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is suitable, else `false`.
 */
function isKeyable(value) {
  var type = typeof value;
  return (type == 'string' || type == 'number' || type == 'symbol' || type == 'boolean')
    ? (value !== '__proto__')
    : (value === null);
}

/**
 * Checks if `func` has its source masked.
 *
 * @private
 * @param {Function} func The function to check.
 * @returns {boolean} Returns `true` if `func` is masked, else `false`.
 */
function isMasked(func) {
  return !!maskSrcKey && (maskSrcKey in func);
}

/**
 * Converts `func` to its source code.
 *
 * @private
 * @param {Function} func The function to process.
 * @returns {string} Returns the source code.
 */
function toSource(func) {
  if (func != null) {
    try {
      return funcToString.call(func);
    } catch (e) {}
    try {
      return (func + '');
    } catch (e) {}
  }
  return '';
}

/**
 * Creates a function that memoizes the result of `func`. If `resolver` is
 * provided, it determines the cache key for storing the result based on the
 * arguments provided to the memoized function. By default, the first argument
 * provided to the memoized function is used as the map cache key. The `func`
 * is invoked with the `this` binding of the memoized function.
 *
 * **Note:** The cache is exposed as the `cache` property on the memoized
 * function. Its creation may be customized by replacing the `_.memoize.Cache`
 * constructor with one whose instances implement the
 * [`Map`](http://ecma-international.org/ecma-262/7.0/#sec-properties-of-the-map-prototype-object)
 * method interface of `delete`, `get`, `has`, and `set`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to have its output memoized.
 * @param {Function} [resolver] The function to resolve the cache key.
 * @returns {Function} Returns the new memoized function.
 * @example
 *
 * var object = { 'a': 1, 'b': 2 };
 * var other = { 'c': 3, 'd': 4 };
 *
 * var values = _.memoize(_.values);
 * values(object);
 * // => [1, 2]
 *
 * values(other);
 * // => [3, 4]
 *
 * object.a = 2;
 * values(object);
 * // => [1, 2]
 *
 * // Modify the result cache.
 * values.cache.set(object, ['a', 'b']);
 * values(object);
 * // => ['a', 'b']
 *
 * // Replace `_.memoize.Cache`.
 * _.memoize.Cache = WeakMap;
 */
function memoize(func, resolver) {
  if (typeof func != 'function' || (resolver && typeof resolver != 'function')) {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  var memoized = function() {
    var args = arguments,
        key = resolver ? resolver.apply(this, args) : args[0],
        cache = memoized.cache;

    if (cache.has(key)) {
      return cache.get(key);
    }
    var result = func.apply(this, args);
    memoized.cache = cache.set(key, result);
    return result;
  };
  memoized.cache = new (memoize.Cache || MapCache);
  return memoized;
}

// Assign cache to `_.memoize`.
memoize.Cache = MapCache;

/**
 * Performs a
 * [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * comparison between two values to determine if they are equivalent.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 * @example
 *
 * var object = { 'a': 1 };
 * var other = { 'a': 1 };
 *
 * _.eq(object, object);
 * // => true
 *
 * _.eq(object, other);
 * // => false
 *
 * _.eq('a', 'a');
 * // => true
 *
 * _.eq('a', Object('a'));
 * // => false
 *
 * _.eq(NaN, NaN);
 * // => true
 */
function eq(value, other) {
  return value === other || (value !== value && other !== other);
}

/**
 * Checks if `value` is classified as a `Function` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a function, else `false`.
 * @example
 *
 * _.isFunction(_);
 * // => true
 *
 * _.isFunction(/abc/);
 * // => false
 */
function isFunction(value) {
  // The use of `Object#toString` avoids issues with the `typeof` operator
  // in Safari 8-9 which returns 'object' for typed array and other constructors.
  var tag = isObject(value) ? objectToString.call(value) : '';
  return tag == funcTag || tag == genTag;
}

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return !!value && (type == 'object' || type == 'function');
}

module.exports = memoize;

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "./node_modules/lodash.merge/index.js":
/*!********************************************!*\
  !*** ./node_modules/lodash.merge/index.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global, module) {/**
 * Lodash (Custom Build) <https://lodash.com/>
 * Build: `lodash modularize exports="npm" -o ./`
 * Copyright OpenJS Foundation and other contributors <https://openjsf.org/>
 * Released under MIT license <https://lodash.com/license>
 * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
 * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 */

/** Used as the size to enable large array optimizations. */
var LARGE_ARRAY_SIZE = 200;

/** Used to stand-in for `undefined` hash values. */
var HASH_UNDEFINED = '__lodash_hash_undefined__';

/** Used to detect hot functions by number of calls within a span of milliseconds. */
var HOT_COUNT = 800,
    HOT_SPAN = 16;

/** Used as references for various `Number` constants. */
var MAX_SAFE_INTEGER = 9007199254740991;

/** `Object#toString` result references. */
var argsTag = '[object Arguments]',
    arrayTag = '[object Array]',
    asyncTag = '[object AsyncFunction]',
    boolTag = '[object Boolean]',
    dateTag = '[object Date]',
    errorTag = '[object Error]',
    funcTag = '[object Function]',
    genTag = '[object GeneratorFunction]',
    mapTag = '[object Map]',
    numberTag = '[object Number]',
    nullTag = '[object Null]',
    objectTag = '[object Object]',
    proxyTag = '[object Proxy]',
    regexpTag = '[object RegExp]',
    setTag = '[object Set]',
    stringTag = '[object String]',
    undefinedTag = '[object Undefined]',
    weakMapTag = '[object WeakMap]';

var arrayBufferTag = '[object ArrayBuffer]',
    dataViewTag = '[object DataView]',
    float32Tag = '[object Float32Array]',
    float64Tag = '[object Float64Array]',
    int8Tag = '[object Int8Array]',
    int16Tag = '[object Int16Array]',
    int32Tag = '[object Int32Array]',
    uint8Tag = '[object Uint8Array]',
    uint8ClampedTag = '[object Uint8ClampedArray]',
    uint16Tag = '[object Uint16Array]',
    uint32Tag = '[object Uint32Array]';

/**
 * Used to match `RegExp`
 * [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).
 */
var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;

/** Used to detect host constructors (Safari). */
var reIsHostCtor = /^\[object .+?Constructor\]$/;

/** Used to detect unsigned integer values. */
var reIsUint = /^(?:0|[1-9]\d*)$/;

/** Used to identify `toStringTag` values of typed arrays. */
var typedArrayTags = {};
typedArrayTags[float32Tag] = typedArrayTags[float64Tag] =
typedArrayTags[int8Tag] = typedArrayTags[int16Tag] =
typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] =
typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] =
typedArrayTags[uint32Tag] = true;
typedArrayTags[argsTag] = typedArrayTags[arrayTag] =
typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] =
typedArrayTags[dataViewTag] = typedArrayTags[dateTag] =
typedArrayTags[errorTag] = typedArrayTags[funcTag] =
typedArrayTags[mapTag] = typedArrayTags[numberTag] =
typedArrayTags[objectTag] = typedArrayTags[regexpTag] =
typedArrayTags[setTag] = typedArrayTags[stringTag] =
typedArrayTags[weakMapTag] = false;

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

/** Detect free variable `exports`. */
var freeExports =  true && exports && !exports.nodeType && exports;

/** Detect free variable `module`. */
var freeModule = freeExports && typeof module == 'object' && module && !module.nodeType && module;

/** Detect the popular CommonJS extension `module.exports`. */
var moduleExports = freeModule && freeModule.exports === freeExports;

/** Detect free variable `process` from Node.js. */
var freeProcess = moduleExports && freeGlobal.process;

/** Used to access faster Node.js helpers. */
var nodeUtil = (function() {
  try {
    // Use `util.types` for Node.js 10+.
    var types = freeModule && freeModule.require && freeModule.require('util').types;

    if (types) {
      return types;
    }

    // Legacy `process.binding('util')` for Node.js < 10.
    return freeProcess && freeProcess.binding && freeProcess.binding('util');
  } catch (e) {}
}());

/* Node.js helper references. */
var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;

/**
 * A faster alternative to `Function#apply`, this function invokes `func`
 * with the `this` binding of `thisArg` and the arguments of `args`.
 *
 * @private
 * @param {Function} func The function to invoke.
 * @param {*} thisArg The `this` binding of `func`.
 * @param {Array} args The arguments to invoke `func` with.
 * @returns {*} Returns the result of `func`.
 */
function apply(func, thisArg, args) {
  switch (args.length) {
    case 0: return func.call(thisArg);
    case 1: return func.call(thisArg, args[0]);
    case 2: return func.call(thisArg, args[0], args[1]);
    case 3: return func.call(thisArg, args[0], args[1], args[2]);
  }
  return func.apply(thisArg, args);
}

/**
 * The base implementation of `_.times` without support for iteratee shorthands
 * or max array length checks.
 *
 * @private
 * @param {number} n The number of times to invoke `iteratee`.
 * @param {Function} iteratee The function invoked per iteration.
 * @returns {Array} Returns the array of results.
 */
function baseTimes(n, iteratee) {
  var index = -1,
      result = Array(n);

  while (++index < n) {
    result[index] = iteratee(index);
  }
  return result;
}

/**
 * The base implementation of `_.unary` without support for storing metadata.
 *
 * @private
 * @param {Function} func The function to cap arguments for.
 * @returns {Function} Returns the new capped function.
 */
function baseUnary(func) {
  return function(value) {
    return func(value);
  };
}

/**
 * Gets the value at `key` of `object`.
 *
 * @private
 * @param {Object} [object] The object to query.
 * @param {string} key The key of the property to get.
 * @returns {*} Returns the property value.
 */
function getValue(object, key) {
  return object == null ? undefined : object[key];
}

/**
 * Creates a unary function that invokes `func` with its argument transformed.
 *
 * @private
 * @param {Function} func The function to wrap.
 * @param {Function} transform The argument transform.
 * @returns {Function} Returns the new function.
 */
function overArg(func, transform) {
  return function(arg) {
    return func(transform(arg));
  };
}

/** Used for built-in method references. */
var arrayProto = Array.prototype,
    funcProto = Function.prototype,
    objectProto = Object.prototype;

/** Used to detect overreaching core-js shims. */
var coreJsData = root['__core-js_shared__'];

/** Used to resolve the decompiled source of functions. */
var funcToString = funcProto.toString;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/** Used to detect methods masquerading as native. */
var maskSrcKey = (function() {
  var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || '');
  return uid ? ('Symbol(src)_1.' + uid) : '';
}());

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/** Used to infer the `Object` constructor. */
var objectCtorString = funcToString.call(Object);

/** Used to detect if a method is native. */
var reIsNative = RegExp('^' +
  funcToString.call(hasOwnProperty).replace(reRegExpChar, '\\$&')
  .replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, '$1.*?') + '$'
);

/** Built-in value references. */
var Buffer = moduleExports ? root.Buffer : undefined,
    Symbol = root.Symbol,
    Uint8Array = root.Uint8Array,
    allocUnsafe = Buffer ? Buffer.allocUnsafe : undefined,
    getPrototype = overArg(Object.getPrototypeOf, Object),
    objectCreate = Object.create,
    propertyIsEnumerable = objectProto.propertyIsEnumerable,
    splice = arrayProto.splice,
    symToStringTag = Symbol ? Symbol.toStringTag : undefined;

var defineProperty = (function() {
  try {
    var func = getNative(Object, 'defineProperty');
    func({}, '', {});
    return func;
  } catch (e) {}
}());

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeIsBuffer = Buffer ? Buffer.isBuffer : undefined,
    nativeMax = Math.max,
    nativeNow = Date.now;

/* Built-in method references that are verified to be native. */
var Map = getNative(root, 'Map'),
    nativeCreate = getNative(Object, 'create');

/**
 * The base implementation of `_.create` without support for assigning
 * properties to the created object.
 *
 * @private
 * @param {Object} proto The object to inherit from.
 * @returns {Object} Returns the new object.
 */
var baseCreate = (function() {
  function object() {}
  return function(proto) {
    if (!isObject(proto)) {
      return {};
    }
    if (objectCreate) {
      return objectCreate(proto);
    }
    object.prototype = proto;
    var result = new object;
    object.prototype = undefined;
    return result;
  };
}());

/**
 * Creates a hash object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Hash(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the hash.
 *
 * @private
 * @name clear
 * @memberOf Hash
 */
function hashClear() {
  this.__data__ = nativeCreate ? nativeCreate(null) : {};
  this.size = 0;
}

/**
 * Removes `key` and its value from the hash.
 *
 * @private
 * @name delete
 * @memberOf Hash
 * @param {Object} hash The hash to modify.
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function hashDelete(key) {
  var result = this.has(key) && delete this.__data__[key];
  this.size -= result ? 1 : 0;
  return result;
}

/**
 * Gets the hash value for `key`.
 *
 * @private
 * @name get
 * @memberOf Hash
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function hashGet(key) {
  var data = this.__data__;
  if (nativeCreate) {
    var result = data[key];
    return result === HASH_UNDEFINED ? undefined : result;
  }
  return hasOwnProperty.call(data, key) ? data[key] : undefined;
}

/**
 * Checks if a hash value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Hash
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function hashHas(key) {
  var data = this.__data__;
  return nativeCreate ? (data[key] !== undefined) : hasOwnProperty.call(data, key);
}

/**
 * Sets the hash `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Hash
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the hash instance.
 */
function hashSet(key, value) {
  var data = this.__data__;
  this.size += this.has(key) ? 0 : 1;
  data[key] = (nativeCreate && value === undefined) ? HASH_UNDEFINED : value;
  return this;
}

// Add methods to `Hash`.
Hash.prototype.clear = hashClear;
Hash.prototype['delete'] = hashDelete;
Hash.prototype.get = hashGet;
Hash.prototype.has = hashHas;
Hash.prototype.set = hashSet;

/**
 * Creates an list cache object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function ListCache(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the list cache.
 *
 * @private
 * @name clear
 * @memberOf ListCache
 */
function listCacheClear() {
  this.__data__ = [];
  this.size = 0;
}

/**
 * Removes `key` and its value from the list cache.
 *
 * @private
 * @name delete
 * @memberOf ListCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function listCacheDelete(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    return false;
  }
  var lastIndex = data.length - 1;
  if (index == lastIndex) {
    data.pop();
  } else {
    splice.call(data, index, 1);
  }
  --this.size;
  return true;
}

/**
 * Gets the list cache value for `key`.
 *
 * @private
 * @name get
 * @memberOf ListCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function listCacheGet(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  return index < 0 ? undefined : data[index][1];
}

/**
 * Checks if a list cache value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf ListCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function listCacheHas(key) {
  return assocIndexOf(this.__data__, key) > -1;
}

/**
 * Sets the list cache `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf ListCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the list cache instance.
 */
function listCacheSet(key, value) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    ++this.size;
    data.push([key, value]);
  } else {
    data[index][1] = value;
  }
  return this;
}

// Add methods to `ListCache`.
ListCache.prototype.clear = listCacheClear;
ListCache.prototype['delete'] = listCacheDelete;
ListCache.prototype.get = listCacheGet;
ListCache.prototype.has = listCacheHas;
ListCache.prototype.set = listCacheSet;

/**
 * Creates a map cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function MapCache(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the map.
 *
 * @private
 * @name clear
 * @memberOf MapCache
 */
function mapCacheClear() {
  this.size = 0;
  this.__data__ = {
    'hash': new Hash,
    'map': new (Map || ListCache),
    'string': new Hash
  };
}

/**
 * Removes `key` and its value from the map.
 *
 * @private
 * @name delete
 * @memberOf MapCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function mapCacheDelete(key) {
  var result = getMapData(this, key)['delete'](key);
  this.size -= result ? 1 : 0;
  return result;
}

/**
 * Gets the map value for `key`.
 *
 * @private
 * @name get
 * @memberOf MapCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function mapCacheGet(key) {
  return getMapData(this, key).get(key);
}

/**
 * Checks if a map value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf MapCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function mapCacheHas(key) {
  return getMapData(this, key).has(key);
}

/**
 * Sets the map `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf MapCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the map cache instance.
 */
function mapCacheSet(key, value) {
  var data = getMapData(this, key),
      size = data.size;

  data.set(key, value);
  this.size += data.size == size ? 0 : 1;
  return this;
}

// Add methods to `MapCache`.
MapCache.prototype.clear = mapCacheClear;
MapCache.prototype['delete'] = mapCacheDelete;
MapCache.prototype.get = mapCacheGet;
MapCache.prototype.has = mapCacheHas;
MapCache.prototype.set = mapCacheSet;

/**
 * Creates a stack cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Stack(entries) {
  var data = this.__data__ = new ListCache(entries);
  this.size = data.size;
}

/**
 * Removes all key-value entries from the stack.
 *
 * @private
 * @name clear
 * @memberOf Stack
 */
function stackClear() {
  this.__data__ = new ListCache;
  this.size = 0;
}

/**
 * Removes `key` and its value from the stack.
 *
 * @private
 * @name delete
 * @memberOf Stack
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function stackDelete(key) {
  var data = this.__data__,
      result = data['delete'](key);

  this.size = data.size;
  return result;
}

/**
 * Gets the stack value for `key`.
 *
 * @private
 * @name get
 * @memberOf Stack
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function stackGet(key) {
  return this.__data__.get(key);
}

/**
 * Checks if a stack value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Stack
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function stackHas(key) {
  return this.__data__.has(key);
}

/**
 * Sets the stack `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Stack
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the stack cache instance.
 */
function stackSet(key, value) {
  var data = this.__data__;
  if (data instanceof ListCache) {
    var pairs = data.__data__;
    if (!Map || (pairs.length < LARGE_ARRAY_SIZE - 1)) {
      pairs.push([key, value]);
      this.size = ++data.size;
      return this;
    }
    data = this.__data__ = new MapCache(pairs);
  }
  data.set(key, value);
  this.size = data.size;
  return this;
}

// Add methods to `Stack`.
Stack.prototype.clear = stackClear;
Stack.prototype['delete'] = stackDelete;
Stack.prototype.get = stackGet;
Stack.prototype.has = stackHas;
Stack.prototype.set = stackSet;

/**
 * Creates an array of the enumerable property names of the array-like `value`.
 *
 * @private
 * @param {*} value The value to query.
 * @param {boolean} inherited Specify returning inherited property names.
 * @returns {Array} Returns the array of property names.
 */
function arrayLikeKeys(value, inherited) {
  var isArr = isArray(value),
      isArg = !isArr && isArguments(value),
      isBuff = !isArr && !isArg && isBuffer(value),
      isType = !isArr && !isArg && !isBuff && isTypedArray(value),
      skipIndexes = isArr || isArg || isBuff || isType,
      result = skipIndexes ? baseTimes(value.length, String) : [],
      length = result.length;

  for (var key in value) {
    if ((inherited || hasOwnProperty.call(value, key)) &&
        !(skipIndexes && (
           // Safari 9 has enumerable `arguments.length` in strict mode.
           key == 'length' ||
           // Node.js 0.10 has enumerable non-index properties on buffers.
           (isBuff && (key == 'offset' || key == 'parent')) ||
           // PhantomJS 2 has enumerable non-index properties on typed arrays.
           (isType && (key == 'buffer' || key == 'byteLength' || key == 'byteOffset')) ||
           // Skip index properties.
           isIndex(key, length)
        ))) {
      result.push(key);
    }
  }
  return result;
}

/**
 * This function is like `assignValue` except that it doesn't assign
 * `undefined` values.
 *
 * @private
 * @param {Object} object The object to modify.
 * @param {string} key The key of the property to assign.
 * @param {*} value The value to assign.
 */
function assignMergeValue(object, key, value) {
  if ((value !== undefined && !eq(object[key], value)) ||
      (value === undefined && !(key in object))) {
    baseAssignValue(object, key, value);
  }
}

/**
 * Assigns `value` to `key` of `object` if the existing value is not equivalent
 * using [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * for equality comparisons.
 *
 * @private
 * @param {Object} object The object to modify.
 * @param {string} key The key of the property to assign.
 * @param {*} value The value to assign.
 */
function assignValue(object, key, value) {
  var objValue = object[key];
  if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) ||
      (value === undefined && !(key in object))) {
    baseAssignValue(object, key, value);
  }
}

/**
 * Gets the index at which the `key` is found in `array` of key-value pairs.
 *
 * @private
 * @param {Array} array The array to inspect.
 * @param {*} key The key to search for.
 * @returns {number} Returns the index of the matched value, else `-1`.
 */
function assocIndexOf(array, key) {
  var length = array.length;
  while (length--) {
    if (eq(array[length][0], key)) {
      return length;
    }
  }
  return -1;
}

/**
 * The base implementation of `assignValue` and `assignMergeValue` without
 * value checks.
 *
 * @private
 * @param {Object} object The object to modify.
 * @param {string} key The key of the property to assign.
 * @param {*} value The value to assign.
 */
function baseAssignValue(object, key, value) {
  if (key == '__proto__' && defineProperty) {
    defineProperty(object, key, {
      'configurable': true,
      'enumerable': true,
      'value': value,
      'writable': true
    });
  } else {
    object[key] = value;
  }
}

/**
 * The base implementation of `baseForOwn` which iterates over `object`
 * properties returned by `keysFunc` and invokes `iteratee` for each property.
 * Iteratee functions may exit iteration early by explicitly returning `false`.
 *
 * @private
 * @param {Object} object The object to iterate over.
 * @param {Function} iteratee The function invoked per iteration.
 * @param {Function} keysFunc The function to get the keys of `object`.
 * @returns {Object} Returns `object`.
 */
var baseFor = createBaseFor();

/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return (symToStringTag && symToStringTag in Object(value))
    ? getRawTag(value)
    : objectToString(value);
}

/**
 * The base implementation of `_.isArguments`.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 */
function baseIsArguments(value) {
  return isObjectLike(value) && baseGetTag(value) == argsTag;
}

/**
 * The base implementation of `_.isNative` without bad shim checks.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a native function,
 *  else `false`.
 */
function baseIsNative(value) {
  if (!isObject(value) || isMasked(value)) {
    return false;
  }
  var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
  return pattern.test(toSource(value));
}

/**
 * The base implementation of `_.isTypedArray` without Node.js optimizations.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 */
function baseIsTypedArray(value) {
  return isObjectLike(value) &&
    isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
}

/**
 * The base implementation of `_.keysIn` which doesn't treat sparse arrays as dense.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 */
function baseKeysIn(object) {
  if (!isObject(object)) {
    return nativeKeysIn(object);
  }
  var isProto = isPrototype(object),
      result = [];

  for (var key in object) {
    if (!(key == 'constructor' && (isProto || !hasOwnProperty.call(object, key)))) {
      result.push(key);
    }
  }
  return result;
}

/**
 * The base implementation of `_.merge` without support for multiple sources.
 *
 * @private
 * @param {Object} object The destination object.
 * @param {Object} source The source object.
 * @param {number} srcIndex The index of `source`.
 * @param {Function} [customizer] The function to customize merged values.
 * @param {Object} [stack] Tracks traversed source values and their merged
 *  counterparts.
 */
function baseMerge(object, source, srcIndex, customizer, stack) {
  if (object === source) {
    return;
  }
  baseFor(source, function(srcValue, key) {
    stack || (stack = new Stack);
    if (isObject(srcValue)) {
      baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
    }
    else {
      var newValue = customizer
        ? customizer(safeGet(object, key), srcValue, (key + ''), object, source, stack)
        : undefined;

      if (newValue === undefined) {
        newValue = srcValue;
      }
      assignMergeValue(object, key, newValue);
    }
  }, keysIn);
}

/**
 * A specialized version of `baseMerge` for arrays and objects which performs
 * deep merges and tracks traversed objects enabling objects with circular
 * references to be merged.
 *
 * @private
 * @param {Object} object The destination object.
 * @param {Object} source The source object.
 * @param {string} key The key of the value to merge.
 * @param {number} srcIndex The index of `source`.
 * @param {Function} mergeFunc The function to merge values.
 * @param {Function} [customizer] The function to customize assigned values.
 * @param {Object} [stack] Tracks traversed source values and their merged
 *  counterparts.
 */
function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
  var objValue = safeGet(object, key),
      srcValue = safeGet(source, key),
      stacked = stack.get(srcValue);

  if (stacked) {
    assignMergeValue(object, key, stacked);
    return;
  }
  var newValue = customizer
    ? customizer(objValue, srcValue, (key + ''), object, source, stack)
    : undefined;

  var isCommon = newValue === undefined;

  if (isCommon) {
    var isArr = isArray(srcValue),
        isBuff = !isArr && isBuffer(srcValue),
        isTyped = !isArr && !isBuff && isTypedArray(srcValue);

    newValue = srcValue;
    if (isArr || isBuff || isTyped) {
      if (isArray(objValue)) {
        newValue = objValue;
      }
      else if (isArrayLikeObject(objValue)) {
        newValue = copyArray(objValue);
      }
      else if (isBuff) {
        isCommon = false;
        newValue = cloneBuffer(srcValue, true);
      }
      else if (isTyped) {
        isCommon = false;
        newValue = cloneTypedArray(srcValue, true);
      }
      else {
        newValue = [];
      }
    }
    else if (isPlainObject(srcValue) || isArguments(srcValue)) {
      newValue = objValue;
      if (isArguments(objValue)) {
        newValue = toPlainObject(objValue);
      }
      else if (!isObject(objValue) || isFunction(objValue)) {
        newValue = initCloneObject(srcValue);
      }
    }
    else {
      isCommon = false;
    }
  }
  if (isCommon) {
    // Recursively merge objects and arrays (susceptible to call stack limits).
    stack.set(srcValue, newValue);
    mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
    stack['delete'](srcValue);
  }
  assignMergeValue(object, key, newValue);
}

/**
 * The base implementation of `_.rest` which doesn't validate or coerce arguments.
 *
 * @private
 * @param {Function} func The function to apply a rest parameter to.
 * @param {number} [start=func.length-1] The start position of the rest parameter.
 * @returns {Function} Returns the new function.
 */
function baseRest(func, start) {
  return setToString(overRest(func, start, identity), func + '');
}

/**
 * The base implementation of `setToString` without support for hot loop shorting.
 *
 * @private
 * @param {Function} func The function to modify.
 * @param {Function} string The `toString` result.
 * @returns {Function} Returns `func`.
 */
var baseSetToString = !defineProperty ? identity : function(func, string) {
  return defineProperty(func, 'toString', {
    'configurable': true,
    'enumerable': false,
    'value': constant(string),
    'writable': true
  });
};

/**
 * Creates a clone of  `buffer`.
 *
 * @private
 * @param {Buffer} buffer The buffer to clone.
 * @param {boolean} [isDeep] Specify a deep clone.
 * @returns {Buffer} Returns the cloned buffer.
 */
function cloneBuffer(buffer, isDeep) {
  if (isDeep) {
    return buffer.slice();
  }
  var length = buffer.length,
      result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);

  buffer.copy(result);
  return result;
}

/**
 * Creates a clone of `arrayBuffer`.
 *
 * @private
 * @param {ArrayBuffer} arrayBuffer The array buffer to clone.
 * @returns {ArrayBuffer} Returns the cloned array buffer.
 */
function cloneArrayBuffer(arrayBuffer) {
  var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
  new Uint8Array(result).set(new Uint8Array(arrayBuffer));
  return result;
}

/**
 * Creates a clone of `typedArray`.
 *
 * @private
 * @param {Object} typedArray The typed array to clone.
 * @param {boolean} [isDeep] Specify a deep clone.
 * @returns {Object} Returns the cloned typed array.
 */
function cloneTypedArray(typedArray, isDeep) {
  var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
  return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
}

/**
 * Copies the values of `source` to `array`.
 *
 * @private
 * @param {Array} source The array to copy values from.
 * @param {Array} [array=[]] The array to copy values to.
 * @returns {Array} Returns `array`.
 */
function copyArray(source, array) {
  var index = -1,
      length = source.length;

  array || (array = Array(length));
  while (++index < length) {
    array[index] = source[index];
  }
  return array;
}

/**
 * Copies properties of `source` to `object`.
 *
 * @private
 * @param {Object} source The object to copy properties from.
 * @param {Array} props The property identifiers to copy.
 * @param {Object} [object={}] The object to copy properties to.
 * @param {Function} [customizer] The function to customize copied values.
 * @returns {Object} Returns `object`.
 */
function copyObject(source, props, object, customizer) {
  var isNew = !object;
  object || (object = {});

  var index = -1,
      length = props.length;

  while (++index < length) {
    var key = props[index];

    var newValue = customizer
      ? customizer(object[key], source[key], key, object, source)
      : undefined;

    if (newValue === undefined) {
      newValue = source[key];
    }
    if (isNew) {
      baseAssignValue(object, key, newValue);
    } else {
      assignValue(object, key, newValue);
    }
  }
  return object;
}

/**
 * Creates a function like `_.assign`.
 *
 * @private
 * @param {Function} assigner The function to assign values.
 * @returns {Function} Returns the new assigner function.
 */
function createAssigner(assigner) {
  return baseRest(function(object, sources) {
    var index = -1,
        length = sources.length,
        customizer = length > 1 ? sources[length - 1] : undefined,
        guard = length > 2 ? sources[2] : undefined;

    customizer = (assigner.length > 3 && typeof customizer == 'function')
      ? (length--, customizer)
      : undefined;

    if (guard && isIterateeCall(sources[0], sources[1], guard)) {
      customizer = length < 3 ? undefined : customizer;
      length = 1;
    }
    object = Object(object);
    while (++index < length) {
      var source = sources[index];
      if (source) {
        assigner(object, source, index, customizer);
      }
    }
    return object;
  });
}

/**
 * Creates a base function for methods like `_.forIn` and `_.forOwn`.
 *
 * @private
 * @param {boolean} [fromRight] Specify iterating from right to left.
 * @returns {Function} Returns the new base function.
 */
function createBaseFor(fromRight) {
  return function(object, iteratee, keysFunc) {
    var index = -1,
        iterable = Object(object),
        props = keysFunc(object),
        length = props.length;

    while (length--) {
      var key = props[fromRight ? length : ++index];
      if (iteratee(iterable[key], key, iterable) === false) {
        break;
      }
    }
    return object;
  };
}

/**
 * Gets the data for `map`.
 *
 * @private
 * @param {Object} map The map to query.
 * @param {string} key The reference key.
 * @returns {*} Returns the map data.
 */
function getMapData(map, key) {
  var data = map.__data__;
  return isKeyable(key)
    ? data[typeof key == 'string' ? 'string' : 'hash']
    : data.map;
}

/**
 * Gets the native function at `key` of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {string} key The key of the method to get.
 * @returns {*} Returns the function if it's native, else `undefined`.
 */
function getNative(object, key) {
  var value = getValue(object, key);
  return baseIsNative(value) ? value : undefined;
}

/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}

/**
 * Initializes an object clone.
 *
 * @private
 * @param {Object} object The object to clone.
 * @returns {Object} Returns the initialized clone.
 */
function initCloneObject(object) {
  return (typeof object.constructor == 'function' && !isPrototype(object))
    ? baseCreate(getPrototype(object))
    : {};
}

/**
 * Checks if `value` is a valid array-like index.
 *
 * @private
 * @param {*} value The value to check.
 * @param {number} [length=MAX_SAFE_INTEGER] The upper bounds of a valid index.
 * @returns {boolean} Returns `true` if `value` is a valid index, else `false`.
 */
function isIndex(value, length) {
  var type = typeof value;
  length = length == null ? MAX_SAFE_INTEGER : length;

  return !!length &&
    (type == 'number' ||
      (type != 'symbol' && reIsUint.test(value))) &&
        (value > -1 && value % 1 == 0 && value < length);
}

/**
 * Checks if the given arguments are from an iteratee call.
 *
 * @private
 * @param {*} value The potential iteratee value argument.
 * @param {*} index The potential iteratee index or key argument.
 * @param {*} object The potential iteratee object argument.
 * @returns {boolean} Returns `true` if the arguments are from an iteratee call,
 *  else `false`.
 */
function isIterateeCall(value, index, object) {
  if (!isObject(object)) {
    return false;
  }
  var type = typeof index;
  if (type == 'number'
        ? (isArrayLike(object) && isIndex(index, object.length))
        : (type == 'string' && index in object)
      ) {
    return eq(object[index], value);
  }
  return false;
}

/**
 * Checks if `value` is suitable for use as unique object key.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is suitable, else `false`.
 */
function isKeyable(value) {
  var type = typeof value;
  return (type == 'string' || type == 'number' || type == 'symbol' || type == 'boolean')
    ? (value !== '__proto__')
    : (value === null);
}

/**
 * Checks if `func` has its source masked.
 *
 * @private
 * @param {Function} func The function to check.
 * @returns {boolean} Returns `true` if `func` is masked, else `false`.
 */
function isMasked(func) {
  return !!maskSrcKey && (maskSrcKey in func);
}

/**
 * Checks if `value` is likely a prototype object.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a prototype, else `false`.
 */
function isPrototype(value) {
  var Ctor = value && value.constructor,
      proto = (typeof Ctor == 'function' && Ctor.prototype) || objectProto;

  return value === proto;
}

/**
 * This function is like
 * [`Object.keys`](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)
 * except that it includes inherited enumerable properties.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 */
function nativeKeysIn(object) {
  var result = [];
  if (object != null) {
    for (var key in Object(object)) {
      result.push(key);
    }
  }
  return result;
}

/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */
function objectToString(value) {
  return nativeObjectToString.call(value);
}

/**
 * A specialized version of `baseRest` which transforms the rest array.
 *
 * @private
 * @param {Function} func The function to apply a rest parameter to.
 * @param {number} [start=func.length-1] The start position of the rest parameter.
 * @param {Function} transform The rest array transform.
 * @returns {Function} Returns the new function.
 */
function overRest(func, start, transform) {
  start = nativeMax(start === undefined ? (func.length - 1) : start, 0);
  return function() {
    var args = arguments,
        index = -1,
        length = nativeMax(args.length - start, 0),
        array = Array(length);

    while (++index < length) {
      array[index] = args[start + index];
    }
    index = -1;
    var otherArgs = Array(start + 1);
    while (++index < start) {
      otherArgs[index] = args[index];
    }
    otherArgs[start] = transform(array);
    return apply(func, this, otherArgs);
  };
}

/**
 * Gets the value at `key`, unless `key` is "__proto__" or "constructor".
 *
 * @private
 * @param {Object} object The object to query.
 * @param {string} key The key of the property to get.
 * @returns {*} Returns the property value.
 */
function safeGet(object, key) {
  if (key === 'constructor' && typeof object[key] === 'function') {
    return;
  }

  if (key == '__proto__') {
    return;
  }

  return object[key];
}

/**
 * Sets the `toString` method of `func` to return `string`.
 *
 * @private
 * @param {Function} func The function to modify.
 * @param {Function} string The `toString` result.
 * @returns {Function} Returns `func`.
 */
var setToString = shortOut(baseSetToString);

/**
 * Creates a function that'll short out and invoke `identity` instead
 * of `func` when it's called `HOT_COUNT` or more times in `HOT_SPAN`
 * milliseconds.
 *
 * @private
 * @param {Function} func The function to restrict.
 * @returns {Function} Returns the new shortable function.
 */
function shortOut(func) {
  var count = 0,
      lastCalled = 0;

  return function() {
    var stamp = nativeNow(),
        remaining = HOT_SPAN - (stamp - lastCalled);

    lastCalled = stamp;
    if (remaining > 0) {
      if (++count >= HOT_COUNT) {
        return arguments[0];
      }
    } else {
      count = 0;
    }
    return func.apply(undefined, arguments);
  };
}

/**
 * Converts `func` to its source code.
 *
 * @private
 * @param {Function} func The function to convert.
 * @returns {string} Returns the source code.
 */
function toSource(func) {
  if (func != null) {
    try {
      return funcToString.call(func);
    } catch (e) {}
    try {
      return (func + '');
    } catch (e) {}
  }
  return '';
}

/**
 * Performs a
 * [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * comparison between two values to determine if they are equivalent.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 * @example
 *
 * var object = { 'a': 1 };
 * var other = { 'a': 1 };
 *
 * _.eq(object, object);
 * // => true
 *
 * _.eq(object, other);
 * // => false
 *
 * _.eq('a', 'a');
 * // => true
 *
 * _.eq('a', Object('a'));
 * // => false
 *
 * _.eq(NaN, NaN);
 * // => true
 */
function eq(value, other) {
  return value === other || (value !== value && other !== other);
}

/**
 * Checks if `value` is likely an `arguments` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 *  else `false`.
 * @example
 *
 * _.isArguments(function() { return arguments; }());
 * // => true
 *
 * _.isArguments([1, 2, 3]);
 * // => false
 */
var isArguments = baseIsArguments(function() { return arguments; }()) ? baseIsArguments : function(value) {
  return isObjectLike(value) && hasOwnProperty.call(value, 'callee') &&
    !propertyIsEnumerable.call(value, 'callee');
};

/**
 * Checks if `value` is classified as an `Array` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an array, else `false`.
 * @example
 *
 * _.isArray([1, 2, 3]);
 * // => true
 *
 * _.isArray(document.body.children);
 * // => false
 *
 * _.isArray('abc');
 * // => false
 *
 * _.isArray(_.noop);
 * // => false
 */
var isArray = Array.isArray;

/**
 * Checks if `value` is array-like. A value is considered array-like if it's
 * not a function and has a `value.length` that's an integer greater than or
 * equal to `0` and less than or equal to `Number.MAX_SAFE_INTEGER`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is array-like, else `false`.
 * @example
 *
 * _.isArrayLike([1, 2, 3]);
 * // => true
 *
 * _.isArrayLike(document.body.children);
 * // => true
 *
 * _.isArrayLike('abc');
 * // => true
 *
 * _.isArrayLike(_.noop);
 * // => false
 */
function isArrayLike(value) {
  return value != null && isLength(value.length) && !isFunction(value);
}

/**
 * This method is like `_.isArrayLike` except that it also checks if `value`
 * is an object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an array-like object,
 *  else `false`.
 * @example
 *
 * _.isArrayLikeObject([1, 2, 3]);
 * // => true
 *
 * _.isArrayLikeObject(document.body.children);
 * // => true
 *
 * _.isArrayLikeObject('abc');
 * // => false
 *
 * _.isArrayLikeObject(_.noop);
 * // => false
 */
function isArrayLikeObject(value) {
  return isObjectLike(value) && isArrayLike(value);
}

/**
 * Checks if `value` is a buffer.
 *
 * @static
 * @memberOf _
 * @since 4.3.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a buffer, else `false`.
 * @example
 *
 * _.isBuffer(new Buffer(2));
 * // => true
 *
 * _.isBuffer(new Uint8Array(2));
 * // => false
 */
var isBuffer = nativeIsBuffer || stubFalse;

/**
 * Checks if `value` is classified as a `Function` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a function, else `false`.
 * @example
 *
 * _.isFunction(_);
 * // => true
 *
 * _.isFunction(/abc/);
 * // => false
 */
function isFunction(value) {
  if (!isObject(value)) {
    return false;
  }
  // The use of `Object#toString` avoids issues with the `typeof` operator
  // in Safari 9 which returns 'object' for typed arrays and other constructors.
  var tag = baseGetTag(value);
  return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
}

/**
 * Checks if `value` is a valid array-like length.
 *
 * **Note:** This method is loosely based on
 * [`ToLength`](http://ecma-international.org/ecma-262/7.0/#sec-tolength).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a valid length, else `false`.
 * @example
 *
 * _.isLength(3);
 * // => true
 *
 * _.isLength(Number.MIN_VALUE);
 * // => false
 *
 * _.isLength(Infinity);
 * // => false
 *
 * _.isLength('3');
 * // => false
 */
function isLength(value) {
  return typeof value == 'number' &&
    value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
}

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return value != null && (type == 'object' || type == 'function');
}

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

/**
 * Checks if `value` is a plain object, that is, an object created by the
 * `Object` constructor or one with a `[[Prototype]]` of `null`.
 *
 * @static
 * @memberOf _
 * @since 0.8.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a plain object, else `false`.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 * }
 *
 * _.isPlainObject(new Foo);
 * // => false
 *
 * _.isPlainObject([1, 2, 3]);
 * // => false
 *
 * _.isPlainObject({ 'x': 0, 'y': 0 });
 * // => true
 *
 * _.isPlainObject(Object.create(null));
 * // => true
 */
function isPlainObject(value) {
  if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
    return false;
  }
  var proto = getPrototype(value);
  if (proto === null) {
    return true;
  }
  var Ctor = hasOwnProperty.call(proto, 'constructor') && proto.constructor;
  return typeof Ctor == 'function' && Ctor instanceof Ctor &&
    funcToString.call(Ctor) == objectCtorString;
}

/**
 * Checks if `value` is classified as a typed array.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 * @example
 *
 * _.isTypedArray(new Uint8Array);
 * // => true
 *
 * _.isTypedArray([]);
 * // => false
 */
var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;

/**
 * Converts `value` to a plain object flattening inherited enumerable string
 * keyed properties of `value` to own properties of the plain object.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param {*} value The value to convert.
 * @returns {Object} Returns the converted plain object.
 * @example
 *
 * function Foo() {
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.assign({ 'a': 1 }, new Foo);
 * // => { 'a': 1, 'b': 2 }
 *
 * _.assign({ 'a': 1 }, _.toPlainObject(new Foo));
 * // => { 'a': 1, 'b': 2, 'c': 3 }
 */
function toPlainObject(value) {
  return copyObject(value, keysIn(value));
}

/**
 * Creates an array of the own and inherited enumerable property names of `object`.
 *
 * **Note:** Non-object values are coerced to objects.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Object
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.keysIn(new Foo);
 * // => ['a', 'b', 'c'] (iteration order is not guaranteed)
 */
function keysIn(object) {
  return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
}

/**
 * This method is like `_.assign` except that it recursively merges own and
 * inherited enumerable string keyed properties of source objects into the
 * destination object. Source properties that resolve to `undefined` are
 * skipped if a destination value exists. Array and plain object properties
 * are merged recursively. Other objects and value types are overridden by
 * assignment. Source objects are applied from left to right. Subsequent
 * sources overwrite property assignments of previous sources.
 *
 * **Note:** This method mutates `object`.
 *
 * @static
 * @memberOf _
 * @since 0.5.0
 * @category Object
 * @param {Object} object The destination object.
 * @param {...Object} [sources] The source objects.
 * @returns {Object} Returns `object`.
 * @example
 *
 * var object = {
 *   'a': [{ 'b': 2 }, { 'd': 4 }]
 * };
 *
 * var other = {
 *   'a': [{ 'c': 3 }, { 'e': 5 }]
 * };
 *
 * _.merge(object, other);
 * // => { 'a': [{ 'b': 2, 'c': 3 }, { 'd': 4, 'e': 5 }] }
 */
var merge = createAssigner(function(object, source, srcIndex) {
  baseMerge(object, source, srcIndex);
});

/**
 * Creates a function that returns `value`.
 *
 * @static
 * @memberOf _
 * @since 2.4.0
 * @category Util
 * @param {*} value The value to return from the new function.
 * @returns {Function} Returns the new constant function.
 * @example
 *
 * var objects = _.times(2, _.constant({ 'a': 1 }));
 *
 * console.log(objects);
 * // => [{ 'a': 1 }, { 'a': 1 }]
 *
 * console.log(objects[0] === objects[1]);
 * // => true
 */
function constant(value) {
  return function() {
    return value;
  };
}

/**
 * This method returns the first argument it receives.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Util
 * @param {*} value Any value.
 * @returns {*} Returns `value`.
 * @example
 *
 * var object = { 'a': 1 };
 *
 * console.log(_.identity(object) === object);
 * // => true
 */
function identity(value) {
  return value;
}

/**
 * This method returns `false`.
 *
 * @static
 * @memberOf _
 * @since 4.13.0
 * @category Util
 * @returns {boolean} Returns `false`.
 * @example
 *
 * _.times(2, _.stubFalse);
 * // => [false, false]
 */
function stubFalse() {
  return false;
}

module.exports = merge;

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js"), __webpack_require__(/*! ./../webpack/buildin/module.js */ "./node_modules/webpack/buildin/module.js")(module)))

/***/ }),

/***/ "./node_modules/object-assign/index.js":
/*!*********************************************!*\
  !*** ./node_modules/object-assign/index.js ***!
  \*********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/


/* eslint-disable no-unused-vars */
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
	if (val === null || val === undefined) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

function shouldUseNative() {
	try {
		if (!Object.assign) {
			return false;
		}

		// Detect buggy property enumeration order in older V8 versions.

		// https://bugs.chromium.org/p/v8/issues/detail?id=4118
		var test1 = new String('abc');  // eslint-disable-line no-new-wrappers
		test1[5] = 'de';
		if (Object.getOwnPropertyNames(test1)[0] === '5') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test2 = {};
		for (var i = 0; i < 10; i++) {
			test2['_' + String.fromCharCode(i)] = i;
		}
		var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
			return test2[n];
		});
		if (order2.join('') !== '0123456789') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test3 = {};
		'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
			test3[letter] = letter;
		});
		if (Object.keys(Object.assign({}, test3)).join('') !==
				'abcdefghijklmnopqrst') {
			return false;
		}

		return true;
	} catch (err) {
		// We don't expect any of the above to throw, but better to be safe.
		return false;
	}
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
	var from;
	var to = toObject(target);
	var symbols;

	for (var s = 1; s < arguments.length; s++) {
		from = Object(arguments[s]);

		for (var key in from) {
			if (hasOwnProperty.call(from, key)) {
				to[key] = from[key];
			}
		}

		if (getOwnPropertySymbols) {
			symbols = getOwnPropertySymbols(from);
			for (var i = 0; i < symbols.length; i++) {
				if (propIsEnumerable.call(from, symbols[i])) {
					to[symbols[i]] = from[symbols[i]];
				}
			}
		}
	}

	return to;
};


/***/ }),

/***/ "./node_modules/prop-types/checkPropTypes.js":
/*!***************************************************!*\
  !*** ./node_modules/prop-types/checkPropTypes.js ***!
  \***************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var printWarning = function() {};

if (true) {
  var ReactPropTypesSecret = __webpack_require__(/*! ./lib/ReactPropTypesSecret */ "./node_modules/prop-types/lib/ReactPropTypesSecret.js");
  var loggedTypeFailures = {};
  var has = Function.call.bind(Object.prototype.hasOwnProperty);

  printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}

/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */
function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
  if (true) {
    for (var typeSpecName in typeSpecs) {
      if (has(typeSpecs, typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          if (typeof typeSpecs[typeSpecName] !== 'function') {
            var err = Error(
              (componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' +
              'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.'
            );
            err.name = 'Invariant Violation';
            throw err;
          }
          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
          error = ex;
        }
        if (error && !(error instanceof Error)) {
          printWarning(
            (componentName || 'React class') + ': type specification of ' +
            location + ' `' + typeSpecName + '` is invalid; the type checker ' +
            'function must return `null` or an `Error` but returned a ' + typeof error + '. ' +
            'You may have forgotten to pass an argument to the type checker ' +
            'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' +
            'shape all require an argument).'
          );
        }
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
          // Only monitor this failure once because there tends to be a lot of the
          // same error.
          loggedTypeFailures[error.message] = true;

          var stack = getStack ? getStack() : '';

          printWarning(
            'Failed ' + location + ' type: ' + error.message + (stack != null ? stack : '')
          );
        }
      }
    }
  }
}

/**
 * Resets warning cache when testing.
 *
 * @private
 */
checkPropTypes.resetWarningCache = function() {
  if (true) {
    loggedTypeFailures = {};
  }
}

module.exports = checkPropTypes;


/***/ }),

/***/ "./node_modules/prop-types/factoryWithTypeCheckers.js":
/*!************************************************************!*\
  !*** ./node_modules/prop-types/factoryWithTypeCheckers.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactIs = __webpack_require__(/*! react-is */ "./node_modules/react-is/index.js");
var assign = __webpack_require__(/*! object-assign */ "./node_modules/object-assign/index.js");

var ReactPropTypesSecret = __webpack_require__(/*! ./lib/ReactPropTypesSecret */ "./node_modules/prop-types/lib/ReactPropTypesSecret.js");
var checkPropTypes = __webpack_require__(/*! ./checkPropTypes */ "./node_modules/prop-types/checkPropTypes.js");

var has = Function.call.bind(Object.prototype.hasOwnProperty);
var printWarning = function() {};

if (true) {
  printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}

function emptyFunctionThatReturnsNull() {
  return null;
}

module.exports = function(isValidElement, throwOnDirectAccess) {
  /* global Symbol */
  var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
  var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

  /**
   * Returns the iterator method function contained on the iterable object.
   *
   * Be sure to invoke the function with the iterable as context:
   *
   *     var iteratorFn = getIteratorFn(myIterable);
   *     if (iteratorFn) {
   *       var iterator = iteratorFn.call(myIterable);
   *       ...
   *     }
   *
   * @param {?object} maybeIterable
   * @return {?function}
   */
  function getIteratorFn(maybeIterable) {
    var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
    if (typeof iteratorFn === 'function') {
      return iteratorFn;
    }
  }

  /**
   * Collection of methods that allow declaration and validation of props that are
   * supplied to React components. Example usage:
   *
   *   var Props = require('ReactPropTypes');
   *   var MyArticle = React.createClass({
   *     propTypes: {
   *       // An optional string prop named "description".
   *       description: Props.string,
   *
   *       // A required enum prop named "category".
   *       category: Props.oneOf(['News','Photos']).isRequired,
   *
   *       // A prop named "dialog" that requires an instance of Dialog.
   *       dialog: Props.instanceOf(Dialog).isRequired
   *     },
   *     render: function() { ... }
   *   });
   *
   * A more formal specification of how these methods are used:
   *
   *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
   *   decl := ReactPropTypes.{type}(.isRequired)?
   *
   * Each and every declaration produces a function with the same signature. This
   * allows the creation of custom validation functions. For example:
   *
   *  var MyLink = React.createClass({
   *    propTypes: {
   *      // An optional string or URI prop named "href".
   *      href: function(props, propName, componentName) {
   *        var propValue = props[propName];
   *        if (propValue != null && typeof propValue !== 'string' &&
   *            !(propValue instanceof URI)) {
   *          return new Error(
   *            'Expected a string or an URI for ' + propName + ' in ' +
   *            componentName
   *          );
   *        }
   *      }
   *    },
   *    render: function() {...}
   *  });
   *
   * @internal
   */

  var ANONYMOUS = '<<anonymous>>';

  // Important!
  // Keep this list in sync with production version in `./factoryWithThrowingShims.js`.
  var ReactPropTypes = {
    array: createPrimitiveTypeChecker('array'),
    bool: createPrimitiveTypeChecker('boolean'),
    func: createPrimitiveTypeChecker('function'),
    number: createPrimitiveTypeChecker('number'),
    object: createPrimitiveTypeChecker('object'),
    string: createPrimitiveTypeChecker('string'),
    symbol: createPrimitiveTypeChecker('symbol'),

    any: createAnyTypeChecker(),
    arrayOf: createArrayOfTypeChecker,
    element: createElementTypeChecker(),
    elementType: createElementTypeTypeChecker(),
    instanceOf: createInstanceTypeChecker,
    node: createNodeChecker(),
    objectOf: createObjectOfTypeChecker,
    oneOf: createEnumTypeChecker,
    oneOfType: createUnionTypeChecker,
    shape: createShapeTypeChecker,
    exact: createStrictShapeTypeChecker,
  };

  /**
   * inlined Object.is polyfill to avoid requiring consumers ship their own
   * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
   */
  /*eslint-disable no-self-compare*/
  function is(x, y) {
    // SameValue algorithm
    if (x === y) {
      // Steps 1-5, 7-10
      // Steps 6.b-6.e: +0 != -0
      return x !== 0 || 1 / x === 1 / y;
    } else {
      // Step 6.a: NaN == NaN
      return x !== x && y !== y;
    }
  }
  /*eslint-enable no-self-compare*/

  /**
   * We use an Error-like object for backward compatibility as people may call
   * PropTypes directly and inspect their output. However, we don't use real
   * Errors anymore. We don't inspect their stack anyway, and creating them
   * is prohibitively expensive if they are created too often, such as what
   * happens in oneOfType() for any type before the one that matched.
   */
  function PropTypeError(message) {
    this.message = message;
    this.stack = '';
  }
  // Make `instanceof Error` still work for returned errors.
  PropTypeError.prototype = Error.prototype;

  function createChainableTypeChecker(validate) {
    if (true) {
      var manualPropTypeCallCache = {};
      var manualPropTypeWarningCount = 0;
    }
    function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
      componentName = componentName || ANONYMOUS;
      propFullName = propFullName || propName;

      if (secret !== ReactPropTypesSecret) {
        if (throwOnDirectAccess) {
          // New behavior only for users of `prop-types` package
          var err = new Error(
            'Calling PropTypes validators directly is not supported by the `prop-types` package. ' +
            'Use `PropTypes.checkPropTypes()` to call them. ' +
            'Read more at http://fb.me/use-check-prop-types'
          );
          err.name = 'Invariant Violation';
          throw err;
        } else if ( true && typeof console !== 'undefined') {
          // Old behavior for people using React.PropTypes
          var cacheKey = componentName + ':' + propName;
          if (
            !manualPropTypeCallCache[cacheKey] &&
            // Avoid spamming the console because they are often not actionable except for lib authors
            manualPropTypeWarningCount < 3
          ) {
            printWarning(
              'You are manually calling a React.PropTypes validation ' +
              'function for the `' + propFullName + '` prop on `' + componentName  + '`. This is deprecated ' +
              'and will throw in the standalone `prop-types` package. ' +
              'You may be seeing this warning due to a third-party PropTypes ' +
              'library. See https://fb.me/react-warning-dont-call-proptypes ' + 'for details.'
            );
            manualPropTypeCallCache[cacheKey] = true;
            manualPropTypeWarningCount++;
          }
        }
      }
      if (props[propName] == null) {
        if (isRequired) {
          if (props[propName] === null) {
            return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required ' + ('in `' + componentName + '`, but its value is `null`.'));
          }
          return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required in ' + ('`' + componentName + '`, but its value is `undefined`.'));
        }
        return null;
      } else {
        return validate(props, propName, componentName, location, propFullName);
      }
    }

    var chainedCheckType = checkType.bind(null, false);
    chainedCheckType.isRequired = checkType.bind(null, true);

    return chainedCheckType;
  }

  function createPrimitiveTypeChecker(expectedType) {
    function validate(props, propName, componentName, location, propFullName, secret) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== expectedType) {
        // `propValue` being instance of, say, date/regexp, pass the 'object'
        // check, but we can offer a more precise error message here rather than
        // 'of type `object`'.
        var preciseType = getPreciseType(propValue);

        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createAnyTypeChecker() {
    return createChainableTypeChecker(emptyFunctionThatReturnsNull);
  }

  function createArrayOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside arrayOf.');
      }
      var propValue = props[propName];
      if (!Array.isArray(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
      }
      for (var i = 0; i < propValue.length; i++) {
        var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']', ReactPropTypesSecret);
        if (error instanceof Error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createElementTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      if (!isValidElement(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createElementTypeTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      if (!ReactIs.isValidElementType(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement type.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createInstanceTypeChecker(expectedClass) {
    function validate(props, propName, componentName, location, propFullName) {
      if (!(props[propName] instanceof expectedClass)) {
        var expectedClassName = expectedClass.name || ANONYMOUS;
        var actualClassName = getClassName(props[propName]);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createEnumTypeChecker(expectedValues) {
    if (!Array.isArray(expectedValues)) {
      if (true) {
        if (arguments.length > 1) {
          printWarning(
            'Invalid arguments supplied to oneOf, expected an array, got ' + arguments.length + ' arguments. ' +
            'A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z]).'
          );
        } else {
          printWarning('Invalid argument supplied to oneOf, expected an array.');
        }
      }
      return emptyFunctionThatReturnsNull;
    }

    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      for (var i = 0; i < expectedValues.length; i++) {
        if (is(propValue, expectedValues[i])) {
          return null;
        }
      }

      var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
        var type = getPreciseType(value);
        if (type === 'symbol') {
          return String(value);
        }
        return value;
      });
      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of value `' + String(propValue) + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createObjectOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside objectOf.');
      }
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
      }
      for (var key in propValue) {
        if (has(propValue, key)) {
          var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
          if (error instanceof Error) {
            return error;
          }
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createUnionTypeChecker(arrayOfTypeCheckers) {
    if (!Array.isArray(arrayOfTypeCheckers)) {
       true ? printWarning('Invalid argument supplied to oneOfType, expected an instance of array.') : undefined;
      return emptyFunctionThatReturnsNull;
    }

    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
      var checker = arrayOfTypeCheckers[i];
      if (typeof checker !== 'function') {
        printWarning(
          'Invalid argument supplied to oneOfType. Expected an array of check functions, but ' +
          'received ' + getPostfixForTypeWarning(checker) + ' at index ' + i + '.'
        );
        return emptyFunctionThatReturnsNull;
      }
    }

    function validate(props, propName, componentName, location, propFullName) {
      for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
        var checker = arrayOfTypeCheckers[i];
        if (checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret) == null) {
          return null;
        }
      }

      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createNodeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      if (!isNode(props[propName])) {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      for (var key in shapeTypes) {
        var checker = shapeTypes[key];
        if (!checker) {
          continue;
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createStrictShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      // We need to check all keys in case some are required but missing from
      // props.
      var allKeys = assign({}, props[propName], shapeTypes);
      for (var key in allKeys) {
        var checker = shapeTypes[key];
        if (!checker) {
          return new PropTypeError(
            'Invalid ' + location + ' `' + propFullName + '` key `' + key + '` supplied to `' + componentName + '`.' +
            '\nBad object: ' + JSON.stringify(props[propName], null, '  ') +
            '\nValid keys: ' +  JSON.stringify(Object.keys(shapeTypes), null, '  ')
          );
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function isNode(propValue) {
    switch (typeof propValue) {
      case 'number':
      case 'string':
      case 'undefined':
        return true;
      case 'boolean':
        return !propValue;
      case 'object':
        if (Array.isArray(propValue)) {
          return propValue.every(isNode);
        }
        if (propValue === null || isValidElement(propValue)) {
          return true;
        }

        var iteratorFn = getIteratorFn(propValue);
        if (iteratorFn) {
          var iterator = iteratorFn.call(propValue);
          var step;
          if (iteratorFn !== propValue.entries) {
            while (!(step = iterator.next()).done) {
              if (!isNode(step.value)) {
                return false;
              }
            }
          } else {
            // Iterator will provide entry [k,v] tuples rather than values.
            while (!(step = iterator.next()).done) {
              var entry = step.value;
              if (entry) {
                if (!isNode(entry[1])) {
                  return false;
                }
              }
            }
          }
        } else {
          return false;
        }

        return true;
      default:
        return false;
    }
  }

  function isSymbol(propType, propValue) {
    // Native Symbol.
    if (propType === 'symbol') {
      return true;
    }

    // falsy value can't be a Symbol
    if (!propValue) {
      return false;
    }

    // 19.4.3.5 Symbol.prototype[@@toStringTag] === 'Symbol'
    if (propValue['@@toStringTag'] === 'Symbol') {
      return true;
    }

    // Fallback for non-spec compliant Symbols which are polyfilled.
    if (typeof Symbol === 'function' && propValue instanceof Symbol) {
      return true;
    }

    return false;
  }

  // Equivalent of `typeof` but with special handling for array and regexp.
  function getPropType(propValue) {
    var propType = typeof propValue;
    if (Array.isArray(propValue)) {
      return 'array';
    }
    if (propValue instanceof RegExp) {
      // Old webkits (at least until Android 4.0) return 'function' rather than
      // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
      // passes PropTypes.object.
      return 'object';
    }
    if (isSymbol(propType, propValue)) {
      return 'symbol';
    }
    return propType;
  }

  // This handles more types than `getPropType`. Only used for error messages.
  // See `createPrimitiveTypeChecker`.
  function getPreciseType(propValue) {
    if (typeof propValue === 'undefined' || propValue === null) {
      return '' + propValue;
    }
    var propType = getPropType(propValue);
    if (propType === 'object') {
      if (propValue instanceof Date) {
        return 'date';
      } else if (propValue instanceof RegExp) {
        return 'regexp';
      }
    }
    return propType;
  }

  // Returns a string that is postfixed to a warning about an invalid type.
  // For example, "undefined" or "of type array"
  function getPostfixForTypeWarning(value) {
    var type = getPreciseType(value);
    switch (type) {
      case 'array':
      case 'object':
        return 'an ' + type;
      case 'boolean':
      case 'date':
      case 'regexp':
        return 'a ' + type;
      default:
        return type;
    }
  }

  // Returns class name of the object, if any.
  function getClassName(propValue) {
    if (!propValue.constructor || !propValue.constructor.name) {
      return ANONYMOUS;
    }
    return propValue.constructor.name;
  }

  ReactPropTypes.checkPropTypes = checkPropTypes;
  ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
  ReactPropTypes.PropTypes = ReactPropTypes;

  return ReactPropTypes;
};


/***/ }),

/***/ "./node_modules/prop-types/index.js":
/*!******************************************!*\
  !*** ./node_modules/prop-types/index.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

if (true) {
  var ReactIs = __webpack_require__(/*! react-is */ "./node_modules/react-is/index.js");

  // By explicitly using `prop-types` you are opting into new development behavior.
  // http://fb.me/prop-types-in-prod
  var throwOnDirectAccess = true;
  module.exports = __webpack_require__(/*! ./factoryWithTypeCheckers */ "./node_modules/prop-types/factoryWithTypeCheckers.js")(ReactIs.isElement, throwOnDirectAccess);
} else {}


/***/ }),

/***/ "./node_modules/prop-types/lib/ReactPropTypesSecret.js":
/*!*************************************************************!*\
  !*** ./node_modules/prop-types/lib/ReactPropTypesSecret.js ***!
  \*************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';

module.exports = ReactPropTypesSecret;


/***/ }),

/***/ "./node_modules/react-dom/cjs/react-dom.development.js":
/*!*************************************************************!*\
  !*** ./node_modules/react-dom/cjs/react-dom.development.js ***!
  \*************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.13.0
 * react-dom.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (true) {
  (function() {
'use strict';

var React = __webpack_require__(/*! react */ "./node_modules/react/index.js");
var _assign = __webpack_require__(/*! object-assign */ "./node_modules/object-assign/index.js");
var Scheduler = __webpack_require__(/*! scheduler */ "./node_modules/scheduler/index.js");
var checkPropTypes = __webpack_require__(/*! prop-types/checkPropTypes */ "./node_modules/prop-types/checkPropTypes.js");
var tracing = __webpack_require__(/*! scheduler/tracing */ "./node_modules/scheduler/tracing.js");

var ReactSharedInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED; // Prevent newer renderers from RTE when used with older react package versions.
// Current owner and dispatcher used to share the same ref,
// but PR #14548 split them out to better support the react-debug-tools package.

if (!ReactSharedInternals.hasOwnProperty('ReactCurrentDispatcher')) {
  ReactSharedInternals.ReactCurrentDispatcher = {
    current: null
  };
}

if (!ReactSharedInternals.hasOwnProperty('ReactCurrentBatchConfig')) {
  ReactSharedInternals.ReactCurrentBatchConfig = {
    suspense: null
  };
}

// by calls to these methods by a Babel plugin.
//
// In PROD (or in packages without access to React internals),
// they are left as they are instead.

function warn(format) {
  {
    for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    printWarning('warn', format, args);
  }
}
function error(format) {
  {
    for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
      args[_key2 - 1] = arguments[_key2];
    }

    printWarning('error', format, args);
  }
}

function printWarning(level, format, args) {
  // When changing this logic, you might want to also
  // update consoleWithStackDev.www.js as well.
  {
    var hasExistingStack = args.length > 0 && typeof args[args.length - 1] === 'string' && args[args.length - 1].indexOf('\n    in') === 0;

    if (!hasExistingStack) {
      var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
      var stack = ReactDebugCurrentFrame.getStackAddendum();

      if (stack !== '') {
        format += '%s';
        args = args.concat([stack]);
      }
    }

    var argsWithFormat = args.map(function (item) {
      return '' + item;
    }); // Careful: RN currently depends on this prefix

    argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it
    // breaks IE9: https://github.com/facebook/react/issues/13610
    // eslint-disable-next-line react-internal/no-production-logging

    Function.prototype.apply.call(console[level], console, argsWithFormat);

    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      var argIndex = 0;
      var message = 'Warning: ' + format.replace(/%s/g, function () {
        return args[argIndex++];
      });
      throw new Error(message);
    } catch (x) {}
  }
}

if (!React) {
  {
    throw Error( "ReactDOM was loaded before React. Make sure you load the React package before loading ReactDOM." );
  }
}

var invokeGuardedCallbackImpl = function (name, func, context, a, b, c, d, e, f) {
  var funcArgs = Array.prototype.slice.call(arguments, 3);

  try {
    func.apply(context, funcArgs);
  } catch (error) {
    this.onError(error);
  }
};

{
  // In DEV mode, we swap out invokeGuardedCallback for a special version
  // that plays more nicely with the browser's DevTools. The idea is to preserve
  // "Pause on exceptions" behavior. Because React wraps all user-provided
  // functions in invokeGuardedCallback, and the production version of
  // invokeGuardedCallback uses a try-catch, all user exceptions are treated
  // like caught exceptions, and the DevTools won't pause unless the developer
  // takes the extra step of enabling pause on caught exceptions. This is
  // unintuitive, though, because even though React has caught the error, from
  // the developer's perspective, the error is uncaught.
  //
  // To preserve the expected "Pause on exceptions" behavior, we don't use a
  // try-catch in DEV. Instead, we synchronously dispatch a fake event to a fake
  // DOM node, and call the user-provided callback from inside an event handler
  // for that fake event. If the callback throws, the error is "captured" using
  // a global event handler. But because the error happens in a different
  // event loop context, it does not interrupt the normal program flow.
  // Effectively, this gives us try-catch behavior without actually using
  // try-catch. Neat!
  // Check that the browser supports the APIs we need to implement our special
  // DEV version of invokeGuardedCallback
  if (typeof window !== 'undefined' && typeof window.dispatchEvent === 'function' && typeof document !== 'undefined' && typeof document.createEvent === 'function') {
    var fakeNode = document.createElement('react');

    var invokeGuardedCallbackDev = function (name, func, context, a, b, c, d, e, f) {
      // If document doesn't exist we know for sure we will crash in this method
      // when we call document.createEvent(). However this can cause confusing
      // errors: https://github.com/facebookincubator/create-react-app/issues/3482
      // So we preemptively throw with a better message instead.
      if (!(typeof document !== 'undefined')) {
        {
          throw Error( "The `document` global was defined when React was initialized, but is not defined anymore. This can happen in a test environment if a component schedules an update from an asynchronous callback, but the test has already finished running. To solve this, you can either unmount the component at the end of your test (and ensure that any asynchronous operations get canceled in `componentWillUnmount`), or you can change the test itself to be asynchronous." );
        }
      }

      var evt = document.createEvent('Event'); // Keeps track of whether the user-provided callback threw an error. We
      // set this to true at the beginning, then set it to false right after
      // calling the function. If the function errors, `didError` will never be
      // set to false. This strategy works even if the browser is flaky and
      // fails to call our global error handler, because it doesn't rely on
      // the error event at all.

      var didError = true; // Keeps track of the value of window.event so that we can reset it
      // during the callback to let user code access window.event in the
      // browsers that support it.

      var windowEvent = window.event; // Keeps track of the descriptor of window.event to restore it after event
      // dispatching: https://github.com/facebook/react/issues/13688

      var windowEventDescriptor = Object.getOwnPropertyDescriptor(window, 'event'); // Create an event handler for our fake event. We will synchronously
      // dispatch our fake event using `dispatchEvent`. Inside the handler, we
      // call the user-provided callback.

      var funcArgs = Array.prototype.slice.call(arguments, 3);

      function callCallback() {
        // We immediately remove the callback from event listeners so that
        // nested `invokeGuardedCallback` calls do not clash. Otherwise, a
        // nested call would trigger the fake event handlers of any call higher
        // in the stack.
        fakeNode.removeEventListener(evtType, callCallback, false); // We check for window.hasOwnProperty('event') to prevent the
        // window.event assignment in both IE <= 10 as they throw an error
        // "Member not found" in strict mode, and in Firefox which does not
        // support window.event.

        if (typeof window.event !== 'undefined' && window.hasOwnProperty('event')) {
          window.event = windowEvent;
        }

        func.apply(context, funcArgs);
        didError = false;
      } // Create a global error event handler. We use this to capture the value
      // that was thrown. It's possible that this error handler will fire more
      // than once; for example, if non-React code also calls `dispatchEvent`
      // and a handler for that event throws. We should be resilient to most of
      // those cases. Even if our error event handler fires more than once, the
      // last error event is always used. If the callback actually does error,
      // we know that the last error event is the correct one, because it's not
      // possible for anything else to have happened in between our callback
      // erroring and the code that follows the `dispatchEvent` call below. If
      // the callback doesn't error, but the error event was fired, we know to
      // ignore it because `didError` will be false, as described above.


      var error; // Use this to track whether the error event is ever called.

      var didSetError = false;
      var isCrossOriginError = false;

      function handleWindowError(event) {
        error = event.error;
        didSetError = true;

        if (error === null && event.colno === 0 && event.lineno === 0) {
          isCrossOriginError = true;
        }

        if (event.defaultPrevented) {
          // Some other error handler has prevented default.
          // Browsers silence the error report if this happens.
          // We'll remember this to later decide whether to log it or not.
          if (error != null && typeof error === 'object') {
            try {
              error._suppressLogging = true;
            } catch (inner) {// Ignore.
            }
          }
        }
      } // Create a fake event type.


      var evtType = "react-" + (name ? name : 'invokeguardedcallback'); // Attach our event handlers

      window.addEventListener('error', handleWindowError);
      fakeNode.addEventListener(evtType, callCallback, false); // Synchronously dispatch our fake event. If the user-provided function
      // errors, it will trigger our global error handler.

      evt.initEvent(evtType, false, false);
      fakeNode.dispatchEvent(evt);

      if (windowEventDescriptor) {
        Object.defineProperty(window, 'event', windowEventDescriptor);
      }

      if (didError) {
        if (!didSetError) {
          // The callback errored, but the error event never fired.
          error = new Error('An error was thrown inside one of your components, but React ' + "doesn't know what it was. This is likely due to browser " + 'flakiness. React does its best to preserve the "Pause on ' + 'exceptions" behavior of the DevTools, which requires some ' + "DEV-mode only tricks. It's possible that these don't work in " + 'your browser. Try triggering the error in production mode, ' + 'or switching to a modern browser. If you suspect that this is ' + 'actually an issue with React, please file an issue.');
        } else if (isCrossOriginError) {
          error = new Error("A cross-origin error was thrown. React doesn't have access to " + 'the actual error object in development. ' + 'See https://fb.me/react-crossorigin-error for more information.');
        }

        this.onError(error);
      } // Remove our event listeners


      window.removeEventListener('error', handleWindowError);
    };

    invokeGuardedCallbackImpl = invokeGuardedCallbackDev;
  }
}

var invokeGuardedCallbackImpl$1 = invokeGuardedCallbackImpl;

var hasError = false;
var caughtError = null; // Used by event system to capture/rethrow the first error.

var hasRethrowError = false;
var rethrowError = null;
var reporter = {
  onError: function (error) {
    hasError = true;
    caughtError = error;
  }
};
/**
 * Call a function while guarding against errors that happens within it.
 * Returns an error if it throws, otherwise null.
 *
 * In production, this is implemented using a try-catch. The reason we don't
 * use a try-catch directly is so that we can swap out a different
 * implementation in DEV mode.
 *
 * @param {String} name of the guard to use for logging or debugging
 * @param {Function} func The function to invoke
 * @param {*} context The context to use when calling the function
 * @param {...*} args Arguments for function
 */

function invokeGuardedCallback(name, func, context, a, b, c, d, e, f) {
  hasError = false;
  caughtError = null;
  invokeGuardedCallbackImpl$1.apply(reporter, arguments);
}
/**
 * Same as invokeGuardedCallback, but instead of returning an error, it stores
 * it in a global so it can be rethrown by `rethrowCaughtError` later.
 * TODO: See if caughtError and rethrowError can be unified.
 *
 * @param {String} name of the guard to use for logging or debugging
 * @param {Function} func The function to invoke
 * @param {*} context The context to use when calling the function
 * @param {...*} args Arguments for function
 */

function invokeGuardedCallbackAndCatchFirstError(name, func, context, a, b, c, d, e, f) {
  invokeGuardedCallback.apply(this, arguments);

  if (hasError) {
    var error = clearCaughtError();

    if (!hasRethrowError) {
      hasRethrowError = true;
      rethrowError = error;
    }
  }
}
/**
 * During execution of guarded functions we will capture the first error which
 * we will rethrow to be handled by the top level error handler.
 */

function rethrowCaughtError() {
  if (hasRethrowError) {
    var error = rethrowError;
    hasRethrowError = false;
    rethrowError = null;
    throw error;
  }
}
function hasCaughtError() {
  return hasError;
}
function clearCaughtError() {
  if (hasError) {
    var error = caughtError;
    hasError = false;
    caughtError = null;
    return error;
  } else {
    {
      {
        throw Error( "clearCaughtError was called but no error was captured. This error is likely caused by a bug in React. Please file an issue." );
      }
    }
  }
}

var getFiberCurrentPropsFromNode = null;
var getInstanceFromNode = null;
var getNodeFromInstance = null;
function setComponentTree(getFiberCurrentPropsFromNodeImpl, getInstanceFromNodeImpl, getNodeFromInstanceImpl) {
  getFiberCurrentPropsFromNode = getFiberCurrentPropsFromNodeImpl;
  getInstanceFromNode = getInstanceFromNodeImpl;
  getNodeFromInstance = getNodeFromInstanceImpl;

  {
    if (!getNodeFromInstance || !getInstanceFromNode) {
      error('EventPluginUtils.setComponentTree(...): Injected ' + 'module is missing getNodeFromInstance or getInstanceFromNode.');
    }
  }
}
var validateEventDispatches;

{
  validateEventDispatches = function (event) {
    var dispatchListeners = event._dispatchListeners;
    var dispatchInstances = event._dispatchInstances;
    var listenersIsArr = Array.isArray(dispatchListeners);
    var listenersLen = listenersIsArr ? dispatchListeners.length : dispatchListeners ? 1 : 0;
    var instancesIsArr = Array.isArray(dispatchInstances);
    var instancesLen = instancesIsArr ? dispatchInstances.length : dispatchInstances ? 1 : 0;

    if (instancesIsArr !== listenersIsArr || instancesLen !== listenersLen) {
      error('EventPluginUtils: Invalid `event`.');
    }
  };
}
/**
 * Dispatch the event to the listener.
 * @param {SyntheticEvent} event SyntheticEvent to handle
 * @param {function} listener Application-level callback
 * @param {*} inst Internal component instance
 */


function executeDispatch(event, listener, inst) {
  var type = event.type || 'unknown-event';
  event.currentTarget = getNodeFromInstance(inst);
  invokeGuardedCallbackAndCatchFirstError(type, listener, undefined, event);
  event.currentTarget = null;
}
/**
 * Standard/simple iteration through an event's collected dispatches.
 */

function executeDispatchesInOrder(event) {
  var dispatchListeners = event._dispatchListeners;
  var dispatchInstances = event._dispatchInstances;

  {
    validateEventDispatches(event);
  }

  if (Array.isArray(dispatchListeners)) {
    for (var i = 0; i < dispatchListeners.length; i++) {
      if (event.isPropagationStopped()) {
        break;
      } // Listeners and Instances are two parallel arrays that are always in sync.


      executeDispatch(event, dispatchListeners[i], dispatchInstances[i]);
    }
  } else if (dispatchListeners) {
    executeDispatch(event, dispatchListeners, dispatchInstances);
  }

  event._dispatchListeners = null;
  event._dispatchInstances = null;
}

var FunctionComponent = 0;
var ClassComponent = 1;
var IndeterminateComponent = 2; // Before we know whether it is function or class

var HostRoot = 3; // Root of a host tree. Could be nested inside another node.

var HostPortal = 4; // A subtree. Could be an entry point to a different renderer.

var HostComponent = 5;
var HostText = 6;
var Fragment = 7;
var Mode = 8;
var ContextConsumer = 9;
var ContextProvider = 10;
var ForwardRef = 11;
var Profiler = 12;
var SuspenseComponent = 13;
var MemoComponent = 14;
var SimpleMemoComponent = 15;
var LazyComponent = 16;
var IncompleteClassComponent = 17;
var DehydratedFragment = 18;
var SuspenseListComponent = 19;
var FundamentalComponent = 20;
var ScopeComponent = 21;
var Block = 22;

var BEFORE_SLASH_RE = /^(.*)[\\\/]/;
function describeComponentFrame (name, source, ownerName) {
  var sourceInfo = '';

  if (source) {
    var path = source.fileName;
    var fileName = path.replace(BEFORE_SLASH_RE, '');

    {
      // In DEV, include code for a common special case:
      // prefer "folder/index.js" instead of just "index.js".
      if (/^index\./.test(fileName)) {
        var match = path.match(BEFORE_SLASH_RE);

        if (match) {
          var pathBeforeSlash = match[1];

          if (pathBeforeSlash) {
            var folderName = pathBeforeSlash.replace(BEFORE_SLASH_RE, '');
            fileName = folderName + '/' + fileName;
          }
        }
      }
    }

    sourceInfo = ' (at ' + fileName + ':' + source.lineNumber + ')';
  } else if (ownerName) {
    sourceInfo = ' (created by ' + ownerName + ')';
  }

  return '\n    in ' + (name || 'Unknown') + sourceInfo;
}

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol.for;
var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;
var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;
var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;
var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;
var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary
var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;
var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;
var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;
var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;
var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;
var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;
var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for('react.block') : 0xead9;
var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator';
function getIteratorFn(maybeIterable) {
  if (maybeIterable === null || typeof maybeIterable !== 'object') {
    return null;
  }

  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];

  if (typeof maybeIterator === 'function') {
    return maybeIterator;
  }

  return null;
}

var Uninitialized = -1;
var Pending = 0;
var Resolved = 1;
var Rejected = 2;
function refineResolvedLazyComponent(lazyComponent) {
  return lazyComponent._status === Resolved ? lazyComponent._result : null;
}
function initializeLazyComponentType(lazyComponent) {
  if (lazyComponent._status === Uninitialized) {
    lazyComponent._status = Pending;
    var ctor = lazyComponent._ctor;
    var thenable = ctor();
    lazyComponent._result = thenable;
    thenable.then(function (moduleObject) {
      if (lazyComponent._status === Pending) {
        var defaultExport = moduleObject.default;

        {
          if (defaultExport === undefined) {
            error('lazy: Expected the result of a dynamic import() call. ' + 'Instead received: %s\n\nYour code should look like: \n  ' + "const MyComponent = lazy(() => import('./MyComponent'))", moduleObject);
          }
        }

        lazyComponent._status = Resolved;
        lazyComponent._result = defaultExport;
      }
    }, function (error) {
      if (lazyComponent._status === Pending) {
        lazyComponent._status = Rejected;
        lazyComponent._result = error;
      }
    });
  }
}

function getWrappedName(outerType, innerType, wrapperName) {
  var functionName = innerType.displayName || innerType.name || '';
  return outerType.displayName || (functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName);
}

function getComponentName(type) {
  if (type == null) {
    // Host root, text node or just invalid type.
    return null;
  }

  {
    if (typeof type.tag === 'number') {
      error('Received an unexpected object in getComponentName(). ' + 'This is likely a bug in React. Please file an issue.');
    }
  }

  if (typeof type === 'function') {
    return type.displayName || type.name || null;
  }

  if (typeof type === 'string') {
    return type;
  }

  switch (type) {
    case REACT_FRAGMENT_TYPE:
      return 'Fragment';

    case REACT_PORTAL_TYPE:
      return 'Portal';

    case REACT_PROFILER_TYPE:
      return "Profiler";

    case REACT_STRICT_MODE_TYPE:
      return 'StrictMode';

    case REACT_SUSPENSE_TYPE:
      return 'Suspense';

    case REACT_SUSPENSE_LIST_TYPE:
      return 'SuspenseList';
  }

  if (typeof type === 'object') {
    switch (type.$$typeof) {
      case REACT_CONTEXT_TYPE:
        return 'Context.Consumer';

      case REACT_PROVIDER_TYPE:
        return 'Context.Provider';

      case REACT_FORWARD_REF_TYPE:
        return getWrappedName(type, type.render, 'ForwardRef');

      case REACT_MEMO_TYPE:
        return getComponentName(type.type);

      case REACT_BLOCK_TYPE:
        return getComponentName(type.render);

      case REACT_LAZY_TYPE:
        {
          var thenable = type;
          var resolvedThenable = refineResolvedLazyComponent(thenable);

          if (resolvedThenable) {
            return getComponentName(resolvedThenable);
          }

          break;
        }
    }
  }

  return null;
}

var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;

function describeFiber(fiber) {
  switch (fiber.tag) {
    case HostRoot:
    case HostPortal:
    case HostText:
    case Fragment:
    case ContextProvider:
    case ContextConsumer:
      return '';

    default:
      var owner = fiber._debugOwner;
      var source = fiber._debugSource;
      var name = getComponentName(fiber.type);
      var ownerName = null;

      if (owner) {
        ownerName = getComponentName(owner.type);
      }

      return describeComponentFrame(name, source, ownerName);
  }
}

function getStackByFiberInDevAndProd(workInProgress) {
  var info = '';
  var node = workInProgress;

  do {
    info += describeFiber(node);
    node = node.return;
  } while (node);

  return info;
}
var current = null;
var phase = null;
function getCurrentFiberOwnerNameInDevOrNull() {
  {
    if (current === null) {
      return null;
    }

    var owner = current._debugOwner;

    if (owner !== null && typeof owner !== 'undefined') {
      return getComponentName(owner.type);
    }
  }

  return null;
}
function getCurrentFiberStackInDev() {
  {
    if (current === null) {
      return '';
    } // Safe because if current fiber exists, we are reconciling,
    // and it is guaranteed to be the work-in-progress version.


    return getStackByFiberInDevAndProd(current);
  }
}
function resetCurrentFiber() {
  {
    ReactDebugCurrentFrame.getCurrentStack = null;
    current = null;
    phase = null;
  }
}
function setCurrentFiber(fiber) {
  {
    ReactDebugCurrentFrame.getCurrentStack = getCurrentFiberStackInDev;
    current = fiber;
    phase = null;
  }
}
function setCurrentPhase(lifeCyclePhase) {
  {
    phase = lifeCyclePhase;
  }
}

/**
 * Injectable ordering of event plugins.
 */
var eventPluginOrder = null;
/**
 * Injectable mapping from names to event plugin modules.
 */

var namesToPlugins = {};
/**
 * Recomputes the plugin list using the injected plugins and plugin ordering.
 *
 * @private
 */

function recomputePluginOrdering() {
  if (!eventPluginOrder) {
    // Wait until an `eventPluginOrder` is injected.
    return;
  }

  for (var pluginName in namesToPlugins) {
    var pluginModule = namesToPlugins[pluginName];
    var pluginIndex = eventPluginOrder.indexOf(pluginName);

    if (!(pluginIndex > -1)) {
      {
        throw Error( "EventPluginRegistry: Cannot inject event plugins that do not exist in the plugin ordering, `" + pluginName + "`." );
      }
    }

    if (plugins[pluginIndex]) {
      continue;
    }

    if (!pluginModule.extractEvents) {
      {
        throw Error( "EventPluginRegistry: Event plugins must implement an `extractEvents` method, but `" + pluginName + "` does not." );
      }
    }

    plugins[pluginIndex] = pluginModule;
    var publishedEvents = pluginModule.eventTypes;

    for (var eventName in publishedEvents) {
      if (!publishEventForPlugin(publishedEvents[eventName], pluginModule, eventName)) {
        {
          throw Error( "EventPluginRegistry: Failed to publish event `" + eventName + "` for plugin `" + pluginName + "`." );
        }
      }
    }
  }
}
/**
 * Publishes an event so that it can be dispatched by the supplied plugin.
 *
 * @param {object} dispatchConfig Dispatch configuration for the event.
 * @param {object} PluginModule Plugin publishing the event.
 * @return {boolean} True if the event was successfully published.
 * @private
 */


function publishEventForPlugin(dispatchConfig, pluginModule, eventName) {
  if (!!eventNameDispatchConfigs.hasOwnProperty(eventName)) {
    {
      throw Error( "EventPluginRegistry: More than one plugin attempted to publish the same event name, `" + eventName + "`." );
    }
  }

  eventNameDispatchConfigs[eventName] = dispatchConfig;
  var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;

  if (phasedRegistrationNames) {
    for (var phaseName in phasedRegistrationNames) {
      if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
        var phasedRegistrationName = phasedRegistrationNames[phaseName];
        publishRegistrationName(phasedRegistrationName, pluginModule, eventName);
      }
    }

    return true;
  } else if (dispatchConfig.registrationName) {
    publishRegistrationName(dispatchConfig.registrationName, pluginModule, eventName);
    return true;
  }

  return false;
}
/**
 * Publishes a registration name that is used to identify dispatched events.
 *
 * @param {string} registrationName Registration name to add.
 * @param {object} PluginModule Plugin publishing the event.
 * @private
 */


function publishRegistrationName(registrationName, pluginModule, eventName) {
  if (!!registrationNameModules[registrationName]) {
    {
      throw Error( "EventPluginRegistry: More than one plugin attempted to publish the same registration name, `" + registrationName + "`." );
    }
  }

  registrationNameModules[registrationName] = pluginModule;
  registrationNameDependencies[registrationName] = pluginModule.eventTypes[eventName].dependencies;

  {
    var lowerCasedName = registrationName.toLowerCase();
    possibleRegistrationNames[lowerCasedName] = registrationName;

    if (registrationName === 'onDoubleClick') {
      possibleRegistrationNames.ondblclick = registrationName;
    }
  }
}
/**
 * Registers plugins so that they can extract and dispatch events.
 */

/**
 * Ordered list of injected plugins.
 */


var plugins = [];
/**
 * Mapping from event name to dispatch config
 */

var eventNameDispatchConfigs = {};
/**
 * Mapping from registration name to plugin module
 */

var registrationNameModules = {};
/**
 * Mapping from registration name to event name
 */

var registrationNameDependencies = {};
/**
 * Mapping from lowercase registration names to the properly cased version,
 * used to warn in the case of missing event handlers. Available
 * only in true.
 * @type {Object}
 */

var possibleRegistrationNames =  {} ; // Trust the developer to only use possibleRegistrationNames in true

/**
 * Injects an ordering of plugins (by plugin name). This allows the ordering
 * to be decoupled from injection of the actual plugins so that ordering is
 * always deterministic regardless of packaging, on-the-fly injection, etc.
 *
 * @param {array} InjectedEventPluginOrder
 * @internal
 */

function injectEventPluginOrder(injectedEventPluginOrder) {
  if (!!eventPluginOrder) {
    {
      throw Error( "EventPluginRegistry: Cannot inject event plugin ordering more than once. You are likely trying to load more than one copy of React." );
    }
  } // Clone the ordering so it cannot be dynamically mutated.


  eventPluginOrder = Array.prototype.slice.call(injectedEventPluginOrder);
  recomputePluginOrdering();
}
/**
 * Injects plugins to be used by plugin event system. The plugin names must be
 * in the ordering injected by `injectEventPluginOrder`.
 *
 * Plugins can be injected as part of page initialization or on-the-fly.
 *
 * @param {object} injectedNamesToPlugins Map from names to plugin modules.
 * @internal
 */

function injectEventPluginsByName(injectedNamesToPlugins) {
  var isOrderingDirty = false;

  for (var pluginName in injectedNamesToPlugins) {
    if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
      continue;
    }

    var pluginModule = injectedNamesToPlugins[pluginName];

    if (!namesToPlugins.hasOwnProperty(pluginName) || namesToPlugins[pluginName] !== pluginModule) {
      if (!!namesToPlugins[pluginName]) {
        {
          throw Error( "EventPluginRegistry: Cannot inject two different event plugins using the same name, `" + pluginName + "`." );
        }
      }

      namesToPlugins[pluginName] = pluginModule;
      isOrderingDirty = true;
    }
  }

  if (isOrderingDirty) {
    recomputePluginOrdering();
  }
}

var canUseDOM = !!(typeof window !== 'undefined' && typeof window.document !== 'undefined' && typeof window.document.createElement !== 'undefined');

var PLUGIN_EVENT_SYSTEM = 1;
var IS_REPLAYED = 1 << 5;
var IS_FIRST_ANCESTOR = 1 << 6;

var restoreImpl = null;
var restoreTarget = null;
var restoreQueue = null;

function restoreStateOfTarget(target) {
  // We perform this translation at the end of the event loop so that we
  // always receive the correct fiber here
  var internalInstance = getInstanceFromNode(target);

  if (!internalInstance) {
    // Unmounted
    return;
  }

  if (!(typeof restoreImpl === 'function')) {
    {
      throw Error( "setRestoreImplementation() needs to be called to handle a target for controlled events. This error is likely caused by a bug in React. Please file an issue." );
    }
  }

  var stateNode = internalInstance.stateNode; // Guard against Fiber being unmounted.

  if (stateNode) {
    var _props = getFiberCurrentPropsFromNode(stateNode);

    restoreImpl(internalInstance.stateNode, internalInstance.type, _props);
  }
}

function setRestoreImplementation(impl) {
  restoreImpl = impl;
}
function enqueueStateRestore(target) {
  if (restoreTarget) {
    if (restoreQueue) {
      restoreQueue.push(target);
    } else {
      restoreQueue = [target];
    }
  } else {
    restoreTarget = target;
  }
}
function needsStateRestore() {
  return restoreTarget !== null || restoreQueue !== null;
}
function restoreStateIfNeeded() {
  if (!restoreTarget) {
    return;
  }

  var target = restoreTarget;
  var queuedTargets = restoreQueue;
  restoreTarget = null;
  restoreQueue = null;
  restoreStateOfTarget(target);

  if (queuedTargets) {
    for (var i = 0; i < queuedTargets.length; i++) {
      restoreStateOfTarget(queuedTargets[i]);
    }
  }
}

var enableProfilerTimer = true; // Trace which interactions trigger each commit.

var enableDeprecatedFlareAPI = false; // Experimental Host Component support.

var enableFundamentalAPI = false; // Experimental Scope support.
var warnAboutStringRefs = false;

// the renderer. Such as when we're dispatching events or if third party
// libraries need to call batchedUpdates. Eventually, this API will go away when
// everything is batched by default. We'll then have a similar API to opt-out of
// scheduled work and instead do synchronous work.
// Defaults

var batchedUpdatesImpl = function (fn, bookkeeping) {
  return fn(bookkeeping);
};

var discreteUpdatesImpl = function (fn, a, b, c, d) {
  return fn(a, b, c, d);
};

var flushDiscreteUpdatesImpl = function () {};

var batchedEventUpdatesImpl = batchedUpdatesImpl;
var isInsideEventHandler = false;
var isBatchingEventUpdates = false;

function finishEventHandler() {
  // Here we wait until all updates have propagated, which is important
  // when using controlled components within layers:
  // https://github.com/facebook/react/issues/1698
  // Then we restore state of any controlled component.
  var controlledComponentsHavePendingUpdates = needsStateRestore();

  if (controlledComponentsHavePendingUpdates) {
    // If a controlled event was fired, we may need to restore the state of
    // the DOM node back to the controlled value. This is necessary when React
    // bails out of the update without touching the DOM.
    flushDiscreteUpdatesImpl();
    restoreStateIfNeeded();
  }
}

function batchedUpdates(fn, bookkeeping) {
  if (isInsideEventHandler) {
    // If we are currently inside another batch, we need to wait until it
    // fully completes before restoring state.
    return fn(bookkeeping);
  }

  isInsideEventHandler = true;

  try {
    return batchedUpdatesImpl(fn, bookkeeping);
  } finally {
    isInsideEventHandler = false;
    finishEventHandler();
  }
}
function batchedEventUpdates(fn, a, b) {
  if (isBatchingEventUpdates) {
    // If we are currently inside another batch, we need to wait until it
    // fully completes before restoring state.
    return fn(a, b);
  }

  isBatchingEventUpdates = true;

  try {
    return batchedEventUpdatesImpl(fn, a, b);
  } finally {
    isBatchingEventUpdates = false;
    finishEventHandler();
  }
} // This is for the React Flare event system
function discreteUpdates(fn, a, b, c, d) {
  var prevIsInsideEventHandler = isInsideEventHandler;
  isInsideEventHandler = true;

  try {
    return discreteUpdatesImpl(fn, a, b, c, d);
  } finally {
    isInsideEventHandler = prevIsInsideEventHandler;

    if (!isInsideEventHandler) {
      finishEventHandler();
    }
  }
}
function flushDiscreteUpdatesIfNeeded(timeStamp) {
  // event.timeStamp isn't overly reliable due to inconsistencies in
  // how different browsers have historically provided the time stamp.
  // Some browsers provide high-resolution time stamps for all events,
  // some provide low-resolution time stamps for all events. FF < 52
  // even mixes both time stamps together. Some browsers even report
  // negative time stamps or time stamps that are 0 (iOS9) in some cases.
  // Given we are only comparing two time stamps with equality (!==),
  // we are safe from the resolution differences. If the time stamp is 0
  // we bail-out of preventing the flush, which can affect semantics,
  // such as if an earlier flush removes or adds event listeners that
  // are fired in the subsequent flush. However, this is the same
  // behaviour as we had before this change, so the risks are low.
  if (!isInsideEventHandler && (!enableDeprecatedFlareAPI  )) {
    flushDiscreteUpdatesImpl();
  }
}
function setBatchingImplementation(_batchedUpdatesImpl, _discreteUpdatesImpl, _flushDiscreteUpdatesImpl, _batchedEventUpdatesImpl) {
  batchedUpdatesImpl = _batchedUpdatesImpl;
  discreteUpdatesImpl = _discreteUpdatesImpl;
  flushDiscreteUpdatesImpl = _flushDiscreteUpdatesImpl;
  batchedEventUpdatesImpl = _batchedEventUpdatesImpl;
}

var DiscreteEvent = 0;
var UserBlockingEvent = 1;
var ContinuousEvent = 2;

// A reserved attribute.
// It is handled by React separately and shouldn't be written to the DOM.
var RESERVED = 0; // A simple string attribute.
// Attributes that aren't in the whitelist are presumed to have this type.

var STRING = 1; // A string attribute that accepts booleans in React. In HTML, these are called
// "enumerated" attributes with "true" and "false" as possible values.
// When true, it should be set to a "true" string.
// When false, it should be set to a "false" string.

var BOOLEANISH_STRING = 2; // A real boolean attribute.
// When true, it should be present (set either to an empty string or its name).
// When false, it should be omitted.

var BOOLEAN = 3; // An attribute that can be used as a flag as well as with a value.
// When true, it should be present (set either to an empty string or its name).
// When false, it should be omitted.
// For any other value, should be present with that value.

var OVERLOADED_BOOLEAN = 4; // An attribute that must be numeric or parse as a numeric.
// When falsy, it should be removed.

var NUMERIC = 5; // An attribute that must be positive numeric or parse as a positive numeric.
// When falsy, it should be removed.

var POSITIVE_NUMERIC = 6;

/* eslint-disable max-len */
var ATTRIBUTE_NAME_START_CHAR = ":A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD";
/* eslint-enable max-len */

var ATTRIBUTE_NAME_CHAR = ATTRIBUTE_NAME_START_CHAR + "\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040";
var ROOT_ATTRIBUTE_NAME = 'data-reactroot';
var VALID_ATTRIBUTE_NAME_REGEX = new RegExp('^[' + ATTRIBUTE_NAME_START_CHAR + '][' + ATTRIBUTE_NAME_CHAR + ']*$');
var hasOwnProperty = Object.prototype.hasOwnProperty;
var illegalAttributeNameCache = {};
var validatedAttributeNameCache = {};
function isAttributeNameSafe(attributeName) {
  if (hasOwnProperty.call(validatedAttributeNameCache, attributeName)) {
    return true;
  }

  if (hasOwnProperty.call(illegalAttributeNameCache, attributeName)) {
    return false;
  }

  if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
    validatedAttributeNameCache[attributeName] = true;
    return true;
  }

  illegalAttributeNameCache[attributeName] = true;

  {
    error('Invalid attribute name: `%s`', attributeName);
  }

  return false;
}
function shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag) {
  if (propertyInfo !== null) {
    return propertyInfo.type === RESERVED;
  }

  if (isCustomComponentTag) {
    return false;
  }

  if (name.length > 2 && (name[0] === 'o' || name[0] === 'O') && (name[1] === 'n' || name[1] === 'N')) {
    return true;
  }

  return false;
}
function shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag) {
  if (propertyInfo !== null && propertyInfo.type === RESERVED) {
    return false;
  }

  switch (typeof value) {
    case 'function': // $FlowIssue symbol is perfectly valid here

    case 'symbol':
      // eslint-disable-line
      return true;

    case 'boolean':
      {
        if (isCustomComponentTag) {
          return false;
        }

        if (propertyInfo !== null) {
          return !propertyInfo.acceptsBooleans;
        } else {
          var prefix = name.toLowerCase().slice(0, 5);
          return prefix !== 'data-' && prefix !== 'aria-';
        }
      }

    default:
      return false;
  }
}
function shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag) {
  if (value === null || typeof value === 'undefined') {
    return true;
  }

  if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, isCustomComponentTag)) {
    return true;
  }

  if (isCustomComponentTag) {
    return false;
  }

  if (propertyInfo !== null) {
    switch (propertyInfo.type) {
      case BOOLEAN:
        return !value;

      case OVERLOADED_BOOLEAN:
        return value === false;

      case NUMERIC:
        return isNaN(value);

      case POSITIVE_NUMERIC:
        return isNaN(value) || value < 1;
    }
  }

  return false;
}
function getPropertyInfo(name) {
  return properties.hasOwnProperty(name) ? properties[name] : null;
}

function PropertyInfoRecord(name, type, mustUseProperty, attributeName, attributeNamespace, sanitizeURL) {
  this.acceptsBooleans = type === BOOLEANISH_STRING || type === BOOLEAN || type === OVERLOADED_BOOLEAN;
  this.attributeName = attributeName;
  this.attributeNamespace = attributeNamespace;
  this.mustUseProperty = mustUseProperty;
  this.propertyName = name;
  this.type = type;
  this.sanitizeURL = sanitizeURL;
} // When adding attributes to this list, be sure to also add them to
// the `possibleStandardNames` module to ensure casing and incorrect
// name warnings.


var properties = {}; // These props are reserved by React. They shouldn't be written to the DOM.

var reservedProps = ['children', 'dangerouslySetInnerHTML', // TODO: This prevents the assignment of defaultValue to regular
// elements (not just inputs). Now that ReactDOMInput assigns to the
// defaultValue property -- do we need this?
'defaultValue', 'defaultChecked', 'innerHTML', 'suppressContentEditableWarning', 'suppressHydrationWarning', 'style'];

reservedProps.forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, RESERVED, false, // mustUseProperty
  name, // attributeName
  null, // attributeNamespace
  false);
}); // A few React string attributes have a different name.
// This is a mapping from React prop names to the attribute names.

[['acceptCharset', 'accept-charset'], ['className', 'class'], ['htmlFor', 'for'], ['httpEquiv', 'http-equiv']].forEach(function (_ref) {
  var name = _ref[0],
      attributeName = _ref[1];
  properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
  attributeName, // attributeName
  null, // attributeNamespace
  false);
}); // These are "enumerated" HTML attributes that accept "true" and "false".
// In React, we let users pass `true` and `false` even though technically
// these aren't boolean attributes (they are coerced to strings).

['contentEditable', 'draggable', 'spellCheck', 'value'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, // mustUseProperty
  name.toLowerCase(), // attributeName
  null, // attributeNamespace
  false);
}); // These are "enumerated" SVG attributes that accept "true" and "false".
// In React, we let users pass `true` and `false` even though technically
// these aren't boolean attributes (they are coerced to strings).
// Since these are SVG attributes, their attribute names are case-sensitive.

['autoReverse', 'externalResourcesRequired', 'focusable', 'preserveAlpha'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, BOOLEANISH_STRING, false, // mustUseProperty
  name, // attributeName
  null, // attributeNamespace
  false);
}); // These are HTML boolean attributes.

['allowFullScreen', 'async', // Note: there is a special case that prevents it from being written to the DOM
// on the client side because the browsers are inconsistent. Instead we call focus().
'autoFocus', 'autoPlay', 'controls', 'default', 'defer', 'disabled', 'disablePictureInPicture', 'formNoValidate', 'hidden', 'loop', 'noModule', 'noValidate', 'open', 'playsInline', 'readOnly', 'required', 'reversed', 'scoped', 'seamless', // Microdata
'itemScope'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, BOOLEAN, false, // mustUseProperty
  name.toLowerCase(), // attributeName
  null, // attributeNamespace
  false);
}); // These are the few React props that we set as DOM properties
// rather than attributes. These are all booleans.

['checked', // Note: `option.selected` is not updated if `select.multiple` is
// disabled with `removeAttribute`. We have special logic for handling this.
'multiple', 'muted', 'selected' // NOTE: if you add a camelCased prop to this list,
// you'll need to set attributeName to name.toLowerCase()
// instead in the assignment below.
].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, BOOLEAN, true, // mustUseProperty
  name, // attributeName
  null, // attributeNamespace
  false);
}); // These are HTML attributes that are "overloaded booleans": they behave like
// booleans, but can also accept a string value.

['capture', 'download' // NOTE: if you add a camelCased prop to this list,
// you'll need to set attributeName to name.toLowerCase()
// instead in the assignment below.
].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, OVERLOADED_BOOLEAN, false, // mustUseProperty
  name, // attributeName
  null, // attributeNamespace
  false);
}); // These are HTML attributes that must be positive numbers.

['cols', 'rows', 'size', 'span' // NOTE: if you add a camelCased prop to this list,
// you'll need to set attributeName to name.toLowerCase()
// instead in the assignment below.
].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, POSITIVE_NUMERIC, false, // mustUseProperty
  name, // attributeName
  null, // attributeNamespace
  false);
}); // These are HTML attributes that must be numbers.

['rowSpan', 'start'].forEach(function (name) {
  properties[name] = new PropertyInfoRecord(name, NUMERIC, false, // mustUseProperty
  name.toLowerCase(), // attributeName
  null, // attributeNamespace
  false);
});
var CAMELIZE = /[\-\:]([a-z])/g;

var capitalize = function (token) {
  return token[1].toUpperCase();
}; // This is a list of all SVG attributes that need special casing, namespacing,
// or boolean value assignment. Regular attributes that just accept strings
// and have the same names are omitted, just like in the HTML whitelist.
// Some of these attributes can be hard to find. This list was created by
// scraping the MDN documentation.


['accent-height', 'alignment-baseline', 'arabic-form', 'baseline-shift', 'cap-height', 'clip-path', 'clip-rule', 'color-interpolation', 'color-interpolation-filters', 'color-profile', 'color-rendering', 'dominant-baseline', 'enable-background', 'fill-opacity', 'fill-rule', 'flood-color', 'flood-opacity', 'font-family', 'font-size', 'font-size-adjust', 'font-stretch', 'font-style', 'font-variant', 'font-weight', 'glyph-name', 'glyph-orientation-horizontal', 'glyph-orientation-vertical', 'horiz-adv-x', 'horiz-origin-x', 'image-rendering', 'letter-spacing', 'lighting-color', 'marker-end', 'marker-mid', 'marker-start', 'overline-position', 'overline-thickness', 'paint-order', 'panose-1', 'pointer-events', 'rendering-intent', 'shape-rendering', 'stop-color', 'stop-opacity', 'strikethrough-position', 'strikethrough-thickness', 'stroke-dasharray', 'stroke-dashoffset', 'stroke-linecap', 'stroke-linejoin', 'stroke-miterlimit', 'stroke-opacity', 'stroke-width', 'text-anchor', 'text-decoration', 'text-rendering', 'underline-position', 'underline-thickness', 'unicode-bidi', 'unicode-range', 'units-per-em', 'v-alphabetic', 'v-hanging', 'v-ideographic', 'v-mathematical', 'vector-effect', 'vert-adv-y', 'vert-origin-x', 'vert-origin-y', 'word-spacing', 'writing-mode', 'xmlns:xlink', 'x-height' // NOTE: if you add a camelCased prop to this list,
// you'll need to set attributeName to name.toLowerCase()
// instead in the assignment below.
].forEach(function (attributeName) {
  var name = attributeName.replace(CAMELIZE, capitalize);
  properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
  attributeName, null, // attributeNamespace
  false);
}); // String SVG attributes with the xlink namespace.

['xlink:actuate', 'xlink:arcrole', 'xlink:role', 'xlink:show', 'xlink:title', 'xlink:type' // NOTE: if you add a camelCased prop to this list,
// you'll need to set attributeName to name.toLowerCase()
// instead in the assignment below.
].forEach(function (attributeName) {
  var name = attributeName.replace(CAMELIZE, capitalize);
  properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
  attributeName, 'http://www.w3.org/1999/xlink', false);
}); // String SVG attributes with the xml namespace.

['xml:base', 'xml:lang', 'xml:space' // NOTE: if you add a camelCased prop to this list,
// you'll need to set attributeName to name.toLowerCase()
// instead in the assignment below.
].forEach(function (attributeName) {
  var name = attributeName.replace(CAMELIZE, capitalize);
  properties[name] = new PropertyInfoRecord(name, STRING, false, // mustUseProperty
  attributeName, 'http://www.w3.org/XML/1998/namespace', false);
}); // These attribute exists both in HTML and SVG.
// The attribute name is case-sensitive in SVG so we can't just use
// the React name like we do for attributes that exist only in HTML.

['tabIndex', 'crossOrigin'].forEach(function (attributeName) {
  properties[attributeName] = new PropertyInfoRecord(attributeName, STRING, false, // mustUseProperty
  attributeName.toLowerCase(), // attributeName
  null, // attributeNamespace
  false);
}); // These attributes accept URLs. These must not allow javascript: URLS.
// These will also need to accept Trusted Types object in the future.

var xlinkHref = 'xlinkHref';
properties[xlinkHref] = new PropertyInfoRecord('xlinkHref', STRING, false, // mustUseProperty
'xlink:href', 'http://www.w3.org/1999/xlink', true);
['src', 'href', 'action', 'formAction'].forEach(function (attributeName) {
  properties[attributeName] = new PropertyInfoRecord(attributeName, STRING, false, // mustUseProperty
  attributeName.toLowerCase(), // attributeName
  null, // attributeNamespace
  true);
});

var ReactDebugCurrentFrame$1 = null;

{
  ReactDebugCurrentFrame$1 = ReactSharedInternals.ReactDebugCurrentFrame;
} // A javascript: URL can contain leading C0 control or \u0020 SPACE,
// and any newline or tab are filtered out as if they're not part of the URL.
// https://url.spec.whatwg.org/#url-parsing
// Tab or newline are defined as \r\n\t:
// https://infra.spec.whatwg.org/#ascii-tab-or-newline
// A C0 control is a code point in the range \u0000 NULL to \u001F
// INFORMATION SEPARATOR ONE, inclusive:
// https://infra.spec.whatwg.org/#c0-control-or-space

/* eslint-disable max-len */


var isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*\:/i;
var didWarn = false;

function sanitizeURL(url) {
  {
    if (!didWarn && isJavaScriptProtocol.test(url)) {
      didWarn = true;

      error('A future version of React will block javascript: URLs as a security precaution. ' + 'Use event handlers instead if you can. If you need to generate unsafe HTML try ' + 'using dangerouslySetInnerHTML instead. React was passed %s.', JSON.stringify(url));
    }
  }
}

/**
 * Get the value for a property on a node. Only used in DEV for SSR validation.
 * The "expected" argument is used as a hint of what the expected value is.
 * Some properties have multiple equivalent values.
 */
function getValueForProperty(node, name, expected, propertyInfo) {
  {
    if (propertyInfo.mustUseProperty) {
      var propertyName = propertyInfo.propertyName;
      return node[propertyName];
    } else {
      if ( propertyInfo.sanitizeURL) {
        // If we haven't fully disabled javascript: URLs, and if
        // the hydration is successful of a javascript: URL, we
        // still want to warn on the client.
        sanitizeURL('' + expected);
      }

      var attributeName = propertyInfo.attributeName;
      var stringValue = null;

      if (propertyInfo.type === OVERLOADED_BOOLEAN) {
        if (node.hasAttribute(attributeName)) {
          var value = node.getAttribute(attributeName);

          if (value === '') {
            return true;
          }

          if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
            return value;
          }

          if (value === '' + expected) {
            return expected;
          }

          return value;
        }
      } else if (node.hasAttribute(attributeName)) {
        if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
          // We had an attribute but shouldn't have had one, so read it
          // for the error message.
          return node.getAttribute(attributeName);
        }

        if (propertyInfo.type === BOOLEAN) {
          // If this was a boolean, it doesn't matter what the value is
          // the fact that we have it is the same as the expected.
          return expected;
        } // Even if this property uses a namespace we use getAttribute
        // because we assume its namespaced name is the same as our config.
        // To use getAttributeNS we need the local name which we don't have
        // in our config atm.


        stringValue = node.getAttribute(attributeName);
      }

      if (shouldRemoveAttribute(name, expected, propertyInfo, false)) {
        return stringValue === null ? expected : stringValue;
      } else if (stringValue === '' + expected) {
        return expected;
      } else {
        return stringValue;
      }
    }
  }
}
/**
 * Get the value for a attribute on a node. Only used in DEV for SSR validation.
 * The third argument is used as a hint of what the expected value is. Some
 * attributes have multiple equivalent values.
 */

function getValueForAttribute(node, name, expected) {
  {
    if (!isAttributeNameSafe(name)) {
      return;
    }

    if (!node.hasAttribute(name)) {
      return expected === undefined ? undefined : null;
    }

    var value = node.getAttribute(name);

    if (value === '' + expected) {
      return expected;
    }

    return value;
  }
}
/**
 * Sets the value for a property on a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 * @param {*} value
 */

function setValueForProperty(node, name, value, isCustomComponentTag) {
  var propertyInfo = getPropertyInfo(name);

  if (shouldIgnoreAttribute(name, propertyInfo, isCustomComponentTag)) {
    return;
  }

  if (shouldRemoveAttribute(name, value, propertyInfo, isCustomComponentTag)) {
    value = null;
  } // If the prop isn't in the special list, treat it as a simple attribute.


  if (isCustomComponentTag || propertyInfo === null) {
    if (isAttributeNameSafe(name)) {
      var _attributeName = name;

      if (value === null) {
        node.removeAttribute(_attributeName);
      } else {
        node.setAttribute(_attributeName,  '' + value);
      }
    }

    return;
  }

  var mustUseProperty = propertyInfo.mustUseProperty;

  if (mustUseProperty) {
    var propertyName = propertyInfo.propertyName;

    if (value === null) {
      var type = propertyInfo.type;
      node[propertyName] = type === BOOLEAN ? false : '';
    } else {
      // Contrary to `setAttribute`, object properties are properly
      // `toString`ed by IE8/9.
      node[propertyName] = value;
    }

    return;
  } // The rest are treated as attributes with special cases.


  var attributeName = propertyInfo.attributeName,
      attributeNamespace = propertyInfo.attributeNamespace;

  if (value === null) {
    node.removeAttribute(attributeName);
  } else {
    var _type = propertyInfo.type;
    var attributeValue;

    if (_type === BOOLEAN || _type === OVERLOADED_BOOLEAN && value === true) {
      // If attribute type is boolean, we know for sure it won't be an execution sink
      // and we won't require Trusted Type here.
      attributeValue = '';
    } else {
      // `setAttribute` with objects becomes only `[object]` in IE8/9,
      // ('' + value) makes it output the correct toString()-value.
      {
        attributeValue = '' + value;
      }

      if (propertyInfo.sanitizeURL) {
        sanitizeURL(attributeValue.toString());
      }
    }

    if (attributeNamespace) {
      node.setAttributeNS(attributeNamespace, attributeName, attributeValue);
    } else {
      node.setAttribute(attributeName, attributeValue);
    }
  }
}

// Flow does not allow string concatenation of most non-string types. To work
// around this limitation, we use an opaque type that can only be obtained by
// passing the value through getToStringValue first.
function toString(value) {
  return '' + value;
}
function getToStringValue(value) {
  switch (typeof value) {
    case 'boolean':
    case 'number':
    case 'object':
    case 'string':
    case 'undefined':
      return value;

    default:
      // function, symbol are assigned as empty strings
      return '';
  }
}

var ReactDebugCurrentFrame$2 = null;
var ReactControlledValuePropTypes = {
  checkPropTypes: null
};

{
  ReactDebugCurrentFrame$2 = ReactSharedInternals.ReactDebugCurrentFrame;
  var hasReadOnlyValue = {
    button: true,
    checkbox: true,
    image: true,
    hidden: true,
    radio: true,
    reset: true,
    submit: true
  };
  var propTypes = {
    value: function (props, propName, componentName) {
      if (hasReadOnlyValue[props.type] || props.onChange || props.readOnly || props.disabled || props[propName] == null || enableDeprecatedFlareAPI ) {
        return null;
      }

      return new Error('You provided a `value` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultValue`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
    },
    checked: function (props, propName, componentName) {
      if (props.onChange || props.readOnly || props.disabled || props[propName] == null || enableDeprecatedFlareAPI ) {
        return null;
      }

      return new Error('You provided a `checked` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultChecked`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
    }
  };
  /**
   * Provide a linked `value` attribute for controlled forms. You should not use
   * this outside of the ReactDOM controlled form components.
   */

  ReactControlledValuePropTypes.checkPropTypes = function (tagName, props) {
    checkPropTypes(propTypes, props, 'prop', tagName, ReactDebugCurrentFrame$2.getStackAddendum);
  };
}

function isCheckable(elem) {
  var type = elem.type;
  var nodeName = elem.nodeName;
  return nodeName && nodeName.toLowerCase() === 'input' && (type === 'checkbox' || type === 'radio');
}

function getTracker(node) {
  return node._valueTracker;
}

function detachTracker(node) {
  node._valueTracker = null;
}

function getValueFromNode(node) {
  var value = '';

  if (!node) {
    return value;
  }

  if (isCheckable(node)) {
    value = node.checked ? 'true' : 'false';
  } else {
    value = node.value;
  }

  return value;
}

function trackValueOnNode(node) {
  var valueField = isCheckable(node) ? 'checked' : 'value';
  var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);
  var currentValue = '' + node[valueField]; // if someone has already defined a value or Safari, then bail
  // and don't track value will cause over reporting of changes,
  // but it's better then a hard failure
  // (needed for certain tests that spyOn input values and Safari)

  if (node.hasOwnProperty(valueField) || typeof descriptor === 'undefined' || typeof descriptor.get !== 'function' || typeof descriptor.set !== 'function') {
    return;
  }

  var get = descriptor.get,
      set = descriptor.set;
  Object.defineProperty(node, valueField, {
    configurable: true,
    get: function () {
      return get.call(this);
    },
    set: function (value) {
      currentValue = '' + value;
      set.call(this, value);
    }
  }); // We could've passed this the first time
  // but it triggers a bug in IE11 and Edge 14/15.
  // Calling defineProperty() again should be equivalent.
  // https://github.com/facebook/react/issues/11768

  Object.defineProperty(node, valueField, {
    enumerable: descriptor.enumerable
  });
  var tracker = {
    getValue: function () {
      return currentValue;
    },
    setValue: function (value) {
      currentValue = '' + value;
    },
    stopTracking: function () {
      detachTracker(node);
      delete node[valueField];
    }
  };
  return tracker;
}

function track(node) {
  if (getTracker(node)) {
    return;
  } // TODO: Once it's just Fiber we can move this to node._wrapperState


  node._valueTracker = trackValueOnNode(node);
}
function updateValueIfChanged(node) {
  if (!node) {
    return false;
  }

  var tracker = getTracker(node); // if there is no tracker at this point it's unlikely
  // that trying again will succeed

  if (!tracker) {
    return true;
  }

  var lastValue = tracker.getValue();
  var nextValue = getValueFromNode(node);

  if (nextValue !== lastValue) {
    tracker.setValue(nextValue);
    return true;
  }

  return false;
}

var didWarnValueDefaultValue = false;
var didWarnCheckedDefaultChecked = false;
var didWarnControlledToUncontrolled = false;
var didWarnUncontrolledToControlled = false;

function isControlled(props) {
  var usesChecked = props.type === 'checkbox' || props.type === 'radio';
  return usesChecked ? props.checked != null : props.value != null;
}
/**
 * Implements an <input> host component that allows setting these optional
 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
 *
 * If `checked` or `value` are not supplied (or null/undefined), user actions
 * that affect the checked state or value will trigger updates to the element.
 *
 * If they are supplied (and not null/undefined), the rendered element will not
 * trigger updates to the element. Instead, the props must change in order for
 * the rendered element to be updated.
 *
 * The rendered element will be initialized as unchecked (or `defaultChecked`)
 * with an empty value (or `defaultValue`).
 *
 * See http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
 */


function getHostProps(element, props) {
  var node = element;
  var checked = props.checked;

  var hostProps = _assign({}, props, {
    defaultChecked: undefined,
    defaultValue: undefined,
    value: undefined,
    checked: checked != null ? checked : node._wrapperState.initialChecked
  });

  return hostProps;
}
function initWrapperState(element, props) {
  {
    ReactControlledValuePropTypes.checkPropTypes('input', props);

    if (props.checked !== undefined && props.defaultChecked !== undefined && !didWarnCheckedDefaultChecked) {
      error('%s contains an input of type %s with both checked and defaultChecked props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the checked prop, or the defaultChecked prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerNameInDevOrNull() || 'A component', props.type);

      didWarnCheckedDefaultChecked = true;
    }

    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue) {
      error('%s contains an input of type %s with both value and defaultValue props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerNameInDevOrNull() || 'A component', props.type);

      didWarnValueDefaultValue = true;
    }
  }

  var node = element;
  var defaultValue = props.defaultValue == null ? '' : props.defaultValue;
  node._wrapperState = {
    initialChecked: props.checked != null ? props.checked : props.defaultChecked,
    initialValue: getToStringValue(props.value != null ? props.value : defaultValue),
    controlled: isControlled(props)
  };
}
function updateChecked(element, props) {
  var node = element;
  var checked = props.checked;

  if (checked != null) {
    setValueForProperty(node, 'checked', checked, false);
  }
}
function updateWrapper(element, props) {
  var node = element;

  {
    var controlled = isControlled(props);

    if (!node._wrapperState.controlled && controlled && !didWarnUncontrolledToControlled) {
      error('A component is changing an uncontrolled input of type %s to be controlled. ' + 'Input elements should not switch from uncontrolled to controlled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components', props.type);

      didWarnUncontrolledToControlled = true;
    }

    if (node._wrapperState.controlled && !controlled && !didWarnControlledToUncontrolled) {
      error('A component is changing a controlled input of type %s to be uncontrolled. ' + 'Input elements should not switch from controlled to uncontrolled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components', props.type);

      didWarnControlledToUncontrolled = true;
    }
  }

  updateChecked(element, props);
  var value = getToStringValue(props.value);
  var type = props.type;

  if (value != null) {
    if (type === 'number') {
      if (value === 0 && node.value === '' || // We explicitly want to coerce to number here if possible.
      // eslint-disable-next-line
      node.value != value) {
        node.value = toString(value);
      }
    } else if (node.value !== toString(value)) {
      node.value = toString(value);
    }
  } else if (type === 'submit' || type === 'reset') {
    // Submit/reset inputs need the attribute removed completely to avoid
    // blank-text buttons.
    node.removeAttribute('value');
    return;
  }

  {
    // When syncing the value attribute, the value comes from a cascade of
    // properties:
    //  1. The value React property
    //  2. The defaultValue React property
    //  3. Otherwise there should be no change
    if (props.hasOwnProperty('value')) {
      setDefaultValue(node, props.type, value);
    } else if (props.hasOwnProperty('defaultValue')) {
      setDefaultValue(node, props.type, getToStringValue(props.defaultValue));
    }
  }

  {
    // When syncing the checked attribute, it only changes when it needs
    // to be removed, such as transitioning from a checkbox into a text input
    if (props.checked == null && props.defaultChecked != null) {
      node.defaultChecked = !!props.defaultChecked;
    }
  }
}
function postMountWrapper(element, props, isHydrating) {
  var node = element; // Do not assign value if it is already set. This prevents user text input
  // from being lost during SSR hydration.

  if (props.hasOwnProperty('value') || props.hasOwnProperty('defaultValue')) {
    var type = props.type;
    var isButton = type === 'submit' || type === 'reset'; // Avoid setting value attribute on submit/reset inputs as it overrides the
    // default value provided by the browser. See: #12872

    if (isButton && (props.value === undefined || props.value === null)) {
      return;
    }

    var initialValue = toString(node._wrapperState.initialValue); // Do not assign value if it is already set. This prevents user text input
    // from being lost during SSR hydration.

    if (!isHydrating) {
      {
        // When syncing the value attribute, the value property should use
        // the wrapperState._initialValue property. This uses:
        //
        //   1. The value React property when present
        //   2. The defaultValue React property when present
        //   3. An empty string
        if (initialValue !== node.value) {
          node.value = initialValue;
        }
      }
    }

    {
      // Otherwise, the value attribute is synchronized to the property,
      // so we assign defaultValue to the same thing as the value property
      // assignment step above.
      node.defaultValue = initialValue;
    }
  } // Normally, we'd just do `node.checked = node.checked` upon initial mount, less this bug
  // this is needed to work around a chrome bug where setting defaultChecked
  // will sometimes influence the value of checked (even after detachment).
  // Reference: https://bugs.chromium.org/p/chromium/issues/detail?id=608416
  // We need to temporarily unset name to avoid disrupting radio button groups.


  var name = node.name;

  if (name !== '') {
    node.name = '';
  }

  {
    // When syncing the checked attribute, both the checked property and
    // attribute are assigned at the same time using defaultChecked. This uses:
    //
    //   1. The checked React property when present
    //   2. The defaultChecked React property when present
    //   3. Otherwise, false
    node.defaultChecked = !node.defaultChecked;
    node.defaultChecked = !!node._wrapperState.initialChecked;
  }

  if (name !== '') {
    node.name = name;
  }
}
function restoreControlledState(element, props) {
  var node = element;
  updateWrapper(node, props);
  updateNamedCousins(node, props);
}

function updateNamedCousins(rootNode, props) {
  var name = props.name;

  if (props.type === 'radio' && name != null) {
    var queryRoot = rootNode;

    while (queryRoot.parentNode) {
      queryRoot = queryRoot.parentNode;
    } // If `rootNode.form` was non-null, then we could try `form.elements`,
    // but that sometimes behaves strangely in IE8. We could also try using
    // `form.getElementsByName`, but that will only return direct children
    // and won't include inputs that use the HTML5 `form=` attribute. Since
    // the input might not even be in a form. It might not even be in the
    // document. Let's just use the local `querySelectorAll` to ensure we don't
    // miss anything.


    var group = queryRoot.querySelectorAll('input[name=' + JSON.stringify('' + name) + '][type="radio"]');

    for (var i = 0; i < group.length; i++) {
      var otherNode = group[i];

      if (otherNode === rootNode || otherNode.form !== rootNode.form) {
        continue;
      } // This will throw if radio buttons rendered by different copies of React
      // and the same name are rendered into the same form (same as #1939).
      // That's probably okay; we don't support it just as we don't support
      // mixing React radio buttons with non-React ones.


      var otherProps = getFiberCurrentPropsFromNode$1(otherNode);

      if (!otherProps) {
        {
          throw Error( "ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported." );
        }
      } // We need update the tracked value on the named cousin since the value
      // was changed but the input saw no event or value set


      updateValueIfChanged(otherNode); // If this is a controlled radio button group, forcing the input that
      // was previously checked to update will cause it to be come re-checked
      // as appropriate.

      updateWrapper(otherNode, otherProps);
    }
  }
} // In Chrome, assigning defaultValue to certain input types triggers input validation.
// For number inputs, the display value loses trailing decimal points. For email inputs,
// Chrome raises "The specified value <x> is not a valid email address".
//
// Here we check to see if the defaultValue has actually changed, avoiding these problems
// when the user is inputting text
//
// https://github.com/facebook/react/issues/7253


function setDefaultValue(node, type, value) {
  if ( // Focused number inputs synchronize on blur. See ChangeEventPlugin.js
  type !== 'number' || node.ownerDocument.activeElement !== node) {
    if (value == null) {
      node.defaultValue = toString(node._wrapperState.initialValue);
    } else if (node.defaultValue !== toString(value)) {
      node.defaultValue = toString(value);
    }
  }
}

var didWarnSelectedSetOnOption = false;
var didWarnInvalidChild = false;

function flattenChildren(children) {
  var content = ''; // Flatten children. We'll warn if they are invalid
  // during validateProps() which runs for hydration too.
  // Note that this would throw on non-element objects.
  // Elements are stringified (which is normally irrelevant
  // but matters for <fbt>).

  React.Children.forEach(children, function (child) {
    if (child == null) {
      return;
    }

    content += child; // Note: we don't warn about invalid children here.
    // Instead, this is done separately below so that
    // it happens during the hydration codepath too.
  });
  return content;
}
/**
 * Implements an <option> host component that warns when `selected` is set.
 */


function validateProps(element, props) {
  {
    // This mirrors the codepath above, but runs for hydration too.
    // Warn about invalid children here so that client and hydration are consistent.
    // TODO: this seems like it could cause a DEV-only throw for hydration
    // if children contains a non-element object. We should try to avoid that.
    if (typeof props.children === 'object' && props.children !== null) {
      React.Children.forEach(props.children, function (child) {
        if (child == null) {
          return;
        }

        if (typeof child === 'string' || typeof child === 'number') {
          return;
        }

        if (typeof child.type !== 'string') {
          return;
        }

        if (!didWarnInvalidChild) {
          didWarnInvalidChild = true;

          error('Only strings and numbers are supported as <option> children.');
        }
      });
    } // TODO: Remove support for `selected` in <option>.


    if (props.selected != null && !didWarnSelectedSetOnOption) {
      error('Use the `defaultValue` or `value` props on <select> instead of ' + 'setting `selected` on <option>.');

      didWarnSelectedSetOnOption = true;
    }
  }
}
function postMountWrapper$1(element, props) {
  // value="" should make a value attribute (#6219)
  if (props.value != null) {
    element.setAttribute('value', toString(getToStringValue(props.value)));
  }
}
function getHostProps$1(element, props) {
  var hostProps = _assign({
    children: undefined
  }, props);

  var content = flattenChildren(props.children);

  if (content) {
    hostProps.children = content;
  }

  return hostProps;
}

var didWarnValueDefaultValue$1;

{
  didWarnValueDefaultValue$1 = false;
}

function getDeclarationErrorAddendum() {
  var ownerName = getCurrentFiberOwnerNameInDevOrNull();

  if (ownerName) {
    return '\n\nCheck the render method of `' + ownerName + '`.';
  }

  return '';
}

var valuePropNames = ['value', 'defaultValue'];
/**
 * Validation function for `value` and `defaultValue`.
 */

function checkSelectPropTypes(props) {
  {
    ReactControlledValuePropTypes.checkPropTypes('select', props);

    for (var i = 0; i < valuePropNames.length; i++) {
      var propName = valuePropNames[i];

      if (props[propName] == null) {
        continue;
      }

      var isArray = Array.isArray(props[propName]);

      if (props.multiple && !isArray) {
        error('The `%s` prop supplied to <select> must be an array if ' + '`multiple` is true.%s', propName, getDeclarationErrorAddendum());
      } else if (!props.multiple && isArray) {
        error('The `%s` prop supplied to <select> must be a scalar ' + 'value if `multiple` is false.%s', propName, getDeclarationErrorAddendum());
      }
    }
  }
}

function updateOptions(node, multiple, propValue, setDefaultSelected) {
  var options = node.options;

  if (multiple) {
    var selectedValues = propValue;
    var selectedValue = {};

    for (var i = 0; i < selectedValues.length; i++) {
      // Prefix to avoid chaos with special keys.
      selectedValue['$' + selectedValues[i]] = true;
    }

    for (var _i = 0; _i < options.length; _i++) {
      var selected = selectedValue.hasOwnProperty('$' + options[_i].value);

      if (options[_i].selected !== selected) {
        options[_i].selected = selected;
      }

      if (selected && setDefaultSelected) {
        options[_i].defaultSelected = true;
      }
    }
  } else {
    // Do not set `select.value` as exact behavior isn't consistent across all
    // browsers for all cases.
    var _selectedValue = toString(getToStringValue(propValue));

    var defaultSelected = null;

    for (var _i2 = 0; _i2 < options.length; _i2++) {
      if (options[_i2].value === _selectedValue) {
        options[_i2].selected = true;

        if (setDefaultSelected) {
          options[_i2].defaultSelected = true;
        }

        return;
      }

      if (defaultSelected === null && !options[_i2].disabled) {
        defaultSelected = options[_i2];
      }
    }

    if (defaultSelected !== null) {
      defaultSelected.selected = true;
    }
  }
}
/**
 * Implements a <select> host component that allows optionally setting the
 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
 * stringable. If `multiple` is true, the prop must be an array of stringables.
 *
 * If `value` is not supplied (or null/undefined), user actions that change the
 * selected option will trigger updates to the rendered options.
 *
 * If it is supplied (and not null/undefined), the rendered options will not
 * update in response to user actions. Instead, the `value` prop must change in
 * order for the rendered options to update.
 *
 * If `defaultValue` is provided, any options with the supplied values will be
 * selected.
 */


function getHostProps$2(element, props) {
  return _assign({}, props, {
    value: undefined
  });
}
function initWrapperState$1(element, props) {
  var node = element;

  {
    checkSelectPropTypes(props);
  }

  node._wrapperState = {
    wasMultiple: !!props.multiple
  };

  {
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue$1) {
      error('Select elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled select ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components');

      didWarnValueDefaultValue$1 = true;
    }
  }
}
function postMountWrapper$2(element, props) {
  var node = element;
  node.multiple = !!props.multiple;
  var value = props.value;

  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  } else if (props.defaultValue != null) {
    updateOptions(node, !!props.multiple, props.defaultValue, true);
  }
}
function postUpdateWrapper(element, props) {
  var node = element;
  var wasMultiple = node._wrapperState.wasMultiple;
  node._wrapperState.wasMultiple = !!props.multiple;
  var value = props.value;

  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  } else if (wasMultiple !== !!props.multiple) {
    // For simplicity, reapply `defaultValue` if `multiple` is toggled.
    if (props.defaultValue != null) {
      updateOptions(node, !!props.multiple, props.defaultValue, true);
    } else {
      // Revert the select back to its default unselected state.
      updateOptions(node, !!props.multiple, props.multiple ? [] : '', false);
    }
  }
}
function restoreControlledState$1(element, props) {
  var node = element;
  var value = props.value;

  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  }
}

var didWarnValDefaultVal = false;

/**
 * Implements a <textarea> host component that allows setting `value`, and
 * `defaultValue`. This differs from the traditional DOM API because value is
 * usually set as PCDATA children.
 *
 * If `value` is not supplied (or null/undefined), user actions that affect the
 * value will trigger updates to the element.
 *
 * If `value` is supplied (and not null/undefined), the rendered element will
 * not trigger updates to the element. Instead, the `value` prop must change in
 * order for the rendered element to be updated.
 *
 * The rendered element will be initialized with an empty value, the prop
 * `defaultValue` if specified, or the children content (deprecated).
 */
function getHostProps$3(element, props) {
  var node = element;

  if (!(props.dangerouslySetInnerHTML == null)) {
    {
      throw Error( "`dangerouslySetInnerHTML` does not make sense on <textarea>." );
    }
  } // Always set children to the same thing. In IE9, the selection range will
  // get reset if `textContent` is mutated.  We could add a check in setTextContent
  // to only set the value if/when the value differs from the node value (which would
  // completely solve this IE9 bug), but Sebastian+Sophie seemed to like this
  // solution. The value can be a boolean or object so that's why it's forced
  // to be a string.


  var hostProps = _assign({}, props, {
    value: undefined,
    defaultValue: undefined,
    children: toString(node._wrapperState.initialValue)
  });

  return hostProps;
}
function initWrapperState$2(element, props) {
  var node = element;

  {
    ReactControlledValuePropTypes.checkPropTypes('textarea', props);

    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValDefaultVal) {
      error('%s contains a textarea with both value and defaultValue props. ' + 'Textarea elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled textarea ' + 'and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerNameInDevOrNull() || 'A component');

      didWarnValDefaultVal = true;
    }
  }

  var initialValue = props.value; // Only bother fetching default value if we're going to use it

  if (initialValue == null) {
    var children = props.children,
        defaultValue = props.defaultValue;

    if (children != null) {
      {
        error('Use the `defaultValue` or `value` props instead of setting ' + 'children on <textarea>.');
      }

      {
        if (!(defaultValue == null)) {
          {
            throw Error( "If you supply `defaultValue` on a <textarea>, do not pass children." );
          }
        }

        if (Array.isArray(children)) {
          if (!(children.length <= 1)) {
            {
              throw Error( "<textarea> can only have at most one child." );
            }
          }

          children = children[0];
        }

        defaultValue = children;
      }
    }

    if (defaultValue == null) {
      defaultValue = '';
    }

    initialValue = defaultValue;
  }

  node._wrapperState = {
    initialValue: getToStringValue(initialValue)
  };
}
function updateWrapper$1(element, props) {
  var node = element;
  var value = getToStringValue(props.value);
  var defaultValue = getToStringValue(props.defaultValue);

  if (value != null) {
    // Cast `value` to a string to ensure the value is set correctly. While
    // browsers typically do this as necessary, jsdom doesn't.
    var newValue = toString(value); // To avoid side effects (such as losing text selection), only set value if changed

    if (newValue !== node.value) {
      node.value = newValue;
    }

    if (props.defaultValue == null && node.defaultValue !== newValue) {
      node.defaultValue = newValue;
    }
  }

  if (defaultValue != null) {
    node.defaultValue = toString(defaultValue);
  }
}
function postMountWrapper$3(element, props) {
  var node = element; // This is in postMount because we need access to the DOM node, which is not
  // available until after the component has mounted.

  var textContent = node.textContent; // Only set node.value if textContent is equal to the expected
  // initial value. In IE10/IE11 there is a bug where the placeholder attribute
  // will populate textContent as well.
  // https://developer.microsoft.com/microsoft-edge/platform/issues/101525/

  if (textContent === node._wrapperState.initialValue) {
    if (textContent !== '' && textContent !== null) {
      node.value = textContent;
    }
  }
}
function restoreControlledState$2(element, props) {
  // DOM component is still mounted; update
  updateWrapper$1(element, props);
}

var HTML_NAMESPACE = 'http://www.w3.org/1999/xhtml';
var MATH_NAMESPACE = 'http://www.w3.org/1998/Math/MathML';
var SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
var Namespaces = {
  html: HTML_NAMESPACE,
  mathml: MATH_NAMESPACE,
  svg: SVG_NAMESPACE
}; // Assumes there is no parent namespace.

function getIntrinsicNamespace(type) {
  switch (type) {
    case 'svg':
      return SVG_NAMESPACE;

    case 'math':
      return MATH_NAMESPACE;

    default:
      return HTML_NAMESPACE;
  }
}
function getChildNamespace(parentNamespace, type) {
  if (parentNamespace == null || parentNamespace === HTML_NAMESPACE) {
    // No (or default) parent namespace: potential entry point.
    return getIntrinsicNamespace(type);
  }

  if (parentNamespace === SVG_NAMESPACE && type === 'foreignObject') {
    // We're leaving SVG.
    return HTML_NAMESPACE;
  } // By default, pass namespace below.


  return parentNamespace;
}

/* globals MSApp */

/**
 * Create a function which has 'unsafe' privileges (required by windows8 apps)
 */
var createMicrosoftUnsafeLocalFunction = function (func) {
  if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) {
    return function (arg0, arg1, arg2, arg3) {
      MSApp.execUnsafeLocalFunction(function () {
        return func(arg0, arg1, arg2, arg3);
      });
    };
  } else {
    return func;
  }
};

var reusableSVGContainer;
/**
 * Set the innerHTML property of a node
 *
 * @param {DOMElement} node
 * @param {string} html
 * @internal
 */

var setInnerHTML = createMicrosoftUnsafeLocalFunction(function (node, html) {
  if (node.namespaceURI === Namespaces.svg) {

    if (!('innerHTML' in node)) {
      // IE does not have innerHTML for SVG nodes, so instead we inject the
      // new markup in a temp node and then move the child nodes across into
      // the target node
      reusableSVGContainer = reusableSVGContainer || document.createElement('div');
      reusableSVGContainer.innerHTML = '<svg>' + html.valueOf().toString() + '</svg>';
      var svgNode = reusableSVGContainer.firstChild;

      while (node.firstChild) {
        node.removeChild(node.firstChild);
      }

      while (svgNode.firstChild) {
        node.appendChild(svgNode.firstChild);
      }

      return;
    }
  }

  node.innerHTML = html;
});

/**
 * HTML nodeType values that represent the type of the node
 */
var ELEMENT_NODE = 1;
var TEXT_NODE = 3;
var COMMENT_NODE = 8;
var DOCUMENT_NODE = 9;
var DOCUMENT_FRAGMENT_NODE = 11;

/**
 * Set the textContent property of a node. For text updates, it's faster
 * to set the `nodeValue` of the Text node directly instead of using
 * `.textContent` which will remove the existing node and create a new one.
 *
 * @param {DOMElement} node
 * @param {string} text
 * @internal
 */

var setTextContent = function (node, text) {
  if (text) {
    var firstChild = node.firstChild;

    if (firstChild && firstChild === node.lastChild && firstChild.nodeType === TEXT_NODE) {
      firstChild.nodeValue = text;
      return;
    }
  }

  node.textContent = text;
};

// Do not use the below two methods directly!
// Instead use constants exported from DOMTopLevelEventTypes in ReactDOM.
// (It is the only module that is allowed to access these methods.)
function unsafeCastStringToDOMTopLevelType(topLevelType) {
  return topLevelType;
}
function unsafeCastDOMTopLevelTypeToString(topLevelType) {
  return topLevelType;
}

/**
 * Generate a mapping of standard vendor prefixes using the defined style property and event name.
 *
 * @param {string} styleProp
 * @param {string} eventName
 * @returns {object}
 */

function makePrefixMap(styleProp, eventName) {
  var prefixes = {};
  prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
  prefixes['Webkit' + styleProp] = 'webkit' + eventName;
  prefixes['Moz' + styleProp] = 'moz' + eventName;
  return prefixes;
}
/**
 * A list of event names to a configurable list of vendor prefixes.
 */


var vendorPrefixes = {
  animationend: makePrefixMap('Animation', 'AnimationEnd'),
  animationiteration: makePrefixMap('Animation', 'AnimationIteration'),
  animationstart: makePrefixMap('Animation', 'AnimationStart'),
  transitionend: makePrefixMap('Transition', 'TransitionEnd')
};
/**
 * Event names that have already been detected and prefixed (if applicable).
 */

var prefixedEventNames = {};
/**
 * Element to check for prefixes on.
 */

var style = {};
/**
 * Bootstrap if a DOM exists.
 */

if (canUseDOM) {
  style = document.createElement('div').style; // On some platforms, in particular some releases of Android 4.x,
  // the un-prefixed "animation" and "transition" properties are defined on the
  // style object but the events that fire will still be prefixed, so we need
  // to check if the un-prefixed events are usable, and if not remove them from the map.

  if (!('AnimationEvent' in window)) {
    delete vendorPrefixes.animationend.animation;
    delete vendorPrefixes.animationiteration.animation;
    delete vendorPrefixes.animationstart.animation;
  } // Same as above


  if (!('TransitionEvent' in window)) {
    delete vendorPrefixes.transitionend.transition;
  }
}
/**
 * Attempts to determine the correct vendor prefixed event name.
 *
 * @param {string} eventName
 * @returns {string}
 */


function getVendorPrefixedEventName(eventName) {
  if (prefixedEventNames[eventName]) {
    return prefixedEventNames[eventName];
  } else if (!vendorPrefixes[eventName]) {
    return eventName;
  }

  var prefixMap = vendorPrefixes[eventName];

  for (var styleProp in prefixMap) {
    if (prefixMap.hasOwnProperty(styleProp) && styleProp in style) {
      return prefixedEventNames[eventName] = prefixMap[styleProp];
    }
  }

  return eventName;
}

/**
 * To identify top level events in ReactDOM, we use constants defined by this
 * module. This is the only module that uses the unsafe* methods to express
 * that the constants actually correspond to the browser event names. This lets
 * us save some bundle size by avoiding a top level type -> event name map.
 * The rest of ReactDOM code should import top level types from this file.
 */

var TOP_ABORT = unsafeCastStringToDOMTopLevelType('abort');
var TOP_ANIMATION_END = unsafeCastStringToDOMTopLevelType(getVendorPrefixedEventName('animationend'));
var TOP_ANIMATION_ITERATION = unsafeCastStringToDOMTopLevelType(getVendorPrefixedEventName('animationiteration'));
var TOP_ANIMATION_START = unsafeCastStringToDOMTopLevelType(getVendorPrefixedEventName('animationstart'));
var TOP_BLUR = unsafeCastStringToDOMTopLevelType('blur');
var TOP_CAN_PLAY = unsafeCastStringToDOMTopLevelType('canplay');
var TOP_CAN_PLAY_THROUGH = unsafeCastStringToDOMTopLevelType('canplaythrough');
var TOP_CANCEL = unsafeCastStringToDOMTopLevelType('cancel');
var TOP_CHANGE = unsafeCastStringToDOMTopLevelType('change');
var TOP_CLICK = unsafeCastStringToDOMTopLevelType('click');
var TOP_CLOSE = unsafeCastStringToDOMTopLevelType('close');
var TOP_COMPOSITION_END = unsafeCastStringToDOMTopLevelType('compositionend');
var TOP_COMPOSITION_START = unsafeCastStringToDOMTopLevelType('compositionstart');
var TOP_COMPOSITION_UPDATE = unsafeCastStringToDOMTopLevelType('compositionupdate');
var TOP_CONTEXT_MENU = unsafeCastStringToDOMTopLevelType('contextmenu');
var TOP_COPY = unsafeCastStringToDOMTopLevelType('copy');
var TOP_CUT = unsafeCastStringToDOMTopLevelType('cut');
var TOP_DOUBLE_CLICK = unsafeCastStringToDOMTopLevelType('dblclick');
var TOP_AUX_CLICK = unsafeCastStringToDOMTopLevelType('auxclick');
var TOP_DRAG = unsafeCastStringToDOMTopLevelType('drag');
var TOP_DRAG_END = unsafeCastStringToDOMTopLevelType('dragend');
var TOP_DRAG_ENTER = unsafeCastStringToDOMTopLevelType('dragenter');
var TOP_DRAG_EXIT = unsafeCastStringToDOMTopLevelType('dragexit');
var TOP_DRAG_LEAVE = unsafeCastStringToDOMTopLevelType('dragleave');
var TOP_DRAG_OVER = unsafeCastStringToDOMTopLevelType('dragover');
var TOP_DRAG_START = unsafeCastStringToDOMTopLevelType('dragstart');
var TOP_DROP = unsafeCastStringToDOMTopLevelType('drop');
var TOP_DURATION_CHANGE = unsafeCastStringToDOMTopLevelType('durationchange');
var TOP_EMPTIED = unsafeCastStringToDOMTopLevelType('emptied');
var TOP_ENCRYPTED = unsafeCastStringToDOMTopLevelType('encrypted');
var TOP_ENDED = unsafeCastStringToDOMTopLevelType('ended');
var TOP_ERROR = unsafeCastStringToDOMTopLevelType('error');
var TOP_FOCUS = unsafeCastStringToDOMTopLevelType('focus');
var TOP_GOT_POINTER_CAPTURE = unsafeCastStringToDOMTopLevelType('gotpointercapture');
var TOP_INPUT = unsafeCastStringToDOMTopLevelType('input');
var TOP_INVALID = unsafeCastStringToDOMTopLevelType('invalid');
var TOP_KEY_DOWN = unsafeCastStringToDOMTopLevelType('keydown');
var TOP_KEY_PRESS = unsafeCastStringToDOMTopLevelType('keypress');
var TOP_KEY_UP = unsafeCastStringToDOMTopLevelType('keyup');
var TOP_LOAD = unsafeCastStringToDOMTopLevelType('load');
var TOP_LOAD_START = unsafeCastStringToDOMTopLevelType('loadstart');
var TOP_LOADED_DATA = unsafeCastStringToDOMTopLevelType('loadeddata');
var TOP_LOADED_METADATA = unsafeCastStringToDOMTopLevelType('loadedmetadata');
var TOP_LOST_POINTER_CAPTURE = unsafeCastStringToDOMTopLevelType('lostpointercapture');
var TOP_MOUSE_DOWN = unsafeCastStringToDOMTopLevelType('mousedown');
var TOP_MOUSE_MOVE = unsafeCastStringToDOMTopLevelType('mousemove');
var TOP_MOUSE_OUT = unsafeCastStringToDOMTopLevelType('mouseout');
var TOP_MOUSE_OVER = unsafeCastStringToDOMTopLevelType('mouseover');
var TOP_MOUSE_UP = unsafeCastStringToDOMTopLevelType('mouseup');
var TOP_PASTE = unsafeCastStringToDOMTopLevelType('paste');
var TOP_PAUSE = unsafeCastStringToDOMTopLevelType('pause');
var TOP_PLAY = unsafeCastStringToDOMTopLevelType('play');
var TOP_PLAYING = unsafeCastStringToDOMTopLevelType('playing');
var TOP_POINTER_CANCEL = unsafeCastStringToDOMTopLevelType('pointercancel');
var TOP_POINTER_DOWN = unsafeCastStringToDOMTopLevelType('pointerdown');
var TOP_POINTER_MOVE = unsafeCastStringToDOMTopLevelType('pointermove');
var TOP_POINTER_OUT = unsafeCastStringToDOMTopLevelType('pointerout');
var TOP_POINTER_OVER = unsafeCastStringToDOMTopLevelType('pointerover');
var TOP_POINTER_UP = unsafeCastStringToDOMTopLevelType('pointerup');
var TOP_PROGRESS = unsafeCastStringToDOMTopLevelType('progress');
var TOP_RATE_CHANGE = unsafeCastStringToDOMTopLevelType('ratechange');
var TOP_RESET = unsafeCastStringToDOMTopLevelType('reset');
var TOP_SCROLL = unsafeCastStringToDOMTopLevelType('scroll');
var TOP_SEEKED = unsafeCastStringToDOMTopLevelType('seeked');
var TOP_SEEKING = unsafeCastStringToDOMTopLevelType('seeking');
var TOP_SELECTION_CHANGE = unsafeCastStringToDOMTopLevelType('selectionchange');
var TOP_STALLED = unsafeCastStringToDOMTopLevelType('stalled');
var TOP_SUBMIT = unsafeCastStringToDOMTopLevelType('submit');
var TOP_SUSPEND = unsafeCastStringToDOMTopLevelType('suspend');
var TOP_TEXT_INPUT = unsafeCastStringToDOMTopLevelType('textInput');
var TOP_TIME_UPDATE = unsafeCastStringToDOMTopLevelType('timeupdate');
var TOP_TOGGLE = unsafeCastStringToDOMTopLevelType('toggle');
var TOP_TOUCH_CANCEL = unsafeCastStringToDOMTopLevelType('touchcancel');
var TOP_TOUCH_END = unsafeCastStringToDOMTopLevelType('touchend');
var TOP_TOUCH_MOVE = unsafeCastStringToDOMTopLevelType('touchmove');
var TOP_TOUCH_START = unsafeCastStringToDOMTopLevelType('touchstart');
var TOP_TRANSITION_END = unsafeCastStringToDOMTopLevelType(getVendorPrefixedEventName('transitionend'));
var TOP_VOLUME_CHANGE = unsafeCastStringToDOMTopLevelType('volumechange');
var TOP_WAITING = unsafeCastStringToDOMTopLevelType('waiting');
var TOP_WHEEL = unsafeCastStringToDOMTopLevelType('wheel'); // List of events that need to be individually attached to media elements.
// Note that events in this list will *not* be listened to at the top level
// unless they're explicitly whitelisted in `ReactBrowserEventEmitter.listenTo`.

var mediaEventTypes = [TOP_ABORT, TOP_CAN_PLAY, TOP_CAN_PLAY_THROUGH, TOP_DURATION_CHANGE, TOP_EMPTIED, TOP_ENCRYPTED, TOP_ENDED, TOP_ERROR, TOP_LOADED_DATA, TOP_LOADED_METADATA, TOP_LOAD_START, TOP_PAUSE, TOP_PLAY, TOP_PLAYING, TOP_PROGRESS, TOP_RATE_CHANGE, TOP_SEEKED, TOP_SEEKING, TOP_STALLED, TOP_SUSPEND, TOP_TIME_UPDATE, TOP_VOLUME_CHANGE, TOP_WAITING];
function getRawEventName(topLevelType) {
  return unsafeCastDOMTopLevelTypeToString(topLevelType);
}

var PossiblyWeakMap = typeof WeakMap === 'function' ? WeakMap : Map; // prettier-ignore

var elementListenerMap = new PossiblyWeakMap();
function getListenerMapForElement(element) {
  var listenerMap = elementListenerMap.get(element);

  if (listenerMap === undefined) {
    listenerMap = new Map();
    elementListenerMap.set(element, listenerMap);
  }

  return listenerMap;
}

/**
 * `ReactInstanceMap` maintains a mapping from a public facing stateful
 * instance (key) and the internal representation (value). This allows public
 * methods to accept the user facing instance as an argument and map them back
 * to internal methods.
 *
 * Note that this module is currently shared and assumed to be stateless.
 * If this becomes an actual Map, that will break.
 */
function get(key) {
  return key._reactInternalFiber;
}
function has(key) {
  return key._reactInternalFiber !== undefined;
}
function set(key, value) {
  key._reactInternalFiber = value;
}

// Don't change these two values. They're used by React Dev Tools.
var NoEffect =
/*              */
0;
var PerformedWork =
/*         */
1; // You can change the rest (and add more).

var Placement =
/*             */
2;
var Update =
/*                */
4;
var PlacementAndUpdate =
/*    */
6;
var Deletion =
/*              */
8;
var ContentReset =
/*          */
16;
var Callback =
/*              */
32;
var DidCapture =
/*            */
64;
var Ref =
/*                   */
128;
var Snapshot =
/*              */
256;
var Passive =
/*               */
512;
var Hydrating =
/*             */
1024;
var HydratingAndUpdate =
/*    */
1028; // Passive & Update & Callback & Ref & Snapshot

var LifecycleEffectMask =
/*   */
932; // Union of all host effects

var HostEffectMask =
/*        */
2047;
var Incomplete =
/*            */
2048;
var ShouldCapture =
/*         */
4096;

var ReactCurrentOwner = ReactSharedInternals.ReactCurrentOwner;
function getNearestMountedFiber(fiber) {
  var node = fiber;
  var nearestMounted = fiber;

  if (!fiber.alternate) {
    // If there is no alternate, this might be a new tree that isn't inserted
    // yet. If it is, then it will have a pending insertion effect on it.
    var nextNode = node;

    do {
      node = nextNode;

      if ((node.effectTag & (Placement | Hydrating)) !== NoEffect) {
        // This is an insertion or in-progress hydration. The nearest possible
        // mounted fiber is the parent but we need to continue to figure out
        // if that one is still mounted.
        nearestMounted = node.return;
      }

      nextNode = node.return;
    } while (nextNode);
  } else {
    while (node.return) {
      node = node.return;
    }
  }

  if (node.tag === HostRoot) {
    // TODO: Check if this was a nested HostRoot when used with
    // renderContainerIntoSubtree.
    return nearestMounted;
  } // If we didn't hit the root, that means that we're in an disconnected tree
  // that has been unmounted.


  return null;
}
function getSuspenseInstanceFromFiber(fiber) {
  if (fiber.tag === SuspenseComponent) {
    var suspenseState = fiber.memoizedState;

    if (suspenseState === null) {
      var current = fiber.alternate;

      if (current !== null) {
        suspenseState = current.memoizedState;
      }
    }

    if (suspenseState !== null) {
      return suspenseState.dehydrated;
    }
  }

  return null;
}
function getContainerFromFiber(fiber) {
  return fiber.tag === HostRoot ? fiber.stateNode.containerInfo : null;
}
function isFiberMounted(fiber) {
  return getNearestMountedFiber(fiber) === fiber;
}
function isMounted(component) {
  {
    var owner = ReactCurrentOwner.current;

    if (owner !== null && owner.tag === ClassComponent) {
      var ownerFiber = owner;
      var instance = ownerFiber.stateNode;

      if (!instance._warnedAboutRefsInRender) {
        error('%s is accessing isMounted inside its render() function. ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(ownerFiber.type) || 'A component');
      }

      instance._warnedAboutRefsInRender = true;
    }
  }

  var fiber = get(component);

  if (!fiber) {
    return false;
  }

  return getNearestMountedFiber(fiber) === fiber;
}

function assertIsMounted(fiber) {
  if (!(getNearestMountedFiber(fiber) === fiber)) {
    {
      throw Error( "Unable to find node on an unmounted component." );
    }
  }
}

function findCurrentFiberUsingSlowPath(fiber) {
  var alternate = fiber.alternate;

  if (!alternate) {
    // If there is no alternate, then we only need to check if it is mounted.
    var nearestMounted = getNearestMountedFiber(fiber);

    if (!(nearestMounted !== null)) {
      {
        throw Error( "Unable to find node on an unmounted component." );
      }
    }

    if (nearestMounted !== fiber) {
      return null;
    }

    return fiber;
  } // If we have two possible branches, we'll walk backwards up to the root
  // to see what path the root points to. On the way we may hit one of the
  // special cases and we'll deal with them.


  var a = fiber;
  var b = alternate;

  while (true) {
    var parentA = a.return;

    if (parentA === null) {
      // We're at the root.
      break;
    }

    var parentB = parentA.alternate;

    if (parentB === null) {
      // There is no alternate. This is an unusual case. Currently, it only
      // happens when a Suspense component is hidden. An extra fragment fiber
      // is inserted in between the Suspense fiber and its children. Skip
      // over this extra fragment fiber and proceed to the next parent.
      var nextParent = parentA.return;

      if (nextParent !== null) {
        a = b = nextParent;
        continue;
      } // If there's no parent, we're at the root.


      break;
    } // If both copies of the parent fiber point to the same child, we can
    // assume that the child is current. This happens when we bailout on low
    // priority: the bailed out fiber's child reuses the current child.


    if (parentA.child === parentB.child) {
      var child = parentA.child;

      while (child) {
        if (child === a) {
          // We've determined that A is the current branch.
          assertIsMounted(parentA);
          return fiber;
        }

        if (child === b) {
          // We've determined that B is the current branch.
          assertIsMounted(parentA);
          return alternate;
        }

        child = child.sibling;
      } // We should never have an alternate for any mounting node. So the only
      // way this could possibly happen is if this was unmounted, if at all.


      {
        {
          throw Error( "Unable to find node on an unmounted component." );
        }
      }
    }

    if (a.return !== b.return) {
      // The return pointer of A and the return pointer of B point to different
      // fibers. We assume that return pointers never criss-cross, so A must
      // belong to the child set of A.return, and B must belong to the child
      // set of B.return.
      a = parentA;
      b = parentB;
    } else {
      // The return pointers point to the same fiber. We'll have to use the
      // default, slow path: scan the child sets of each parent alternate to see
      // which child belongs to which set.
      //
      // Search parent A's child set
      var didFindChild = false;
      var _child = parentA.child;

      while (_child) {
        if (_child === a) {
          didFindChild = true;
          a = parentA;
          b = parentB;
          break;
        }

        if (_child === b) {
          didFindChild = true;
          b = parentA;
          a = parentB;
          break;
        }

        _child = _child.sibling;
      }

      if (!didFindChild) {
        // Search parent B's child set
        _child = parentB.child;

        while (_child) {
          if (_child === a) {
            didFindChild = true;
            a = parentB;
            b = parentA;
            break;
          }

          if (_child === b) {
            didFindChild = true;
            b = parentB;
            a = parentA;
            break;
          }

          _child = _child.sibling;
        }

        if (!didFindChild) {
          {
            throw Error( "Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue." );
          }
        }
      }
    }

    if (!(a.alternate === b)) {
      {
        throw Error( "Return fibers should always be each others' alternates. This error is likely caused by a bug in React. Please file an issue." );
      }
    }
  } // If the root is not a host container, we're in a disconnected tree. I.e.
  // unmounted.


  if (!(a.tag === HostRoot)) {
    {
      throw Error( "Unable to find node on an unmounted component." );
    }
  }

  if (a.stateNode.current === a) {
    // We've determined that A is the current branch.
    return fiber;
  } // Otherwise B has to be current branch.


  return alternate;
}
function findCurrentHostFiber(parent) {
  var currentParent = findCurrentFiberUsingSlowPath(parent);

  if (!currentParent) {
    return null;
  } // Next we'll drill down this component to find the first HostComponent/Text.


  var node = currentParent;

  while (true) {
    if (node.tag === HostComponent || node.tag === HostText) {
      return node;
    } else if (node.child) {
      node.child.return = node;
      node = node.child;
      continue;
    }

    if (node === currentParent) {
      return null;
    }

    while (!node.sibling) {
      if (!node.return || node.return === currentParent) {
        return null;
      }

      node = node.return;
    }

    node.sibling.return = node.return;
    node = node.sibling;
  } // Flow needs the return null here, but ESLint complains about it.
  // eslint-disable-next-line no-unreachable


  return null;
}
function findCurrentHostFiberWithNoPortals(parent) {
  var currentParent = findCurrentFiberUsingSlowPath(parent);

  if (!currentParent) {
    return null;
  } // Next we'll drill down this component to find the first HostComponent/Text.


  var node = currentParent;

  while (true) {
    if (node.tag === HostComponent || node.tag === HostText || enableFundamentalAPI ) {
      return node;
    } else if (node.child && node.tag !== HostPortal) {
      node.child.return = node;
      node = node.child;
      continue;
    }

    if (node === currentParent) {
      return null;
    }

    while (!node.sibling) {
      if (!node.return || node.return === currentParent) {
        return null;
      }

      node = node.return;
    }

    node.sibling.return = node.return;
    node = node.sibling;
  } // Flow needs the return null here, but ESLint complains about it.
  // eslint-disable-next-line no-unreachable


  return null;
}

/**
 * Accumulates items that must not be null or undefined into the first one. This
 * is used to conserve memory by avoiding array allocations, and thus sacrifices
 * API cleanness. Since `current` can be null before being passed in and not
 * null after this function, make sure to assign it back to `current`:
 *
 * `a = accumulateInto(a, b);`
 *
 * This API should be sparingly used. Try `accumulate` for something cleaner.
 *
 * @return {*|array<*>} An accumulation of items.
 */

function accumulateInto(current, next) {
  if (!(next != null)) {
    {
      throw Error( "accumulateInto(...): Accumulated items must not be null or undefined." );
    }
  }

  if (current == null) {
    return next;
  } // Both are not empty. Warning: Never call x.concat(y) when you are not
  // certain that x is an Array (x could be a string with concat method).


  if (Array.isArray(current)) {
    if (Array.isArray(next)) {
      current.push.apply(current, next);
      return current;
    }

    current.push(next);
    return current;
  }

  if (Array.isArray(next)) {
    // A bit too dangerous to mutate `next`.
    return [current].concat(next);
  }

  return [current, next];
}

/**
 * @param {array} arr an "accumulation" of items which is either an Array or
 * a single item. Useful when paired with the `accumulate` module. This is a
 * simple utility that allows us to reason about a collection of items, but
 * handling the case when there is exactly one item (and we do not need to
 * allocate an array).
 * @param {function} cb Callback invoked with each element or a collection.
 * @param {?} [scope] Scope used as `this` in a callback.
 */
function forEachAccumulated(arr, cb, scope) {
  if (Array.isArray(arr)) {
    arr.forEach(cb, scope);
  } else if (arr) {
    cb.call(scope, arr);
  }
}

/**
 * Internal queue of events that have accumulated their dispatches and are
 * waiting to have their dispatches executed.
 */

var eventQueue = null;
/**
 * Dispatches an event and releases it back into the pool, unless persistent.
 *
 * @param {?object} event Synthetic event to be dispatched.
 * @private
 */

var executeDispatchesAndRelease = function (event) {
  if (event) {
    executeDispatchesInOrder(event);

    if (!event.isPersistent()) {
      event.constructor.release(event);
    }
  }
};

var executeDispatchesAndReleaseTopLevel = function (e) {
  return executeDispatchesAndRelease(e);
};

function runEventsInBatch(events) {
  if (events !== null) {
    eventQueue = accumulateInto(eventQueue, events);
  } // Set `eventQueue` to null before processing it so that we can tell if more
  // events get enqueued while processing.


  var processingEventQueue = eventQueue;
  eventQueue = null;

  if (!processingEventQueue) {
    return;
  }

  forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseTopLevel);

  if (!!eventQueue) {
    {
      throw Error( "processEventQueue(): Additional events were enqueued while processing an event queue. Support for this has not yet been implemented." );
    }
  } // This would be a good time to rethrow if any of the event handlers threw.


  rethrowCaughtError();
}

/**
 * Gets the target node from a native browser event by accounting for
 * inconsistencies in browser DOM APIs.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {DOMEventTarget} Target node.
 */

function getEventTarget(nativeEvent) {
  // Fallback to nativeEvent.srcElement for IE9
  // https://github.com/facebook/react/issues/12506
  var target = nativeEvent.target || nativeEvent.srcElement || window; // Normalize SVG <use> element events #4963

  if (target.correspondingUseElement) {
    target = target.correspondingUseElement;
  } // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
  // @see http://www.quirksmode.org/js/events_properties.html


  return target.nodeType === TEXT_NODE ? target.parentNode : target;
}

/**
 * Checks if an event is supported in the current execution environment.
 *
 * NOTE: This will not work correctly for non-generic events such as `change`,
 * `reset`, `load`, `error`, and `select`.
 *
 * Borrows from Modernizr.
 *
 * @param {string} eventNameSuffix Event name, e.g. "click".
 * @return {boolean} True if the event is supported.
 * @internal
 * @license Modernizr 3.0.0pre (Custom Build) | MIT
 */

function isEventSupported(eventNameSuffix) {
  if (!canUseDOM) {
    return false;
  }

  var eventName = 'on' + eventNameSuffix;
  var isSupported = eventName in document;

  if (!isSupported) {
    var element = document.createElement('div');
    element.setAttribute(eventName, 'return;');
    isSupported = typeof element[eventName] === 'function';
  }

  return isSupported;
}

/**
 * Summary of `DOMEventPluginSystem` event handling:
 *
 *  - Top-level delegation is used to trap most native browser events. This
 *    may only occur in the main thread and is the responsibility of
 *    ReactDOMEventListener, which is injected and can therefore support
 *    pluggable event sources. This is the only work that occurs in the main
 *    thread.
 *
 *  - We normalize and de-duplicate events to account for browser quirks. This
 *    may be done in the worker thread.
 *
 *  - Forward these native events (with the associated top-level type used to
 *    trap it) to `EventPluginRegistry`, which in turn will ask plugins if they want
 *    to extract any synthetic events.
 *
 *  - The `EventPluginRegistry` will then process each event by annotating them with
 *    "dispatches", a sequence of listeners and IDs that care about that event.
 *
 *  - The `EventPluginRegistry` then dispatches the events.
 *
 * Overview of React and the event system:
 *
 * +------------+    .
 * |    DOM     |    .
 * +------------+    .
 *       |           .
 *       v           .
 * +------------+    .
 * | ReactEvent |    .
 * |  Listener  |    .
 * +------------+    .                         +-----------+
 *       |           .               +--------+|SimpleEvent|
 *       |           .               |         |Plugin     |
 * +-----|------+    .               v         +-----------+
 * |     |      |    .    +--------------+                    +------------+
 * |     +-----------.--->|PluginRegistry|                    |    Event   |
 * |            |    .    |              |     +-----------+  | Propagators|
 * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
 * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
 * |            |    .    |              |     +-----------+  |  utilities |
 * |     +-----------.--->|              |                    +------------+
 * |     |      |    .    +--------------+
 * +-----|------+    .                ^        +-----------+
 *       |           .                |        |Enter/Leave|
 *       +           .                +-------+|Plugin     |
 * +-------------+   .                         +-----------+
 * | application |   .
 * |-------------|   .
 * |             |   .
 * |             |   .
 * +-------------+   .
 *                   .
 *    React Core     .  General Purpose Event Plugin System
 */

var CALLBACK_BOOKKEEPING_POOL_SIZE = 10;
var callbackBookkeepingPool = [];

function releaseTopLevelCallbackBookKeeping(instance) {
  instance.topLevelType = null;
  instance.nativeEvent = null;
  instance.targetInst = null;
  instance.ancestors.length = 0;

  if (callbackBookkeepingPool.length < CALLBACK_BOOKKEEPING_POOL_SIZE) {
    callbackBookkeepingPool.push(instance);
  }
} // Used to store ancestor hierarchy in top level callback


function getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst, eventSystemFlags) {
  if (callbackBookkeepingPool.length) {
    var instance = callbackBookkeepingPool.pop();
    instance.topLevelType = topLevelType;
    instance.eventSystemFlags = eventSystemFlags;
    instance.nativeEvent = nativeEvent;
    instance.targetInst = targetInst;
    return instance;
  }

  return {
    topLevelType: topLevelType,
    eventSystemFlags: eventSystemFlags,
    nativeEvent: nativeEvent,
    targetInst: targetInst,
    ancestors: []
  };
}
/**
 * Find the deepest React component completely containing the root of the
 * passed-in instance (for use when entire React trees are nested within each
 * other). If React trees are not nested, returns null.
 */


function findRootContainerNode(inst) {
  if (inst.tag === HostRoot) {
    return inst.stateNode.containerInfo;
  } // TODO: It may be a good idea to cache this to prevent unnecessary DOM
  // traversal, but caching is difficult to do correctly without using a
  // mutation observer to listen for all DOM changes.


  while (inst.return) {
    inst = inst.return;
  }

  if (inst.tag !== HostRoot) {
    // This can happen if we're in a detached tree.
    return null;
  }

  return inst.stateNode.containerInfo;
}
/**
 * Allows registered plugins an opportunity to extract events from top-level
 * native browser events.
 *
 * @return {*} An accumulation of synthetic events.
 * @internal
 */


function extractPluginEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags) {
  var events = null;

  for (var i = 0; i < plugins.length; i++) {
    // Not every plugin in the ordering may be loaded at runtime.
    var possiblePlugin = plugins[i];

    if (possiblePlugin) {
      var extractedEvents = possiblePlugin.extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);

      if (extractedEvents) {
        events = accumulateInto(events, extractedEvents);
      }
    }
  }

  return events;
}

function runExtractedPluginEventsInBatch(topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags) {
  var events = extractPluginEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags);
  runEventsInBatch(events);
}

function handleTopLevel(bookKeeping) {
  var targetInst = bookKeeping.targetInst; // Loop through the hierarchy, in case there's any nested components.
  // It's important that we build the array of ancestors before calling any
  // event handlers, because event handlers can modify the DOM, leading to
  // inconsistencies with ReactMount's node cache. See #1105.

  var ancestor = targetInst;

  do {
    if (!ancestor) {
      var ancestors = bookKeeping.ancestors;
      ancestors.push(ancestor);
      break;
    }

    var root = findRootContainerNode(ancestor);

    if (!root) {
      break;
    }

    var tag = ancestor.tag;

    if (tag === HostComponent || tag === HostText) {
      bookKeeping.ancestors.push(ancestor);
    }

    ancestor = getClosestInstanceFromNode(root);
  } while (ancestor);

  for (var i = 0; i < bookKeeping.ancestors.length; i++) {
    targetInst = bookKeeping.ancestors[i];
    var eventTarget = getEventTarget(bookKeeping.nativeEvent);
    var topLevelType = bookKeeping.topLevelType;
    var nativeEvent = bookKeeping.nativeEvent;
    var eventSystemFlags = bookKeeping.eventSystemFlags; // If this is the first ancestor, we mark it on the system flags

    if (i === 0) {
      eventSystemFlags |= IS_FIRST_ANCESTOR;
    }

    runExtractedPluginEventsInBatch(topLevelType, targetInst, nativeEvent, eventTarget, eventSystemFlags);
  }
}

function dispatchEventForLegacyPluginEventSystem(topLevelType, eventSystemFlags, nativeEvent, targetInst) {
  var bookKeeping = getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst, eventSystemFlags);

  try {
    // Event queue being processed in the same cycle allows
    // `preventDefault`.
    batchedEventUpdates(handleTopLevel, bookKeeping);
  } finally {
    releaseTopLevelCallbackBookKeeping(bookKeeping);
  }
}
/**
 * We listen for bubbled touch events on the document object.
 *
 * Firefox v8.01 (and possibly others) exhibited strange behavior when
 * mounting `onmousemove` events at some node that was not the document
 * element. The symptoms were that if your mouse is not moving over something
 * contained within that mount point (for example on the background) the
 * top-level listeners for `onmousemove` won't be called. However, if you
 * register the `mousemove` on the document object, then it will of course
 * catch all `mousemove`s. This along with iOS quirks, justifies restricting
 * top-level listeners to the document object only, at least for these
 * movement types of events and possibly all events.
 *
 * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
 *
 * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
 * they bubble to document.
 *
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @param {object} mountAt Container where to mount the listener
 */

function legacyListenToEvent(registrationName, mountAt) {
  var listenerMap = getListenerMapForElement(mountAt);
  var dependencies = registrationNameDependencies[registrationName];

  for (var i = 0; i < dependencies.length; i++) {
    var dependency = dependencies[i];
    legacyListenToTopLevelEvent(dependency, mountAt, listenerMap);
  }
}
function legacyListenToTopLevelEvent(topLevelType, mountAt, listenerMap) {
  if (!listenerMap.has(topLevelType)) {
    switch (topLevelType) {
      case TOP_SCROLL:
        trapCapturedEvent(TOP_SCROLL, mountAt);
        break;

      case TOP_FOCUS:
      case TOP_BLUR:
        trapCapturedEvent(TOP_FOCUS, mountAt);
        trapCapturedEvent(TOP_BLUR, mountAt); // We set the flag for a single dependency later in this function,
        // but this ensures we mark both as attached rather than just one.

        listenerMap.set(TOP_BLUR, null);
        listenerMap.set(TOP_FOCUS, null);
        break;

      case TOP_CANCEL:
      case TOP_CLOSE:
        if (isEventSupported(getRawEventName(topLevelType))) {
          trapCapturedEvent(topLevelType, mountAt);
        }

        break;

      case TOP_INVALID:
      case TOP_SUBMIT:
      case TOP_RESET:
        // We listen to them on the target DOM elements.
        // Some of them bubble so we don't want them to fire twice.
        break;

      default:
        // By default, listen on the top level to all non-media events.
        // Media events don't bubble so adding the listener wouldn't do anything.
        var isMediaEvent = mediaEventTypes.indexOf(topLevelType) !== -1;

        if (!isMediaEvent) {
          trapBubbledEvent(topLevelType, mountAt);
        }

        break;
    }

    listenerMap.set(topLevelType, null);
  }
}
function isListeningToAllDependencies(registrationName, mountAt) {
  var listenerMap = getListenerMapForElement(mountAt);
  var dependencies = registrationNameDependencies[registrationName];

  for (var i = 0; i < dependencies.length; i++) {
    var dependency = dependencies[i];

    if (!listenerMap.has(dependency)) {
      return false;
    }
  }

  return true;
}

var attemptUserBlockingHydration;
function setAttemptUserBlockingHydration(fn) {
  attemptUserBlockingHydration = fn;
}
var attemptContinuousHydration;
function setAttemptContinuousHydration(fn) {
  attemptContinuousHydration = fn;
}
var attemptHydrationAtCurrentPriority;
function setAttemptHydrationAtCurrentPriority(fn) {
  attemptHydrationAtCurrentPriority = fn;
} // TODO: Upgrade this definition once we're on a newer version of Flow that
var hasScheduledReplayAttempt = false; // The queue of discrete events to be replayed.

var queuedDiscreteEvents = []; // Indicates if any continuous event targets are non-null for early bailout.
// if the last target was dehydrated.

var queuedFocus = null;
var queuedDrag = null;
var queuedMouse = null; // For pointer events there can be one latest event per pointerId.

var queuedPointers = new Map();
var queuedPointerCaptures = new Map(); // We could consider replaying selectionchange and touchmoves too.

var queuedExplicitHydrationTargets = [];
function hasQueuedDiscreteEvents() {
  return queuedDiscreteEvents.length > 0;
}
var discreteReplayableEvents = [TOP_MOUSE_DOWN, TOP_MOUSE_UP, TOP_TOUCH_CANCEL, TOP_TOUCH_END, TOP_TOUCH_START, TOP_AUX_CLICK, TOP_DOUBLE_CLICK, TOP_POINTER_CANCEL, TOP_POINTER_DOWN, TOP_POINTER_UP, TOP_DRAG_END, TOP_DRAG_START, TOP_DROP, TOP_COMPOSITION_END, TOP_COMPOSITION_START, TOP_KEY_DOWN, TOP_KEY_PRESS, TOP_KEY_UP, TOP_INPUT, TOP_TEXT_INPUT, TOP_CLOSE, TOP_CANCEL, TOP_COPY, TOP_CUT, TOP_PASTE, TOP_CLICK, TOP_CHANGE, TOP_CONTEXT_MENU, TOP_RESET, TOP_SUBMIT];
var continuousReplayableEvents = [TOP_FOCUS, TOP_BLUR, TOP_DRAG_ENTER, TOP_DRAG_LEAVE, TOP_MOUSE_OVER, TOP_MOUSE_OUT, TOP_POINTER_OVER, TOP_POINTER_OUT, TOP_GOT_POINTER_CAPTURE, TOP_LOST_POINTER_CAPTURE];
function isReplayableDiscreteEvent(eventType) {
  return discreteReplayableEvents.indexOf(eventType) > -1;
}

function trapReplayableEventForDocument(topLevelType, document, listenerMap) {
  legacyListenToTopLevelEvent(topLevelType, document, listenerMap);
}

function eagerlyTrapReplayableEvents(container, document) {
  var listenerMapForDoc = getListenerMapForElement(document); // Discrete

  discreteReplayableEvents.forEach(function (topLevelType) {
    trapReplayableEventForDocument(topLevelType, document, listenerMapForDoc);
  }); // Continuous

  continuousReplayableEvents.forEach(function (topLevelType) {
    trapReplayableEventForDocument(topLevelType, document, listenerMapForDoc);
  });
}

function createQueuedReplayableEvent(blockedOn, topLevelType, eventSystemFlags, container, nativeEvent) {
  return {
    blockedOn: blockedOn,
    topLevelType: topLevelType,
    eventSystemFlags: eventSystemFlags | IS_REPLAYED,
    nativeEvent: nativeEvent,
    container: container
  };
}

function queueDiscreteEvent(blockedOn, topLevelType, eventSystemFlags, container, nativeEvent) {
  var queuedEvent = createQueuedReplayableEvent(blockedOn, topLevelType, eventSystemFlags, container, nativeEvent);
  queuedDiscreteEvents.push(queuedEvent);
} // Resets the replaying for this type of continuous event to no event.

function clearIfContinuousEvent(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case TOP_FOCUS:
    case TOP_BLUR:
      queuedFocus = null;
      break;

    case TOP_DRAG_ENTER:
    case TOP_DRAG_LEAVE:
      queuedDrag = null;
      break;

    case TOP_MOUSE_OVER:
    case TOP_MOUSE_OUT:
      queuedMouse = null;
      break;

    case TOP_POINTER_OVER:
    case TOP_POINTER_OUT:
      {
        var pointerId = nativeEvent.pointerId;
        queuedPointers.delete(pointerId);
        break;
      }

    case TOP_GOT_POINTER_CAPTURE:
    case TOP_LOST_POINTER_CAPTURE:
      {
        var _pointerId = nativeEvent.pointerId;
        queuedPointerCaptures.delete(_pointerId);
        break;
      }
  }
}

function accumulateOrCreateContinuousQueuedReplayableEvent(existingQueuedEvent, blockedOn, topLevelType, eventSystemFlags, container, nativeEvent) {
  if (existingQueuedEvent === null || existingQueuedEvent.nativeEvent !== nativeEvent) {
    var queuedEvent = createQueuedReplayableEvent(blockedOn, topLevelType, eventSystemFlags, container, nativeEvent);

    if (blockedOn !== null) {
      var _fiber2 = getInstanceFromNode$1(blockedOn);

      if (_fiber2 !== null) {
        // Attempt to increase the priority of this target.
        attemptContinuousHydration(_fiber2);
      }
    }

    return queuedEvent;
  } // If we have already queued this exact event, then it's because
  // the different event systems have different DOM event listeners.
  // We can accumulate the flags and store a single event to be
  // replayed.


  existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
  return existingQueuedEvent;
}

function queueIfContinuousEvent(blockedOn, topLevelType, eventSystemFlags, container, nativeEvent) {
  // These set relatedTarget to null because the replayed event will be treated as if we
  // moved from outside the window (no target) onto the target once it hydrates.
  // Instead of mutating we could clone the event.
  switch (topLevelType) {
    case TOP_FOCUS:
      {
        var focusEvent = nativeEvent;
        queuedFocus = accumulateOrCreateContinuousQueuedReplayableEvent(queuedFocus, blockedOn, topLevelType, eventSystemFlags, container, focusEvent);
        return true;
      }

    case TOP_DRAG_ENTER:
      {
        var dragEvent = nativeEvent;
        queuedDrag = accumulateOrCreateContinuousQueuedReplayableEvent(queuedDrag, blockedOn, topLevelType, eventSystemFlags, container, dragEvent);
        return true;
      }

    case TOP_MOUSE_OVER:
      {
        var mouseEvent = nativeEvent;
        queuedMouse = accumulateOrCreateContinuousQueuedReplayableEvent(queuedMouse, blockedOn, topLevelType, eventSystemFlags, container, mouseEvent);
        return true;
      }

    case TOP_POINTER_OVER:
      {
        var pointerEvent = nativeEvent;
        var pointerId = pointerEvent.pointerId;
        queuedPointers.set(pointerId, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointers.get(pointerId) || null, blockedOn, topLevelType, eventSystemFlags, container, pointerEvent));
        return true;
      }

    case TOP_GOT_POINTER_CAPTURE:
      {
        var _pointerEvent = nativeEvent;
        var _pointerId2 = _pointerEvent.pointerId;
        queuedPointerCaptures.set(_pointerId2, accumulateOrCreateContinuousQueuedReplayableEvent(queuedPointerCaptures.get(_pointerId2) || null, blockedOn, topLevelType, eventSystemFlags, container, _pointerEvent));
        return true;
      }
  }

  return false;
} // Check if this target is unblocked. Returns true if it's unblocked.

function attemptExplicitHydrationTarget(queuedTarget) {
  // TODO: This function shares a lot of logic with attemptToDispatchEvent.
  // Try to unify them. It's a bit tricky since it would require two return
  // values.
  var targetInst = getClosestInstanceFromNode(queuedTarget.target);

  if (targetInst !== null) {
    var nearestMounted = getNearestMountedFiber(targetInst);

    if (nearestMounted !== null) {
      var tag = nearestMounted.tag;

      if (tag === SuspenseComponent) {
        var instance = getSuspenseInstanceFromFiber(nearestMounted);

        if (instance !== null) {
          // We're blocked on hydrating this boundary.
          // Increase its priority.
          queuedTarget.blockedOn = instance;
          Scheduler.unstable_runWithPriority(queuedTarget.priority, function () {
            attemptHydrationAtCurrentPriority(nearestMounted);
          });
          return;
        }
      } else if (tag === HostRoot) {
        var root = nearestMounted.stateNode;

        if (root.hydrate) {
          queuedTarget.blockedOn = getContainerFromFiber(nearestMounted); // We don't currently have a way to increase the priority of
          // a root other than sync.

          return;
        }
      }
    }
  }

  queuedTarget.blockedOn = null;
}

function attemptReplayContinuousQueuedEvent(queuedEvent) {
  if (queuedEvent.blockedOn !== null) {
    return false;
  }

  var nextBlockedOn = attemptToDispatchEvent(queuedEvent.topLevelType, queuedEvent.eventSystemFlags, queuedEvent.container, queuedEvent.nativeEvent);

  if (nextBlockedOn !== null) {
    // We're still blocked. Try again later.
    var _fiber3 = getInstanceFromNode$1(nextBlockedOn);

    if (_fiber3 !== null) {
      attemptContinuousHydration(_fiber3);
    }

    queuedEvent.blockedOn = nextBlockedOn;
    return false;
  }

  return true;
}

function attemptReplayContinuousQueuedEventInMap(queuedEvent, key, map) {
  if (attemptReplayContinuousQueuedEvent(queuedEvent)) {
    map.delete(key);
  }
}

function replayUnblockedEvents() {
  hasScheduledReplayAttempt = false; // First replay discrete events.

  while (queuedDiscreteEvents.length > 0) {
    var nextDiscreteEvent = queuedDiscreteEvents[0];

    if (nextDiscreteEvent.blockedOn !== null) {
      // We're still blocked.
      // Increase the priority of this boundary to unblock
      // the next discrete event.
      var _fiber4 = getInstanceFromNode$1(nextDiscreteEvent.blockedOn);

      if (_fiber4 !== null) {
        attemptUserBlockingHydration(_fiber4);
      }

      break;
    }

    var nextBlockedOn = attemptToDispatchEvent(nextDiscreteEvent.topLevelType, nextDiscreteEvent.eventSystemFlags, nextDiscreteEvent.container, nextDiscreteEvent.nativeEvent);

    if (nextBlockedOn !== null) {
      // We're still blocked. Try again later.
      nextDiscreteEvent.blockedOn = nextBlockedOn;
    } else {
      // We've successfully replayed the first event. Let's try the next one.
      queuedDiscreteEvents.shift();
    }
  } // Next replay any continuous events.


  if (queuedFocus !== null && attemptReplayContinuousQueuedEvent(queuedFocus)) {
    queuedFocus = null;
  }

  if (queuedDrag !== null && attemptReplayContinuousQueuedEvent(queuedDrag)) {
    queuedDrag = null;
  }

  if (queuedMouse !== null && attemptReplayContinuousQueuedEvent(queuedMouse)) {
    queuedMouse = null;
  }

  queuedPointers.forEach(attemptReplayContinuousQueuedEventInMap);
  queuedPointerCaptures.forEach(attemptReplayContinuousQueuedEventInMap);
}

function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
  if (queuedEvent.blockedOn === unblocked) {
    queuedEvent.blockedOn = null;

    if (!hasScheduledReplayAttempt) {
      hasScheduledReplayAttempt = true; // Schedule a callback to attempt replaying as many events as are
      // now unblocked. This first might not actually be unblocked yet.
      // We could check it early to avoid scheduling an unnecessary callback.

      Scheduler.unstable_scheduleCallback(Scheduler.unstable_NormalPriority, replayUnblockedEvents);
    }
  }
}

function retryIfBlockedOn(unblocked) {
  // Mark anything that was blocked on this as no longer blocked
  // and eligible for a replay.
  if (queuedDiscreteEvents.length > 0) {
    scheduleCallbackIfUnblocked(queuedDiscreteEvents[0], unblocked); // This is a exponential search for each boundary that commits. I think it's
    // worth it because we expect very few discrete events to queue up and once
    // we are actually fully unblocked it will be fast to replay them.

    for (var i = 1; i < queuedDiscreteEvents.length; i++) {
      var queuedEvent = queuedDiscreteEvents[i];

      if (queuedEvent.blockedOn === unblocked) {
        queuedEvent.blockedOn = null;
      }
    }
  }

  if (queuedFocus !== null) {
    scheduleCallbackIfUnblocked(queuedFocus, unblocked);
  }

  if (queuedDrag !== null) {
    scheduleCallbackIfUnblocked(queuedDrag, unblocked);
  }

  if (queuedMouse !== null) {
    scheduleCallbackIfUnblocked(queuedMouse, unblocked);
  }

  var unblock = function (queuedEvent) {
    return scheduleCallbackIfUnblocked(queuedEvent, unblocked);
  };

  queuedPointers.forEach(unblock);
  queuedPointerCaptures.forEach(unblock);

  for (var _i = 0; _i < queuedExplicitHydrationTargets.length; _i++) {
    var queuedTarget = queuedExplicitHydrationTargets[_i];

    if (queuedTarget.blockedOn === unblocked) {
      queuedTarget.blockedOn = null;
    }
  }

  while (queuedExplicitHydrationTargets.length > 0) {
    var nextExplicitTarget = queuedExplicitHydrationTargets[0];

    if (nextExplicitTarget.blockedOn !== null) {
      // We're still blocked.
      break;
    } else {
      attemptExplicitHydrationTarget(nextExplicitTarget);

      if (nextExplicitTarget.blockedOn === null) {
        // We're unblocked.
        queuedExplicitHydrationTargets.shift();
      }
    }
  }
}

function addEventBubbleListener(element, eventType, listener) {
  element.addEventListener(eventType, listener, false);
}
function addEventCaptureListener(element, eventType, listener) {
  element.addEventListener(eventType, listener, true);
}

// do it in two places, which duplicates logic
// and increases the bundle size, we do it all
// here once. If we remove or refactor the
// SimpleEventPlugin, we should also remove or
// update the below line.

var simpleEventPluginEventTypes = {};
var topLevelEventsToDispatchConfig = new Map();
var eventPriorities = new Map(); // We store most of the events in this module in pairs of two strings so we can re-use
// the code required to apply the same logic for event prioritization and that of the
// SimpleEventPlugin. This complicates things slightly, but the aim is to reduce code
// duplication (for which there would be quite a bit). For the events that are not needed
// for the SimpleEventPlugin (otherDiscreteEvents) we process them separately as an
// array of top level events.
// Lastly, we ignore prettier so we can keep the formatting sane.
// prettier-ignore

var discreteEventPairsForSimpleEventPlugin = [TOP_BLUR, 'blur', TOP_CANCEL, 'cancel', TOP_CLICK, 'click', TOP_CLOSE, 'close', TOP_CONTEXT_MENU, 'contextMenu', TOP_COPY, 'copy', TOP_CUT, 'cut', TOP_AUX_CLICK, 'auxClick', TOP_DOUBLE_CLICK, 'doubleClick', TOP_DRAG_END, 'dragEnd', TOP_DRAG_START, 'dragStart', TOP_DROP, 'drop', TOP_FOCUS, 'focus', TOP_INPUT, 'input', TOP_INVALID, 'invalid', TOP_KEY_DOWN, 'keyDown', TOP_KEY_PRESS, 'keyPress', TOP_KEY_UP, 'keyUp', TOP_MOUSE_DOWN, 'mouseDown', TOP_MOUSE_UP, 'mouseUp', TOP_PASTE, 'paste', TOP_PAUSE, 'pause', TOP_PLAY, 'play', TOP_POINTER_CANCEL, 'pointerCancel', TOP_POINTER_DOWN, 'pointerDown', TOP_POINTER_UP, 'pointerUp', TOP_RATE_CHANGE, 'rateChange', TOP_RESET, 'reset', TOP_SEEKED, 'seeked', TOP_SUBMIT, 'submit', TOP_TOUCH_CANCEL, 'touchCancel', TOP_TOUCH_END, 'touchEnd', TOP_TOUCH_START, 'touchStart', TOP_VOLUME_CHANGE, 'volumeChange'];
var otherDiscreteEvents = [TOP_CHANGE, TOP_SELECTION_CHANGE, TOP_TEXT_INPUT, TOP_COMPOSITION_START, TOP_COMPOSITION_END, TOP_COMPOSITION_UPDATE]; // prettier-ignore

var userBlockingPairsForSimpleEventPlugin = [TOP_DRAG, 'drag', TOP_DRAG_ENTER, 'dragEnter', TOP_DRAG_EXIT, 'dragExit', TOP_DRAG_LEAVE, 'dragLeave', TOP_DRAG_OVER, 'dragOver', TOP_MOUSE_MOVE, 'mouseMove', TOP_MOUSE_OUT, 'mouseOut', TOP_MOUSE_OVER, 'mouseOver', TOP_POINTER_MOVE, 'pointerMove', TOP_POINTER_OUT, 'pointerOut', TOP_POINTER_OVER, 'pointerOver', TOP_SCROLL, 'scroll', TOP_TOGGLE, 'toggle', TOP_TOUCH_MOVE, 'touchMove', TOP_WHEEL, 'wheel']; // prettier-ignore

var continuousPairsForSimpleEventPlugin = [TOP_ABORT, 'abort', TOP_ANIMATION_END, 'animationEnd', TOP_ANIMATION_ITERATION, 'animationIteration', TOP_ANIMATION_START, 'animationStart', TOP_CAN_PLAY, 'canPlay', TOP_CAN_PLAY_THROUGH, 'canPlayThrough', TOP_DURATION_CHANGE, 'durationChange', TOP_EMPTIED, 'emptied', TOP_ENCRYPTED, 'encrypted', TOP_ENDED, 'ended', TOP_ERROR, 'error', TOP_GOT_POINTER_CAPTURE, 'gotPointerCapture', TOP_LOAD, 'load', TOP_LOADED_DATA, 'loadedData', TOP_LOADED_METADATA, 'loadedMetadata', TOP_LOAD_START, 'loadStart', TOP_LOST_POINTER_CAPTURE, 'lostPointerCapture', TOP_PLAYING, 'playing', TOP_PROGRESS, 'progress', TOP_SEEKING, 'seeking', TOP_STALLED, 'stalled', TOP_SUSPEND, 'suspend', TOP_TIME_UPDATE, 'timeUpdate', TOP_TRANSITION_END, 'transitionEnd', TOP_WAITING, 'waiting'];
/**
 * Turns
 * ['abort', ...]
 * into
 * eventTypes = {
 *   'abort': {
 *     phasedRegistrationNames: {
 *       bubbled: 'onAbort',
 *       captured: 'onAbortCapture',
 *     },
 *     dependencies: [TOP_ABORT],
 *   },
 *   ...
 * };
 * topLevelEventsToDispatchConfig = new Map([
 *   [TOP_ABORT, { sameConfig }],
 * ]);
 */

function processSimpleEventPluginPairsByPriority(eventTypes, priority) {
  // As the event types are in pairs of two, we need to iterate
  // through in twos. The events are in pairs of two to save code
  // and improve init perf of processing this array, as it will
  // result in far fewer object allocations and property accesses
  // if we only use three arrays to process all the categories of
  // instead of tuples.
  for (var i = 0; i < eventTypes.length; i += 2) {
    var topEvent = eventTypes[i];
    var event = eventTypes[i + 1];
    var capitalizedEvent = event[0].toUpperCase() + event.slice(1);
    var onEvent = 'on' + capitalizedEvent;
    var config = {
      phasedRegistrationNames: {
        bubbled: onEvent,
        captured: onEvent + 'Capture'
      },
      dependencies: [topEvent],
      eventPriority: priority
    };
    eventPriorities.set(topEvent, priority);
    topLevelEventsToDispatchConfig.set(topEvent, config);
    simpleEventPluginEventTypes[event] = config;
  }
}

function processTopEventPairsByPriority(eventTypes, priority) {
  for (var i = 0; i < eventTypes.length; i++) {
    eventPriorities.set(eventTypes[i], priority);
  }
} // SimpleEventPlugin


processSimpleEventPluginPairsByPriority(discreteEventPairsForSimpleEventPlugin, DiscreteEvent);
processSimpleEventPluginPairsByPriority(userBlockingPairsForSimpleEventPlugin, UserBlockingEvent);
processSimpleEventPluginPairsByPriority(continuousPairsForSimpleEventPlugin, ContinuousEvent); // Not used by SimpleEventPlugin

processTopEventPairsByPriority(otherDiscreteEvents, DiscreteEvent);
function getEventPriorityForPluginSystem(topLevelType) {
  var priority = eventPriorities.get(topLevelType); // Default to a ContinuousEvent. Note: we might
  // want to warn if we can't detect the priority
  // for the event.

  return priority === undefined ? ContinuousEvent : priority;
}

// Intentionally not named imports because Rollup would use dynamic dispatch for
var UserBlockingPriority = Scheduler.unstable_UserBlockingPriority,
    runWithPriority = Scheduler.unstable_runWithPriority; // TODO: can we stop exporting these?

var _enabled = true;
function setEnabled(enabled) {
  _enabled = !!enabled;
}
function isEnabled() {
  return _enabled;
}
function trapBubbledEvent(topLevelType, element) {
  trapEventForPluginEventSystem(element, topLevelType, false);
}
function trapCapturedEvent(topLevelType, element) {
  trapEventForPluginEventSystem(element, topLevelType, true);
}

function trapEventForPluginEventSystem(container, topLevelType, capture) {
  var listener;

  switch (getEventPriorityForPluginSystem(topLevelType)) {
    case DiscreteEvent:
      listener = dispatchDiscreteEvent.bind(null, topLevelType, PLUGIN_EVENT_SYSTEM, container);
      break;

    case UserBlockingEvent:
      listener = dispatchUserBlockingUpdate.bind(null, topLevelType, PLUGIN_EVENT_SYSTEM, container);
      break;

    case ContinuousEvent:
    default:
      listener = dispatchEvent.bind(null, topLevelType, PLUGIN_EVENT_SYSTEM, container);
      break;
  }

  var rawEventName = getRawEventName(topLevelType);

  if (capture) {
    addEventCaptureListener(container, rawEventName, listener);
  } else {
    addEventBubbleListener(container, rawEventName, listener);
  }
}

function dispatchDiscreteEvent(topLevelType, eventSystemFlags, container, nativeEvent) {
  flushDiscreteUpdatesIfNeeded(nativeEvent.timeStamp);
  discreteUpdates(dispatchEvent, topLevelType, eventSystemFlags, container, nativeEvent);
}

function dispatchUserBlockingUpdate(topLevelType, eventSystemFlags, container, nativeEvent) {
  runWithPriority(UserBlockingPriority, dispatchEvent.bind(null, topLevelType, eventSystemFlags, container, nativeEvent));
}

function dispatchEvent(topLevelType, eventSystemFlags, container, nativeEvent) {
  if (!_enabled) {
    return;
  }

  if (hasQueuedDiscreteEvents() && isReplayableDiscreteEvent(topLevelType)) {
    // If we already have a queue of discrete events, and this is another discrete
    // event, then we can't dispatch it regardless of its target, since they
    // need to dispatch in order.
    queueDiscreteEvent(null, // Flags that we're not actually blocked on anything as far as we know.
    topLevelType, eventSystemFlags, container, nativeEvent);
    return;
  }

  var blockedOn = attemptToDispatchEvent(topLevelType, eventSystemFlags, container, nativeEvent);

  if (blockedOn === null) {
    // We successfully dispatched this event.
    clearIfContinuousEvent(topLevelType, nativeEvent);
    return;
  }

  if (isReplayableDiscreteEvent(topLevelType)) {
    // This this to be replayed later once the target is available.
    queueDiscreteEvent(blockedOn, topLevelType, eventSystemFlags, container, nativeEvent);
    return;
  }

  if (queueIfContinuousEvent(blockedOn, topLevelType, eventSystemFlags, container, nativeEvent)) {
    return;
  } // We need to clear only if we didn't queue because
  // queueing is accummulative.


  clearIfContinuousEvent(topLevelType, nativeEvent); // This is not replayable so we'll invoke it but without a target,
  // in case the event system needs to trace it.

  {
    dispatchEventForLegacyPluginEventSystem(topLevelType, eventSystemFlags, nativeEvent, null);
  }
} // Attempt dispatching an event. Returns a SuspenseInstance or Container if it's blocked.

function attemptToDispatchEvent(topLevelType, eventSystemFlags, container, nativeEvent) {
  // TODO: Warn if _enabled is false.
  var nativeEventTarget = getEventTarget(nativeEvent);
  var targetInst = getClosestInstanceFromNode(nativeEventTarget);

  if (targetInst !== null) {
    var nearestMounted = getNearestMountedFiber(targetInst);

    if (nearestMounted === null) {
      // This tree has been unmounted already. Dispatch without a target.
      targetInst = null;
    } else {
      var tag = nearestMounted.tag;

      if (tag === SuspenseComponent) {
        var instance = getSuspenseInstanceFromFiber(nearestMounted);

        if (instance !== null) {
          // Queue the event to be replayed later. Abort dispatching since we
          // don't want this event dispatched twice through the event system.
          // TODO: If this is the first discrete event in the queue. Schedule an increased
          // priority for this boundary.
          return instance;
        } // This shouldn't happen, something went wrong but to avoid blocking
        // the whole system, dispatch the event without a target.
        // TODO: Warn.


        targetInst = null;
      } else if (tag === HostRoot) {
        var root = nearestMounted.stateNode;

        if (root.hydrate) {
          // If this happens during a replay something went wrong and it might block
          // the whole system.
          return getContainerFromFiber(nearestMounted);
        }

        targetInst = null;
      } else if (nearestMounted !== targetInst) {
        // If we get an event (ex: img onload) before committing that
        // component's mount, ignore it for now (that is, treat it as if it was an
        // event on a non-React tree). We might also consider queueing events and
        // dispatching them after the mount.
        targetInst = null;
      }
    }
  }

  {
    dispatchEventForLegacyPluginEventSystem(topLevelType, eventSystemFlags, nativeEvent, targetInst);
  } // We're not blocked on anything.


  return null;
}

// List derived from Gecko source code:
// https://github.com/mozilla/gecko-dev/blob/4e638efc71/layout/style/test/property_database.js
var shorthandToLonghand = {
  animation: ['animationDelay', 'animationDirection', 'animationDuration', 'animationFillMode', 'animationIterationCount', 'animationName', 'animationPlayState', 'animationTimingFunction'],
  background: ['backgroundAttachment', 'backgroundClip', 'backgroundColor', 'backgroundImage', 'backgroundOrigin', 'backgroundPositionX', 'backgroundPositionY', 'backgroundRepeat', 'backgroundSize'],
  backgroundPosition: ['backgroundPositionX', 'backgroundPositionY'],
  border: ['borderBottomColor', 'borderBottomStyle', 'borderBottomWidth', 'borderImageOutset', 'borderImageRepeat', 'borderImageSlice', 'borderImageSource', 'borderImageWidth', 'borderLeftColor', 'borderLeftStyle', 'borderLeftWidth', 'borderRightColor', 'borderRightStyle', 'borderRightWidth', 'borderTopColor', 'borderTopStyle', 'borderTopWidth'],
  borderBlockEnd: ['borderBlockEndColor', 'borderBlockEndStyle', 'borderBlockEndWidth'],
  borderBlockStart: ['borderBlockStartColor', 'borderBlockStartStyle', 'borderBlockStartWidth'],
  borderBottom: ['borderBottomColor', 'borderBottomStyle', 'borderBottomWidth'],
  borderColor: ['borderBottomColor', 'borderLeftColor', 'borderRightColor', 'borderTopColor'],
  borderImage: ['borderImageOutset', 'borderImageRepeat', 'borderImageSlice', 'borderImageSource', 'borderImageWidth'],
  borderInlineEnd: ['borderInlineEndColor', 'borderInlineEndStyle', 'borderInlineEndWidth'],
  borderInlineStart: ['borderInlineStartColor', 'borderInlineStartStyle', 'borderInlineStartWidth'],
  borderLeft: ['borderLeftColor', 'borderLeftStyle', 'borderLeftWidth'],
  borderRadius: ['borderBottomLeftRadius', 'borderBottomRightRadius', 'borderTopLeftRadius', 'borderTopRightRadius'],
  borderRight: ['borderRightColor', 'borderRightStyle', 'borderRightWidth'],
  borderStyle: ['borderBottomStyle', 'borderLeftStyle', 'borderRightStyle', 'borderTopStyle'],
  borderTop: ['borderTopColor', 'borderTopStyle', 'borderTopWidth'],
  borderWidth: ['borderBottomWidth', 'borderLeftWidth', 'borderRightWidth', 'borderTopWidth'],
  columnRule: ['columnRuleColor', 'columnRuleStyle', 'columnRuleWidth'],
  columns: ['columnCount', 'columnWidth'],
  flex: ['flexBasis', 'flexGrow', 'flexShrink'],
  flexFlow: ['flexDirection', 'flexWrap'],
  font: ['fontFamily', 'fontFeatureSettings', 'fontKerning', 'fontLanguageOverride', 'fontSize', 'fontSizeAdjust', 'fontStretch', 'fontStyle', 'fontVariant', 'fontVariantAlternates', 'fontVariantCaps', 'fontVariantEastAsian', 'fontVariantLigatures', 'fontVariantNumeric', 'fontVariantPosition', 'fontWeight', 'lineHeight'],
  fontVariant: ['fontVariantAlternates', 'fontVariantCaps', 'fontVariantEastAsian', 'fontVariantLigatures', 'fontVariantNumeric', 'fontVariantPosition'],
  gap: ['columnGap', 'rowGap'],
  grid: ['gridAutoColumns', 'gridAutoFlow', 'gridAutoRows', 'gridTemplateAreas', 'gridTemplateColumns', 'gridTemplateRows'],
  gridArea: ['gridColumnEnd', 'gridColumnStart', 'gridRowEnd', 'gridRowStart'],
  gridColumn: ['gridColumnEnd', 'gridColumnStart'],
  gridColumnGap: ['columnGap'],
  gridGap: ['columnGap', 'rowGap'],
  gridRow: ['gridRowEnd', 'gridRowStart'],
  gridRowGap: ['rowGap'],
  gridTemplate: ['gridTemplateAreas', 'gridTemplateColumns', 'gridTemplateRows'],
  listStyle: ['listStyleImage', 'listStylePosition', 'listStyleType'],
  margin: ['marginBottom', 'marginLeft', 'marginRight', 'marginTop'],
  marker: ['markerEnd', 'markerMid', 'markerStart'],
  mask: ['maskClip', 'maskComposite', 'maskImage', 'maskMode', 'maskOrigin', 'maskPositionX', 'maskPositionY', 'maskRepeat', 'maskSize'],
  maskPosition: ['maskPositionX', 'maskPositionY'],
  outline: ['outlineColor', 'outlineStyle', 'outlineWidth'],
  overflow: ['overflowX', 'overflowY'],
  padding: ['paddingBottom', 'paddingLeft', 'paddingRight', 'paddingTop'],
  placeContent: ['alignContent', 'justifyContent'],
  placeItems: ['alignItems', 'justifyItems'],
  placeSelf: ['alignSelf', 'justifySelf'],
  textDecoration: ['textDecorationColor', 'textDecorationLine', 'textDecorationStyle'],
  textEmphasis: ['textEmphasisColor', 'textEmphasisStyle'],
  transition: ['transitionDelay', 'transitionDuration', 'transitionProperty', 'transitionTimingFunction'],
  wordWrap: ['overflowWrap']
};

/**
 * CSS properties which accept numbers but are not in units of "px".
 */
var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridArea: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,
  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};
/**
 * @param {string} prefix vendor-specific prefix, eg: Webkit
 * @param {string} key style name, eg: transitionDuration
 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
 * WebkitTransitionDuration
 */

function prefixKey(prefix, key) {
  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
}
/**
 * Support style names that may come passed in prefixed by adding permutations
 * of vendor prefixes.
 */


var prefixes = ['Webkit', 'ms', 'Moz', 'O']; // Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
// infinite loop, because it iterates over the newly added props too.

Object.keys(isUnitlessNumber).forEach(function (prop) {
  prefixes.forEach(function (prefix) {
    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
  });
});

/**
 * Convert a value into the proper css writable value. The style name `name`
 * should be logical (no hyphens), as specified
 * in `CSSProperty.isUnitlessNumber`.
 *
 * @param {string} name CSS property name such as `topMargin`.
 * @param {*} value CSS property value such as `10px`.
 * @return {string} Normalized style value with dimensions applied.
 */

function dangerousStyleValue(name, value, isCustomProperty) {
  // Note that we've removed escapeTextForBrowser() calls here since the
  // whole string will be escaped when the attribute is injected into
  // the markup. If you provide unsafe user data here they can inject
  // arbitrary CSS which may be problematic (I couldn't repro this):
  // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
  // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
  // This is not an XSS hole but instead a potential CSS injection issue
  // which has lead to a greater discussion about how we're going to
  // trust URLs moving forward. See #2115901
  var isEmpty = value == null || typeof value === 'boolean' || value === '';

  if (isEmpty) {
    return '';
  }

  if (!isCustomProperty && typeof value === 'number' && value !== 0 && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name])) {
    return value + 'px'; // Presumes implicit 'px' suffix for unitless numbers
  }

  return ('' + value).trim();
}

var uppercasePattern = /([A-Z])/g;
var msPattern = /^ms-/;
/**
 * Hyphenates a camelcased CSS property name, for example:
 *
 *   > hyphenateStyleName('backgroundColor')
 *   < "background-color"
 *   > hyphenateStyleName('MozTransition')
 *   < "-moz-transition"
 *   > hyphenateStyleName('msTransition')
 *   < "-ms-transition"
 *
 * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
 * is converted to `-ms-`.
 */

function hyphenateStyleName(name) {
  return name.replace(uppercasePattern, '-$1').toLowerCase().replace(msPattern, '-ms-');
}

var warnValidStyle = function () {};

{
  // 'msTransform' is correct, but the other prefixes should be capitalized
  var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;
  var msPattern$1 = /^-ms-/;
  var hyphenPattern = /-(.)/g; // style values shouldn't contain a semicolon

  var badStyleValueWithSemicolonPattern = /;\s*$/;
  var warnedStyleNames = {};
  var warnedStyleValues = {};
  var warnedForNaNValue = false;
  var warnedForInfinityValue = false;

  var camelize = function (string) {
    return string.replace(hyphenPattern, function (_, character) {
      return character.toUpperCase();
    });
  };

  var warnHyphenatedStyleName = function (name) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;

    error('Unsupported style property %s. Did you mean %s?', name, // As Andi Smith suggests
    // (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
    // is converted to lowercase `ms`.
    camelize(name.replace(msPattern$1, 'ms-')));
  };

  var warnBadVendoredStyleName = function (name) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;

    error('Unsupported vendor-prefixed style property %s. Did you mean %s?', name, name.charAt(0).toUpperCase() + name.slice(1));
  };

  var warnStyleValueWithSemicolon = function (name, value) {
    if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
      return;
    }

    warnedStyleValues[value] = true;

    error("Style property values shouldn't contain a semicolon. " + 'Try "%s: %s" instead.', name, value.replace(badStyleValueWithSemicolonPattern, ''));
  };

  var warnStyleValueIsNaN = function (name, value) {
    if (warnedForNaNValue) {
      return;
    }

    warnedForNaNValue = true;

    error('`NaN` is an invalid value for the `%s` css style property.', name);
  };

  var warnStyleValueIsInfinity = function (name, value) {
    if (warnedForInfinityValue) {
      return;
    }

    warnedForInfinityValue = true;

    error('`Infinity` is an invalid value for the `%s` css style property.', name);
  };

  warnValidStyle = function (name, value) {
    if (name.indexOf('-') > -1) {
      warnHyphenatedStyleName(name);
    } else if (badVendoredStyleNamePattern.test(name)) {
      warnBadVendoredStyleName(name);
    } else if (badStyleValueWithSemicolonPattern.test(value)) {
      warnStyleValueWithSemicolon(name, value);
    }

    if (typeof value === 'number') {
      if (isNaN(value)) {
        warnStyleValueIsNaN(name, value);
      } else if (!isFinite(value)) {
        warnStyleValueIsInfinity(name, value);
      }
    }
  };
}

var warnValidStyle$1 = warnValidStyle;

/**
 * Operations for dealing with CSS properties.
 */

/**
 * This creates a string that is expected to be equivalent to the style
 * attribute generated by server-side rendering. It by-passes warnings and
 * security checks so it's not safe to use this value for anything other than
 * comparison. It is only used in DEV for SSR validation.
 */

function createDangerousStringForStyles(styles) {
  {
    var serialized = '';
    var delimiter = '';

    for (var styleName in styles) {
      if (!styles.hasOwnProperty(styleName)) {
        continue;
      }

      var styleValue = styles[styleName];

      if (styleValue != null) {
        var isCustomProperty = styleName.indexOf('--') === 0;
        serialized += delimiter + (isCustomProperty ? styleName : hyphenateStyleName(styleName)) + ':';
        serialized += dangerousStyleValue(styleName, styleValue, isCustomProperty);
        delimiter = ';';
      }
    }

    return serialized || null;
  }
}
/**
 * Sets the value for multiple styles on a node.  If a value is specified as
 * '' (empty string), the corresponding style property will be unset.
 *
 * @param {DOMElement} node
 * @param {object} styles
 */

function setValueForStyles(node, styles) {
  var style = node.style;

  for (var styleName in styles) {
    if (!styles.hasOwnProperty(styleName)) {
      continue;
    }

    var isCustomProperty = styleName.indexOf('--') === 0;

    {
      if (!isCustomProperty) {
        warnValidStyle$1(styleName, styles[styleName]);
      }
    }

    var styleValue = dangerousStyleValue(styleName, styles[styleName], isCustomProperty);

    if (styleName === 'float') {
      styleName = 'cssFloat';
    }

    if (isCustomProperty) {
      style.setProperty(styleName, styleValue);
    } else {
      style[styleName] = styleValue;
    }
  }
}

function isValueEmpty(value) {
  return value == null || typeof value === 'boolean' || value === '';
}
/**
 * Given {color: 'red', overflow: 'hidden'} returns {
 *   color: 'color',
 *   overflowX: 'overflow',
 *   overflowY: 'overflow',
 * }. This can be read as "the overflowY property was set by the overflow
 * shorthand". That is, the values are the property that each was derived from.
 */


function expandShorthandMap(styles) {
  var expanded = {};

  for (var key in styles) {
    var longhands = shorthandToLonghand[key] || [key];

    for (var i = 0; i < longhands.length; i++) {
      expanded[longhands[i]] = key;
    }
  }

  return expanded;
}
/**
 * When mixing shorthand and longhand property names, we warn during updates if
 * we expect an incorrect result to occur. In particular, we warn for:
 *
 * Updating a shorthand property (longhand gets overwritten):
 *   {font: 'foo', fontVariant: 'bar'} -> {font: 'baz', fontVariant: 'bar'}
 *   becomes .style.font = 'baz'
 * Removing a shorthand property (longhand gets lost too):
 *   {font: 'foo', fontVariant: 'bar'} -> {fontVariant: 'bar'}
 *   becomes .style.font = ''
 * Removing a longhand property (should revert to shorthand; doesn't):
 *   {font: 'foo', fontVariant: 'bar'} -> {font: 'foo'}
 *   becomes .style.fontVariant = ''
 */


function validateShorthandPropertyCollisionInDev(styleUpdates, nextStyles) {
  {

    if (!nextStyles) {
      return;
    }

    var expandedUpdates = expandShorthandMap(styleUpdates);
    var expandedStyles = expandShorthandMap(nextStyles);
    var warnedAbout = {};

    for (var key in expandedUpdates) {
      var originalKey = expandedUpdates[key];
      var correctOriginalKey = expandedStyles[key];

      if (correctOriginalKey && originalKey !== correctOriginalKey) {
        var warningKey = originalKey + ',' + correctOriginalKey;

        if (warnedAbout[warningKey]) {
          continue;
        }

        warnedAbout[warningKey] = true;

        error('%s a style property during rerender (%s) when a ' + 'conflicting property is set (%s) can lead to styling bugs. To ' + "avoid this, don't mix shorthand and non-shorthand properties " + 'for the same value; instead, replace the shorthand with ' + 'separate values.', isValueEmpty(styleUpdates[originalKey]) ? 'Removing' : 'Updating', originalKey, correctOriginalKey);
      }
    }
  }
}

// For HTML, certain tags should omit their close tag. We keep a whitelist for
// those special-case tags.
var omittedCloseTags = {
  area: true,
  base: true,
  br: true,
  col: true,
  embed: true,
  hr: true,
  img: true,
  input: true,
  keygen: true,
  link: true,
  meta: true,
  param: true,
  source: true,
  track: true,
  wbr: true // NOTE: menuitem's close tag should be omitted, but that causes problems.

};

// `omittedCloseTags` except that `menuitem` should still have its closing tag.

var voidElementTags = _assign({
  menuitem: true
}, omittedCloseTags);

var HTML = '__html';
var ReactDebugCurrentFrame$3 = null;

{
  ReactDebugCurrentFrame$3 = ReactSharedInternals.ReactDebugCurrentFrame;
}

function assertValidProps(tag, props) {
  if (!props) {
    return;
  } // Note the use of `==` which checks for null or undefined.


  if (voidElementTags[tag]) {
    if (!(props.children == null && props.dangerouslySetInnerHTML == null)) {
      {
        throw Error( tag + " is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`." + ( ReactDebugCurrentFrame$3.getStackAddendum() ) );
      }
    }
  }

  if (props.dangerouslySetInnerHTML != null) {
    if (!(props.children == null)) {
      {
        throw Error( "Can only set one of `children` or `props.dangerouslySetInnerHTML`." );
      }
    }

    if (!(typeof props.dangerouslySetInnerHTML === 'object' && HTML in props.dangerouslySetInnerHTML)) {
      {
        throw Error( "`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://fb.me/react-invariant-dangerously-set-inner-html for more information." );
      }
    }
  }

  {
    if (!props.suppressContentEditableWarning && props.contentEditable && props.children != null) {
      error('A component is `contentEditable` and contains `children` managed by ' + 'React. It is now your responsibility to guarantee that none of ' + 'those nodes are unexpectedly modified or duplicated. This is ' + 'probably not intentional.');
    }
  }

  if (!(props.style == null || typeof props.style === 'object')) {
    {
      throw Error( "The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX." + ( ReactDebugCurrentFrame$3.getStackAddendum() ) );
    }
  }
}

function isCustomComponent(tagName, props) {
  if (tagName.indexOf('-') === -1) {
    return typeof props.is === 'string';
  }

  switch (tagName) {
    // These are reserved SVG and MathML elements.
    // We don't mind this whitelist too much because we expect it to never grow.
    // The alternative is to track the namespace in a few places which is convoluted.
    // https://w3c.github.io/webcomponents/spec/custom/#custom-elements-core-concepts
    case 'annotation-xml':
    case 'color-profile':
    case 'font-face':
    case 'font-face-src':
    case 'font-face-uri':
    case 'font-face-format':
    case 'font-face-name':
    case 'missing-glyph':
      return false;

    default:
      return true;
  }
}

// When adding attributes to the HTML or SVG whitelist, be sure to
// also add them to this module to ensure casing and incorrect name
// warnings.
var possibleStandardNames = {
  // HTML
  accept: 'accept',
  acceptcharset: 'acceptCharset',
  'accept-charset': 'acceptCharset',
  accesskey: 'accessKey',
  action: 'action',
  allowfullscreen: 'allowFullScreen',
  alt: 'alt',
  as: 'as',
  async: 'async',
  autocapitalize: 'autoCapitalize',
  autocomplete: 'autoComplete',
  autocorrect: 'autoCorrect',
  autofocus: 'autoFocus',
  autoplay: 'autoPlay',
  autosave: 'autoSave',
  capture: 'capture',
  cellpadding: 'cellPadding',
  cellspacing: 'cellSpacing',
  challenge: 'challenge',
  charset: 'charSet',
  checked: 'checked',
  children: 'children',
  cite: 'cite',
  class: 'className',
  classid: 'classID',
  classname: 'className',
  cols: 'cols',
  colspan: 'colSpan',
  content: 'content',
  contenteditable: 'contentEditable',
  contextmenu: 'contextMenu',
  controls: 'controls',
  controlslist: 'controlsList',
  coords: 'coords',
  crossorigin: 'crossOrigin',
  dangerouslysetinnerhtml: 'dangerouslySetInnerHTML',
  data: 'data',
  datetime: 'dateTime',
  default: 'default',
  defaultchecked: 'defaultChecked',
  defaultvalue: 'defaultValue',
  defer: 'defer',
  dir: 'dir',
  disabled: 'disabled',
  disablepictureinpicture: 'disablePictureInPicture',
  download: 'download',
  draggable: 'draggable',
  enctype: 'encType',
  for: 'htmlFor',
  form: 'form',
  formmethod: 'formMethod',
  formaction: 'formAction',
  formenctype: 'formEncType',
  formnovalidate: 'formNoValidate',
  formtarget: 'formTarget',
  frameborder: 'frameBorder',
  headers: 'headers',
  height: 'height',
  hidden: 'hidden',
  high: 'high',
  href: 'href',
  hreflang: 'hrefLang',
  htmlfor: 'htmlFor',
  httpequiv: 'httpEquiv',
  'http-equiv': 'httpEquiv',
  icon: 'icon',
  id: 'id',
  innerhtml: 'innerHTML',
  inputmode: 'inputMode',
  integrity: 'integrity',
  is: 'is',
  itemid: 'itemID',
  itemprop: 'itemProp',
  itemref: 'itemRef',
  itemscope: 'itemScope',
  itemtype: 'itemType',
  keyparams: 'keyParams',
  keytype: 'keyType',
  kind: 'kind',
  label: 'label',
  lang: 'lang',
  list: 'list',
  loop: 'loop',
  low: 'low',
  manifest: 'manifest',
  marginwidth: 'marginWidth',
  marginheight: 'marginHeight',
  max: 'max',
  maxlength: 'maxLength',
  media: 'media',
  mediagroup: 'mediaGroup',
  method: 'method',
  min: 'min',
  minlength: 'minLength',
  multiple: 'multiple',
  muted: 'muted',
  name: 'name',
  nomodule: 'noModule',
  nonce: 'nonce',
  novalidate: 'noValidate',
  open: 'open',
  optimum: 'optimum',
  pattern: 'pattern',
  placeholder: 'placeholder',
  playsinline: 'playsInline',
  poster: 'poster',
  preload: 'preload',
  profile: 'profile',
  radiogroup: 'radioGroup',
  readonly: 'readOnly',
  referrerpolicy: 'referrerPolicy',
  rel: 'rel',
  required: 'required',
  reversed: 'reversed',
  role: 'role',
  rows: 'rows',
  rowspan: 'rowSpan',
  sandbox: 'sandbox',
  scope: 'scope',
  scoped: 'scoped',
  scrolling: 'scrolling',
  seamless: 'seamless',
  selected: 'selected',
  shape: 'shape',
  size: 'size',
  sizes: 'sizes',
  span: 'span',
  spellcheck: 'spellCheck',
  src: 'src',
  srcdoc: 'srcDoc',
  srclang: 'srcLang',
  srcset: 'srcSet',
  start: 'start',
  step: 'step',
  style: 'style',
  summary: 'summary',
  tabindex: 'tabIndex',
  target: 'target',
  title: 'title',
  type: 'type',
  usemap: 'useMap',
  value: 'value',
  width: 'width',
  wmode: 'wmode',
  wrap: 'wrap',
  // SVG
  about: 'about',
  accentheight: 'accentHeight',
  'accent-height': 'accentHeight',
  accumulate: 'accumulate',
  additive: 'additive',
  alignmentbaseline: 'alignmentBaseline',
  'alignment-baseline': 'alignmentBaseline',
  allowreorder: 'allowReorder',
  alphabetic: 'alphabetic',
  amplitude: 'amplitude',
  arabicform: 'arabicForm',
  'arabic-form': 'arabicForm',
  ascent: 'ascent',
  attributename: 'attributeName',
  attributetype: 'attributeType',
  autoreverse: 'autoReverse',
  azimuth: 'azimuth',
  basefrequency: 'baseFrequency',
  baselineshift: 'baselineShift',
  'baseline-shift': 'baselineShift',
  baseprofile: 'baseProfile',
  bbox: 'bbox',
  begin: 'begin',
  bias: 'bias',
  by: 'by',
  calcmode: 'calcMode',
  capheight: 'capHeight',
  'cap-height': 'capHeight',
  clip: 'clip',
  clippath: 'clipPath',
  'clip-path': 'clipPath',
  clippathunits: 'clipPathUnits',
  cliprule: 'clipRule',
  'clip-rule': 'clipRule',
  color: 'color',
  colorinterpolation: 'colorInterpolation',
  'color-interpolation': 'colorInterpolation',
  colorinterpolationfilters: 'colorInterpolationFilters',
  'color-interpolation-filters': 'colorInterpolationFilters',
  colorprofile: 'colorProfile',
  'color-profile': 'colorProfile',
  colorrendering: 'colorRendering',
  'color-rendering': 'colorRendering',
  contentscripttype: 'contentScriptType',
  contentstyletype: 'contentStyleType',
  cursor: 'cursor',
  cx: 'cx',
  cy: 'cy',
  d: 'd',
  datatype: 'datatype',
  decelerate: 'decelerate',
  descent: 'descent',
  diffuseconstant: 'diffuseConstant',
  direction: 'direction',
  display: 'display',
  divisor: 'divisor',
  dominantbaseline: 'dominantBaseline',
  'dominant-baseline': 'dominantBaseline',
  dur: 'dur',
  dx: 'dx',
  dy: 'dy',
  edgemode: 'edgeMode',
  elevation: 'elevation',
  enablebackground: 'enableBackground',
  'enable-background': 'enableBackground',
  end: 'end',
  exponent: 'exponent',
  externalresourcesrequired: 'externalResourcesRequired',
  fill: 'fill',
  fillopacity: 'fillOpacity',
  'fill-opacity': 'fillOpacity',
  fillrule: 'fillRule',
  'fill-rule': 'fillRule',
  filter: 'filter',
  filterres: 'filterRes',
  filterunits: 'filterUnits',
  floodopacity: 'floodOpacity',
  'flood-opacity': 'floodOpacity',
  floodcolor: 'floodColor',
  'flood-color': 'floodColor',
  focusable: 'focusable',
  fontfamily: 'fontFamily',
  'font-family': 'fontFamily',
  fontsize: 'fontSize',
  'font-size': 'fontSize',
  fontsizeadjust: 'fontSizeAdjust',
  'font-size-adjust': 'fontSizeAdjust',
  fontstretch: 'fontStretch',
  'font-stretch': 'fontStretch',
  fontstyle: 'fontStyle',
  'font-style': 'fontStyle',
  fontvariant: 'fontVariant',
  'font-variant': 'fontVariant',
  fontweight: 'fontWeight',
  'font-weight': 'fontWeight',
  format: 'format',
  from: 'from',
  fx: 'fx',
  fy: 'fy',
  g1: 'g1',
  g2: 'g2',
  glyphname: 'glyphName',
  'glyph-name': 'glyphName',
  glyphorientationhorizontal: 'glyphOrientationHorizontal',
  'glyph-orientation-horizontal': 'glyphOrientationHorizontal',
  glyphorientationvertical: 'glyphOrientationVertical',
  'glyph-orientation-vertical': 'glyphOrientationVertical',
  glyphref: 'glyphRef',
  gradienttransform: 'gradientTransform',
  gradientunits: 'gradientUnits',
  hanging: 'hanging',
  horizadvx: 'horizAdvX',
  'horiz-adv-x': 'horizAdvX',
  horizoriginx: 'horizOriginX',
  'horiz-origin-x': 'horizOriginX',
  ideographic: 'ideographic',
  imagerendering: 'imageRendering',
  'image-rendering': 'imageRendering',
  in2: 'in2',
  in: 'in',
  inlist: 'inlist',
  intercept: 'intercept',
  k1: 'k1',
  k2: 'k2',
  k3: 'k3',
  k4: 'k4',
  k: 'k',
  kernelmatrix: 'kernelMatrix',
  kernelunitlength: 'kernelUnitLength',
  kerning: 'kerning',
  keypoints: 'keyPoints',
  keysplines: 'keySplines',
  keytimes: 'keyTimes',
  lengthadjust: 'lengthAdjust',
  letterspacing: 'letterSpacing',
  'letter-spacing': 'letterSpacing',
  lightingcolor: 'lightingColor',
  'lighting-color': 'lightingColor',
  limitingconeangle: 'limitingConeAngle',
  local: 'local',
  markerend: 'markerEnd',
  'marker-end': 'markerEnd',
  markerheight: 'markerHeight',
  markermid: 'markerMid',
  'marker-mid': 'markerMid',
  markerstart: 'markerStart',
  'marker-start': 'markerStart',
  markerunits: 'markerUnits',
  markerwidth: 'markerWidth',
  mask: 'mask',
  maskcontentunits: 'maskContentUnits',
  maskunits: 'maskUnits',
  mathematical: 'mathematical',
  mode: 'mode',
  numoctaves: 'numOctaves',
  offset: 'offset',
  opacity: 'opacity',
  operator: 'operator',
  order: 'order',
  orient: 'orient',
  orientation: 'orientation',
  origin: 'origin',
  overflow: 'overflow',
  overlineposition: 'overlinePosition',
  'overline-position': 'overlinePosition',
  overlinethickness: 'overlineThickness',
  'overline-thickness': 'overlineThickness',
  paintorder: 'paintOrder',
  'paint-order': 'paintOrder',
  panose1: 'panose1',
  'panose-1': 'panose1',
  pathlength: 'pathLength',
  patterncontentunits: 'patternContentUnits',
  patterntransform: 'patternTransform',
  patternunits: 'patternUnits',
  pointerevents: 'pointerEvents',
  'pointer-events': 'pointerEvents',
  points: 'points',
  pointsatx: 'pointsAtX',
  pointsaty: 'pointsAtY',
  pointsatz: 'pointsAtZ',
  prefix: 'prefix',
  preservealpha: 'preserveAlpha',
  preserveaspectratio: 'preserveAspectRatio',
  primitiveunits: 'primitiveUnits',
  property: 'property',
  r: 'r',
  radius: 'radius',
  refx: 'refX',
  refy: 'refY',
  renderingintent: 'renderingIntent',
  'rendering-intent': 'renderingIntent',
  repeatcount: 'repeatCount',
  repeatdur: 'repeatDur',
  requiredextensions: 'requiredExtensions',
  requiredfeatures: 'requiredFeatures',
  resource: 'resource',
  restart: 'restart',
  result: 'result',
  results: 'results',
  rotate: 'rotate',
  rx: 'rx',
  ry: 'ry',
  scale: 'scale',
  security: 'security',
  seed: 'seed',
  shaperendering: 'shapeRendering',
  'shape-rendering': 'shapeRendering',
  slope: 'slope',
  spacing: 'spacing',
  specularconstant: 'specularConstant',
  specularexponent: 'specularExponent',
  speed: 'speed',
  spreadmethod: 'spreadMethod',
  startoffset: 'startOffset',
  stddeviation: 'stdDeviation',
  stemh: 'stemh',
  stemv: 'stemv',
  stitchtiles: 'stitchTiles',
  stopcolor: 'stopColor',
  'stop-color': 'stopColor',
  stopopacity: 'stopOpacity',
  'stop-opacity': 'stopOpacity',
  strikethroughposition: 'strikethroughPosition',
  'strikethrough-position': 'strikethroughPosition',
  strikethroughthickness: 'strikethroughThickness',
  'strikethrough-thickness': 'strikethroughThickness',
  string: 'string',
  stroke: 'stroke',
  strokedasharray: 'strokeDasharray',
  'stroke-dasharray': 'strokeDasharray',
  strokedashoffset: 'strokeDashoffset',
  'stroke-dashoffset': 'strokeDashoffset',
  strokelinecap: 'strokeLinecap',
  'stroke-linecap': 'strokeLinecap',
  strokelinejoin: 'strokeLinejoin',
  'stroke-linejoin': 'strokeLinejoin',
  strokemiterlimit: 'strokeMiterlimit',
  'stroke-miterlimit': 'strokeMiterlimit',
  strokewidth: 'strokeWidth',
  'stroke-width': 'strokeWidth',
  strokeopacity: 'strokeOpacity',
  'stroke-opacity': 'strokeOpacity',
  suppresscontenteditablewarning: 'suppressContentEditableWarning',
  suppresshydrationwarning: 'suppressHydrationWarning',
  surfacescale: 'surfaceScale',
  systemlanguage: 'systemLanguage',
  tablevalues: 'tableValues',
  targetx: 'targetX',
  targety: 'targetY',
  textanchor: 'textAnchor',
  'text-anchor': 'textAnchor',
  textdecoration: 'textDecoration',
  'text-decoration': 'textDecoration',
  textlength: 'textLength',
  textrendering: 'textRendering',
  'text-rendering': 'textRendering',
  to: 'to',
  transform: 'transform',
  typeof: 'typeof',
  u1: 'u1',
  u2: 'u2',
  underlineposition: 'underlinePosition',
  'underline-position': 'underlinePosition',
  underlinethickness: 'underlineThickness',
  'underline-thickness': 'underlineThickness',
  unicode: 'unicode',
  unicodebidi: 'unicodeBidi',
  'unicode-bidi': 'unicodeBidi',
  unicoderange: 'unicodeRange',
  'unicode-range': 'unicodeRange',
  unitsperem: 'unitsPerEm',
  'units-per-em': 'unitsPerEm',
  unselectable: 'unselectable',
  valphabetic: 'vAlphabetic',
  'v-alphabetic': 'vAlphabetic',
  values: 'values',
  vectoreffect: 'vectorEffect',
  'vector-effect': 'vectorEffect',
  version: 'version',
  vertadvy: 'vertAdvY',
  'vert-adv-y': 'vertAdvY',
  vertoriginx: 'vertOriginX',
  'vert-origin-x': 'vertOriginX',
  vertoriginy: 'vertOriginY',
  'vert-origin-y': 'vertOriginY',
  vhanging: 'vHanging',
  'v-hanging': 'vHanging',
  videographic: 'vIdeographic',
  'v-ideographic': 'vIdeographic',
  viewbox: 'viewBox',
  viewtarget: 'viewTarget',
  visibility: 'visibility',
  vmathematical: 'vMathematical',
  'v-mathematical': 'vMathematical',
  vocab: 'vocab',
  widths: 'widths',
  wordspacing: 'wordSpacing',
  'word-spacing': 'wordSpacing',
  writingmode: 'writingMode',
  'writing-mode': 'writingMode',
  x1: 'x1',
  x2: 'x2',
  x: 'x',
  xchannelselector: 'xChannelSelector',
  xheight: 'xHeight',
  'x-height': 'xHeight',
  xlinkactuate: 'xlinkActuate',
  'xlink:actuate': 'xlinkActuate',
  xlinkarcrole: 'xlinkArcrole',
  'xlink:arcrole': 'xlinkArcrole',
  xlinkhref: 'xlinkHref',
  'xlink:href': 'xlinkHref',
  xlinkrole: 'xlinkRole',
  'xlink:role': 'xlinkRole',
  xlinkshow: 'xlinkShow',
  'xlink:show': 'xlinkShow',
  xlinktitle: 'xlinkTitle',
  'xlink:title': 'xlinkTitle',
  xlinktype: 'xlinkType',
  'xlink:type': 'xlinkType',
  xmlbase: 'xmlBase',
  'xml:base': 'xmlBase',
  xmllang: 'xmlLang',
  'xml:lang': 'xmlLang',
  xmlns: 'xmlns',
  'xml:space': 'xmlSpace',
  xmlnsxlink: 'xmlnsXlink',
  'xmlns:xlink': 'xmlnsXlink',
  xmlspace: 'xmlSpace',
  y1: 'y1',
  y2: 'y2',
  y: 'y',
  ychannelselector: 'yChannelSelector',
  z: 'z',
  zoomandpan: 'zoomAndPan'
};

var ariaProperties = {
  'aria-current': 0,
  // state
  'aria-details': 0,
  'aria-disabled': 0,
  // state
  'aria-hidden': 0,
  // state
  'aria-invalid': 0,
  // state
  'aria-keyshortcuts': 0,
  'aria-label': 0,
  'aria-roledescription': 0,
  // Widget Attributes
  'aria-autocomplete': 0,
  'aria-checked': 0,
  'aria-expanded': 0,
  'aria-haspopup': 0,
  'aria-level': 0,
  'aria-modal': 0,
  'aria-multiline': 0,
  'aria-multiselectable': 0,
  'aria-orientation': 0,
  'aria-placeholder': 0,
  'aria-pressed': 0,
  'aria-readonly': 0,
  'aria-required': 0,
  'aria-selected': 0,
  'aria-sort': 0,
  'aria-valuemax': 0,
  'aria-valuemin': 0,
  'aria-valuenow': 0,
  'aria-valuetext': 0,
  // Live Region Attributes
  'aria-atomic': 0,
  'aria-busy': 0,
  'aria-live': 0,
  'aria-relevant': 0,
  // Drag-and-Drop Attributes
  'aria-dropeffect': 0,
  'aria-grabbed': 0,
  // Relationship Attributes
  'aria-activedescendant': 0,
  'aria-colcount': 0,
  'aria-colindex': 0,
  'aria-colspan': 0,
  'aria-controls': 0,
  'aria-describedby': 0,
  'aria-errormessage': 0,
  'aria-flowto': 0,
  'aria-labelledby': 0,
  'aria-owns': 0,
  'aria-posinset': 0,
  'aria-rowcount': 0,
  'aria-rowindex': 0,
  'aria-rowspan': 0,
  'aria-setsize': 0
};

var warnedProperties = {};
var rARIA = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
var rARIACamel = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');
var hasOwnProperty$1 = Object.prototype.hasOwnProperty;

function validateProperty(tagName, name) {
  {
    if (hasOwnProperty$1.call(warnedProperties, name) && warnedProperties[name]) {
      return true;
    }

    if (rARIACamel.test(name)) {
      var ariaName = 'aria-' + name.slice(4).toLowerCase();
      var correctName = ariaProperties.hasOwnProperty(ariaName) ? ariaName : null; // If this is an aria-* attribute, but is not listed in the known DOM
      // DOM properties, then it is an invalid aria-* attribute.

      if (correctName == null) {
        error('Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.', name);

        warnedProperties[name] = true;
        return true;
      } // aria-* attributes should be lowercase; suggest the lowercase version.


      if (name !== correctName) {
        error('Invalid ARIA attribute `%s`. Did you mean `%s`?', name, correctName);

        warnedProperties[name] = true;
        return true;
      }
    }

    if (rARIA.test(name)) {
      var lowerCasedName = name.toLowerCase();
      var standardName = ariaProperties.hasOwnProperty(lowerCasedName) ? lowerCasedName : null; // If this is an aria-* attribute, but is not listed in the known DOM
      // DOM properties, then it is an invalid aria-* attribute.

      if (standardName == null) {
        warnedProperties[name] = true;
        return false;
      } // aria-* attributes should be lowercase; suggest the lowercase version.


      if (name !== standardName) {
        error('Unknown ARIA attribute `%s`. Did you mean `%s`?', name, standardName);

        warnedProperties[name] = true;
        return true;
      }
    }
  }

  return true;
}

function warnInvalidARIAProps(type, props) {
  {
    var invalidProps = [];

    for (var key in props) {
      var isValid = validateProperty(type, key);

      if (!isValid) {
        invalidProps.push(key);
      }
    }

    var unknownPropString = invalidProps.map(function (prop) {
      return '`' + prop + '`';
    }).join(', ');

    if (invalidProps.length === 1) {
      error('Invalid aria prop %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop', unknownPropString, type);
    } else if (invalidProps.length > 1) {
      error('Invalid aria props %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop', unknownPropString, type);
    }
  }
}

function validateProperties(type, props) {
  if (isCustomComponent(type, props)) {
    return;
  }

  warnInvalidARIAProps(type, props);
}

var didWarnValueNull = false;
function validateProperties$1(type, props) {
  {
    if (type !== 'input' && type !== 'textarea' && type !== 'select') {
      return;
    }

    if (props != null && props.value === null && !didWarnValueNull) {
      didWarnValueNull = true;

      if (type === 'select' && props.multiple) {
        error('`value` prop on `%s` should not be null. ' + 'Consider using an empty array when `multiple` is set to `true` ' + 'to clear the component or `undefined` for uncontrolled components.', type);
      } else {
        error('`value` prop on `%s` should not be null. ' + 'Consider using an empty string to clear the component or `undefined` ' + 'for uncontrolled components.', type);
      }
    }
  }
}

var validateProperty$1 = function () {};

{
  var warnedProperties$1 = {};
  var _hasOwnProperty = Object.prototype.hasOwnProperty;
  var EVENT_NAME_REGEX = /^on./;
  var INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/;
  var rARIA$1 = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
  var rARIACamel$1 = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');

  validateProperty$1 = function (tagName, name, value, canUseEventSystem) {
    if (_hasOwnProperty.call(warnedProperties$1, name) && warnedProperties$1[name]) {
      return true;
    }

    var lowerCasedName = name.toLowerCase();

    if (lowerCasedName === 'onfocusin' || lowerCasedName === 'onfocusout') {
      error('React uses onFocus and onBlur instead of onFocusIn and onFocusOut. ' + 'All React events are normalized to bubble, so onFocusIn and onFocusOut ' + 'are not needed/supported by React.');

      warnedProperties$1[name] = true;
      return true;
    } // We can't rely on the event system being injected on the server.


    if (canUseEventSystem) {
      if (registrationNameModules.hasOwnProperty(name)) {
        return true;
      }

      var registrationName = possibleRegistrationNames.hasOwnProperty(lowerCasedName) ? possibleRegistrationNames[lowerCasedName] : null;

      if (registrationName != null) {
        error('Invalid event handler property `%s`. Did you mean `%s`?', name, registrationName);

        warnedProperties$1[name] = true;
        return true;
      }

      if (EVENT_NAME_REGEX.test(name)) {
        error('Unknown event handler property `%s`. It will be ignored.', name);

        warnedProperties$1[name] = true;
        return true;
      }
    } else if (EVENT_NAME_REGEX.test(name)) {
      // If no event plugins have been injected, we are in a server environment.
      // So we can't tell if the event name is correct for sure, but we can filter
      // out known bad ones like `onclick`. We can't suggest a specific replacement though.
      if (INVALID_EVENT_NAME_REGEX.test(name)) {
        error('Invalid event handler property `%s`. ' + 'React events use the camelCase naming convention, for example `onClick`.', name);
      }

      warnedProperties$1[name] = true;
      return true;
    } // Let the ARIA attribute hook validate ARIA attributes


    if (rARIA$1.test(name) || rARIACamel$1.test(name)) {
      return true;
    }

    if (lowerCasedName === 'innerhtml') {
      error('Directly setting property `innerHTML` is not permitted. ' + 'For more information, lookup documentation on `dangerouslySetInnerHTML`.');

      warnedProperties$1[name] = true;
      return true;
    }

    if (lowerCasedName === 'aria') {
      error('The `aria` attribute is reserved for future use in React. ' + 'Pass individual `aria-` attributes instead.');

      warnedProperties$1[name] = true;
      return true;
    }

    if (lowerCasedName === 'is' && value !== null && value !== undefined && typeof value !== 'string') {
      error('Received a `%s` for a string attribute `is`. If this is expected, cast ' + 'the value to a string.', typeof value);

      warnedProperties$1[name] = true;
      return true;
    }

    if (typeof value === 'number' && isNaN(value)) {
      error('Received NaN for the `%s` attribute. If this is expected, cast ' + 'the value to a string.', name);

      warnedProperties$1[name] = true;
      return true;
    }

    var propertyInfo = getPropertyInfo(name);
    var isReserved = propertyInfo !== null && propertyInfo.type === RESERVED; // Known attributes should match the casing specified in the property config.

    if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
      var standardName = possibleStandardNames[lowerCasedName];

      if (standardName !== name) {
        error('Invalid DOM property `%s`. Did you mean `%s`?', name, standardName);

        warnedProperties$1[name] = true;
        return true;
      }
    } else if (!isReserved && name !== lowerCasedName) {
      // Unknown attributes should have lowercase casing since that's how they
      // will be cased anyway with server rendering.
      error('React does not recognize the `%s` prop on a DOM element. If you ' + 'intentionally want it to appear in the DOM as a custom ' + 'attribute, spell it as lowercase `%s` instead. ' + 'If you accidentally passed it from a parent component, remove ' + 'it from the DOM element.', name, lowerCasedName);

      warnedProperties$1[name] = true;
      return true;
    }

    if (typeof value === 'boolean' && shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
      if (value) {
        error('Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.', value, name, name, value, name);
      } else {
        error('Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.', value, name, name, value, name, name, name);
      }

      warnedProperties$1[name] = true;
      return true;
    } // Now that we've validated casing, do not validate
    // data types for reserved props


    if (isReserved) {
      return true;
    } // Warn when a known attribute is a bad type


    if (shouldRemoveAttributeWithWarning(name, value, propertyInfo, false)) {
      warnedProperties$1[name] = true;
      return false;
    } // Warn when passing the strings 'false' or 'true' into a boolean prop


    if ((value === 'false' || value === 'true') && propertyInfo !== null && propertyInfo.type === BOOLEAN) {
      error('Received the string `%s` for the boolean attribute `%s`. ' + '%s ' + 'Did you mean %s={%s}?', value, name, value === 'false' ? 'The browser will interpret it as a truthy value.' : 'Although this works, it will not work as expected if you pass the string "false".', name, value);

      warnedProperties$1[name] = true;
      return true;
    }

    return true;
  };
}

var warnUnknownProperties = function (type, props, canUseEventSystem) {
  {
    var unknownProps = [];

    for (var key in props) {
      var isValid = validateProperty$1(type, key, props[key], canUseEventSystem);

      if (!isValid) {
        unknownProps.push(key);
      }
    }

    var unknownPropString = unknownProps.map(function (prop) {
      return '`' + prop + '`';
    }).join(', ');

    if (unknownProps.length === 1) {
      error('Invalid value for prop %s on <%s> tag. Either remove it from the element, ' + 'or pass a string or number value to keep it in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior', unknownPropString, type);
    } else if (unknownProps.length > 1) {
      error('Invalid values for props %s on <%s> tag. Either remove them from the element, ' + 'or pass a string or number value to keep them in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior', unknownPropString, type);
    }
  }
};

function validateProperties$2(type, props, canUseEventSystem) {
  if (isCustomComponent(type, props)) {
    return;
  }

  warnUnknownProperties(type, props, canUseEventSystem);
}

var didWarnInvalidHydration = false;
var didWarnShadyDOM = false;
var DANGEROUSLY_SET_INNER_HTML = 'dangerouslySetInnerHTML';
var SUPPRESS_CONTENT_EDITABLE_WARNING = 'suppressContentEditableWarning';
var SUPPRESS_HYDRATION_WARNING = 'suppressHydrationWarning';
var AUTOFOCUS = 'autoFocus';
var CHILDREN = 'children';
var STYLE = 'style';
var HTML$1 = '__html';
var HTML_NAMESPACE$1 = Namespaces.html;
var warnedUnknownTags;
var suppressHydrationWarning;
var validatePropertiesInDevelopment;
var warnForTextDifference;
var warnForPropDifference;
var warnForExtraAttributes;
var warnForInvalidEventListener;
var canDiffStyleForHydrationWarning;
var normalizeMarkupForTextOrAttribute;
var normalizeHTML;

{
  warnedUnknownTags = {
    // Chrome is the only major browser not shipping <time>. But as of July
    // 2017 it intends to ship it due to widespread usage. We intentionally
    // *don't* warn for <time> even if it's unrecognized by Chrome because
    // it soon will be, and many apps have been using it anyway.
    time: true,
    // There are working polyfills for <dialog>. Let people use it.
    dialog: true,
    // Electron ships a custom <webview> tag to display external web content in
    // an isolated frame and process.
    // This tag is not present in non Electron environments such as JSDom which
    // is often used for testing purposes.
    // @see https://electronjs.org/docs/api/webview-tag
    webview: true
  };

  validatePropertiesInDevelopment = function (type, props) {
    validateProperties(type, props);
    validateProperties$1(type, props);
    validateProperties$2(type, props,
    /* canUseEventSystem */
    true);
  }; // IE 11 parses & normalizes the style attribute as opposed to other
  // browsers. It adds spaces and sorts the properties in some
  // non-alphabetical order. Handling that would require sorting CSS
  // properties in the client & server versions or applying
  // `expectedStyle` to a temporary DOM node to read its `style` attribute
  // normalized. Since it only affects IE, we're skipping style warnings
  // in that browser completely in favor of doing all that work.
  // See https://github.com/facebook/react/issues/11807


  canDiffStyleForHydrationWarning = canUseDOM && !document.documentMode; // HTML parsing normalizes CR and CRLF to LF.
  // It also can turn \u0000 into \uFFFD inside attributes.
  // https://www.w3.org/TR/html5/single-page.html#preprocessing-the-input-stream
  // If we have a mismatch, it might be caused by that.
  // We will still patch up in this case but not fire the warning.

  var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
  var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;

  normalizeMarkupForTextOrAttribute = function (markup) {
    var markupString = typeof markup === 'string' ? markup : '' + markup;
    return markupString.replace(NORMALIZE_NEWLINES_REGEX, '\n').replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, '');
  };

  warnForTextDifference = function (serverText, clientText) {
    if (didWarnInvalidHydration) {
      return;
    }

    var normalizedClientText = normalizeMarkupForTextOrAttribute(clientText);
    var normalizedServerText = normalizeMarkupForTextOrAttribute(serverText);

    if (normalizedServerText === normalizedClientText) {
      return;
    }

    didWarnInvalidHydration = true;

    error('Text content did not match. Server: "%s" Client: "%s"', normalizedServerText, normalizedClientText);
  };

  warnForPropDifference = function (propName, serverValue, clientValue) {
    if (didWarnInvalidHydration) {
      return;
    }

    var normalizedClientValue = normalizeMarkupForTextOrAttribute(clientValue);
    var normalizedServerValue = normalizeMarkupForTextOrAttribute(serverValue);

    if (normalizedServerValue === normalizedClientValue) {
      return;
    }

    didWarnInvalidHydration = true;

    error('Prop `%s` did not match. Server: %s Client: %s', propName, JSON.stringify(normalizedServerValue), JSON.stringify(normalizedClientValue));
  };

  warnForExtraAttributes = function (attributeNames) {
    if (didWarnInvalidHydration) {
      return;
    }

    didWarnInvalidHydration = true;
    var names = [];
    attributeNames.forEach(function (name) {
      names.push(name);
    });

    error('Extra attributes from the server: %s', names);
  };

  warnForInvalidEventListener = function (registrationName, listener) {
    if (listener === false) {
      error('Expected `%s` listener to be a function, instead got `false`.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.', registrationName, registrationName, registrationName);
    } else {
      error('Expected `%s` listener to be a function, instead got a value of `%s` type.', registrationName, typeof listener);
    }
  }; // Parse the HTML and read it back to normalize the HTML string so that it
  // can be used for comparison.


  normalizeHTML = function (parent, html) {
    // We could have created a separate document here to avoid
    // re-initializing custom elements if they exist. But this breaks
    // how <noscript> is being handled. So we use the same document.
    // See the discussion in https://github.com/facebook/react/pull/11157.
    var testElement = parent.namespaceURI === HTML_NAMESPACE$1 ? parent.ownerDocument.createElement(parent.tagName) : parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName);
    testElement.innerHTML = html;
    return testElement.innerHTML;
  };
}

function ensureListeningTo(rootContainerElement, registrationName) {
  var isDocumentOrFragment = rootContainerElement.nodeType === DOCUMENT_NODE || rootContainerElement.nodeType === DOCUMENT_FRAGMENT_NODE;
  var doc = isDocumentOrFragment ? rootContainerElement : rootContainerElement.ownerDocument;
  legacyListenToEvent(registrationName, doc);
}

function getOwnerDocumentFromRootContainer(rootContainerElement) {
  return rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
}

function noop() {}

function trapClickOnNonInteractiveElement(node) {
  // Mobile Safari does not fire properly bubble click events on
  // non-interactive elements, which means delegated click listeners do not
  // fire. The workaround for this bug involves attaching an empty click
  // listener on the target node.
  // http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
  // Just set it using the onclick property so that we don't have to manage any
  // bookkeeping for it. Not sure if we need to clear it when the listener is
  // removed.
  // TODO: Only do this for the relevant Safaris maybe?
  node.onclick = noop;
}

function setInitialDOMProperties(tag, domElement, rootContainerElement, nextProps, isCustomComponentTag) {
  for (var propKey in nextProps) {
    if (!nextProps.hasOwnProperty(propKey)) {
      continue;
    }

    var nextProp = nextProps[propKey];

    if (propKey === STYLE) {
      {
        if (nextProp) {
          // Freeze the next style object so that we can assume it won't be
          // mutated. We have already warned for this in the past.
          Object.freeze(nextProp);
        }
      } // Relies on `updateStylesByID` not mutating `styleUpdates`.


      setValueForStyles(domElement, nextProp);
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      var nextHtml = nextProp ? nextProp[HTML$1] : undefined;

      if (nextHtml != null) {
        setInnerHTML(domElement, nextHtml);
      }
    } else if (propKey === CHILDREN) {
      if (typeof nextProp === 'string') {
        // Avoid setting initial textContent when the text is empty. In IE11 setting
        // textContent on a <textarea> will cause the placeholder to not
        // show within the <textarea> until it has been focused and blurred again.
        // https://github.com/facebook/react/issues/6731#issuecomment-254874553
        var canSetTextContent = tag !== 'textarea' || nextProp !== '';

        if (canSetTextContent) {
          setTextContent(domElement, nextProp);
        }
      } else if (typeof nextProp === 'number') {
        setTextContent(domElement, '' + nextProp);
      }
    } else if ( propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ; else if (propKey === AUTOFOCUS) ; else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        if ( typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }

        ensureListeningTo(rootContainerElement, propKey);
      }
    } else if (nextProp != null) {
      setValueForProperty(domElement, propKey, nextProp, isCustomComponentTag);
    }
  }
}

function updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag) {
  // TODO: Handle wasCustomComponentTag
  for (var i = 0; i < updatePayload.length; i += 2) {
    var propKey = updatePayload[i];
    var propValue = updatePayload[i + 1];

    if (propKey === STYLE) {
      setValueForStyles(domElement, propValue);
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      setInnerHTML(domElement, propValue);
    } else if (propKey === CHILDREN) {
      setTextContent(domElement, propValue);
    } else {
      setValueForProperty(domElement, propKey, propValue, isCustomComponentTag);
    }
  }
}

function createElement(type, props, rootContainerElement, parentNamespace) {
  var isCustomComponentTag; // We create tags in the namespace of their parent container, except HTML
  // tags get no namespace.

  var ownerDocument = getOwnerDocumentFromRootContainer(rootContainerElement);
  var domElement;
  var namespaceURI = parentNamespace;

  if (namespaceURI === HTML_NAMESPACE$1) {
    namespaceURI = getIntrinsicNamespace(type);
  }

  if (namespaceURI === HTML_NAMESPACE$1) {
    {
      isCustomComponentTag = isCustomComponent(type, props); // Should this check be gated by parent namespace? Not sure we want to
      // allow <SVG> or <mATH>.

      if (!isCustomComponentTag && type !== type.toLowerCase()) {
        error('<%s /> is using incorrect casing. ' + 'Use PascalCase for React components, ' + 'or lowercase for HTML elements.', type);
      }
    }

    if (type === 'script') {
      // Create the script via .innerHTML so its "parser-inserted" flag is
      // set to true and it does not execute
      var div = ownerDocument.createElement('div');

      div.innerHTML = '<script><' + '/script>'; // eslint-disable-line
      // This is guaranteed to yield a script element.

      var firstChild = div.firstChild;
      domElement = div.removeChild(firstChild);
    } else if (typeof props.is === 'string') {
      // $FlowIssue `createElement` should be updated for Web Components
      domElement = ownerDocument.createElement(type, {
        is: props.is
      });
    } else {
      // Separate else branch instead of using `props.is || undefined` above because of a Firefox bug.
      // See discussion in https://github.com/facebook/react/pull/6896
      // and discussion in https://bugzilla.mozilla.org/show_bug.cgi?id=1276240
      domElement = ownerDocument.createElement(type); // Normally attributes are assigned in `setInitialDOMProperties`, however the `multiple` and `size`
      // attributes on `select`s needs to be added before `option`s are inserted.
      // This prevents:
      // - a bug where the `select` does not scroll to the correct option because singular
      //  `select` elements automatically pick the first item #13222
      // - a bug where the `select` set the first item as selected despite the `size` attribute #14239
      // See https://github.com/facebook/react/issues/13222
      // and https://github.com/facebook/react/issues/14239

      if (type === 'select') {
        var node = domElement;

        if (props.multiple) {
          node.multiple = true;
        } else if (props.size) {
          // Setting a size greater than 1 causes a select to behave like `multiple=true`, where
          // it is possible that no option is selected.
          //
          // This is only necessary when a select in "single selection mode".
          node.size = props.size;
        }
      }
    }
  } else {
    domElement = ownerDocument.createElementNS(namespaceURI, type);
  }

  {
    if (namespaceURI === HTML_NAMESPACE$1) {
      if (!isCustomComponentTag && Object.prototype.toString.call(domElement) === '[object HTMLUnknownElement]' && !Object.prototype.hasOwnProperty.call(warnedUnknownTags, type)) {
        warnedUnknownTags[type] = true;

        error('The tag <%s> is unrecognized in this browser. ' + 'If you meant to render a React component, start its name with ' + 'an uppercase letter.', type);
      }
    }
  }

  return domElement;
}
function createTextNode(text, rootContainerElement) {
  return getOwnerDocumentFromRootContainer(rootContainerElement).createTextNode(text);
}
function setInitialProperties(domElement, tag, rawProps, rootContainerElement) {
  var isCustomComponentTag = isCustomComponent(tag, rawProps);

  {
    validatePropertiesInDevelopment(tag, rawProps);

    if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
      error('%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerNameInDevOrNull() || 'A component');

      didWarnShadyDOM = true;
    }
  } // TODO: Make sure that we check isMounted before firing any of these events.


  var props;

  switch (tag) {
    case 'iframe':
    case 'object':
    case 'embed':
      trapBubbledEvent(TOP_LOAD, domElement);
      props = rawProps;
      break;

    case 'video':
    case 'audio':
      // Create listener for each media event
      for (var i = 0; i < mediaEventTypes.length; i++) {
        trapBubbledEvent(mediaEventTypes[i], domElement);
      }

      props = rawProps;
      break;

    case 'source':
      trapBubbledEvent(TOP_ERROR, domElement);
      props = rawProps;
      break;

    case 'img':
    case 'image':
    case 'link':
      trapBubbledEvent(TOP_ERROR, domElement);
      trapBubbledEvent(TOP_LOAD, domElement);
      props = rawProps;
      break;

    case 'form':
      trapBubbledEvent(TOP_RESET, domElement);
      trapBubbledEvent(TOP_SUBMIT, domElement);
      props = rawProps;
      break;

    case 'details':
      trapBubbledEvent(TOP_TOGGLE, domElement);
      props = rawProps;
      break;

    case 'input':
      initWrapperState(domElement, rawProps);
      props = getHostProps(domElement, rawProps);
      trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.

      ensureListeningTo(rootContainerElement, 'onChange');
      break;

    case 'option':
      validateProps(domElement, rawProps);
      props = getHostProps$1(domElement, rawProps);
      break;

    case 'select':
      initWrapperState$1(domElement, rawProps);
      props = getHostProps$2(domElement, rawProps);
      trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.

      ensureListeningTo(rootContainerElement, 'onChange');
      break;

    case 'textarea':
      initWrapperState$2(domElement, rawProps);
      props = getHostProps$3(domElement, rawProps);
      trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.

      ensureListeningTo(rootContainerElement, 'onChange');
      break;

    default:
      props = rawProps;
  }

  assertValidProps(tag, props);
  setInitialDOMProperties(tag, domElement, rootContainerElement, props, isCustomComponentTag);

  switch (tag) {
    case 'input':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper(domElement, rawProps, false);
      break;

    case 'textarea':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper$3(domElement);
      break;

    case 'option':
      postMountWrapper$1(domElement, rawProps);
      break;

    case 'select':
      postMountWrapper$2(domElement, rawProps);
      break;

    default:
      if (typeof props.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }

      break;
  }
} // Calculate the diff between the two objects.

function diffProperties(domElement, tag, lastRawProps, nextRawProps, rootContainerElement) {
  {
    validatePropertiesInDevelopment(tag, nextRawProps);
  }

  var updatePayload = null;
  var lastProps;
  var nextProps;

  switch (tag) {
    case 'input':
      lastProps = getHostProps(domElement, lastRawProps);
      nextProps = getHostProps(domElement, nextRawProps);
      updatePayload = [];
      break;

    case 'option':
      lastProps = getHostProps$1(domElement, lastRawProps);
      nextProps = getHostProps$1(domElement, nextRawProps);
      updatePayload = [];
      break;

    case 'select':
      lastProps = getHostProps$2(domElement, lastRawProps);
      nextProps = getHostProps$2(domElement, nextRawProps);
      updatePayload = [];
      break;

    case 'textarea':
      lastProps = getHostProps$3(domElement, lastRawProps);
      nextProps = getHostProps$3(domElement, nextRawProps);
      updatePayload = [];
      break;

    default:
      lastProps = lastRawProps;
      nextProps = nextRawProps;

      if (typeof lastProps.onClick !== 'function' && typeof nextProps.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }

      break;
  }

  assertValidProps(tag, nextProps);
  var propKey;
  var styleName;
  var styleUpdates = null;

  for (propKey in lastProps) {
    if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey) || lastProps[propKey] == null) {
      continue;
    }

    if (propKey === STYLE) {
      var lastStyle = lastProps[propKey];

      for (styleName in lastStyle) {
        if (lastStyle.hasOwnProperty(styleName)) {
          if (!styleUpdates) {
            styleUpdates = {};
          }

          styleUpdates[styleName] = '';
        }
      }
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML || propKey === CHILDREN) ; else if ( propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ; else if (propKey === AUTOFOCUS) ; else if (registrationNameModules.hasOwnProperty(propKey)) {
      // This is a special case. If any listener updates we need to ensure
      // that the "current" fiber pointer gets updated so we need a commit
      // to update this element.
      if (!updatePayload) {
        updatePayload = [];
      }
    } else {
      // For all other deleted properties we add it to the queue. We use
      // the whitelist in the commit phase instead.
      (updatePayload = updatePayload || []).push(propKey, null);
    }
  }

  for (propKey in nextProps) {
    var nextProp = nextProps[propKey];
    var lastProp = lastProps != null ? lastProps[propKey] : undefined;

    if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp || nextProp == null && lastProp == null) {
      continue;
    }

    if (propKey === STYLE) {
      {
        if (nextProp) {
          // Freeze the next style object so that we can assume it won't be
          // mutated. We have already warned for this in the past.
          Object.freeze(nextProp);
        }
      }

      if (lastProp) {
        // Unset styles on `lastProp` but not on `nextProp`.
        for (styleName in lastProp) {
          if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
            if (!styleUpdates) {
              styleUpdates = {};
            }

            styleUpdates[styleName] = '';
          }
        } // Update styles that changed since `lastProp`.


        for (styleName in nextProp) {
          if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
            if (!styleUpdates) {
              styleUpdates = {};
            }

            styleUpdates[styleName] = nextProp[styleName];
          }
        }
      } else {
        // Relies on `updateStylesByID` not mutating `styleUpdates`.
        if (!styleUpdates) {
          if (!updatePayload) {
            updatePayload = [];
          }

          updatePayload.push(propKey, styleUpdates);
        }

        styleUpdates = nextProp;
      }
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      var nextHtml = nextProp ? nextProp[HTML$1] : undefined;
      var lastHtml = lastProp ? lastProp[HTML$1] : undefined;

      if (nextHtml != null) {
        if (lastHtml !== nextHtml) {
          (updatePayload = updatePayload || []).push(propKey, nextHtml);
        }
      }
    } else if (propKey === CHILDREN) {
      if (lastProp !== nextProp && (typeof nextProp === 'string' || typeof nextProp === 'number')) {
        (updatePayload = updatePayload || []).push(propKey, '' + nextProp);
      }
    } else if ( propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING) ; else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        // We eagerly listen to this even though we haven't committed yet.
        if ( typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }

        ensureListeningTo(rootContainerElement, propKey);
      }

      if (!updatePayload && lastProp !== nextProp) {
        // This is a special case. If any listener updates we need to ensure
        // that the "current" props pointer gets updated so we need a commit
        // to update this element.
        updatePayload = [];
      }
    } else {
      // For any other property we always add it to the queue and then we
      // filter it out using the whitelist during the commit.
      (updatePayload = updatePayload || []).push(propKey, nextProp);
    }
  }

  if (styleUpdates) {
    {
      validateShorthandPropertyCollisionInDev(styleUpdates, nextProps[STYLE]);
    }

    (updatePayload = updatePayload || []).push(STYLE, styleUpdates);
  }

  return updatePayload;
} // Apply the diff.

function updateProperties(domElement, updatePayload, tag, lastRawProps, nextRawProps) {
  // Update checked *before* name.
  // In the middle of an update, it is possible to have multiple checked.
  // When a checked radio tries to change name, browser makes another radio's checked false.
  if (tag === 'input' && nextRawProps.type === 'radio' && nextRawProps.name != null) {
    updateChecked(domElement, nextRawProps);
  }

  var wasCustomComponentTag = isCustomComponent(tag, lastRawProps);
  var isCustomComponentTag = isCustomComponent(tag, nextRawProps); // Apply the diff.

  updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag); // TODO: Ensure that an update gets scheduled if any of the special props
  // changed.

  switch (tag) {
    case 'input':
      // Update the wrapper around inputs *after* updating props. This has to
      // happen after `updateDOMProperties`. Otherwise HTML5 input validations
      // raise warnings and prevent the new value from being assigned.
      updateWrapper(domElement, nextRawProps);
      break;

    case 'textarea':
      updateWrapper$1(domElement, nextRawProps);
      break;

    case 'select':
      // <select> value update needs to occur after <option> children
      // reconciliation
      postUpdateWrapper(domElement, nextRawProps);
      break;
  }
}

function getPossibleStandardName(propName) {
  {
    var lowerCasedName = propName.toLowerCase();

    if (!possibleStandardNames.hasOwnProperty(lowerCasedName)) {
      return null;
    }

    return possibleStandardNames[lowerCasedName] || null;
  }
}

function diffHydratedProperties(domElement, tag, rawProps, parentNamespace, rootContainerElement) {
  var isCustomComponentTag;
  var extraAttributeNames;

  {
    suppressHydrationWarning = rawProps[SUPPRESS_HYDRATION_WARNING] === true;
    isCustomComponentTag = isCustomComponent(tag, rawProps);
    validatePropertiesInDevelopment(tag, rawProps);

    if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
      error('%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerNameInDevOrNull() || 'A component');

      didWarnShadyDOM = true;
    }
  } // TODO: Make sure that we check isMounted before firing any of these events.


  switch (tag) {
    case 'iframe':
    case 'object':
    case 'embed':
      trapBubbledEvent(TOP_LOAD, domElement);
      break;

    case 'video':
    case 'audio':
      // Create listener for each media event
      for (var i = 0; i < mediaEventTypes.length; i++) {
        trapBubbledEvent(mediaEventTypes[i], domElement);
      }

      break;

    case 'source':
      trapBubbledEvent(TOP_ERROR, domElement);
      break;

    case 'img':
    case 'image':
    case 'link':
      trapBubbledEvent(TOP_ERROR, domElement);
      trapBubbledEvent(TOP_LOAD, domElement);
      break;

    case 'form':
      trapBubbledEvent(TOP_RESET, domElement);
      trapBubbledEvent(TOP_SUBMIT, domElement);
      break;

    case 'details':
      trapBubbledEvent(TOP_TOGGLE, domElement);
      break;

    case 'input':
      initWrapperState(domElement, rawProps);
      trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.

      ensureListeningTo(rootContainerElement, 'onChange');
      break;

    case 'option':
      validateProps(domElement, rawProps);
      break;

    case 'select':
      initWrapperState$1(domElement, rawProps);
      trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.

      ensureListeningTo(rootContainerElement, 'onChange');
      break;

    case 'textarea':
      initWrapperState$2(domElement, rawProps);
      trapBubbledEvent(TOP_INVALID, domElement); // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.

      ensureListeningTo(rootContainerElement, 'onChange');
      break;
  }

  assertValidProps(tag, rawProps);

  {
    extraAttributeNames = new Set();
    var attributes = domElement.attributes;

    for (var _i = 0; _i < attributes.length; _i++) {
      var name = attributes[_i].name.toLowerCase();

      switch (name) {
        // Built-in SSR attribute is whitelisted
        case 'data-reactroot':
          break;
        // Controlled attributes are not validated
        // TODO: Only ignore them on controlled tags.

        case 'value':
          break;

        case 'checked':
          break;

        case 'selected':
          break;

        default:
          // Intentionally use the original name.
          // See discussion in https://github.com/facebook/react/pull/10676.
          extraAttributeNames.add(attributes[_i].name);
      }
    }
  }

  var updatePayload = null;

  for (var propKey in rawProps) {
    if (!rawProps.hasOwnProperty(propKey)) {
      continue;
    }

    var nextProp = rawProps[propKey];

    if (propKey === CHILDREN) {
      // For text content children we compare against textContent. This
      // might match additional HTML that is hidden when we read it using
      // textContent. E.g. "foo" will match "f<span>oo</span>" but that still
      // satisfies our requirement. Our requirement is not to produce perfect
      // HTML and attributes. Ideally we should preserve structure but it's
      // ok not to if the visible content is still enough to indicate what
      // even listeners these nodes might be wired up to.
      // TODO: Warn if there is more than a single textNode as a child.
      // TODO: Should we use domElement.firstChild.nodeValue to compare?
      if (typeof nextProp === 'string') {
        if (domElement.textContent !== nextProp) {
          if ( !suppressHydrationWarning) {
            warnForTextDifference(domElement.textContent, nextProp);
          }

          updatePayload = [CHILDREN, nextProp];
        }
      } else if (typeof nextProp === 'number') {
        if (domElement.textContent !== '' + nextProp) {
          if ( !suppressHydrationWarning) {
            warnForTextDifference(domElement.textContent, nextProp);
          }

          updatePayload = [CHILDREN, '' + nextProp];
        }
      }
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        if ( typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }

        ensureListeningTo(rootContainerElement, propKey);
      }
    } else if ( // Convince Flow we've calculated it (it's DEV-only in this method.)
    typeof isCustomComponentTag === 'boolean') {
      // Validate that the properties correspond to their expected values.
      var serverValue = void 0;
      var propertyInfo = getPropertyInfo(propKey);

      if (suppressHydrationWarning) ; else if ( propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING || // Controlled attributes are not validated
      // TODO: Only ignore them on controlled tags.
      propKey === 'value' || propKey === 'checked' || propKey === 'selected') ; else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
        var serverHTML = domElement.innerHTML;
        var nextHtml = nextProp ? nextProp[HTML$1] : undefined;
        var expectedHTML = normalizeHTML(domElement, nextHtml != null ? nextHtml : '');

        if (expectedHTML !== serverHTML) {
          warnForPropDifference(propKey, serverHTML, expectedHTML);
        }
      } else if (propKey === STYLE) {
        // $FlowFixMe - Should be inferred as not undefined.
        extraAttributeNames.delete(propKey);

        if (canDiffStyleForHydrationWarning) {
          var expectedStyle = createDangerousStringForStyles(nextProp);
          serverValue = domElement.getAttribute('style');

          if (expectedStyle !== serverValue) {
            warnForPropDifference(propKey, serverValue, expectedStyle);
          }
        }
      } else if (isCustomComponentTag) {
        // $FlowFixMe - Should be inferred as not undefined.
        extraAttributeNames.delete(propKey.toLowerCase());
        serverValue = getValueForAttribute(domElement, propKey, nextProp);

        if (nextProp !== serverValue) {
          warnForPropDifference(propKey, serverValue, nextProp);
        }
      } else if (!shouldIgnoreAttribute(propKey, propertyInfo, isCustomComponentTag) && !shouldRemoveAttribute(propKey, nextProp, propertyInfo, isCustomComponentTag)) {
        var isMismatchDueToBadCasing = false;

        if (propertyInfo !== null) {
          // $FlowFixMe - Should be inferred as not undefined.
          extraAttributeNames.delete(propertyInfo.attributeName);
          serverValue = getValueForProperty(domElement, propKey, nextProp, propertyInfo);
        } else {
          var ownNamespace = parentNamespace;

          if (ownNamespace === HTML_NAMESPACE$1) {
            ownNamespace = getIntrinsicNamespace(tag);
          }

          if (ownNamespace === HTML_NAMESPACE$1) {
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames.delete(propKey.toLowerCase());
          } else {
            var standardName = getPossibleStandardName(propKey);

            if (standardName !== null && standardName !== propKey) {
              // If an SVG prop is supplied with bad casing, it will
              // be successfully parsed from HTML, but will produce a mismatch
              // (and would be incorrectly rendered on the client).
              // However, we already warn about bad casing elsewhere.
              // So we'll skip the misleading extra mismatch warning in this case.
              isMismatchDueToBadCasing = true; // $FlowFixMe - Should be inferred as not undefined.

              extraAttributeNames.delete(standardName);
            } // $FlowFixMe - Should be inferred as not undefined.


            extraAttributeNames.delete(propKey);
          }

          serverValue = getValueForAttribute(domElement, propKey, nextProp);
        }

        if (nextProp !== serverValue && !isMismatchDueToBadCasing) {
          warnForPropDifference(propKey, serverValue, nextProp);
        }
      }
    }
  }

  {
    // $FlowFixMe - Should be inferred as not undefined.
    if (extraAttributeNames.size > 0 && !suppressHydrationWarning) {
      // $FlowFixMe - Should be inferred as not undefined.
      warnForExtraAttributes(extraAttributeNames);
    }
  }

  switch (tag) {
    case 'input':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper(domElement, rawProps, true);
      break;

    case 'textarea':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper$3(domElement);
      break;

    case 'select':
    case 'option':
      // For input and textarea we current always set the value property at
      // post mount to force it to diverge from attributes. However, for
      // option and select we don't quite do the same thing and select
      // is not resilient to the DOM state changing so we don't do that here.
      // TODO: Consider not doing this for input and textarea.
      break;

    default:
      if (typeof rawProps.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }

      break;
  }

  return updatePayload;
}
function diffHydratedText(textNode, text) {
  var isDifferent = textNode.nodeValue !== text;
  return isDifferent;
}
function warnForUnmatchedText(textNode, text) {
  {
    warnForTextDifference(textNode.nodeValue, text);
  }
}
function warnForDeletedHydratableElement(parentNode, child) {
  {
    if (didWarnInvalidHydration) {
      return;
    }

    didWarnInvalidHydration = true;

    error('Did not expect server HTML to contain a <%s> in <%s>.', child.nodeName.toLowerCase(), parentNode.nodeName.toLowerCase());
  }
}
function warnForDeletedHydratableText(parentNode, child) {
  {
    if (didWarnInvalidHydration) {
      return;
    }

    didWarnInvalidHydration = true;

    error('Did not expect server HTML to contain the text node "%s" in <%s>.', child.nodeValue, parentNode.nodeName.toLowerCase());
  }
}
function warnForInsertedHydratedElement(parentNode, tag, props) {
  {
    if (didWarnInvalidHydration) {
      return;
    }

    didWarnInvalidHydration = true;

    error('Expected server HTML to contain a matching <%s> in <%s>.', tag, parentNode.nodeName.toLowerCase());
  }
}
function warnForInsertedHydratedText(parentNode, text) {
  {
    if (text === '') {
      // We expect to insert empty text nodes since they're not represented in
      // the HTML.
      // TODO: Remove this special case if we can just avoid inserting empty
      // text nodes.
      return;
    }

    if (didWarnInvalidHydration) {
      return;
    }

    didWarnInvalidHydration = true;

    error('Expected server HTML to contain a matching text node for "%s" in <%s>.', text, parentNode.nodeName.toLowerCase());
  }
}
function restoreControlledState$3(domElement, tag, props) {
  switch (tag) {
    case 'input':
      restoreControlledState(domElement, props);
      return;

    case 'textarea':
      restoreControlledState$2(domElement, props);
      return;

    case 'select':
      restoreControlledState$1(domElement, props);
      return;
  }
}

function getActiveElement(doc) {
  doc = doc || (typeof document !== 'undefined' ? document : undefined);

  if (typeof doc === 'undefined') {
    return null;
  }

  try {
    return doc.activeElement || doc.body;
  } catch (e) {
    return doc.body;
  }
}

/**
 * Given any node return the first leaf node without children.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {DOMElement|DOMTextNode}
 */

function getLeafNode(node) {
  while (node && node.firstChild) {
    node = node.firstChild;
  }

  return node;
}
/**
 * Get the next sibling within a container. This will walk up the
 * DOM if a node's siblings have been exhausted.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {?DOMElement|DOMTextNode}
 */


function getSiblingNode(node) {
  while (node) {
    if (node.nextSibling) {
      return node.nextSibling;
    }

    node = node.parentNode;
  }
}
/**
 * Get object describing the nodes which contain characters at offset.
 *
 * @param {DOMElement|DOMTextNode} root
 * @param {number} offset
 * @return {?object}
 */


function getNodeForCharacterOffset(root, offset) {
  var node = getLeafNode(root);
  var nodeStart = 0;
  var nodeEnd = 0;

  while (node) {
    if (node.nodeType === TEXT_NODE) {
      nodeEnd = nodeStart + node.textContent.length;

      if (nodeStart <= offset && nodeEnd >= offset) {
        return {
          node: node,
          offset: offset - nodeStart
        };
      }

      nodeStart = nodeEnd;
    }

    node = getLeafNode(getSiblingNode(node));
  }
}

/**
 * @param {DOMElement} outerNode
 * @return {?object}
 */

function getOffsets(outerNode) {
  var ownerDocument = outerNode.ownerDocument;
  var win = ownerDocument && ownerDocument.defaultView || window;
  var selection = win.getSelection && win.getSelection();

  if (!selection || selection.rangeCount === 0) {
    return null;
  }

  var anchorNode = selection.anchorNode,
      anchorOffset = selection.anchorOffset,
      focusNode = selection.focusNode,
      focusOffset = selection.focusOffset; // In Firefox, anchorNode and focusNode can be "anonymous divs", e.g. the
  // up/down buttons on an <input type="number">. Anonymous divs do not seem to
  // expose properties, triggering a "Permission denied error" if any of its
  // properties are accessed. The only seemingly possible way to avoid erroring
  // is to access a property that typically works for non-anonymous divs and
  // catch any error that may otherwise arise. See
  // https://bugzilla.mozilla.org/show_bug.cgi?id=208427

  try {
    /* eslint-disable no-unused-expressions */
    anchorNode.nodeType;
    focusNode.nodeType;
    /* eslint-enable no-unused-expressions */
  } catch (e) {
    return null;
  }

  return getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset);
}
/**
 * Returns {start, end} where `start` is the character/codepoint index of
 * (anchorNode, anchorOffset) within the textContent of `outerNode`, and
 * `end` is the index of (focusNode, focusOffset).
 *
 * Returns null if you pass in garbage input but we should probably just crash.
 *
 * Exported only for testing.
 */

function getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode, focusOffset) {
  var length = 0;
  var start = -1;
  var end = -1;
  var indexWithinAnchor = 0;
  var indexWithinFocus = 0;
  var node = outerNode;
  var parentNode = null;

  outer: while (true) {
    var next = null;

    while (true) {
      if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) {
        start = length + anchorOffset;
      }

      if (node === focusNode && (focusOffset === 0 || node.nodeType === TEXT_NODE)) {
        end = length + focusOffset;
      }

      if (node.nodeType === TEXT_NODE) {
        length += node.nodeValue.length;
      }

      if ((next = node.firstChild) === null) {
        break;
      } // Moving from `node` to its first child `next`.


      parentNode = node;
      node = next;
    }

    while (true) {
      if (node === outerNode) {
        // If `outerNode` has children, this is always the second time visiting
        // it. If it has no children, this is still the first loop, and the only
        // valid selection is anchorNode and focusNode both equal to this node
        // and both offsets 0, in which case we will have handled above.
        break outer;
      }

      if (parentNode === anchorNode && ++indexWithinAnchor === anchorOffset) {
        start = length;
      }

      if (parentNode === focusNode && ++indexWithinFocus === focusOffset) {
        end = length;
      }

      if ((next = node.nextSibling) !== null) {
        break;
      }

      node = parentNode;
      parentNode = node.parentNode;
    } // Moving from `node` to its next sibling `next`.


    node = next;
  }

  if (start === -1 || end === -1) {
    // This should never happen. (Would happen if the anchor/focus nodes aren't
    // actually inside the passed-in node.)
    return null;
  }

  return {
    start: start,
    end: end
  };
}
/**
 * In modern non-IE browsers, we can support both forward and backward
 * selections.
 *
 * Note: IE10+ supports the Selection object, but it does not support
 * the `extend` method, which means that even in modern IE, it's not possible
 * to programmatically create a backward selection. Thus, for all IE
 * versions, we use the old IE API to create our selections.
 *
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */

function setOffsets(node, offsets) {
  var doc = node.ownerDocument || document;
  var win = doc && doc.defaultView || window; // Edge fails with "Object expected" in some scenarios.
  // (For instance: TinyMCE editor used in a list component that supports pasting to add more,
  // fails when pasting 100+ items)

  if (!win.getSelection) {
    return;
  }

  var selection = win.getSelection();
  var length = node.textContent.length;
  var start = Math.min(offsets.start, length);
  var end = offsets.end === undefined ? start : Math.min(offsets.end, length); // IE 11 uses modern selection, but doesn't support the extend method.
  // Flip backward selections, so we can set with a single range.

  if (!selection.extend && start > end) {
    var temp = end;
    end = start;
    start = temp;
  }

  var startMarker = getNodeForCharacterOffset(node, start);
  var endMarker = getNodeForCharacterOffset(node, end);

  if (startMarker && endMarker) {
    if (selection.rangeCount === 1 && selection.anchorNode === startMarker.node && selection.anchorOffset === startMarker.offset && selection.focusNode === endMarker.node && selection.focusOffset === endMarker.offset) {
      return;
    }

    var range = doc.createRange();
    range.setStart(startMarker.node, startMarker.offset);
    selection.removeAllRanges();

    if (start > end) {
      selection.addRange(range);
      selection.extend(endMarker.node, endMarker.offset);
    } else {
      range.setEnd(endMarker.node, endMarker.offset);
      selection.addRange(range);
    }
  }
}

function isTextNode(node) {
  return node && node.nodeType === TEXT_NODE;
}

function containsNode(outerNode, innerNode) {
  if (!outerNode || !innerNode) {
    return false;
  } else if (outerNode === innerNode) {
    return true;
  } else if (isTextNode(outerNode)) {
    return false;
  } else if (isTextNode(innerNode)) {
    return containsNode(outerNode, innerNode.parentNode);
  } else if ('contains' in outerNode) {
    return outerNode.contains(innerNode);
  } else if (outerNode.compareDocumentPosition) {
    return !!(outerNode.compareDocumentPosition(innerNode) & 16);
  } else {
    return false;
  }
}

function isInDocument(node) {
  return node && node.ownerDocument && containsNode(node.ownerDocument.documentElement, node);
}

function isSameOriginFrame(iframe) {
  try {
    // Accessing the contentDocument of a HTMLIframeElement can cause the browser
    // to throw, e.g. if it has a cross-origin src attribute.
    // Safari will show an error in the console when the access results in "Blocked a frame with origin". e.g:
    // iframe.contentDocument.defaultView;
    // A safety way is to access one of the cross origin properties: Window or Location
    // Which might result in "SecurityError" DOM Exception and it is compatible to Safari.
    // https://html.spec.whatwg.org/multipage/browsers.html#integration-with-idl
    return typeof iframe.contentWindow.location.href === 'string';
  } catch (err) {
    return false;
  }
}

function getActiveElementDeep() {
  var win = window;
  var element = getActiveElement();

  while (element instanceof win.HTMLIFrameElement) {
    if (isSameOriginFrame(element)) {
      win = element.contentWindow;
    } else {
      return element;
    }

    element = getActiveElement(win.document);
  }

  return element;
}
/**
 * @ReactInputSelection: React input selection module. Based on Selection.js,
 * but modified to be suitable for react and has a couple of bug fixes (doesn't
 * assume buttons have range selections allowed).
 * Input selection module for React.
 */

/**
 * @hasSelectionCapabilities: we get the element types that support selection
 * from https://html.spec.whatwg.org/#do-not-apply, looking at `selectionStart`
 * and `selectionEnd` rows.
 */


function hasSelectionCapabilities(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName && (nodeName === 'input' && (elem.type === 'text' || elem.type === 'search' || elem.type === 'tel' || elem.type === 'url' || elem.type === 'password') || nodeName === 'textarea' || elem.contentEditable === 'true');
}
function getSelectionInformation() {
  var focusedElem = getActiveElementDeep();
  return {
    // Used by Flare
    activeElementDetached: null,
    focusedElem: focusedElem,
    selectionRange: hasSelectionCapabilities(focusedElem) ? getSelection(focusedElem) : null
  };
}
/**
 * @restoreSelection: If any selection information was potentially lost,
 * restore it. This is useful when performing operations that could remove dom
 * nodes and place them back in, resulting in focus being lost.
 */

function restoreSelection(priorSelectionInformation) {
  var curFocusedElem = getActiveElementDeep();
  var priorFocusedElem = priorSelectionInformation.focusedElem;
  var priorSelectionRange = priorSelectionInformation.selectionRange;

  if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
    if (priorSelectionRange !== null && hasSelectionCapabilities(priorFocusedElem)) {
      setSelection(priorFocusedElem, priorSelectionRange);
    } // Focusing a node can change the scroll position, which is undesirable


    var ancestors = [];
    var ancestor = priorFocusedElem;

    while (ancestor = ancestor.parentNode) {
      if (ancestor.nodeType === ELEMENT_NODE) {
        ancestors.push({
          element: ancestor,
          left: ancestor.scrollLeft,
          top: ancestor.scrollTop
        });
      }
    }

    if (typeof priorFocusedElem.focus === 'function') {
      priorFocusedElem.focus();
    }

    for (var i = 0; i < ancestors.length; i++) {
      var info = ancestors[i];
      info.element.scrollLeft = info.left;
      info.element.scrollTop = info.top;
    }
  }
}
/**
 * @getSelection: Gets the selection bounds of a focused textarea, input or
 * contentEditable node.
 * -@input: Look up selection bounds of this input
 * -@return {start: selectionStart, end: selectionEnd}
 */

function getSelection(input) {
  var selection;

  if ('selectionStart' in input) {
    // Modern browser with input or textarea.
    selection = {
      start: input.selectionStart,
      end: input.selectionEnd
    };
  } else {
    // Content editable or old IE textarea.
    selection = getOffsets(input);
  }

  return selection || {
    start: 0,
    end: 0
  };
}
/**
 * @setSelection: Sets the selection bounds of a textarea or input and focuses
 * the input.
 * -@input     Set selection bounds of this input or textarea
 * -@offsets   Object of same form that is returned from get*
 */

function setSelection(input, offsets) {
  var start = offsets.start,
      end = offsets.end;

  if (end === undefined) {
    end = start;
  }

  if ('selectionStart' in input) {
    input.selectionStart = start;
    input.selectionEnd = Math.min(end, input.value.length);
  } else {
    setOffsets(input, offsets);
  }
}

var validateDOMNesting = function () {};

var updatedAncestorInfo = function () {};

{
  // This validation code was written based on the HTML5 parsing spec:
  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  //
  // Note: this does not catch all invalid nesting, nor does it try to (as it's
  // not clear what practical benefit doing so provides); instead, we warn only
  // for cases where the parser will give a parse tree differing from what React
  // intended. For example, <b><div></div></b> is invalid but we don't warn
  // because it still parses correctly; we do warn for other cases like nested
  // <p> tags where the beginning of the second element implicitly closes the
  // first, causing a confusing mess.
  // https://html.spec.whatwg.org/multipage/syntax.html#special
  var specialTags = ['address', 'applet', 'area', 'article', 'aside', 'base', 'basefont', 'bgsound', 'blockquote', 'body', 'br', 'button', 'caption', 'center', 'col', 'colgroup', 'dd', 'details', 'dir', 'div', 'dl', 'dt', 'embed', 'fieldset', 'figcaption', 'figure', 'footer', 'form', 'frame', 'frameset', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'iframe', 'img', 'input', 'isindex', 'li', 'link', 'listing', 'main', 'marquee', 'menu', 'menuitem', 'meta', 'nav', 'noembed', 'noframes', 'noscript', 'object', 'ol', 'p', 'param', 'plaintext', 'pre', 'script', 'section', 'select', 'source', 'style', 'summary', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'title', 'tr', 'track', 'ul', 'wbr', 'xmp']; // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope

  var inScopeTags = ['applet', 'caption', 'html', 'table', 'td', 'th', 'marquee', 'object', 'template', // https://html.spec.whatwg.org/multipage/syntax.html#html-integration-point
  // TODO: Distinguish by namespace here -- for <title>, including it here
  // errs on the side of fewer warnings
  'foreignObject', 'desc', 'title']; // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-button-scope

  var buttonScopeTags = inScopeTags.concat(['button']); // https://html.spec.whatwg.org/multipage/syntax.html#generate-implied-end-tags

  var impliedEndTags = ['dd', 'dt', 'li', 'option', 'optgroup', 'p', 'rp', 'rt'];
  var emptyAncestorInfo = {
    current: null,
    formTag: null,
    aTagInScope: null,
    buttonTagInScope: null,
    nobrTagInScope: null,
    pTagInButtonScope: null,
    listItemTagAutoclosing: null,
    dlItemTagAutoclosing: null
  };

  updatedAncestorInfo = function (oldInfo, tag) {
    var ancestorInfo = _assign({}, oldInfo || emptyAncestorInfo);

    var info = {
      tag: tag
    };

    if (inScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.aTagInScope = null;
      ancestorInfo.buttonTagInScope = null;
      ancestorInfo.nobrTagInScope = null;
    }

    if (buttonScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.pTagInButtonScope = null;
    } // See rules for 'li', 'dd', 'dt' start tags in
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody


    if (specialTags.indexOf(tag) !== -1 && tag !== 'address' && tag !== 'div' && tag !== 'p') {
      ancestorInfo.listItemTagAutoclosing = null;
      ancestorInfo.dlItemTagAutoclosing = null;
    }

    ancestorInfo.current = info;

    if (tag === 'form') {
      ancestorInfo.formTag = info;
    }

    if (tag === 'a') {
      ancestorInfo.aTagInScope = info;
    }

    if (tag === 'button') {
      ancestorInfo.buttonTagInScope = info;
    }

    if (tag === 'nobr') {
      ancestorInfo.nobrTagInScope = info;
    }

    if (tag === 'p') {
      ancestorInfo.pTagInButtonScope = info;
    }

    if (tag === 'li') {
      ancestorInfo.listItemTagAutoclosing = info;
    }

    if (tag === 'dd' || tag === 'dt') {
      ancestorInfo.dlItemTagAutoclosing = info;
    }

    return ancestorInfo;
  };
  /**
   * Returns whether
   */


  var isTagValidWithParent = function (tag, parentTag) {
    // First, let's check if we're in an unusual parsing mode...
    switch (parentTag) {
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
      case 'select':
        return tag === 'option' || tag === 'optgroup' || tag === '#text';

      case 'optgroup':
        return tag === 'option' || tag === '#text';
      // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
      // but

      case 'option':
        return tag === '#text';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
      // No special behavior since these rules fall back to "in body" mode for
      // all except special table nodes which cause bad parsing behavior anyway.
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr

      case 'tr':
        return tag === 'th' || tag === 'td' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody

      case 'tbody':
      case 'thead':
      case 'tfoot':
        return tag === 'tr' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup

      case 'colgroup':
        return tag === 'col' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable

      case 'table':
        return tag === 'caption' || tag === 'colgroup' || tag === 'tbody' || tag === 'tfoot' || tag === 'thead' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead

      case 'head':
        return tag === 'base' || tag === 'basefont' || tag === 'bgsound' || tag === 'link' || tag === 'meta' || tag === 'title' || tag === 'noscript' || tag === 'noframes' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element

      case 'html':
        return tag === 'head' || tag === 'body' || tag === 'frameset';

      case 'frameset':
        return tag === 'frame';

      case '#document':
        return tag === 'html';
    } // Probably in the "in body" parsing mode, so we outlaw only tag combos
    // where the parsing rules cause implicit opens or closes to be added.
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody


    switch (tag) {
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return parentTag !== 'h1' && parentTag !== 'h2' && parentTag !== 'h3' && parentTag !== 'h4' && parentTag !== 'h5' && parentTag !== 'h6';

      case 'rp':
      case 'rt':
        return impliedEndTags.indexOf(parentTag) === -1;

      case 'body':
      case 'caption':
      case 'col':
      case 'colgroup':
      case 'frameset':
      case 'frame':
      case 'head':
      case 'html':
      case 'tbody':
      case 'td':
      case 'tfoot':
      case 'th':
      case 'thead':
      case 'tr':
        // These tags are only valid with a few parents that have special child
        // parsing rules -- if we're down here, then none of those matched and
        // so we allow it only if we don't know what the parent is, as all other
        // cases are invalid.
        return parentTag == null;
    }

    return true;
  };
  /**
   * Returns whether
   */


  var findInvalidAncestorForTag = function (tag, ancestorInfo) {
    switch (tag) {
      case 'address':
      case 'article':
      case 'aside':
      case 'blockquote':
      case 'center':
      case 'details':
      case 'dialog':
      case 'dir':
      case 'div':
      case 'dl':
      case 'fieldset':
      case 'figcaption':
      case 'figure':
      case 'footer':
      case 'header':
      case 'hgroup':
      case 'main':
      case 'menu':
      case 'nav':
      case 'ol':
      case 'p':
      case 'section':
      case 'summary':
      case 'ul':
      case 'pre':
      case 'listing':
      case 'table':
      case 'hr':
      case 'xmp':
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return ancestorInfo.pTagInButtonScope;

      case 'form':
        return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;

      case 'li':
        return ancestorInfo.listItemTagAutoclosing;

      case 'dd':
      case 'dt':
        return ancestorInfo.dlItemTagAutoclosing;

      case 'button':
        return ancestorInfo.buttonTagInScope;

      case 'a':
        // Spec says something about storing a list of markers, but it sounds
        // equivalent to this check.
        return ancestorInfo.aTagInScope;

      case 'nobr':
        return ancestorInfo.nobrTagInScope;
    }

    return null;
  };

  var didWarn$1 = {};

  validateDOMNesting = function (childTag, childText, ancestorInfo) {
    ancestorInfo = ancestorInfo || emptyAncestorInfo;
    var parentInfo = ancestorInfo.current;
    var parentTag = parentInfo && parentInfo.tag;

    if (childText != null) {
      if (childTag != null) {
        error('validateDOMNesting: when childText is passed, childTag should be null');
      }

      childTag = '#text';
    }

    var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
    var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
    var invalidParentOrAncestor = invalidParent || invalidAncestor;

    if (!invalidParentOrAncestor) {
      return;
    }

    var ancestorTag = invalidParentOrAncestor.tag;
    var addendum = getCurrentFiberStackInDev();
    var warnKey = !!invalidParent + '|' + childTag + '|' + ancestorTag + '|' + addendum;

    if (didWarn$1[warnKey]) {
      return;
    }

    didWarn$1[warnKey] = true;
    var tagDisplayName = childTag;
    var whitespaceInfo = '';

    if (childTag === '#text') {
      if (/\S/.test(childText)) {
        tagDisplayName = 'Text nodes';
      } else {
        tagDisplayName = 'Whitespace text nodes';
        whitespaceInfo = " Make sure you don't have any extra whitespace between tags on " + 'each line of your source code.';
      }
    } else {
      tagDisplayName = '<' + childTag + '>';
    }

    if (invalidParent) {
      var info = '';

      if (ancestorTag === 'table' && childTag === 'tr') {
        info += ' Add a <tbody>, <thead> or <tfoot> to your code to match the DOM tree generated by ' + 'the browser.';
      }

      error('validateDOMNesting(...): %s cannot appear as a child of <%s>.%s%s', tagDisplayName, ancestorTag, whitespaceInfo, info);
    } else {
      error('validateDOMNesting(...): %s cannot appear as a descendant of ' + '<%s>.', tagDisplayName, ancestorTag);
    }
  };
}

var SUPPRESS_HYDRATION_WARNING$1;

{
  SUPPRESS_HYDRATION_WARNING$1 = 'suppressHydrationWarning';
}

var SUSPENSE_START_DATA = '$';
var SUSPENSE_END_DATA = '/$';
var SUSPENSE_PENDING_START_DATA = '$?';
var SUSPENSE_FALLBACK_START_DATA = '$!';
var STYLE$1 = 'style';
var eventsEnabled = null;
var selectionInformation = null;

function shouldAutoFocusHostComponent(type, props) {
  switch (type) {
    case 'button':
    case 'input':
    case 'select':
    case 'textarea':
      return !!props.autoFocus;
  }

  return false;
}
function getRootHostContext(rootContainerInstance) {
  var type;
  var namespace;
  var nodeType = rootContainerInstance.nodeType;

  switch (nodeType) {
    case DOCUMENT_NODE:
    case DOCUMENT_FRAGMENT_NODE:
      {
        type = nodeType === DOCUMENT_NODE ? '#document' : '#fragment';
        var root = rootContainerInstance.documentElement;
        namespace = root ? root.namespaceURI : getChildNamespace(null, '');
        break;
      }

    default:
      {
        var container = nodeType === COMMENT_NODE ? rootContainerInstance.parentNode : rootContainerInstance;
        var ownNamespace = container.namespaceURI || null;
        type = container.tagName;
        namespace = getChildNamespace(ownNamespace, type);
        break;
      }
  }

  {
    var validatedTag = type.toLowerCase();
    var ancestorInfo = updatedAncestorInfo(null, validatedTag);
    return {
      namespace: namespace,
      ancestorInfo: ancestorInfo
    };
  }
}
function getChildHostContext(parentHostContext, type, rootContainerInstance) {
  {
    var parentHostContextDev = parentHostContext;
    var namespace = getChildNamespace(parentHostContextDev.namespace, type);
    var ancestorInfo = updatedAncestorInfo(parentHostContextDev.ancestorInfo, type);
    return {
      namespace: namespace,
      ancestorInfo: ancestorInfo
    };
  }
}
function getPublicInstance(instance) {
  return instance;
}
function prepareForCommit(containerInfo) {
  eventsEnabled = isEnabled();
  selectionInformation = getSelectionInformation();
  setEnabled(false);
}
function resetAfterCommit(containerInfo) {
  restoreSelection(selectionInformation);
  setEnabled(eventsEnabled);
  eventsEnabled = null;

  selectionInformation = null;
}
function createInstance(type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
  var parentNamespace;

  {
    // TODO: take namespace into account when validating.
    var hostContextDev = hostContext;
    validateDOMNesting(type, null, hostContextDev.ancestorInfo);

    if (typeof props.children === 'string' || typeof props.children === 'number') {
      var string = '' + props.children;
      var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
      validateDOMNesting(null, string, ownAncestorInfo);
    }

    parentNamespace = hostContextDev.namespace;
  }

  var domElement = createElement(type, props, rootContainerInstance, parentNamespace);
  precacheFiberNode(internalInstanceHandle, domElement);
  updateFiberProps(domElement, props);
  return domElement;
}
function appendInitialChild(parentInstance, child) {
  parentInstance.appendChild(child);
}
function finalizeInitialChildren(domElement, type, props, rootContainerInstance, hostContext) {
  setInitialProperties(domElement, type, props, rootContainerInstance);
  return shouldAutoFocusHostComponent(type, props);
}
function prepareUpdate(domElement, type, oldProps, newProps, rootContainerInstance, hostContext) {
  {
    var hostContextDev = hostContext;

    if (typeof newProps.children !== typeof oldProps.children && (typeof newProps.children === 'string' || typeof newProps.children === 'number')) {
      var string = '' + newProps.children;
      var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type);
      validateDOMNesting(null, string, ownAncestorInfo);
    }
  }

  return diffProperties(domElement, type, oldProps, newProps, rootContainerInstance);
}
function shouldSetTextContent(type, props) {
  return type === 'textarea' || type === 'option' || type === 'noscript' || typeof props.children === 'string' || typeof props.children === 'number' || typeof props.dangerouslySetInnerHTML === 'object' && props.dangerouslySetInnerHTML !== null && props.dangerouslySetInnerHTML.__html != null;
}
function shouldDeprioritizeSubtree(type, props) {
  return !!props.hidden;
}
function createTextInstance(text, rootContainerInstance, hostContext, internalInstanceHandle) {
  {
    var hostContextDev = hostContext;
    validateDOMNesting(null, text, hostContextDev.ancestorInfo);
  }

  var textNode = createTextNode(text, rootContainerInstance);
  precacheFiberNode(internalInstanceHandle, textNode);
  return textNode;
}
// if a component just imports ReactDOM (e.g. for findDOMNode).
// Some environments might not have setTimeout or clearTimeout.

var scheduleTimeout = typeof setTimeout === 'function' ? setTimeout : undefined;
var cancelTimeout = typeof clearTimeout === 'function' ? clearTimeout : undefined;
var noTimeout = -1; // -------------------
function commitMount(domElement, type, newProps, internalInstanceHandle) {
  // Despite the naming that might imply otherwise, this method only
  // fires if there is an `Update` effect scheduled during mounting.
  // This happens if `finalizeInitialChildren` returns `true` (which it
  // does to implement the `autoFocus` attribute on the client). But
  // there are also other cases when this might happen (such as patching
  // up text content during hydration mismatch). So we'll check this again.
  if (shouldAutoFocusHostComponent(type, newProps)) {
    domElement.focus();
  }
}
function commitUpdate(domElement, updatePayload, type, oldProps, newProps, internalInstanceHandle) {
  // Update the props handle so that we know which props are the ones with
  // with current event handlers.
  updateFiberProps(domElement, newProps); // Apply the diff to the DOM node.

  updateProperties(domElement, updatePayload, type, oldProps, newProps);
}
function resetTextContent(domElement) {
  setTextContent(domElement, '');
}
function commitTextUpdate(textInstance, oldText, newText) {
  textInstance.nodeValue = newText;
}
function appendChild(parentInstance, child) {
  parentInstance.appendChild(child);
}
function appendChildToContainer(container, child) {
  var parentNode;

  if (container.nodeType === COMMENT_NODE) {
    parentNode = container.parentNode;
    parentNode.insertBefore(child, container);
  } else {
    parentNode = container;
    parentNode.appendChild(child);
  } // This container might be used for a portal.
  // If something inside a portal is clicked, that click should bubble
  // through the React tree. However, on Mobile Safari the click would
  // never bubble through the *DOM* tree unless an ancestor with onclick
  // event exists. So we wouldn't see it and dispatch it.
  // This is why we ensure that non React root containers have inline onclick
  // defined.
  // https://github.com/facebook/react/issues/11918


  var reactRootContainer = container._reactRootContainer;

  if ((reactRootContainer === null || reactRootContainer === undefined) && parentNode.onclick === null) {
    // TODO: This cast may not be sound for SVG, MathML or custom elements.
    trapClickOnNonInteractiveElement(parentNode);
  }
}
function insertBefore(parentInstance, child, beforeChild) {
  parentInstance.insertBefore(child, beforeChild);
}
function insertInContainerBefore(container, child, beforeChild) {
  if (container.nodeType === COMMENT_NODE) {
    container.parentNode.insertBefore(child, beforeChild);
  } else {
    container.insertBefore(child, beforeChild);
  }
}
function removeChild(parentInstance, child) {
  parentInstance.removeChild(child);
}
function removeChildFromContainer(container, child) {
  if (container.nodeType === COMMENT_NODE) {
    container.parentNode.removeChild(child);
  } else {
    container.removeChild(child);
  }
}

function hideInstance(instance) {
  // pass host context to this method?


  instance = instance;
  var style = instance.style;

  if (typeof style.setProperty === 'function') {
    style.setProperty('display', 'none', 'important');
  } else {
    style.display = 'none';
  }
}
function hideTextInstance(textInstance) {
  textInstance.nodeValue = '';
}
function unhideInstance(instance, props) {
  instance = instance;
  var styleProp = props[STYLE$1];
  var display = styleProp !== undefined && styleProp !== null && styleProp.hasOwnProperty('display') ? styleProp.display : null;
  instance.style.display = dangerousStyleValue('display', display);
}
function unhideTextInstance(textInstance, text) {
  textInstance.nodeValue = text;
} // -------------------
function canHydrateInstance(instance, type, props) {
  if (instance.nodeType !== ELEMENT_NODE || type.toLowerCase() !== instance.nodeName.toLowerCase()) {
    return null;
  } // This has now been refined to an element node.


  return instance;
}
function canHydrateTextInstance(instance, text) {
  if (text === '' || instance.nodeType !== TEXT_NODE) {
    // Empty strings are not parsed by HTML so there won't be a correct match here.
    return null;
  } // This has now been refined to a text node.


  return instance;
}
function isSuspenseInstancePending(instance) {
  return instance.data === SUSPENSE_PENDING_START_DATA;
}
function isSuspenseInstanceFallback(instance) {
  return instance.data === SUSPENSE_FALLBACK_START_DATA;
}

function getNextHydratable(node) {
  // Skip non-hydratable nodes.
  for (; node != null; node = node.nextSibling) {
    var nodeType = node.nodeType;

    if (nodeType === ELEMENT_NODE || nodeType === TEXT_NODE) {
      break;
    }
  }

  return node;
}

function getNextHydratableSibling(instance) {
  return getNextHydratable(instance.nextSibling);
}
function getFirstHydratableChild(parentInstance) {
  return getNextHydratable(parentInstance.firstChild);
}
function hydrateInstance(instance, type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
  precacheFiberNode(internalInstanceHandle, instance); // TODO: Possibly defer this until the commit phase where all the events
  // get attached.

  updateFiberProps(instance, props);
  var parentNamespace;

  {
    var hostContextDev = hostContext;
    parentNamespace = hostContextDev.namespace;
  }

  return diffHydratedProperties(instance, type, props, parentNamespace, rootContainerInstance);
}
function hydrateTextInstance(textInstance, text, internalInstanceHandle) {
  precacheFiberNode(internalInstanceHandle, textInstance);
  return diffHydratedText(textInstance, text);
}
function getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance) {
  var node = suspenseInstance.nextSibling; // Skip past all nodes within this suspense boundary.
  // There might be nested nodes so we need to keep track of how
  // deep we are and only break out when we're back on top.

  var depth = 0;

  while (node) {
    if (node.nodeType === COMMENT_NODE) {
      var data = node.data;

      if (data === SUSPENSE_END_DATA) {
        if (depth === 0) {
          return getNextHydratableSibling(node);
        } else {
          depth--;
        }
      } else if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) {
        depth++;
      }
    }

    node = node.nextSibling;
  } // TODO: Warn, we didn't find the end comment boundary.


  return null;
} // Returns the SuspenseInstance if this node is a direct child of a
// SuspenseInstance. I.e. if its previous sibling is a Comment with
// SUSPENSE_x_START_DATA. Otherwise, null.

function getParentSuspenseInstance(targetInstance) {
  var node = targetInstance.previousSibling; // Skip past all nodes within this suspense boundary.
  // There might be nested nodes so we need to keep track of how
  // deep we are and only break out when we're back on top.

  var depth = 0;

  while (node) {
    if (node.nodeType === COMMENT_NODE) {
      var data = node.data;

      if (data === SUSPENSE_START_DATA || data === SUSPENSE_FALLBACK_START_DATA || data === SUSPENSE_PENDING_START_DATA) {
        if (depth === 0) {
          return node;
        } else {
          depth--;
        }
      } else if (data === SUSPENSE_END_DATA) {
        depth++;
      }
    }

    node = node.previousSibling;
  }

  return null;
}
function commitHydratedContainer(container) {
  // Retry if any event replaying was blocked on this.
  retryIfBlockedOn(container);
}
function commitHydratedSuspenseInstance(suspenseInstance) {
  // Retry if any event replaying was blocked on this.
  retryIfBlockedOn(suspenseInstance);
}
function didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, text) {
  {
    warnForUnmatchedText(textInstance, text);
  }
}
function didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, text) {
  if ( parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
    warnForUnmatchedText(textInstance, text);
  }
}
function didNotHydrateContainerInstance(parentContainer, instance) {
  {
    if (instance.nodeType === ELEMENT_NODE) {
      warnForDeletedHydratableElement(parentContainer, instance);
    } else if (instance.nodeType === COMMENT_NODE) ; else {
      warnForDeletedHydratableText(parentContainer, instance);
    }
  }
}
function didNotHydrateInstance(parentType, parentProps, parentInstance, instance) {
  if ( parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
    if (instance.nodeType === ELEMENT_NODE) {
      warnForDeletedHydratableElement(parentInstance, instance);
    } else if (instance.nodeType === COMMENT_NODE) ; else {
      warnForDeletedHydratableText(parentInstance, instance);
    }
  }
}
function didNotFindHydratableContainerInstance(parentContainer, type, props) {
  {
    warnForInsertedHydratedElement(parentContainer, type);
  }
}
function didNotFindHydratableContainerTextInstance(parentContainer, text) {
  {
    warnForInsertedHydratedText(parentContainer, text);
  }
}
function didNotFindHydratableInstance(parentType, parentProps, parentInstance, type, props) {
  if ( parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
    warnForInsertedHydratedElement(parentInstance, type);
  }
}
function didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, text) {
  if ( parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) {
    warnForInsertedHydratedText(parentInstance, text);
  }
}
function didNotFindHydratableSuspenseInstance(parentType, parentProps, parentInstance) {
  if ( parentProps[SUPPRESS_HYDRATION_WARNING$1] !== true) ;
}

var randomKey = Math.random().toString(36).slice(2);
var internalInstanceKey = '__reactInternalInstance$' + randomKey;
var internalEventHandlersKey = '__reactEventHandlers$' + randomKey;
var internalContainerInstanceKey = '__reactContainere$' + randomKey;
function precacheFiberNode(hostInst, node) {
  node[internalInstanceKey] = hostInst;
}
function markContainerAsRoot(hostRoot, node) {
  node[internalContainerInstanceKey] = hostRoot;
}
function unmarkContainerAsRoot(node) {
  node[internalContainerInstanceKey] = null;
}
function isContainerMarkedAsRoot(node) {
  return !!node[internalContainerInstanceKey];
} // Given a DOM node, return the closest HostComponent or HostText fiber ancestor.
// If the target node is part of a hydrated or not yet rendered subtree, then
// this may also return a SuspenseComponent or HostRoot to indicate that.
// Conceptually the HostRoot fiber is a child of the Container node. So if you
// pass the Container node as the targetNode, you will not actually get the
// HostRoot back. To get to the HostRoot, you need to pass a child of it.
// The same thing applies to Suspense boundaries.

function getClosestInstanceFromNode(targetNode) {
  var targetInst = targetNode[internalInstanceKey];

  if (targetInst) {
    // Don't return HostRoot or SuspenseComponent here.
    return targetInst;
  } // If the direct event target isn't a React owned DOM node, we need to look
  // to see if one of its parents is a React owned DOM node.


  var parentNode = targetNode.parentNode;

  while (parentNode) {
    // We'll check if this is a container root that could include
    // React nodes in the future. We need to check this first because
    // if we're a child of a dehydrated container, we need to first
    // find that inner container before moving on to finding the parent
    // instance. Note that we don't check this field on  the targetNode
    // itself because the fibers are conceptually between the container
    // node and the first child. It isn't surrounding the container node.
    // If it's not a container, we check if it's an instance.
    targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey];

    if (targetInst) {
      // Since this wasn't the direct target of the event, we might have
      // stepped past dehydrated DOM nodes to get here. However they could
      // also have been non-React nodes. We need to answer which one.
      // If we the instance doesn't have any children, then there can't be
      // a nested suspense boundary within it. So we can use this as a fast
      // bailout. Most of the time, when people add non-React children to
      // the tree, it is using a ref to a child-less DOM node.
      // Normally we'd only need to check one of the fibers because if it
      // has ever gone from having children to deleting them or vice versa
      // it would have deleted the dehydrated boundary nested inside already.
      // However, since the HostRoot starts out with an alternate it might
      // have one on the alternate so we need to check in case this was a
      // root.
      var alternate = targetInst.alternate;

      if (targetInst.child !== null || alternate !== null && alternate.child !== null) {
        // Next we need to figure out if the node that skipped past is
        // nested within a dehydrated boundary and if so, which one.
        var suspenseInstance = getParentSuspenseInstance(targetNode);

        while (suspenseInstance !== null) {
          // We found a suspense instance. That means that we haven't
          // hydrated it yet. Even though we leave the comments in the
          // DOM after hydrating, and there are boundaries in the DOM
          // that could already be hydrated, we wouldn't have found them
          // through this pass since if the target is hydrated it would
          // have had an internalInstanceKey on it.
          // Let's get the fiber associated with the SuspenseComponent
          // as the deepest instance.
          var targetSuspenseInst = suspenseInstance[internalInstanceKey];

          if (targetSuspenseInst) {
            return targetSuspenseInst;
          } // If we don't find a Fiber on the comment, it might be because
          // we haven't gotten to hydrate it yet. There might still be a
          // parent boundary that hasn't above this one so we need to find
          // the outer most that is known.


          suspenseInstance = getParentSuspenseInstance(suspenseInstance); // If we don't find one, then that should mean that the parent
          // host component also hasn't hydrated yet. We can return it
          // below since it will bail out on the isMounted check later.
        }
      }

      return targetInst;
    }

    targetNode = parentNode;
    parentNode = targetNode.parentNode;
  }

  return null;
}
/**
 * Given a DOM node, return the ReactDOMComponent or ReactDOMTextComponent
 * instance, or null if the node was not rendered by this React.
 */

function getInstanceFromNode$1(node) {
  var inst = node[internalInstanceKey] || node[internalContainerInstanceKey];

  if (inst) {
    if (inst.tag === HostComponent || inst.tag === HostText || inst.tag === SuspenseComponent || inst.tag === HostRoot) {
      return inst;
    } else {
      return null;
    }
  }

  return null;
}
/**
 * Given a ReactDOMComponent or ReactDOMTextComponent, return the corresponding
 * DOM node.
 */

function getNodeFromInstance$1(inst) {
  if (inst.tag === HostComponent || inst.tag === HostText) {
    // In Fiber this, is just the state node right now. We assume it will be
    // a host component or host text.
    return inst.stateNode;
  } // Without this first invariant, passing a non-DOM-component triggers the next
  // invariant for a missing parent, which is super confusing.


  {
    {
      throw Error( "getNodeFromInstance: Invalid argument." );
    }
  }
}
function getFiberCurrentPropsFromNode$1(node) {
  return node[internalEventHandlersKey] || null;
}
function updateFiberProps(node, props) {
  node[internalEventHandlersKey] = props;
}

function getParent(inst) {
  do {
    inst = inst.return; // TODO: If this is a HostRoot we might want to bail out.
    // That is depending on if we want nested subtrees (layers) to bubble
    // events to their parent. We could also go through parentNode on the
    // host node but that wouldn't work for React Native and doesn't let us
    // do the portal feature.
  } while (inst && inst.tag !== HostComponent);

  if (inst) {
    return inst;
  }

  return null;
}
/**
 * Return the lowest common ancestor of A and B, or null if they are in
 * different trees.
 */


function getLowestCommonAncestor(instA, instB) {
  var depthA = 0;

  for (var tempA = instA; tempA; tempA = getParent(tempA)) {
    depthA++;
  }

  var depthB = 0;

  for (var tempB = instB; tempB; tempB = getParent(tempB)) {
    depthB++;
  } // If A is deeper, crawl up.


  while (depthA - depthB > 0) {
    instA = getParent(instA);
    depthA--;
  } // If B is deeper, crawl up.


  while (depthB - depthA > 0) {
    instB = getParent(instB);
    depthB--;
  } // Walk in lockstep until we find a match.


  var depth = depthA;

  while (depth--) {
    if (instA === instB || instA === instB.alternate) {
      return instA;
    }

    instA = getParent(instA);
    instB = getParent(instB);
  }

  return null;
}
/**
 * Simulates the traversal of a two-phase, capture/bubble event dispatch.
 */

function traverseTwoPhase(inst, fn, arg) {
  var path = [];

  while (inst) {
    path.push(inst);
    inst = getParent(inst);
  }

  var i;

  for (i = path.length; i-- > 0;) {
    fn(path[i], 'captured', arg);
  }

  for (i = 0; i < path.length; i++) {
    fn(path[i], 'bubbled', arg);
  }
}
/**
 * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
 * should would receive a `mouseEnter` or `mouseLeave` event.
 *
 * Does not invoke the callback on the nearest common ancestor because nothing
 * "entered" or "left" that element.
 */

function traverseEnterLeave(from, to, fn, argFrom, argTo) {
  var common = from && to ? getLowestCommonAncestor(from, to) : null;
  var pathFrom = [];

  while (true) {
    if (!from) {
      break;
    }

    if (from === common) {
      break;
    }

    var alternate = from.alternate;

    if (alternate !== null && alternate === common) {
      break;
    }

    pathFrom.push(from);
    from = getParent(from);
  }

  var pathTo = [];

  while (true) {
    if (!to) {
      break;
    }

    if (to === common) {
      break;
    }

    var _alternate = to.alternate;

    if (_alternate !== null && _alternate === common) {
      break;
    }

    pathTo.push(to);
    to = getParent(to);
  }

  for (var i = 0; i < pathFrom.length; i++) {
    fn(pathFrom[i], 'bubbled', argFrom);
  }

  for (var _i = pathTo.length; _i-- > 0;) {
    fn(pathTo[_i], 'captured', argTo);
  }
}

function isInteractive(tag) {
  return tag === 'button' || tag === 'input' || tag === 'select' || tag === 'textarea';
}

function shouldPreventMouseEvent(name, type, props) {
  switch (name) {
    case 'onClick':
    case 'onClickCapture':
    case 'onDoubleClick':
    case 'onDoubleClickCapture':
    case 'onMouseDown':
    case 'onMouseDownCapture':
    case 'onMouseMove':
    case 'onMouseMoveCapture':
    case 'onMouseUp':
    case 'onMouseUpCapture':
    case 'onMouseEnter':
      return !!(props.disabled && isInteractive(type));

    default:
      return false;
  }
}
/**
 * @param {object} inst The instance, which is the source of events.
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @return {?function} The stored callback.
 */


function getListener(inst, registrationName) {
  var listener; // TODO: shouldPreventMouseEvent is DOM-specific and definitely should not
  // live here; needs to be moved to a better place soon

  var stateNode = inst.stateNode;

  if (!stateNode) {
    // Work in progress (ex: onload events in incremental mode).
    return null;
  }

  var props = getFiberCurrentPropsFromNode(stateNode);

  if (!props) {
    // Work in progress.
    return null;
  }

  listener = props[registrationName];

  if (shouldPreventMouseEvent(registrationName, inst.type, props)) {
    return null;
  }

  if (!(!listener || typeof listener === 'function')) {
    {
      throw Error( "Expected `" + registrationName + "` listener to be a function, instead got a value of `" + typeof listener + "` type." );
    }
  }

  return listener;
}

/**
 * Some event types have a notion of different registration names for different
 * "phases" of propagation. This finds listeners by a given phase.
 */
function listenerAtPhase(inst, event, propagationPhase) {
  var registrationName = event.dispatchConfig.phasedRegistrationNames[propagationPhase];
  return getListener(inst, registrationName);
}
/**
 * A small set of propagation patterns, each of which will accept a small amount
 * of information, and generate a set of "dispatch ready event objects" - which
 * are sets of events that have already been annotated with a set of dispatched
 * listener functions/ids. The API is designed this way to discourage these
 * propagation strategies from actually executing the dispatches, since we
 * always want to collect the entire set of dispatches before executing even a
 * single one.
 */

/**
 * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
 * here, allows us to not have to bind or create functions for each event.
 * Mutating the event's members allows us to not have to create a wrapping
 * "dispatch" object that pairs the event with the listener.
 */


function accumulateDirectionalDispatches(inst, phase, event) {
  {
    if (!inst) {
      error('Dispatching inst must not be null');
    }
  }

  var listener = listenerAtPhase(inst, event, phase);

  if (listener) {
    event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
    event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
  }
}
/**
 * Collect dispatches (must be entirely collected before dispatching - see unit
 * tests). Lazily allocate the array to conserve memory.  We must loop through
 * each event and perform the traversal for each one. We cannot perform a
 * single traversal for the entire collection of events because each event may
 * have a different target.
 */


function accumulateTwoPhaseDispatchesSingle(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    traverseTwoPhase(event._targetInst, accumulateDirectionalDispatches, event);
  }
}
/**
 * Accumulates without regard to direction, does not look for phased
 * registration names. Same as `accumulateDirectDispatchesSingle` but without
 * requiring that the `dispatchMarker` be the same as the dispatched ID.
 */


function accumulateDispatches(inst, ignoredDirection, event) {
  if (inst && event && event.dispatchConfig.registrationName) {
    var registrationName = event.dispatchConfig.registrationName;
    var listener = getListener(inst, registrationName);

    if (listener) {
      event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
      event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
    }
  }
}
/**
 * Accumulates dispatches on an `SyntheticEvent`, but only for the
 * `dispatchMarker`.
 * @param {SyntheticEvent} event
 */


function accumulateDirectDispatchesSingle(event) {
  if (event && event.dispatchConfig.registrationName) {
    accumulateDispatches(event._targetInst, null, event);
  }
}

function accumulateTwoPhaseDispatches(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
}
function accumulateEnterLeaveDispatches(leave, enter, from, to) {
  traverseEnterLeave(from, to, accumulateDispatches, leave, enter);
}
function accumulateDirectDispatches(events) {
  forEachAccumulated(events, accumulateDirectDispatchesSingle);
}

/**
 * These variables store information about text content of a target node,
 * allowing comparison of content before and after a given event.
 *
 * Identify the node where selection currently begins, then observe
 * both its text content and its current position in the DOM. Since the
 * browser may natively replace the target node during composition, we can
 * use its position to find its replacement.
 *
 *
 */
var root = null;
var startText = null;
var fallbackText = null;
function initialize(nativeEventTarget) {
  root = nativeEventTarget;
  startText = getText();
  return true;
}
function reset() {
  root = null;
  startText = null;
  fallbackText = null;
}
function getData() {
  if (fallbackText) {
    return fallbackText;
  }

  var start;
  var startValue = startText;
  var startLength = startValue.length;
  var end;
  var endValue = getText();
  var endLength = endValue.length;

  for (start = 0; start < startLength; start++) {
    if (startValue[start] !== endValue[start]) {
      break;
    }
  }

  var minEnd = startLength - start;

  for (end = 1; end <= minEnd; end++) {
    if (startValue[startLength - end] !== endValue[endLength - end]) {
      break;
    }
  }

  var sliceTail = end > 1 ? 1 - end : undefined;
  fallbackText = endValue.slice(start, sliceTail);
  return fallbackText;
}
function getText() {
  if ('value' in root) {
    return root.value;
  }

  return root.textContent;
}

var EVENT_POOL_SIZE = 10;
/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */

var EventInterface = {
  type: null,
  target: null,
  // currentTarget is set when dispatching; no use in copying it here
  currentTarget: function () {
    return null;
  },
  eventPhase: null,
  bubbles: null,
  cancelable: null,
  timeStamp: function (event) {
    return event.timeStamp || Date.now();
  },
  defaultPrevented: null,
  isTrusted: null
};

function functionThatReturnsTrue() {
  return true;
}

function functionThatReturnsFalse() {
  return false;
}
/**
 * Synthetic events are dispatched by event plugins, typically in response to a
 * top-level event delegation handler.
 *
 * These systems should generally use pooling to reduce the frequency of garbage
 * collection. The system should check `isPersistent` to determine whether the
 * event should be released into the pool after being dispatched. Users that
 * need a persisted event should invoke `persist`.
 *
 * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
 * normalizing browser quirks. Subclasses do not necessarily have to implement a
 * DOM interface; custom application-specific events can also subclass this.
 *
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {*} targetInst Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @param {DOMEventTarget} nativeEventTarget Target node.
 */


function SyntheticEvent(dispatchConfig, targetInst, nativeEvent, nativeEventTarget) {
  {
    // these have a getter/setter for warnings
    delete this.nativeEvent;
    delete this.preventDefault;
    delete this.stopPropagation;
    delete this.isDefaultPrevented;
    delete this.isPropagationStopped;
  }

  this.dispatchConfig = dispatchConfig;
  this._targetInst = targetInst;
  this.nativeEvent = nativeEvent;
  var Interface = this.constructor.Interface;

  for (var propName in Interface) {
    if (!Interface.hasOwnProperty(propName)) {
      continue;
    }

    {
      delete this[propName]; // this has a getter/setter for warnings
    }

    var normalize = Interface[propName];

    if (normalize) {
      this[propName] = normalize(nativeEvent);
    } else {
      if (propName === 'target') {
        this.target = nativeEventTarget;
      } else {
        this[propName] = nativeEvent[propName];
      }
    }
  }

  var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;

  if (defaultPrevented) {
    this.isDefaultPrevented = functionThatReturnsTrue;
  } else {
    this.isDefaultPrevented = functionThatReturnsFalse;
  }

  this.isPropagationStopped = functionThatReturnsFalse;
  return this;
}

_assign(SyntheticEvent.prototype, {
  preventDefault: function () {
    this.defaultPrevented = true;
    var event = this.nativeEvent;

    if (!event) {
      return;
    }

    if (event.preventDefault) {
      event.preventDefault();
    } else if (typeof event.returnValue !== 'unknown') {
      event.returnValue = false;
    }

    this.isDefaultPrevented = functionThatReturnsTrue;
  },
  stopPropagation: function () {
    var event = this.nativeEvent;

    if (!event) {
      return;
    }

    if (event.stopPropagation) {
      event.stopPropagation();
    } else if (typeof event.cancelBubble !== 'unknown') {
      // The ChangeEventPlugin registers a "propertychange" event for
      // IE. This event does not support bubbling or cancelling, and
      // any references to cancelBubble throw "Member not found".  A
      // typeof check of "unknown" circumvents this issue (and is also
      // IE specific).
      event.cancelBubble = true;
    }

    this.isPropagationStopped = functionThatReturnsTrue;
  },

  /**
   * We release all dispatched `SyntheticEvent`s after each event loop, adding
   * them back into the pool. This allows a way to hold onto a reference that
   * won't be added back into the pool.
   */
  persist: function () {
    this.isPersistent = functionThatReturnsTrue;
  },

  /**
   * Checks if this event should be released back into the pool.
   *
   * @return {boolean} True if this should not be released, false otherwise.
   */
  isPersistent: functionThatReturnsFalse,

  /**
   * `PooledClass` looks for `destructor` on each instance it releases.
   */
  destructor: function () {
    var Interface = this.constructor.Interface;

    for (var propName in Interface) {
      {
        Object.defineProperty(this, propName, getPooledWarningPropertyDefinition(propName, Interface[propName]));
      }
    }

    this.dispatchConfig = null;
    this._targetInst = null;
    this.nativeEvent = null;
    this.isDefaultPrevented = functionThatReturnsFalse;
    this.isPropagationStopped = functionThatReturnsFalse;
    this._dispatchListeners = null;
    this._dispatchInstances = null;

    {
      Object.defineProperty(this, 'nativeEvent', getPooledWarningPropertyDefinition('nativeEvent', null));
      Object.defineProperty(this, 'isDefaultPrevented', getPooledWarningPropertyDefinition('isDefaultPrevented', functionThatReturnsFalse));
      Object.defineProperty(this, 'isPropagationStopped', getPooledWarningPropertyDefinition('isPropagationStopped', functionThatReturnsFalse));
      Object.defineProperty(this, 'preventDefault', getPooledWarningPropertyDefinition('preventDefault', function () {}));
      Object.defineProperty(this, 'stopPropagation', getPooledWarningPropertyDefinition('stopPropagation', function () {}));
    }
  }
});

SyntheticEvent.Interface = EventInterface;
/**
 * Helper to reduce boilerplate when creating subclasses.
 */

SyntheticEvent.extend = function (Interface) {
  var Super = this;

  var E = function () {};

  E.prototype = Super.prototype;
  var prototype = new E();

  function Class() {
    return Super.apply(this, arguments);
  }

  _assign(prototype, Class.prototype);

  Class.prototype = prototype;
  Class.prototype.constructor = Class;
  Class.Interface = _assign({}, Super.Interface, Interface);
  Class.extend = Super.extend;
  addEventPoolingTo(Class);
  return Class;
};

addEventPoolingTo(SyntheticEvent);
/**
 * Helper to nullify syntheticEvent instance properties when destructing
 *
 * @param {String} propName
 * @param {?object} getVal
 * @return {object} defineProperty object
 */

function getPooledWarningPropertyDefinition(propName, getVal) {
  var isFunction = typeof getVal === 'function';
  return {
    configurable: true,
    set: set,
    get: get
  };

  function set(val) {
    var action = isFunction ? 'setting the method' : 'setting the property';
    warn(action, 'This is effectively a no-op');
    return val;
  }

  function get() {
    var action = isFunction ? 'accessing the method' : 'accessing the property';
    var result = isFunction ? 'This is a no-op function' : 'This is set to null';
    warn(action, result);
    return getVal;
  }

  function warn(action, result) {
    {
      error("This synthetic event is reused for performance reasons. If you're seeing this, " + "you're %s `%s` on a released/nullified synthetic event. %s. " + 'If you must keep the original synthetic event around, use event.persist(). ' + 'See https://fb.me/react-event-pooling for more information.', action, propName, result);
    }
  }
}

function getPooledEvent(dispatchConfig, targetInst, nativeEvent, nativeInst) {
  var EventConstructor = this;

  if (EventConstructor.eventPool.length) {
    var instance = EventConstructor.eventPool.pop();
    EventConstructor.call(instance, dispatchConfig, targetInst, nativeEvent, nativeInst);
    return instance;
  }

  return new EventConstructor(dispatchConfig, targetInst, nativeEvent, nativeInst);
}

function releasePooledEvent(event) {
  var EventConstructor = this;

  if (!(event instanceof EventConstructor)) {
    {
      throw Error( "Trying to release an event instance into a pool of a different type." );
    }
  }

  event.destructor();

  if (EventConstructor.eventPool.length < EVENT_POOL_SIZE) {
    EventConstructor.eventPool.push(event);
  }
}

function addEventPoolingTo(EventConstructor) {
  EventConstructor.eventPool = [];
  EventConstructor.getPooled = getPooledEvent;
  EventConstructor.release = releasePooledEvent;
}

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
 */

var SyntheticCompositionEvent = SyntheticEvent.extend({
  data: null
});

/**
 * @interface Event
 * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
 *      /#events-inputevents
 */

var SyntheticInputEvent = SyntheticEvent.extend({
  data: null
});

var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space

var START_KEYCODE = 229;
var canUseCompositionEvent = canUseDOM && 'CompositionEvent' in window;
var documentMode = null;

if (canUseDOM && 'documentMode' in document) {
  documentMode = document.documentMode;
} // Webkit offers a very useful `textInput` event that can be used to
// directly represent `beforeInput`. The IE `textinput` event is not as
// useful, so we don't use it.


var canUseTextInputEvent = canUseDOM && 'TextEvent' in window && !documentMode; // In IE9+, we have access to composition events, but the data supplied
// by the native compositionend event may be incorrect. Japanese ideographic
// spaces, for instance (\u3000) are not recorded correctly.

var useFallbackCompositionData = canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);
var SPACEBAR_CODE = 32;
var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE); // Events and their corresponding property names.

var eventTypes = {
  beforeInput: {
    phasedRegistrationNames: {
      bubbled: 'onBeforeInput',
      captured: 'onBeforeInputCapture'
    },
    dependencies: [TOP_COMPOSITION_END, TOP_KEY_PRESS, TOP_TEXT_INPUT, TOP_PASTE]
  },
  compositionEnd: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionEnd',
      captured: 'onCompositionEndCapture'
    },
    dependencies: [TOP_BLUR, TOP_COMPOSITION_END, TOP_KEY_DOWN, TOP_KEY_PRESS, TOP_KEY_UP, TOP_MOUSE_DOWN]
  },
  compositionStart: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionStart',
      captured: 'onCompositionStartCapture'
    },
    dependencies: [TOP_BLUR, TOP_COMPOSITION_START, TOP_KEY_DOWN, TOP_KEY_PRESS, TOP_KEY_UP, TOP_MOUSE_DOWN]
  },
  compositionUpdate: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionUpdate',
      captured: 'onCompositionUpdateCapture'
    },
    dependencies: [TOP_BLUR, TOP_COMPOSITION_UPDATE, TOP_KEY_DOWN, TOP_KEY_PRESS, TOP_KEY_UP, TOP_MOUSE_DOWN]
  }
}; // Track whether we've ever handled a keypress on the space key.

var hasSpaceKeypress = false;
/**
 * Return whether a native keypress event is assumed to be a command.
 * This is required because Firefox fires `keypress` events for key commands
 * (cut, copy, select-all, etc.) even though no character is inserted.
 */

function isKeypressCommand(nativeEvent) {
  return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) && // ctrlKey && altKey is equivalent to AltGr, and is not a command.
  !(nativeEvent.ctrlKey && nativeEvent.altKey);
}
/**
 * Translate native top level events into event types.
 *
 * @param {string} topLevelType
 * @return {object}
 */


function getCompositionEventType(topLevelType) {
  switch (topLevelType) {
    case TOP_COMPOSITION_START:
      return eventTypes.compositionStart;

    case TOP_COMPOSITION_END:
      return eventTypes.compositionEnd;

    case TOP_COMPOSITION_UPDATE:
      return eventTypes.compositionUpdate;
  }
}
/**
 * Does our fallback best-guess model think this event signifies that
 * composition has begun?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */


function isFallbackCompositionStart(topLevelType, nativeEvent) {
  return topLevelType === TOP_KEY_DOWN && nativeEvent.keyCode === START_KEYCODE;
}
/**
 * Does our fallback mode think that this event is the end of composition?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */


function isFallbackCompositionEnd(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case TOP_KEY_UP:
      // Command keys insert or clear IME input.
      return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;

    case TOP_KEY_DOWN:
      // Expect IME keyCode on each keydown. If we get any other
      // code we must have exited earlier.
      return nativeEvent.keyCode !== START_KEYCODE;

    case TOP_KEY_PRESS:
    case TOP_MOUSE_DOWN:
    case TOP_BLUR:
      // Events are not possible without cancelling IME.
      return true;

    default:
      return false;
  }
}
/**
 * Google Input Tools provides composition data via a CustomEvent,
 * with the `data` property populated in the `detail` object. If this
 * is available on the event object, use it. If not, this is a plain
 * composition event and we have nothing special to extract.
 *
 * @param {object} nativeEvent
 * @return {?string}
 */


function getDataFromCustomEvent(nativeEvent) {
  var detail = nativeEvent.detail;

  if (typeof detail === 'object' && 'data' in detail) {
    return detail.data;
  }

  return null;
}
/**
 * Check if a composition event was triggered by Korean IME.
 * Our fallback mode does not work well with IE's Korean IME,
 * so just use native composition events when Korean IME is used.
 * Although CompositionEvent.locale property is deprecated,
 * it is available in IE, where our fallback mode is enabled.
 *
 * @param {object} nativeEvent
 * @return {boolean}
 */


function isUsingKoreanIME(nativeEvent) {
  return nativeEvent.locale === 'ko';
} // Track the current IME composition status, if any.


var isComposing = false;
/**
 * @return {?object} A SyntheticCompositionEvent.
 */

function extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var eventType;
  var fallbackData;

  if (canUseCompositionEvent) {
    eventType = getCompositionEventType(topLevelType);
  } else if (!isComposing) {
    if (isFallbackCompositionStart(topLevelType, nativeEvent)) {
      eventType = eventTypes.compositionStart;
    }
  } else if (isFallbackCompositionEnd(topLevelType, nativeEvent)) {
    eventType = eventTypes.compositionEnd;
  }

  if (!eventType) {
    return null;
  }

  if (useFallbackCompositionData && !isUsingKoreanIME(nativeEvent)) {
    // The current composition is stored statically and must not be
    // overwritten while composition continues.
    if (!isComposing && eventType === eventTypes.compositionStart) {
      isComposing = initialize(nativeEventTarget);
    } else if (eventType === eventTypes.compositionEnd) {
      if (isComposing) {
        fallbackData = getData();
      }
    }
  }

  var event = SyntheticCompositionEvent.getPooled(eventType, targetInst, nativeEvent, nativeEventTarget);

  if (fallbackData) {
    // Inject data generated from fallback path into the synthetic event.
    // This matches the property of native CompositionEventInterface.
    event.data = fallbackData;
  } else {
    var customData = getDataFromCustomEvent(nativeEvent);

    if (customData !== null) {
      event.data = customData;
    }
  }

  accumulateTwoPhaseDispatches(event);
  return event;
}
/**
 * @param {TopLevelType} topLevelType Number from `TopLevelType`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The string corresponding to this `beforeInput` event.
 */


function getNativeBeforeInputChars(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case TOP_COMPOSITION_END:
      return getDataFromCustomEvent(nativeEvent);

    case TOP_KEY_PRESS:
      /**
       * If native `textInput` events are available, our goal is to make
       * use of them. However, there is a special case: the spacebar key.
       * In Webkit, preventing default on a spacebar `textInput` event
       * cancels character insertion, but it *also* causes the browser
       * to fall back to its default spacebar behavior of scrolling the
       * page.
       *
       * Tracking at:
       * https://code.google.com/p/chromium/issues/detail?id=355103
       *
       * To avoid this issue, use the keypress event as if no `textInput`
       * event is available.
       */
      var which = nativeEvent.which;

      if (which !== SPACEBAR_CODE) {
        return null;
      }

      hasSpaceKeypress = true;
      return SPACEBAR_CHAR;

    case TOP_TEXT_INPUT:
      // Record the characters to be added to the DOM.
      var chars = nativeEvent.data; // If it's a spacebar character, assume that we have already handled
      // it at the keypress level and bail immediately. Android Chrome
      // doesn't give us keycodes, so we need to ignore it.

      if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
        return null;
      }

      return chars;

    default:
      // For other native event types, do nothing.
      return null;
  }
}
/**
 * For browsers that do not provide the `textInput` event, extract the
 * appropriate string to use for SyntheticInputEvent.
 *
 * @param {number} topLevelType Number from `TopLevelEventTypes`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The fallback string for this `beforeInput` event.
 */


function getFallbackBeforeInputChars(topLevelType, nativeEvent) {
  // If we are currently composing (IME) and using a fallback to do so,
  // try to extract the composed characters from the fallback object.
  // If composition event is available, we extract a string only at
  // compositionevent, otherwise extract it at fallback events.
  if (isComposing) {
    if (topLevelType === TOP_COMPOSITION_END || !canUseCompositionEvent && isFallbackCompositionEnd(topLevelType, nativeEvent)) {
      var chars = getData();
      reset();
      isComposing = false;
      return chars;
    }

    return null;
  }

  switch (topLevelType) {
    case TOP_PASTE:
      // If a paste event occurs after a keypress, throw out the input
      // chars. Paste events should not lead to BeforeInput events.
      return null;

    case TOP_KEY_PRESS:
      /**
       * As of v27, Firefox may fire keypress events even when no character
       * will be inserted. A few possibilities:
       *
       * - `which` is `0`. Arrow keys, Esc key, etc.
       *
       * - `which` is the pressed key code, but no char is available.
       *   Ex: 'AltGr + d` in Polish. There is no modified character for
       *   this key combination and no character is inserted into the
       *   document, but FF fires the keypress for char code `100` anyway.
       *   No `input` event will occur.
       *
       * - `which` is the pressed key code, but a command combination is
       *   being used. Ex: `Cmd+C`. No character is inserted, and no
       *   `input` event will occur.
       */
      if (!isKeypressCommand(nativeEvent)) {
        // IE fires the `keypress` event when a user types an emoji via
        // Touch keyboard of Windows.  In such a case, the `char` property
        // holds an emoji character like `\uD83D\uDE0A`.  Because its length
        // is 2, the property `which` does not represent an emoji correctly.
        // In such a case, we directly return the `char` property instead of
        // using `which`.
        if (nativeEvent.char && nativeEvent.char.length > 1) {
          return nativeEvent.char;
        } else if (nativeEvent.which) {
          return String.fromCharCode(nativeEvent.which);
        }
      }

      return null;

    case TOP_COMPOSITION_END:
      return useFallbackCompositionData && !isUsingKoreanIME(nativeEvent) ? null : nativeEvent.data;

    default:
      return null;
  }
}
/**
 * Extract a SyntheticInputEvent for `beforeInput`, based on either native
 * `textInput` or fallback behavior.
 *
 * @return {?object} A SyntheticInputEvent.
 */


function extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var chars;

  if (canUseTextInputEvent) {
    chars = getNativeBeforeInputChars(topLevelType, nativeEvent);
  } else {
    chars = getFallbackBeforeInputChars(topLevelType, nativeEvent);
  } // If no characters are being inserted, no BeforeInput event should
  // be fired.


  if (!chars) {
    return null;
  }

  var event = SyntheticInputEvent.getPooled(eventTypes.beforeInput, targetInst, nativeEvent, nativeEventTarget);
  event.data = chars;
  accumulateTwoPhaseDispatches(event);
  return event;
}
/**
 * Create an `onBeforeInput` event to match
 * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
 *
 * This event plugin is based on the native `textInput` event
 * available in Chrome, Safari, Opera, and IE. This event fires after
 * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
 *
 * `beforeInput` is spec'd but not implemented in any browsers, and
 * the `input` event does not provide any useful information about what has
 * actually been added, contrary to the spec. Thus, `textInput` is the best
 * available event to identify the characters that have actually been inserted
 * into the target node.
 *
 * This plugin is also responsible for emitting `composition` events, thus
 * allowing us to share composition fallback code for both `beforeInput` and
 * `composition` event types.
 */


var BeforeInputEventPlugin = {
  eventTypes: eventTypes,
  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags) {
    var composition = extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget);
    var beforeInput = extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget);

    if (composition === null) {
      return beforeInput;
    }

    if (beforeInput === null) {
      return composition;
    }

    return [composition, beforeInput];
  }
};

/**
 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
 */
var supportedInputTypes = {
  color: true,
  date: true,
  datetime: true,
  'datetime-local': true,
  email: true,
  month: true,
  number: true,
  password: true,
  range: true,
  search: true,
  tel: true,
  text: true,
  time: true,
  url: true,
  week: true
};

function isTextInputElement(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();

  if (nodeName === 'input') {
    return !!supportedInputTypes[elem.type];
  }

  if (nodeName === 'textarea') {
    return true;
  }

  return false;
}

var eventTypes$1 = {
  change: {
    phasedRegistrationNames: {
      bubbled: 'onChange',
      captured: 'onChangeCapture'
    },
    dependencies: [TOP_BLUR, TOP_CHANGE, TOP_CLICK, TOP_FOCUS, TOP_INPUT, TOP_KEY_DOWN, TOP_KEY_UP, TOP_SELECTION_CHANGE]
  }
};

function createAndAccumulateChangeEvent(inst, nativeEvent, target) {
  var event = SyntheticEvent.getPooled(eventTypes$1.change, inst, nativeEvent, target);
  event.type = 'change'; // Flag this event loop as needing state restore.

  enqueueStateRestore(target);
  accumulateTwoPhaseDispatches(event);
  return event;
}
/**
 * For IE shims
 */


var activeElement = null;
var activeElementInst = null;
/**
 * SECTION: handle `change` event
 */

function shouldUseChangeEvent(elem) {
  var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName === 'select' || nodeName === 'input' && elem.type === 'file';
}

function manualDispatchChangeEvent(nativeEvent) {
  var event = createAndAccumulateChangeEvent(activeElementInst, nativeEvent, getEventTarget(nativeEvent)); // If change and propertychange bubbled, we'd just bind to it like all the
  // other events and have it go through ReactBrowserEventEmitter. Since it
  // doesn't, we manually listen for the events and so we have to enqueue and
  // process the abstract event manually.
  //
  // Batching is necessary here in order to ensure that all event handlers run
  // before the next rerender (including event handlers attached to ancestor
  // elements instead of directly on the input). Without this, controlled
  // components don't work properly in conjunction with event bubbling because
  // the component is rerendered and the value reverted before all the event
  // handlers can run. See https://github.com/facebook/react/issues/708.

  batchedUpdates(runEventInBatch, event);
}

function runEventInBatch(event) {
  runEventsInBatch(event);
}

function getInstIfValueChanged(targetInst) {
  var targetNode = getNodeFromInstance$1(targetInst);

  if (updateValueIfChanged(targetNode)) {
    return targetInst;
  }
}

function getTargetInstForChangeEvent(topLevelType, targetInst) {
  if (topLevelType === TOP_CHANGE) {
    return targetInst;
  }
}
/**
 * SECTION: handle `input` event
 */


var isInputEventSupported = false;

if (canUseDOM) {
  // IE9 claims to support the input event but fails to trigger it when
  // deleting text, so we ignore its input events.
  isInputEventSupported = isEventSupported('input') && (!document.documentMode || document.documentMode > 9);
}
/**
 * (For IE <=9) Starts tracking propertychange events on the passed-in element
 * and override the value property so that we can distinguish user events from
 * value changes in JS.
 */


function startWatchingForValueChange(target, targetInst) {
  activeElement = target;
  activeElementInst = targetInst;
  activeElement.attachEvent('onpropertychange', handlePropertyChange);
}
/**
 * (For IE <=9) Removes the event listeners from the currently-tracked element,
 * if any exists.
 */


function stopWatchingForValueChange() {
  if (!activeElement) {
    return;
  }

  activeElement.detachEvent('onpropertychange', handlePropertyChange);
  activeElement = null;
  activeElementInst = null;
}
/**
 * (For IE <=9) Handles a propertychange event, sending a `change` event if
 * the value of the active element has changed.
 */


function handlePropertyChange(nativeEvent) {
  if (nativeEvent.propertyName !== 'value') {
    return;
  }

  if (getInstIfValueChanged(activeElementInst)) {
    manualDispatchChangeEvent(nativeEvent);
  }
}

function handleEventsForInputEventPolyfill(topLevelType, target, targetInst) {
  if (topLevelType === TOP_FOCUS) {
    // In IE9, propertychange fires for most input events but is buggy and
    // doesn't fire when text is deleted, but conveniently, selectionchange
    // appears to fire in all of the remaining cases so we catch those and
    // forward the event if the value has changed
    // In either case, we don't want to call the event handler if the value
    // is changed from JS so we redefine a setter for `.value` that updates
    // our activeElementValue variable, allowing us to ignore those changes
    //
    // stopWatching() should be a noop here but we call it just in case we
    // missed a blur event somehow.
    stopWatchingForValueChange();
    startWatchingForValueChange(target, targetInst);
  } else if (topLevelType === TOP_BLUR) {
    stopWatchingForValueChange();
  }
} // For IE8 and IE9.


function getTargetInstForInputEventPolyfill(topLevelType, targetInst) {
  if (topLevelType === TOP_SELECTION_CHANGE || topLevelType === TOP_KEY_UP || topLevelType === TOP_KEY_DOWN) {
    // On the selectionchange event, the target is just document which isn't
    // helpful for us so just check activeElement instead.
    //
    // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
    // propertychange on the first input event after setting `value` from a
    // script and fires only keydown, keypress, keyup. Catching keyup usually
    // gets it and catching keydown lets us fire an event for the first
    // keystroke if user does a key repeat (it'll be a little delayed: right
    // before the second keystroke). Other input methods (e.g., paste) seem to
    // fire selectionchange normally.
    return getInstIfValueChanged(activeElementInst);
  }
}
/**
 * SECTION: handle `click` event
 */


function shouldUseClickEvent(elem) {
  // Use the `click` event to detect changes to checkbox and radio inputs.
  // This approach works across all browsers, whereas `change` does not fire
  // until `blur` in IE8.
  var nodeName = elem.nodeName;
  return nodeName && nodeName.toLowerCase() === 'input' && (elem.type === 'checkbox' || elem.type === 'radio');
}

function getTargetInstForClickEvent(topLevelType, targetInst) {
  if (topLevelType === TOP_CLICK) {
    return getInstIfValueChanged(targetInst);
  }
}

function getTargetInstForInputOrChangeEvent(topLevelType, targetInst) {
  if (topLevelType === TOP_INPUT || topLevelType === TOP_CHANGE) {
    return getInstIfValueChanged(targetInst);
  }
}

function handleControlledInputBlur(node) {
  var state = node._wrapperState;

  if (!state || !state.controlled || node.type !== 'number') {
    return;
  }

  {
    // If controlled, assign the value attribute to the current value on blur
    setDefaultValue(node, 'number', node.value);
  }
}
/**
 * This plugin creates an `onChange` event that normalizes change events
 * across form elements. This event fires at a time when it's possible to
 * change the element's value without seeing a flicker.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - select
 */


var ChangeEventPlugin = {
  eventTypes: eventTypes$1,
  _isInputEventSupported: isInputEventSupported,
  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags) {
    var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;
    var getTargetInstFunc, handleEventFunc;

    if (shouldUseChangeEvent(targetNode)) {
      getTargetInstFunc = getTargetInstForChangeEvent;
    } else if (isTextInputElement(targetNode)) {
      if (isInputEventSupported) {
        getTargetInstFunc = getTargetInstForInputOrChangeEvent;
      } else {
        getTargetInstFunc = getTargetInstForInputEventPolyfill;
        handleEventFunc = handleEventsForInputEventPolyfill;
      }
    } else if (shouldUseClickEvent(targetNode)) {
      getTargetInstFunc = getTargetInstForClickEvent;
    }

    if (getTargetInstFunc) {
      var inst = getTargetInstFunc(topLevelType, targetInst);

      if (inst) {
        var event = createAndAccumulateChangeEvent(inst, nativeEvent, nativeEventTarget);
        return event;
      }
    }

    if (handleEventFunc) {
      handleEventFunc(topLevelType, targetNode, targetInst);
    } // When blurring, set the value attribute for number inputs


    if (topLevelType === TOP_BLUR) {
      handleControlledInputBlur(targetNode);
    }
  }
};

var SyntheticUIEvent = SyntheticEvent.extend({
  view: null,
  detail: null
});

/**
 * Translation from modifier key to the associated property in the event.
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
 */
var modifierKeyToProp = {
  Alt: 'altKey',
  Control: 'ctrlKey',
  Meta: 'metaKey',
  Shift: 'shiftKey'
}; // Older browsers (Safari <= 10, iOS Safari <= 10.2) do not support
// getModifierState. If getModifierState is not supported, we map it to a set of
// modifier keys exposed by the event. In this case, Lock-keys are not supported.

function modifierStateGetter(keyArg) {
  var syntheticEvent = this;
  var nativeEvent = syntheticEvent.nativeEvent;

  if (nativeEvent.getModifierState) {
    return nativeEvent.getModifierState(keyArg);
  }

  var keyProp = modifierKeyToProp[keyArg];
  return keyProp ? !!nativeEvent[keyProp] : false;
}

function getEventModifierState(nativeEvent) {
  return modifierStateGetter;
}

var previousScreenX = 0;
var previousScreenY = 0; // Use flags to signal movementX/Y has already been set

var isMovementXSet = false;
var isMovementYSet = false;
/**
 * @interface MouseEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */

var SyntheticMouseEvent = SyntheticUIEvent.extend({
  screenX: null,
  screenY: null,
  clientX: null,
  clientY: null,
  pageX: null,
  pageY: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  getModifierState: getEventModifierState,
  button: null,
  buttons: null,
  relatedTarget: function (event) {
    return event.relatedTarget || (event.fromElement === event.srcElement ? event.toElement : event.fromElement);
  },
  movementX: function (event) {
    if ('movementX' in event) {
      return event.movementX;
    }

    var screenX = previousScreenX;
    previousScreenX = event.screenX;

    if (!isMovementXSet) {
      isMovementXSet = true;
      return 0;
    }

    return event.type === 'mousemove' ? event.screenX - screenX : 0;
  },
  movementY: function (event) {
    if ('movementY' in event) {
      return event.movementY;
    }

    var screenY = previousScreenY;
    previousScreenY = event.screenY;

    if (!isMovementYSet) {
      isMovementYSet = true;
      return 0;
    }

    return event.type === 'mousemove' ? event.screenY - screenY : 0;
  }
});

/**
 * @interface PointerEvent
 * @see http://www.w3.org/TR/pointerevents/
 */

var SyntheticPointerEvent = SyntheticMouseEvent.extend({
  pointerId: null,
  width: null,
  height: null,
  pressure: null,
  tangentialPressure: null,
  tiltX: null,
  tiltY: null,
  twist: null,
  pointerType: null,
  isPrimary: null
});

var eventTypes$2 = {
  mouseEnter: {
    registrationName: 'onMouseEnter',
    dependencies: [TOP_MOUSE_OUT, TOP_MOUSE_OVER]
  },
  mouseLeave: {
    registrationName: 'onMouseLeave',
    dependencies: [TOP_MOUSE_OUT, TOP_MOUSE_OVER]
  },
  pointerEnter: {
    registrationName: 'onPointerEnter',
    dependencies: [TOP_POINTER_OUT, TOP_POINTER_OVER]
  },
  pointerLeave: {
    registrationName: 'onPointerLeave',
    dependencies: [TOP_POINTER_OUT, TOP_POINTER_OVER]
  }
};
var EnterLeaveEventPlugin = {
  eventTypes: eventTypes$2,

  /**
   * For almost every interaction we care about, there will be both a top-level
   * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
   * we do not extract duplicate events. However, moving the mouse into the
   * browser from outside will not fire a `mouseout` event. In this case, we use
   * the `mouseover` top-level event.
   */
  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags) {
    var isOverEvent = topLevelType === TOP_MOUSE_OVER || topLevelType === TOP_POINTER_OVER;
    var isOutEvent = topLevelType === TOP_MOUSE_OUT || topLevelType === TOP_POINTER_OUT;

    if (isOverEvent && (eventSystemFlags & IS_REPLAYED) === 0 && (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
      // If this is an over event with a target, then we've already dispatched
      // the event in the out event of the other target. If this is replayed,
      // then it's because we couldn't dispatch against this target previously
      // so we have to do it now instead.
      return null;
    }

    if (!isOutEvent && !isOverEvent) {
      // Must not be a mouse or pointer in or out - ignoring.
      return null;
    }

    var win;

    if (nativeEventTarget.window === nativeEventTarget) {
      // `nativeEventTarget` is probably a window object.
      win = nativeEventTarget;
    } else {
      // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
      var doc = nativeEventTarget.ownerDocument;

      if (doc) {
        win = doc.defaultView || doc.parentWindow;
      } else {
        win = window;
      }
    }

    var from;
    var to;

    if (isOutEvent) {
      from = targetInst;
      var related = nativeEvent.relatedTarget || nativeEvent.toElement;
      to = related ? getClosestInstanceFromNode(related) : null;

      if (to !== null) {
        var nearestMounted = getNearestMountedFiber(to);

        if (to !== nearestMounted || to.tag !== HostComponent && to.tag !== HostText) {
          to = null;
        }
      }
    } else {
      // Moving to a node from outside the window.
      from = null;
      to = targetInst;
    }

    if (from === to) {
      // Nothing pertains to our managed components.
      return null;
    }

    var eventInterface, leaveEventType, enterEventType, eventTypePrefix;

    if (topLevelType === TOP_MOUSE_OUT || topLevelType === TOP_MOUSE_OVER) {
      eventInterface = SyntheticMouseEvent;
      leaveEventType = eventTypes$2.mouseLeave;
      enterEventType = eventTypes$2.mouseEnter;
      eventTypePrefix = 'mouse';
    } else if (topLevelType === TOP_POINTER_OUT || topLevelType === TOP_POINTER_OVER) {
      eventInterface = SyntheticPointerEvent;
      leaveEventType = eventTypes$2.pointerLeave;
      enterEventType = eventTypes$2.pointerEnter;
      eventTypePrefix = 'pointer';
    }

    var fromNode = from == null ? win : getNodeFromInstance$1(from);
    var toNode = to == null ? win : getNodeFromInstance$1(to);
    var leave = eventInterface.getPooled(leaveEventType, from, nativeEvent, nativeEventTarget);
    leave.type = eventTypePrefix + 'leave';
    leave.target = fromNode;
    leave.relatedTarget = toNode;
    var enter = eventInterface.getPooled(enterEventType, to, nativeEvent, nativeEventTarget);
    enter.type = eventTypePrefix + 'enter';
    enter.target = toNode;
    enter.relatedTarget = fromNode;
    accumulateEnterLeaveDispatches(leave, enter, from, to); // If we are not processing the first ancestor, then we
    // should not process the same nativeEvent again, as we
    // will have already processed it in the first ancestor.

    if ((eventSystemFlags & IS_FIRST_ANCESTOR) === 0) {
      return [leave];
    }

    return [leave, enter];
  }
};

/**
 * inlined Object.is polyfill to avoid requiring consumers ship their own
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
 */
function is(x, y) {
  return x === y && (x !== 0 || 1 / x === 1 / y) || x !== x && y !== y // eslint-disable-line no-self-compare
  ;
}

var objectIs = typeof Object.is === 'function' ? Object.is : is;

var hasOwnProperty$2 = Object.prototype.hasOwnProperty;
/**
 * Performs equality by iterating through keys on an object and returning false
 * when any key has values which are not strictly equal between the arguments.
 * Returns true when the values of all keys are strictly equal.
 */

function shallowEqual(objA, objB) {
  if (objectIs(objA, objB)) {
    return true;
  }

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);

  if (keysA.length !== keysB.length) {
    return false;
  } // Test for A's keys different from B.


  for (var i = 0; i < keysA.length; i++) {
    if (!hasOwnProperty$2.call(objB, keysA[i]) || !objectIs(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
}

var skipSelectionChangeEvent = canUseDOM && 'documentMode' in document && document.documentMode <= 11;
var eventTypes$3 = {
  select: {
    phasedRegistrationNames: {
      bubbled: 'onSelect',
      captured: 'onSelectCapture'
    },
    dependencies: [TOP_BLUR, TOP_CONTEXT_MENU, TOP_DRAG_END, TOP_FOCUS, TOP_KEY_DOWN, TOP_KEY_UP, TOP_MOUSE_DOWN, TOP_MOUSE_UP, TOP_SELECTION_CHANGE]
  }
};
var activeElement$1 = null;
var activeElementInst$1 = null;
var lastSelection = null;
var mouseDown = false;
/**
 * Get an object which is a unique representation of the current selection.
 *
 * The return value will not be consistent across nodes or browsers, but
 * two identical selections on the same node will return identical objects.
 *
 * @param {DOMElement} node
 * @return {object}
 */

function getSelection$1(node) {
  if ('selectionStart' in node && hasSelectionCapabilities(node)) {
    return {
      start: node.selectionStart,
      end: node.selectionEnd
    };
  } else {
    var win = node.ownerDocument && node.ownerDocument.defaultView || window;
    var selection = win.getSelection();
    return {
      anchorNode: selection.anchorNode,
      anchorOffset: selection.anchorOffset,
      focusNode: selection.focusNode,
      focusOffset: selection.focusOffset
    };
  }
}
/**
 * Get document associated with the event target.
 *
 * @param {object} nativeEventTarget
 * @return {Document}
 */


function getEventTargetDocument(eventTarget) {
  return eventTarget.window === eventTarget ? eventTarget.document : eventTarget.nodeType === DOCUMENT_NODE ? eventTarget : eventTarget.ownerDocument;
}
/**
 * Poll selection to see whether it's changed.
 *
 * @param {object} nativeEvent
 * @param {object} nativeEventTarget
 * @return {?SyntheticEvent}
 */


function constructSelectEvent(nativeEvent, nativeEventTarget) {
  // Ensure we have the right element, and that the user is not dragging a
  // selection (this matches native `select` event behavior). In HTML5, select
  // fires only on input and textarea thus if there's no focused element we
  // won't dispatch.
  var doc = getEventTargetDocument(nativeEventTarget);

  if (mouseDown || activeElement$1 == null || activeElement$1 !== getActiveElement(doc)) {
    return null;
  } // Only fire when selection has actually changed.


  var currentSelection = getSelection$1(activeElement$1);

  if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
    lastSelection = currentSelection;
    var syntheticEvent = SyntheticEvent.getPooled(eventTypes$3.select, activeElementInst$1, nativeEvent, nativeEventTarget);
    syntheticEvent.type = 'select';
    syntheticEvent.target = activeElement$1;
    accumulateTwoPhaseDispatches(syntheticEvent);
    return syntheticEvent;
  }

  return null;
}
/**
 * This plugin creates an `onSelect` event that normalizes select events
 * across form elements.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - contentEditable
 *
 * This differs from native browser implementations in the following ways:
 * - Fires on contentEditable fields as well as inputs.
 * - Fires for collapsed selection.
 * - Fires after user input.
 */


var SelectEventPlugin = {
  eventTypes: eventTypes$3,
  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags, container) {
    var containerOrDoc = container || getEventTargetDocument(nativeEventTarget); // Track whether all listeners exists for this plugin. If none exist, we do
    // not extract events. See #3639.

    if (!containerOrDoc || !isListeningToAllDependencies('onSelect', containerOrDoc)) {
      return null;
    }

    var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;

    switch (topLevelType) {
      // Track the input node that has focus.
      case TOP_FOCUS:
        if (isTextInputElement(targetNode) || targetNode.contentEditable === 'true') {
          activeElement$1 = targetNode;
          activeElementInst$1 = targetInst;
          lastSelection = null;
        }

        break;

      case TOP_BLUR:
        activeElement$1 = null;
        activeElementInst$1 = null;
        lastSelection = null;
        break;
      // Don't fire the event while the user is dragging. This matches the
      // semantics of the native select event.

      case TOP_MOUSE_DOWN:
        mouseDown = true;
        break;

      case TOP_CONTEXT_MENU:
      case TOP_MOUSE_UP:
      case TOP_DRAG_END:
        mouseDown = false;
        return constructSelectEvent(nativeEvent, nativeEventTarget);
      // Chrome and IE fire non-standard event when selection is changed (and
      // sometimes when it hasn't). IE's event fires out of order with respect
      // to key and input events on deletion, so we discard it.
      //
      // Firefox doesn't support selectionchange, so check selection status
      // after each key entry. The selection changes after keydown and before
      // keyup, but we check on keydown as well in the case of holding down a
      // key, when multiple keydown events are fired but only one keyup is.
      // This is also our approach for IE handling, for the reason above.

      case TOP_SELECTION_CHANGE:
        if (skipSelectionChangeEvent) {
          break;
        }

      // falls through

      case TOP_KEY_DOWN:
      case TOP_KEY_UP:
        return constructSelectEvent(nativeEvent, nativeEventTarget);
    }

    return null;
  }
};

/**
 * @interface Event
 * @see http://www.w3.org/TR/css3-animations/#AnimationEvent-interface
 * @see https://developer.mozilla.org/en-US/docs/Web/API/AnimationEvent
 */

var SyntheticAnimationEvent = SyntheticEvent.extend({
  animationName: null,
  elapsedTime: null,
  pseudoElement: null
});

/**
 * @interface Event
 * @see http://www.w3.org/TR/clipboard-apis/
 */

var SyntheticClipboardEvent = SyntheticEvent.extend({
  clipboardData: function (event) {
    return 'clipboardData' in event ? event.clipboardData : window.clipboardData;
  }
});

/**
 * @interface FocusEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */

var SyntheticFocusEvent = SyntheticUIEvent.extend({
  relatedTarget: null
});

/**
 * `charCode` represents the actual "character code" and is safe to use with
 * `String.fromCharCode`. As such, only keys that correspond to printable
 * characters produce a valid `charCode`, the only exception to this is Enter.
 * The Tab-key is considered non-printable and does not have a `charCode`,
 * presumably because it does not produce a tab-character in browsers.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {number} Normalized `charCode` property.
 */
function getEventCharCode(nativeEvent) {
  var charCode;
  var keyCode = nativeEvent.keyCode;

  if ('charCode' in nativeEvent) {
    charCode = nativeEvent.charCode; // FF does not set `charCode` for the Enter-key, check against `keyCode`.

    if (charCode === 0 && keyCode === 13) {
      charCode = 13;
    }
  } else {
    // IE8 does not implement `charCode`, but `keyCode` has the correct value.
    charCode = keyCode;
  } // IE and Edge (on Windows) and Chrome / Safari (on Windows and Linux)
  // report Enter as charCode 10 when ctrl is pressed.


  if (charCode === 10) {
    charCode = 13;
  } // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
  // Must not discard the (non-)printable Enter-key.


  if (charCode >= 32 || charCode === 13) {
    return charCode;
  }

  return 0;
}

/**
 * Normalization of deprecated HTML5 `key` values
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */

var normalizeKey = {
  Esc: 'Escape',
  Spacebar: ' ',
  Left: 'ArrowLeft',
  Up: 'ArrowUp',
  Right: 'ArrowRight',
  Down: 'ArrowDown',
  Del: 'Delete',
  Win: 'OS',
  Menu: 'ContextMenu',
  Apps: 'ContextMenu',
  Scroll: 'ScrollLock',
  MozPrintableKey: 'Unidentified'
};
/**
 * Translation from legacy `keyCode` to HTML5 `key`
 * Only special keys supported, all others depend on keyboard layout or browser
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */

var translateToKey = {
  '8': 'Backspace',
  '9': 'Tab',
  '12': 'Clear',
  '13': 'Enter',
  '16': 'Shift',
  '17': 'Control',
  '18': 'Alt',
  '19': 'Pause',
  '20': 'CapsLock',
  '27': 'Escape',
  '32': ' ',
  '33': 'PageUp',
  '34': 'PageDown',
  '35': 'End',
  '36': 'Home',
  '37': 'ArrowLeft',
  '38': 'ArrowUp',
  '39': 'ArrowRight',
  '40': 'ArrowDown',
  '45': 'Insert',
  '46': 'Delete',
  '112': 'F1',
  '113': 'F2',
  '114': 'F3',
  '115': 'F4',
  '116': 'F5',
  '117': 'F6',
  '118': 'F7',
  '119': 'F8',
  '120': 'F9',
  '121': 'F10',
  '122': 'F11',
  '123': 'F12',
  '144': 'NumLock',
  '145': 'ScrollLock',
  '224': 'Meta'
};
/**
 * @param {object} nativeEvent Native browser event.
 * @return {string} Normalized `key` property.
 */

function getEventKey(nativeEvent) {
  if (nativeEvent.key) {
    // Normalize inconsistent values reported by browsers due to
    // implementations of a working draft specification.
    // FireFox implements `key` but returns `MozPrintableKey` for all
    // printable characters (normalized to `Unidentified`), ignore it.
    var key = normalizeKey[nativeEvent.key] || nativeEvent.key;

    if (key !== 'Unidentified') {
      return key;
    }
  } // Browser does not implement `key`, polyfill as much of it as we can.


  if (nativeEvent.type === 'keypress') {
    var charCode = getEventCharCode(nativeEvent); // The enter-key is technically both printable and non-printable and can
    // thus be captured by `keypress`, no other non-printable key should.

    return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
  }

  if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') {
    // While user keyboard layout determines the actual meaning of each
    // `keyCode` value, almost all function keys have a universal value.
    return translateToKey[nativeEvent.keyCode] || 'Unidentified';
  }

  return '';
}

/**
 * @interface KeyboardEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */

var SyntheticKeyboardEvent = SyntheticUIEvent.extend({
  key: getEventKey,
  location: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  repeat: null,
  locale: null,
  getModifierState: getEventModifierState,
  // Legacy Interface
  charCode: function (event) {
    // `charCode` is the result of a KeyPress event and represents the value of
    // the actual printable character.
    // KeyPress is deprecated, but its replacement is not yet final and not
    // implemented in any major browser. Only KeyPress has charCode.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }

    return 0;
  },
  keyCode: function (event) {
    // `keyCode` is the result of a KeyDown/Up event and represents the value of
    // physical keyboard key.
    // The actual meaning of the value depends on the users' keyboard layout
    // which cannot be detected. Assuming that it is a US keyboard layout
    // provides a surprisingly accurate mapping for US and European users.
    // Due to this, it is left to the user to implement at this time.
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }

    return 0;
  },
  which: function (event) {
    // `which` is an alias for either `keyCode` or `charCode` depending on the
    // type of the event.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }

    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }

    return 0;
  }
});

/**
 * @interface DragEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */

var SyntheticDragEvent = SyntheticMouseEvent.extend({
  dataTransfer: null
});

/**
 * @interface TouchEvent
 * @see http://www.w3.org/TR/touch-events/
 */

var SyntheticTouchEvent = SyntheticUIEvent.extend({
  touches: null,
  targetTouches: null,
  changedTouches: null,
  altKey: null,
  metaKey: null,
  ctrlKey: null,
  shiftKey: null,
  getModifierState: getEventModifierState
});

/**
 * @interface Event
 * @see http://www.w3.org/TR/2009/WD-css3-transitions-20090320/#transition-events-
 * @see https://developer.mozilla.org/en-US/docs/Web/API/TransitionEvent
 */

var SyntheticTransitionEvent = SyntheticEvent.extend({
  propertyName: null,
  elapsedTime: null,
  pseudoElement: null
});

/**
 * @interface WheelEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */

var SyntheticWheelEvent = SyntheticMouseEvent.extend({
  deltaX: function (event) {
    return 'deltaX' in event ? event.deltaX : // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
    'wheelDeltaX' in event ? -event.wheelDeltaX : 0;
  },
  deltaY: function (event) {
    return 'deltaY' in event ? event.deltaY : // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
    'wheelDeltaY' in event ? -event.wheelDeltaY : // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
    'wheelDelta' in event ? -event.wheelDelta : 0;
  },
  deltaZ: null,
  // Browsers without "deltaMode" is reporting in raw wheel delta where one
  // notch on the scroll is always +/- 120, roughly equivalent to pixels.
  // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
  // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
  deltaMode: null
});

var knownHTMLTopLevelTypes = [TOP_ABORT, TOP_CANCEL, TOP_CAN_PLAY, TOP_CAN_PLAY_THROUGH, TOP_CLOSE, TOP_DURATION_CHANGE, TOP_EMPTIED, TOP_ENCRYPTED, TOP_ENDED, TOP_ERROR, TOP_INPUT, TOP_INVALID, TOP_LOAD, TOP_LOADED_DATA, TOP_LOADED_METADATA, TOP_LOAD_START, TOP_PAUSE, TOP_PLAY, TOP_PLAYING, TOP_PROGRESS, TOP_RATE_CHANGE, TOP_RESET, TOP_SEEKED, TOP_SEEKING, TOP_STALLED, TOP_SUBMIT, TOP_SUSPEND, TOP_TIME_UPDATE, TOP_TOGGLE, TOP_VOLUME_CHANGE, TOP_WAITING];
var SimpleEventPlugin = {
  // simpleEventPluginEventTypes gets populated from
  // the DOMEventProperties module.
  eventTypes: simpleEventPluginEventTypes,
  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget, eventSystemFlags) {
    var dispatchConfig = topLevelEventsToDispatchConfig.get(topLevelType);

    if (!dispatchConfig) {
      return null;
    }

    var EventConstructor;

    switch (topLevelType) {
      case TOP_KEY_PRESS:
        // Firefox creates a keypress event for function keys too. This removes
        // the unwanted keypress events. Enter is however both printable and
        // non-printable. One would expect Tab to be as well (but it isn't).
        if (getEventCharCode(nativeEvent) === 0) {
          return null;
        }

      /* falls through */

      case TOP_KEY_DOWN:
      case TOP_KEY_UP:
        EventConstructor = SyntheticKeyboardEvent;
        break;

      case TOP_BLUR:
      case TOP_FOCUS:
        EventConstructor = SyntheticFocusEvent;
        break;

      case TOP_CLICK:
        // Firefox creates a click event on right mouse clicks. This removes the
        // unwanted click events.
        if (nativeEvent.button === 2) {
          return null;
        }

      /* falls through */

      case TOP_AUX_CLICK:
      case TOP_DOUBLE_CLICK:
      case TOP_MOUSE_DOWN:
      case TOP_MOUSE_MOVE:
      case TOP_MOUSE_UP: // TODO: Disabled elements should not respond to mouse events

      /* falls through */

      case TOP_MOUSE_OUT:
      case TOP_MOUSE_OVER:
      case TOP_CONTEXT_MENU:
        EventConstructor = SyntheticMouseEvent;
        break;

      case TOP_DRAG:
      case TOP_DRAG_END:
      case TOP_DRAG_ENTER:
      case TOP_DRAG_EXIT:
      case TOP_DRAG_LEAVE:
      case TOP_DRAG_OVER:
      case TOP_DRAG_START:
      case TOP_DROP:
        EventConstructor = SyntheticDragEvent;
        break;

      case TOP_TOUCH_CANCEL:
      case TOP_TOUCH_END:
      case TOP_TOUCH_MOVE:
      case TOP_TOUCH_START:
        EventConstructor = SyntheticTouchEvent;
        break;

      case TOP_ANIMATION_END:
      case TOP_ANIMATION_ITERATION:
      case TOP_ANIMATION_START:
        EventConstructor = SyntheticAnimationEvent;
        break;

      case TOP_TRANSITION_END:
        EventConstructor = SyntheticTransitionEvent;
        break;

      case TOP_SCROLL:
        EventConstructor = SyntheticUIEvent;
        break;

      case TOP_WHEEL:
        EventConstructor = SyntheticWheelEvent;
        break;

      case TOP_COPY:
      case TOP_CUT:
      case TOP_PASTE:
        EventConstructor = SyntheticClipboardEvent;
        break;

      case TOP_GOT_POINTER_CAPTURE:
      case TOP_LOST_POINTER_CAPTURE:
      case TOP_POINTER_CANCEL:
      case TOP_POINTER_DOWN:
      case TOP_POINTER_MOVE:
      case TOP_POINTER_OUT:
      case TOP_POINTER_OVER:
      case TOP_POINTER_UP:
        EventConstructor = SyntheticPointerEvent;
        break;

      default:
        {
          if (knownHTMLTopLevelTypes.indexOf(topLevelType) === -1) {
            error('SimpleEventPlugin: Unhandled event type, `%s`. This warning ' + 'is likely caused by a bug in React. Please file an issue.', topLevelType);
          }
        } // HTML Events
        // @see http://www.w3.org/TR/html5/index.html#events-0


        EventConstructor = SyntheticEvent;
        break;
    }

    var event = EventConstructor.getPooled(dispatchConfig, targetInst, nativeEvent, nativeEventTarget);
    accumulateTwoPhaseDispatches(event);
    return event;
  }
};

/**
 * Specifies a deterministic ordering of `EventPlugin`s. A convenient way to
 * reason about plugins, without having to package every one of them. This
 * is better than having plugins be ordered in the same order that they
 * are injected because that ordering would be influenced by the packaging order.
 * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
 * preventing default on events is convenient in `SimpleEventPlugin` handlers.
 */

var DOMEventPluginOrder = ['ResponderEventPlugin', 'SimpleEventPlugin', 'EnterLeaveEventPlugin', 'ChangeEventPlugin', 'SelectEventPlugin', 'BeforeInputEventPlugin'];
/**
 * Inject modules for resolving DOM hierarchy and plugin ordering.
 */

injectEventPluginOrder(DOMEventPluginOrder);
setComponentTree(getFiberCurrentPropsFromNode$1, getInstanceFromNode$1, getNodeFromInstance$1);
/**
 * Some important event plugins included by default (without having to require
 * them).
 */

injectEventPluginsByName({
  SimpleEventPlugin: SimpleEventPlugin,
  EnterLeaveEventPlugin: EnterLeaveEventPlugin,
  ChangeEventPlugin: ChangeEventPlugin,
  SelectEventPlugin: SelectEventPlugin,
  BeforeInputEventPlugin: BeforeInputEventPlugin
});

// Prefix measurements so that it's possible to filter them.
// Longer prefixes are hard to read in DevTools.
var reactEmoji = "\u269B";
var warningEmoji = "\u26D4";
var supportsUserTiming = typeof performance !== 'undefined' && typeof performance.mark === 'function' && typeof performance.clearMarks === 'function' && typeof performance.measure === 'function' && typeof performance.clearMeasures === 'function'; // Keep track of current fiber so that we know the path to unwind on pause.
// TODO: this looks the same as nextUnitOfWork in scheduler. Can we unify them?

var currentFiber = null; // If we're in the middle of user code, which fiber and method is it?
// Reusing `currentFiber` would be confusing for this because user code fiber
// can change during commit phase too, but we don't need to unwind it (since
// lifecycles in the commit phase don't resemble a tree).

var currentPhase = null;
var currentPhaseFiber = null; // Did lifecycle hook schedule an update? This is often a performance problem,
// so we will keep track of it, and include it in the report.
// Track commits caused by cascading updates.

var isCommitting = false;
var hasScheduledUpdateInCurrentCommit = false;
var hasScheduledUpdateInCurrentPhase = false;
var commitCountInCurrentWorkLoop = 0;
var effectCountInCurrentCommit = 0;
// to avoid stretch the commit phase with measurement overhead.

var labelsInCurrentCommit = new Set();

var formatMarkName = function (markName) {
  return reactEmoji + " " + markName;
};

var formatLabel = function (label, warning) {
  var prefix = warning ? warningEmoji + " " : reactEmoji + " ";
  var suffix = warning ? " Warning: " + warning : '';
  return "" + prefix + label + suffix;
};

var beginMark = function (markName) {
  performance.mark(formatMarkName(markName));
};

var clearMark = function (markName) {
  performance.clearMarks(formatMarkName(markName));
};

var endMark = function (label, markName, warning) {
  var formattedMarkName = formatMarkName(markName);
  var formattedLabel = formatLabel(label, warning);

  try {
    performance.measure(formattedLabel, formattedMarkName);
  } catch (err) {} // If previous mark was missing for some reason, this will throw.
  // This could only happen if React crashed in an unexpected place earlier.
  // Don't pile on with more errors.
  // Clear marks immediately to avoid growing buffer.


  performance.clearMarks(formattedMarkName);
  performance.clearMeasures(formattedLabel);
};

var getFiberMarkName = function (label, debugID) {
  return label + " (#" + debugID + ")";
};

var getFiberLabel = function (componentName, isMounted, phase) {
  if (phase === null) {
    // These are composite component total time measurements.
    return componentName + " [" + (isMounted ? 'update' : 'mount') + "]";
  } else {
    // Composite component methods.
    return componentName + "." + phase;
  }
};

var beginFiberMark = function (fiber, phase) {
  var componentName = getComponentName(fiber.type) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);

  if (isCommitting && labelsInCurrentCommit.has(label)) {
    // During the commit phase, we don't show duplicate labels because
    // there is a fixed overhead for every measurement, and we don't
    // want to stretch the commit phase beyond necessary.
    return false;
  }

  labelsInCurrentCommit.add(label);
  var markName = getFiberMarkName(label, debugID);
  beginMark(markName);
  return true;
};

var clearFiberMark = function (fiber, phase) {
  var componentName = getComponentName(fiber.type) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);
  var markName = getFiberMarkName(label, debugID);
  clearMark(markName);
};

var endFiberMark = function (fiber, phase, warning) {
  var componentName = getComponentName(fiber.type) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);
  var markName = getFiberMarkName(label, debugID);
  endMark(label, markName, warning);
};

var shouldIgnoreFiber = function (fiber) {
  // Host components should be skipped in the timeline.
  // We could check typeof fiber.type, but does this work with RN?
  switch (fiber.tag) {
    case HostRoot:
    case HostComponent:
    case HostText:
    case HostPortal:
    case Fragment:
    case ContextProvider:
    case ContextConsumer:
    case Mode:
      return true;

    default:
      return false;
  }
};

var clearPendingPhaseMeasurement = function () {
  if (currentPhase !== null && currentPhaseFiber !== null) {
    clearFiberMark(currentPhaseFiber, currentPhase);
  }

  currentPhaseFiber = null;
  currentPhase = null;
  hasScheduledUpdateInCurrentPhase = false;
};

var pauseTimers = function () {
  // Stops all currently active measurements so that they can be resumed
  // if we continue in a later deferred loop from the same unit of work.
  var fiber = currentFiber;

  while (fiber) {
    if (fiber._debugIsCurrentlyTiming) {
      endFiberMark(fiber, null, null);
    }

    fiber = fiber.return;
  }
};

var resumeTimersRecursively = function (fiber) {
  if (fiber.return !== null) {
    resumeTimersRecursively(fiber.return);
  }

  if (fiber._debugIsCurrentlyTiming) {
    beginFiberMark(fiber, null);
  }
};

var resumeTimers = function () {
  // Resumes all measurements that were active during the last deferred loop.
  if (currentFiber !== null) {
    resumeTimersRecursively(currentFiber);
  }
};

function recordEffect() {
  {
    effectCountInCurrentCommit++;
  }
}
function recordScheduleUpdate() {
  {
    if (isCommitting) {
      hasScheduledUpdateInCurrentCommit = true;
    }

    if (currentPhase !== null && currentPhase !== 'componentWillMount' && currentPhase !== 'componentWillReceiveProps') {
      hasScheduledUpdateInCurrentPhase = true;
    }
  }
}
function startWorkTimer(fiber) {
  {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    } // If we pause, this is the fiber to unwind from.


    currentFiber = fiber;

    if (!beginFiberMark(fiber, null)) {
      return;
    }

    fiber._debugIsCurrentlyTiming = true;
  }
}
function cancelWorkTimer(fiber) {
  {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    } // Remember we shouldn't complete measurement for this fiber.
    // Otherwise flamechart will be deep even for small updates.


    fiber._debugIsCurrentlyTiming = false;
    clearFiberMark(fiber, null);
  }
}
function stopWorkTimer(fiber) {
  {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    } // If we pause, its parent is the fiber to unwind from.


    currentFiber = fiber.return;

    if (!fiber._debugIsCurrentlyTiming) {
      return;
    }

    fiber._debugIsCurrentlyTiming = false;
    endFiberMark(fiber, null, null);
  }
}
function stopFailedWorkTimer(fiber) {
  {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    } // If we pause, its parent is the fiber to unwind from.


    currentFiber = fiber.return;

    if (!fiber._debugIsCurrentlyTiming) {
      return;
    }

    fiber._debugIsCurrentlyTiming = false;
    var warning = fiber.tag === SuspenseComponent ? 'Rendering was suspended' : 'An error was thrown inside this error boundary';
    endFiberMark(fiber, null, warning);
  }
}
function startPhaseTimer(fiber, phase) {
  {
    if (!supportsUserTiming) {
      return;
    }

    clearPendingPhaseMeasurement();

    if (!beginFiberMark(fiber, phase)) {
      return;
    }

    currentPhaseFiber = fiber;
    currentPhase = phase;
  }
}
function stopPhaseTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    if (currentPhase !== null && currentPhaseFiber !== null) {
      var warning = hasScheduledUpdateInCurrentPhase ? 'Scheduled a cascading update' : null;
      endFiberMark(currentPhaseFiber, currentPhase, warning);
    }

    currentPhase = null;
    currentPhaseFiber = null;
  }
}
function startWorkLoopTimer(nextUnitOfWork) {
  {
    currentFiber = nextUnitOfWork;

    if (!supportsUserTiming) {
      return;
    }

    commitCountInCurrentWorkLoop = 0; // This is top level call.
    // Any other measurements are performed within.

    beginMark('(React Tree Reconciliation)'); // Resume any measurements that were in progress during the last loop.

    resumeTimers();
  }
}
function stopWorkLoopTimer(interruptedBy, didCompleteRoot) {
  {
    if (!supportsUserTiming) {
      return;
    }

    var warning = null;

    if (interruptedBy !== null) {
      if (interruptedBy.tag === HostRoot) {
        warning = 'A top-level update interrupted the previous render';
      } else {
        var componentName = getComponentName(interruptedBy.type) || 'Unknown';
        warning = "An update to " + componentName + " interrupted the previous render";
      }
    } else if (commitCountInCurrentWorkLoop > 1) {
      warning = 'There were cascading updates';
    }

    commitCountInCurrentWorkLoop = 0;
    var label = didCompleteRoot ? '(React Tree Reconciliation: Completed Root)' : '(React Tree Reconciliation: Yielded)'; // Pause any measurements until the next loop.

    pauseTimers();
    endMark(label, '(React Tree Reconciliation)', warning);
  }
}
function startCommitTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    isCommitting = true;
    hasScheduledUpdateInCurrentCommit = false;
    labelsInCurrentCommit.clear();
    beginMark('(Committing Changes)');
  }
}
function stopCommitTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    var warning = null;

    if (hasScheduledUpdateInCurrentCommit) {
      warning = 'Lifecycle hook scheduled a cascading update';
    } else if (commitCountInCurrentWorkLoop > 0) {
      warning = 'Caused by a cascading update in earlier commit';
    }

    hasScheduledUpdateInCurrentCommit = false;
    commitCountInCurrentWorkLoop++;
    isCommitting = false;
    labelsInCurrentCommit.clear();
    endMark('(Committing Changes)', '(Committing Changes)', warning);
  }
}
function startCommitSnapshotEffectsTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    effectCountInCurrentCommit = 0;
    beginMark('(Committing Snapshot Effects)');
  }
}
function stopCommitSnapshotEffectsTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark("(Committing Snapshot Effects: " + count + " Total)", '(Committing Snapshot Effects)', null);
  }
}
function startCommitHostEffectsTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    effectCountInCurrentCommit = 0;
    beginMark('(Committing Host Effects)');
  }
}
function stopCommitHostEffectsTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark("(Committing Host Effects: " + count + " Total)", '(Committing Host Effects)', null);
  }
}
function startCommitLifeCyclesTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    effectCountInCurrentCommit = 0;
    beginMark('(Calling Lifecycle Methods)');
  }
}
function stopCommitLifeCyclesTimer() {
  {
    if (!supportsUserTiming) {
      return;
    }

    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark("(Calling Lifecycle Methods: " + count + " Total)", '(Calling Lifecycle Methods)', null);
  }
}

var valueStack = [];
var fiberStack;

{
  fiberStack = [];
}

var index = -1;

function createCursor(defaultValue) {
  return {
    current: defaultValue
  };
}

function pop(cursor, fiber) {
  if (index < 0) {
    {
      error('Unexpected pop.');
    }

    return;
  }

  {
    if (fiber !== fiberStack[index]) {
      error('Unexpected Fiber popped.');
    }
  }

  cursor.current = valueStack[index];
  valueStack[index] = null;

  {
    fiberStack[index] = null;
  }

  index--;
}

function push(cursor, value, fiber) {
  index++;
  valueStack[index] = cursor.current;

  {
    fiberStack[index] = fiber;
  }

  cursor.current = value;
}

var warnedAboutMissingGetChildContext;

{
  warnedAboutMissingGetChildContext = {};
}

var emptyContextObject = {};

{
  Object.freeze(emptyContextObject);
} // A cursor to the current merged context object on the stack.


var contextStackCursor = createCursor(emptyContextObject); // A cursor to a boolean indicating whether the context has changed.

var didPerformWorkStackCursor = createCursor(false); // Keep track of the previous context object that was on the stack.
// We use this to get access to the parent context after we have already
// pushed the next context provider, and now need to merge their contexts.

var previousContext = emptyContextObject;

function getUnmaskedContext(workInProgress, Component, didPushOwnContextIfProvider) {
  {
    if (didPushOwnContextIfProvider && isContextProvider(Component)) {
      // If the fiber is a context provider itself, when we read its context
      // we may have already pushed its own child context on the stack. A context
      // provider should not "see" its own child context. Therefore we read the
      // previous (parent) context instead for a context provider.
      return previousContext;
    }

    return contextStackCursor.current;
  }
}

function cacheContext(workInProgress, unmaskedContext, maskedContext) {
  {
    var instance = workInProgress.stateNode;
    instance.__reactInternalMemoizedUnmaskedChildContext = unmaskedContext;
    instance.__reactInternalMemoizedMaskedChildContext = maskedContext;
  }
}

function getMaskedContext(workInProgress, unmaskedContext) {
  {
    var type = workInProgress.type;
    var contextTypes = type.contextTypes;

    if (!contextTypes) {
      return emptyContextObject;
    } // Avoid recreating masked context unless unmasked context has changed.
    // Failing to do this will result in unnecessary calls to componentWillReceiveProps.
    // This may trigger infinite loops if componentWillReceiveProps calls setState.


    var instance = workInProgress.stateNode;

    if (instance && instance.__reactInternalMemoizedUnmaskedChildContext === unmaskedContext) {
      return instance.__reactInternalMemoizedMaskedChildContext;
    }

    var context = {};

    for (var key in contextTypes) {
      context[key] = unmaskedContext[key];
    }

    {
      var name = getComponentName(type) || 'Unknown';
      checkPropTypes(contextTypes, context, 'context', name, getCurrentFiberStackInDev);
    } // Cache unmasked context so we can avoid recreating masked context unless necessary.
    // Context is created before the class component is instantiated so check for instance.


    if (instance) {
      cacheContext(workInProgress, unmaskedContext, context);
    }

    return context;
  }
}

function hasContextChanged() {
  {
    return didPerformWorkStackCursor.current;
  }
}

function isContextProvider(type) {
  {
    var childContextTypes = type.childContextTypes;
    return childContextTypes !== null && childContextTypes !== undefined;
  }
}

function popContext(fiber) {
  {
    pop(didPerformWorkStackCursor, fiber);
    pop(contextStackCursor, fiber);
  }
}

function popTopLevelContextObject(fiber) {
  {
    pop(didPerformWorkStackCursor, fiber);
    pop(contextStackCursor, fiber);
  }
}

function pushTopLevelContextObject(fiber, context, didChange) {
  {
    if (!(contextStackCursor.current === emptyContextObject)) {
      {
        throw Error( "Unexpected context found on stack. This error is likely caused by a bug in React. Please file an issue." );
      }
    }

    push(contextStackCursor, context, fiber);
    push(didPerformWorkStackCursor, didChange, fiber);
  }
}

function processChildContext(fiber, type, parentContext) {
  {
    var instance = fiber.stateNode;
    var childContextTypes = type.childContextTypes; // TODO (bvaughn) Replace this behavior with an invariant() in the future.
    // It has only been added in Fiber to match the (unintentional) behavior in Stack.

    if (typeof instance.getChildContext !== 'function') {
      {
        var componentName = getComponentName(type) || 'Unknown';

        if (!warnedAboutMissingGetChildContext[componentName]) {
          warnedAboutMissingGetChildContext[componentName] = true;

          error('%s.childContextTypes is specified but there is no getChildContext() method ' + 'on the instance. You can either define getChildContext() on %s or remove ' + 'childContextTypes from it.', componentName, componentName);
        }
      }

      return parentContext;
    }

    var childContext;

    {
      setCurrentPhase('getChildContext');
    }

    startPhaseTimer(fiber, 'getChildContext');
    childContext = instance.getChildContext();
    stopPhaseTimer();

    {
      setCurrentPhase(null);
    }

    for (var contextKey in childContext) {
      if (!(contextKey in childContextTypes)) {
        {
          throw Error( (getComponentName(type) || 'Unknown') + ".getChildContext(): key \"" + contextKey + "\" is not defined in childContextTypes." );
        }
      }
    }

    {
      var name = getComponentName(type) || 'Unknown';
      checkPropTypes(childContextTypes, childContext, 'child context', name, // In practice, there is one case in which we won't get a stack. It's when
      // somebody calls unstable_renderSubtreeIntoContainer() and we process
      // context from the parent component instance. The stack will be missing
      // because it's outside of the reconciliation, and so the pointer has not
      // been set. This is rare and doesn't matter. We'll also remove that API.
      getCurrentFiberStackInDev);
    }

    return _assign({}, parentContext, {}, childContext);
  }
}

function pushContextProvider(workInProgress) {
  {
    var instance = workInProgress.stateNode; // We push the context as early as possible to ensure stack integrity.
    // If the instance does not exist yet, we will push null at first,
    // and replace it on the stack later when invalidating the context.

    var memoizedMergedChildContext = instance && instance.__reactInternalMemoizedMergedChildContext || emptyContextObject; // Remember the parent context so we can merge with it later.
    // Inherit the parent's did-perform-work value to avoid inadvertently blocking updates.

    previousContext = contextStackCursor.current;
    push(contextStackCursor, memoizedMergedChildContext, workInProgress);
    push(didPerformWorkStackCursor, didPerformWorkStackCursor.current, workInProgress);
    return true;
  }
}

function invalidateContextProvider(workInProgress, type, didChange) {
  {
    var instance = workInProgress.stateNode;

    if (!instance) {
      {
        throw Error( "Expected to have an instance by this point. This error is likely caused by a bug in React. Please file an issue." );
      }
    }

    if (didChange) {
      // Merge parent and own context.
      // Skip this if we're not updating due to sCU.
      // This avoids unnecessarily recomputing memoized values.
      var mergedContext = processChildContext(workInProgress, type, previousContext);
      instance.__reactInternalMemoizedMergedChildContext = mergedContext; // Replace the old (or empty) context with the new one.
      // It is important to unwind the context in the reverse order.

      pop(didPerformWorkStackCursor, workInProgress);
      pop(contextStackCursor, workInProgress); // Now push the new context and mark that it has changed.

      push(contextStackCursor, mergedContext, workInProgress);
      push(didPerformWorkStackCursor, didChange, workInProgress);
    } else {
      pop(didPerformWorkStackCursor, workInProgress);
      push(didPerformWorkStackCursor, didChange, workInProgress);
    }
  }
}

function findCurrentUnmaskedContext(fiber) {
  {
    // Currently this is only used with renderSubtreeIntoContainer; not sure if it
    // makes sense elsewhere
    if (!(isFiberMounted(fiber) && fiber.tag === ClassComponent)) {
      {
        throw Error( "Expected subtree parent to be a mounted class component. This error is likely caused by a bug in React. Please file an issue." );
      }
    }

    var node = fiber;

    do {
      switch (node.tag) {
        case HostRoot:
          return node.stateNode.context;

        case ClassComponent:
          {
            var Component = node.type;

            if (isContextProvider(Component)) {
              return node.stateNode.__reactInternalMemoizedMergedChildContext;
            }

            break;
          }
      }

      node = node.return;
    } while (node !== null);

    {
      {
        throw Error( "Found unexpected detached subtree parent. This error is likely caused by a bug in React. Please file an issue." );
      }
    }
  }
}

var LegacyRoot = 0;
var BlockingRoot = 1;
var ConcurrentRoot = 2;

var Scheduler_runWithPriority = Scheduler.unstable_runWithPriority,
    Scheduler_scheduleCallback = Scheduler.unstable_scheduleCallback,
    Scheduler_cancelCallback = Scheduler.unstable_cancelCallback,
    Scheduler_shouldYield = Scheduler.unstable_shouldYield,
    Scheduler_requestPaint = Scheduler.unstable_requestPaint,
    Scheduler_now = Scheduler.unstable_now,
    Scheduler_getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel,
    Scheduler_ImmediatePriority = Scheduler.unstable_ImmediatePriority,
    Scheduler_UserBlockingPriority = Scheduler.unstable_UserBlockingPriority,
    Scheduler_NormalPriority = Scheduler.unstable_NormalPriority,
    Scheduler_LowPriority = Scheduler.unstable_LowPriority,
    Scheduler_IdlePriority = Scheduler.unstable_IdlePriority;

{
  // Provide explicit error message when production+profiling bundle of e.g.
  // react-dom is used with production (non-profiling) bundle of
  // scheduler/tracing
  if (!(tracing.__interactionsRef != null && tracing.__interactionsRef.current != null)) {
    {
      throw Error( "It is not supported to run the profiling version of a renderer (for example, `react-dom/profiling`) without also replacing the `scheduler/tracing` module with `scheduler/tracing-profiling`. Your bundler might have a setting for aliasing both modules. Learn more at http://fb.me/react-profiling" );
    }
  }
}

var fakeCallbackNode = {}; // Except for NoPriority, these correspond to Scheduler priorities. We use
// ascending numbers so we can compare them like numbers. They start at 90 to
// avoid clashing with Scheduler's priorities.

var ImmediatePriority = 99;
var UserBlockingPriority$1 = 98;
var NormalPriority = 97;
var LowPriority = 96;
var IdlePriority = 95; // NoPriority is the absence of priority. Also React-only.

var NoPriority = 90;
var shouldYield = Scheduler_shouldYield;
var requestPaint = // Fall back gracefully if we're running an older version of Scheduler.
Scheduler_requestPaint !== undefined ? Scheduler_requestPaint : function () {};
var syncQueue = null;
var immediateQueueCallbackNode = null;
var isFlushingSyncQueue = false;
var initialTimeMs = Scheduler_now(); // If the initial timestamp is reasonably small, use Scheduler's `now` directly.
// This will be the case for modern browsers that support `performance.now`. In
// older browsers, Scheduler falls back to `Date.now`, which returns a Unix
// timestamp. In that case, subtract the module initialization time to simulate
// the behavior of performance.now and keep our times small enough to fit
// within 32 bits.
// TODO: Consider lifting this into Scheduler.

var now = initialTimeMs < 10000 ? Scheduler_now : function () {
  return Scheduler_now() - initialTimeMs;
};
function getCurrentPriorityLevel() {
  switch (Scheduler_getCurrentPriorityLevel()) {
    case Scheduler_ImmediatePriority:
      return ImmediatePriority;

    case Scheduler_UserBlockingPriority:
      return UserBlockingPriority$1;

    case Scheduler_NormalPriority:
      return NormalPriority;

    case Scheduler_LowPriority:
      return LowPriority;

    case Scheduler_IdlePriority:
      return IdlePriority;

    default:
      {
        {
          throw Error( "Unknown priority level." );
        }
      }

  }
}

function reactPriorityToSchedulerPriority(reactPriorityLevel) {
  switch (reactPriorityLevel) {
    case ImmediatePriority:
      return Scheduler_ImmediatePriority;

    case UserBlockingPriority$1:
      return Scheduler_UserBlockingPriority;

    case NormalPriority:
      return Scheduler_NormalPriority;

    case LowPriority:
      return Scheduler_LowPriority;

    case IdlePriority:
      return Scheduler_IdlePriority;

    default:
      {
        {
          throw Error( "Unknown priority level." );
        }
      }

  }
}

function runWithPriority$1(reactPriorityLevel, fn) {
  var priorityLevel = reactPriorityToSchedulerPriority(reactPriorityLevel);
  return Scheduler_runWithPriority(priorityLevel, fn);
}
function scheduleCallback(reactPriorityLevel, callback, options) {
  var priorityLevel = reactPriorityToSchedulerPriority(reactPriorityLevel);
  return Scheduler_scheduleCallback(priorityLevel, callback, options);
}
function scheduleSyncCallback(callback) {
  // Push this callback into an internal queue. We'll flush these either in
  // the next tick, or earlier if something calls `flushSyncCallbackQueue`.
  if (syncQueue === null) {
    syncQueue = [callback]; // Flush the queue in the next tick, at the earliest.

    immediateQueueCallbackNode = Scheduler_scheduleCallback(Scheduler_ImmediatePriority, flushSyncCallbackQueueImpl);
  } else {
    // Push onto existing queue. Don't need to schedule a callback because
    // we already scheduled one when we created the queue.
    syncQueue.push(callback);
  }

  return fakeCallbackNode;
}
function cancelCallback(callbackNode) {
  if (callbackNode !== fakeCallbackNode) {
    Scheduler_cancelCallback(callbackNode);
  }
}
function flushSyncCallbackQueue() {
  if (immediateQueueCallbackNode !== null) {
    var node = immediateQueueCallbackNode;
    immediateQueueCallbackNode = null;
    Scheduler_cancelCallback(node);
  }

  flushSyncCallbackQueueImpl();
}

function flushSyncCallbackQueueImpl() {
  if (!isFlushingSyncQueue && syncQueue !== null) {
    // Prevent re-entrancy.
    isFlushingSyncQueue = true;
    var i = 0;

    try {
      var _isSync = true;
      var queue = syncQueue;
      runWithPriority$1(ImmediatePriority, function () {
        for (; i < queue.length; i++) {
          var callback = queue[i];

          do {
            callback = callback(_isSync);
          } while (callback !== null);
        }
      });
      syncQueue = null;
    } catch (error) {
      // If something throws, leave the remaining callbacks on the queue.
      if (syncQueue !== null) {
        syncQueue = syncQueue.slice(i + 1);
      } // Resume flushing in the next tick


      Scheduler_scheduleCallback(Scheduler_ImmediatePriority, flushSyncCallbackQueue);
      throw error;
    } finally {
      isFlushingSyncQueue = false;
    }
  }
}

var NoMode = 0;
var StrictMode = 1; // TODO: Remove BlockingMode and ConcurrentMode by reading from the root
// tag instead

var BlockingMode = 2;
var ConcurrentMode = 4;
var ProfileMode = 8;

// Max 31 bit integer. The max integer size in V8 for 32-bit systems.
// Math.pow(2, 30) - 1
// 0b111111111111111111111111111111
var MAX_SIGNED_31_BIT_INT = 1073741823;

var NoWork = 0; // TODO: Think of a better name for Never. The key difference with Idle is that
// Never work can be committed in an inconsistent state without tearing the UI.
// The main example is offscreen content, like a hidden subtree. So one possible
// name is Offscreen. However, it also includes dehydrated Suspense boundaries,
// which are inconsistent in the sense that they haven't finished yet, but
// aren't visibly inconsistent because the server rendered HTML matches what the
// hydrated tree would look like.

var Never = 1; // Idle is slightly higher priority than Never. It must completely finish in
// order to be consistent.

var Idle = 2; // Continuous Hydration is slightly higher than Idle and is used to increase
// priority of hover targets.

var ContinuousHydration = 3;
var Sync = MAX_SIGNED_31_BIT_INT;
var Batched = Sync - 1;
var UNIT_SIZE = 10;
var MAGIC_NUMBER_OFFSET = Batched - 1; // 1 unit of expiration time represents 10ms.

function msToExpirationTime(ms) {
  // Always subtract from the offset so that we don't clash with the magic number for NoWork.
  return MAGIC_NUMBER_OFFSET - (ms / UNIT_SIZE | 0);
}
function expirationTimeToMs(expirationTime) {
  return (MAGIC_NUMBER_OFFSET - expirationTime) * UNIT_SIZE;
}

function ceiling(num, precision) {
  return ((num / precision | 0) + 1) * precision;
}

function computeExpirationBucket(currentTime, expirationInMs, bucketSizeMs) {
  return MAGIC_NUMBER_OFFSET - ceiling(MAGIC_NUMBER_OFFSET - currentTime + expirationInMs / UNIT_SIZE, bucketSizeMs / UNIT_SIZE);
} // TODO: This corresponds to Scheduler's NormalPriority, not LowPriority. Update
// the names to reflect.


var LOW_PRIORITY_EXPIRATION = 5000;
var LOW_PRIORITY_BATCH_SIZE = 250;
function computeAsyncExpiration(currentTime) {
  return computeExpirationBucket(currentTime, LOW_PRIORITY_EXPIRATION, LOW_PRIORITY_BATCH_SIZE);
}
function computeSuspenseExpiration(currentTime, timeoutMs) {
  // TODO: Should we warn if timeoutMs is lower than the normal pri expiration time?
  return computeExpirationBucket(currentTime, timeoutMs, LOW_PRIORITY_BATCH_SIZE);
} // We intentionally set a higher expiration time for interactive updates in
// dev than in production.
//
// If the main thread is being blocked so long that you hit the expiration,
// it's a problem that could be solved with better scheduling.
//
// People will be more likely to notice this and fix it with the long
// expiration time in development.
//
// In production we opt for better UX at the risk of masking scheduling
// problems, by expiring fast.

var HIGH_PRIORITY_EXPIRATION =  500 ;
var HIGH_PRIORITY_BATCH_SIZE = 100;
function computeInteractiveExpiration(currentTime) {
  return computeExpirationBucket(currentTime, HIGH_PRIORITY_EXPIRATION, HIGH_PRIORITY_BATCH_SIZE);
}
function inferPriorityFromExpirationTime(currentTime, expirationTime) {
  if (expirationTime === Sync) {
    return ImmediatePriority;
  }

  if (expirationTime === Never || expirationTime === Idle) {
    return IdlePriority;
  }

  var msUntil = expirationTimeToMs(expirationTime) - expirationTimeToMs(currentTime);

  if (msUntil <= 0) {
    return ImmediatePriority;
  }

  if (msUntil <= HIGH_PRIORITY_EXPIRATION + HIGH_PRIORITY_BATCH_SIZE) {
    return UserBlockingPriority$1;
  }

  if (msUntil <= LOW_PRIORITY_EXPIRATION + LOW_PRIORITY_BATCH_SIZE) {
    return NormalPriority;
  } // TODO: Handle LowPriority
  // Assume anything lower has idle priority


  return IdlePriority;
}

var ReactStrictModeWarnings = {
  recordUnsafeLifecycleWarnings: function (fiber, instance) {},
  flushPendingUnsafeLifecycleWarnings: function () {},
  recordLegacyContextWarning: function (fiber, instance) {},
  flushLegacyContextWarning: function () {},
  discardPendingWarnings: function () {}
};

{
  var findStrictRoot = function (fiber) {
    var maybeStrictRoot = null;
    var node = fiber;

    while (node !== null) {
      if (node.mode & StrictMode) {
        maybeStrictRoot = node;
      }

      node = node.return;
    }

    return maybeStrictRoot;
  };

  var setToSortedString = function (set) {
    var array = [];
    set.forEach(function (value) {
      array.push(value);
    });
    return array.sort().join(', ');
  };

  var pendingComponentWillMountWarnings = [];
  var pendingUNSAFE_ComponentWillMountWarnings = [];
  var pendingComponentWillReceivePropsWarnings = [];
  var pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
  var pendingComponentWillUpdateWarnings = [];
  var pendingUNSAFE_ComponentWillUpdateWarnings = []; // Tracks components we have already warned about.

  var didWarnAboutUnsafeLifecycles = new Set();

  ReactStrictModeWarnings.recordUnsafeLifecycleWarnings = function (fiber, instance) {
    // Dedup strategy: Warn once per component.
    if (didWarnAboutUnsafeLifecycles.has(fiber.type)) {
      return;
    }

    if (typeof instance.componentWillMount === 'function' && // Don't warn about react-lifecycles-compat polyfilled components.
    instance.componentWillMount.__suppressDeprecationWarning !== true) {
      pendingComponentWillMountWarnings.push(fiber);
    }

    if (fiber.mode & StrictMode && typeof instance.UNSAFE_componentWillMount === 'function') {
      pendingUNSAFE_ComponentWillMountWarnings.push(fiber);
    }

    if (typeof instance.componentWillReceiveProps === 'function' && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) {
      pendingComponentWillReceivePropsWarnings.push(fiber);
    }

    if (fiber.mode & StrictMode && typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
      pendingUNSAFE_ComponentWillReceivePropsWarnings.push(fiber);
    }

    if (typeof instance.componentWillUpdate === 'function' && instance.componentWillUpdate.__suppressDeprecationWarning !== true) {
      pendingComponentWillUpdateWarnings.push(fiber);
    }

    if (fiber.mode & StrictMode && typeof instance.UNSAFE_componentWillUpdate === 'function') {
      pendingUNSAFE_ComponentWillUpdateWarnings.push(fiber);
    }
  };

  ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings = function () {
    // We do an initial pass to gather component names
    var componentWillMountUniqueNames = new Set();

    if (pendingComponentWillMountWarnings.length > 0) {
      pendingComponentWillMountWarnings.forEach(function (fiber) {
        componentWillMountUniqueNames.add(getComponentName(fiber.type) || 'Component');
        didWarnAboutUnsafeLifecycles.add(fiber.type);
      });
      pendingComponentWillMountWarnings = [];
    }

    var UNSAFE_componentWillMountUniqueNames = new Set();

    if (pendingUNSAFE_ComponentWillMountWarnings.length > 0) {
      pendingUNSAFE_ComponentWillMountWarnings.forEach(function (fiber) {
        UNSAFE_componentWillMountUniqueNames.add(getComponentName(fiber.type) || 'Component');
        didWarnAboutUnsafeLifecycles.add(fiber.type);
      });
      pendingUNSAFE_ComponentWillMountWarnings = [];
    }

    var componentWillReceivePropsUniqueNames = new Set();

    if (pendingComponentWillReceivePropsWarnings.length > 0) {
      pendingComponentWillReceivePropsWarnings.forEach(function (fiber) {
        componentWillReceivePropsUniqueNames.add(getComponentName(fiber.type) || 'Component');
        didWarnAboutUnsafeLifecycles.add(fiber.type);
      });
      pendingComponentWillReceivePropsWarnings = [];
    }

    var UNSAFE_componentWillReceivePropsUniqueNames = new Set();

    if (pendingUNSAFE_ComponentWillReceivePropsWarnings.length > 0) {
      pendingUNSAFE_ComponentWillReceivePropsWarnings.forEach(function (fiber) {
        UNSAFE_componentWillReceivePropsUniqueNames.add(getComponentName(fiber.type) || 'Component');
        didWarnAboutUnsafeLifecycles.add(fiber.type);
      });
      pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
    }

    var componentWillUpdateUniqueNames = new Set();

    if (pendingComponentWillUpdateWarnings.length > 0) {
      pendingComponentWillUpdateWarnings.forEach(function (fiber) {
        componentWillUpdateUniqueNames.add(getComponentName(fiber.type) || 'Component');
        didWarnAboutUnsafeLifecycles.add(fiber.type);
      });
      pendingComponentWillUpdateWarnings = [];
    }

    var UNSAFE_componentWillUpdateUniqueNames = new Set();

    if (pendingUNSAFE_ComponentWillUpdateWarnings.length > 0) {
      pendingUNSAFE_ComponentWillUpdateWarnings.forEach(function (fiber) {
        UNSAFE_componentWillUpdateUniqueNames.add(getComponentName(fiber.type) || 'Component');
        didWarnAboutUnsafeLifecycles.add(fiber.type);
      });
      pendingUNSAFE_ComponentWillUpdateWarnings = [];
    } // Finally, we flush all the warnings
    // UNSAFE_ ones before the deprecated ones, since they'll be 'louder'


    if (UNSAFE_componentWillMountUniqueNames.size > 0) {
      var sortedNames = setToSortedString(UNSAFE_componentWillMountUniqueNames);

      error('Using UNSAFE_componentWillMount in strict mode is not recommended and may indicate bugs in your code. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move code with side effects to componentDidMount, and set initial state in the constructor.\n' + '\nPlease update the following components: %s', sortedNames);
    }

    if (UNSAFE_componentWillReceivePropsUniqueNames.size > 0) {
      var _sortedNames = setToSortedString(UNSAFE_componentWillReceivePropsUniqueNames);

      error('Using UNSAFE_componentWillReceiveProps in strict mode is not recommended ' + 'and may indicate bugs in your code. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move data fetching code or side effects to componentDidUpdate.\n' + "* If you're updating state whenever props change, " + 'refactor your code to use memoization techniques or move it to ' + 'static getDerivedStateFromProps. Learn more at: https://fb.me/react-derived-state\n' + '\nPlease update the following components: %s', _sortedNames);
    }

    if (UNSAFE_componentWillUpdateUniqueNames.size > 0) {
      var _sortedNames2 = setToSortedString(UNSAFE_componentWillUpdateUniqueNames);

      error('Using UNSAFE_componentWillUpdate in strict mode is not recommended ' + 'and may indicate bugs in your code. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move data fetching code or side effects to componentDidUpdate.\n' + '\nPlease update the following components: %s', _sortedNames2);
    }

    if (componentWillMountUniqueNames.size > 0) {
      var _sortedNames3 = setToSortedString(componentWillMountUniqueNames);

      warn('componentWillMount has been renamed, and is not recommended for use. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move code with side effects to componentDidMount, and set initial state in the constructor.\n' + '* Rename componentWillMount to UNSAFE_componentWillMount to suppress ' + 'this warning in non-strict mode. In React 17.x, only the UNSAFE_ name will work. ' + 'To rename all deprecated lifecycles to their new names, you can run ' + '`npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n' + '\nPlease update the following components: %s', _sortedNames3);
    }

    if (componentWillReceivePropsUniqueNames.size > 0) {
      var _sortedNames4 = setToSortedString(componentWillReceivePropsUniqueNames);

      warn('componentWillReceiveProps has been renamed, and is not recommended for use. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move data fetching code or side effects to componentDidUpdate.\n' + "* If you're updating state whenever props change, refactor your " + 'code to use memoization techniques or move it to ' + 'static getDerivedStateFromProps. Learn more at: https://fb.me/react-derived-state\n' + '* Rename componentWillReceiveProps to UNSAFE_componentWillReceiveProps to suppress ' + 'this warning in non-strict mode. In React 17.x, only the UNSAFE_ name will work. ' + 'To rename all deprecated lifecycles to their new names, you can run ' + '`npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n' + '\nPlease update the following components: %s', _sortedNames4);
    }

    if (componentWillUpdateUniqueNames.size > 0) {
      var _sortedNames5 = setToSortedString(componentWillUpdateUniqueNames);

      warn('componentWillUpdate has been renamed, and is not recommended for use. ' + 'See https://fb.me/react-unsafe-component-lifecycles for details.\n\n' + '* Move data fetching code or side effects to componentDidUpdate.\n' + '* Rename componentWillUpdate to UNSAFE_componentWillUpdate to suppress ' + 'this warning in non-strict mode. In React 17.x, only the UNSAFE_ name will work. ' + 'To rename all deprecated lifecycles to their new names, you can run ' + '`npx react-codemod rename-unsafe-lifecycles` in your project source folder.\n' + '\nPlease update the following components: %s', _sortedNames5);
    }
  };

  var pendingLegacyContextWarning = new Map(); // Tracks components we have already warned about.

  var didWarnAboutLegacyContext = new Set();

  ReactStrictModeWarnings.recordLegacyContextWarning = function (fiber, instance) {
    var strictRoot = findStrictRoot(fiber);

    if (strictRoot === null) {
      error('Expected to find a StrictMode component in a strict mode tree. ' + 'This error is likely caused by a bug in React. Please file an issue.');

      return;
    } // Dedup strategy: Warn once per component.


    if (didWarnAboutLegacyContext.has(fiber.type)) {
      return;
    }

    var warningsForRoot = pendingLegacyContextWarning.get(strictRoot);

    if (fiber.type.contextTypes != null || fiber.type.childContextTypes != null || instance !== null && typeof instance.getChildContext === 'function') {
      if (warningsForRoot === undefined) {
        warningsForRoot = [];
        pendingLegacyContextWarning.set(strictRoot, warningsForRoot);
      }

      warningsForRoot.push(fiber);
    }
  };

  ReactStrictModeWarnings.flushLegacyContextWarning = function () {
    pendingLegacyContextWarning.forEach(function (fiberArray, strictRoot) {
      if (fiberArray.length === 0) {
        return;
      }

      var firstFiber = fiberArray[0];
      var uniqueNames = new Set();
      fiberArray.forEach(function (fiber) {
        uniqueNames.add(getComponentName(fiber.type) || 'Component');
        didWarnAboutLegacyContext.add(fiber.type);
      });
      var sortedNames = setToSortedString(uniqueNames);
      var firstComponentStack = getStackByFiberInDevAndProd(firstFiber);

      error('Legacy context API has been detected within a strict-mode tree.' + '\n\nThe old API will be supported in all 16.x releases, but applications ' + 'using it should migrate to the new version.' + '\n\nPlease update the following components: %s' + '\n\nLearn more about this warning here: https://fb.me/react-legacy-context' + '%s', sortedNames, firstComponentStack);
    });
  };

  ReactStrictModeWarnings.discardPendingWarnings = function () {
    pendingComponentWillMountWarnings = [];
    pendingUNSAFE_ComponentWillMountWarnings = [];
    pendingComponentWillReceivePropsWarnings = [];
    pendingUNSAFE_ComponentWillReceivePropsWarnings = [];
    pendingComponentWillUpdateWarnings = [];
    pendingUNSAFE_ComponentWillUpdateWarnings = [];
    pendingLegacyContextWarning = new Map();
  };
}

var resolveFamily = null; // $FlowFixMe Flow gets confused by a WeakSet feature check below.

var failedBoundaries = null;
var setRefreshHandler = function (handler) {
  {
    resolveFamily = handler;
  }
};
function resolveFunctionForHotReloading(type) {
  {
    if (resolveFamily === null) {
      // Hot reloading is disabled.
      return type;
    }

    var family = resolveFamily(type);

    if (family === undefined) {
      return type;
    } // Use the latest known implementation.


    return family.current;
  }
}
function resolveClassForHotReloading(type) {
  // No implementation differences.
  return resolveFunctionForHotReloading(type);
}
function resolveForwardRefForHotReloading(type) {
  {
    if (resolveFamily === null) {
      // Hot reloading is disabled.
      return type;
    }

    var family = resolveFamily(type);

    if (family === undefined) {
      // Check if we're dealing with a real forwardRef. Don't want to crash early.
      if (type !== null && type !== undefined && typeof type.render === 'function') {
        // ForwardRef is special because its resolved .type is an object,
        // but it's possible that we only have its inner render function in the map.
        // If that inner render function is different, we'll build a new forwardRef type.
        var currentRender = resolveFunctionForHotReloading(type.render);

        if (type.render !== currentRender) {
          var syntheticType = {
            $$typeof: REACT_FORWARD_REF_TYPE,
            render: currentRender
          };

          if (type.displayName !== undefined) {
            syntheticType.displayName = type.displayName;
          }

          return syntheticType;
        }
      }

      return type;
    } // Use the latest known implementation.


    return family.current;
  }
}
function isCompatibleFamilyForHotReloading(fiber, element) {
  {
    if (resolveFamily === null) {
      // Hot reloading is disabled.
      return false;
    }

    var prevType = fiber.elementType;
    var nextType = element.type; // If we got here, we know types aren't === equal.

    var needsCompareFamilies = false;
    var $$typeofNextType = typeof nextType === 'object' && nextType !== null ? nextType.$$typeof : null;

    switch (fiber.tag) {
      case ClassComponent:
        {
          if (typeof nextType === 'function') {
            needsCompareFamilies = true;
          }

          break;
        }

      case FunctionComponent:
        {
          if (typeof nextType === 'function') {
            needsCompareFamilies = true;
          } else if ($$typeofNextType === REACT_LAZY_TYPE) {
            // We don't know the inner type yet.
            // We're going to assume that the lazy inner type is stable,
            // and so it is sufficient to avoid reconciling it away.
            // We're not going to unwrap or actually use the new lazy type.
            needsCompareFamilies = true;
          }

          break;
        }

      case ForwardRef:
        {
          if ($$typeofNextType === REACT_FORWARD_REF_TYPE) {
            needsCompareFamilies = true;
          } else if ($$typeofNextType === REACT_LAZY_TYPE) {
            needsCompareFamilies = true;
          }

          break;
        }

      case MemoComponent:
      case SimpleMemoComponent:
        {
          if ($$typeofNextType === REACT_MEMO_TYPE) {
            // TODO: if it was but can no longer be simple,
            // we shouldn't set this.
            needsCompareFamilies = true;
          } else if ($$typeofNextType === REACT_LAZY_TYPE) {
            needsCompareFamilies = true;
          }

          break;
        }

      default:
        return false;
    } // Check if both types have a family and it's the same one.


    if (needsCompareFamilies) {
      // Note: memo() and forwardRef() we'll compare outer rather than inner type.
      // This means both of them need to be registered to preserve state.
      // If we unwrapped and compared the inner types for wrappers instead,
      // then we would risk falsely saying two separate memo(Foo)
      // calls are equivalent because they wrap the same Foo function.
      var prevFamily = resolveFamily(prevType);

      if (prevFamily !== undefined && prevFamily === resolveFamily(nextType)) {
        return true;
      }
    }

    return false;
  }
}
function markFailedErrorBoundaryForHotReloading(fiber) {
  {
    if (resolveFamily === null) {
      // Hot reloading is disabled.
      return;
    }

    if (typeof WeakSet !== 'function') {
      return;
    }

    if (failedBoundaries === null) {
      failedBoundaries = new WeakSet();
    }

    failedBoundaries.add(fiber);
  }
}
var scheduleRefresh = function (root, update) {
  {
    if (resolveFamily === null) {
      // Hot reloading is disabled.
      return;
    }

    var staleFamilies = update.staleFamilies,
        updatedFamilies = update.updatedFamilies;
    flushPassiveEffects();
    flushSync(function () {
      scheduleFibersWithFamiliesRecursively(root.current, updatedFamilies, staleFamilies);
    });
  }
};
var scheduleRoot = function (root, element) {
  {
    if (root.context !== emptyContextObject) {
      // Super edge case: root has a legacy _renderSubtree context
      // but we don't know the parentComponent so we can't pass it.
      // Just ignore. We'll delete this with _renderSubtree code path later.
      return;
    }

    flushPassiveEffects();
    syncUpdates(function () {
      updateContainer(element, root, null, null);
    });
  }
};

function scheduleFibersWithFamiliesRecursively(fiber, updatedFamilies, staleFamilies) {
  {
    var alternate = fiber.alternate,
        child = fiber.child,
        sibling = fiber.sibling,
        tag = fiber.tag,
        type = fiber.type;
    var candidateType = null;

    switch (tag) {
      case FunctionComponent:
      case SimpleMemoComponent:
      case ClassComponent:
        candidateType = type;
        break;

      case ForwardRef:
        candidateType = type.render;
        break;
    }

    if (resolveFamily === null) {
      throw new Error('Expected resolveFamily to be set during hot reload.');
    }

    var needsRender = false;
    var needsRemount = false;

    if (candidateType !== null) {
      var family = resolveFamily(candidateType);

      if (family !== undefined) {
        if (staleFamilies.has(family)) {
          needsRemount = true;
        } else if (updatedFamilies.has(family)) {
          if (tag === ClassComponent) {
            needsRemount = true;
          } else {
            needsRender = true;
          }
        }
      }
    }

    if (failedBoundaries !== null) {
      if (failedBoundaries.has(fiber) || alternate !== null && failedBoundaries.has(alternate)) {
        needsRemount = true;
      }
    }

    if (needsRemount) {
      fiber._debugNeedsRemount = true;
    }

    if (needsRemount || needsRender) {
      scheduleWork(fiber, Sync);
    }

    if (child !== null && !needsRemount) {
      scheduleFibersWithFamiliesRecursively(child, updatedFamilies, staleFamilies);
    }

    if (sibling !== null) {
      scheduleFibersWithFamiliesRecursively(sibling, updatedFamilies, staleFamilies);
    }
  }
}

var findHostInstancesForRefresh = function (root, families) {
  {
    var hostInstances = new Set();
    var types = new Set(families.map(function (family) {
      return family.current;
    }));
    findHostInstancesForMatchingFibersRecursively(root.current, types, hostInstances);
    return hostInstances;
  }
};

function findHostInstancesForMatchingFibersRecursively(fiber, types, hostInstances) {
  {
    var child = fiber.child,
        sibling = fiber.sibling,
        tag = fiber.tag,
        type = fiber.type;
    var candidateType = null;

    switch (tag) {
      case FunctionComponent:
      case SimpleMemoComponent:
      case ClassComponent:
        candidateType = type;
        break;

      case ForwardRef:
        candidateType = type.render;
        break;
    }

    var didMatch = false;

    if (candidateType !== null) {
      if (types.has(candidateType)) {
        didMatch = true;
      }
    }

    if (didMatch) {
      // We have a match. This only drills down to the closest host components.
      // There's no need to search deeper because for the purpose of giving
      // visual feedback, "flashing" outermost parent rectangles is sufficient.
      findHostInstancesForFiberShallowly(fiber, hostInstances);
    } else {
      // If there's no match, maybe there will be one further down in the child tree.
      if (child !== null) {
        findHostInstancesForMatchingFibersRecursively(child, types, hostInstances);
      }
    }

    if (sibling !== null) {
      findHostInstancesForMatchingFibersRecursively(sibling, types, hostInstances);
    }
  }
}

function findHostInstancesForFiberShallowly(fiber, hostInstances) {
  {
    var foundHostInstances = findChildHostInstancesForFiberShallowly(fiber, hostInstances);

    if (foundHostInstances) {
      return;
    } // If we didn't find any host children, fallback to closest host parent.


    var node = fiber;

    while (true) {
      switch (node.tag) {
        case HostComponent:
          hostInstances.add(node.stateNode);
          return;

        case HostPortal:
          hostInstances.add(node.stateNode.containerInfo);
          return;

        case HostRoot:
          hostInstances.add(node.stateNode.containerInfo);
          return;
      }

      if (node.return === null) {
        throw new Error('Expected to reach root first.');
      }

      node = node.return;
    }
  }
}

function findChildHostInstancesForFiberShallowly(fiber, hostInstances) {
  {
    var node = fiber;
    var foundHostInstances = false;

    while (true) {
      if (node.tag === HostComponent) {
        // We got a match.
        foundHostInstances = true;
        hostInstances.add(node.stateNode); // There may still be more, so keep searching.
      } else if (node.child !== null) {
        node.child.return = node;
        node = node.child;
        continue;
      }

      if (node === fiber) {
        return foundHostInstances;
      }

      while (node.sibling === null) {
        if (node.return === null || node.return === fiber) {
          return foundHostInstances;
        }

        node = node.return;
      }

      node.sibling.return = node.return;
      node = node.sibling;
    }
  }

  return false;
}

function resolveDefaultProps(Component, baseProps) {
  if (Component && Component.defaultProps) {
    // Resolve default props. Taken from ReactElement
    var props = _assign({}, baseProps);

    var defaultProps = Component.defaultProps;

    for (var propName in defaultProps) {
      if (props[propName] === undefined) {
        props[propName] = defaultProps[propName];
      }
    }

    return props;
  }

  return baseProps;
}
function readLazyComponentType(lazyComponent) {
  initializeLazyComponentType(lazyComponent);

  if (lazyComponent._status !== Resolved) {
    throw lazyComponent._result;
  }

  return lazyComponent._result;
}

var valueCursor = createCursor(null);
var rendererSigil;

{
  // Use this to detect multiple renderers using the same context
  rendererSigil = {};
}

var currentlyRenderingFiber = null;
var lastContextDependency = null;
var lastContextWithAllBitsObserved = null;
var isDisallowedContextReadInDEV = false;
function resetContextDependencies() {
  // This is called right before React yields execution, to ensure `readContext`
  // cannot be called outside the render phase.
  currentlyRenderingFiber = null;
  lastContextDependency = null;
  lastContextWithAllBitsObserved = null;

  {
    isDisallowedContextReadInDEV = false;
  }
}
function enterDisallowedContextReadInDEV() {
  {
    isDisallowedContextReadInDEV = true;
  }
}
function exitDisallowedContextReadInDEV() {
  {
    isDisallowedContextReadInDEV = false;
  }
}
function pushProvider(providerFiber, nextValue) {
  var context = providerFiber.type._context;

  {
    push(valueCursor, context._currentValue, providerFiber);
    context._currentValue = nextValue;

    {
      if (context._currentRenderer !== undefined && context._currentRenderer !== null && context._currentRenderer !== rendererSigil) {
        error('Detected multiple renderers concurrently rendering the ' + 'same context provider. This is currently unsupported.');
      }

      context._currentRenderer = rendererSigil;
    }
  }
}
function popProvider(providerFiber) {
  var currentValue = valueCursor.current;
  pop(valueCursor, providerFiber);
  var context = providerFiber.type._context;

  {
    context._currentValue = currentValue;
  }
}
function calculateChangedBits(context, newValue, oldValue) {
  if (objectIs(oldValue, newValue)) {
    // No change
    return 0;
  } else {
    var changedBits = typeof context._calculateChangedBits === 'function' ? context._calculateChangedBits(oldValue, newValue) : MAX_SIGNED_31_BIT_INT;

    {
      if ((changedBits & MAX_SIGNED_31_BIT_INT) !== changedBits) {
        error('calculateChangedBits: Expected the return value to be a ' + '31-bit integer. Instead received: %s', changedBits);
      }
    }

    return changedBits | 0;
  }
}
function scheduleWorkOnParentPath(parent, renderExpirationTime) {
  // Update the child expiration time of all the ancestors, including
  // the alternates.
  var node = parent;

  while (node !== null) {
    var alternate = node.alternate;

    if (node.childExpirationTime < renderExpirationTime) {
      node.childExpirationTime = renderExpirationTime;

      if (alternate !== null && alternate.childExpirationTime < renderExpirationTime) {
        alternate.childExpirationTime = renderExpirationTime;
      }
    } else if (alternate !== null && alternate.childExpirationTime < renderExpirationTime) {
      alternate.childExpirationTime = renderExpirationTime;
    } else {
      // Neither alternate was updated, which means the rest of the
      // ancestor path already has sufficient priority.
      break;
    }

    node = node.return;
  }
}
function propagateContextChange(workInProgress, context, changedBits, renderExpirationTime) {
  var fiber = workInProgress.child;

  if (fiber !== null) {
    // Set the return pointer of the child to the work-in-progress fiber.
    fiber.return = workInProgress;
  }

  while (fiber !== null) {
    var nextFiber = void 0; // Visit this fiber.

    var list = fiber.dependencies;

    if (list !== null) {
      nextFiber = fiber.child;
      var dependency = list.firstContext;

      while (dependency !== null) {
        // Check if the context matches.
        if (dependency.context === context && (dependency.observedBits & changedBits) !== 0) {
          // Match! Schedule an update on this fiber.
          if (fiber.tag === ClassComponent) {
            // Schedule a force update on the work-in-progress.
            var update = createUpdate(renderExpirationTime, null);
            update.tag = ForceUpdate; // TODO: Because we don't have a work-in-progress, this will add the
            // update to the current fiber, too, which means it will persist even if
            // this render is thrown away. Since it's a race condition, not sure it's
            // worth fixing.

            enqueueUpdate(fiber, update);
          }

          if (fiber.expirationTime < renderExpirationTime) {
            fiber.expirationTime = renderExpirationTime;
          }

          var alternate = fiber.alternate;

          if (alternate !== null && alternate.expirationTime < renderExpirationTime) {
            alternate.expirationTime = renderExpirationTime;
          }

          scheduleWorkOnParentPath(fiber.return, renderExpirationTime); // Mark the expiration time on the list, too.

          if (list.expirationTime < renderExpirationTime) {
            list.expirationTime = renderExpirationTime;
          } // Since we already found a match, we can stop traversing the
          // dependency list.


          break;
        }

        dependency = dependency.next;
      }
    } else if (fiber.tag === ContextProvider) {
      // Don't scan deeper if this is a matching provider
      nextFiber = fiber.type === workInProgress.type ? null : fiber.child;
    } else {
      // Traverse down.
      nextFiber = fiber.child;
    }

    if (nextFiber !== null) {
      // Set the return pointer of the child to the work-in-progress fiber.
      nextFiber.return = fiber;
    } else {
      // No child. Traverse to next sibling.
      nextFiber = fiber;

      while (nextFiber !== null) {
        if (nextFiber === workInProgress) {
          // We're back to the root of this subtree. Exit.
          nextFiber = null;
          break;
        }

        var sibling = nextFiber.sibling;

        if (sibling !== null) {
          // Set the return pointer of the sibling to the work-in-progress fiber.
          sibling.return = nextFiber.return;
          nextFiber = sibling;
          break;
        } // No more siblings. Traverse up.


        nextFiber = nextFiber.return;
      }
    }

    fiber = nextFiber;
  }
}
function prepareToReadContext(workInProgress, renderExpirationTime) {
  currentlyRenderingFiber = workInProgress;
  lastContextDependency = null;
  lastContextWithAllBitsObserved = null;
  var dependencies = workInProgress.dependencies;

  if (dependencies !== null) {
    var firstContext = dependencies.firstContext;

    if (firstContext !== null) {
      if (dependencies.expirationTime >= renderExpirationTime) {
        // Context list has a pending update. Mark that this fiber performed work.
        markWorkInProgressReceivedUpdate();
      } // Reset the work-in-progress list


      dependencies.firstContext = null;
    }
  }
}
function readContext(context, observedBits) {
  {
    // This warning would fire if you read context inside a Hook like useMemo.
    // Unlike the class check below, it's not enforced in production for perf.
    if (isDisallowedContextReadInDEV) {
      error('Context can only be read while React is rendering. ' + 'In classes, you can read it in the render method or getDerivedStateFromProps. ' + 'In function components, you can read it directly in the function body, but not ' + 'inside Hooks like useReducer() or useMemo().');
    }
  }

  if (lastContextWithAllBitsObserved === context) ; else if (observedBits === false || observedBits === 0) ; else {
    var resolvedObservedBits; // Avoid deopting on observable arguments or heterogeneous types.

    if (typeof observedBits !== 'number' || observedBits === MAX_SIGNED_31_BIT_INT) {
      // Observe all updates.
      lastContextWithAllBitsObserved = context;
      resolvedObservedBits = MAX_SIGNED_31_BIT_INT;
    } else {
      resolvedObservedBits = observedBits;
    }

    var contextItem = {
      context: context,
      observedBits: resolvedObservedBits,
      next: null
    };

    if (lastContextDependency === null) {
      if (!(currentlyRenderingFiber !== null)) {
        {
          throw Error( "Context can only be read while React is rendering. In classes, you can read it in the render method or getDerivedStateFromProps. In function components, you can read it directly in the function body, but not inside Hooks like useReducer() or useMemo()." );
        }
      } // This is the first dependency for this component. Create a new list.


      lastContextDependency = contextItem;
      currentlyRenderingFiber.dependencies = {
        expirationTime: NoWork,
        firstContext: contextItem,
        responders: null
      };
    } else {
      // Append a new context item.
      lastContextDependency = lastContextDependency.next = contextItem;
    }
  }

  return  context._currentValue ;
}

var UpdateState = 0;
var ReplaceState = 1;
var ForceUpdate = 2;
var CaptureUpdate = 3; // Global state that is reset at the beginning of calling `processUpdateQueue`.
// It should only be read right after calling `processUpdateQueue`, via
// `checkHasForceUpdateAfterProcessing`.

var hasForceUpdate = false;
var didWarnUpdateInsideUpdate;
var currentlyProcessingQueue;

{
  didWarnUpdateInsideUpdate = false;
  currentlyProcessingQueue = null;
}

function initializeUpdateQueue(fiber) {
  var queue = {
    baseState: fiber.memoizedState,
    baseQueue: null,
    shared: {
      pending: null
    },
    effects: null
  };
  fiber.updateQueue = queue;
}
function cloneUpdateQueue(current, workInProgress) {
  // Clone the update queue from current. Unless it's already a clone.
  var queue = workInProgress.updateQueue;
  var currentQueue = current.updateQueue;

  if (queue === currentQueue) {
    var clone = {
      baseState: currentQueue.baseState,
      baseQueue: currentQueue.baseQueue,
      shared: currentQueue.shared,
      effects: currentQueue.effects
    };
    workInProgress.updateQueue = clone;
  }
}
function createUpdate(expirationTime, suspenseConfig) {
  var update = {
    expirationTime: expirationTime,
    suspenseConfig: suspenseConfig,
    tag: UpdateState,
    payload: null,
    callback: null,
    next: null
  };
  update.next = update;

  {
    update.priority = getCurrentPriorityLevel();
  }

  return update;
}
function enqueueUpdate(fiber, update) {
  var updateQueue = fiber.updateQueue;

  if (updateQueue === null) {
    // Only occurs if the fiber has been unmounted.
    return;
  }

  var sharedQueue = updateQueue.shared;
  var pending = sharedQueue.pending;

  if (pending === null) {
    // This is the first update. Create a circular list.
    update.next = update;
  } else {
    update.next = pending.next;
    pending.next = update;
  }

  sharedQueue.pending = update;

  {
    if (currentlyProcessingQueue === sharedQueue && !didWarnUpdateInsideUpdate) {
      error('An update (setState, replaceState, or forceUpdate) was scheduled ' + 'from inside an update function. Update functions should be pure, ' + 'with zero side-effects. Consider using componentDidUpdate or a ' + 'callback.');

      didWarnUpdateInsideUpdate = true;
    }
  }
}
function enqueueCapturedUpdate(workInProgress, update) {
  var current = workInProgress.alternate;

  if (current !== null) {
    // Ensure the work-in-progress queue is a clone
    cloneUpdateQueue(current, workInProgress);
  } // Captured updates go only on the work-in-progress queue.


  var queue = workInProgress.updateQueue; // Append the update to the end of the list.

  var last = queue.baseQueue;

  if (last === null) {
    queue.baseQueue = update.next = update;
    update.next = update;
  } else {
    update.next = last.next;
    last.next = update;
  }
}

function getStateFromUpdate(workInProgress, queue, update, prevState, nextProps, instance) {
  switch (update.tag) {
    case ReplaceState:
      {
        var payload = update.payload;

        if (typeof payload === 'function') {
          // Updater function
          {
            enterDisallowedContextReadInDEV();

            if ( workInProgress.mode & StrictMode) {
              payload.call(instance, prevState, nextProps);
            }
          }

          var nextState = payload.call(instance, prevState, nextProps);

          {
            exitDisallowedContextReadInDEV();
          }

          return nextState;
        } // State object


        return payload;
      }

    case CaptureUpdate:
      {
        workInProgress.effectTag = workInProgress.effectTag & ~ShouldCapture | DidCapture;
      }
    // Intentional fallthrough

    case UpdateState:
      {
        var _payload = update.payload;
        var partialState;

        if (typeof _payload === 'function') {
          // Updater function
          {
            enterDisallowedContextReadInDEV();

            if ( workInProgress.mode & StrictMode) {
              _payload.call(instance, prevState, nextProps);
            }
          }

          partialState = _payload.call(instance, prevState, nextProps);

          {
            exitDisallowedContextReadInDEV();
          }
        } else {
          // Partial state object
          partialState = _payload;
        }

        if (partialState === null || partialState === undefined) {
          // Null and undefined are treated as no-ops.
          return prevState;
        } // Merge the partial state and the previous state.


        return _assign({}, prevState, partialState);
      }

    case ForceUpdate:
      {
        hasForceUpdate = true;
        return prevState;
      }
  }

  return prevState;
}

function processUpdateQueue(workInProgress, props, instance, renderExpirationTime) {
  // This is always non-null on a ClassComponent or HostRoot
  var queue = workInProgress.updateQueue;
  hasForceUpdate = false;

  {
    currentlyProcessingQueue = queue.shared;
  } // The last rebase update that is NOT part of the base state.


  var baseQueue = queue.baseQueue; // The last pending update that hasn't been processed yet.

  var pendingQueue = queue.shared.pending;

  if (pendingQueue !== null) {
    // We have new updates that haven't been processed yet.
    // We'll add them to the base queue.
    if (baseQueue !== null) {
      // Merge the pending queue and the base queue.
      var baseFirst = baseQueue.next;
      var pendingFirst = pendingQueue.next;
      baseQueue.next = pendingFirst;
      pendingQueue.next = baseFirst;
    }

    baseQueue = pendingQueue;
    queue.shared.pending = null; // TODO: Pass `current` as argument

    var current = workInProgress.alternate;

    if (current !== null) {
      var currentQueue = current.updateQueue;

      if (currentQueue !== null) {
        currentQueue.baseQueue = pendingQueue;
      }
    }
  } // These values may change as we process the queue.


  if (baseQueue !== null) {
    var first = baseQueue.next; // Iterate through the list of updates to compute the result.

    var newState = queue.baseState;
    var newExpirationTime = NoWork;
    var newBaseState = null;
    var newBaseQueueFirst = null;
    var newBaseQueueLast = null;

    if (first !== null) {
      var update = first;

      do {
        var updateExpirationTime = update.expirationTime;

        if (updateExpirationTime < renderExpirationTime) {
          // Priority is insufficient. Skip this update. If this is the first
          // skipped update, the previous update/state is the new base
          // update/state.
          var clone = {
            expirationTime: update.expirationTime,
            suspenseConfig: update.suspenseConfig,
            tag: update.tag,
            payload: update.payload,
            callback: update.callback,
            next: null
          };

          if (newBaseQueueLast === null) {
            newBaseQueueFirst = newBaseQueueLast = clone;
            newBaseState = newState;
          } else {
            newBaseQueueLast = newBaseQueueLast.next = clone;
          } // Update the remaining priority in the queue.


          if (updateExpirationTime > newExpirationTime) {
            newExpirationTime = updateExpirationTime;
          }
        } else {
          // This update does have sufficient priority.
          if (newBaseQueueLast !== null) {
            var _clone = {
              expirationTime: Sync,
              // This update is going to be committed so we never want uncommit it.
              suspenseConfig: update.suspenseConfig,
              tag: update.tag,
              payload: update.payload,
              callback: update.callback,
              next: null
            };
            newBaseQueueLast = newBaseQueueLast.next = _clone;
          } // Mark the event time of this update as relevant to this render pass.
          // TODO: This should ideally use the true event time of this update rather than
          // its priority which is a derived and not reverseable value.
          // TODO: We should skip this update if it was already committed but currently
          // we have no way of detecting the difference between a committed and suspended
          // update here.


          markRenderEventTimeAndConfig(updateExpirationTime, update.suspenseConfig); // Process this update.

          newState = getStateFromUpdate(workInProgress, queue, update, newState, props, instance);
          var callback = update.callback;

          if (callback !== null) {
            workInProgress.effectTag |= Callback;
            var effects = queue.effects;

            if (effects === null) {
              queue.effects = [update];
            } else {
              effects.push(update);
            }
          }
        }

        update = update.next;

        if (update === null || update === first) {
          pendingQueue = queue.shared.pending;

          if (pendingQueue === null) {
            break;
          } else {
            // An update was scheduled from inside a reducer. Add the new
            // pending updates to the end of the list and keep processing.
            update = baseQueue.next = pendingQueue.next;
            pendingQueue.next = first;
            queue.baseQueue = baseQueue = pendingQueue;
            queue.shared.pending = null;
          }
        }
      } while (true);
    }

    if (newBaseQueueLast === null) {
      newBaseState = newState;
    } else {
      newBaseQueueLast.next = newBaseQueueFirst;
    }

    queue.baseState = newBaseState;
    queue.baseQueue = newBaseQueueLast; // Set the remaining expiration time to be whatever is remaining in the queue.
    // This should be fine because the only two other things that contribute to
    // expiration time are props and context. We're already in the middle of the
    // begin phase by the time we start processing the queue, so we've already
    // dealt with the props. Context in components that specify
    // shouldComponentUpdate is tricky; but we'll have to account for
    // that regardless.

    markUnprocessedUpdateTime(newExpirationTime);
    workInProgress.expirationTime = newExpirationTime;
    workInProgress.memoizedState = newState;
  }

  {
    currentlyProcessingQueue = null;
  }
}

function callCallback(callback, context) {
  if (!(typeof callback === 'function')) {
    {
      throw Error( "Invalid argument passed as callback. Expected a function. Instead received: " + callback );
    }
  }

  callback.call(context);
}

function resetHasForceUpdateBeforeProcessing() {
  hasForceUpdate = false;
}
function checkHasForceUpdateAfterProcessing() {
  return hasForceUpdate;
}
function commitUpdateQueue(finishedWork, finishedQueue, instance) {
  // Commit the effects
  var effects = finishedQueue.effects;
  finishedQueue.effects = null;

  if (effects !== null) {
    for (var i = 0; i < effects.length; i++) {
      var effect = effects[i];
      var callback = effect.callback;

      if (callback !== null) {
        effect.callback = null;
        callCallback(callback, instance);
      }
    }
  }
}

var ReactCurrentBatchConfig = ReactSharedInternals.ReactCurrentBatchConfig;
function requestCurrentSuspenseConfig() {
  return ReactCurrentBatchConfig.suspense;
}

var fakeInternalInstance = {};
var isArray = Array.isArray; // React.Component uses a shared frozen object by default.
// We'll use it to determine whether we need to initialize legacy refs.

var emptyRefsObject = new React.Component().refs;
var didWarnAboutStateAssignmentForComponent;
var didWarnAboutUninitializedState;
var didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate;
var didWarnAboutLegacyLifecyclesAndDerivedState;
var didWarnAboutUndefinedDerivedState;
var warnOnUndefinedDerivedState;
var warnOnInvalidCallback;
var didWarnAboutDirectlyAssigningPropsToState;
var didWarnAboutContextTypeAndContextTypes;
var didWarnAboutInvalidateContextType;

{
  didWarnAboutStateAssignmentForComponent = new Set();
  didWarnAboutUninitializedState = new Set();
  didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate = new Set();
  didWarnAboutLegacyLifecyclesAndDerivedState = new Set();
  didWarnAboutDirectlyAssigningPropsToState = new Set();
  didWarnAboutUndefinedDerivedState = new Set();
  didWarnAboutContextTypeAndContextTypes = new Set();
  didWarnAboutInvalidateContextType = new Set();
  var didWarnOnInvalidCallback = new Set();

  warnOnInvalidCallback = function (callback, callerName) {
    if (callback === null || typeof callback === 'function') {
      return;
    }

    var key = callerName + "_" + callback;

    if (!didWarnOnInvalidCallback.has(key)) {
      didWarnOnInvalidCallback.add(key);

      error('%s(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callerName, callback);
    }
  };

  warnOnUndefinedDerivedState = function (type, partialState) {
    if (partialState === undefined) {
      var componentName = getComponentName(type) || 'Component';

      if (!didWarnAboutUndefinedDerivedState.has(componentName)) {
        didWarnAboutUndefinedDerivedState.add(componentName);

        error('%s.getDerivedStateFromProps(): A valid state object (or null) must be returned. ' + 'You have returned undefined.', componentName);
      }
    }
  }; // This is so gross but it's at least non-critical and can be removed if
  // it causes problems. This is meant to give a nicer error message for
  // ReactDOM15.unstable_renderSubtreeIntoContainer(reactDOM16Component,
  // ...)) which otherwise throws a "_processChildContext is not a function"
  // exception.


  Object.defineProperty(fakeInternalInstance, '_processChildContext', {
    enumerable: false,
    value: function () {
      {
        {
          throw Error( "_processChildContext is not available in React 16+. This likely means you have multiple copies of React and are attempting to nest a React 15 tree inside a React 16 tree using unstable_renderSubtreeIntoContainer, which isn't supported. Try to make sure you have only one copy of React (and ideally, switch to ReactDOM.createPortal)." );
        }
      }
    }
  });
  Object.freeze(fakeInternalInstance);
}

function applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, nextProps) {
  var prevState = workInProgress.memoizedState;

  {
    if ( workInProgress.mode & StrictMode) {
      // Invoke the function an extra time to help detect side-effects.
      getDerivedStateFromProps(nextProps, prevState);
    }
  }

  var partialState = getDerivedStateFromProps(nextProps, prevState);

  {
    warnOnUndefinedDerivedState(ctor, partialState);
  } // Merge the partial state and the previous state.


  var memoizedState = partialState === null || partialState === undefined ? prevState : _assign({}, prevState, partialState);
  workInProgress.memoizedState = memoizedState; // Once the update queue is empty, persist the derived state onto the
  // base state.

  if (workInProgress.expirationTime === NoWork) {
    // Queue is always non-null for classes
    var updateQueue = workInProgress.updateQueue;
    updateQueue.baseState = memoizedState;
  }
}
var classComponentUpdater = {
  isMounted: isMounted,
  enqueueSetState: function (inst, payload, callback) {
    var fiber = get(inst);
    var currentTime = requestCurrentTimeForUpdate();
    var suspenseConfig = requestCurrentSuspenseConfig();
    var expirationTime = computeExpirationForFiber(currentTime, fiber, suspenseConfig);
    var update = createUpdate(expirationTime, suspenseConfig);
    update.payload = payload;

    if (callback !== undefined && callback !== null) {
      {
        warnOnInvalidCallback(callback, 'setState');
      }

      update.callback = callback;
    }

    enqueueUpdate(fiber, update);
    scheduleWork(fiber, expirationTime);
  },
  enqueueReplaceState: function (inst, payload, callback) {
    var fiber = get(inst);
    var currentTime = requestCurrentTimeForUpdate();
    var suspenseConfig = requestCurrentSuspenseConfig();
    var expirationTime = computeExpirationForFiber(currentTime, fiber, suspenseConfig);
    var update = createUpdate(expirationTime, suspenseConfig);
    update.tag = ReplaceState;
    update.payload = payload;

    if (callback !== undefined && callback !== null) {
      {
        warnOnInvalidCallback(callback, 'replaceState');
      }

      update.callback = callback;
    }

    enqueueUpdate(fiber, update);
    scheduleWork(fiber, expirationTime);
  },
  enqueueForceUpdate: function (inst, callback) {
    var fiber = get(inst);
    var currentTime = requestCurrentTimeForUpdate();
    var suspenseConfig = requestCurrentSuspenseConfig();
    var expirationTime = computeExpirationForFiber(currentTime, fiber, suspenseConfig);
    var update = createUpdate(expirationTime, suspenseConfig);
    update.tag = ForceUpdate;

    if (callback !== undefined && callback !== null) {
      {
        warnOnInvalidCallback(callback, 'forceUpdate');
      }

      update.callback = callback;
    }

    enqueueUpdate(fiber, update);
    scheduleWork(fiber, expirationTime);
  }
};

function checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext) {
  var instance = workInProgress.stateNode;

  if (typeof instance.shouldComponentUpdate === 'function') {
    {
      if ( workInProgress.mode & StrictMode) {
        // Invoke the function an extra time to help detect side-effects.
        instance.shouldComponentUpdate(newProps, newState, nextContext);
      }
    }

    startPhaseTimer(workInProgress, 'shouldComponentUpdate');
    var shouldUpdate = instance.shouldComponentUpdate(newProps, newState, nextContext);
    stopPhaseTimer();

    {
      if (shouldUpdate === undefined) {
        error('%s.shouldComponentUpdate(): Returned undefined instead of a ' + 'boolean value. Make sure to return true or false.', getComponentName(ctor) || 'Component');
      }
    }

    return shouldUpdate;
  }

  if (ctor.prototype && ctor.prototype.isPureReactComponent) {
    return !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState);
  }

  return true;
}

function checkClassInstance(workInProgress, ctor, newProps) {
  var instance = workInProgress.stateNode;

  {
    var name = getComponentName(ctor) || 'Component';
    var renderPresent = instance.render;

    if (!renderPresent) {
      if (ctor.prototype && typeof ctor.prototype.render === 'function') {
        error('%s(...): No `render` method found on the returned component ' + 'instance: did you accidentally return an object from the constructor?', name);
      } else {
        error('%s(...): No `render` method found on the returned component ' + 'instance: you may have forgotten to define `render`.', name);
      }
    }

    if (instance.getInitialState && !instance.getInitialState.isReactClassApproved && !instance.state) {
      error('getInitialState was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Did you mean to define a state property instead?', name);
    }

    if (instance.getDefaultProps && !instance.getDefaultProps.isReactClassApproved) {
      error('getDefaultProps was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Use a static property to define defaultProps instead.', name);
    }

    if (instance.propTypes) {
      error('propTypes was defined as an instance property on %s. Use a static ' + 'property to define propTypes instead.', name);
    }

    if (instance.contextType) {
      error('contextType was defined as an instance property on %s. Use a static ' + 'property to define contextType instead.', name);
    }

    {
      if (instance.contextTypes) {
        error('contextTypes was defined as an instance property on %s. Use a static ' + 'property to define contextTypes instead.', name);
      }

      if (ctor.contextType && ctor.contextTypes && !didWarnAboutContextTypeAndContextTypes.has(ctor)) {
        didWarnAboutContextTypeAndContextTypes.add(ctor);

        error('%s declares both contextTypes and contextType static properties. ' + 'The legacy contextTypes property will be ignored.', name);
      }
    }

    if (typeof instance.componentShouldUpdate === 'function') {
      error('%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', name);
    }

    if (ctor.prototype && ctor.prototype.isPureReactComponent && typeof instance.shouldComponentUpdate !== 'undefined') {
      error('%s has a method called shouldComponentUpdate(). ' + 'shouldComponentUpdate should not be used when extending React.PureComponent. ' + 'Please extend React.Component if shouldComponentUpdate is used.', getComponentName(ctor) || 'A pure component');
    }

    if (typeof instance.componentDidUnmount === 'function') {
      error('%s has a method called ' + 'componentDidUnmount(). But there is no such lifecycle method. ' + 'Did you mean componentWillUnmount()?', name);
    }

    if (typeof instance.componentDidReceiveProps === 'function') {
      error('%s has a method called ' + 'componentDidReceiveProps(). But there is no such lifecycle method. ' + 'If you meant to update the state in response to changing props, ' + 'use componentWillReceiveProps(). If you meant to fetch data or ' + 'run side-effects or mutations after React has updated the UI, use componentDidUpdate().', name);
    }

    if (typeof instance.componentWillRecieveProps === 'function') {
      error('%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', name);
    }

    if (typeof instance.UNSAFE_componentWillRecieveProps === 'function') {
      error('%s has a method called ' + 'UNSAFE_componentWillRecieveProps(). Did you mean UNSAFE_componentWillReceiveProps()?', name);
    }

    var hasMutatedProps = instance.props !== newProps;

    if (instance.props !== undefined && hasMutatedProps) {
      error('%s(...): When calling super() in `%s`, make sure to pass ' + "up the same props that your component's constructor was passed.", name, name);
    }

    if (instance.defaultProps) {
      error('Setting defaultProps as an instance property on %s is not supported and will be ignored.' + ' Instead, define defaultProps as a static property on %s.', name, name);
    }

    if (typeof instance.getSnapshotBeforeUpdate === 'function' && typeof instance.componentDidUpdate !== 'function' && !didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.has(ctor)) {
      didWarnAboutGetSnapshotBeforeUpdateWithoutDidUpdate.add(ctor);

      error('%s: getSnapshotBeforeUpdate() should be used with componentDidUpdate(). ' + 'This component defines getSnapshotBeforeUpdate() only.', getComponentName(ctor));
    }

    if (typeof instance.getDerivedStateFromProps === 'function') {
      error('%s: getDerivedStateFromProps() is defined as an instance method ' + 'and will be ignored. Instead, declare it as a static method.', name);
    }

    if (typeof instance.getDerivedStateFromError === 'function') {
      error('%s: getDerivedStateFromError() is defined as an instance method ' + 'and will be ignored. Instead, declare it as a static method.', name);
    }

    if (typeof ctor.getSnapshotBeforeUpdate === 'function') {
      error('%s: getSnapshotBeforeUpdate() is defined as a static method ' + 'and will be ignored. Instead, declare it as an instance method.', name);
    }

    var _state = instance.state;

    if (_state && (typeof _state !== 'object' || isArray(_state))) {
      error('%s.state: must be set to an object or null', name);
    }

    if (typeof instance.getChildContext === 'function' && typeof ctor.childContextTypes !== 'object') {
      error('%s.getChildContext(): childContextTypes must be defined in order to ' + 'use getChildContext().', name);
    }
  }
}

function adoptClassInstance(workInProgress, instance) {
  instance.updater = classComponentUpdater;
  workInProgress.stateNode = instance; // The instance needs access to the fiber so that it can schedule updates

  set(instance, workInProgress);

  {
    instance._reactInternalInstance = fakeInternalInstance;
  }
}

function constructClassInstance(workInProgress, ctor, props) {
  var isLegacyContextConsumer = false;
  var unmaskedContext = emptyContextObject;
  var context = emptyContextObject;
  var contextType = ctor.contextType;

  {
    if ('contextType' in ctor) {
      var isValid = // Allow null for conditional declaration
      contextType === null || contextType !== undefined && contextType.$$typeof === REACT_CONTEXT_TYPE && contextType._context === undefined; // Not a <Context.Consumer>

      if (!isValid && !didWarnAboutInvalidateContextType.has(ctor)) {
        didWarnAboutInvalidateContextType.add(ctor);
        var addendum = '';

        if (contextType === undefined) {
          addendum = ' However, it is set to undefined. ' + 'This can be caused by a typo or by mixing up named and default imports. ' + 'This can also happen due to a circular dependency, so ' + 'try moving the createContext() call to a separate file.';
        } else if (typeof contextType !== 'object') {
          addendum = ' However, it is set to a ' + typeof contextType + '.';
        } else if (contextType.$$typeof === REACT_PROVIDER_TYPE) {
          addendum = ' Did you accidentally pass the Context.Provider instead?';
        } else if (contextType._context !== undefined) {
          // <Context.Consumer>
          addendum = ' Did you accidentally pass the Context.Consumer instead?';
        } else {
          addendum = ' However, it is set to an object with keys {' + Object.keys(contextType).join(', ') + '}.';
        }

        error('%s defines an invalid contextType. ' + 'contextType should point to the Context object returned by React.createContext().%s', getComponentName(ctor) || 'Component', addendum);
      }
    }
  }

  if (typeof contextType === 'object' && contextType !== null) {
    context = readContext(contextType);
  } else {
    unmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
    var contextTypes = ctor.contextTypes;
    isLegacyContextConsumer = contextTypes !== null && contextTypes !== undefined;
    context = isLegacyContextConsumer ? getMaskedContext(workInProgress, unmaskedContext) : emptyContextObject;
  } // Instantiate twice to help detect side-effects.


  {
    if ( workInProgress.mode & StrictMode) {
      new ctor(props, context); // eslint-disable-line no-new
    }
  }

  var instance = new ctor(props, context);
  var state = workInProgress.memoizedState = instance.state !== null && instance.state !== undefined ? instance.state : null;
  adoptClassInstance(workInProgress, instance);

  {
    if (typeof ctor.getDerivedStateFromProps === 'function' && state === null) {
      var componentName = getComponentName(ctor) || 'Component';

      if (!didWarnAboutUninitializedState.has(componentName)) {
        didWarnAboutUninitializedState.add(componentName);

        error('`%s` uses `getDerivedStateFromProps` but its initial state is ' + '%s. This is not recommended. Instead, define the initial state by ' + 'assigning an object to `this.state` in the constructor of `%s`. ' + 'This ensures that `getDerivedStateFromProps` arguments have a consistent shape.', componentName, instance.state === null ? 'null' : 'undefined', componentName);
      }
    } // If new component APIs are defined, "unsafe" lifecycles won't be called.
    // Warn about these lifecycles if they are present.
    // Don't warn about react-lifecycles-compat polyfilled methods though.


    if (typeof ctor.getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function') {
      var foundWillMountName = null;
      var foundWillReceivePropsName = null;
      var foundWillUpdateName = null;

      if (typeof instance.componentWillMount === 'function' && instance.componentWillMount.__suppressDeprecationWarning !== true) {
        foundWillMountName = 'componentWillMount';
      } else if (typeof instance.UNSAFE_componentWillMount === 'function') {
        foundWillMountName = 'UNSAFE_componentWillMount';
      }

      if (typeof instance.componentWillReceiveProps === 'function' && instance.componentWillReceiveProps.__suppressDeprecationWarning !== true) {
        foundWillReceivePropsName = 'componentWillReceiveProps';
      } else if (typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
        foundWillReceivePropsName = 'UNSAFE_componentWillReceiveProps';
      }

      if (typeof instance.componentWillUpdate === 'function' && instance.componentWillUpdate.__suppressDeprecationWarning !== true) {
        foundWillUpdateName = 'componentWillUpdate';
      } else if (typeof instance.UNSAFE_componentWillUpdate === 'function') {
        foundWillUpdateName = 'UNSAFE_componentWillUpdate';
      }

      if (foundWillMountName !== null || foundWillReceivePropsName !== null || foundWillUpdateName !== null) {
        var _componentName = getComponentName(ctor) || 'Component';

        var newApiName = typeof ctor.getDerivedStateFromProps === 'function' ? 'getDerivedStateFromProps()' : 'getSnapshotBeforeUpdate()';

        if (!didWarnAboutLegacyLifecyclesAndDerivedState.has(_componentName)) {
          didWarnAboutLegacyLifecyclesAndDerivedState.add(_componentName);

          error('Unsafe legacy lifecycles will not be called for components using new component APIs.\n\n' + '%s uses %s but also contains the following legacy lifecycles:%s%s%s\n\n' + 'The above lifecycles should be removed. Learn more about this warning here:\n' + 'https://fb.me/react-unsafe-component-lifecycles', _componentName, newApiName, foundWillMountName !== null ? "\n  " + foundWillMountName : '', foundWillReceivePropsName !== null ? "\n  " + foundWillReceivePropsName : '', foundWillUpdateName !== null ? "\n  " + foundWillUpdateName : '');
        }
      }
    }
  } // Cache unmasked context so we can avoid recreating masked context unless necessary.
  // ReactFiberContext usually updates this cache but can't for newly-created instances.


  if (isLegacyContextConsumer) {
    cacheContext(workInProgress, unmaskedContext, context);
  }

  return instance;
}

function callComponentWillMount(workInProgress, instance) {
  startPhaseTimer(workInProgress, 'componentWillMount');
  var oldState = instance.state;

  if (typeof instance.componentWillMount === 'function') {
    instance.componentWillMount();
  }

  if (typeof instance.UNSAFE_componentWillMount === 'function') {
    instance.UNSAFE_componentWillMount();
  }

  stopPhaseTimer();

  if (oldState !== instance.state) {
    {
      error('%s.componentWillMount(): Assigning directly to this.state is ' + "deprecated (except inside a component's " + 'constructor). Use setState instead.', getComponentName(workInProgress.type) || 'Component');
    }

    classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
  }
}

function callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext) {
  var oldState = instance.state;
  startPhaseTimer(workInProgress, 'componentWillReceiveProps');

  if (typeof instance.componentWillReceiveProps === 'function') {
    instance.componentWillReceiveProps(newProps, nextContext);
  }

  if (typeof instance.UNSAFE_componentWillReceiveProps === 'function') {
    instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
  }

  stopPhaseTimer();

  if (instance.state !== oldState) {
    {
      var componentName = getComponentName(workInProgress.type) || 'Component';

      if (!didWarnAboutStateAssignmentForComponent.has(componentName)) {
        didWarnAboutStateAssignmentForComponent.add(componentName);

        error('%s.componentWillReceiveProps(): Assigning directly to ' + "this.state is deprecated (except inside a component's " + 'constructor). Use setState instead.', componentName);
      }
    }

    classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
  }
} // Invokes the mount life-cycles on a previously never rendered instance.


function mountClassInstance(workInProgress, ctor, newProps, renderExpirationTime) {
  {
    checkClassInstance(workInProgress, ctor, newProps);
  }

  var instance = workInProgress.stateNode;
  instance.props = newProps;
  instance.state = workInProgress.memoizedState;
  instance.refs = emptyRefsObject;
  initializeUpdateQueue(workInProgress);
  var contextType = ctor.contextType;

  if (typeof contextType === 'object' && contextType !== null) {
    instance.context = readContext(contextType);
  } else {
    var unmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
    instance.context = getMaskedContext(workInProgress, unmaskedContext);
  }

  {
    if (instance.state === newProps) {
      var componentName = getComponentName(ctor) || 'Component';

      if (!didWarnAboutDirectlyAssigningPropsToState.has(componentName)) {
        didWarnAboutDirectlyAssigningPropsToState.add(componentName);

        error('%s: It is not recommended to assign props directly to state ' + "because updates to props won't be reflected in state. " + 'In most cases, it is better to use props directly.', componentName);
      }
    }

    if (workInProgress.mode & StrictMode) {
      ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, instance);
    }

    {
      ReactStrictModeWarnings.recordUnsafeLifecycleWarnings(workInProgress, instance);
    }
  }

  processUpdateQueue(workInProgress, newProps, instance, renderExpirationTime);
  instance.state = workInProgress.memoizedState;
  var getDerivedStateFromProps = ctor.getDerivedStateFromProps;

  if (typeof getDerivedStateFromProps === 'function') {
    applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
    instance.state = workInProgress.memoizedState;
  } // In order to support react-lifecycles-compat polyfilled components,
  // Unsafe lifecycles should not be invoked for components using the new APIs.


  if (typeof ctor.getDerivedStateFromProps !== 'function' && typeof instance.getSnapshotBeforeUpdate !== 'function' && (typeof instance.UNSAFE_componentWillMount === 'function' || typeof instance.componentWillMount === 'function')) {
    callComponentWillMount(workInProgress, instance); // If we had additional state updates during this life-cycle, let's
    // process them now.

    processUpdateQueue(workInProgress, newProps, instance, renderExpirationTime);
    instance.state = workInProgress.memoizedState;
  }

  if (typeof instance.componentDidMount === 'function') {
    workInProgress.effectTag |= Update;
  }
}

function resumeMountClassInstance(workInProgress, ctor, newProps, renderExpirationTime) {
  var instance = workInProgress.stateNode;
  var oldProps = workInProgress.memoizedProps;
  instance.props = oldProps;
  var oldContext = instance.context;
  var contextType = ctor.contextType;
  var nextContext = emptyContextObject;

  if (typeof contextType === 'object' && contextType !== null) {
    nextContext = readContext(contextType);
  } else {
    var nextLegacyUnmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
    nextContext = getMaskedContext(workInProgress, nextLegacyUnmaskedContext);
  }

  var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
  var hasNewLifecycles = typeof getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function'; // Note: During these life-cycles, instance.props/instance.state are what
  // ever the previously attempted to render - not the "current". However,
  // during componentDidUpdate we pass the "current" props.
  // In order to support react-lifecycles-compat polyfilled components,
  // Unsafe lifecycles should not be invoked for components using the new APIs.

  if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === 'function' || typeof instance.componentWillReceiveProps === 'function')) {
    if (oldProps !== newProps || oldContext !== nextContext) {
      callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext);
    }
  }

  resetHasForceUpdateBeforeProcessing();
  var oldState = workInProgress.memoizedState;
  var newState = instance.state = oldState;
  processUpdateQueue(workInProgress, newProps, instance, renderExpirationTime);
  newState = workInProgress.memoizedState;

  if (oldProps === newProps && oldState === newState && !hasContextChanged() && !checkHasForceUpdateAfterProcessing()) {
    // If an update was already in progress, we should schedule an Update
    // effect even though we're bailing out, so that cWU/cDU are called.
    if (typeof instance.componentDidMount === 'function') {
      workInProgress.effectTag |= Update;
    }

    return false;
  }

  if (typeof getDerivedStateFromProps === 'function') {
    applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
    newState = workInProgress.memoizedState;
  }

  var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext);

  if (shouldUpdate) {
    // In order to support react-lifecycles-compat polyfilled components,
    // Unsafe lifecycles should not be invoked for components using the new APIs.
    if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillMount === 'function' || typeof instance.componentWillMount === 'function')) {
      startPhaseTimer(workInProgress, 'componentWillMount');

      if (typeof instance.componentWillMount === 'function') {
        instance.componentWillMount();
      }

      if (typeof instance.UNSAFE_componentWillMount === 'function') {
        instance.UNSAFE_componentWillMount();
      }

      stopPhaseTimer();
    }

    if (typeof instance.componentDidMount === 'function') {
      workInProgress.effectTag |= Update;
    }
  } else {
    // If an update was already in progress, we should schedule an Update
    // effect even though we're bailing out, so that cWU/cDU are called.
    if (typeof instance.componentDidMount === 'function') {
      workInProgress.effectTag |= Update;
    } // If shouldComponentUpdate returned false, we should still update the
    // memoized state to indicate that this work can be reused.


    workInProgress.memoizedProps = newProps;
    workInProgress.memoizedState = newState;
  } // Update the existing instance's state, props, and context pointers even
  // if shouldComponentUpdate returns false.


  instance.props = newProps;
  instance.state = newState;
  instance.context = nextContext;
  return shouldUpdate;
} // Invokes the update life-cycles and returns false if it shouldn't rerender.


function updateClassInstance(current, workInProgress, ctor, newProps, renderExpirationTime) {
  var instance = workInProgress.stateNode;
  cloneUpdateQueue(current, workInProgress);
  var oldProps = workInProgress.memoizedProps;
  instance.props = workInProgress.type === workInProgress.elementType ? oldProps : resolveDefaultProps(workInProgress.type, oldProps);
  var oldContext = instance.context;
  var contextType = ctor.contextType;
  var nextContext = emptyContextObject;

  if (typeof contextType === 'object' && contextType !== null) {
    nextContext = readContext(contextType);
  } else {
    var nextUnmaskedContext = getUnmaskedContext(workInProgress, ctor, true);
    nextContext = getMaskedContext(workInProgress, nextUnmaskedContext);
  }

  var getDerivedStateFromProps = ctor.getDerivedStateFromProps;
  var hasNewLifecycles = typeof getDerivedStateFromProps === 'function' || typeof instance.getSnapshotBeforeUpdate === 'function'; // Note: During these life-cycles, instance.props/instance.state are what
  // ever the previously attempted to render - not the "current". However,
  // during componentDidUpdate we pass the "current" props.
  // In order to support react-lifecycles-compat polyfilled components,
  // Unsafe lifecycles should not be invoked for components using the new APIs.

  if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillReceiveProps === 'function' || typeof instance.componentWillReceiveProps === 'function')) {
    if (oldProps !== newProps || oldContext !== nextContext) {
      callComponentWillReceiveProps(workInProgress, instance, newProps, nextContext);
    }
  }

  resetHasForceUpdateBeforeProcessing();
  var oldState = workInProgress.memoizedState;
  var newState = instance.state = oldState;
  processUpdateQueue(workInProgress, newProps, instance, renderExpirationTime);
  newState = workInProgress.memoizedState;

  if (oldProps === newProps && oldState === newState && !hasContextChanged() && !checkHasForceUpdateAfterProcessing()) {
    // If an update was already in progress, we should schedule an Update
    // effect even though we're bailing out, so that cWU/cDU are called.
    if (typeof instance.componentDidUpdate === 'function') {
      if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
        workInProgress.effectTag |= Update;
      }
    }

    if (typeof instance.getSnapshotBeforeUpdate === 'function') {
      if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
        workInProgress.effectTag |= Snapshot;
      }
    }

    return false;
  }

  if (typeof getDerivedStateFromProps === 'function') {
    applyDerivedStateFromProps(workInProgress, ctor, getDerivedStateFromProps, newProps);
    newState = workInProgress.memoizedState;
  }

  var shouldUpdate = checkHasForceUpdateAfterProcessing() || checkShouldComponentUpdate(workInProgress, ctor, oldProps, newProps, oldState, newState, nextContext);

  if (shouldUpdate) {
    // In order to support react-lifecycles-compat polyfilled components,
    // Unsafe lifecycles should not be invoked for components using the new APIs.
    if (!hasNewLifecycles && (typeof instance.UNSAFE_componentWillUpdate === 'function' || typeof instance.componentWillUpdate === 'function')) {
      startPhaseTimer(workInProgress, 'componentWillUpdate');

      if (typeof instance.componentWillUpdate === 'function') {
        instance.componentWillUpdate(newProps, newState, nextContext);
      }

      if (typeof instance.UNSAFE_componentWillUpdate === 'function') {
        instance.UNSAFE_componentWillUpdate(newProps, newState, nextContext);
      }

      stopPhaseTimer();
    }

    if (typeof instance.componentDidUpdate === 'function') {
      workInProgress.effectTag |= Update;
    }

    if (typeof instance.getSnapshotBeforeUpdate === 'function') {
      workInProgress.effectTag |= Snapshot;
    }
  } else {
    // If an update was already in progress, we should schedule an Update
    // effect even though we're bailing out, so that cWU/cDU are called.
    if (typeof instance.componentDidUpdate === 'function') {
      if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
        workInProgress.effectTag |= Update;
      }
    }

    if (typeof instance.getSnapshotBeforeUpdate === 'function') {
      if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
        workInProgress.effectTag |= Snapshot;
      }
    } // If shouldComponentUpdate returned false, we should still update the
    // memoized props/state to indicate that this work can be reused.


    workInProgress.memoizedProps = newProps;
    workInProgress.memoizedState = newState;
  } // Update the existing instance's state, props, and context pointers even
  // if shouldComponentUpdate returns false.


  instance.props = newProps;
  instance.state = newState;
  instance.context = nextContext;
  return shouldUpdate;
}

var didWarnAboutMaps;
var didWarnAboutGenerators;
var didWarnAboutStringRefs;
var ownerHasKeyUseWarning;
var ownerHasFunctionTypeWarning;

var warnForMissingKey = function (child) {};

{
  didWarnAboutMaps = false;
  didWarnAboutGenerators = false;
  didWarnAboutStringRefs = {};
  /**
   * Warn if there's no key explicitly set on dynamic arrays of children or
   * object keys are not valid. This allows us to keep track of children between
   * updates.
   */

  ownerHasKeyUseWarning = {};
  ownerHasFunctionTypeWarning = {};

  warnForMissingKey = function (child) {
    if (child === null || typeof child !== 'object') {
      return;
    }

    if (!child._store || child._store.validated || child.key != null) {
      return;
    }

    if (!(typeof child._store === 'object')) {
      {
        throw Error( "React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue." );
      }
    }

    child._store.validated = true;
    var currentComponentErrorInfo = 'Each child in a list should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.' + getCurrentFiberStackInDev();

    if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
      return;
    }

    ownerHasKeyUseWarning[currentComponentErrorInfo] = true;

    error('Each child in a list should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.');
  };
}

var isArray$1 = Array.isArray;

function coerceRef(returnFiber, current, element) {
  var mixedRef = element.ref;

  if (mixedRef !== null && typeof mixedRef !== 'function' && typeof mixedRef !== 'object') {
    {
      // TODO: Clean this up once we turn on the string ref warning for
      // everyone, because the strict mode case will no longer be relevant
      if ((returnFiber.mode & StrictMode || warnAboutStringRefs) && // We warn in ReactElement.js if owner and self are equal for string refs
      // because these cannot be automatically converted to an arrow function
      // using a codemod. Therefore, we don't have to warn about string refs again.
      !(element._owner && element._self && element._owner.stateNode !== element._self)) {
        var componentName = getComponentName(returnFiber.type) || 'Component';

        if (!didWarnAboutStringRefs[componentName]) {
          {
            error('A string ref, "%s", has been found within a strict mode tree. ' + 'String refs are a source of potential bugs and should be avoided. ' + 'We recommend using useRef() or createRef() instead. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-string-ref%s', mixedRef, getStackByFiberInDevAndProd(returnFiber));
          }

          didWarnAboutStringRefs[componentName] = true;
        }
      }
    }

    if (element._owner) {
      var owner = element._owner;
      var inst;

      if (owner) {
        var ownerFiber = owner;

        if (!(ownerFiber.tag === ClassComponent)) {
          {
            throw Error( "Function components cannot have string refs. We recommend using useRef() instead. Learn more about using refs safely here: https://fb.me/react-strict-mode-string-ref" );
          }
        }

        inst = ownerFiber.stateNode;
      }

      if (!inst) {
        {
          throw Error( "Missing owner for string ref " + mixedRef + ". This error is likely caused by a bug in React. Please file an issue." );
        }
      }

      var stringRef = '' + mixedRef; // Check if previous string ref matches new string ref

      if (current !== null && current.ref !== null && typeof current.ref === 'function' && current.ref._stringRef === stringRef) {
        return current.ref;
      }

      var ref = function (value) {
        var refs = inst.refs;

        if (refs === emptyRefsObject) {
          // This is a lazy pooled frozen object, so we need to initialize.
          refs = inst.refs = {};
        }

        if (value === null) {
          delete refs[stringRef];
        } else {
          refs[stringRef] = value;
        }
      };

      ref._stringRef = stringRef;
      return ref;
    } else {
      if (!(typeof mixedRef === 'string')) {
        {
          throw Error( "Expected ref to be a function, a string, an object returned by React.createRef(), or null." );
        }
      }

      if (!element._owner) {
        {
          throw Error( "Element ref was specified as a string (" + mixedRef + ") but no owner was set. This could happen for one of the following reasons:\n1. You may be adding a ref to a function component\n2. You may be adding a ref to a component that was not created inside a component's render method\n3. You have multiple copies of React loaded\nSee https://fb.me/react-refs-must-have-owner for more information." );
        }
      }
    }
  }

  return mixedRef;
}

function throwOnInvalidObjectType(returnFiber, newChild) {
  if (returnFiber.type !== 'textarea') {
    var addendum = '';

    {
      addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + getCurrentFiberStackInDev();
    }

    {
      {
        throw Error( "Objects are not valid as a React child (found: " + (Object.prototype.toString.call(newChild) === '[object Object]' ? 'object with keys {' + Object.keys(newChild).join(', ') + '}' : newChild) + ")." + addendum );
      }
    }
  }
}

function warnOnFunctionType() {
  {
    var currentComponentErrorInfo = 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.' + getCurrentFiberStackInDev();

    if (ownerHasFunctionTypeWarning[currentComponentErrorInfo]) {
      return;
    }

    ownerHasFunctionTypeWarning[currentComponentErrorInfo] = true;

    error('Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.');
  }
} // This wrapper function exists because I expect to clone the code in each path
// to be able to optimize each path individually by branching early. This needs
// a compiler or we can do it manually. Helpers that don't need this branching
// live outside of this function.


function ChildReconciler(shouldTrackSideEffects) {
  function deleteChild(returnFiber, childToDelete) {
    if (!shouldTrackSideEffects) {
      // Noop.
      return;
    } // Deletions are added in reversed order so we add it to the front.
    // At this point, the return fiber's effect list is empty except for
    // deletions, so we can just append the deletion to the list. The remaining
    // effects aren't added until the complete phase. Once we implement
    // resuming, this may not be true.


    var last = returnFiber.lastEffect;

    if (last !== null) {
      last.nextEffect = childToDelete;
      returnFiber.lastEffect = childToDelete;
    } else {
      returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
    }

    childToDelete.nextEffect = null;
    childToDelete.effectTag = Deletion;
  }

  function deleteRemainingChildren(returnFiber, currentFirstChild) {
    if (!shouldTrackSideEffects) {
      // Noop.
      return null;
    } // TODO: For the shouldClone case, this could be micro-optimized a bit by
    // assuming that after the first child we've already added everything.


    var childToDelete = currentFirstChild;

    while (childToDelete !== null) {
      deleteChild(returnFiber, childToDelete);
      childToDelete = childToDelete.sibling;
    }

    return null;
  }

  function mapRemainingChildren(returnFiber, currentFirstChild) {
    // Add the remaining children to a temporary map so that we can find them by
    // keys quickly. Implicit (null) keys get added to this set with their index
    // instead.
    var existingChildren = new Map();
    var existingChild = currentFirstChild;

    while (existingChild !== null) {
      if (existingChild.key !== null) {
        existingChildren.set(existingChild.key, existingChild);
      } else {
        existingChildren.set(existingChild.index, existingChild);
      }

      existingChild = existingChild.sibling;
    }

    return existingChildren;
  }

  function useFiber(fiber, pendingProps) {
    // We currently set sibling to null and index to 0 here because it is easy
    // to forget to do before returning it. E.g. for the single child case.
    var clone = createWorkInProgress(fiber, pendingProps);
    clone.index = 0;
    clone.sibling = null;
    return clone;
  }

  function placeChild(newFiber, lastPlacedIndex, newIndex) {
    newFiber.index = newIndex;

    if (!shouldTrackSideEffects) {
      // Noop.
      return lastPlacedIndex;
    }

    var current = newFiber.alternate;

    if (current !== null) {
      var oldIndex = current.index;

      if (oldIndex < lastPlacedIndex) {
        // This is a move.
        newFiber.effectTag = Placement;
        return lastPlacedIndex;
      } else {
        // This item can stay in place.
        return oldIndex;
      }
    } else {
      // This is an insertion.
      newFiber.effectTag = Placement;
      return lastPlacedIndex;
    }
  }

  function placeSingleChild(newFiber) {
    // This is simpler for the single child case. We only need to do a
    // placement for inserting new children.
    if (shouldTrackSideEffects && newFiber.alternate === null) {
      newFiber.effectTag = Placement;
    }

    return newFiber;
  }

  function updateTextNode(returnFiber, current, textContent, expirationTime) {
    if (current === null || current.tag !== HostText) {
      // Insert
      var created = createFiberFromText(textContent, returnFiber.mode, expirationTime);
      created.return = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, textContent);
      existing.return = returnFiber;
      return existing;
    }
  }

  function updateElement(returnFiber, current, element, expirationTime) {
    if (current !== null) {
      if (current.elementType === element.type || ( // Keep this check inline so it only runs on the false path:
       isCompatibleFamilyForHotReloading(current, element) )) {
        // Move based on index
        var existing = useFiber(current, element.props);
        existing.ref = coerceRef(returnFiber, current, element);
        existing.return = returnFiber;

        {
          existing._debugSource = element._source;
          existing._debugOwner = element._owner;
        }

        return existing;
      }
    } // Insert


    var created = createFiberFromElement(element, returnFiber.mode, expirationTime);
    created.ref = coerceRef(returnFiber, current, element);
    created.return = returnFiber;
    return created;
  }

  function updatePortal(returnFiber, current, portal, expirationTime) {
    if (current === null || current.tag !== HostPortal || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation) {
      // Insert
      var created = createFiberFromPortal(portal, returnFiber.mode, expirationTime);
      created.return = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, portal.children || []);
      existing.return = returnFiber;
      return existing;
    }
  }

  function updateFragment(returnFiber, current, fragment, expirationTime, key) {
    if (current === null || current.tag !== Fragment) {
      // Insert
      var created = createFiberFromFragment(fragment, returnFiber.mode, expirationTime, key);
      created.return = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, fragment);
      existing.return = returnFiber;
      return existing;
    }
  }

  function createChild(returnFiber, newChild, expirationTime) {
    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys. If the previous node is implicitly keyed
      // we can continue to replace it without aborting even if it is not a text
      // node.
      var created = createFiberFromText('' + newChild, returnFiber.mode, expirationTime);
      created.return = returnFiber;
      return created;
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            var _created = createFiberFromElement(newChild, returnFiber.mode, expirationTime);

            _created.ref = coerceRef(returnFiber, null, newChild);
            _created.return = returnFiber;
            return _created;
          }

        case REACT_PORTAL_TYPE:
          {
            var _created2 = createFiberFromPortal(newChild, returnFiber.mode, expirationTime);

            _created2.return = returnFiber;
            return _created2;
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        var _created3 = createFiberFromFragment(newChild, returnFiber.mode, expirationTime, null);

        _created3.return = returnFiber;
        return _created3;
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  function updateSlot(returnFiber, oldFiber, newChild, expirationTime) {
    // Update the fiber if the keys match, otherwise return null.
    var key = oldFiber !== null ? oldFiber.key : null;

    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys. If the previous node is implicitly keyed
      // we can continue to replace it without aborting even if it is not a text
      // node.
      if (key !== null) {
        return null;
      }

      return updateTextNode(returnFiber, oldFiber, '' + newChild, expirationTime);
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            if (newChild.key === key) {
              if (newChild.type === REACT_FRAGMENT_TYPE) {
                return updateFragment(returnFiber, oldFiber, newChild.props.children, expirationTime, key);
              }

              return updateElement(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }

        case REACT_PORTAL_TYPE:
          {
            if (newChild.key === key) {
              return updatePortal(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        if (key !== null) {
          return null;
        }

        return updateFragment(returnFiber, oldFiber, newChild, expirationTime, null);
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  function updateFromMap(existingChildren, returnFiber, newIdx, newChild, expirationTime) {
    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys, so we neither have to check the old nor
      // new node for the key. If both are text nodes, they match.
      var matchedFiber = existingChildren.get(newIdx) || null;
      return updateTextNode(returnFiber, matchedFiber, '' + newChild, expirationTime);
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            var _matchedFiber = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;

            if (newChild.type === REACT_FRAGMENT_TYPE) {
              return updateFragment(returnFiber, _matchedFiber, newChild.props.children, expirationTime, newChild.key);
            }

            return updateElement(returnFiber, _matchedFiber, newChild, expirationTime);
          }

        case REACT_PORTAL_TYPE:
          {
            var _matchedFiber2 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;

            return updatePortal(returnFiber, _matchedFiber2, newChild, expirationTime);
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        var _matchedFiber3 = existingChildren.get(newIdx) || null;

        return updateFragment(returnFiber, _matchedFiber3, newChild, expirationTime, null);
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }
  /**
   * Warns if there is a duplicate or missing key
   */


  function warnOnInvalidKey(child, knownKeys) {
    {
      if (typeof child !== 'object' || child === null) {
        return knownKeys;
      }

      switch (child.$$typeof) {
        case REACT_ELEMENT_TYPE:
        case REACT_PORTAL_TYPE:
          warnForMissingKey(child);
          var key = child.key;

          if (typeof key !== 'string') {
            break;
          }

          if (knownKeys === null) {
            knownKeys = new Set();
            knownKeys.add(key);
            break;
          }

          if (!knownKeys.has(key)) {
            knownKeys.add(key);
            break;
          }

          error('Encountered two children with the same key, `%s`. ' + 'Keys should be unique so that components maintain their identity ' + 'across updates. Non-unique keys may cause children to be ' + 'duplicated and/or omitted — the behavior is unsupported and ' + 'could change in a future version.', key);

          break;
      }
    }

    return knownKeys;
  }

  function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, expirationTime) {
    // This algorithm can't optimize by searching from both ends since we
    // don't have backpointers on fibers. I'm trying to see how far we can get
    // with that model. If it ends up not being worth the tradeoffs, we can
    // add it later.
    // Even with a two ended optimization, we'd want to optimize for the case
    // where there are few changes and brute force the comparison instead of
    // going for the Map. It'd like to explore hitting that path first in
    // forward-only mode and only go for the Map once we notice that we need
    // lots of look ahead. This doesn't handle reversal as well as two ended
    // search but that's unusual. Besides, for the two ended optimization to
    // work on Iterables, we'd need to copy the whole set.
    // In this first iteration, we'll just live with hitting the bad case
    // (adding everything to a Map) in for every insert/move.
    // If you change this code, also update reconcileChildrenIterator() which
    // uses the same algorithm.
    {
      // First, validate keys.
      var knownKeys = null;

      for (var i = 0; i < newChildren.length; i++) {
        var child = newChildren[i];
        knownKeys = warnOnInvalidKey(child, knownKeys);
      }
    }

    var resultingFirstChild = null;
    var previousNewFiber = null;
    var oldFiber = currentFirstChild;
    var lastPlacedIndex = 0;
    var newIdx = 0;
    var nextOldFiber = null;

    for (; oldFiber !== null && newIdx < newChildren.length; newIdx++) {
      if (oldFiber.index > newIdx) {
        nextOldFiber = oldFiber;
        oldFiber = null;
      } else {
        nextOldFiber = oldFiber.sibling;
      }

      var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], expirationTime);

      if (newFiber === null) {
        // TODO: This breaks on empty slots like null children. That's
        // unfortunate because it triggers the slow path all the time. We need
        // a better way to communicate whether this was a miss or null,
        // boolean, undefined, etc.
        if (oldFiber === null) {
          oldFiber = nextOldFiber;
        }

        break;
      }

      if (shouldTrackSideEffects) {
        if (oldFiber && newFiber.alternate === null) {
          // We matched the slot, but we didn't reuse the existing fiber, so we
          // need to delete the existing child.
          deleteChild(returnFiber, oldFiber);
        }
      }

      lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);

      if (previousNewFiber === null) {
        // TODO: Move out of the loop. This only happens for the first run.
        resultingFirstChild = newFiber;
      } else {
        // TODO: Defer siblings if we're not at the right index for this slot.
        // I.e. if we had null values before, then we want to defer this
        // for each null value. However, we also don't want to call updateSlot
        // with the previous one.
        previousNewFiber.sibling = newFiber;
      }

      previousNewFiber = newFiber;
      oldFiber = nextOldFiber;
    }

    if (newIdx === newChildren.length) {
      // We've reached the end of the new children. We can delete the rest.
      deleteRemainingChildren(returnFiber, oldFiber);
      return resultingFirstChild;
    }

    if (oldFiber === null) {
      // If we don't have any more existing children we can choose a fast path
      // since the rest will all be insertions.
      for (; newIdx < newChildren.length; newIdx++) {
        var _newFiber = createChild(returnFiber, newChildren[newIdx], expirationTime);

        if (_newFiber === null) {
          continue;
        }

        lastPlacedIndex = placeChild(_newFiber, lastPlacedIndex, newIdx);

        if (previousNewFiber === null) {
          // TODO: Move out of the loop. This only happens for the first run.
          resultingFirstChild = _newFiber;
        } else {
          previousNewFiber.sibling = _newFiber;
        }

        previousNewFiber = _newFiber;
      }

      return resultingFirstChild;
    } // Add all children to a key map for quick lookups.


    var existingChildren = mapRemainingChildren(returnFiber, oldFiber); // Keep scanning and use the map to restore deleted items as moves.

    for (; newIdx < newChildren.length; newIdx++) {
      var _newFiber2 = updateFromMap(existingChildren, returnFiber, newIdx, newChildren[newIdx], expirationTime);

      if (_newFiber2 !== null) {
        if (shouldTrackSideEffects) {
          if (_newFiber2.alternate !== null) {
            // The new fiber is a work in progress, but if there exists a
            // current, that means that we reused the fiber. We need to delete
            // it from the child list so that we don't add it to the deletion
            // list.
            existingChildren.delete(_newFiber2.key === null ? newIdx : _newFiber2.key);
          }
        }

        lastPlacedIndex = placeChild(_newFiber2, lastPlacedIndex, newIdx);

        if (previousNewFiber === null) {
          resultingFirstChild = _newFiber2;
        } else {
          previousNewFiber.sibling = _newFiber2;
        }

        previousNewFiber = _newFiber2;
      }
    }

    if (shouldTrackSideEffects) {
      // Any existing children that weren't consumed above were deleted. We need
      // to add them to the deletion list.
      existingChildren.forEach(function (child) {
        return deleteChild(returnFiber, child);
      });
    }

    return resultingFirstChild;
  }

  function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildrenIterable, expirationTime) {
    // This is the same implementation as reconcileChildrenArray(),
    // but using the iterator instead.
    var iteratorFn = getIteratorFn(newChildrenIterable);

    if (!(typeof iteratorFn === 'function')) {
      {
        throw Error( "An object is not an iterable. This error is likely caused by a bug in React. Please file an issue." );
      }
    }

    {
      // We don't support rendering Generators because it's a mutation.
      // See https://github.com/facebook/react/issues/12995
      if (typeof Symbol === 'function' && // $FlowFixMe Flow doesn't know about toStringTag
      newChildrenIterable[Symbol.toStringTag] === 'Generator') {
        if (!didWarnAboutGenerators) {
          error('Using Generators as children is unsupported and will likely yield ' + 'unexpected results because enumerating a generator mutates it. ' + 'You may convert it to an array with `Array.from()` or the ' + '`[...spread]` operator before rendering. Keep in mind ' + 'you might need to polyfill these features for older browsers.');
        }

        didWarnAboutGenerators = true;
      } // Warn about using Maps as children


      if (newChildrenIterable.entries === iteratorFn) {
        if (!didWarnAboutMaps) {
          error('Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.');
        }

        didWarnAboutMaps = true;
      } // First, validate keys.
      // We'll get a different iterator later for the main pass.


      var _newChildren = iteratorFn.call(newChildrenIterable);

      if (_newChildren) {
        var knownKeys = null;

        var _step = _newChildren.next();

        for (; !_step.done; _step = _newChildren.next()) {
          var child = _step.value;
          knownKeys = warnOnInvalidKey(child, knownKeys);
        }
      }
    }

    var newChildren = iteratorFn.call(newChildrenIterable);

    if (!(newChildren != null)) {
      {
        throw Error( "An iterable object provided no iterator." );
      }
    }

    var resultingFirstChild = null;
    var previousNewFiber = null;
    var oldFiber = currentFirstChild;
    var lastPlacedIndex = 0;
    var newIdx = 0;
    var nextOldFiber = null;
    var step = newChildren.next();

    for (; oldFiber !== null && !step.done; newIdx++, step = newChildren.next()) {
      if (oldFiber.index > newIdx) {
        nextOldFiber = oldFiber;
        oldFiber = null;
      } else {
        nextOldFiber = oldFiber.sibling;
      }

      var newFiber = updateSlot(returnFiber, oldFiber, step.value, expirationTime);

      if (newFiber === null) {
        // TODO: This breaks on empty slots like null children. That's
        // unfortunate because it triggers the slow path all the time. We need
        // a better way to communicate whether this was a miss or null,
        // boolean, undefined, etc.
        if (oldFiber === null) {
          oldFiber = nextOldFiber;
        }

        break;
      }

      if (shouldTrackSideEffects) {
        if (oldFiber && newFiber.alternate === null) {
          // We matched the slot, but we didn't reuse the existing fiber, so we
          // need to delete the existing child.
          deleteChild(returnFiber, oldFiber);
        }
      }

      lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);

      if (previousNewFiber === null) {
        // TODO: Move out of the loop. This only happens for the first run.
        resultingFirstChild = newFiber;
      } else {
        // TODO: Defer siblings if we're not at the right index for this slot.
        // I.e. if we had null values before, then we want to defer this
        // for each null value. However, we also don't want to call updateSlot
        // with the previous one.
        previousNewFiber.sibling = newFiber;
      }

      previousNewFiber = newFiber;
      oldFiber = nextOldFiber;
    }

    if (step.done) {
      // We've reached the end of the new children. We can delete the rest.
      deleteRemainingChildren(returnFiber, oldFiber);
      return resultingFirstChild;
    }

    if (oldFiber === null) {
      // If we don't have any more existing children we can choose a fast path
      // since the rest will all be insertions.
      for (; !step.done; newIdx++, step = newChildren.next()) {
        var _newFiber3 = createChild(returnFiber, step.value, expirationTime);

        if (_newFiber3 === null) {
          continue;
        }

        lastPlacedIndex = placeChild(_newFiber3, lastPlacedIndex, newIdx);

        if (previousNewFiber === null) {
          // TODO: Move out of the loop. This only happens for the first run.
          resultingFirstChild = _newFiber3;
        } else {
          previousNewFiber.sibling = _newFiber3;
        }

        previousNewFiber = _newFiber3;
      }

      return resultingFirstChild;
    } // Add all children to a key map for quick lookups.


    var existingChildren = mapRemainingChildren(returnFiber, oldFiber); // Keep scanning and use the map to restore deleted items as moves.

    for (; !step.done; newIdx++, step = newChildren.next()) {
      var _newFiber4 = updateFromMap(existingChildren, returnFiber, newIdx, step.value, expirationTime);

      if (_newFiber4 !== null) {
        if (shouldTrackSideEffects) {
          if (_newFiber4.alternate !== null) {
            // The new fiber is a work in progress, but if there exists a
            // current, that means that we reused the fiber. We need to delete
            // it from the child list so that we don't add it to the deletion
            // list.
            existingChildren.delete(_newFiber4.key === null ? newIdx : _newFiber4.key);
          }
        }

        lastPlacedIndex = placeChild(_newFiber4, lastPlacedIndex, newIdx);

        if (previousNewFiber === null) {
          resultingFirstChild = _newFiber4;
        } else {
          previousNewFiber.sibling = _newFiber4;
        }

        previousNewFiber = _newFiber4;
      }
    }

    if (shouldTrackSideEffects) {
      // Any existing children that weren't consumed above were deleted. We need
      // to add them to the deletion list.
      existingChildren.forEach(function (child) {
        return deleteChild(returnFiber, child);
      });
    }

    return resultingFirstChild;
  }

  function reconcileSingleTextNode(returnFiber, currentFirstChild, textContent, expirationTime) {
    // There's no need to check for keys on text nodes since we don't have a
    // way to define them.
    if (currentFirstChild !== null && currentFirstChild.tag === HostText) {
      // We already have an existing node so let's just update it and delete
      // the rest.
      deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
      var existing = useFiber(currentFirstChild, textContent);
      existing.return = returnFiber;
      return existing;
    } // The existing first child is not a text node so we need to create one
    // and delete the existing ones.


    deleteRemainingChildren(returnFiber, currentFirstChild);
    var created = createFiberFromText(textContent, returnFiber.mode, expirationTime);
    created.return = returnFiber;
    return created;
  }

  function reconcileSingleElement(returnFiber, currentFirstChild, element, expirationTime) {
    var key = element.key;
    var child = currentFirstChild;

    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        switch (child.tag) {
          case Fragment:
            {
              if (element.type === REACT_FRAGMENT_TYPE) {
                deleteRemainingChildren(returnFiber, child.sibling);
                var existing = useFiber(child, element.props.children);
                existing.return = returnFiber;

                {
                  existing._debugSource = element._source;
                  existing._debugOwner = element._owner;
                }

                return existing;
              }

              break;
            }

          case Block:

          // We intentionally fallthrough here if enableBlocksAPI is not on.
          // eslint-disable-next-lined no-fallthrough

          default:
            {
              if (child.elementType === element.type || ( // Keep this check inline so it only runs on the false path:
               isCompatibleFamilyForHotReloading(child, element) )) {
                deleteRemainingChildren(returnFiber, child.sibling);

                var _existing3 = useFiber(child, element.props);

                _existing3.ref = coerceRef(returnFiber, child, element);
                _existing3.return = returnFiber;

                {
                  _existing3._debugSource = element._source;
                  _existing3._debugOwner = element._owner;
                }

                return _existing3;
              }

              break;
            }
        } // Didn't match.


        deleteRemainingChildren(returnFiber, child);
        break;
      } else {
        deleteChild(returnFiber, child);
      }

      child = child.sibling;
    }

    if (element.type === REACT_FRAGMENT_TYPE) {
      var created = createFiberFromFragment(element.props.children, returnFiber.mode, expirationTime, element.key);
      created.return = returnFiber;
      return created;
    } else {
      var _created4 = createFiberFromElement(element, returnFiber.mode, expirationTime);

      _created4.ref = coerceRef(returnFiber, currentFirstChild, element);
      _created4.return = returnFiber;
      return _created4;
    }
  }

  function reconcileSinglePortal(returnFiber, currentFirstChild, portal, expirationTime) {
    var key = portal.key;
    var child = currentFirstChild;

    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === HostPortal && child.stateNode.containerInfo === portal.containerInfo && child.stateNode.implementation === portal.implementation) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, portal.children || []);
          existing.return = returnFiber;
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }

      child = child.sibling;
    }

    var created = createFiberFromPortal(portal, returnFiber.mode, expirationTime);
    created.return = returnFiber;
    return created;
  } // This API will tag the children with the side-effect of the reconciliation
  // itself. They will be added to the side-effect list as we pass through the
  // children and the parent.


  function reconcileChildFibers(returnFiber, currentFirstChild, newChild, expirationTime) {
    // This function is not recursive.
    // If the top level item is an array, we treat it as a set of children,
    // not as a fragment. Nested arrays on the other hand will be treated as
    // fragment nodes. Recursion happens at the normal flow.
    // Handle top level unkeyed fragments as if they were arrays.
    // This leads to an ambiguity between <>{[...]}</> and <>...</>.
    // We treat the ambiguous cases above the same.
    var isUnkeyedTopLevelFragment = typeof newChild === 'object' && newChild !== null && newChild.type === REACT_FRAGMENT_TYPE && newChild.key === null;

    if (isUnkeyedTopLevelFragment) {
      newChild = newChild.props.children;
    } // Handle object types


    var isObject = typeof newChild === 'object' && newChild !== null;

    if (isObject) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          return placeSingleChild(reconcileSingleElement(returnFiber, currentFirstChild, newChild, expirationTime));

        case REACT_PORTAL_TYPE:
          return placeSingleChild(reconcileSinglePortal(returnFiber, currentFirstChild, newChild, expirationTime));
      }
    }

    if (typeof newChild === 'string' || typeof newChild === 'number') {
      return placeSingleChild(reconcileSingleTextNode(returnFiber, currentFirstChild, '' + newChild, expirationTime));
    }

    if (isArray$1(newChild)) {
      return reconcileChildrenArray(returnFiber, currentFirstChild, newChild, expirationTime);
    }

    if (getIteratorFn(newChild)) {
      return reconcileChildrenIterator(returnFiber, currentFirstChild, newChild, expirationTime);
    }

    if (isObject) {
      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    if (typeof newChild === 'undefined' && !isUnkeyedTopLevelFragment) {
      // If the new child is undefined, and the return fiber is a composite
      // component, throw an error. If Fiber return types are disabled,
      // we already threw above.
      switch (returnFiber.tag) {
        case ClassComponent:
          {
            {
              var instance = returnFiber.stateNode;

              if (instance.render._isMockFunction) {
                // We allow auto-mocks to proceed as if they're returning null.
                break;
              }
            }
          }
        // Intentionally fall through to the next case, which handles both
        // functions and classes
        // eslint-disable-next-lined no-fallthrough

        case FunctionComponent:
          {
            var Component = returnFiber.type;

            {
              {
                throw Error( (Component.displayName || Component.name || 'Component') + "(...): Nothing was returned from render. This usually means a return statement is missing. Or, to render nothing, return null." );
              }
            }
          }
      }
    } // Remaining cases are all treated as empty.


    return deleteRemainingChildren(returnFiber, currentFirstChild);
  }

  return reconcileChildFibers;
}

var reconcileChildFibers = ChildReconciler(true);
var mountChildFibers = ChildReconciler(false);
function cloneChildFibers(current, workInProgress) {
  if (!(current === null || workInProgress.child === current.child)) {
    {
      throw Error( "Resuming work not yet implemented." );
    }
  }

  if (workInProgress.child === null) {
    return;
  }

  var currentChild = workInProgress.child;
  var newChild = createWorkInProgress(currentChild, currentChild.pendingProps);
  workInProgress.child = newChild;
  newChild.return = workInProgress;

  while (currentChild.sibling !== null) {
    currentChild = currentChild.sibling;
    newChild = newChild.sibling = createWorkInProgress(currentChild, currentChild.pendingProps);
    newChild.return = workInProgress;
  }

  newChild.sibling = null;
} // Reset a workInProgress child set to prepare it for a second pass.

function resetChildFibers(workInProgress, renderExpirationTime) {
  var child = workInProgress.child;

  while (child !== null) {
    resetWorkInProgress(child, renderExpirationTime);
    child = child.sibling;
  }
}

var NO_CONTEXT = {};
var contextStackCursor$1 = createCursor(NO_CONTEXT);
var contextFiberStackCursor = createCursor(NO_CONTEXT);
var rootInstanceStackCursor = createCursor(NO_CONTEXT);

function requiredContext(c) {
  if (!(c !== NO_CONTEXT)) {
    {
      throw Error( "Expected host context to exist. This error is likely caused by a bug in React. Please file an issue." );
    }
  }

  return c;
}

function getRootHostContainer() {
  var rootInstance = requiredContext(rootInstanceStackCursor.current);
  return rootInstance;
}

function pushHostContainer(fiber, nextRootInstance) {
  // Push current root instance onto the stack;
  // This allows us to reset root when portals are popped.
  push(rootInstanceStackCursor, nextRootInstance, fiber); // Track the context and the Fiber that provided it.
  // This enables us to pop only Fibers that provide unique contexts.

  push(contextFiberStackCursor, fiber, fiber); // Finally, we need to push the host context to the stack.
  // However, we can't just call getRootHostContext() and push it because
  // we'd have a different number of entries on the stack depending on
  // whether getRootHostContext() throws somewhere in renderer code or not.
  // So we push an empty value first. This lets us safely unwind on errors.

  push(contextStackCursor$1, NO_CONTEXT, fiber);
  var nextRootContext = getRootHostContext(nextRootInstance); // Now that we know this function doesn't throw, replace it.

  pop(contextStackCursor$1, fiber);
  push(contextStackCursor$1, nextRootContext, fiber);
}

function popHostContainer(fiber) {
  pop(contextStackCursor$1, fiber);
  pop(contextFiberStackCursor, fiber);
  pop(rootInstanceStackCursor, fiber);
}

function getHostContext() {
  var context = requiredContext(contextStackCursor$1.current);
  return context;
}

function pushHostContext(fiber) {
  var rootInstance = requiredContext(rootInstanceStackCursor.current);
  var context = requiredContext(contextStackCursor$1.current);
  var nextContext = getChildHostContext(context, fiber.type); // Don't push this Fiber's context unless it's unique.

  if (context === nextContext) {
    return;
  } // Track the context and the Fiber that provided it.
  // This enables us to pop only Fibers that provide unique contexts.


  push(contextFiberStackCursor, fiber, fiber);
  push(contextStackCursor$1, nextContext, fiber);
}

function popHostContext(fiber) {
  // Do not pop unless this Fiber provided the current context.
  // pushHostContext() only pushes Fibers that provide unique contexts.
  if (contextFiberStackCursor.current !== fiber) {
    return;
  }

  pop(contextStackCursor$1, fiber);
  pop(contextFiberStackCursor, fiber);
}

var DefaultSuspenseContext = 0; // The Suspense Context is split into two parts. The lower bits is
// inherited deeply down the subtree. The upper bits only affect
// this immediate suspense boundary and gets reset each new
// boundary or suspense list.

var SubtreeSuspenseContextMask = 1; // Subtree Flags:
// InvisibleParentSuspenseContext indicates that one of our parent Suspense
// boundaries is not currently showing visible main content.
// Either because it is already showing a fallback or is not mounted at all.
// We can use this to determine if it is desirable to trigger a fallback at
// the parent. If not, then we might need to trigger undesirable boundaries
// and/or suspend the commit to avoid hiding the parent content.

var InvisibleParentSuspenseContext = 1; // Shallow Flags:
// ForceSuspenseFallback can be used by SuspenseList to force newly added
// items into their fallback state during one of the render passes.

var ForceSuspenseFallback = 2;
var suspenseStackCursor = createCursor(DefaultSuspenseContext);
function hasSuspenseContext(parentContext, flag) {
  return (parentContext & flag) !== 0;
}
function setDefaultShallowSuspenseContext(parentContext) {
  return parentContext & SubtreeSuspenseContextMask;
}
function setShallowSuspenseContext(parentContext, shallowContext) {
  return parentContext & SubtreeSuspenseContextMask | shallowContext;
}
function addSubtreeSuspenseContext(parentContext, subtreeContext) {
  return parentContext | subtreeContext;
}
function pushSuspenseContext(fiber, newContext) {
  push(suspenseStackCursor, newContext, fiber);
}
function popSuspenseContext(fiber) {
  pop(suspenseStackCursor, fiber);
}

function shouldCaptureSuspense(workInProgress, hasInvisibleParent) {
  // If it was the primary children that just suspended, capture and render the
  // fallback. Otherwise, don't capture and bubble to the next boundary.
  var nextState = workInProgress.memoizedState;

  if (nextState !== null) {
    if (nextState.dehydrated !== null) {
      // A dehydrated boundary always captures.
      return true;
    }

    return false;
  }

  var props = workInProgress.memoizedProps; // In order to capture, the Suspense component must have a fallback prop.

  if (props.fallback === undefined) {
    return false;
  } // Regular boundaries always capture.


  if (props.unstable_avoidThisFallback !== true) {
    return true;
  } // If it's a boundary we should avoid, then we prefer to bubble up to the
  // parent boundary if it is currently invisible.


  if (hasInvisibleParent) {
    return false;
  } // If the parent is not able to handle it, we must handle it.


  return true;
}
function findFirstSuspended(row) {
  var node = row;

  while (node !== null) {
    if (node.tag === SuspenseComponent) {
      var state = node.memoizedState;

      if (state !== null) {
        var dehydrated = state.dehydrated;

        if (dehydrated === null || isSuspenseInstancePending(dehydrated) || isSuspenseInstanceFallback(dehydrated)) {
          return node;
        }
      }
    } else if (node.tag === SuspenseListComponent && // revealOrder undefined can't be trusted because it don't
    // keep track of whether it suspended or not.
    node.memoizedProps.revealOrder !== undefined) {
      var didSuspend = (node.effectTag & DidCapture) !== NoEffect;

      if (didSuspend) {
        return node;
      }
    } else if (node.child !== null) {
      node.child.return = node;
      node = node.child;
      continue;
    }

    if (node === row) {
      return null;
    }

    while (node.sibling === null) {
      if (node.return === null || node.return === row) {
        return null;
      }

      node = node.return;
    }

    node.sibling.return = node.return;
    node = node.sibling;
  }

  return null;
}

function createDeprecatedResponderListener(responder, props) {
  var eventResponderListener = {
    responder: responder,
    props: props
  };

  {
    Object.freeze(eventResponderListener);
  }

  return eventResponderListener;
}

var HasEffect =
/* */
1; // Represents the phase in which the effect (not the clean-up) fires.

var Layout =
/*    */
2;
var Passive$1 =
/*   */
4;

var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher,
    ReactCurrentBatchConfig$1 = ReactSharedInternals.ReactCurrentBatchConfig;
var didWarnAboutMismatchedHooksForComponent;

{
  didWarnAboutMismatchedHooksForComponent = new Set();
}

// These are set right before calling the component.
var renderExpirationTime = NoWork; // The work-in-progress fiber. I've named it differently to distinguish it from
// the work-in-progress hook.

var currentlyRenderingFiber$1 = null; // Hooks are stored as a linked list on the fiber's memoizedState field. The
// current hook list is the list that belongs to the current fiber. The
// work-in-progress hook list is a new list that will be added to the
// work-in-progress fiber.

var currentHook = null;
var workInProgressHook = null; // Whether an update was scheduled at any point during the render phase. This
// does not get reset if we do another render pass; only when we're completely
// finished evaluating this component. This is an optimization so we know
// whether we need to clear render phase updates after a throw.

var didScheduleRenderPhaseUpdate = false;
var RE_RENDER_LIMIT = 25; // In DEV, this is the name of the currently executing primitive hook

var currentHookNameInDev = null; // In DEV, this list ensures that hooks are called in the same order between renders.
// The list stores the order of hooks used during the initial render (mount).
// Subsequent renders (updates) reference this list.

var hookTypesDev = null;
var hookTypesUpdateIndexDev = -1; // In DEV, this tracks whether currently rendering component needs to ignore
// the dependencies for Hooks that need them (e.g. useEffect or useMemo).
// When true, such Hooks will always be "remounted". Only used during hot reload.

var ignorePreviousDependencies = false;

function mountHookTypesDev() {
  {
    var hookName = currentHookNameInDev;

    if (hookTypesDev === null) {
      hookTypesDev = [hookName];
    } else {
      hookTypesDev.push(hookName);
    }
  }
}

function updateHookTypesDev() {
  {
    var hookName = currentHookNameInDev;

    if (hookTypesDev !== null) {
      hookTypesUpdateIndexDev++;

      if (hookTypesDev[hookTypesUpdateIndexDev] !== hookName) {
        warnOnHookMismatchInDev(hookName);
      }
    }
  }
}

function checkDepsAreArrayDev(deps) {
  {
    if (deps !== undefined && deps !== null && !Array.isArray(deps)) {
      // Verify deps, but only on mount to avoid extra checks.
      // It's unlikely their type would change as usually you define them inline.
      error('%s received a final argument that is not an array (instead, received `%s`). When ' + 'specified, the final argument must be an array.', currentHookNameInDev, typeof deps);
    }
  }
}

function warnOnHookMismatchInDev(currentHookName) {
  {
    var componentName = getComponentName(currentlyRenderingFiber$1.type);

    if (!didWarnAboutMismatchedHooksForComponent.has(componentName)) {
      didWarnAboutMismatchedHooksForComponent.add(componentName);

      if (hookTypesDev !== null) {
        var table = '';
        var secondColumnStart = 30;

        for (var i = 0; i <= hookTypesUpdateIndexDev; i++) {
          var oldHookName = hookTypesDev[i];
          var newHookName = i === hookTypesUpdateIndexDev ? currentHookName : oldHookName;
          var row = i + 1 + ". " + oldHookName; // Extra space so second column lines up
          // lol @ IE not supporting String#repeat

          while (row.length < secondColumnStart) {
            row += ' ';
          }

          row += newHookName + '\n';
          table += row;
        }

        error('React has detected a change in the order of Hooks called by %s. ' + 'This will lead to bugs and errors if not fixed. ' + 'For more information, read the Rules of Hooks: https://fb.me/rules-of-hooks\n\n' + '   Previous render            Next render\n' + '   ------------------------------------------------------\n' + '%s' + '   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n', componentName, table);
      }
    }
  }
}

function throwInvalidHookError() {
  {
    {
      throw Error( "Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://fb.me/react-invalid-hook-call for tips about how to debug and fix this problem." );
    }
  }
}

function areHookInputsEqual(nextDeps, prevDeps) {
  {
    if (ignorePreviousDependencies) {
      // Only true when this component is being hot reloaded.
      return false;
    }
  }

  if (prevDeps === null) {
    {
      error('%s received a final argument during this render, but not during ' + 'the previous render. Even though the final argument is optional, ' + 'its type cannot change between renders.', currentHookNameInDev);
    }

    return false;
  }

  {
    // Don't bother comparing lengths in prod because these arrays should be
    // passed inline.
    if (nextDeps.length !== prevDeps.length) {
      error('The final argument passed to %s changed size between renders. The ' + 'order and size of this array must remain constant.\n\n' + 'Previous: %s\n' + 'Incoming: %s', currentHookNameInDev, "[" + prevDeps.join(', ') + "]", "[" + nextDeps.join(', ') + "]");
    }
  }

  for (var i = 0; i < prevDeps.length && i < nextDeps.length; i++) {
    if (objectIs(nextDeps[i], prevDeps[i])) {
      continue;
    }

    return false;
  }

  return true;
}

function renderWithHooks(current, workInProgress, Component, props, secondArg, nextRenderExpirationTime) {
  renderExpirationTime = nextRenderExpirationTime;
  currentlyRenderingFiber$1 = workInProgress;

  {
    hookTypesDev = current !== null ? current._debugHookTypes : null;
    hookTypesUpdateIndexDev = -1; // Used for hot reloading:

    ignorePreviousDependencies = current !== null && current.type !== workInProgress.type;
  }

  workInProgress.memoizedState = null;
  workInProgress.updateQueue = null;
  workInProgress.expirationTime = NoWork; // The following should have already been reset
  // currentHook = null;
  // workInProgressHook = null;
  // didScheduleRenderPhaseUpdate = false;
  // TODO Warn if no hooks are used at all during mount, then some are used during update.
  // Currently we will identify the update render as a mount because memoizedState === null.
  // This is tricky because it's valid for certain types of components (e.g. React.lazy)
  // Using memoizedState to differentiate between mount/update only works if at least one stateful hook is used.
  // Non-stateful hooks (e.g. context) don't get added to memoizedState,
  // so memoizedState would be null during updates and mounts.

  {
    if (current !== null && current.memoizedState !== null) {
      ReactCurrentDispatcher.current = HooksDispatcherOnUpdateInDEV;
    } else if (hookTypesDev !== null) {
      // This dispatcher handles an edge case where a component is updating,
      // but no stateful hooks have been used.
      // We want to match the production code behavior (which will use HooksDispatcherOnMount),
      // but with the extra DEV validation to ensure hooks ordering hasn't changed.
      // This dispatcher does that.
      ReactCurrentDispatcher.current = HooksDispatcherOnMountWithHookTypesInDEV;
    } else {
      ReactCurrentDispatcher.current = HooksDispatcherOnMountInDEV;
    }
  }

  var children = Component(props, secondArg); // Check if there was a render phase update

  if (workInProgress.expirationTime === renderExpirationTime) {
    // Keep rendering in a loop for as long as render phase updates continue to
    // be scheduled. Use a counter to prevent infinite loops.
    var numberOfReRenders = 0;

    do {
      workInProgress.expirationTime = NoWork;

      if (!(numberOfReRenders < RE_RENDER_LIMIT)) {
        {
          throw Error( "Too many re-renders. React limits the number of renders to prevent an infinite loop." );
        }
      }

      numberOfReRenders += 1;

      {
        // Even when hot reloading, allow dependencies to stabilize
        // after first render to prevent infinite render phase updates.
        ignorePreviousDependencies = false;
      } // Start over from the beginning of the list


      currentHook = null;
      workInProgressHook = null;
      workInProgress.updateQueue = null;

      {
        // Also validate hook order for cascading updates.
        hookTypesUpdateIndexDev = -1;
      }

      ReactCurrentDispatcher.current =  HooksDispatcherOnRerenderInDEV ;
      children = Component(props, secondArg);
    } while (workInProgress.expirationTime === renderExpirationTime);
  } // We can assume the previous dispatcher is always this one, since we set it
  // at the beginning of the render phase and there's no re-entrancy.


  ReactCurrentDispatcher.current = ContextOnlyDispatcher;

  {
    workInProgress._debugHookTypes = hookTypesDev;
  } // This check uses currentHook so that it works the same in DEV and prod bundles.
  // hookTypesDev could catch more cases (e.g. context) but only in DEV bundles.


  var didRenderTooFewHooks = currentHook !== null && currentHook.next !== null;
  renderExpirationTime = NoWork;
  currentlyRenderingFiber$1 = null;
  currentHook = null;
  workInProgressHook = null;

  {
    currentHookNameInDev = null;
    hookTypesDev = null;
    hookTypesUpdateIndexDev = -1;
  }

  didScheduleRenderPhaseUpdate = false;

  if (!!didRenderTooFewHooks) {
    {
      throw Error( "Rendered fewer hooks than expected. This may be caused by an accidental early return statement." );
    }
  }

  return children;
}
function bailoutHooks(current, workInProgress, expirationTime) {
  workInProgress.updateQueue = current.updateQueue;
  workInProgress.effectTag &= ~(Passive | Update);

  if (current.expirationTime <= expirationTime) {
    current.expirationTime = NoWork;
  }
}
function resetHooksAfterThrow() {
  // We can assume the previous dispatcher is always this one, since we set it
  // at the beginning of the render phase and there's no re-entrancy.
  ReactCurrentDispatcher.current = ContextOnlyDispatcher;

  if (didScheduleRenderPhaseUpdate) {
    // There were render phase updates. These are only valid for this render
    // phase, which we are now aborting. Remove the updates from the queues so
    // they do not persist to the next render. Do not remove updates from hooks
    // that weren't processed.
    //
    // Only reset the updates from the queue if it has a clone. If it does
    // not have a clone, that means it wasn't processed, and the updates were
    // scheduled before we entered the render phase.
    var hook = currentlyRenderingFiber$1.memoizedState;

    while (hook !== null) {
      var queue = hook.queue;

      if (queue !== null) {
        queue.pending = null;
      }

      hook = hook.next;
    }
  }

  renderExpirationTime = NoWork;
  currentlyRenderingFiber$1 = null;
  currentHook = null;
  workInProgressHook = null;

  {
    hookTypesDev = null;
    hookTypesUpdateIndexDev = -1;
    currentHookNameInDev = null;
  }

  didScheduleRenderPhaseUpdate = false;
}

function mountWorkInProgressHook() {
  var hook = {
    memoizedState: null,
    baseState: null,
    baseQueue: null,
    queue: null,
    next: null
  };

  if (workInProgressHook === null) {
    // This is the first hook in the list
    currentlyRenderingFiber$1.memoizedState = workInProgressHook = hook;
  } else {
    // Append to the end of the list
    workInProgressHook = workInProgressHook.next = hook;
  }

  return workInProgressHook;
}

function updateWorkInProgressHook() {
  // This function is used both for updates and for re-renders triggered by a
  // render phase update. It assumes there is either a current hook we can
  // clone, or a work-in-progress hook from a previous render pass that we can
  // use as a base. When we reach the end of the base list, we must switch to
  // the dispatcher used for mounts.
  var nextCurrentHook;

  if (currentHook === null) {
    var current = currentlyRenderingFiber$1.alternate;

    if (current !== null) {
      nextCurrentHook = current.memoizedState;
    } else {
      nextCurrentHook = null;
    }
  } else {
    nextCurrentHook = currentHook.next;
  }

  var nextWorkInProgressHook;

  if (workInProgressHook === null) {
    nextWorkInProgressHook = currentlyRenderingFiber$1.memoizedState;
  } else {
    nextWorkInProgressHook = workInProgressHook.next;
  }

  if (nextWorkInProgressHook !== null) {
    // There's already a work-in-progress. Reuse it.
    workInProgressHook = nextWorkInProgressHook;
    nextWorkInProgressHook = workInProgressHook.next;
    currentHook = nextCurrentHook;
  } else {
    // Clone from the current hook.
    if (!(nextCurrentHook !== null)) {
      {
        throw Error( "Rendered more hooks than during the previous render." );
      }
    }

    currentHook = nextCurrentHook;
    var newHook = {
      memoizedState: currentHook.memoizedState,
      baseState: currentHook.baseState,
      baseQueue: currentHook.baseQueue,
      queue: currentHook.queue,
      next: null
    };

    if (workInProgressHook === null) {
      // This is the first hook in the list.
      currentlyRenderingFiber$1.memoizedState = workInProgressHook = newHook;
    } else {
      // Append to the end of the list.
      workInProgressHook = workInProgressHook.next = newHook;
    }
  }

  return workInProgressHook;
}

function createFunctionComponentUpdateQueue() {
  return {
    lastEffect: null
  };
}

function basicStateReducer(state, action) {
  // $FlowFixMe: Flow doesn't like mixed types
  return typeof action === 'function' ? action(state) : action;
}

function mountReducer(reducer, initialArg, init) {
  var hook = mountWorkInProgressHook();
  var initialState;

  if (init !== undefined) {
    initialState = init(initialArg);
  } else {
    initialState = initialArg;
  }

  hook.memoizedState = hook.baseState = initialState;
  var queue = hook.queue = {
    pending: null,
    dispatch: null,
    lastRenderedReducer: reducer,
    lastRenderedState: initialState
  };
  var dispatch = queue.dispatch = dispatchAction.bind(null, currentlyRenderingFiber$1, queue);
  return [hook.memoizedState, dispatch];
}

function updateReducer(reducer, initialArg, init) {
  var hook = updateWorkInProgressHook();
  var queue = hook.queue;

  if (!(queue !== null)) {
    {
      throw Error( "Should have a queue. This is likely a bug in React. Please file an issue." );
    }
  }

  queue.lastRenderedReducer = reducer;
  var current = currentHook; // The last rebase update that is NOT part of the base state.

  var baseQueue = current.baseQueue; // The last pending update that hasn't been processed yet.

  var pendingQueue = queue.pending;

  if (pendingQueue !== null) {
    // We have new updates that haven't been processed yet.
    // We'll add them to the base queue.
    if (baseQueue !== null) {
      // Merge the pending queue and the base queue.
      var baseFirst = baseQueue.next;
      var pendingFirst = pendingQueue.next;
      baseQueue.next = pendingFirst;
      pendingQueue.next = baseFirst;
    }

    current.baseQueue = baseQueue = pendingQueue;
    queue.pending = null;
  }

  if (baseQueue !== null) {
    // We have a queue to process.
    var first = baseQueue.next;
    var newState = current.baseState;
    var newBaseState = null;
    var newBaseQueueFirst = null;
    var newBaseQueueLast = null;
    var update = first;

    do {
      var updateExpirationTime = update.expirationTime;

      if (updateExpirationTime < renderExpirationTime) {
        // Priority is insufficient. Skip this update. If this is the first
        // skipped update, the previous update/state is the new base
        // update/state.
        var clone = {
          expirationTime: update.expirationTime,
          suspenseConfig: update.suspenseConfig,
          action: update.action,
          eagerReducer: update.eagerReducer,
          eagerState: update.eagerState,
          next: null
        };

        if (newBaseQueueLast === null) {
          newBaseQueueFirst = newBaseQueueLast = clone;
          newBaseState = newState;
        } else {
          newBaseQueueLast = newBaseQueueLast.next = clone;
        } // Update the remaining priority in the queue.


        if (updateExpirationTime > currentlyRenderingFiber$1.expirationTime) {
          currentlyRenderingFiber$1.expirationTime = updateExpirationTime;
          markUnprocessedUpdateTime(updateExpirationTime);
        }
      } else {
        // This update does have sufficient priority.
        if (newBaseQueueLast !== null) {
          var _clone = {
            expirationTime: Sync,
            // This update is going to be committed so we never want uncommit it.
            suspenseConfig: update.suspenseConfig,
            action: update.action,
            eagerReducer: update.eagerReducer,
            eagerState: update.eagerState,
            next: null
          };
          newBaseQueueLast = newBaseQueueLast.next = _clone;
        } // Mark the event time of this update as relevant to this render pass.
        // TODO: This should ideally use the true event time of this update rather than
        // its priority which is a derived and not reverseable value.
        // TODO: We should skip this update if it was already committed but currently
        // we have no way of detecting the difference between a committed and suspended
        // update here.


        markRenderEventTimeAndConfig(updateExpirationTime, update.suspenseConfig); // Process this update.

        if (update.eagerReducer === reducer) {
          // If this update was processed eagerly, and its reducer matches the
          // current reducer, we can use the eagerly computed state.
          newState = update.eagerState;
        } else {
          var action = update.action;
          newState = reducer(newState, action);
        }
      }

      update = update.next;
    } while (update !== null && update !== first);

    if (newBaseQueueLast === null) {
      newBaseState = newState;
    } else {
      newBaseQueueLast.next = newBaseQueueFirst;
    } // Mark that the fiber performed work, but only if the new state is
    // different from the current state.


    if (!objectIs(newState, hook.memoizedState)) {
      markWorkInProgressReceivedUpdate();
    }

    hook.memoizedState = newState;
    hook.baseState = newBaseState;
    hook.baseQueue = newBaseQueueLast;
    queue.lastRenderedState = newState;
  }

  var dispatch = queue.dispatch;
  return [hook.memoizedState, dispatch];
}

function rerenderReducer(reducer, initialArg, init) {
  var hook = updateWorkInProgressHook();
  var queue = hook.queue;

  if (!(queue !== null)) {
    {
      throw Error( "Should have a queue. This is likely a bug in React. Please file an issue." );
    }
  }

  queue.lastRenderedReducer = reducer; // This is a re-render. Apply the new render phase updates to the previous
  // work-in-progress hook.

  var dispatch = queue.dispatch;
  var lastRenderPhaseUpdate = queue.pending;
  var newState = hook.memoizedState;

  if (lastRenderPhaseUpdate !== null) {
    // The queue doesn't persist past this render pass.
    queue.pending = null;
    var firstRenderPhaseUpdate = lastRenderPhaseUpdate.next;
    var update = firstRenderPhaseUpdate;

    do {
      // Process this render phase update. We don't have to check the
      // priority because it will always be the same as the current
      // render's.
      var action = update.action;
      newState = reducer(newState, action);
      update = update.next;
    } while (update !== firstRenderPhaseUpdate); // Mark that the fiber performed work, but only if the new state is
    // different from the current state.


    if (!objectIs(newState, hook.memoizedState)) {
      markWorkInProgressReceivedUpdate();
    }

    hook.memoizedState = newState; // Don't persist the state accumulated from the render phase updates to
    // the base state unless the queue is empty.
    // TODO: Not sure if this is the desired semantics, but it's what we
    // do for gDSFP. I can't remember why.

    if (hook.baseQueue === null) {
      hook.baseState = newState;
    }

    queue.lastRenderedState = newState;
  }

  return [newState, dispatch];
}

function mountState(initialState) {
  var hook = mountWorkInProgressHook();

  if (typeof initialState === 'function') {
    // $FlowFixMe: Flow doesn't like mixed types
    initialState = initialState();
  }

  hook.memoizedState = hook.baseState = initialState;
  var queue = hook.queue = {
    pending: null,
    dispatch: null,
    lastRenderedReducer: basicStateReducer,
    lastRenderedState: initialState
  };
  var dispatch = queue.dispatch = dispatchAction.bind(null, currentlyRenderingFiber$1, queue);
  return [hook.memoizedState, dispatch];
}

function updateState(initialState) {
  return updateReducer(basicStateReducer);
}

function rerenderState(initialState) {
  return rerenderReducer(basicStateReducer);
}

function pushEffect(tag, create, destroy, deps) {
  var effect = {
    tag: tag,
    create: create,
    destroy: destroy,
    deps: deps,
    // Circular
    next: null
  };
  var componentUpdateQueue = currentlyRenderingFiber$1.updateQueue;

  if (componentUpdateQueue === null) {
    componentUpdateQueue = createFunctionComponentUpdateQueue();
    currentlyRenderingFiber$1.updateQueue = componentUpdateQueue;
    componentUpdateQueue.lastEffect = effect.next = effect;
  } else {
    var lastEffect = componentUpdateQueue.lastEffect;

    if (lastEffect === null) {
      componentUpdateQueue.lastEffect = effect.next = effect;
    } else {
      var firstEffect = lastEffect.next;
      lastEffect.next = effect;
      effect.next = firstEffect;
      componentUpdateQueue.lastEffect = effect;
    }
  }

  return effect;
}

function mountRef(initialValue) {
  var hook = mountWorkInProgressHook();
  var ref = {
    current: initialValue
  };

  {
    Object.seal(ref);
  }

  hook.memoizedState = ref;
  return ref;
}

function updateRef(initialValue) {
  var hook = updateWorkInProgressHook();
  return hook.memoizedState;
}

function mountEffectImpl(fiberEffectTag, hookEffectTag, create, deps) {
  var hook = mountWorkInProgressHook();
  var nextDeps = deps === undefined ? null : deps;
  currentlyRenderingFiber$1.effectTag |= fiberEffectTag;
  hook.memoizedState = pushEffect(HasEffect | hookEffectTag, create, undefined, nextDeps);
}

function updateEffectImpl(fiberEffectTag, hookEffectTag, create, deps) {
  var hook = updateWorkInProgressHook();
  var nextDeps = deps === undefined ? null : deps;
  var destroy = undefined;

  if (currentHook !== null) {
    var prevEffect = currentHook.memoizedState;
    destroy = prevEffect.destroy;

    if (nextDeps !== null) {
      var prevDeps = prevEffect.deps;

      if (areHookInputsEqual(nextDeps, prevDeps)) {
        pushEffect(hookEffectTag, create, destroy, nextDeps);
        return;
      }
    }
  }

  currentlyRenderingFiber$1.effectTag |= fiberEffectTag;
  hook.memoizedState = pushEffect(HasEffect | hookEffectTag, create, destroy, nextDeps);
}

function mountEffect(create, deps) {
  {
    // $FlowExpectedError - jest isn't a global, and isn't recognized outside of tests
    if ('undefined' !== typeof jest) {
      warnIfNotCurrentlyActingEffectsInDEV(currentlyRenderingFiber$1);
    }
  }

  return mountEffectImpl(Update | Passive, Passive$1, create, deps);
}

function updateEffect(create, deps) {
  {
    // $FlowExpectedError - jest isn't a global, and isn't recognized outside of tests
    if ('undefined' !== typeof jest) {
      warnIfNotCurrentlyActingEffectsInDEV(currentlyRenderingFiber$1);
    }
  }

  return updateEffectImpl(Update | Passive, Passive$1, create, deps);
}

function mountLayoutEffect(create, deps) {
  return mountEffectImpl(Update, Layout, create, deps);
}

function updateLayoutEffect(create, deps) {
  return updateEffectImpl(Update, Layout, create, deps);
}

function imperativeHandleEffect(create, ref) {
  if (typeof ref === 'function') {
    var refCallback = ref;

    var _inst = create();

    refCallback(_inst);
    return function () {
      refCallback(null);
    };
  } else if (ref !== null && ref !== undefined) {
    var refObject = ref;

    {
      if (!refObject.hasOwnProperty('current')) {
        error('Expected useImperativeHandle() first argument to either be a ' + 'ref callback or React.createRef() object. Instead received: %s.', 'an object with keys {' + Object.keys(refObject).join(', ') + '}');
      }
    }

    var _inst2 = create();

    refObject.current = _inst2;
    return function () {
      refObject.current = null;
    };
  }
}

function mountImperativeHandle(ref, create, deps) {
  {
    if (typeof create !== 'function') {
      error('Expected useImperativeHandle() second argument to be a function ' + 'that creates a handle. Instead received: %s.', create !== null ? typeof create : 'null');
    }
  } // TODO: If deps are provided, should we skip comparing the ref itself?


  var effectDeps = deps !== null && deps !== undefined ? deps.concat([ref]) : null;
  return mountEffectImpl(Update, Layout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
}

function updateImperativeHandle(ref, create, deps) {
  {
    if (typeof create !== 'function') {
      error('Expected useImperativeHandle() second argument to be a function ' + 'that creates a handle. Instead received: %s.', create !== null ? typeof create : 'null');
    }
  } // TODO: If deps are provided, should we skip comparing the ref itself?


  var effectDeps = deps !== null && deps !== undefined ? deps.concat([ref]) : null;
  return updateEffectImpl(Update, Layout, imperativeHandleEffect.bind(null, create, ref), effectDeps);
}

function mountDebugValue(value, formatterFn) {// This hook is normally a no-op.
  // The react-debug-hooks package injects its own implementation
  // so that e.g. DevTools can display custom hook values.
}

var updateDebugValue = mountDebugValue;

function mountCallback(callback, deps) {
  var hook = mountWorkInProgressHook();
  var nextDeps = deps === undefined ? null : deps;
  hook.memoizedState = [callback, nextDeps];
  return callback;
}

function updateCallback(callback, deps) {
  var hook = updateWorkInProgressHook();
  var nextDeps = deps === undefined ? null : deps;
  var prevState = hook.memoizedState;

  if (prevState !== null) {
    if (nextDeps !== null) {
      var prevDeps = prevState[1];

      if (areHookInputsEqual(nextDeps, prevDeps)) {
        return prevState[0];
      }
    }
  }

  hook.memoizedState = [callback, nextDeps];
  return callback;
}

function mountMemo(nextCreate, deps) {
  var hook = mountWorkInProgressHook();
  var nextDeps = deps === undefined ? null : deps;
  var nextValue = nextCreate();
  hook.memoizedState = [nextValue, nextDeps];
  return nextValue;
}

function updateMemo(nextCreate, deps) {
  var hook = updateWorkInProgressHook();
  var nextDeps = deps === undefined ? null : deps;
  var prevState = hook.memoizedState;

  if (prevState !== null) {
    // Assume these are defined. If they're not, areHookInputsEqual will warn.
    if (nextDeps !== null) {
      var prevDeps = prevState[1];

      if (areHookInputsEqual(nextDeps, prevDeps)) {
        return prevState[0];
      }
    }
  }

  var nextValue = nextCreate();
  hook.memoizedState = [nextValue, nextDeps];
  return nextValue;
}

function mountDeferredValue(value, config) {
  var _mountState = mountState(value),
      prevValue = _mountState[0],
      setValue = _mountState[1];

  mountEffect(function () {
    var previousConfig = ReactCurrentBatchConfig$1.suspense;
    ReactCurrentBatchConfig$1.suspense = config === undefined ? null : config;

    try {
      setValue(value);
    } finally {
      ReactCurrentBatchConfig$1.suspense = previousConfig;
    }
  }, [value, config]);
  return prevValue;
}

function updateDeferredValue(value, config) {
  var _updateState = updateState(),
      prevValue = _updateState[0],
      setValue = _updateState[1];

  updateEffect(function () {
    var previousConfig = ReactCurrentBatchConfig$1.suspense;
    ReactCurrentBatchConfig$1.suspense = config === undefined ? null : config;

    try {
      setValue(value);
    } finally {
      ReactCurrentBatchConfig$1.suspense = previousConfig;
    }
  }, [value, config]);
  return prevValue;
}

function rerenderDeferredValue(value, config) {
  var _rerenderState = rerenderState(),
      prevValue = _rerenderState[0],
      setValue = _rerenderState[1];

  updateEffect(function () {
    var previousConfig = ReactCurrentBatchConfig$1.suspense;
    ReactCurrentBatchConfig$1.suspense = config === undefined ? null : config;

    try {
      setValue(value);
    } finally {
      ReactCurrentBatchConfig$1.suspense = previousConfig;
    }
  }, [value, config]);
  return prevValue;
}

function startTransition(setPending, config, callback) {
  var priorityLevel = getCurrentPriorityLevel();
  runWithPriority$1(priorityLevel < UserBlockingPriority$1 ? UserBlockingPriority$1 : priorityLevel, function () {
    setPending(true);
  });
  runWithPriority$1(priorityLevel > NormalPriority ? NormalPriority : priorityLevel, function () {
    var previousConfig = ReactCurrentBatchConfig$1.suspense;
    ReactCurrentBatchConfig$1.suspense = config === undefined ? null : config;

    try {
      setPending(false);
      callback();
    } finally {
      ReactCurrentBatchConfig$1.suspense = previousConfig;
    }
  });
}

function mountTransition(config) {
  var _mountState2 = mountState(false),
      isPending = _mountState2[0],
      setPending = _mountState2[1];

  var start = mountCallback(startTransition.bind(null, setPending, config), [setPending, config]);
  return [start, isPending];
}

function updateTransition(config) {
  var _updateState2 = updateState(),
      isPending = _updateState2[0],
      setPending = _updateState2[1];

  var start = updateCallback(startTransition.bind(null, setPending, config), [setPending, config]);
  return [start, isPending];
}

function rerenderTransition(config) {
  var _rerenderState2 = rerenderState(),
      isPending = _rerenderState2[0],
      setPending = _rerenderState2[1];

  var start = updateCallback(startTransition.bind(null, setPending, config), [setPending, config]);
  return [start, isPending];
}

function dispatchAction(fiber, queue, action) {
  {
    if (typeof arguments[3] === 'function') {
      error("State updates from the useState() and useReducer() Hooks don't support the " + 'second callback argument. To execute a side effect after ' + 'rendering, declare it in the component body with useEffect().');
    }
  }

  var currentTime = requestCurrentTimeForUpdate();
  var suspenseConfig = requestCurrentSuspenseConfig();
  var expirationTime = computeExpirationForFiber(currentTime, fiber, suspenseConfig);
  var update = {
    expirationTime: expirationTime,
    suspenseConfig: suspenseConfig,
    action: action,
    eagerReducer: null,
    eagerState: null,
    next: null
  };

  {
    update.priority = getCurrentPriorityLevel();
  } // Append the update to the end of the list.


  var pending = queue.pending;

  if (pending === null) {
    // This is the first update. Create a circular list.
    update.next = update;
  } else {
    update.next = pending.next;
    pending.next = update;
  }

  queue.pending = update;
  var alternate = fiber.alternate;

  if (fiber === currentlyRenderingFiber$1 || alternate !== null && alternate === currentlyRenderingFiber$1) {
    // This is a render phase update. Stash it in a lazily-created map of
    // queue -> linked list of updates. After this render pass, we'll restart
    // and apply the stashed updates on top of the work-in-progress hook.
    didScheduleRenderPhaseUpdate = true;
    update.expirationTime = renderExpirationTime;
    currentlyRenderingFiber$1.expirationTime = renderExpirationTime;
  } else {
    if (fiber.expirationTime === NoWork && (alternate === null || alternate.expirationTime === NoWork)) {
      // The queue is currently empty, which means we can eagerly compute the
      // next state before entering the render phase. If the new state is the
      // same as the current state, we may be able to bail out entirely.
      var lastRenderedReducer = queue.lastRenderedReducer;

      if (lastRenderedReducer !== null) {
        var prevDispatcher;

        {
          prevDispatcher = ReactCurrentDispatcher.current;
          ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;
        }

        try {
          var currentState = queue.lastRenderedState;
          var eagerState = lastRenderedReducer(currentState, action); // Stash the eagerly computed state, and the reducer used to compute
          // it, on the update object. If the reducer hasn't changed by the
          // time we enter the render phase, then the eager state can be used
          // without calling the reducer again.

          update.eagerReducer = lastRenderedReducer;
          update.eagerState = eagerState;

          if (objectIs(eagerState, currentState)) {
            // Fast path. We can bail out without scheduling React to re-render.
            // It's still possible that we'll need to rebase this update later,
            // if the component re-renders for a different reason and by that
            // time the reducer has changed.
            return;
          }
        } catch (error) {// Suppress the error. It will throw again in the render phase.
        } finally {
          {
            ReactCurrentDispatcher.current = prevDispatcher;
          }
        }
      }
    }

    {
      // $FlowExpectedError - jest isn't a global, and isn't recognized outside of tests
      if ('undefined' !== typeof jest) {
        warnIfNotScopedWithMatchingAct(fiber);
        warnIfNotCurrentlyActingUpdatesInDev(fiber);
      }
    }

    scheduleWork(fiber, expirationTime);
  }
}

var ContextOnlyDispatcher = {
  readContext: readContext,
  useCallback: throwInvalidHookError,
  useContext: throwInvalidHookError,
  useEffect: throwInvalidHookError,
  useImperativeHandle: throwInvalidHookError,
  useLayoutEffect: throwInvalidHookError,
  useMemo: throwInvalidHookError,
  useReducer: throwInvalidHookError,
  useRef: throwInvalidHookError,
  useState: throwInvalidHookError,
  useDebugValue: throwInvalidHookError,
  useResponder: throwInvalidHookError,
  useDeferredValue: throwInvalidHookError,
  useTransition: throwInvalidHookError
};
var HooksDispatcherOnMountInDEV = null;
var HooksDispatcherOnMountWithHookTypesInDEV = null;
var HooksDispatcherOnUpdateInDEV = null;
var HooksDispatcherOnRerenderInDEV = null;
var InvalidNestedHooksDispatcherOnMountInDEV = null;
var InvalidNestedHooksDispatcherOnUpdateInDEV = null;
var InvalidNestedHooksDispatcherOnRerenderInDEV = null;

{
  var warnInvalidContextAccess = function () {
    error('Context can only be read while React is rendering. ' + 'In classes, you can read it in the render method or getDerivedStateFromProps. ' + 'In function components, you can read it directly in the function body, but not ' + 'inside Hooks like useReducer() or useMemo().');
  };

  var warnInvalidHookAccess = function () {
    error('Do not call Hooks inside useEffect(...), useMemo(...), or other built-in Hooks. ' + 'You can only call Hooks at the top level of your React function. ' + 'For more information, see ' + 'https://fb.me/rules-of-hooks');
  };

  HooksDispatcherOnMountInDEV = {
    readContext: function (context, observedBits) {
      return readContext(context, observedBits);
    },
    useCallback: function (callback, deps) {
      currentHookNameInDev = 'useCallback';
      mountHookTypesDev();
      checkDepsAreArrayDev(deps);
      return mountCallback(callback, deps);
    },
    useContext: function (context, observedBits) {
      currentHookNameInDev = 'useContext';
      mountHookTypesDev();
      return readContext(context, observedBits);
    },
    useEffect: function (create, deps) {
      currentHookNameInDev = 'useEffect';
      mountHookTypesDev();
      checkDepsAreArrayDev(deps);
      return mountEffect(create, deps);
    },
    useImperativeHandle: function (ref, create, deps) {
      currentHookNameInDev = 'useImperativeHandle';
      mountHookTypesDev();
      checkDepsAreArrayDev(deps);
      return mountImperativeHandle(ref, create, deps);
    },
    useLayoutEffect: function (create, deps) {
      currentHookNameInDev = 'useLayoutEffect';
      mountHookTypesDev();
      checkDepsAreArrayDev(deps);
      return mountLayoutEffect(create, deps);
    },
    useMemo: function (create, deps) {
      currentHookNameInDev = 'useMemo';
      mountHookTypesDev();
      checkDepsAreArrayDev(deps);
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountMemo(create, deps);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useReducer: function (reducer, initialArg, init) {
      currentHookNameInDev = 'useReducer';
      mountHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountReducer(reducer, initialArg, init);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useRef: function (initialValue) {
      currentHookNameInDev = 'useRef';
      mountHookTypesDev();
      return mountRef(initialValue);
    },
    useState: function (initialState) {
      currentHookNameInDev = 'useState';
      mountHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountState(initialState);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useDebugValue: function (value, formatterFn) {
      currentHookNameInDev = 'useDebugValue';
      mountHookTypesDev();
      return mountDebugValue();
    },
    useResponder: function (responder, props) {
      currentHookNameInDev = 'useResponder';
      mountHookTypesDev();
      return createDeprecatedResponderListener(responder, props);
    },
    useDeferredValue: function (value, config) {
      currentHookNameInDev = 'useDeferredValue';
      mountHookTypesDev();
      return mountDeferredValue(value, config);
    },
    useTransition: function (config) {
      currentHookNameInDev = 'useTransition';
      mountHookTypesDev();
      return mountTransition(config);
    }
  };
  HooksDispatcherOnMountWithHookTypesInDEV = {
    readContext: function (context, observedBits) {
      return readContext(context, observedBits);
    },
    useCallback: function (callback, deps) {
      currentHookNameInDev = 'useCallback';
      updateHookTypesDev();
      return mountCallback(callback, deps);
    },
    useContext: function (context, observedBits) {
      currentHookNameInDev = 'useContext';
      updateHookTypesDev();
      return readContext(context, observedBits);
    },
    useEffect: function (create, deps) {
      currentHookNameInDev = 'useEffect';
      updateHookTypesDev();
      return mountEffect(create, deps);
    },
    useImperativeHandle: function (ref, create, deps) {
      currentHookNameInDev = 'useImperativeHandle';
      updateHookTypesDev();
      return mountImperativeHandle(ref, create, deps);
    },
    useLayoutEffect: function (create, deps) {
      currentHookNameInDev = 'useLayoutEffect';
      updateHookTypesDev();
      return mountLayoutEffect(create, deps);
    },
    useMemo: function (create, deps) {
      currentHookNameInDev = 'useMemo';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountMemo(create, deps);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useReducer: function (reducer, initialArg, init) {
      currentHookNameInDev = 'useReducer';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountReducer(reducer, initialArg, init);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useRef: function (initialValue) {
      currentHookNameInDev = 'useRef';
      updateHookTypesDev();
      return mountRef(initialValue);
    },
    useState: function (initialState) {
      currentHookNameInDev = 'useState';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountState(initialState);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useDebugValue: function (value, formatterFn) {
      currentHookNameInDev = 'useDebugValue';
      updateHookTypesDev();
      return mountDebugValue();
    },
    useResponder: function (responder, props) {
      currentHookNameInDev = 'useResponder';
      updateHookTypesDev();
      return createDeprecatedResponderListener(responder, props);
    },
    useDeferredValue: function (value, config) {
      currentHookNameInDev = 'useDeferredValue';
      updateHookTypesDev();
      return mountDeferredValue(value, config);
    },
    useTransition: function (config) {
      currentHookNameInDev = 'useTransition';
      updateHookTypesDev();
      return mountTransition(config);
    }
  };
  HooksDispatcherOnUpdateInDEV = {
    readContext: function (context, observedBits) {
      return readContext(context, observedBits);
    },
    useCallback: function (callback, deps) {
      currentHookNameInDev = 'useCallback';
      updateHookTypesDev();
      return updateCallback(callback, deps);
    },
    useContext: function (context, observedBits) {
      currentHookNameInDev = 'useContext';
      updateHookTypesDev();
      return readContext(context, observedBits);
    },
    useEffect: function (create, deps) {
      currentHookNameInDev = 'useEffect';
      updateHookTypesDev();
      return updateEffect(create, deps);
    },
    useImperativeHandle: function (ref, create, deps) {
      currentHookNameInDev = 'useImperativeHandle';
      updateHookTypesDev();
      return updateImperativeHandle(ref, create, deps);
    },
    useLayoutEffect: function (create, deps) {
      currentHookNameInDev = 'useLayoutEffect';
      updateHookTypesDev();
      return updateLayoutEffect(create, deps);
    },
    useMemo: function (create, deps) {
      currentHookNameInDev = 'useMemo';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return updateMemo(create, deps);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useReducer: function (reducer, initialArg, init) {
      currentHookNameInDev = 'useReducer';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return updateReducer(reducer, initialArg, init);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useRef: function (initialValue) {
      currentHookNameInDev = 'useRef';
      updateHookTypesDev();
      return updateRef();
    },
    useState: function (initialState) {
      currentHookNameInDev = 'useState';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return updateState(initialState);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useDebugValue: function (value, formatterFn) {
      currentHookNameInDev = 'useDebugValue';
      updateHookTypesDev();
      return updateDebugValue();
    },
    useResponder: function (responder, props) {
      currentHookNameInDev = 'useResponder';
      updateHookTypesDev();
      return createDeprecatedResponderListener(responder, props);
    },
    useDeferredValue: function (value, config) {
      currentHookNameInDev = 'useDeferredValue';
      updateHookTypesDev();
      return updateDeferredValue(value, config);
    },
    useTransition: function (config) {
      currentHookNameInDev = 'useTransition';
      updateHookTypesDev();
      return updateTransition(config);
    }
  };
  HooksDispatcherOnRerenderInDEV = {
    readContext: function (context, observedBits) {
      return readContext(context, observedBits);
    },
    useCallback: function (callback, deps) {
      currentHookNameInDev = 'useCallback';
      updateHookTypesDev();
      return updateCallback(callback, deps);
    },
    useContext: function (context, observedBits) {
      currentHookNameInDev = 'useContext';
      updateHookTypesDev();
      return readContext(context, observedBits);
    },
    useEffect: function (create, deps) {
      currentHookNameInDev = 'useEffect';
      updateHookTypesDev();
      return updateEffect(create, deps);
    },
    useImperativeHandle: function (ref, create, deps) {
      currentHookNameInDev = 'useImperativeHandle';
      updateHookTypesDev();
      return updateImperativeHandle(ref, create, deps);
    },
    useLayoutEffect: function (create, deps) {
      currentHookNameInDev = 'useLayoutEffect';
      updateHookTypesDev();
      return updateLayoutEffect(create, deps);
    },
    useMemo: function (create, deps) {
      currentHookNameInDev = 'useMemo';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnRerenderInDEV;

      try {
        return updateMemo(create, deps);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useReducer: function (reducer, initialArg, init) {
      currentHookNameInDev = 'useReducer';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnRerenderInDEV;

      try {
        return rerenderReducer(reducer, initialArg, init);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useRef: function (initialValue) {
      currentHookNameInDev = 'useRef';
      updateHookTypesDev();
      return updateRef();
    },
    useState: function (initialState) {
      currentHookNameInDev = 'useState';
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnRerenderInDEV;

      try {
        return rerenderState(initialState);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useDebugValue: function (value, formatterFn) {
      currentHookNameInDev = 'useDebugValue';
      updateHookTypesDev();
      return updateDebugValue();
    },
    useResponder: function (responder, props) {
      currentHookNameInDev = 'useResponder';
      updateHookTypesDev();
      return createDeprecatedResponderListener(responder, props);
    },
    useDeferredValue: function (value, config) {
      currentHookNameInDev = 'useDeferredValue';
      updateHookTypesDev();
      return rerenderDeferredValue(value, config);
    },
    useTransition: function (config) {
      currentHookNameInDev = 'useTransition';
      updateHookTypesDev();
      return rerenderTransition(config);
    }
  };
  InvalidNestedHooksDispatcherOnMountInDEV = {
    readContext: function (context, observedBits) {
      warnInvalidContextAccess();
      return readContext(context, observedBits);
    },
    useCallback: function (callback, deps) {
      currentHookNameInDev = 'useCallback';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return mountCallback(callback, deps);
    },
    useContext: function (context, observedBits) {
      currentHookNameInDev = 'useContext';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return readContext(context, observedBits);
    },
    useEffect: function (create, deps) {
      currentHookNameInDev = 'useEffect';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return mountEffect(create, deps);
    },
    useImperativeHandle: function (ref, create, deps) {
      currentHookNameInDev = 'useImperativeHandle';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return mountImperativeHandle(ref, create, deps);
    },
    useLayoutEffect: function (create, deps) {
      currentHookNameInDev = 'useLayoutEffect';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return mountLayoutEffect(create, deps);
    },
    useMemo: function (create, deps) {
      currentHookNameInDev = 'useMemo';
      warnInvalidHookAccess();
      mountHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountMemo(create, deps);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useReducer: function (reducer, initialArg, init) {
      currentHookNameInDev = 'useReducer';
      warnInvalidHookAccess();
      mountHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountReducer(reducer, initialArg, init);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useRef: function (initialValue) {
      currentHookNameInDev = 'useRef';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return mountRef(initialValue);
    },
    useState: function (initialState) {
      currentHookNameInDev = 'useState';
      warnInvalidHookAccess();
      mountHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnMountInDEV;

      try {
        return mountState(initialState);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useDebugValue: function (value, formatterFn) {
      currentHookNameInDev = 'useDebugValue';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return mountDebugValue();
    },
    useResponder: function (responder, props) {
      currentHookNameInDev = 'useResponder';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return createDeprecatedResponderListener(responder, props);
    },
    useDeferredValue: function (value, config) {
      currentHookNameInDev = 'useDeferredValue';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return mountDeferredValue(value, config);
    },
    useTransition: function (config) {
      currentHookNameInDev = 'useTransition';
      warnInvalidHookAccess();
      mountHookTypesDev();
      return mountTransition(config);
    }
  };
  InvalidNestedHooksDispatcherOnUpdateInDEV = {
    readContext: function (context, observedBits) {
      warnInvalidContextAccess();
      return readContext(context, observedBits);
    },
    useCallback: function (callback, deps) {
      currentHookNameInDev = 'useCallback';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateCallback(callback, deps);
    },
    useContext: function (context, observedBits) {
      currentHookNameInDev = 'useContext';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return readContext(context, observedBits);
    },
    useEffect: function (create, deps) {
      currentHookNameInDev = 'useEffect';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateEffect(create, deps);
    },
    useImperativeHandle: function (ref, create, deps) {
      currentHookNameInDev = 'useImperativeHandle';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateImperativeHandle(ref, create, deps);
    },
    useLayoutEffect: function (create, deps) {
      currentHookNameInDev = 'useLayoutEffect';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateLayoutEffect(create, deps);
    },
    useMemo: function (create, deps) {
      currentHookNameInDev = 'useMemo';
      warnInvalidHookAccess();
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return updateMemo(create, deps);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useReducer: function (reducer, initialArg, init) {
      currentHookNameInDev = 'useReducer';
      warnInvalidHookAccess();
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return updateReducer(reducer, initialArg, init);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useRef: function (initialValue) {
      currentHookNameInDev = 'useRef';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateRef();
    },
    useState: function (initialState) {
      currentHookNameInDev = 'useState';
      warnInvalidHookAccess();
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return updateState(initialState);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useDebugValue: function (value, formatterFn) {
      currentHookNameInDev = 'useDebugValue';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateDebugValue();
    },
    useResponder: function (responder, props) {
      currentHookNameInDev = 'useResponder';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return createDeprecatedResponderListener(responder, props);
    },
    useDeferredValue: function (value, config) {
      currentHookNameInDev = 'useDeferredValue';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateDeferredValue(value, config);
    },
    useTransition: function (config) {
      currentHookNameInDev = 'useTransition';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateTransition(config);
    }
  };
  InvalidNestedHooksDispatcherOnRerenderInDEV = {
    readContext: function (context, observedBits) {
      warnInvalidContextAccess();
      return readContext(context, observedBits);
    },
    useCallback: function (callback, deps) {
      currentHookNameInDev = 'useCallback';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateCallback(callback, deps);
    },
    useContext: function (context, observedBits) {
      currentHookNameInDev = 'useContext';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return readContext(context, observedBits);
    },
    useEffect: function (create, deps) {
      currentHookNameInDev = 'useEffect';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateEffect(create, deps);
    },
    useImperativeHandle: function (ref, create, deps) {
      currentHookNameInDev = 'useImperativeHandle';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateImperativeHandle(ref, create, deps);
    },
    useLayoutEffect: function (create, deps) {
      currentHookNameInDev = 'useLayoutEffect';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateLayoutEffect(create, deps);
    },
    useMemo: function (create, deps) {
      currentHookNameInDev = 'useMemo';
      warnInvalidHookAccess();
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return updateMemo(create, deps);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useReducer: function (reducer, initialArg, init) {
      currentHookNameInDev = 'useReducer';
      warnInvalidHookAccess();
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return rerenderReducer(reducer, initialArg, init);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useRef: function (initialValue) {
      currentHookNameInDev = 'useRef';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateRef();
    },
    useState: function (initialState) {
      currentHookNameInDev = 'useState';
      warnInvalidHookAccess();
      updateHookTypesDev();
      var prevDispatcher = ReactCurrentDispatcher.current;
      ReactCurrentDispatcher.current = InvalidNestedHooksDispatcherOnUpdateInDEV;

      try {
        return rerenderState(initialState);
      } finally {
        ReactCurrentDispatcher.current = prevDispatcher;
      }
    },
    useDebugValue: function (value, formatterFn) {
      currentHookNameInDev = 'useDebugValue';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return updateDebugValue();
    },
    useResponder: function (responder, props) {
      currentHookNameInDev = 'useResponder';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return createDeprecatedResponderListener(responder, props);
    },
    useDeferredValue: function (value, config) {
      currentHookNameInDev = 'useDeferredValue';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return rerenderDeferredValue(value, config);
    },
    useTransition: function (config) {
      currentHookNameInDev = 'useTransition';
      warnInvalidHookAccess();
      updateHookTypesDev();
      return rerenderTransition(config);
    }
  };
}

var now$1 = Scheduler.unstable_now;
var commitTime = 0;
var profilerStartTime = -1;

function getCommitTime() {
  return commitTime;
}

function recordCommitTime() {

  commitTime = now$1();
}

function startProfilerTimer(fiber) {

  profilerStartTime = now$1();

  if (fiber.actualStartTime < 0) {
    fiber.actualStartTime = now$1();
  }
}

function stopProfilerTimerIfRunning(fiber) {

  profilerStartTime = -1;
}

function stopProfilerTimerIfRunningAndRecordDelta(fiber, overrideBaseTime) {

  if (profilerStartTime >= 0) {
    var elapsedTime = now$1() - profilerStartTime;
    fiber.actualDuration += elapsedTime;

    if (overrideBaseTime) {
      fiber.selfBaseDuration = elapsedTime;
    }

    profilerStartTime = -1;
  }
}

// This may have been an insertion or a hydration.

var hydrationParentFiber = null;
var nextHydratableInstance = null;
var isHydrating = false;

function enterHydrationState(fiber) {

  var parentInstance = fiber.stateNode.containerInfo;
  nextHydratableInstance = getFirstHydratableChild(parentInstance);
  hydrationParentFiber = fiber;
  isHydrating = true;
  return true;
}

function deleteHydratableInstance(returnFiber, instance) {
  {
    switch (returnFiber.tag) {
      case HostRoot:
        didNotHydrateContainerInstance(returnFiber.stateNode.containerInfo, instance);
        break;

      case HostComponent:
        didNotHydrateInstance(returnFiber.type, returnFiber.memoizedProps, returnFiber.stateNode, instance);
        break;
    }
  }

  var childToDelete = createFiberFromHostInstanceForDeletion();
  childToDelete.stateNode = instance;
  childToDelete.return = returnFiber;
  childToDelete.effectTag = Deletion; // This might seem like it belongs on progressedFirstDeletion. However,
  // these children are not part of the reconciliation list of children.
  // Even if we abort and rereconcile the children, that will try to hydrate
  // again and the nodes are still in the host tree so these will be
  // recreated.

  if (returnFiber.lastEffect !== null) {
    returnFiber.lastEffect.nextEffect = childToDelete;
    returnFiber.lastEffect = childToDelete;
  } else {
    returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
  }
}

function insertNonHydratedInstance(returnFiber, fiber) {
  fiber.effectTag = fiber.effectTag & ~Hydrating | Placement;

  {
    switch (returnFiber.tag) {
      case HostRoot:
        {
          var parentContainer = returnFiber.stateNode.containerInfo;

          switch (fiber.tag) {
            case HostComponent:
              var type = fiber.type;
              var props = fiber.pendingProps;
              didNotFindHydratableContainerInstance(parentContainer, type);
              break;

            case HostText:
              var text = fiber.pendingProps;
              didNotFindHydratableContainerTextInstance(parentContainer, text);
              break;
          }

          break;
        }

      case HostComponent:
        {
          var parentType = returnFiber.type;
          var parentProps = returnFiber.memoizedProps;
          var parentInstance = returnFiber.stateNode;

          switch (fiber.tag) {
            case HostComponent:
              var _type = fiber.type;
              var _props = fiber.pendingProps;
              didNotFindHydratableInstance(parentType, parentProps, parentInstance, _type);
              break;

            case HostText:
              var _text = fiber.pendingProps;
              didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, _text);
              break;

            case SuspenseComponent:
              didNotFindHydratableSuspenseInstance(parentType, parentProps);
              break;
          }

          break;
        }

      default:
        return;
    }
  }
}

function tryHydrate(fiber, nextInstance) {
  switch (fiber.tag) {
    case HostComponent:
      {
        var type = fiber.type;
        var props = fiber.pendingProps;
        var instance = canHydrateInstance(nextInstance, type);

        if (instance !== null) {
          fiber.stateNode = instance;
          return true;
        }

        return false;
      }

    case HostText:
      {
        var text = fiber.pendingProps;
        var textInstance = canHydrateTextInstance(nextInstance, text);

        if (textInstance !== null) {
          fiber.stateNode = textInstance;
          return true;
        }

        return false;
      }

    case SuspenseComponent:
      {

        return false;
      }

    default:
      return false;
  }
}

function tryToClaimNextHydratableInstance(fiber) {
  if (!isHydrating) {
    return;
  }

  var nextInstance = nextHydratableInstance;

  if (!nextInstance) {
    // Nothing to hydrate. Make it an insertion.
    insertNonHydratedInstance(hydrationParentFiber, fiber);
    isHydrating = false;
    hydrationParentFiber = fiber;
    return;
  }

  var firstAttemptedInstance = nextInstance;

  if (!tryHydrate(fiber, nextInstance)) {
    // If we can't hydrate this instance let's try the next one.
    // We use this as a heuristic. It's based on intuition and not data so it
    // might be flawed or unnecessary.
    nextInstance = getNextHydratableSibling(firstAttemptedInstance);

    if (!nextInstance || !tryHydrate(fiber, nextInstance)) {
      // Nothing to hydrate. Make it an insertion.
      insertNonHydratedInstance(hydrationParentFiber, fiber);
      isHydrating = false;
      hydrationParentFiber = fiber;
      return;
    } // We matched the next one, we'll now assume that the first one was
    // superfluous and we'll delete it. Since we can't eagerly delete it
    // we'll have to schedule a deletion. To do that, this node needs a dummy
    // fiber associated with it.


    deleteHydratableInstance(hydrationParentFiber, firstAttemptedInstance);
  }

  hydrationParentFiber = fiber;
  nextHydratableInstance = getFirstHydratableChild(nextInstance);
}

function prepareToHydrateHostInstance(fiber, rootContainerInstance, hostContext) {

  var instance = fiber.stateNode;
  var updatePayload = hydrateInstance(instance, fiber.type, fiber.memoizedProps, rootContainerInstance, hostContext, fiber); // TODO: Type this specific to this type of component.

  fiber.updateQueue = updatePayload; // If the update payload indicates that there is a change or if there
  // is a new ref we mark this as an update.

  if (updatePayload !== null) {
    return true;
  }

  return false;
}

function prepareToHydrateHostTextInstance(fiber) {

  var textInstance = fiber.stateNode;
  var textContent = fiber.memoizedProps;
  var shouldUpdate = hydrateTextInstance(textInstance, textContent, fiber);

  {
    if (shouldUpdate) {
      // We assume that prepareToHydrateHostTextInstance is called in a context where the
      // hydration parent is the parent host component of this host text.
      var returnFiber = hydrationParentFiber;

      if (returnFiber !== null) {
        switch (returnFiber.tag) {
          case HostRoot:
            {
              var parentContainer = returnFiber.stateNode.containerInfo;
              didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, textContent);
              break;
            }

          case HostComponent:
            {
              var parentType = returnFiber.type;
              var parentProps = returnFiber.memoizedProps;
              var parentInstance = returnFiber.stateNode;
              didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, textContent);
              break;
            }
        }
      }
    }
  }

  return shouldUpdate;
}

function skipPastDehydratedSuspenseInstance(fiber) {

  var suspenseState = fiber.memoizedState;
  var suspenseInstance = suspenseState !== null ? suspenseState.dehydrated : null;

  if (!suspenseInstance) {
    {
      throw Error( "Expected to have a hydrated suspense instance. This error is likely caused by a bug in React. Please file an issue." );
    }
  }

  return getNextHydratableInstanceAfterSuspenseInstance(suspenseInstance);
}

function popToNextHostParent(fiber) {
  var parent = fiber.return;

  while (parent !== null && parent.tag !== HostComponent && parent.tag !== HostRoot && parent.tag !== SuspenseComponent) {
    parent = parent.return;
  }

  hydrationParentFiber = parent;
}

function popHydrationState(fiber) {

  if (fiber !== hydrationParentFiber) {
    // We're deeper than the current hydration context, inside an inserted
    // tree.
    return false;
  }

  if (!isHydrating) {
    // If we're not currently hydrating but we're in a hydration context, then
    // we were an insertion and now need to pop up reenter hydration of our
    // siblings.
    popToNextHostParent(fiber);
    isHydrating = true;
    return false;
  }

  var type = fiber.type; // If we have any remaining hydratable nodes, we need to delete them now.
  // We only do this deeper than head and body since they tend to have random
  // other nodes in them. We also ignore components with pure text content in
  // side of them.
  // TODO: Better heuristic.

  if (fiber.tag !== HostComponent || type !== 'head' && type !== 'body' && !shouldSetTextContent(type, fiber.memoizedProps)) {
    var nextInstance = nextHydratableInstance;

    while (nextInstance) {
      deleteHydratableInstance(fiber, nextInstance);
      nextInstance = getNextHydratableSibling(nextInstance);
    }
  }

  popToNextHostParent(fiber);

  if (fiber.tag === SuspenseComponent) {
    nextHydratableInstance = skipPastDehydratedSuspenseInstance(fiber);
  } else {
    nextHydratableInstance = hydrationParentFiber ? getNextHydratableSibling(fiber.stateNode) : null;
  }

  return true;
}

function resetHydrationState() {

  hydrationParentFiber = null;
  nextHydratableInstance = null;
  isHydrating = false;
}

var ReactCurrentOwner$1 = ReactSharedInternals.ReactCurrentOwner;
var didReceiveUpdate = false;
var didWarnAboutBadClass;
var didWarnAboutModulePatternComponent;
var didWarnAboutContextTypeOnFunctionComponent;
var didWarnAboutGetDerivedStateOnFunctionComponent;
var didWarnAboutFunctionRefs;
var didWarnAboutReassigningProps;
var didWarnAboutMaxDuration;
var didWarnAboutRevealOrder;
var didWarnAboutTailOptions;

{
  didWarnAboutBadClass = {};
  didWarnAboutModulePatternComponent = {};
  didWarnAboutContextTypeOnFunctionComponent = {};
  didWarnAboutGetDerivedStateOnFunctionComponent = {};
  didWarnAboutFunctionRefs = {};
  didWarnAboutReassigningProps = false;
  didWarnAboutMaxDuration = false;
  didWarnAboutRevealOrder = {};
  didWarnAboutTailOptions = {};
}

function reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime) {
  if (current === null) {
    // If this is a fresh new component that hasn't been rendered yet, we
    // won't update its child set by applying minimal side-effects. Instead,
    // we will add them all to the child before it gets rendered. That means
    // we can optimize this reconciliation pass by not tracking side-effects.
    workInProgress.child = mountChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
  } else {
    // If the current child is the same as the work in progress, it means that
    // we haven't yet started any work on these children. Therefore, we use
    // the clone algorithm to create a copy of all the current children.
    // If we had any progressed work already, that is invalid at this point so
    // let's throw it out.
    workInProgress.child = reconcileChildFibers(workInProgress, current.child, nextChildren, renderExpirationTime);
  }
}

function forceUnmountCurrentAndReconcile(current, workInProgress, nextChildren, renderExpirationTime) {
  // This function is fork of reconcileChildren. It's used in cases where we
  // want to reconcile without matching against the existing set. This has the
  // effect of all current children being unmounted; even if the type and key
  // are the same, the old child is unmounted and a new child is created.
  //
  // To do this, we're going to go through the reconcile algorithm twice. In
  // the first pass, we schedule a deletion for all the current children by
  // passing null.
  workInProgress.child = reconcileChildFibers(workInProgress, current.child, null, renderExpirationTime); // In the second pass, we mount the new children. The trick here is that we
  // pass null in place of where we usually pass the current child set. This has
  // the effect of remounting all children regardless of whether their
  // identities match.

  workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
}

function updateForwardRef(current, workInProgress, Component, nextProps, renderExpirationTime) {
  // TODO: current can be non-null here even if the component
  // hasn't yet mounted. This happens after the first render suspends.
  // We'll need to figure out if this is fine or can cause issues.
  {
    if (workInProgress.type !== workInProgress.elementType) {
      // Lazy component props can't be validated in createElement
      // because they're only guaranteed to be resolved here.
      var innerPropTypes = Component.propTypes;

      if (innerPropTypes) {
        checkPropTypes(innerPropTypes, nextProps, // Resolved props
        'prop', getComponentName(Component), getCurrentFiberStackInDev);
      }
    }
  }

  var render = Component.render;
  var ref = workInProgress.ref; // The rest is a fork of updateFunctionComponent

  var nextChildren;
  prepareToReadContext(workInProgress, renderExpirationTime);

  {
    ReactCurrentOwner$1.current = workInProgress;
    setCurrentPhase('render');
    nextChildren = renderWithHooks(current, workInProgress, render, nextProps, ref, renderExpirationTime);

    if ( workInProgress.mode & StrictMode) {
      // Only double-render components with Hooks
      if (workInProgress.memoizedState !== null) {
        nextChildren = renderWithHooks(current, workInProgress, render, nextProps, ref, renderExpirationTime);
      }
    }

    setCurrentPhase(null);
  }

  if (current !== null && !didReceiveUpdate) {
    bailoutHooks(current, workInProgress, renderExpirationTime);
    return bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);
  } // React DevTools reads this flag.


  workInProgress.effectTag |= PerformedWork;
  reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
  return workInProgress.child;
}

function updateMemoComponent(current, workInProgress, Component, nextProps, updateExpirationTime, renderExpirationTime) {
  if (current === null) {
    var type = Component.type;

    if (isSimpleFunctionComponent(type) && Component.compare === null && // SimpleMemoComponent codepath doesn't resolve outer props either.
    Component.defaultProps === undefined) {
      var resolvedType = type;

      {
        resolvedType = resolveFunctionForHotReloading(type);
      } // If this is a plain function component without default props,
      // and with only the default shallow comparison, we upgrade it
      // to a SimpleMemoComponent to allow fast path updates.


      workInProgress.tag = SimpleMemoComponent;
      workInProgress.type = resolvedType;

      {
        validateFunctionComponentInDev(workInProgress, type);
      }

      return updateSimpleMemoComponent(current, workInProgress, resolvedType, nextProps, updateExpirationTime, renderExpirationTime);
    }

    {
      var innerPropTypes = type.propTypes;

      if (innerPropTypes) {
        // Inner memo component props aren't currently validated in createElement.
        // We could move it there, but we'd still need this for lazy code path.
        checkPropTypes(innerPropTypes, nextProps, // Resolved props
        'prop', getComponentName(type), getCurrentFiberStackInDev);
      }
    }

    var child = createFiberFromTypeAndProps(Component.type, null, nextProps, null, workInProgress.mode, renderExpirationTime);
    child.ref = workInProgress.ref;
    child.return = workInProgress;
    workInProgress.child = child;
    return child;
  }

  {
    var _type = Component.type;
    var _innerPropTypes = _type.propTypes;

    if (_innerPropTypes) {
      // Inner memo component props aren't currently validated in createElement.
      // We could move it there, but we'd still need this for lazy code path.
      checkPropTypes(_innerPropTypes, nextProps, // Resolved props
      'prop', getComponentName(_type), getCurrentFiberStackInDev);
    }
  }

  var currentChild = current.child; // This is always exactly one child

  if (updateExpirationTime < renderExpirationTime) {
    // This will be the props with resolved defaultProps,
    // unlike current.memoizedProps which will be the unresolved ones.
    var prevProps = currentChild.memoizedProps; // Default to shallow comparison

    var compare = Component.compare;
    compare = compare !== null ? compare : shallowEqual;

    if (compare(prevProps, nextProps) && current.ref === workInProgress.ref) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);
    }
  } // React DevTools reads this flag.


  workInProgress.effectTag |= PerformedWork;
  var newChild = createWorkInProgress(currentChild, nextProps);
  newChild.ref = workInProgress.ref;
  newChild.return = workInProgress;
  workInProgress.child = newChild;
  return newChild;
}

function updateSimpleMemoComponent(current, workInProgress, Component, nextProps, updateExpirationTime, renderExpirationTime) {
  // TODO: current can be non-null here even if the component
  // hasn't yet mounted. This happens when the inner render suspends.
  // We'll need to figure out if this is fine or can cause issues.
  {
    if (workInProgress.type !== workInProgress.elementType) {
      // Lazy component props can't be validated in createElement
      // because they're only guaranteed to be resolved here.
      var outerMemoType = workInProgress.elementType;

      if (outerMemoType.$$typeof === REACT_LAZY_TYPE) {
        // We warn when you define propTypes on lazy()
        // so let's just skip over it to find memo() outer wrapper.
        // Inner props for memo are validated later.
        outerMemoType = refineResolvedLazyComponent(outerMemoType);
      }

      var outerPropTypes = outerMemoType && outerMemoType.propTypes;

      if (outerPropTypes) {
        checkPropTypes(outerPropTypes, nextProps, // Resolved (SimpleMemoComponent has no defaultProps)
        'prop', getComponentName(outerMemoType), getCurrentFiberStackInDev);
      } // Inner propTypes will be validated in the function component path.

    }
  }

  if (current !== null) {
    var prevProps = current.memoizedProps;

    if (shallowEqual(prevProps, nextProps) && current.ref === workInProgress.ref && ( // Prevent bailout if the implementation changed due to hot reload.
     workInProgress.type === current.type )) {
      didReceiveUpdate = false;

      if (updateExpirationTime < renderExpirationTime) {
        // The pending update priority was cleared at the beginning of
        // beginWork. We're about to bail out, but there might be additional
        // updates at a lower priority. Usually, the priority level of the
        // remaining updates is accumlated during the evaluation of the
        // component (i.e. when processing the update queue). But since since
        // we're bailing out early *without* evaluating the component, we need
        // to account for it here, too. Reset to the value of the current fiber.
        // NOTE: This only applies to SimpleMemoComponent, not MemoComponent,
        // because a MemoComponent fiber does not have hooks or an update queue;
        // rather, it wraps around an inner component, which may or may not
        // contains hooks.
        // TODO: Move the reset at in beginWork out of the common path so that
        // this is no longer necessary.
        workInProgress.expirationTime = current.expirationTime;
        return bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);
      }
    }
  }

  return updateFunctionComponent(current, workInProgress, Component, nextProps, renderExpirationTime);
}

function updateFragment(current, workInProgress, renderExpirationTime) {
  var nextChildren = workInProgress.pendingProps;
  reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
  return workInProgress.child;
}

function updateMode(current, workInProgress, renderExpirationTime) {
  var nextChildren = workInProgress.pendingProps.children;
  reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
  return workInProgress.child;
}

function updateProfiler(current, workInProgress, renderExpirationTime) {
  {
    workInProgress.effectTag |= Update;
  }

  var nextProps = workInProgress.pendingProps;
  var nextChildren = nextProps.children;
  reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
  return workInProgress.child;
}

function markRef(current, workInProgress) {
  var ref = workInProgress.ref;

  if (current === null && ref !== null || current !== null && current.ref !== ref) {
    // Schedule a Ref effect
    workInProgress.effectTag |= Ref;
  }
}

function updateFunctionComponent(current, workInProgress, Component, nextProps, renderExpirationTime) {
  {
    if (workInProgress.type !== workInProgress.elementType) {
      // Lazy component props can't be validated in createElement
      // because they're only guaranteed to be resolved here.
      var innerPropTypes = Component.propTypes;

      if (innerPropTypes) {
        checkPropTypes(innerPropTypes, nextProps, // Resolved props
        'prop', getComponentName(Component), getCurrentFiberStackInDev);
      }
    }
  }

  var context;

  {
    var unmaskedContext = getUnmaskedContext(workInProgress, Component, true);
    context = getMaskedContext(workInProgress, unmaskedContext);
  }

  var nextChildren;
  prepareToReadContext(workInProgress, renderExpirationTime);

  {
    ReactCurrentOwner$1.current = workInProgress;
    setCurrentPhase('render');
    nextChildren = renderWithHooks(current, workInProgress, Component, nextProps, context, renderExpirationTime);

    if ( workInProgress.mode & StrictMode) {
      // Only double-render components with Hooks
      if (workInProgress.memoizedState !== null) {
        nextChildren = renderWithHooks(current, workInProgress, Component, nextProps, context, renderExpirationTime);
      }
    }

    setCurrentPhase(null);
  }

  if (current !== null && !didReceiveUpdate) {
    bailoutHooks(current, workInProgress, renderExpirationTime);
    return bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);
  } // React DevTools reads this flag.


  workInProgress.effectTag |= PerformedWork;
  reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
  return workInProgress.child;
}

function updateClassComponent(current, workInProgress, Component, nextProps, renderExpirationTime) {
  {
    if (workInProgress.type !== workInProgress.elementType) {
      // Lazy component props can't be validated in createElement
      // because they're only guaranteed to be resolved here.
      var innerPropTypes = Component.propTypes;

      if (innerPropTypes) {
        checkPropTypes(innerPropTypes, nextProps, // Resolved props
        'prop', getComponentName(Component), getCurrentFiberStackInDev);
      }
    }
  } // Push context providers early to prevent context stack mismatches.
  // During mounting we don't know the child context yet as the instance doesn't exist.
  // We will invalidate the child context in finishClassComponent() right after rendering.


  var hasContext;

  if (isContextProvider(Component)) {
    hasContext = true;
    pushContextProvider(workInProgress);
  } else {
    hasContext = false;
  }

  prepareToReadContext(workInProgress, renderExpirationTime);
  var instance = workInProgress.stateNode;
  var shouldUpdate;

  if (instance === null) {
    if (current !== null) {
      // A class component without an instance only mounts if it suspended
      // inside a non-concurrent tree, in an inconsistent state. We want to
      // treat it like a new mount, even though an empty version of it already
      // committed. Disconnect the alternate pointers.
      current.alternate = null;
      workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect

      workInProgress.effectTag |= Placement;
    } // In the initial pass we might need to construct the instance.


    constructClassInstance(workInProgress, Component, nextProps);
    mountClassInstance(workInProgress, Component, nextProps, renderExpirationTime);
    shouldUpdate = true;
  } else if (current === null) {
    // In a resume, we'll already have an instance we can reuse.
    shouldUpdate = resumeMountClassInstance(workInProgress, Component, nextProps, renderExpirationTime);
  } else {
    shouldUpdate = updateClassInstance(current, workInProgress, Component, nextProps, renderExpirationTime);
  }

  var nextUnitOfWork = finishClassComponent(current, workInProgress, Component, shouldUpdate, hasContext, renderExpirationTime);

  {
    var inst = workInProgress.stateNode;

    if (inst.props !== nextProps) {
      if (!didWarnAboutReassigningProps) {
        error('It looks like %s is reassigning its own `this.props` while rendering. ' + 'This is not supported and can lead to confusing bugs.', getComponentName(workInProgress.type) || 'a component');
      }

      didWarnAboutReassigningProps = true;
    }
  }

  return nextUnitOfWork;
}

function finishClassComponent(current, workInProgress, Component, shouldUpdate, hasContext, renderExpirationTime) {
  // Refs should update even if shouldComponentUpdate returns false
  markRef(current, workInProgress);
  var didCaptureError = (workInProgress.effectTag & DidCapture) !== NoEffect;

  if (!shouldUpdate && !didCaptureError) {
    // Context providers should defer to sCU for rendering
    if (hasContext) {
      invalidateContextProvider(workInProgress, Component, false);
    }

    return bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);
  }

  var instance = workInProgress.stateNode; // Rerender

  ReactCurrentOwner$1.current = workInProgress;
  var nextChildren;

  if (didCaptureError && typeof Component.getDerivedStateFromError !== 'function') {
    // If we captured an error, but getDerivedStateFromError is not defined,
    // unmount all the children. componentDidCatch will schedule an update to
    // re-render a fallback. This is temporary until we migrate everyone to
    // the new API.
    // TODO: Warn in a future release.
    nextChildren = null;

    {
      stopProfilerTimerIfRunning();
    }
  } else {
    {
      setCurrentPhase('render');
      nextChildren = instance.render();

      if ( workInProgress.mode & StrictMode) {
        instance.render();
      }

      setCurrentPhase(null);
    }
  } // React DevTools reads this flag.


  workInProgress.effectTag |= PerformedWork;

  if (current !== null && didCaptureError) {
    // If we're recovering from an error, reconcile without reusing any of
    // the existing children. Conceptually, the normal children and the children
    // that are shown on error are two different sets, so we shouldn't reuse
    // normal children even if their identities match.
    forceUnmountCurrentAndReconcile(current, workInProgress, nextChildren, renderExpirationTime);
  } else {
    reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
  } // Memoize state using the values we just used to render.
  // TODO: Restructure so we never read values from the instance.


  workInProgress.memoizedState = instance.state; // The context might have changed so we need to recalculate it.

  if (hasContext) {
    invalidateContextProvider(workInProgress, Component, true);
  }

  return workInProgress.child;
}

function pushHostRootContext(workInProgress) {
  var root = workInProgress.stateNode;

  if (root.pendingContext) {
    pushTopLevelContextObject(workInProgress, root.pendingContext, root.pendingContext !== root.context);
  } else if (root.context) {
    // Should always be set
    pushTopLevelContextObject(workInProgress, root.context, false);
  }

  pushHostContainer(workInProgress, root.containerInfo);
}

function updateHostRoot(current, workInProgress, renderExpirationTime) {
  pushHostRootContext(workInProgress);
  var updateQueue = workInProgress.updateQueue;

  if (!(current !== null && updateQueue !== null)) {
    {
      throw Error( "If the root does not have an updateQueue, we should have already bailed out. This error is likely caused by a bug in React. Please file an issue." );
    }
  }

  var nextProps = workInProgress.pendingProps;
  var prevState = workInProgress.memoizedState;
  var prevChildren = prevState !== null ? prevState.element : null;
  cloneUpdateQueue(current, workInProgress);
  processUpdateQueue(workInProgress, nextProps, null, renderExpirationTime);
  var nextState = workInProgress.memoizedState; // Caution: React DevTools currently depends on this property
  // being called "element".

  var nextChildren = nextState.element;

  if (nextChildren === prevChildren) {
    // If the state is the same as before, that's a bailout because we had
    // no work that expires at this time.
    resetHydrationState();
    return bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);
  }

  var root = workInProgress.stateNode;

  if (root.hydrate && enterHydrationState(workInProgress)) {
    // If we don't have any current children this might be the first pass.
    // We always try to hydrate. If this isn't a hydration pass there won't
    // be any children to hydrate which is effectively the same thing as
    // not hydrating.
    var child = mountChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
    workInProgress.child = child;
    var node = child;

    while (node) {
      // Mark each child as hydrating. This is a fast path to know whether this
      // tree is part of a hydrating tree. This is used to determine if a child
      // node has fully mounted yet, and for scheduling event replaying.
      // Conceptually this is similar to Placement in that a new subtree is
      // inserted into the React tree here. It just happens to not need DOM
      // mutations because it already exists.
      node.effectTag = node.effectTag & ~Placement | Hydrating;
      node = node.sibling;
    }
  } else {
    // Otherwise reset hydration state in case we aborted and resumed another
    // root.
    reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
    resetHydrationState();
  }

  return workInProgress.child;
}

function updateHostComponent(current, workInProgress, renderExpirationTime) {
  pushHostContext(workInProgress);

  if (current === null) {
    tryToClaimNextHydratableInstance(workInProgress);
  }

  var type = workInProgress.type;
  var nextProps = workInProgress.pendingProps;
  var prevProps = current !== null ? current.memoizedProps : null;
  var nextChildren = nextProps.children;
  var isDirectTextChild = shouldSetTextContent(type, nextProps);

  if (isDirectTextChild) {
    // We special case a direct text child of a host node. This is a common
    // case. We won't handle it as a reified child. We will instead handle
    // this in the host environment that also has access to this prop. That
    // avoids allocating another HostText fiber and traversing it.
    nextChildren = null;
  } else if (prevProps !== null && shouldSetTextContent(type, prevProps)) {
    // If we're switching from a direct text child to a normal child, or to
    // empty, we need to schedule the text content to be reset.
    workInProgress.effectTag |= ContentReset;
  }

  markRef(current, workInProgress); // Check the host config to see if the children are offscreen/hidden.

  if (workInProgress.mode & ConcurrentMode && renderExpirationTime !== Never && shouldDeprioritizeSubtree(type, nextProps)) {
    {
      markSpawnedWork(Never);
    } // Schedule this fiber to re-render at offscreen priority. Then bailout.


    workInProgress.expirationTime = workInProgress.childExpirationTime = Never;
    return null;
  }

  reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
  return workInProgress.child;
}

function updateHostText(current, workInProgress) {
  if (current === null) {
    tryToClaimNextHydratableInstance(workInProgress);
  } // Nothing to do here. This is terminal. We'll do the completion step
  // immediately after.


  return null;
}

function mountLazyComponent(_current, workInProgress, elementType, updateExpirationTime, renderExpirationTime) {
  if (_current !== null) {
    // A lazy component only mounts if it suspended inside a non-
    // concurrent tree, in an inconsistent state. We want to treat it like
    // a new mount, even though an empty version of it already committed.
    // Disconnect the alternate pointers.
    _current.alternate = null;
    workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect

    workInProgress.effectTag |= Placement;
  }

  var props = workInProgress.pendingProps; // We can't start a User Timing measurement with correct label yet.
  // Cancel and resume right after we know the tag.

  cancelWorkTimer(workInProgress);
  var Component = readLazyComponentType(elementType); // Store the unwrapped component in the type.

  workInProgress.type = Component;
  var resolvedTag = workInProgress.tag = resolveLazyComponentTag(Component);
  startWorkTimer(workInProgress);
  var resolvedProps = resolveDefaultProps(Component, props);
  var child;

  switch (resolvedTag) {
    case FunctionComponent:
      {
        {
          validateFunctionComponentInDev(workInProgress, Component);
          workInProgress.type = Component = resolveFunctionForHotReloading(Component);
        }

        child = updateFunctionComponent(null, workInProgress, Component, resolvedProps, renderExpirationTime);
        return child;
      }

    case ClassComponent:
      {
        {
          workInProgress.type = Component = resolveClassForHotReloading(Component);
        }

        child = updateClassComponent(null, workInProgress, Component, resolvedProps, renderExpirationTime);
        return child;
      }

    case ForwardRef:
      {
        {
          workInProgress.type = Component = resolveForwardRefForHotReloading(Component);
        }

        child = updateForwardRef(null, workInProgress, Component, resolvedProps, renderExpirationTime);
        return child;
      }

    case MemoComponent:
      {
        {
          if (workInProgress.type !== workInProgress.elementType) {
            var outerPropTypes = Component.propTypes;

            if (outerPropTypes) {
              checkPropTypes(outerPropTypes, resolvedProps, // Resolved for outer only
              'prop', getComponentName(Component), getCurrentFiberStackInDev);
            }
          }
        }

        child = updateMemoComponent(null, workInProgress, Component, resolveDefaultProps(Component.type, resolvedProps), // The inner type can have defaults too
        updateExpirationTime, renderExpirationTime);
        return child;
      }
  }

  var hint = '';

  {
    if (Component !== null && typeof Component === 'object' && Component.$$typeof === REACT_LAZY_TYPE) {
      hint = ' Did you wrap a component in React.lazy() more than once?';
    }
  } // This message intentionally doesn't mention ForwardRef or MemoComponent
  // because the fact that it's a separate type of work is an
  // implementation detail.


  {
    {
      throw Error( "Element type is invalid. Received a promise that resolves to: " + Component + ". Lazy element type must resolve to a class or function." + hint );
    }
  }
}

function mountIncompleteClassComponent(_current, workInProgress, Component, nextProps, renderExpirationTime) {
  if (_current !== null) {
    // An incomplete component only mounts if it suspended inside a non-
    // concurrent tree, in an inconsistent state. We want to treat it like
    // a new mount, even though an empty version of it already committed.
    // Disconnect the alternate pointers.
    _current.alternate = null;
    workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect

    workInProgress.effectTag |= Placement;
  } // Promote the fiber to a class and try rendering again.


  workInProgress.tag = ClassComponent; // The rest of this function is a fork of `updateClassComponent`
  // Push context providers early to prevent context stack mismatches.
  // During mounting we don't know the child context yet as the instance doesn't exist.
  // We will invalidate the child context in finishClassComponent() right after rendering.

  var hasContext;

  if (isContextProvider(Component)) {
    hasContext = true;
    pushContextProvider(workInProgress);
  } else {
    hasContext = false;
  }

  prepareToReadContext(workInProgress, renderExpirationTime);
  constructClassInstance(workInProgress, Component, nextProps);
  mountClassInstance(workInProgress, Component, nextProps, renderExpirationTime);
  return finishClassComponent(null, workInProgress, Component, true, hasContext, renderExpirationTime);
}

function mountIndeterminateComponent(_current, workInProgress, Component, renderExpirationTime) {
  if (_current !== null) {
    // An indeterminate component only mounts if it suspended inside a non-
    // concurrent tree, in an inconsistent state. We want to treat it like
    // a new mount, even though an empty version of it already committed.
    // Disconnect the alternate pointers.
    _current.alternate = null;
    workInProgress.alternate = null; // Since this is conceptually a new fiber, schedule a Placement effect

    workInProgress.effectTag |= Placement;
  }

  var props = workInProgress.pendingProps;
  var context;

  {
    var unmaskedContext = getUnmaskedContext(workInProgress, Component, false);
    context = getMaskedContext(workInProgress, unmaskedContext);
  }

  prepareToReadContext(workInProgress, renderExpirationTime);
  var value;

  {
    if (Component.prototype && typeof Component.prototype.render === 'function') {
      var componentName = getComponentName(Component) || 'Unknown';

      if (!didWarnAboutBadClass[componentName]) {
        error("The <%s /> component appears to have a render method, but doesn't extend React.Component. " + 'This is likely to cause errors. Change %s to extend React.Component instead.', componentName, componentName);

        didWarnAboutBadClass[componentName] = true;
      }
    }

    if (workInProgress.mode & StrictMode) {
      ReactStrictModeWarnings.recordLegacyContextWarning(workInProgress, null);
    }

    ReactCurrentOwner$1.current = workInProgress;
    value = renderWithHooks(null, workInProgress, Component, props, context, renderExpirationTime);
  } // React DevTools reads this flag.


  workInProgress.effectTag |= PerformedWork;

  if (typeof value === 'object' && value !== null && typeof value.render === 'function' && value.$$typeof === undefined) {
    {
      var _componentName = getComponentName(Component) || 'Unknown';

      if (!didWarnAboutModulePatternComponent[_componentName]) {
        error('The <%s /> component appears to be a function component that returns a class instance. ' + 'Change %s to a class that extends React.Component instead. ' + "If you can't use a class try assigning the prototype on the function as a workaround. " + "`%s.prototype = React.Component.prototype`. Don't use an arrow function since it " + 'cannot be called with `new` by React.', _componentName, _componentName, _componentName);

        didWarnAboutModulePatternComponent[_componentName] = true;
      }
    } // Proceed under the assumption that this is a class instance


    workInProgress.tag = ClassComponent; // Throw out any hooks that were used.

    workInProgress.memoizedState = null;
    workInProgress.updateQueue = null; // Push context providers early to prevent context stack mismatches.
    // During mounting we don't know the child context yet as the instance doesn't exist.
    // We will invalidate the child context in finishClassComponent() right after rendering.

    var hasContext = false;

    if (isContextProvider(Component)) {
      hasContext = true;
      pushContextProvider(workInProgress);
    } else {
      hasContext = false;
    }

    workInProgress.memoizedState = value.state !== null && value.state !== undefined ? value.state : null;
    initializeUpdateQueue(workInProgress);
    var getDerivedStateFromProps = Component.getDerivedStateFromProps;

    if (typeof getDerivedStateFromProps === 'function') {
      applyDerivedStateFromProps(workInProgress, Component, getDerivedStateFromProps, props);
    }

    adoptClassInstance(workInProgress, value);
    mountClassInstance(workInProgress, Component, props, renderExpirationTime);
    return finishClassComponent(null, workInProgress, Component, true, hasContext, renderExpirationTime);
  } else {
    // Proceed under the assumption that this is a function component
    workInProgress.tag = FunctionComponent;

    {

      if ( workInProgress.mode & StrictMode) {
        // Only double-render components with Hooks
        if (workInProgress.memoizedState !== null) {
          value = renderWithHooks(null, workInProgress, Component, props, context, renderExpirationTime);
        }
      }
    }

    reconcileChildren(null, workInProgress, value, renderExpirationTime);

    {
      validateFunctionComponentInDev(workInProgress, Component);
    }

    return workInProgress.child;
  }
}

function validateFunctionComponentInDev(workInProgress, Component) {
  {
    if (Component) {
      if (Component.childContextTypes) {
        error('%s(...): childContextTypes cannot be defined on a function component.', Component.displayName || Component.name || 'Component');
      }
    }

    if (workInProgress.ref !== null) {
      var info = '';
      var ownerName = getCurrentFiberOwnerNameInDevOrNull();

      if (ownerName) {
        info += '\n\nCheck the render method of `' + ownerName + '`.';
      }

      var warningKey = ownerName || workInProgress._debugID || '';
      var debugSource = workInProgress._debugSource;

      if (debugSource) {
        warningKey = debugSource.fileName + ':' + debugSource.lineNumber;
      }

      if (!didWarnAboutFunctionRefs[warningKey]) {
        didWarnAboutFunctionRefs[warningKey] = true;

        error('Function components cannot be given refs. ' + 'Attempts to access this ref will fail. ' + 'Did you mean to use React.forwardRef()?%s', info);
      }
    }

    if (typeof Component.getDerivedStateFromProps === 'function') {
      var _componentName2 = getComponentName(Component) || 'Unknown';

      if (!didWarnAboutGetDerivedStateOnFunctionComponent[_componentName2]) {
        error('%s: Function components do not support getDerivedStateFromProps.', _componentName2);

        didWarnAboutGetDerivedStateOnFunctionComponent[_componentName2] = true;
      }
    }

    if (typeof Component.contextType === 'object' && Component.contextType !== null) {
      var _componentName3 = getComponentName(Component) || 'Unknown';

      if (!didWarnAboutContextTypeOnFunctionComponent[_componentName3]) {
        error('%s: Function components do not support contextType.', _componentName3);

        didWarnAboutContextTypeOnFunctionComponent[_componentName3] = true;
      }
    }
  }
}

var SUSPENDED_MARKER = {
  dehydrated: null,
  retryTime: NoWork
};

function shouldRemainOnFallback(suspenseContext, current, workInProgress) {
  // If the context is telling us that we should show a fallback, and we're not
  // already showing content, then we should show the fallback instead.
  return hasSuspenseContext(suspenseContext, ForceSuspenseFallback) && (current === null || current.memoizedState !== null);
}

function updateSuspenseComponent(current, workInProgress, renderExpirationTime) {
  var mode = workInProgress.mode;
  var nextProps = workInProgress.pendingProps; // This is used by DevTools to force a boundary to suspend.

  {
    if (shouldSuspend(workInProgress)) {
      workInProgress.effectTag |= DidCapture;
    }
  }

  var suspenseContext = suspenseStackCursor.current;
  var nextDidTimeout = false;
  var didSuspend = (workInProgress.effectTag & DidCapture) !== NoEffect;

  if (didSuspend || shouldRemainOnFallback(suspenseContext, current)) {
    // Something in this boundary's subtree already suspended. Switch to
    // rendering the fallback children.
    nextDidTimeout = true;
    workInProgress.effectTag &= ~DidCapture;
  } else {
    // Attempting the main content
    if (current === null || current.memoizedState !== null) {
      // This is a new mount or this boundary is already showing a fallback state.
      // Mark this subtree context as having at least one invisible parent that could
      // handle the fallback state.
      // Boundaries without fallbacks or should be avoided are not considered since
      // they cannot handle preferred fallback states.
      if (nextProps.fallback !== undefined && nextProps.unstable_avoidThisFallback !== true) {
        suspenseContext = addSubtreeSuspenseContext(suspenseContext, InvisibleParentSuspenseContext);
      }
    }
  }

  suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
  pushSuspenseContext(workInProgress, suspenseContext);

  {
    if ('maxDuration' in nextProps) {
      if (!didWarnAboutMaxDuration) {
        didWarnAboutMaxDuration = true;

        error('maxDuration has been removed from React. ' + 'Remove the maxDuration prop.');
      }
    }
  } // This next part is a bit confusing. If the children timeout, we switch to
  // showing the fallback children in place of the "primary" children.
  // However, we don't want to delete the primary children because then their
  // state will be lost (both the React state and the host state, e.g.
  // uncontrolled form inputs). Instead we keep them mounted and hide them.
  // Both the fallback children AND the primary children are rendered at the
  // same time. Once the primary children are un-suspended, we can delete
  // the fallback children — don't need to preserve their state.
  //
  // The two sets of children are siblings in the host environment, but
  // semantically, for purposes of reconciliation, they are two separate sets.
  // So we store them using two fragment fibers.
  //
  // However, we want to avoid allocating extra fibers for every placeholder.
  // They're only necessary when the children time out, because that's the
  // only time when both sets are mounted.
  //
  // So, the extra fragment fibers are only used if the children time out.
  // Otherwise, we render the primary children directly. This requires some
  // custom reconciliation logic to preserve the state of the primary
  // children. It's essentially a very basic form of re-parenting.


  if (current === null) {
    // If we're currently hydrating, try to hydrate this boundary.
    // But only if this has a fallback.
    if (nextProps.fallback !== undefined) {
      tryToClaimNextHydratableInstance(workInProgress); // This could've been a dehydrated suspense component.
    } // This is the initial mount. This branch is pretty simple because there's
    // no previous state that needs to be preserved.


    if (nextDidTimeout) {
      // Mount separate fragments for primary and fallback children.
      var nextFallbackChildren = nextProps.fallback;
      var primaryChildFragment = createFiberFromFragment(null, mode, NoWork, null);
      primaryChildFragment.return = workInProgress;

      if ((workInProgress.mode & BlockingMode) === NoMode) {
        // Outside of blocking mode, we commit the effects from the
        // partially completed, timed-out tree, too.
        var progressedState = workInProgress.memoizedState;
        var progressedPrimaryChild = progressedState !== null ? workInProgress.child.child : workInProgress.child;
        primaryChildFragment.child = progressedPrimaryChild;
        var progressedChild = progressedPrimaryChild;

        while (progressedChild !== null) {
          progressedChild.return = primaryChildFragment;
          progressedChild = progressedChild.sibling;
        }
      }

      var fallbackChildFragment = createFiberFromFragment(nextFallbackChildren, mode, renderExpirationTime, null);
      fallbackChildFragment.return = workInProgress;
      primaryChildFragment.sibling = fallbackChildFragment; // Skip the primary children, and continue working on the
      // fallback children.

      workInProgress.memoizedState = SUSPENDED_MARKER;
      workInProgress.child = primaryChildFragment;
      return fallbackChildFragment;
    } else {
      // Mount the primary children without an intermediate fragment fiber.
      var nextPrimaryChildren = nextProps.children;
      workInProgress.memoizedState = null;
      return workInProgress.child = mountChildFibers(workInProgress, null, nextPrimaryChildren, renderExpirationTime);
    }
  } else {
    // This is an update. This branch is more complicated because we need to
    // ensure the state of the primary children is preserved.
    var prevState = current.memoizedState;

    if (prevState !== null) {
      // wrapped in a fragment fiber.


      var currentPrimaryChildFragment = current.child;
      var currentFallbackChildFragment = currentPrimaryChildFragment.sibling;

      if (nextDidTimeout) {
        // Still timed out. Reuse the current primary children by cloning
        // its fragment. We're going to skip over these entirely.
        var _nextFallbackChildren2 = nextProps.fallback;

        var _primaryChildFragment2 = createWorkInProgress(currentPrimaryChildFragment, currentPrimaryChildFragment.pendingProps);

        _primaryChildFragment2.return = workInProgress;

        if ((workInProgress.mode & BlockingMode) === NoMode) {
          // Outside of blocking mode, we commit the effects from the
          // partially completed, timed-out tree, too.
          var _progressedState = workInProgress.memoizedState;

          var _progressedPrimaryChild = _progressedState !== null ? workInProgress.child.child : workInProgress.child;

          if (_progressedPrimaryChild !== currentPrimaryChildFragment.child) {
            _primaryChildFragment2.child = _progressedPrimaryChild;
            var _progressedChild2 = _progressedPrimaryChild;

            while (_progressedChild2 !== null) {
              _progressedChild2.return = _primaryChildFragment2;
              _progressedChild2 = _progressedChild2.sibling;
            }
          }
        } // Because primaryChildFragment is a new fiber that we're inserting as the
        // parent of a new tree, we need to set its treeBaseDuration.


        if ( workInProgress.mode & ProfileMode) {
          // treeBaseDuration is the sum of all the child tree base durations.
          var _treeBaseDuration = 0;
          var _hiddenChild = _primaryChildFragment2.child;

          while (_hiddenChild !== null) {
            _treeBaseDuration += _hiddenChild.treeBaseDuration;
            _hiddenChild = _hiddenChild.sibling;
          }

          _primaryChildFragment2.treeBaseDuration = _treeBaseDuration;
        } // Clone the fallback child fragment, too. These we'll continue
        // working on.


        var _fallbackChildFragment2 = createWorkInProgress(currentFallbackChildFragment, _nextFallbackChildren2);

        _fallbackChildFragment2.return = workInProgress;
        _primaryChildFragment2.sibling = _fallbackChildFragment2;
        _primaryChildFragment2.childExpirationTime = NoWork; // Skip the primary children, and continue working on the
        // fallback children.

        workInProgress.memoizedState = SUSPENDED_MARKER;
        workInProgress.child = _primaryChildFragment2;
        return _fallbackChildFragment2;
      } else {
        // No longer suspended. Switch back to showing the primary children,
        // and remove the intermediate fragment fiber.
        var _nextPrimaryChildren = nextProps.children;
        var currentPrimaryChild = currentPrimaryChildFragment.child;
        var primaryChild = reconcileChildFibers(workInProgress, currentPrimaryChild, _nextPrimaryChildren, renderExpirationTime); // If this render doesn't suspend, we need to delete the fallback
        // children. Wait until the complete phase, after we've confirmed the
        // fallback is no longer needed.
        // TODO: Would it be better to store the fallback fragment on
        // the stateNode?
        // Continue rendering the children, like we normally do.

        workInProgress.memoizedState = null;
        return workInProgress.child = primaryChild;
      }
    } else {
      // The current tree has not already timed out. That means the primary
      // children are not wrapped in a fragment fiber.
      var _currentPrimaryChild = current.child;

      if (nextDidTimeout) {
        // Timed out. Wrap the children in a fragment fiber to keep them
        // separate from the fallback children.
        var _nextFallbackChildren3 = nextProps.fallback;

        var _primaryChildFragment3 = createFiberFromFragment( // It shouldn't matter what the pending props are because we aren't
        // going to render this fragment.
        null, mode, NoWork, null);

        _primaryChildFragment3.return = workInProgress;
        _primaryChildFragment3.child = _currentPrimaryChild;

        if (_currentPrimaryChild !== null) {
          _currentPrimaryChild.return = _primaryChildFragment3;
        } // Even though we're creating a new fiber, there are no new children,
        // because we're reusing an already mounted tree. So we don't need to
        // schedule a placement.
        // primaryChildFragment.effectTag |= Placement;


        if ((workInProgress.mode & BlockingMode) === NoMode) {
          // Outside of blocking mode, we commit the effects from the
          // partially completed, timed-out tree, too.
          var _progressedState2 = workInProgress.memoizedState;

          var _progressedPrimaryChild2 = _progressedState2 !== null ? workInProgress.child.child : workInProgress.child;

          _primaryChildFragment3.child = _progressedPrimaryChild2;
          var _progressedChild3 = _progressedPrimaryChild2;

          while (_progressedChild3 !== null) {
            _progressedChild3.return = _primaryChildFragment3;
            _progressedChild3 = _progressedChild3.sibling;
          }
        } // Because primaryChildFragment is a new fiber that we're inserting as the
        // parent of a new tree, we need to set its treeBaseDuration.


        if ( workInProgress.mode & ProfileMode) {
          // treeBaseDuration is the sum of all the child tree base durations.
          var _treeBaseDuration2 = 0;
          var _hiddenChild2 = _primaryChildFragment3.child;

          while (_hiddenChild2 !== null) {
            _treeBaseDuration2 += _hiddenChild2.treeBaseDuration;
            _hiddenChild2 = _hiddenChild2.sibling;
          }

          _primaryChildFragment3.treeBaseDuration = _treeBaseDuration2;
        } // Create a fragment from the fallback children, too.


        var _fallbackChildFragment3 = createFiberFromFragment(_nextFallbackChildren3, mode, renderExpirationTime, null);

        _fallbackChildFragment3.return = workInProgress;
        _primaryChildFragment3.sibling = _fallbackChildFragment3;
        _fallbackChildFragment3.effectTag |= Placement;
        _primaryChildFragment3.childExpirationTime = NoWork; // Skip the primary children, and continue working on the
        // fallback children.

        workInProgress.memoizedState = SUSPENDED_MARKER;
        workInProgress.child = _primaryChildFragment3;
        return _fallbackChildFragment3;
      } else {
        // Still haven't timed out. Continue rendering the children, like we
        // normally do.
        workInProgress.memoizedState = null;
        var _nextPrimaryChildren2 = nextProps.children;
        return workInProgress.child = reconcileChildFibers(workInProgress, _currentPrimaryChild, _nextPrimaryChildren2, renderExpirationTime);
      }
    }
  }
}

function scheduleWorkOnFiber(fiber, renderExpirationTime) {
  if (fiber.expirationTime < renderExpirationTime) {
    fiber.expirationTime = renderExpirationTime;
  }

  var alternate = fiber.alternate;

  if (alternate !== null && alternate.expirationTime < renderExpirationTime) {
    alternate.expirationTime = renderExpirationTime;
  }

  scheduleWorkOnParentPath(fiber.return, renderExpirationTime);
}

function propagateSuspenseContextChange(workInProgress, firstChild, renderExpirationTime) {
  // Mark any Suspense boundaries with fallbacks as having work to do.
  // If they were previously forced into fallbacks, they may now be able
  // to unblock.
  var node = firstChild;

  while (node !== null) {
    if (node.tag === SuspenseComponent) {
      var state = node.memoizedState;

      if (state !== null) {
        scheduleWorkOnFiber(node, renderExpirationTime);
      }
    } else if (node.tag === SuspenseListComponent) {
      // If the tail is hidden there might not be an Suspense boundaries
      // to schedule work on. In this case we have to schedule it on the
      // list itself.
      // We don't have to traverse to the children of the list since
      // the list will propagate the change when it rerenders.
      scheduleWorkOnFiber(node, renderExpirationTime);
    } else if (node.child !== null) {
      node.child.return = node;
      node = node.child;
      continue;
    }

    if (node === workInProgress) {
      return;
    }

    while (node.sibling === null) {
      if (node.return === null || node.return === workInProgress) {
        return;
      }

      node = node.return;
    }

    node.sibling.return = node.return;
    node = node.sibling;
  }
}

function findLastContentRow(firstChild) {
  // This is going to find the last row among these children that is already
  // showing content on the screen, as opposed to being in fallback state or
  // new. If a row has multiple Suspense boundaries, any of them being in the
  // fallback state, counts as the whole row being in a fallback state.
  // Note that the "rows" will be workInProgress, but any nested children
  // will still be current since we haven't rendered them yet. The mounted
  // order may not be the same as the new order. We use the new order.
  var row = firstChild;
  var lastContentRow = null;

  while (row !== null) {
    var currentRow = row.alternate; // New rows can't be content rows.

    if (currentRow !== null && findFirstSuspended(currentRow) === null) {
      lastContentRow = row;
    }

    row = row.sibling;
  }

  return lastContentRow;
}

function validateRevealOrder(revealOrder) {
  {
    if (revealOrder !== undefined && revealOrder !== 'forwards' && revealOrder !== 'backwards' && revealOrder !== 'together' && !didWarnAboutRevealOrder[revealOrder]) {
      didWarnAboutRevealOrder[revealOrder] = true;

      if (typeof revealOrder === 'string') {
        switch (revealOrder.toLowerCase()) {
          case 'together':
          case 'forwards':
          case 'backwards':
            {
              error('"%s" is not a valid value for revealOrder on <SuspenseList />. ' + 'Use lowercase "%s" instead.', revealOrder, revealOrder.toLowerCase());

              break;
            }

          case 'forward':
          case 'backward':
            {
              error('"%s" is not a valid value for revealOrder on <SuspenseList />. ' + 'React uses the -s suffix in the spelling. Use "%ss" instead.', revealOrder, revealOrder.toLowerCase());

              break;
            }

          default:
            error('"%s" is not a supported revealOrder on <SuspenseList />. ' + 'Did you mean "together", "forwards" or "backwards"?', revealOrder);

            break;
        }
      } else {
        error('%s is not a supported value for revealOrder on <SuspenseList />. ' + 'Did you mean "together", "forwards" or "backwards"?', revealOrder);
      }
    }
  }
}

function validateTailOptions(tailMode, revealOrder) {
  {
    if (tailMode !== undefined && !didWarnAboutTailOptions[tailMode]) {
      if (tailMode !== 'collapsed' && tailMode !== 'hidden') {
        didWarnAboutTailOptions[tailMode] = true;

        error('"%s" is not a supported value for tail on <SuspenseList />. ' + 'Did you mean "collapsed" or "hidden"?', tailMode);
      } else if (revealOrder !== 'forwards' && revealOrder !== 'backwards') {
        didWarnAboutTailOptions[tailMode] = true;

        error('<SuspenseList tail="%s" /> is only valid if revealOrder is ' + '"forwards" or "backwards". ' + 'Did you mean to specify revealOrder="forwards"?', tailMode);
      }
    }
  }
}

function validateSuspenseListNestedChild(childSlot, index) {
  {
    var isArray = Array.isArray(childSlot);
    var isIterable = !isArray && typeof getIteratorFn(childSlot) === 'function';

    if (isArray || isIterable) {
      var type = isArray ? 'array' : 'iterable';

      error('A nested %s was passed to row #%s in <SuspenseList />. Wrap it in ' + 'an additional SuspenseList to configure its revealOrder: ' + '<SuspenseList revealOrder=...> ... ' + '<SuspenseList revealOrder=...>{%s}</SuspenseList> ... ' + '</SuspenseList>', type, index, type);

      return false;
    }
  }

  return true;
}

function validateSuspenseListChildren(children, revealOrder) {
  {
    if ((revealOrder === 'forwards' || revealOrder === 'backwards') && children !== undefined && children !== null && children !== false) {
      if (Array.isArray(children)) {
        for (var i = 0; i < children.length; i++) {
          if (!validateSuspenseListNestedChild(children[i], i)) {
            return;
          }
        }
      } else {
        var iteratorFn = getIteratorFn(children);

        if (typeof iteratorFn === 'function') {
          var childrenIterator = iteratorFn.call(children);

          if (childrenIterator) {
            var step = childrenIterator.next();
            var _i = 0;

            for (; !step.done; step = childrenIterator.next()) {
              if (!validateSuspenseListNestedChild(step.value, _i)) {
                return;
              }

              _i++;
            }
          }
        } else {
          error('A single row was passed to a <SuspenseList revealOrder="%s" />. ' + 'This is not useful since it needs multiple rows. ' + 'Did you mean to pass multiple children or an array?', revealOrder);
        }
      }
    }
  }
}

function initSuspenseListRenderState(workInProgress, isBackwards, tail, lastContentRow, tailMode, lastEffectBeforeRendering) {
  var renderState = workInProgress.memoizedState;

  if (renderState === null) {
    workInProgress.memoizedState = {
      isBackwards: isBackwards,
      rendering: null,
      renderingStartTime: 0,
      last: lastContentRow,
      tail: tail,
      tailExpiration: 0,
      tailMode: tailMode,
      lastEffect: lastEffectBeforeRendering
    };
  } else {
    // We can reuse the existing object from previous renders.
    renderState.isBackwards = isBackwards;
    renderState.rendering = null;
    renderState.renderingStartTime = 0;
    renderState.last = lastContentRow;
    renderState.tail = tail;
    renderState.tailExpiration = 0;
    renderState.tailMode = tailMode;
    renderState.lastEffect = lastEffectBeforeRendering;
  }
} // This can end up rendering this component multiple passes.
// The first pass splits the children fibers into two sets. A head and tail.
// We first render the head. If anything is in fallback state, we do another
// pass through beginWork to rerender all children (including the tail) with
// the force suspend context. If the first render didn't have anything in
// in fallback state. Then we render each row in the tail one-by-one.
// That happens in the completeWork phase without going back to beginWork.


function updateSuspenseListComponent(current, workInProgress, renderExpirationTime) {
  var nextProps = workInProgress.pendingProps;
  var revealOrder = nextProps.revealOrder;
  var tailMode = nextProps.tail;
  var newChildren = nextProps.children;
  validateRevealOrder(revealOrder);
  validateTailOptions(tailMode, revealOrder);
  validateSuspenseListChildren(newChildren, revealOrder);
  reconcileChildren(current, workInProgress, newChildren, renderExpirationTime);
  var suspenseContext = suspenseStackCursor.current;
  var shouldForceFallback = hasSuspenseContext(suspenseContext, ForceSuspenseFallback);

  if (shouldForceFallback) {
    suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
    workInProgress.effectTag |= DidCapture;
  } else {
    var didSuspendBefore = current !== null && (current.effectTag & DidCapture) !== NoEffect;

    if (didSuspendBefore) {
      // If we previously forced a fallback, we need to schedule work
      // on any nested boundaries to let them know to try to render
      // again. This is the same as context updating.
      propagateSuspenseContextChange(workInProgress, workInProgress.child, renderExpirationTime);
    }

    suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
  }

  pushSuspenseContext(workInProgress, suspenseContext);

  if ((workInProgress.mode & BlockingMode) === NoMode) {
    // Outside of blocking mode, SuspenseList doesn't work so we just
    // use make it a noop by treating it as the default revealOrder.
    workInProgress.memoizedState = null;
  } else {
    switch (revealOrder) {
      case 'forwards':
        {
          var lastContentRow = findLastContentRow(workInProgress.child);
          var tail;

          if (lastContentRow === null) {
            // The whole list is part of the tail.
            // TODO: We could fast path by just rendering the tail now.
            tail = workInProgress.child;
            workInProgress.child = null;
          } else {
            // Disconnect the tail rows after the content row.
            // We're going to render them separately later.
            tail = lastContentRow.sibling;
            lastContentRow.sibling = null;
          }

          initSuspenseListRenderState(workInProgress, false, // isBackwards
          tail, lastContentRow, tailMode, workInProgress.lastEffect);
          break;
        }

      case 'backwards':
        {
          // We're going to find the first row that has existing content.
          // At the same time we're going to reverse the list of everything
          // we pass in the meantime. That's going to be our tail in reverse
          // order.
          var _tail = null;
          var row = workInProgress.child;
          workInProgress.child = null;

          while (row !== null) {
            var currentRow = row.alternate; // New rows can't be content rows.

            if (currentRow !== null && findFirstSuspended(currentRow) === null) {
              // This is the beginning of the main content.
              workInProgress.child = row;
              break;
            }

            var nextRow = row.sibling;
            row.sibling = _tail;
            _tail = row;
            row = nextRow;
          } // TODO: If workInProgress.child is null, we can continue on the tail immediately.


          initSuspenseListRenderState(workInProgress, true, // isBackwards
          _tail, null, // last
          tailMode, workInProgress.lastEffect);
          break;
        }

      case 'together':
        {
          initSuspenseListRenderState(workInProgress, false, // isBackwards
          null, // tail
          null, // last
          undefined, workInProgress.lastEffect);
          break;
        }

      default:
        {
          // The default reveal order is the same as not having
          // a boundary.
          workInProgress.memoizedState = null;
        }
    }
  }

  return workInProgress.child;
}

function updatePortalComponent(current, workInProgress, renderExpirationTime) {
  pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
  var nextChildren = workInProgress.pendingProps;

  if (current === null) {
    // Portals are special because we don't append the children during mount
    // but at commit. Therefore we need to track insertions which the normal
    // flow doesn't do during mount. This doesn't happen at the root because
    // the root always starts with a "current" with a null child.
    // TODO: Consider unifying this with how the root works.
    workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
  } else {
    reconcileChildren(current, workInProgress, nextChildren, renderExpirationTime);
  }

  return workInProgress.child;
}

function updateContextProvider(current, workInProgress, renderExpirationTime) {
  var providerType = workInProgress.type;
  var context = providerType._context;
  var newProps = workInProgress.pendingProps;
  var oldProps = workInProgress.memoizedProps;
  var newValue = newProps.value;

  {
    var providerPropTypes = workInProgress.type.propTypes;

    if (providerPropTypes) {
      checkPropTypes(providerPropTypes, newProps, 'prop', 'Context.Provider', getCurrentFiberStackInDev);
    }
  }

  pushProvider(workInProgress, newValue);

  if (oldProps !== null) {
    var oldValue = oldProps.value;
    var changedBits = calculateChangedBits(context, newValue, oldValue);

    if (changedBits === 0) {
      // No change. Bailout early if children are the same.
      if (oldProps.children === newProps.children && !hasContextChanged()) {
        return bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);
      }
    } else {
      // The context value changed. Search for matching consumers and schedule
      // them to update.
      propagateContextChange(workInProgress, context, changedBits, renderExpirationTime);
    }
  }

  var newChildren = newProps.children;
  reconcileChildren(current, workInProgress, newChildren, renderExpirationTime);
  return workInProgress.child;
}

var hasWarnedAboutUsingContextAsConsumer = false;

function updateContextConsumer(current, workInProgress, renderExpirationTime) {
  var context = workInProgress.type; // The logic below for Context differs depending on PROD or DEV mode. In
  // DEV mode, we create a separate object for Context.Consumer that acts
  // like a proxy to Context. This proxy object adds unnecessary code in PROD
  // so we use the old behaviour (Context.Consumer references Context) to
  // reduce size and overhead. The separate object references context via
  // a property called "_context", which also gives us the ability to check
  // in DEV mode if this property exists or not and warn if it does not.

  {
    if (context._context === undefined) {
      // This may be because it's a Context (rather than a Consumer).
      // Or it may be because it's older React where they're the same thing.
      // We only want to warn if we're sure it's a new React.
      if (context !== context.Consumer) {
        if (!hasWarnedAboutUsingContextAsConsumer) {
          hasWarnedAboutUsingContextAsConsumer = true;

          error('Rendering <Context> directly is not supported and will be removed in ' + 'a future major release. Did you mean to render <Context.Consumer> instead?');
        }
      }
    } else {
      context = context._context;
    }
  }

  var newProps = workInProgress.pendingProps;
  var render = newProps.children;

  {
    if (typeof render !== 'function') {
      error('A context consumer was rendered with multiple children, or a child ' + "that isn't a function. A context consumer expects a single child " + 'that is a function. If you did pass a function, make sure there ' + 'is no trailing or leading whitespace around it.');
    }
  }

  prepareToReadContext(workInProgress, renderExpirationTime);
  var newValue = readContext(context, newProps.unstable_observedBits);
  var newChildren;

  {
    ReactCurrentOwner$1.current = workInProgress;
    setCurrentPhase('render');
    newChildren = render(newValue);
    setCurrentPhase(null);
  } // React DevTools reads this flag.


  workInProgress.effectTag |= PerformedWork;
  reconcileChildren(current, workInProgress, newChildren, renderExpirationTime);
  return workInProgress.child;
}

function markWorkInProgressReceivedUpdate() {
  didReceiveUpdate = true;
}

function bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime) {
  cancelWorkTimer(workInProgress);

  if (current !== null) {
    // Reuse previous dependencies
    workInProgress.dependencies = current.dependencies;
  }

  {
    // Don't update "base" render times for bailouts.
    stopProfilerTimerIfRunning();
  }

  var updateExpirationTime = workInProgress.expirationTime;

  if (updateExpirationTime !== NoWork) {
    markUnprocessedUpdateTime(updateExpirationTime);
  } // Check if the children have any pending work.


  var childExpirationTime = workInProgress.childExpirationTime;

  if (childExpirationTime < renderExpirationTime) {
    // The children don't have any work either. We can skip them.
    // TODO: Once we add back resuming, we should check if the children are
    // a work-in-progress set. If so, we need to transfer their effects.
    return null;
  } else {
    // This fiber doesn't have work, but its subtree does. Clone the child
    // fibers and continue.
    cloneChildFibers(current, workInProgress);
    return workInProgress.child;
  }
}

function remountFiber(current, oldWorkInProgress, newWorkInProgress) {
  {
    var returnFiber = oldWorkInProgress.return;

    if (returnFiber === null) {
      throw new Error('Cannot swap the root fiber.');
    } // Disconnect from the old current.
    // It will get deleted.


    current.alternate = null;
    oldWorkInProgress.alternate = null; // Connect to the new tree.

    newWorkInProgress.index = oldWorkInProgress.index;
    newWorkInProgress.sibling = oldWorkInProgress.sibling;
    newWorkInProgress.return = oldWorkInProgress.return;
    newWorkInProgress.ref = oldWorkInProgress.ref; // Replace the child/sibling pointers above it.

    if (oldWorkInProgress === returnFiber.child) {
      returnFiber.child = newWorkInProgress;
    } else {
      var prevSibling = returnFiber.child;

      if (prevSibling === null) {
        throw new Error('Expected parent to have a child.');
      }

      while (prevSibling.sibling !== oldWorkInProgress) {
        prevSibling = prevSibling.sibling;

        if (prevSibling === null) {
          throw new Error('Expected to find the previous sibling.');
        }
      }

      prevSibling.sibling = newWorkInProgress;
    } // Delete the old fiber and place the new one.
    // Since the old fiber is disconnected, we have to schedule it manually.


    var last = returnFiber.lastEffect;

    if (last !== null) {
      last.nextEffect = current;
      returnFiber.lastEffect = current;
    } else {
      returnFiber.firstEffect = returnFiber.lastEffect = current;
    }

    current.nextEffect = null;
    current.effectTag = Deletion;
    newWorkInProgress.effectTag |= Placement; // Restart work from the new fiber.

    return newWorkInProgress;
  }
}

function beginWork(current, workInProgress, renderExpirationTime) {
  var updateExpirationTime = workInProgress.expirationTime;

  {
    if (workInProgress._debugNeedsRemount && current !== null) {
      // This will restart the begin phase with a new fiber.
      return remountFiber(current, workInProgress, createFiberFromTypeAndProps(workInProgress.type, workInProgress.key, workInProgress.pendingProps, workInProgress._debugOwner || null, workInProgress.mode, workInProgress.expirationTime));
    }
  }

  if (current !== null) {
    var oldProps = current.memoizedProps;
    var newProps = workInProgress.pendingProps;

    if (oldProps !== newProps || hasContextChanged() || ( // Force a re-render if the implementation changed due to hot reload:
     workInProgress.type !== current.type )) {
      // If props or context changed, mark the fiber as having performed work.
      // This may be unset if the props are determined to be equal later (memo).
      didReceiveUpdate = true;
    } else if (updateExpirationTime < renderExpirationTime) {
      didReceiveUpdate = false; // This fiber does not have any pending work. Bailout without entering
      // the begin phase. There's still some bookkeeping we that needs to be done
      // in this optimized path, mostly pushing stuff onto the stack.

      switch (workInProgress.tag) {
        case HostRoot:
          pushHostRootContext(workInProgress);
          resetHydrationState();
          break;

        case HostComponent:
          pushHostContext(workInProgress);

          if (workInProgress.mode & ConcurrentMode && renderExpirationTime !== Never && shouldDeprioritizeSubtree(workInProgress.type, newProps)) {
            {
              markSpawnedWork(Never);
            } // Schedule this fiber to re-render at offscreen priority. Then bailout.


            workInProgress.expirationTime = workInProgress.childExpirationTime = Never;
            return null;
          }

          break;

        case ClassComponent:
          {
            var Component = workInProgress.type;

            if (isContextProvider(Component)) {
              pushContextProvider(workInProgress);
            }

            break;
          }

        case HostPortal:
          pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
          break;

        case ContextProvider:
          {
            var newValue = workInProgress.memoizedProps.value;
            pushProvider(workInProgress, newValue);
            break;
          }

        case Profiler:
          {
            // Profiler should only call onRender when one of its descendants actually rendered.
            var hasChildWork = workInProgress.childExpirationTime >= renderExpirationTime;

            if (hasChildWork) {
              workInProgress.effectTag |= Update;
            }
          }

          break;

        case SuspenseComponent:
          {
            var state = workInProgress.memoizedState;

            if (state !== null) {
              // whether to retry the primary children, or to skip over it and
              // go straight to the fallback. Check the priority of the primary
              // child fragment.


              var primaryChildFragment = workInProgress.child;
              var primaryChildExpirationTime = primaryChildFragment.childExpirationTime;

              if (primaryChildExpirationTime !== NoWork && primaryChildExpirationTime >= renderExpirationTime) {
                // The primary children have pending work. Use the normal path
                // to attempt to render the primary children again.
                return updateSuspenseComponent(current, workInProgress, renderExpirationTime);
              } else {
                pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current)); // The primary children do not have pending work with sufficient
                // priority. Bailout.

                var child = bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);

                if (child !== null) {
                  // The fallback children have pending work. Skip over the
                  // primary children and work on the fallback.
                  return child.sibling;
                } else {
                  return null;
                }
              }
            } else {
              pushSuspenseContext(workInProgress, setDefaultShallowSuspenseContext(suspenseStackCursor.current));
            }

            break;
          }

        case SuspenseListComponent:
          {
            var didSuspendBefore = (current.effectTag & DidCapture) !== NoEffect;

            var _hasChildWork = workInProgress.childExpirationTime >= renderExpirationTime;

            if (didSuspendBefore) {
              if (_hasChildWork) {
                // If something was in fallback state last time, and we have all the
                // same children then we're still in progressive loading state.
                // Something might get unblocked by state updates or retries in the
                // tree which will affect the tail. So we need to use the normal
                // path to compute the correct tail.
                return updateSuspenseListComponent(current, workInProgress, renderExpirationTime);
              } // If none of the children had any work, that means that none of
              // them got retried so they'll still be blocked in the same way
              // as before. We can fast bail out.


              workInProgress.effectTag |= DidCapture;
            } // If nothing suspended before and we're rendering the same children,
            // then the tail doesn't matter. Anything new that suspends will work
            // in the "together" mode, so we can continue from the state we had.


            var renderState = workInProgress.memoizedState;

            if (renderState !== null) {
              // Reset to the "together" mode in case we've started a different
              // update in the past but didn't complete it.
              renderState.rendering = null;
              renderState.tail = null;
            }

            pushSuspenseContext(workInProgress, suspenseStackCursor.current);

            if (_hasChildWork) {
              break;
            } else {
              // If none of the children had any work, that means that none of
              // them got retried so they'll still be blocked in the same way
              // as before. We can fast bail out.
              return null;
            }
          }
      }

      return bailoutOnAlreadyFinishedWork(current, workInProgress, renderExpirationTime);
    } else {
      // An update was scheduled on this fiber, but there are no new props
      // nor legacy context. Set this to false. If an update queue or context
      // consumer produces a changed value, it will set this to true. Otherwise,
      // the component will assume the children have not changed and bail out.
      didReceiveUpdate = false;
    }
  } else {
    didReceiveUpdate = false;
  } // Before entering the begin phase, clear pending update priority.
  // TODO: This assumes that we're about to evaluate the component and process
  // the update queue. However, there's an exception: SimpleMemoComponent
  // sometimes bails out later in the begin phase. This indicates that we should
  // move this assignment out of the common path and into each branch.


  workInProgress.expirationTime = NoWork;

  switch (workInProgress.tag) {
    case IndeterminateComponent:
      {
        return mountIndeterminateComponent(current, workInProgress, workInProgress.type, renderExpirationTime);
      }

    case LazyComponent:
      {
        var elementType = workInProgress.elementType;
        return mountLazyComponent(current, workInProgress, elementType, updateExpirationTime, renderExpirationTime);
      }

    case FunctionComponent:
      {
        var _Component = workInProgress.type;
        var unresolvedProps = workInProgress.pendingProps;
        var resolvedProps = workInProgress.elementType === _Component ? unresolvedProps : resolveDefaultProps(_Component, unresolvedProps);
        return updateFunctionComponent(current, workInProgress, _Component, resolvedProps, renderExpirationTime);
      }

    case ClassComponent:
      {
        var _Component2 = workInProgress.type;
        var _unresolvedProps = workInProgress.pendingProps;

        var _resolvedProps = workInProgress.elementType === _Component2 ? _unresolvedProps : resolveDefaultProps(_Component2, _unresolvedProps);

        return updateClassComponent(current, workInProgress, _Component2, _resolvedProps, renderExpirationTime);
      }

    case HostRoot:
      return updateHostRoot(current, workInProgress, renderExpirationTime);

    case HostComponent:
      return updateHostComponent(current, workInProgress, renderExpirationTime);

    case HostText:
      return updateHostText(current, workInProgress);

    case SuspenseComponent:
      return updateSuspenseComponent(current, workInProgress, renderExpirationTime);

    case HostPortal:
      return updatePortalComponent(current, workInProgress, renderExpirationTime);

    case ForwardRef:
      {
        var type = workInProgress.type;
        var _unresolvedProps2 = workInProgress.pendingProps;

        var _resolvedProps2 = workInProgress.elementType === type ? _unresolvedProps2 : resolveDefaultProps(type, _unresolvedProps2);

        return updateForwardRef(current, workInProgress, type, _resolvedProps2, renderExpirationTime);
      }

    case Fragment:
      return updateFragment(current, workInProgress, renderExpirationTime);

    case Mode:
      return updateMode(current, workInProgress, renderExpirationTime);

    case Profiler:
      return updateProfiler(current, workInProgress, renderExpirationTime);

    case ContextProvider:
      return updateContextProvider(current, workInProgress, renderExpirationTime);

    case ContextConsumer:
      return updateContextConsumer(current, workInProgress, renderExpirationTime);

    case MemoComponent:
      {
        var _type2 = workInProgress.type;
        var _unresolvedProps3 = workInProgress.pendingProps; // Resolve outer props first, then resolve inner props.

        var _resolvedProps3 = resolveDefaultProps(_type2, _unresolvedProps3);

        {
          if (workInProgress.type !== workInProgress.elementType) {
            var outerPropTypes = _type2.propTypes;

            if (outerPropTypes) {
              checkPropTypes(outerPropTypes, _resolvedProps3, // Resolved for outer only
              'prop', getComponentName(_type2), getCurrentFiberStackInDev);
            }
          }
        }

        _resolvedProps3 = resolveDefaultProps(_type2.type, _resolvedProps3);
        return updateMemoComponent(current, workInProgress, _type2, _resolvedProps3, updateExpirationTime, renderExpirationTime);
      }

    case SimpleMemoComponent:
      {
        return updateSimpleMemoComponent(current, workInProgress, workInProgress.type, workInProgress.pendingProps, updateExpirationTime, renderExpirationTime);
      }

    case IncompleteClassComponent:
      {
        var _Component3 = workInProgress.type;
        var _unresolvedProps4 = workInProgress.pendingProps;

        var _resolvedProps4 = workInProgress.elementType === _Component3 ? _unresolvedProps4 : resolveDefaultProps(_Component3, _unresolvedProps4);

        return mountIncompleteClassComponent(current, workInProgress, _Component3, _resolvedProps4, renderExpirationTime);
      }

    case SuspenseListComponent:
      {
        return updateSuspenseListComponent(current, workInProgress, renderExpirationTime);
      }
  }

  {
    {
      throw Error( "Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in React. Please file an issue." );
    }
  }
}

function markUpdate(workInProgress) {
  // Tag the fiber with an update effect. This turns a Placement into
  // a PlacementAndUpdate.
  workInProgress.effectTag |= Update;
}

function markRef$1(workInProgress) {
  workInProgress.effectTag |= Ref;
}

var appendAllChildren;
var updateHostContainer;
var updateHostComponent$1;
var updateHostText$1;

{
  // Mutation mode
  appendAllChildren = function (parent, workInProgress, needsVisibilityToggle, isHidden) {
    // We only have the top Fiber that was created but we need recurse down its
    // children to find all the terminal nodes.
    var node = workInProgress.child;

    while (node !== null) {
      if (node.tag === HostComponent || node.tag === HostText) {
        appendInitialChild(parent, node.stateNode);
      } else if (node.tag === HostPortal) ; else if (node.child !== null) {
        node.child.return = node;
        node = node.child;
        continue;
      }

      if (node === workInProgress) {
        return;
      }

      while (node.sibling === null) {
        if (node.return === null || node.return === workInProgress) {
          return;
        }

        node = node.return;
      }

      node.sibling.return = node.return;
      node = node.sibling;
    }
  };

  updateHostContainer = function (workInProgress) {// Noop
  };

  updateHostComponent$1 = function (current, workInProgress, type, newProps, rootContainerInstance) {
    // If we have an alternate, that means this is an update and we need to
    // schedule a side-effect to do the updates.
    var oldProps = current.memoizedProps;

    if (oldProps === newProps) {
      // In mutation mode, this is sufficient for a bailout because
      // we won't touch this node even if children changed.
      return;
    } // If we get updated because one of our children updated, we don't
    // have newProps so we'll have to reuse them.
    // TODO: Split the update API as separate for the props vs. children.
    // Even better would be if children weren't special cased at all tho.


    var instance = workInProgress.stateNode;
    var currentHostContext = getHostContext(); // TODO: Experiencing an error where oldProps is null. Suggests a host
    // component is hitting the resume path. Figure out why. Possibly
    // related to `hidden`.

    var updatePayload = prepareUpdate(instance, type, oldProps, newProps, rootContainerInstance, currentHostContext); // TODO: Type this specific to this type of component.

    workInProgress.updateQueue = updatePayload; // If the update payload indicates that there is a change or if there
    // is a new ref we mark this as an update. All the work is done in commitWork.

    if (updatePayload) {
      markUpdate(workInProgress);
    }
  };

  updateHostText$1 = function (current, workInProgress, oldText, newText) {
    // If the text differs, mark it as an update. All the work in done in commitWork.
    if (oldText !== newText) {
      markUpdate(workInProgress);
    }
  };
}

function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
  switch (renderState.tailMode) {
    case 'hidden':
      {
        // Any insertions at the end of the tail list after this point
        // should be invisible. If there are already mounted boundaries
        // anything before them are not considered for collapsing.
        // Therefore we need to go through the whole tail to find if
        // there are any.
        var tailNode = renderState.tail;
        var lastTailNode = null;

        while (tailNode !== null) {
          if (tailNode.alternate !== null) {
            lastTailNode = tailNode;
          }

          tailNode = tailNode.sibling;
        } // Next we're simply going to delete all insertions after the
        // last rendered item.


        if (lastTailNode === null) {
          // All remaining items in the tail are insertions.
          renderState.tail = null;
        } else {
          // Detach the insertion after the last node that was already
          // inserted.
          lastTailNode.sibling = null;
        }

        break;
      }

    case 'collapsed':
      {
        // Any insertions at the end of the tail list after this point
        // should be invisible. If there are already mounted boundaries
        // anything before them are not considered for collapsing.
        // Therefore we need to go through the whole tail to find if
        // there are any.
        var _tailNode = renderState.tail;
        var _lastTailNode = null;

        while (_tailNode !== null) {
          if (_tailNode.alternate !== null) {
            _lastTailNode = _tailNode;
          }

          _tailNode = _tailNode.sibling;
        } // Next we're simply going to delete all insertions after the
        // last rendered item.


        if (_lastTailNode === null) {
          // All remaining items in the tail are insertions.
          if (!hasRenderedATailFallback && renderState.tail !== null) {
            // We suspended during the head. We want to show at least one
            // row at the tail. So we'll keep on and cut off the rest.
            renderState.tail.sibling = null;
          } else {
            renderState.tail = null;
          }
        } else {
          // Detach the insertion after the last node that was already
          // inserted.
          _lastTailNode.sibling = null;
        }

        break;
      }
  }
}

function completeWork(current, workInProgress, renderExpirationTime) {
  var newProps = workInProgress.pendingProps;

  switch (workInProgress.tag) {
    case IndeterminateComponent:
    case LazyComponent:
    case SimpleMemoComponent:
    case FunctionComponent:
    case ForwardRef:
    case Fragment:
    case Mode:
    case Profiler:
    case ContextConsumer:
    case MemoComponent:
      return null;

    case ClassComponent:
      {
        var Component = workInProgress.type;

        if (isContextProvider(Component)) {
          popContext(workInProgress);
        }

        return null;
      }

    case HostRoot:
      {
        popHostContainer(workInProgress);
        popTopLevelContextObject(workInProgress);
        var fiberRoot = workInProgress.stateNode;

        if (fiberRoot.pendingContext) {
          fiberRoot.context = fiberRoot.pendingContext;
          fiberRoot.pendingContext = null;
        }

        if (current === null || current.child === null) {
          // If we hydrated, pop so that we can delete any remaining children
          // that weren't hydrated.
          var wasHydrated = popHydrationState(workInProgress);

          if (wasHydrated) {
            // If we hydrated, then we'll need to schedule an update for
            // the commit side-effects on the root.
            markUpdate(workInProgress);
          }
        }

        updateHostContainer(workInProgress);
        return null;
      }

    case HostComponent:
      {
        popHostContext(workInProgress);
        var rootContainerInstance = getRootHostContainer();
        var type = workInProgress.type;

        if (current !== null && workInProgress.stateNode != null) {
          updateHostComponent$1(current, workInProgress, type, newProps, rootContainerInstance);

          if (current.ref !== workInProgress.ref) {
            markRef$1(workInProgress);
          }
        } else {
          if (!newProps) {
            if (!(workInProgress.stateNode !== null)) {
              {
                throw Error( "We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue." );
              }
            } // This can happen when we abort work.


            return null;
          }

          var currentHostContext = getHostContext(); // TODO: Move createInstance to beginWork and keep it on a context
          // "stack" as the parent. Then append children as we go in beginWork
          // or completeWork depending on whether we want to add them top->down or
          // bottom->up. Top->down is faster in IE11.

          var _wasHydrated = popHydrationState(workInProgress);

          if (_wasHydrated) {
            // TODO: Move this and createInstance step into the beginPhase
            // to consolidate.
            if (prepareToHydrateHostInstance(workInProgress, rootContainerInstance, currentHostContext)) {
              // If changes to the hydrated node need to be applied at the
              // commit-phase we mark this as such.
              markUpdate(workInProgress);
            }
          } else {
            var instance = createInstance(type, newProps, rootContainerInstance, currentHostContext, workInProgress);
            appendAllChildren(instance, workInProgress, false, false); // This needs to be set before we mount Flare event listeners

            workInProgress.stateNode = instance;
            // (eg DOM renderer supports auto-focus for certain elements).
            // Make sure such renderers get scheduled for later work.


            if (finalizeInitialChildren(instance, type, newProps, rootContainerInstance)) {
              markUpdate(workInProgress);
            }
          }

          if (workInProgress.ref !== null) {
            // If there is a ref on a host node we need to schedule a callback
            markRef$1(workInProgress);
          }
        }

        return null;
      }

    case HostText:
      {
        var newText = newProps;

        if (current && workInProgress.stateNode != null) {
          var oldText = current.memoizedProps; // If we have an alternate, that means this is an update and we need
          // to schedule a side-effect to do the updates.

          updateHostText$1(current, workInProgress, oldText, newText);
        } else {
          if (typeof newText !== 'string') {
            if (!(workInProgress.stateNode !== null)) {
              {
                throw Error( "We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue." );
              }
            } // This can happen when we abort work.

          }

          var _rootContainerInstance = getRootHostContainer();

          var _currentHostContext = getHostContext();

          var _wasHydrated2 = popHydrationState(workInProgress);

          if (_wasHydrated2) {
            if (prepareToHydrateHostTextInstance(workInProgress)) {
              markUpdate(workInProgress);
            }
          } else {
            workInProgress.stateNode = createTextInstance(newText, _rootContainerInstance, _currentHostContext, workInProgress);
          }
        }

        return null;
      }

    case SuspenseComponent:
      {
        popSuspenseContext(workInProgress);
        var nextState = workInProgress.memoizedState;

        if ((workInProgress.effectTag & DidCapture) !== NoEffect) {
          // Something suspended. Re-render with the fallback children.
          workInProgress.expirationTime = renderExpirationTime; // Do not reset the effect list.

          return workInProgress;
        }

        var nextDidTimeout = nextState !== null;
        var prevDidTimeout = false;

        if (current === null) {
          if (workInProgress.memoizedProps.fallback !== undefined) {
            popHydrationState(workInProgress);
          }
        } else {
          var prevState = current.memoizedState;
          prevDidTimeout = prevState !== null;

          if (!nextDidTimeout && prevState !== null) {
            // We just switched from the fallback to the normal children.
            // Delete the fallback.
            // TODO: Would it be better to store the fallback fragment on
            // the stateNode during the begin phase?
            var currentFallbackChild = current.child.sibling;

            if (currentFallbackChild !== null) {
              // Deletions go at the beginning of the return fiber's effect list
              var first = workInProgress.firstEffect;

              if (first !== null) {
                workInProgress.firstEffect = currentFallbackChild;
                currentFallbackChild.nextEffect = first;
              } else {
                workInProgress.firstEffect = workInProgress.lastEffect = currentFallbackChild;
                currentFallbackChild.nextEffect = null;
              }

              currentFallbackChild.effectTag = Deletion;
            }
          }
        }

        if (nextDidTimeout && !prevDidTimeout) {
          // If this subtreee is running in blocking mode we can suspend,
          // otherwise we won't suspend.
          // TODO: This will still suspend a synchronous tree if anything
          // in the concurrent tree already suspended during this render.
          // This is a known bug.
          if ((workInProgress.mode & BlockingMode) !== NoMode) {
            // TODO: Move this back to throwException because this is too late
            // if this is a large tree which is common for initial loads. We
            // don't know if we should restart a render or not until we get
            // this marker, and this is too late.
            // If this render already had a ping or lower pri updates,
            // and this is the first time we know we're going to suspend we
            // should be able to immediately restart from within throwException.
            var hasInvisibleChildContext = current === null && workInProgress.memoizedProps.unstable_avoidThisFallback !== true;

            if (hasInvisibleChildContext || hasSuspenseContext(suspenseStackCursor.current, InvisibleParentSuspenseContext)) {
              // If this was in an invisible tree or a new render, then showing
              // this boundary is ok.
              renderDidSuspend();
            } else {
              // Otherwise, we're going to have to hide content so we should
              // suspend for longer if possible.
              renderDidSuspendDelayIfPossible();
            }
          }
        }

        {
          // TODO: Only schedule updates if these values are non equal, i.e. it changed.
          if (nextDidTimeout || prevDidTimeout) {
            // If this boundary just timed out, schedule an effect to attach a
            // retry listener to the promise. This flag is also used to hide the
            // primary children. In mutation mode, we also need the flag to
            // *unhide* children that were previously hidden, so check if this
            // is currently timed out, too.
            workInProgress.effectTag |= Update;
          }
        }

        return null;
      }

    case HostPortal:
      popHostContainer(workInProgress);
      updateHostContainer(workInProgress);
      return null;

    case ContextProvider:
      // Pop provider fiber
      popProvider(workInProgress);
      return null;

    case IncompleteClassComponent:
      {
        // Same as class component case. I put it down here so that the tags are
        // sequential to ensure this switch is compiled to a jump table.
        var _Component = workInProgress.type;

        if (isContextProvider(_Component)) {
          popContext(workInProgress);
        }

        return null;
      }

    case SuspenseListComponent:
      {
        popSuspenseContext(workInProgress);
        var renderState = workInProgress.memoizedState;

        if (renderState === null) {
          // We're running in the default, "independent" mode.
          // We don't do anything in this mode.
          return null;
        }

        var didSuspendAlready = (workInProgress.effectTag & DidCapture) !== NoEffect;
        var renderedTail = renderState.rendering;

        if (renderedTail === null) {
          // We just rendered the head.
          if (!didSuspendAlready) {
            // This is the first pass. We need to figure out if anything is still
            // suspended in the rendered set.
            // If new content unsuspended, but there's still some content that
            // didn't. Then we need to do a second pass that forces everything
            // to keep showing their fallbacks.
            // We might be suspended if something in this render pass suspended, or
            // something in the previous committed pass suspended. Otherwise,
            // there's no chance so we can skip the expensive call to
            // findFirstSuspended.
            var cannotBeSuspended = renderHasNotSuspendedYet() && (current === null || (current.effectTag & DidCapture) === NoEffect);

            if (!cannotBeSuspended) {
              var row = workInProgress.child;

              while (row !== null) {
                var suspended = findFirstSuspended(row);

                if (suspended !== null) {
                  didSuspendAlready = true;
                  workInProgress.effectTag |= DidCapture;
                  cutOffTailIfNeeded(renderState, false); // If this is a newly suspended tree, it might not get committed as
                  // part of the second pass. In that case nothing will subscribe to
                  // its thennables. Instead, we'll transfer its thennables to the
                  // SuspenseList so that it can retry if they resolve.
                  // There might be multiple of these in the list but since we're
                  // going to wait for all of them anyway, it doesn't really matter
                  // which ones gets to ping. In theory we could get clever and keep
                  // track of how many dependencies remain but it gets tricky because
                  // in the meantime, we can add/remove/change items and dependencies.
                  // We might bail out of the loop before finding any but that
                  // doesn't matter since that means that the other boundaries that
                  // we did find already has their listeners attached.

                  var newThennables = suspended.updateQueue;

                  if (newThennables !== null) {
                    workInProgress.updateQueue = newThennables;
                    workInProgress.effectTag |= Update;
                  } // Rerender the whole list, but this time, we'll force fallbacks
                  // to stay in place.
                  // Reset the effect list before doing the second pass since that's now invalid.


                  if (renderState.lastEffect === null) {
                    workInProgress.firstEffect = null;
                  }

                  workInProgress.lastEffect = renderState.lastEffect; // Reset the child fibers to their original state.

                  resetChildFibers(workInProgress, renderExpirationTime); // Set up the Suspense Context to force suspense and immediately
                  // rerender the children.

                  pushSuspenseContext(workInProgress, setShallowSuspenseContext(suspenseStackCursor.current, ForceSuspenseFallback));
                  return workInProgress.child;
                }

                row = row.sibling;
              }
            }
          } else {
            cutOffTailIfNeeded(renderState, false);
          } // Next we're going to render the tail.

        } else {
          // Append the rendered row to the child list.
          if (!didSuspendAlready) {
            var _suspended = findFirstSuspended(renderedTail);

            if (_suspended !== null) {
              workInProgress.effectTag |= DidCapture;
              didSuspendAlready = true; // Ensure we transfer the update queue to the parent so that it doesn't
              // get lost if this row ends up dropped during a second pass.

              var _newThennables = _suspended.updateQueue;

              if (_newThennables !== null) {
                workInProgress.updateQueue = _newThennables;
                workInProgress.effectTag |= Update;
              }

              cutOffTailIfNeeded(renderState, true); // This might have been modified.

              if (renderState.tail === null && renderState.tailMode === 'hidden' && !renderedTail.alternate) {
                // We need to delete the row we just rendered.
                // Reset the effect list to what it was before we rendered this
                // child. The nested children have already appended themselves.
                var lastEffect = workInProgress.lastEffect = renderState.lastEffect; // Remove any effects that were appended after this point.

                if (lastEffect !== null) {
                  lastEffect.nextEffect = null;
                } // We're done.


                return null;
              }
            } else if ( // The time it took to render last row is greater than time until
            // the expiration.
            now() * 2 - renderState.renderingStartTime > renderState.tailExpiration && renderExpirationTime > Never) {
              // We have now passed our CPU deadline and we'll just give up further
              // attempts to render the main content and only render fallbacks.
              // The assumption is that this is usually faster.
              workInProgress.effectTag |= DidCapture;
              didSuspendAlready = true;
              cutOffTailIfNeeded(renderState, false); // Since nothing actually suspended, there will nothing to ping this
              // to get it started back up to attempt the next item. If we can show
              // them, then they really have the same priority as this render.
              // So we'll pick it back up the very next render pass once we've had
              // an opportunity to yield for paint.

              var nextPriority = renderExpirationTime - 1;
              workInProgress.expirationTime = workInProgress.childExpirationTime = nextPriority;

              {
                markSpawnedWork(nextPriority);
              }
            }
          }

          if (renderState.isBackwards) {
            // The effect list of the backwards tail will have been added
            // to the end. This breaks the guarantee that life-cycles fire in
            // sibling order but that isn't a strong guarantee promised by React.
            // Especially since these might also just pop in during future commits.
            // Append to the beginning of the list.
            renderedTail.sibling = workInProgress.child;
            workInProgress.child = renderedTail;
          } else {
            var previousSibling = renderState.last;

            if (previousSibling !== null) {
              previousSibling.sibling = renderedTail;
            } else {
              workInProgress.child = renderedTail;
            }

            renderState.last = renderedTail;
          }
        }

        if (renderState.tail !== null) {
          // We still have tail rows to render.
          if (renderState.tailExpiration === 0) {
            // Heuristic for how long we're willing to spend rendering rows
            // until we just give up and show what we have so far.
            var TAIL_EXPIRATION_TIMEOUT_MS = 500;
            renderState.tailExpiration = now() + TAIL_EXPIRATION_TIMEOUT_MS; // TODO: This is meant to mimic the train model or JND but this
            // is a per component value. It should really be since the start
            // of the total render or last commit. Consider using something like
            // globalMostRecentFallbackTime. That doesn't account for being
            // suspended for part of the time or when it's a new render.
            // It should probably use a global start time value instead.
          } // Pop a row.


          var next = renderState.tail;
          renderState.rendering = next;
          renderState.tail = next.sibling;
          renderState.lastEffect = workInProgress.lastEffect;
          renderState.renderingStartTime = now();
          next.sibling = null; // Restore the context.
          // TODO: We can probably just avoid popping it instead and only
          // setting it the first time we go from not suspended to suspended.

          var suspenseContext = suspenseStackCursor.current;

          if (didSuspendAlready) {
            suspenseContext = setShallowSuspenseContext(suspenseContext, ForceSuspenseFallback);
          } else {
            suspenseContext = setDefaultShallowSuspenseContext(suspenseContext);
          }

          pushSuspenseContext(workInProgress, suspenseContext); // Do a pass over the next row.

          return next;
        }

        return null;
      }
  }

  {
    {
      throw Error( "Unknown unit of work tag (" + workInProgress.tag + "). This error is likely caused by a bug in React. Please file an issue." );
    }
  }
}

function unwindWork(workInProgress, renderExpirationTime) {
  switch (workInProgress.tag) {
    case ClassComponent:
      {
        var Component = workInProgress.type;

        if (isContextProvider(Component)) {
          popContext(workInProgress);
        }

        var effectTag = workInProgress.effectTag;

        if (effectTag & ShouldCapture) {
          workInProgress.effectTag = effectTag & ~ShouldCapture | DidCapture;
          return workInProgress;
        }

        return null;
      }

    case HostRoot:
      {
        popHostContainer(workInProgress);
        popTopLevelContextObject(workInProgress);
        var _effectTag = workInProgress.effectTag;

        if (!((_effectTag & DidCapture) === NoEffect)) {
          {
            throw Error( "The root failed to unmount after an error. This is likely a bug in React. Please file an issue." );
          }
        }

        workInProgress.effectTag = _effectTag & ~ShouldCapture | DidCapture;
        return workInProgress;
      }

    case HostComponent:
      {
        // TODO: popHydrationState
        popHostContext(workInProgress);
        return null;
      }

    case SuspenseComponent:
      {
        popSuspenseContext(workInProgress);

        var _effectTag2 = workInProgress.effectTag;

        if (_effectTag2 & ShouldCapture) {
          workInProgress.effectTag = _effectTag2 & ~ShouldCapture | DidCapture; // Captured a suspense effect. Re-render the boundary.

          return workInProgress;
        }

        return null;
      }

    case SuspenseListComponent:
      {
        popSuspenseContext(workInProgress); // SuspenseList doesn't actually catch anything. It should've been
        // caught by a nested boundary. If not, it should bubble through.

        return null;
      }

    case HostPortal:
      popHostContainer(workInProgress);
      return null;

    case ContextProvider:
      popProvider(workInProgress);
      return null;

    default:
      return null;
  }
}

function unwindInterruptedWork(interruptedWork) {
  switch (interruptedWork.tag) {
    case ClassComponent:
      {
        var childContextTypes = interruptedWork.type.childContextTypes;

        if (childContextTypes !== null && childContextTypes !== undefined) {
          popContext(interruptedWork);
        }

        break;
      }

    case HostRoot:
      {
        popHostContainer(interruptedWork);
        popTopLevelContextObject(interruptedWork);
        break;
      }

    case HostComponent:
      {
        popHostContext(interruptedWork);
        break;
      }

    case HostPortal:
      popHostContainer(interruptedWork);
      break;

    case SuspenseComponent:
      popSuspenseContext(interruptedWork);
      break;

    case SuspenseListComponent:
      popSuspenseContext(interruptedWork);
      break;

    case ContextProvider:
      popProvider(interruptedWork);
      break;
  }
}

function createCapturedValue(value, source) {
  // If the value is an error, call this function immediately after it is thrown
  // so the stack is accurate.
  return {
    value: value,
    source: source,
    stack: getStackByFiberInDevAndProd(source)
  };
}

function logCapturedError(capturedError) {

  var error = capturedError.error;

  {
    var componentName = capturedError.componentName,
        componentStack = capturedError.componentStack,
        errorBoundaryName = capturedError.errorBoundaryName,
        errorBoundaryFound = capturedError.errorBoundaryFound,
        willRetry = capturedError.willRetry; // Browsers support silencing uncaught errors by calling
    // `preventDefault()` in window `error` handler.
    // We record this information as an expando on the error.

    if (error != null && error._suppressLogging) {
      if (errorBoundaryFound && willRetry) {
        // The error is recoverable and was silenced.
        // Ignore it and don't print the stack addendum.
        // This is handy for testing error boundaries without noise.
        return;
      } // The error is fatal. Since the silencing might have
      // been accidental, we'll surface it anyway.
      // However, the browser would have silenced the original error
      // so we'll print it first, and then print the stack addendum.


      console['error'](error); // Don't transform to our wrapper
      // For a more detailed description of this block, see:
      // https://github.com/facebook/react/pull/13384
    }

    var componentNameMessage = componentName ? "The above error occurred in the <" + componentName + "> component:" : 'The above error occurred in one of your React components:';
    var errorBoundaryMessage; // errorBoundaryFound check is sufficient; errorBoundaryName check is to satisfy Flow.

    if (errorBoundaryFound && errorBoundaryName) {
      if (willRetry) {
        errorBoundaryMessage = "React will try to recreate this component tree from scratch " + ("using the error boundary you provided, " + errorBoundaryName + ".");
      } else {
        errorBoundaryMessage = "This error was initially handled by the error boundary " + errorBoundaryName + ".\n" + "Recreating the tree from scratch failed so React will unmount the tree.";
      }
    } else {
      errorBoundaryMessage = 'Consider adding an error boundary to your tree to customize error handling behavior.\n' + 'Visit https://fb.me/react-error-boundaries to learn more about error boundaries.';
    }

    var combinedMessage = "" + componentNameMessage + componentStack + "\n\n" + ("" + errorBoundaryMessage); // In development, we provide our own message with just the component stack.
    // We don't include the original error message and JS stack because the browser
    // has already printed it. Even if the application swallows the error, it is still
    // displayed by the browser thanks to the DEV-only fake event trick in ReactErrorUtils.

    console['error'](combinedMessage); // Don't transform to our wrapper
  }
}

var didWarnAboutUndefinedSnapshotBeforeUpdate = null;

{
  didWarnAboutUndefinedSnapshotBeforeUpdate = new Set();
}

var PossiblyWeakSet = typeof WeakSet === 'function' ? WeakSet : Set;
function logError(boundary, errorInfo) {
  var source = errorInfo.source;
  var stack = errorInfo.stack;

  if (stack === null && source !== null) {
    stack = getStackByFiberInDevAndProd(source);
  }

  var capturedError = {
    componentName: source !== null ? getComponentName(source.type) : null,
    componentStack: stack !== null ? stack : '',
    error: errorInfo.value,
    errorBoundary: null,
    errorBoundaryName: null,
    errorBoundaryFound: false,
    willRetry: false
  };

  if (boundary !== null && boundary.tag === ClassComponent) {
    capturedError.errorBoundary = boundary.stateNode;
    capturedError.errorBoundaryName = getComponentName(boundary.type);
    capturedError.errorBoundaryFound = true;
    capturedError.willRetry = true;
  }

  try {
    logCapturedError(capturedError);
  } catch (e) {
    // This method must not throw, or React internal state will get messed up.
    // If console.error is overridden, or logCapturedError() shows a dialog that throws,
    // we want to report this error outside of the normal stack as a last resort.
    // https://github.com/facebook/react/issues/13188
    setTimeout(function () {
      throw e;
    });
  }
}

var callComponentWillUnmountWithTimer = function (current, instance) {
  startPhaseTimer(current, 'componentWillUnmount');
  instance.props = current.memoizedProps;
  instance.state = current.memoizedState;
  instance.componentWillUnmount();
  stopPhaseTimer();
}; // Capture errors so they don't interrupt unmounting.


function safelyCallComponentWillUnmount(current, instance) {
  {
    invokeGuardedCallback(null, callComponentWillUnmountWithTimer, null, current, instance);

    if (hasCaughtError()) {
      var unmountError = clearCaughtError();
      captureCommitPhaseError(current, unmountError);
    }
  }
}

function safelyDetachRef(current) {
  var ref = current.ref;

  if (ref !== null) {
    if (typeof ref === 'function') {
      {
        invokeGuardedCallback(null, ref, null, null);

        if (hasCaughtError()) {
          var refError = clearCaughtError();
          captureCommitPhaseError(current, refError);
        }
      }
    } else {
      ref.current = null;
    }
  }
}

function safelyCallDestroy(current, destroy) {
  {
    invokeGuardedCallback(null, destroy, null);

    if (hasCaughtError()) {
      var error = clearCaughtError();
      captureCommitPhaseError(current, error);
    }
  }
}

function commitBeforeMutationLifeCycles(current, finishedWork) {
  switch (finishedWork.tag) {
    case FunctionComponent:
    case ForwardRef:
    case SimpleMemoComponent:
    case Block:
      {
        return;
      }

    case ClassComponent:
      {
        if (finishedWork.effectTag & Snapshot) {
          if (current !== null) {
            var prevProps = current.memoizedProps;
            var prevState = current.memoizedState;
            startPhaseTimer(finishedWork, 'getSnapshotBeforeUpdate');
            var instance = finishedWork.stateNode; // We could update instance props and state here,
            // but instead we rely on them being set during last render.
            // TODO: revisit this when we implement resuming.

            {
              if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                if (instance.props !== finishedWork.memoizedProps) {
                  error('Expected %s props to match memoized props before ' + 'getSnapshotBeforeUpdate. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance');
                }

                if (instance.state !== finishedWork.memoizedState) {
                  error('Expected %s state to match memoized state before ' + 'getSnapshotBeforeUpdate. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance');
                }
              }
            }

            var snapshot = instance.getSnapshotBeforeUpdate(finishedWork.elementType === finishedWork.type ? prevProps : resolveDefaultProps(finishedWork.type, prevProps), prevState);

            {
              var didWarnSet = didWarnAboutUndefinedSnapshotBeforeUpdate;

              if (snapshot === undefined && !didWarnSet.has(finishedWork.type)) {
                didWarnSet.add(finishedWork.type);

                error('%s.getSnapshotBeforeUpdate(): A snapshot value (or null) ' + 'must be returned. You have returned undefined.', getComponentName(finishedWork.type));
              }
            }

            instance.__reactInternalSnapshotBeforeUpdate = snapshot;
            stopPhaseTimer();
          }
        }

        return;
      }

    case HostRoot:
    case HostComponent:
    case HostText:
    case HostPortal:
    case IncompleteClassComponent:
      // Nothing to do for these component types
      return;
  }

  {
    {
      throw Error( "This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue." );
    }
  }
}

function commitHookEffectListUnmount(tag, finishedWork) {
  var updateQueue = finishedWork.updateQueue;
  var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;

  if (lastEffect !== null) {
    var firstEffect = lastEffect.next;
    var effect = firstEffect;

    do {
      if ((effect.tag & tag) === tag) {
        // Unmount
        var destroy = effect.destroy;
        effect.destroy = undefined;

        if (destroy !== undefined) {
          destroy();
        }
      }

      effect = effect.next;
    } while (effect !== firstEffect);
  }
}

function commitHookEffectListMount(tag, finishedWork) {
  var updateQueue = finishedWork.updateQueue;
  var lastEffect = updateQueue !== null ? updateQueue.lastEffect : null;

  if (lastEffect !== null) {
    var firstEffect = lastEffect.next;
    var effect = firstEffect;

    do {
      if ((effect.tag & tag) === tag) {
        // Mount
        var create = effect.create;
        effect.destroy = create();

        {
          var destroy = effect.destroy;

          if (destroy !== undefined && typeof destroy !== 'function') {
            var addendum = void 0;

            if (destroy === null) {
              addendum = ' You returned null. If your effect does not require clean ' + 'up, return undefined (or nothing).';
            } else if (typeof destroy.then === 'function') {
              addendum = '\n\nIt looks like you wrote useEffect(async () => ...) or returned a Promise. ' + 'Instead, write the async function inside your effect ' + 'and call it immediately:\n\n' + 'useEffect(() => {\n' + '  async function fetchData() {\n' + '    // You can await here\n' + '    const response = await MyAPI.getData(someId);\n' + '    // ...\n' + '  }\n' + '  fetchData();\n' + "}, [someId]); // Or [] if effect doesn't need props or state\n\n" + 'Learn more about data fetching with Hooks: https://fb.me/react-hooks-data-fetching';
            } else {
              addendum = ' You returned: ' + destroy;
            }

            error('An effect function must not return anything besides a function, ' + 'which is used for clean-up.%s%s', addendum, getStackByFiberInDevAndProd(finishedWork));
          }
        }
      }

      effect = effect.next;
    } while (effect !== firstEffect);
  }
}

function commitPassiveHookEffects(finishedWork) {
  if ((finishedWork.effectTag & Passive) !== NoEffect) {
    switch (finishedWork.tag) {
      case FunctionComponent:
      case ForwardRef:
      case SimpleMemoComponent:
      case Block:
        {
          // TODO (#17945) We should call all passive destroy functions (for all fibers)
          // before calling any create functions. The current approach only serializes
          // these for a single fiber.
          commitHookEffectListUnmount(Passive$1 | HasEffect, finishedWork);
          commitHookEffectListMount(Passive$1 | HasEffect, finishedWork);
          break;
        }
    }
  }
}

function commitLifeCycles(finishedRoot, current, finishedWork, committedExpirationTime) {
  switch (finishedWork.tag) {
    case FunctionComponent:
    case ForwardRef:
    case SimpleMemoComponent:
    case Block:
      {
        // At this point layout effects have already been destroyed (during mutation phase).
        // This is done to prevent sibling component effects from interfering with each other,
        // e.g. a destroy function in one component should never override a ref set
        // by a create function in another component during the same commit.
        commitHookEffectListMount(Layout | HasEffect, finishedWork);

        return;
      }

    case ClassComponent:
      {
        var instance = finishedWork.stateNode;

        if (finishedWork.effectTag & Update) {
          if (current === null) {
            startPhaseTimer(finishedWork, 'componentDidMount'); // We could update instance props and state here,
            // but instead we rely on them being set during last render.
            // TODO: revisit this when we implement resuming.

            {
              if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                if (instance.props !== finishedWork.memoizedProps) {
                  error('Expected %s props to match memoized props before ' + 'componentDidMount. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance');
                }

                if (instance.state !== finishedWork.memoizedState) {
                  error('Expected %s state to match memoized state before ' + 'componentDidMount. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance');
                }
              }
            }

            instance.componentDidMount();
            stopPhaseTimer();
          } else {
            var prevProps = finishedWork.elementType === finishedWork.type ? current.memoizedProps : resolveDefaultProps(finishedWork.type, current.memoizedProps);
            var prevState = current.memoizedState;
            startPhaseTimer(finishedWork, 'componentDidUpdate'); // We could update instance props and state here,
            // but instead we rely on them being set during last render.
            // TODO: revisit this when we implement resuming.

            {
              if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
                if (instance.props !== finishedWork.memoizedProps) {
                  error('Expected %s props to match memoized props before ' + 'componentDidUpdate. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance');
                }

                if (instance.state !== finishedWork.memoizedState) {
                  error('Expected %s state to match memoized state before ' + 'componentDidUpdate. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance');
                }
              }
            }

            instance.componentDidUpdate(prevProps, prevState, instance.__reactInternalSnapshotBeforeUpdate);
            stopPhaseTimer();
          }
        }

        var updateQueue = finishedWork.updateQueue;

        if (updateQueue !== null) {
          {
            if (finishedWork.type === finishedWork.elementType && !didWarnAboutReassigningProps) {
              if (instance.props !== finishedWork.memoizedProps) {
                error('Expected %s props to match memoized props before ' + 'processing the update queue. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance');
              }

              if (instance.state !== finishedWork.memoizedState) {
                error('Expected %s state to match memoized state before ' + 'processing the update queue. ' + 'This might either be because of a bug in React, or because ' + 'a component reassigns its own `this.props`. ' + 'Please file an issue.', getComponentName(finishedWork.type) || 'instance');
              }
            }
          } // We could update instance props and state here,
          // but instead we rely on them being set during last render.
          // TODO: revisit this when we implement resuming.


          commitUpdateQueue(finishedWork, updateQueue, instance);
        }

        return;
      }

    case HostRoot:
      {
        var _updateQueue = finishedWork.updateQueue;

        if (_updateQueue !== null) {
          var _instance = null;

          if (finishedWork.child !== null) {
            switch (finishedWork.child.tag) {
              case HostComponent:
                _instance = getPublicInstance(finishedWork.child.stateNode);
                break;

              case ClassComponent:
                _instance = finishedWork.child.stateNode;
                break;
            }
          }

          commitUpdateQueue(finishedWork, _updateQueue, _instance);
        }

        return;
      }

    case HostComponent:
      {
        var _instance2 = finishedWork.stateNode; // Renderers may schedule work to be done after host components are mounted
        // (eg DOM renderer may schedule auto-focus for inputs and form controls).
        // These effects should only be committed when components are first mounted,
        // aka when there is no current/alternate.

        if (current === null && finishedWork.effectTag & Update) {
          var type = finishedWork.type;
          var props = finishedWork.memoizedProps;
          commitMount(_instance2, type, props);
        }

        return;
      }

    case HostText:
      {
        // We have no life-cycles associated with text.
        return;
      }

    case HostPortal:
      {
        // We have no life-cycles associated with portals.
        return;
      }

    case Profiler:
      {
        {
          var onRender = finishedWork.memoizedProps.onRender;

          if (typeof onRender === 'function') {
            {
              onRender(finishedWork.memoizedProps.id, current === null ? 'mount' : 'update', finishedWork.actualDuration, finishedWork.treeBaseDuration, finishedWork.actualStartTime, getCommitTime(), finishedRoot.memoizedInteractions);
            }
          }
        }

        return;
      }

    case SuspenseComponent:
      {
        commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
        return;
      }

    case SuspenseListComponent:
    case IncompleteClassComponent:
    case FundamentalComponent:
    case ScopeComponent:
      return;
  }

  {
    {
      throw Error( "This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue." );
    }
  }
}

function hideOrUnhideAllChildren(finishedWork, isHidden) {
  {
    // We only have the top Fiber that was inserted but we need to recurse down its
    // children to find all the terminal nodes.
    var node = finishedWork;

    while (true) {
      if (node.tag === HostComponent) {
        var instance = node.stateNode;

        if (isHidden) {
          hideInstance(instance);
        } else {
          unhideInstance(node.stateNode, node.memoizedProps);
        }
      } else if (node.tag === HostText) {
        var _instance3 = node.stateNode;

        if (isHidden) {
          hideTextInstance(_instance3);
        } else {
          unhideTextInstance(_instance3, node.memoizedProps);
        }
      } else if (node.tag === SuspenseComponent && node.memoizedState !== null && node.memoizedState.dehydrated === null) {
        // Found a nested Suspense component that timed out. Skip over the
        // primary child fragment, which should remain hidden.
        var fallbackChildFragment = node.child.sibling;
        fallbackChildFragment.return = node;
        node = fallbackChildFragment;
        continue;
      } else if (node.child !== null) {
        node.child.return = node;
        node = node.child;
        continue;
      }

      if (node === finishedWork) {
        return;
      }

      while (node.sibling === null) {
        if (node.return === null || node.return === finishedWork) {
          return;
        }

        node = node.return;
      }

      node.sibling.return = node.return;
      node = node.sibling;
    }
  }
}

function commitAttachRef(finishedWork) {
  var ref = finishedWork.ref;

  if (ref !== null) {
    var instance = finishedWork.stateNode;
    var instanceToUse;

    switch (finishedWork.tag) {
      case HostComponent:
        instanceToUse = getPublicInstance(instance);
        break;

      default:
        instanceToUse = instance;
    } // Moved outside to ensure DCE works with this flag

    if (typeof ref === 'function') {
      ref(instanceToUse);
    } else {
      {
        if (!ref.hasOwnProperty('current')) {
          error('Unexpected ref object provided for %s. ' + 'Use either a ref-setter function or React.createRef().%s', getComponentName(finishedWork.type), getStackByFiberInDevAndProd(finishedWork));
        }
      }

      ref.current = instanceToUse;
    }
  }
}

function commitDetachRef(current) {
  var currentRef = current.ref;

  if (currentRef !== null) {
    if (typeof currentRef === 'function') {
      currentRef(null);
    } else {
      currentRef.current = null;
    }
  }
} // User-originating errors (lifecycles and refs) should not interrupt
// deletion, so don't let them throw. Host-originating errors should
// interrupt deletion, so it's okay


function commitUnmount(finishedRoot, current, renderPriorityLevel) {
  onCommitUnmount(current);

  switch (current.tag) {
    case FunctionComponent:
    case ForwardRef:
    case MemoComponent:
    case SimpleMemoComponent:
    case Block:
      {
        var updateQueue = current.updateQueue;

        if (updateQueue !== null) {
          var lastEffect = updateQueue.lastEffect;

          if (lastEffect !== null) {
            var firstEffect = lastEffect.next;

            {
              // When the owner fiber is deleted, the destroy function of a passive
              // effect hook is called during the synchronous commit phase. This is
              // a concession to implementation complexity. Calling it in the
              // passive effect phase (like they usually are, when dependencies
              // change during an update) would require either traversing the
              // children of the deleted fiber again, or including unmount effects
              // as part of the fiber effect list.
              //
              // Because this is during the sync commit phase, we need to change
              // the priority.
              //
              // TODO: Reconsider this implementation trade off.
              var priorityLevel = renderPriorityLevel > NormalPriority ? NormalPriority : renderPriorityLevel;
              runWithPriority$1(priorityLevel, function () {
                var effect = firstEffect;

                do {
                  var _destroy = effect.destroy;

                  if (_destroy !== undefined) {
                    safelyCallDestroy(current, _destroy);
                  }

                  effect = effect.next;
                } while (effect !== firstEffect);
              });
            }
          }
        }

        return;
      }

    case ClassComponent:
      {
        safelyDetachRef(current);
        var instance = current.stateNode;

        if (typeof instance.componentWillUnmount === 'function') {
          safelyCallComponentWillUnmount(current, instance);
        }

        return;
      }

    case HostComponent:
      {

        safelyDetachRef(current);
        return;
      }

    case HostPortal:
      {
        // TODO: this is recursive.
        // We are also not using this parent because
        // the portal will get pushed immediately.
        {
          unmountHostComponents(finishedRoot, current, renderPriorityLevel);
        }

        return;
      }

    case FundamentalComponent:
      {

        return;
      }

    case DehydratedFragment:
      {

        return;
      }

    case ScopeComponent:
      {

        return;
      }
  }
}

function commitNestedUnmounts(finishedRoot, root, renderPriorityLevel) {
  // While we're inside a removed host node we don't want to call
  // removeChild on the inner nodes because they're removed by the top
  // call anyway. We also want to call componentWillUnmount on all
  // composites before this host node is removed from the tree. Therefore
  // we do an inner loop while we're still inside the host node.
  var node = root;

  while (true) {
    commitUnmount(finishedRoot, node, renderPriorityLevel); // Visit children because they may contain more composite or host nodes.
    // Skip portals because commitUnmount() currently visits them recursively.

    if (node.child !== null && ( // If we use mutation we drill down into portals using commitUnmount above.
    // If we don't use mutation we drill down into portals here instead.
     node.tag !== HostPortal)) {
      node.child.return = node;
      node = node.child;
      continue;
    }

    if (node === root) {
      return;
    }

    while (node.sibling === null) {
      if (node.return === null || node.return === root) {
        return;
      }

      node = node.return;
    }

    node.sibling.return = node.return;
    node = node.sibling;
  }
}

function detachFiber(current) {
  var alternate = current.alternate; // Cut off the return pointers to disconnect it from the tree. Ideally, we
  // should clear the child pointer of the parent alternate to let this
  // get GC:ed but we don't know which for sure which parent is the current
  // one so we'll settle for GC:ing the subtree of this child. This child
  // itself will be GC:ed when the parent updates the next time.

  current.return = null;
  current.child = null;
  current.memoizedState = null;
  current.updateQueue = null;
  current.dependencies = null;
  current.alternate = null;
  current.firstEffect = null;
  current.lastEffect = null;
  current.pendingProps = null;
  current.memoizedProps = null;
  current.stateNode = null;

  if (alternate !== null) {
    detachFiber(alternate);
  }
}

function getHostParentFiber(fiber) {
  var parent = fiber.return;

  while (parent !== null) {
    if (isHostParent(parent)) {
      return parent;
    }

    parent = parent.return;
  }

  {
    {
      throw Error( "Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue." );
    }
  }
}

function isHostParent(fiber) {
  return fiber.tag === HostComponent || fiber.tag === HostRoot || fiber.tag === HostPortal;
}

function getHostSibling(fiber) {
  // We're going to search forward into the tree until we find a sibling host
  // node. Unfortunately, if multiple insertions are done in a row we have to
  // search past them. This leads to exponential search for the next sibling.
  // TODO: Find a more efficient way to do this.
  var node = fiber;

  siblings: while (true) {
    // If we didn't find anything, let's try the next sibling.
    while (node.sibling === null) {
      if (node.return === null || isHostParent(node.return)) {
        // If we pop out of the root or hit the parent the fiber we are the
        // last sibling.
        return null;
      }

      node = node.return;
    }

    node.sibling.return = node.return;
    node = node.sibling;

    while (node.tag !== HostComponent && node.tag !== HostText && node.tag !== DehydratedFragment) {
      // If it is not host node and, we might have a host node inside it.
      // Try to search down until we find one.
      if (node.effectTag & Placement) {
        // If we don't have a child, try the siblings instead.
        continue siblings;
      } // If we don't have a child, try the siblings instead.
      // We also skip portals because they are not part of this host tree.


      if (node.child === null || node.tag === HostPortal) {
        continue siblings;
      } else {
        node.child.return = node;
        node = node.child;
      }
    } // Check if this host node is stable or about to be placed.


    if (!(node.effectTag & Placement)) {
      // Found it!
      return node.stateNode;
    }
  }
}

function commitPlacement(finishedWork) {


  var parentFiber = getHostParentFiber(finishedWork); // Note: these two variables *must* always be updated together.

  var parent;
  var isContainer;
  var parentStateNode = parentFiber.stateNode;

  switch (parentFiber.tag) {
    case HostComponent:
      parent = parentStateNode;
      isContainer = false;
      break;

    case HostRoot:
      parent = parentStateNode.containerInfo;
      isContainer = true;
      break;

    case HostPortal:
      parent = parentStateNode.containerInfo;
      isContainer = true;
      break;

    case FundamentalComponent:

    // eslint-disable-next-line-no-fallthrough

    default:
      {
        {
          throw Error( "Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue." );
        }
      }

  }

  if (parentFiber.effectTag & ContentReset) {
    // Reset the text content of the parent before doing any insertions
    resetTextContent(parent); // Clear ContentReset from the effect tag

    parentFiber.effectTag &= ~ContentReset;
  }

  var before = getHostSibling(finishedWork); // We only have the top Fiber that was inserted but we need to recurse down its
  // children to find all the terminal nodes.

  if (isContainer) {
    insertOrAppendPlacementNodeIntoContainer(finishedWork, before, parent);
  } else {
    insertOrAppendPlacementNode(finishedWork, before, parent);
  }
}

function insertOrAppendPlacementNodeIntoContainer(node, before, parent) {
  var tag = node.tag;
  var isHost = tag === HostComponent || tag === HostText;

  if (isHost || enableFundamentalAPI ) {
    var stateNode = isHost ? node.stateNode : node.stateNode.instance;

    if (before) {
      insertInContainerBefore(parent, stateNode, before);
    } else {
      appendChildToContainer(parent, stateNode);
    }
  } else if (tag === HostPortal) ; else {
    var child = node.child;

    if (child !== null) {
      insertOrAppendPlacementNodeIntoContainer(child, before, parent);
      var sibling = child.sibling;

      while (sibling !== null) {
        insertOrAppendPlacementNodeIntoContainer(sibling, before, parent);
        sibling = sibling.sibling;
      }
    }
  }
}

function insertOrAppendPlacementNode(node, before, parent) {
  var tag = node.tag;
  var isHost = tag === HostComponent || tag === HostText;

  if (isHost || enableFundamentalAPI ) {
    var stateNode = isHost ? node.stateNode : node.stateNode.instance;

    if (before) {
      insertBefore(parent, stateNode, before);
    } else {
      appendChild(parent, stateNode);
    }
  } else if (tag === HostPortal) ; else {
    var child = node.child;

    if (child !== null) {
      insertOrAppendPlacementNode(child, before, parent);
      var sibling = child.sibling;

      while (sibling !== null) {
        insertOrAppendPlacementNode(sibling, before, parent);
        sibling = sibling.sibling;
      }
    }
  }
}

function unmountHostComponents(finishedRoot, current, renderPriorityLevel) {
  // We only have the top Fiber that was deleted but we need to recurse down its
  // children to find all the terminal nodes.
  var node = current; // Each iteration, currentParent is populated with node's host parent if not
  // currentParentIsValid.

  var currentParentIsValid = false; // Note: these two variables *must* always be updated together.

  var currentParent;
  var currentParentIsContainer;

  while (true) {
    if (!currentParentIsValid) {
      var parent = node.return;

      findParent: while (true) {
        if (!(parent !== null)) {
          {
            throw Error( "Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue." );
          }
        }

        var parentStateNode = parent.stateNode;

        switch (parent.tag) {
          case HostComponent:
            currentParent = parentStateNode;
            currentParentIsContainer = false;
            break findParent;

          case HostRoot:
            currentParent = parentStateNode.containerInfo;
            currentParentIsContainer = true;
            break findParent;

          case HostPortal:
            currentParent = parentStateNode.containerInfo;
            currentParentIsContainer = true;
            break findParent;

        }

        parent = parent.return;
      }

      currentParentIsValid = true;
    }

    if (node.tag === HostComponent || node.tag === HostText) {
      commitNestedUnmounts(finishedRoot, node, renderPriorityLevel); // After all the children have unmounted, it is now safe to remove the
      // node from the tree.

      if (currentParentIsContainer) {
        removeChildFromContainer(currentParent, node.stateNode);
      } else {
        removeChild(currentParent, node.stateNode);
      } // Don't visit children because we already visited them.

    } else if (node.tag === HostPortal) {
      if (node.child !== null) {
        // When we go into a portal, it becomes the parent to remove from.
        // We will reassign it back when we pop the portal on the way up.
        currentParent = node.stateNode.containerInfo;
        currentParentIsContainer = true; // Visit children because portals might contain host components.

        node.child.return = node;
        node = node.child;
        continue;
      }
    } else {
      commitUnmount(finishedRoot, node, renderPriorityLevel); // Visit children because we may find more host components below.

      if (node.child !== null) {
        node.child.return = node;
        node = node.child;
        continue;
      }
    }

    if (node === current) {
      return;
    }

    while (node.sibling === null) {
      if (node.return === null || node.return === current) {
        return;
      }

      node = node.return;

      if (node.tag === HostPortal) {
        // When we go out of the portal, we need to restore the parent.
        // Since we don't keep a stack of them, we will search for it.
        currentParentIsValid = false;
      }
    }

    node.sibling.return = node.return;
    node = node.sibling;
  }
}

function commitDeletion(finishedRoot, current, renderPriorityLevel) {
  {
    // Recursively delete all host nodes from the parent.
    // Detach refs and call componentWillUnmount() on the whole subtree.
    unmountHostComponents(finishedRoot, current, renderPriorityLevel);
  }

  detachFiber(current);
}

function commitWork(current, finishedWork) {

  switch (finishedWork.tag) {
    case FunctionComponent:
    case ForwardRef:
    case MemoComponent:
    case SimpleMemoComponent:
    case Block:
      {
        // Layout effects are destroyed during the mutation phase so that all
        // destroy functions for all fibers are called before any create functions.
        // This prevents sibling component effects from interfering with each other,
        // e.g. a destroy function in one component should never override a ref set
        // by a create function in another component during the same commit.
        commitHookEffectListUnmount(Layout | HasEffect, finishedWork);
        return;
      }

    case ClassComponent:
      {
        return;
      }

    case HostComponent:
      {
        var instance = finishedWork.stateNode;

        if (instance != null) {
          // Commit the work prepared earlier.
          var newProps = finishedWork.memoizedProps; // For hydration we reuse the update path but we treat the oldProps
          // as the newProps. The updatePayload will contain the real change in
          // this case.

          var oldProps = current !== null ? current.memoizedProps : newProps;
          var type = finishedWork.type; // TODO: Type the updateQueue to be specific to host components.

          var updatePayload = finishedWork.updateQueue;
          finishedWork.updateQueue = null;

          if (updatePayload !== null) {
            commitUpdate(instance, updatePayload, type, oldProps, newProps);
          }
        }

        return;
      }

    case HostText:
      {
        if (!(finishedWork.stateNode !== null)) {
          {
            throw Error( "This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue." );
          }
        }

        var textInstance = finishedWork.stateNode;
        var newText = finishedWork.memoizedProps; // For hydration we reuse the update path but we treat the oldProps
        // as the newProps. The updatePayload will contain the real change in
        // this case.

        var oldText = current !== null ? current.memoizedProps : newText;
        commitTextUpdate(textInstance, oldText, newText);
        return;
      }

    case HostRoot:
      {
        {
          var _root = finishedWork.stateNode;

          if (_root.hydrate) {
            // We've just hydrated. No need to hydrate again.
            _root.hydrate = false;
            commitHydratedContainer(_root.containerInfo);
          }
        }

        return;
      }

    case Profiler:
      {
        return;
      }

    case SuspenseComponent:
      {
        commitSuspenseComponent(finishedWork);
        attachSuspenseRetryListeners(finishedWork);
        return;
      }

    case SuspenseListComponent:
      {
        attachSuspenseRetryListeners(finishedWork);
        return;
      }

    case IncompleteClassComponent:
      {
        return;
      }
  }

  {
    {
      throw Error( "This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue." );
    }
  }
}

function commitSuspenseComponent(finishedWork) {
  var newState = finishedWork.memoizedState;
  var newDidTimeout;
  var primaryChildParent = finishedWork;

  if (newState === null) {
    newDidTimeout = false;
  } else {
    newDidTimeout = true;
    primaryChildParent = finishedWork.child;
    markCommitTimeOfFallback();
  }

  if ( primaryChildParent !== null) {
    hideOrUnhideAllChildren(primaryChildParent, newDidTimeout);
  }
}

function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {

  var newState = finishedWork.memoizedState;

  if (newState === null) {
    var current = finishedWork.alternate;

    if (current !== null) {
      var prevState = current.memoizedState;

      if (prevState !== null) {
        var suspenseInstance = prevState.dehydrated;

        if (suspenseInstance !== null) {
          commitHydratedSuspenseInstance(suspenseInstance);
        }
      }
    }
  }
}

function attachSuspenseRetryListeners(finishedWork) {
  // If this boundary just timed out, then it will have a set of thenables.
  // For each thenable, attach a listener so that when it resolves, React
  // attempts to re-render the boundary in the primary (pre-timeout) state.
  var thenables = finishedWork.updateQueue;

  if (thenables !== null) {
    finishedWork.updateQueue = null;
    var retryCache = finishedWork.stateNode;

    if (retryCache === null) {
      retryCache = finishedWork.stateNode = new PossiblyWeakSet();
    }

    thenables.forEach(function (thenable) {
      // Memoize using the boundary fiber to prevent redundant listeners.
      var retry = resolveRetryThenable.bind(null, finishedWork, thenable);

      if (!retryCache.has(thenable)) {
        {
          if (thenable.__reactDoNotTraceInteractions !== true) {
            retry = tracing.unstable_wrap(retry);
          }
        }

        retryCache.add(thenable);
        thenable.then(retry, retry);
      }
    });
  }
}

function commitResetTextContent(current) {

  resetTextContent(current.stateNode);
}

var PossiblyWeakMap$1 = typeof WeakMap === 'function' ? WeakMap : Map;

function createRootErrorUpdate(fiber, errorInfo, expirationTime) {
  var update = createUpdate(expirationTime, null); // Unmount the root by rendering null.

  update.tag = CaptureUpdate; // Caution: React DevTools currently depends on this property
  // being called "element".

  update.payload = {
    element: null
  };
  var error = errorInfo.value;

  update.callback = function () {
    onUncaughtError(error);
    logError(fiber, errorInfo);
  };

  return update;
}

function createClassErrorUpdate(fiber, errorInfo, expirationTime) {
  var update = createUpdate(expirationTime, null);
  update.tag = CaptureUpdate;
  var getDerivedStateFromError = fiber.type.getDerivedStateFromError;

  if (typeof getDerivedStateFromError === 'function') {
    var error$1 = errorInfo.value;

    update.payload = function () {
      logError(fiber, errorInfo);
      return getDerivedStateFromError(error$1);
    };
  }

  var inst = fiber.stateNode;

  if (inst !== null && typeof inst.componentDidCatch === 'function') {
    update.callback = function callback() {
      {
        markFailedErrorBoundaryForHotReloading(fiber);
      }

      if (typeof getDerivedStateFromError !== 'function') {
        // To preserve the preexisting retry behavior of error boundaries,
        // we keep track of which ones already failed during this batch.
        // This gets reset before we yield back to the browser.
        // TODO: Warn in strict mode if getDerivedStateFromError is
        // not defined.
        markLegacyErrorBoundaryAsFailed(this); // Only log here if componentDidCatch is the only error boundary method defined

        logError(fiber, errorInfo);
      }

      var error$1 = errorInfo.value;
      var stack = errorInfo.stack;
      this.componentDidCatch(error$1, {
        componentStack: stack !== null ? stack : ''
      });

      {
        if (typeof getDerivedStateFromError !== 'function') {
          // If componentDidCatch is the only error boundary method defined,
          // then it needs to call setState to recover from errors.
          // If no state update is scheduled then the boundary will swallow the error.
          if (fiber.expirationTime !== Sync) {
            error('%s: Error boundaries should implement getDerivedStateFromError(). ' + 'In that method, return a state update to display an error message or fallback UI.', getComponentName(fiber.type) || 'Unknown');
          }
        }
      }
    };
  } else {
    update.callback = function () {
      markFailedErrorBoundaryForHotReloading(fiber);
    };
  }

  return update;
}

function attachPingListener(root, renderExpirationTime, thenable) {
  // Attach a listener to the promise to "ping" the root and retry. But
  // only if one does not already exist for the current render expiration
  // time (which acts like a "thread ID" here).
  var pingCache = root.pingCache;
  var threadIDs;

  if (pingCache === null) {
    pingCache = root.pingCache = new PossiblyWeakMap$1();
    threadIDs = new Set();
    pingCache.set(thenable, threadIDs);
  } else {
    threadIDs = pingCache.get(thenable);

    if (threadIDs === undefined) {
      threadIDs = new Set();
      pingCache.set(thenable, threadIDs);
    }
  }

  if (!threadIDs.has(renderExpirationTime)) {
    // Memoize using the thread ID to prevent redundant listeners.
    threadIDs.add(renderExpirationTime);
    var ping = pingSuspendedRoot.bind(null, root, thenable, renderExpirationTime);
    thenable.then(ping, ping);
  }
}

function throwException(root, returnFiber, sourceFiber, value, renderExpirationTime) {
  // The source fiber did not complete.
  sourceFiber.effectTag |= Incomplete; // Its effect list is no longer valid.

  sourceFiber.firstEffect = sourceFiber.lastEffect = null;

  if (value !== null && typeof value === 'object' && typeof value.then === 'function') {
    // This is a thenable.
    var thenable = value;

    if ((sourceFiber.mode & BlockingMode) === NoMode) {
      // Reset the memoizedState to what it was before we attempted
      // to render it.
      var currentSource = sourceFiber.alternate;

      if (currentSource) {
        sourceFiber.memoizedState = currentSource.memoizedState;
        sourceFiber.expirationTime = currentSource.expirationTime;
      } else {
        sourceFiber.memoizedState = null;
      }
    }

    var hasInvisibleParentBoundary = hasSuspenseContext(suspenseStackCursor.current, InvisibleParentSuspenseContext); // Schedule the nearest Suspense to re-render the timed out view.

    var _workInProgress = returnFiber;

    do {
      if (_workInProgress.tag === SuspenseComponent && shouldCaptureSuspense(_workInProgress, hasInvisibleParentBoundary)) {
        // Found the nearest boundary.
        // Stash the promise on the boundary fiber. If the boundary times out, we'll
        // attach another listener to flip the boundary back to its normal state.
        var thenables = _workInProgress.updateQueue;

        if (thenables === null) {
          var updateQueue = new Set();
          updateQueue.add(thenable);
          _workInProgress.updateQueue = updateQueue;
        } else {
          thenables.add(thenable);
        } // If the boundary is outside of blocking mode, we should *not*
        // suspend the commit. Pretend as if the suspended component rendered
        // null and keep rendering. In the commit phase, we'll schedule a
        // subsequent synchronous update to re-render the Suspense.
        //
        // Note: It doesn't matter whether the component that suspended was
        // inside a blocking mode tree. If the Suspense is outside of it, we
        // should *not* suspend the commit.


        if ((_workInProgress.mode & BlockingMode) === NoMode) {
          _workInProgress.effectTag |= DidCapture; // We're going to commit this fiber even though it didn't complete.
          // But we shouldn't call any lifecycle methods or callbacks. Remove
          // all lifecycle effect tags.

          sourceFiber.effectTag &= ~(LifecycleEffectMask | Incomplete);

          if (sourceFiber.tag === ClassComponent) {
            var currentSourceFiber = sourceFiber.alternate;

            if (currentSourceFiber === null) {
              // This is a new mount. Change the tag so it's not mistaken for a
              // completed class component. For example, we should not call
              // componentWillUnmount if it is deleted.
              sourceFiber.tag = IncompleteClassComponent;
            } else {
              // When we try rendering again, we should not reuse the current fiber,
              // since it's known to be in an inconsistent state. Use a force update to
              // prevent a bail out.
              var update = createUpdate(Sync, null);
              update.tag = ForceUpdate;
              enqueueUpdate(sourceFiber, update);
            }
          } // The source fiber did not complete. Mark it with Sync priority to
          // indicate that it still has pending work.


          sourceFiber.expirationTime = Sync; // Exit without suspending.

          return;
        } // Confirmed that the boundary is in a concurrent mode tree. Continue
        // with the normal suspend path.
        //
        // After this we'll use a set of heuristics to determine whether this
        // render pass will run to completion or restart or "suspend" the commit.
        // The actual logic for this is spread out in different places.
        //
        // This first principle is that if we're going to suspend when we complete
        // a root, then we should also restart if we get an update or ping that
        // might unsuspend it, and vice versa. The only reason to suspend is
        // because you think you might want to restart before committing. However,
        // it doesn't make sense to restart only while in the period we're suspended.
        //
        // Restarting too aggressively is also not good because it starves out any
        // intermediate loading state. So we use heuristics to determine when.
        // Suspense Heuristics
        //
        // If nothing threw a Promise or all the same fallbacks are already showing,
        // then don't suspend/restart.
        //
        // If this is an initial render of a new tree of Suspense boundaries and
        // those trigger a fallback, then don't suspend/restart. We want to ensure
        // that we can show the initial loading state as quickly as possible.
        //
        // If we hit a "Delayed" case, such as when we'd switch from content back into
        // a fallback, then we should always suspend/restart. SuspenseConfig applies to
        // this case. If none is defined, JND is used instead.
        //
        // If we're already showing a fallback and it gets "retried", allowing us to show
        // another level, but there's still an inner boundary that would show a fallback,
        // then we suspend/restart for 500ms since the last time we showed a fallback
        // anywhere in the tree. This effectively throttles progressive loading into a
        // consistent train of commits. This also gives us an opportunity to restart to
        // get to the completed state slightly earlier.
        //
        // If there's ambiguity due to batching it's resolved in preference of:
        // 1) "delayed", 2) "initial render", 3) "retry".
        //
        // We want to ensure that a "busy" state doesn't get force committed. We want to
        // ensure that new initial loading states can commit as soon as possible.


        attachPingListener(root, renderExpirationTime, thenable);
        _workInProgress.effectTag |= ShouldCapture;
        _workInProgress.expirationTime = renderExpirationTime;
        return;
      } // This boundary already captured during this render. Continue to the next
      // boundary.


      _workInProgress = _workInProgress.return;
    } while (_workInProgress !== null); // No boundary was found. Fallthrough to error mode.
    // TODO: Use invariant so the message is stripped in prod?


    value = new Error((getComponentName(sourceFiber.type) || 'A React component') + ' suspended while rendering, but no fallback UI was specified.\n' + '\n' + 'Add a <Suspense fallback=...> component higher in the tree to ' + 'provide a loading indicator or placeholder to display.' + getStackByFiberInDevAndProd(sourceFiber));
  } // We didn't find a boundary that could handle this type of exception. Start
  // over and traverse parent path again, this time treating the exception
  // as an error.


  renderDidError();
  value = createCapturedValue(value, sourceFiber);
  var workInProgress = returnFiber;

  do {
    switch (workInProgress.tag) {
      case HostRoot:
        {
          var _errorInfo = value;
          workInProgress.effectTag |= ShouldCapture;
          workInProgress.expirationTime = renderExpirationTime;

          var _update = createRootErrorUpdate(workInProgress, _errorInfo, renderExpirationTime);

          enqueueCapturedUpdate(workInProgress, _update);
          return;
        }

      case ClassComponent:
        // Capture and retry
        var errorInfo = value;
        var ctor = workInProgress.type;
        var instance = workInProgress.stateNode;

        if ((workInProgress.effectTag & DidCapture) === NoEffect && (typeof ctor.getDerivedStateFromError === 'function' || instance !== null && typeof instance.componentDidCatch === 'function' && !isAlreadyFailedLegacyErrorBoundary(instance))) {
          workInProgress.effectTag |= ShouldCapture;
          workInProgress.expirationTime = renderExpirationTime; // Schedule the error boundary to re-render using updated state

          var _update2 = createClassErrorUpdate(workInProgress, errorInfo, renderExpirationTime);

          enqueueCapturedUpdate(workInProgress, _update2);
          return;
        }

        break;
    }

    workInProgress = workInProgress.return;
  } while (workInProgress !== null);
}

var ceil = Math.ceil;
var ReactCurrentDispatcher$1 = ReactSharedInternals.ReactCurrentDispatcher,
    ReactCurrentOwner$2 = ReactSharedInternals.ReactCurrentOwner,
    IsSomeRendererActing = ReactSharedInternals.IsSomeRendererActing;
var NoContext =
/*                    */
0;
var BatchedContext =
/*               */
1;
var EventContext =
/*                 */
2;
var DiscreteEventContext =
/*         */
4;
var LegacyUnbatchedContext =
/*       */
8;
var RenderContext =
/*                */
16;
var CommitContext =
/*                */
32;
var RootIncomplete = 0;
var RootFatalErrored = 1;
var RootErrored = 2;
var RootSuspended = 3;
var RootSuspendedWithDelay = 4;
var RootCompleted = 5;
// Describes where we are in the React execution stack
var executionContext = NoContext; // The root we're working on

var workInProgressRoot = null; // The fiber we're working on

var workInProgress = null; // The expiration time we're rendering

var renderExpirationTime$1 = NoWork; // Whether to root completed, errored, suspended, etc.

var workInProgressRootExitStatus = RootIncomplete; // A fatal error, if one is thrown

var workInProgressRootFatalError = null; // Most recent event time among processed updates during this render.
// This is conceptually a time stamp but expressed in terms of an ExpirationTime
// because we deal mostly with expiration times in the hot path, so this avoids
// the conversion happening in the hot path.

var workInProgressRootLatestProcessedExpirationTime = Sync;
var workInProgressRootLatestSuspenseTimeout = Sync;
var workInProgressRootCanSuspendUsingConfig = null; // The work left over by components that were visited during this render. Only
// includes unprocessed updates, not work in bailed out children.

var workInProgressRootNextUnprocessedUpdateTime = NoWork; // If we're pinged while rendering we don't always restart immediately.
// This flag determines if it might be worthwhile to restart if an opportunity
// happens latere.

var workInProgressRootHasPendingPing = false; // The most recent time we committed a fallback. This lets us ensure a train
// model where we don't commit new loading states in too quick succession.

var globalMostRecentFallbackTime = 0;
var FALLBACK_THROTTLE_MS = 500;
var nextEffect = null;
var hasUncaughtError = false;
var firstUncaughtError = null;
var legacyErrorBoundariesThatAlreadyFailed = null;
var rootDoesHavePassiveEffects = false;
var rootWithPendingPassiveEffects = null;
var pendingPassiveEffectsRenderPriority = NoPriority;
var pendingPassiveEffectsExpirationTime = NoWork;
var rootsWithPendingDiscreteUpdates = null; // Use these to prevent an infinite loop of nested updates

var NESTED_UPDATE_LIMIT = 50;
var nestedUpdateCount = 0;
var rootWithNestedUpdates = null;
var NESTED_PASSIVE_UPDATE_LIMIT = 50;
var nestedPassiveUpdateCount = 0;
var interruptedBy = null; // Marks the need to reschedule pending interactions at these expiration times
// during the commit phase. This enables them to be traced across components
// that spawn new work during render. E.g. hidden boundaries, suspended SSR
// hydration or SuspenseList.

var spawnedWorkDuringRender = null; // Expiration times are computed by adding to the current time (the start
// time). However, if two updates are scheduled within the same event, we
// should treat their start times as simultaneous, even if the actual clock
// time has advanced between the first and second call.
// In other words, because expiration times determine how updates are batched,
// we want all updates of like priority that occur within the same event to
// receive the same expiration time. Otherwise we get tearing.

var currentEventTime = NoWork;
function requestCurrentTimeForUpdate() {
  if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
    // We're inside React, so it's fine to read the actual time.
    return msToExpirationTime(now());
  } // We're not inside React, so we may be in the middle of a browser event.


  if (currentEventTime !== NoWork) {
    // Use the same start time for all updates until we enter React again.
    return currentEventTime;
  } // This is the first update since React yielded. Compute a new start time.


  currentEventTime = msToExpirationTime(now());
  return currentEventTime;
}
function getCurrentTime() {
  return msToExpirationTime(now());
}
function computeExpirationForFiber(currentTime, fiber, suspenseConfig) {
  var mode = fiber.mode;

  if ((mode & BlockingMode) === NoMode) {
    return Sync;
  }

  var priorityLevel = getCurrentPriorityLevel();

  if ((mode & ConcurrentMode) === NoMode) {
    return priorityLevel === ImmediatePriority ? Sync : Batched;
  }

  if ((executionContext & RenderContext) !== NoContext) {
    // Use whatever time we're already rendering
    // TODO: Should there be a way to opt out, like with `runWithPriority`?
    return renderExpirationTime$1;
  }

  var expirationTime;

  if (suspenseConfig !== null) {
    // Compute an expiration time based on the Suspense timeout.
    expirationTime = computeSuspenseExpiration(currentTime, suspenseConfig.timeoutMs | 0 || LOW_PRIORITY_EXPIRATION);
  } else {
    // Compute an expiration time based on the Scheduler priority.
    switch (priorityLevel) {
      case ImmediatePriority:
        expirationTime = Sync;
        break;

      case UserBlockingPriority$1:
        // TODO: Rename this to computeUserBlockingExpiration
        expirationTime = computeInteractiveExpiration(currentTime);
        break;

      case NormalPriority:
      case LowPriority:
        // TODO: Handle LowPriority
        // TODO: Rename this to... something better.
        expirationTime = computeAsyncExpiration(currentTime);
        break;

      case IdlePriority:
        expirationTime = Idle;
        break;

      default:
        {
          {
            throw Error( "Expected a valid priority level" );
          }
        }

    }
  } // If we're in the middle of rendering a tree, do not update at the same
  // expiration time that is already rendering.
  // TODO: We shouldn't have to do this if the update is on a different root.
  // Refactor computeExpirationForFiber + scheduleUpdate so we have access to
  // the root when we check for this condition.


  if (workInProgressRoot !== null && expirationTime === renderExpirationTime$1) {
    // This is a trick to move this update into a separate batch
    expirationTime -= 1;
  }

  return expirationTime;
}
function scheduleUpdateOnFiber(fiber, expirationTime) {
  checkForNestedUpdates();
  warnAboutRenderPhaseUpdatesInDEV(fiber);
  var root = markUpdateTimeFromFiberToRoot(fiber, expirationTime);

  if (root === null) {
    warnAboutUpdateOnUnmountedFiberInDEV(fiber);
    return;
  }

  checkForInterruption(fiber, expirationTime);
  recordScheduleUpdate(); // TODO: computeExpirationForFiber also reads the priority. Pass the
  // priority as an argument to that function and this one.

  var priorityLevel = getCurrentPriorityLevel();

  if (expirationTime === Sync) {
    if ( // Check if we're inside unbatchedUpdates
    (executionContext & LegacyUnbatchedContext) !== NoContext && // Check if we're not already rendering
    (executionContext & (RenderContext | CommitContext)) === NoContext) {
      // Register pending interactions on the root to avoid losing traced interaction data.
      schedulePendingInteractions(root, expirationTime); // This is a legacy edge case. The initial mount of a ReactDOM.render-ed
      // root inside of batchedUpdates should be synchronous, but layout updates
      // should be deferred until the end of the batch.

      performSyncWorkOnRoot(root);
    } else {
      ensureRootIsScheduled(root);
      schedulePendingInteractions(root, expirationTime);

      if (executionContext === NoContext) {
        // Flush the synchronous work now, unless we're already working or inside
        // a batch. This is intentionally inside scheduleUpdateOnFiber instead of
        // scheduleCallbackForFiber to preserve the ability to schedule a callback
        // without immediately flushing it. We only do this for user-initiated
        // updates, to preserve historical behavior of legacy mode.
        flushSyncCallbackQueue();
      }
    }
  } else {
    ensureRootIsScheduled(root);
    schedulePendingInteractions(root, expirationTime);
  }

  if ((executionContext & DiscreteEventContext) !== NoContext && ( // Only updates at user-blocking priority or greater are considered
  // discrete, even inside a discrete event.
  priorityLevel === UserBlockingPriority$1 || priorityLevel === ImmediatePriority)) {
    // This is the result of a discrete event. Track the lowest priority
    // discrete update per root so we can flush them early, if needed.
    if (rootsWithPendingDiscreteUpdates === null) {
      rootsWithPendingDiscreteUpdates = new Map([[root, expirationTime]]);
    } else {
      var lastDiscreteTime = rootsWithPendingDiscreteUpdates.get(root);

      if (lastDiscreteTime === undefined || lastDiscreteTime > expirationTime) {
        rootsWithPendingDiscreteUpdates.set(root, expirationTime);
      }
    }
  }
}
var scheduleWork = scheduleUpdateOnFiber; // This is split into a separate function so we can mark a fiber with pending
// work without treating it as a typical update that originates from an event;
// e.g. retrying a Suspense boundary isn't an update, but it does schedule work
// on a fiber.

function markUpdateTimeFromFiberToRoot(fiber, expirationTime) {
  // Update the source fiber's expiration time
  if (fiber.expirationTime < expirationTime) {
    fiber.expirationTime = expirationTime;
  }

  var alternate = fiber.alternate;

  if (alternate !== null && alternate.expirationTime < expirationTime) {
    alternate.expirationTime = expirationTime;
  } // Walk the parent path to the root and update the child expiration time.


  var node = fiber.return;
  var root = null;

  if (node === null && fiber.tag === HostRoot) {
    root = fiber.stateNode;
  } else {
    while (node !== null) {
      alternate = node.alternate;

      if (node.childExpirationTime < expirationTime) {
        node.childExpirationTime = expirationTime;

        if (alternate !== null && alternate.childExpirationTime < expirationTime) {
          alternate.childExpirationTime = expirationTime;
        }
      } else if (alternate !== null && alternate.childExpirationTime < expirationTime) {
        alternate.childExpirationTime = expirationTime;
      }

      if (node.return === null && node.tag === HostRoot) {
        root = node.stateNode;
        break;
      }

      node = node.return;
    }
  }

  if (root !== null) {
    if (workInProgressRoot === root) {
      // Received an update to a tree that's in the middle of rendering. Mark
      // that's unprocessed work on this root.
      markUnprocessedUpdateTime(expirationTime);

      if (workInProgressRootExitStatus === RootSuspendedWithDelay) {
        // The root already suspended with a delay, which means this render
        // definitely won't finish. Since we have a new update, let's mark it as
        // suspended now, right before marking the incoming update. This has the
        // effect of interrupting the current render and switching to the update.
        // TODO: This happens to work when receiving an update during the render
        // phase, because of the trick inside computeExpirationForFiber to
        // subtract 1 from `renderExpirationTime` to move it into a
        // separate bucket. But we should probably model it with an exception,
        // using the same mechanism we use to force hydration of a subtree.
        // TODO: This does not account for low pri updates that were already
        // scheduled before the root started rendering. Need to track the next
        // pending expiration time (perhaps by backtracking the return path) and
        // then trigger a restart in the `renderDidSuspendDelayIfPossible` path.
        markRootSuspendedAtTime(root, renderExpirationTime$1);
      }
    } // Mark that the root has a pending update.


    markRootUpdatedAtTime(root, expirationTime);
  }

  return root;
}

function getNextRootExpirationTimeToWorkOn(root) {
  // Determines the next expiration time that the root should render, taking
  // into account levels that may be suspended, or levels that may have
  // received a ping.
  var lastExpiredTime = root.lastExpiredTime;

  if (lastExpiredTime !== NoWork) {
    return lastExpiredTime;
  } // "Pending" refers to any update that hasn't committed yet, including if it
  // suspended. The "suspended" range is therefore a subset.


  var firstPendingTime = root.firstPendingTime;

  if (!isRootSuspendedAtTime(root, firstPendingTime)) {
    // The highest priority pending time is not suspended. Let's work on that.
    return firstPendingTime;
  } // If the first pending time is suspended, check if there's a lower priority
  // pending level that we know about. Or check if we received a ping. Work
  // on whichever is higher priority.


  var lastPingedTime = root.lastPingedTime;
  var nextKnownPendingLevel = root.nextKnownPendingLevel;
  var nextLevel = lastPingedTime > nextKnownPendingLevel ? lastPingedTime : nextKnownPendingLevel;

  if ( nextLevel <= Idle && firstPendingTime !== nextLevel) {
    // Don't work on Idle/Never priority unless everything else is committed.
    return NoWork;
  }

  return nextLevel;
} // Use this function to schedule a task for a root. There's only one task per
// root; if a task was already scheduled, we'll check to make sure the
// expiration time of the existing task is the same as the expiration time of
// the next level that the root has work on. This function is called on every
// update, and right before exiting a task.


function ensureRootIsScheduled(root) {
  var lastExpiredTime = root.lastExpiredTime;

  if (lastExpiredTime !== NoWork) {
    // Special case: Expired work should flush synchronously.
    root.callbackExpirationTime = Sync;
    root.callbackPriority = ImmediatePriority;
    root.callbackNode = scheduleSyncCallback(performSyncWorkOnRoot.bind(null, root));
    return;
  }

  var expirationTime = getNextRootExpirationTimeToWorkOn(root);
  var existingCallbackNode = root.callbackNode;

  if (expirationTime === NoWork) {
    // There's nothing to work on.
    if (existingCallbackNode !== null) {
      root.callbackNode = null;
      root.callbackExpirationTime = NoWork;
      root.callbackPriority = NoPriority;
    }

    return;
  } // TODO: If this is an update, we already read the current time. Pass the
  // time as an argument.


  var currentTime = requestCurrentTimeForUpdate();
  var priorityLevel = inferPriorityFromExpirationTime(currentTime, expirationTime); // If there's an existing render task, confirm it has the correct priority and
  // expiration time. Otherwise, we'll cancel it and schedule a new one.

  if (existingCallbackNode !== null) {
    var existingCallbackPriority = root.callbackPriority;
    var existingCallbackExpirationTime = root.callbackExpirationTime;

    if ( // Callback must have the exact same expiration time.
    existingCallbackExpirationTime === expirationTime && // Callback must have greater or equal priority.
    existingCallbackPriority >= priorityLevel) {
      // Existing callback is sufficient.
      return;
    } // Need to schedule a new task.
    // TODO: Instead of scheduling a new task, we should be able to change the
    // priority of the existing one.


    cancelCallback(existingCallbackNode);
  }

  root.callbackExpirationTime = expirationTime;
  root.callbackPriority = priorityLevel;
  var callbackNode;

  if (expirationTime === Sync) {
    // Sync React callbacks are scheduled on a special internal queue
    callbackNode = scheduleSyncCallback(performSyncWorkOnRoot.bind(null, root));
  } else {
    callbackNode = scheduleCallback(priorityLevel, performConcurrentWorkOnRoot.bind(null, root), // Compute a task timeout based on the expiration time. This also affects
    // ordering because tasks are processed in timeout order.
    {
      timeout: expirationTimeToMs(expirationTime) - now()
    });
  }

  root.callbackNode = callbackNode;
} // This is the entry point for every concurrent task, i.e. anything that
// goes through Scheduler.


function performConcurrentWorkOnRoot(root, didTimeout) {
  // Since we know we're in a React event, we can clear the current
  // event time. The next update will compute a new event time.
  currentEventTime = NoWork;

  if (didTimeout) {
    // The render task took too long to complete. Mark the current time as
    // expired to synchronously render all expired work in a single batch.
    var currentTime = requestCurrentTimeForUpdate();
    markRootExpiredAtTime(root, currentTime); // This will schedule a synchronous callback.

    ensureRootIsScheduled(root);
    return null;
  } // Determine the next expiration time to work on, using the fields stored
  // on the root.


  var expirationTime = getNextRootExpirationTimeToWorkOn(root);

  if (expirationTime !== NoWork) {
    var originalCallbackNode = root.callbackNode;

    if (!((executionContext & (RenderContext | CommitContext)) === NoContext)) {
      {
        throw Error( "Should not already be working." );
      }
    }

    flushPassiveEffects(); // If the root or expiration time have changed, throw out the existing stack
    // and prepare a fresh one. Otherwise we'll continue where we left off.

    if (root !== workInProgressRoot || expirationTime !== renderExpirationTime$1) {
      prepareFreshStack(root, expirationTime);
      startWorkOnPendingInteractions(root, expirationTime);
    } // If we have a work-in-progress fiber, it means there's still work to do
    // in this root.


    if (workInProgress !== null) {
      var prevExecutionContext = executionContext;
      executionContext |= RenderContext;
      var prevDispatcher = pushDispatcher();
      var prevInteractions = pushInteractions(root);
      startWorkLoopTimer(workInProgress);

      do {
        try {
          workLoopConcurrent();
          break;
        } catch (thrownValue) {
          handleError(root, thrownValue);
        }
      } while (true);

      resetContextDependencies();
      executionContext = prevExecutionContext;
      popDispatcher(prevDispatcher);

      {
        popInteractions(prevInteractions);
      }

      if (workInProgressRootExitStatus === RootFatalErrored) {
        var fatalError = workInProgressRootFatalError;
        stopInterruptedWorkLoopTimer();
        prepareFreshStack(root, expirationTime);
        markRootSuspendedAtTime(root, expirationTime);
        ensureRootIsScheduled(root);
        throw fatalError;
      }

      if (workInProgress !== null) {
        // There's still work left over. Exit without committing.
        stopInterruptedWorkLoopTimer();
      } else {
        // We now have a consistent tree. The next step is either to commit it,
        // or, if something suspended, wait to commit it after a timeout.
        stopFinishedWorkLoopTimer();
        var finishedWork = root.finishedWork = root.current.alternate;
        root.finishedExpirationTime = expirationTime;
        finishConcurrentRender(root, finishedWork, workInProgressRootExitStatus, expirationTime);
      }

      ensureRootIsScheduled(root);

      if (root.callbackNode === originalCallbackNode) {
        // The task node scheduled for this root is the same one that's
        // currently executed. Need to return a continuation.
        return performConcurrentWorkOnRoot.bind(null, root);
      }
    }
  }

  return null;
}

function finishConcurrentRender(root, finishedWork, exitStatus, expirationTime) {
  // Set this to null to indicate there's no in-progress render.
  workInProgressRoot = null;

  switch (exitStatus) {
    case RootIncomplete:
    case RootFatalErrored:
      {
        {
          {
            throw Error( "Root did not complete. This is a bug in React." );
          }
        }
      }
    // Flow knows about invariant, so it complains if I add a break
    // statement, but eslint doesn't know about invariant, so it complains
    // if I do. eslint-disable-next-line no-fallthrough

    case RootErrored:
      {
        // If this was an async render, the error may have happened due to
        // a mutation in a concurrent event. Try rendering one more time,
        // synchronously, to see if the error goes away. If there are
        // lower priority updates, let's include those, too, in case they
        // fix the inconsistency. Render at Idle to include all updates.
        // If it was Idle or Never or some not-yet-invented time, render
        // at that time.
        markRootExpiredAtTime(root, expirationTime > Idle ? Idle : expirationTime); // We assume that this second render pass will be synchronous
        // and therefore not hit this path again.

        break;
      }

    case RootSuspended:
      {
        markRootSuspendedAtTime(root, expirationTime);
        var lastSuspendedTime = root.lastSuspendedTime;

        if (expirationTime === lastSuspendedTime) {
          root.nextKnownPendingLevel = getRemainingExpirationTime(finishedWork);
        } // We have an acceptable loading state. We need to figure out if we
        // should immediately commit it or wait a bit.
        // If we have processed new updates during this render, we may now
        // have a new loading state ready. We want to ensure that we commit
        // that as soon as possible.


        var hasNotProcessedNewUpdates = workInProgressRootLatestProcessedExpirationTime === Sync;

        if (hasNotProcessedNewUpdates && // do not delay if we're inside an act() scope
        !( IsThisRendererActing.current)) {
          // If we have not processed any new updates during this pass, then
          // this is either a retry of an existing fallback state or a
          // hidden tree. Hidden trees shouldn't be batched with other work
          // and after that's fixed it can only be a retry. We're going to
          // throttle committing retries so that we don't show too many
          // loading states too quickly.
          var msUntilTimeout = globalMostRecentFallbackTime + FALLBACK_THROTTLE_MS - now(); // Don't bother with a very short suspense time.

          if (msUntilTimeout > 10) {
            if (workInProgressRootHasPendingPing) {
              var lastPingedTime = root.lastPingedTime;

              if (lastPingedTime === NoWork || lastPingedTime >= expirationTime) {
                // This render was pinged but we didn't get to restart
                // earlier so try restarting now instead.
                root.lastPingedTime = expirationTime;
                prepareFreshStack(root, expirationTime);
                break;
              }
            }

            var nextTime = getNextRootExpirationTimeToWorkOn(root);

            if (nextTime !== NoWork && nextTime !== expirationTime) {
              // There's additional work on this root.
              break;
            }

            if (lastSuspendedTime !== NoWork && lastSuspendedTime !== expirationTime) {
              // We should prefer to render the fallback of at the last
              // suspended level. Ping the last suspended level to try
              // rendering it again.
              root.lastPingedTime = lastSuspendedTime;
              break;
            } // The render is suspended, it hasn't timed out, and there's no
            // lower priority work to do. Instead of committing the fallback
            // immediately, wait for more data to arrive.


            root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root), msUntilTimeout);
            break;
          }
        } // The work expired. Commit immediately.


        commitRoot(root);
        break;
      }

    case RootSuspendedWithDelay:
      {
        markRootSuspendedAtTime(root, expirationTime);
        var _lastSuspendedTime = root.lastSuspendedTime;

        if (expirationTime === _lastSuspendedTime) {
          root.nextKnownPendingLevel = getRemainingExpirationTime(finishedWork);
        }

        if ( // do not delay if we're inside an act() scope
        !( IsThisRendererActing.current)) {
          // We're suspended in a state that should be avoided. We'll try to
          // avoid committing it for as long as the timeouts let us.
          if (workInProgressRootHasPendingPing) {
            var _lastPingedTime = root.lastPingedTime;

            if (_lastPingedTime === NoWork || _lastPingedTime >= expirationTime) {
              // This render was pinged but we didn't get to restart earlier
              // so try restarting now instead.
              root.lastPingedTime = expirationTime;
              prepareFreshStack(root, expirationTime);
              break;
            }
          }

          var _nextTime = getNextRootExpirationTimeToWorkOn(root);

          if (_nextTime !== NoWork && _nextTime !== expirationTime) {
            // There's additional work on this root.
            break;
          }

          if (_lastSuspendedTime !== NoWork && _lastSuspendedTime !== expirationTime) {
            // We should prefer to render the fallback of at the last
            // suspended level. Ping the last suspended level to try
            // rendering it again.
            root.lastPingedTime = _lastSuspendedTime;
            break;
          }

          var _msUntilTimeout;

          if (workInProgressRootLatestSuspenseTimeout !== Sync) {
            // We have processed a suspense config whose expiration time we
            // can use as the timeout.
            _msUntilTimeout = expirationTimeToMs(workInProgressRootLatestSuspenseTimeout) - now();
          } else if (workInProgressRootLatestProcessedExpirationTime === Sync) {
            // This should never normally happen because only new updates
            // cause delayed states, so we should have processed something.
            // However, this could also happen in an offscreen tree.
            _msUntilTimeout = 0;
          } else {
            // If we don't have a suspense config, we're going to use a
            // heuristic to determine how long we can suspend.
            var eventTimeMs = inferTimeFromExpirationTime(workInProgressRootLatestProcessedExpirationTime);
            var currentTimeMs = now();
            var timeUntilExpirationMs = expirationTimeToMs(expirationTime) - currentTimeMs;
            var timeElapsed = currentTimeMs - eventTimeMs;

            if (timeElapsed < 0) {
              // We get this wrong some time since we estimate the time.
              timeElapsed = 0;
            }

            _msUntilTimeout = jnd(timeElapsed) - timeElapsed; // Clamp the timeout to the expiration time. TODO: Once the
            // event time is exact instead of inferred from expiration time
            // we don't need this.

            if (timeUntilExpirationMs < _msUntilTimeout) {
              _msUntilTimeout = timeUntilExpirationMs;
            }
          } // Don't bother with a very short suspense time.


          if (_msUntilTimeout > 10) {
            // The render is suspended, it hasn't timed out, and there's no
            // lower priority work to do. Instead of committing the fallback
            // immediately, wait for more data to arrive.
            root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root), _msUntilTimeout);
            break;
          }
        } // The work expired. Commit immediately.


        commitRoot(root);
        break;
      }

    case RootCompleted:
      {
        // The work completed. Ready to commit.
        if ( // do not delay if we're inside an act() scope
        !( IsThisRendererActing.current) && workInProgressRootLatestProcessedExpirationTime !== Sync && workInProgressRootCanSuspendUsingConfig !== null) {
          // If we have exceeded the minimum loading delay, which probably
          // means we have shown a spinner already, we might have to suspend
          // a bit longer to ensure that the spinner is shown for
          // enough time.
          var _msUntilTimeout2 = computeMsUntilSuspenseLoadingDelay(workInProgressRootLatestProcessedExpirationTime, expirationTime, workInProgressRootCanSuspendUsingConfig);

          if (_msUntilTimeout2 > 10) {
            markRootSuspendedAtTime(root, expirationTime);
            root.timeoutHandle = scheduleTimeout(commitRoot.bind(null, root), _msUntilTimeout2);
            break;
          }
        }

        commitRoot(root);
        break;
      }

    default:
      {
        {
          {
            throw Error( "Unknown root exit status." );
          }
        }
      }
  }
} // This is the entry point for synchronous tasks that don't go
// through Scheduler


function performSyncWorkOnRoot(root) {
  // Check if there's expired work on this root. Otherwise, render at Sync.
  var lastExpiredTime = root.lastExpiredTime;
  var expirationTime = lastExpiredTime !== NoWork ? lastExpiredTime : Sync;

  if (!((executionContext & (RenderContext | CommitContext)) === NoContext)) {
    {
      throw Error( "Should not already be working." );
    }
  }

  flushPassiveEffects(); // If the root or expiration time have changed, throw out the existing stack
  // and prepare a fresh one. Otherwise we'll continue where we left off.

  if (root !== workInProgressRoot || expirationTime !== renderExpirationTime$1) {
    prepareFreshStack(root, expirationTime);
    startWorkOnPendingInteractions(root, expirationTime);
  } // If we have a work-in-progress fiber, it means there's still work to do
  // in this root.


  if (workInProgress !== null) {
    var prevExecutionContext = executionContext;
    executionContext |= RenderContext;
    var prevDispatcher = pushDispatcher();
    var prevInteractions = pushInteractions(root);
    startWorkLoopTimer(workInProgress);

    do {
      try {
        workLoopSync();
        break;
      } catch (thrownValue) {
        handleError(root, thrownValue);
      }
    } while (true);

    resetContextDependencies();
    executionContext = prevExecutionContext;
    popDispatcher(prevDispatcher);

    {
      popInteractions(prevInteractions);
    }

    if (workInProgressRootExitStatus === RootFatalErrored) {
      var fatalError = workInProgressRootFatalError;
      stopInterruptedWorkLoopTimer();
      prepareFreshStack(root, expirationTime);
      markRootSuspendedAtTime(root, expirationTime);
      ensureRootIsScheduled(root);
      throw fatalError;
    }

    if (workInProgress !== null) {
      // This is a sync render, so we should have finished the whole tree.
      {
        {
          throw Error( "Cannot commit an incomplete root. This error is likely caused by a bug in React. Please file an issue." );
        }
      }
    } else {
      // We now have a consistent tree. Because this is a sync render, we
      // will commit it even if something suspended.
      stopFinishedWorkLoopTimer();
      root.finishedWork = root.current.alternate;
      root.finishedExpirationTime = expirationTime;
      finishSyncRender(root);
    } // Before exiting, make sure there's a callback scheduled for the next
    // pending level.


    ensureRootIsScheduled(root);
  }

  return null;
}

function finishSyncRender(root) {
  // Set this to null to indicate there's no in-progress render.
  workInProgressRoot = null;
  commitRoot(root);
}
function flushDiscreteUpdates() {
  // TODO: Should be able to flush inside batchedUpdates, but not inside `act`.
  // However, `act` uses `batchedUpdates`, so there's no way to distinguish
  // those two cases. Need to fix this before exposing flushDiscreteUpdates
  // as a public API.
  if ((executionContext & (BatchedContext | RenderContext | CommitContext)) !== NoContext) {
    {
      if ((executionContext & RenderContext) !== NoContext) {
        error('unstable_flushDiscreteUpdates: Cannot flush updates when React is ' + 'already rendering.');
      }
    } // We're already rendering, so we can't synchronously flush pending work.
    // This is probably a nested event dispatch triggered by a lifecycle/effect,
    // like `el.focus()`. Exit.


    return;
  }

  flushPendingDiscreteUpdates(); // If the discrete updates scheduled passive effects, flush them now so that
  // they fire before the next serial event.

  flushPassiveEffects();
}
function syncUpdates(fn, a, b, c) {
  return runWithPriority$1(ImmediatePriority, fn.bind(null, a, b, c));
}

function flushPendingDiscreteUpdates() {
  if (rootsWithPendingDiscreteUpdates !== null) {
    // For each root with pending discrete updates, schedule a callback to
    // immediately flush them.
    var roots = rootsWithPendingDiscreteUpdates;
    rootsWithPendingDiscreteUpdates = null;
    roots.forEach(function (expirationTime, root) {
      markRootExpiredAtTime(root, expirationTime);
      ensureRootIsScheduled(root);
    }); // Now flush the immediate queue.

    flushSyncCallbackQueue();
  }
}

function batchedUpdates$1(fn, a) {
  var prevExecutionContext = executionContext;
  executionContext |= BatchedContext;

  try {
    return fn(a);
  } finally {
    executionContext = prevExecutionContext;

    if (executionContext === NoContext) {
      // Flush the immediate callbacks that were scheduled during this batch
      flushSyncCallbackQueue();
    }
  }
}
function batchedEventUpdates$1(fn, a) {
  var prevExecutionContext = executionContext;
  executionContext |= EventContext;

  try {
    return fn(a);
  } finally {
    executionContext = prevExecutionContext;

    if (executionContext === NoContext) {
      // Flush the immediate callbacks that were scheduled during this batch
      flushSyncCallbackQueue();
    }
  }
}
function discreteUpdates$1(fn, a, b, c, d) {
  var prevExecutionContext = executionContext;
  executionContext |= DiscreteEventContext;

  try {
    // Should this
    return runWithPriority$1(UserBlockingPriority$1, fn.bind(null, a, b, c, d));
  } finally {
    executionContext = prevExecutionContext;

    if (executionContext === NoContext) {
      // Flush the immediate callbacks that were scheduled during this batch
      flushSyncCallbackQueue();
    }
  }
}
function unbatchedUpdates(fn, a) {
  var prevExecutionContext = executionContext;
  executionContext &= ~BatchedContext;
  executionContext |= LegacyUnbatchedContext;

  try {
    return fn(a);
  } finally {
    executionContext = prevExecutionContext;

    if (executionContext === NoContext) {
      // Flush the immediate callbacks that were scheduled during this batch
      flushSyncCallbackQueue();
    }
  }
}
function flushSync(fn, a) {
  if ((executionContext & (RenderContext | CommitContext)) !== NoContext) {
    {
      {
        throw Error( "flushSync was called from inside a lifecycle method. It cannot be called when React is already rendering." );
      }
    }
  }

  var prevExecutionContext = executionContext;
  executionContext |= BatchedContext;

  try {
    return runWithPriority$1(ImmediatePriority, fn.bind(null, a));
  } finally {
    executionContext = prevExecutionContext; // Flush the immediate callbacks that were scheduled during this batch.
    // Note that this will happen even if batchedUpdates is higher up
    // the stack.

    flushSyncCallbackQueue();
  }
}

function prepareFreshStack(root, expirationTime) {
  root.finishedWork = null;
  root.finishedExpirationTime = NoWork;
  var timeoutHandle = root.timeoutHandle;

  if (timeoutHandle !== noTimeout) {
    // The root previous suspended and scheduled a timeout to commit a fallback
    // state. Now that we have additional work, cancel the timeout.
    root.timeoutHandle = noTimeout; // $FlowFixMe Complains noTimeout is not a TimeoutID, despite the check above

    cancelTimeout(timeoutHandle);
  }

  if (workInProgress !== null) {
    var interruptedWork = workInProgress.return;

    while (interruptedWork !== null) {
      unwindInterruptedWork(interruptedWork);
      interruptedWork = interruptedWork.return;
    }
  }

  workInProgressRoot = root;
  workInProgress = createWorkInProgress(root.current, null);
  renderExpirationTime$1 = expirationTime;
  workInProgressRootExitStatus = RootIncomplete;
  workInProgressRootFatalError = null;
  workInProgressRootLatestProcessedExpirationTime = Sync;
  workInProgressRootLatestSuspenseTimeout = Sync;
  workInProgressRootCanSuspendUsingConfig = null;
  workInProgressRootNextUnprocessedUpdateTime = NoWork;
  workInProgressRootHasPendingPing = false;

  {
    spawnedWorkDuringRender = null;
  }

  {
    ReactStrictModeWarnings.discardPendingWarnings();
  }
}

function handleError(root, thrownValue) {
  do {
    try {
      // Reset module-level state that was set during the render phase.
      resetContextDependencies();
      resetHooksAfterThrow();
      resetCurrentFiber();

      if (workInProgress === null || workInProgress.return === null) {
        // Expected to be working on a non-root fiber. This is a fatal error
        // because there's no ancestor that can handle it; the root is
        // supposed to capture all errors that weren't caught by an error
        // boundary.
        workInProgressRootExitStatus = RootFatalErrored;
        workInProgressRootFatalError = thrownValue; // Set `workInProgress` to null. This represents advancing to the next
        // sibling, or the parent if there are no siblings. But since the root
        // has no siblings nor a parent, we set it to null. Usually this is
        // handled by `completeUnitOfWork` or `unwindWork`, but since we're
        // interntionally not calling those, we need set it here.
        // TODO: Consider calling `unwindWork` to pop the contexts.

        workInProgress = null;
        return null;
      }

      if (enableProfilerTimer && workInProgress.mode & ProfileMode) {
        // Record the time spent rendering before an error was thrown. This
        // avoids inaccurate Profiler durations in the case of a
        // suspended render.
        stopProfilerTimerIfRunningAndRecordDelta(workInProgress, true);
      }

      throwException(root, workInProgress.return, workInProgress, thrownValue, renderExpirationTime$1);
      workInProgress = completeUnitOfWork(workInProgress);
    } catch (yetAnotherThrownValue) {
      // Something in the return path also threw.
      thrownValue = yetAnotherThrownValue;
      continue;
    } // Return to the normal work loop.


    return;
  } while (true);
}

function pushDispatcher(root) {
  var prevDispatcher = ReactCurrentDispatcher$1.current;
  ReactCurrentDispatcher$1.current = ContextOnlyDispatcher;

  if (prevDispatcher === null) {
    // The React isomorphic package does not include a default dispatcher.
    // Instead the first renderer will lazily attach one, in order to give
    // nicer error messages.
    return ContextOnlyDispatcher;
  } else {
    return prevDispatcher;
  }
}

function popDispatcher(prevDispatcher) {
  ReactCurrentDispatcher$1.current = prevDispatcher;
}

function pushInteractions(root) {
  {
    var prevInteractions = tracing.__interactionsRef.current;
    tracing.__interactionsRef.current = root.memoizedInteractions;
    return prevInteractions;
  }
}

function popInteractions(prevInteractions) {
  {
    tracing.__interactionsRef.current = prevInteractions;
  }
}

function markCommitTimeOfFallback() {
  globalMostRecentFallbackTime = now();
}
function markRenderEventTimeAndConfig(expirationTime, suspenseConfig) {
  if (expirationTime < workInProgressRootLatestProcessedExpirationTime && expirationTime > Idle) {
    workInProgressRootLatestProcessedExpirationTime = expirationTime;
  }

  if (suspenseConfig !== null) {
    if (expirationTime < workInProgressRootLatestSuspenseTimeout && expirationTime > Idle) {
      workInProgressRootLatestSuspenseTimeout = expirationTime; // Most of the time we only have one config and getting wrong is not bad.

      workInProgressRootCanSuspendUsingConfig = suspenseConfig;
    }
  }
}
function markUnprocessedUpdateTime(expirationTime) {
  if (expirationTime > workInProgressRootNextUnprocessedUpdateTime) {
    workInProgressRootNextUnprocessedUpdateTime = expirationTime;
  }
}
function renderDidSuspend() {
  if (workInProgressRootExitStatus === RootIncomplete) {
    workInProgressRootExitStatus = RootSuspended;
  }
}
function renderDidSuspendDelayIfPossible() {
  if (workInProgressRootExitStatus === RootIncomplete || workInProgressRootExitStatus === RootSuspended) {
    workInProgressRootExitStatus = RootSuspendedWithDelay;
  } // Check if there's a lower priority update somewhere else in the tree.


  if (workInProgressRootNextUnprocessedUpdateTime !== NoWork && workInProgressRoot !== null) {
    // Mark the current render as suspended, and then mark that there's a
    // pending update.
    // TODO: This should immediately interrupt the current render, instead
    // of waiting until the next time we yield.
    markRootSuspendedAtTime(workInProgressRoot, renderExpirationTime$1);
    markRootUpdatedAtTime(workInProgressRoot, workInProgressRootNextUnprocessedUpdateTime);
  }
}
function renderDidError() {
  if (workInProgressRootExitStatus !== RootCompleted) {
    workInProgressRootExitStatus = RootErrored;
  }
} // Called during render to determine if anything has suspended.
// Returns false if we're not sure.

function renderHasNotSuspendedYet() {
  // If something errored or completed, we can't really be sure,
  // so those are false.
  return workInProgressRootExitStatus === RootIncomplete;
}

function inferTimeFromExpirationTime(expirationTime) {
  // We don't know exactly when the update was scheduled, but we can infer an
  // approximate start time from the expiration time.
  var earliestExpirationTimeMs = expirationTimeToMs(expirationTime);
  return earliestExpirationTimeMs - LOW_PRIORITY_EXPIRATION;
}

function inferTimeFromExpirationTimeWithSuspenseConfig(expirationTime, suspenseConfig) {
  // We don't know exactly when the update was scheduled, but we can infer an
  // approximate start time from the expiration time by subtracting the timeout
  // that was added to the event time.
  var earliestExpirationTimeMs = expirationTimeToMs(expirationTime);
  return earliestExpirationTimeMs - (suspenseConfig.timeoutMs | 0 || LOW_PRIORITY_EXPIRATION);
} // The work loop is an extremely hot path. Tell Closure not to inline it.

/** @noinline */


function workLoopSync() {
  // Already timed out, so perform work without checking if we need to yield.
  while (workInProgress !== null) {
    workInProgress = performUnitOfWork(workInProgress);
  }
}
/** @noinline */


function workLoopConcurrent() {
  // Perform work until Scheduler asks us to yield
  while (workInProgress !== null && !shouldYield()) {
    workInProgress = performUnitOfWork(workInProgress);
  }
}

function performUnitOfWork(unitOfWork) {
  // The current, flushed, state of this fiber is the alternate. Ideally
  // nothing should rely on this, but relying on it here means that we don't
  // need an additional field on the work in progress.
  var current = unitOfWork.alternate;
  startWorkTimer(unitOfWork);
  setCurrentFiber(unitOfWork);
  var next;

  if ( (unitOfWork.mode & ProfileMode) !== NoMode) {
    startProfilerTimer(unitOfWork);
    next = beginWork$1(current, unitOfWork, renderExpirationTime$1);
    stopProfilerTimerIfRunningAndRecordDelta(unitOfWork, true);
  } else {
    next = beginWork$1(current, unitOfWork, renderExpirationTime$1);
  }

  resetCurrentFiber();
  unitOfWork.memoizedProps = unitOfWork.pendingProps;

  if (next === null) {
    // If this doesn't spawn new work, complete the current work.
    next = completeUnitOfWork(unitOfWork);
  }

  ReactCurrentOwner$2.current = null;
  return next;
}

function completeUnitOfWork(unitOfWork) {
  // Attempt to complete the current unit of work, then move to the next
  // sibling. If there are no more siblings, return to the parent fiber.
  workInProgress = unitOfWork;

  do {
    // The current, flushed, state of this fiber is the alternate. Ideally
    // nothing should rely on this, but relying on it here means that we don't
    // need an additional field on the work in progress.
    var current = workInProgress.alternate;
    var returnFiber = workInProgress.return; // Check if the work completed or if something threw.

    if ((workInProgress.effectTag & Incomplete) === NoEffect) {
      setCurrentFiber(workInProgress);
      var next = void 0;

      if ( (workInProgress.mode & ProfileMode) === NoMode) {
        next = completeWork(current, workInProgress, renderExpirationTime$1);
      } else {
        startProfilerTimer(workInProgress);
        next = completeWork(current, workInProgress, renderExpirationTime$1); // Update render duration assuming we didn't error.

        stopProfilerTimerIfRunningAndRecordDelta(workInProgress, false);
      }

      stopWorkTimer(workInProgress);
      resetCurrentFiber();
      resetChildExpirationTime(workInProgress);

      if (next !== null) {
        // Completing this fiber spawned new work. Work on that next.
        return next;
      }

      if (returnFiber !== null && // Do not append effects to parents if a sibling failed to complete
      (returnFiber.effectTag & Incomplete) === NoEffect) {
        // Append all the effects of the subtree and this fiber onto the effect
        // list of the parent. The completion order of the children affects the
        // side-effect order.
        if (returnFiber.firstEffect === null) {
          returnFiber.firstEffect = workInProgress.firstEffect;
        }

        if (workInProgress.lastEffect !== null) {
          if (returnFiber.lastEffect !== null) {
            returnFiber.lastEffect.nextEffect = workInProgress.firstEffect;
          }

          returnFiber.lastEffect = workInProgress.lastEffect;
        } // If this fiber had side-effects, we append it AFTER the children's
        // side-effects. We can perform certain side-effects earlier if needed,
        // by doing multiple passes over the effect list. We don't want to
        // schedule our own side-effect on our own list because if end up
        // reusing children we'll schedule this effect onto itself since we're
        // at the end.


        var effectTag = workInProgress.effectTag; // Skip both NoWork and PerformedWork tags when creating the effect
        // list. PerformedWork effect is read by React DevTools but shouldn't be
        // committed.

        if (effectTag > PerformedWork) {
          if (returnFiber.lastEffect !== null) {
            returnFiber.lastEffect.nextEffect = workInProgress;
          } else {
            returnFiber.firstEffect = workInProgress;
          }

          returnFiber.lastEffect = workInProgress;
        }
      }
    } else {
      // This fiber did not complete because something threw. Pop values off
      // the stack without entering the complete phase. If this is a boundary,
      // capture values if possible.
      var _next = unwindWork(workInProgress); // Because this fiber did not complete, don't reset its expiration time.


      if ( (workInProgress.mode & ProfileMode) !== NoMode) {
        // Record the render duration for the fiber that errored.
        stopProfilerTimerIfRunningAndRecordDelta(workInProgress, false); // Include the time spent working on failed children before continuing.

        var actualDuration = workInProgress.actualDuration;
        var child = workInProgress.child;

        while (child !== null) {
          actualDuration += child.actualDuration;
          child = child.sibling;
        }

        workInProgress.actualDuration = actualDuration;
      }

      if (_next !== null) {
        // If completing this work spawned new work, do that next. We'll come
        // back here again.
        // Since we're restarting, remove anything that is not a host effect
        // from the effect tag.
        // TODO: The name stopFailedWorkTimer is misleading because Suspense
        // also captures and restarts.
        stopFailedWorkTimer(workInProgress);
        _next.effectTag &= HostEffectMask;
        return _next;
      }

      stopWorkTimer(workInProgress);

      if (returnFiber !== null) {
        // Mark the parent fiber as incomplete and clear its effect list.
        returnFiber.firstEffect = returnFiber.lastEffect = null;
        returnFiber.effectTag |= Incomplete;
      }
    }

    var siblingFiber = workInProgress.sibling;

    if (siblingFiber !== null) {
      // If there is more work to do in this returnFiber, do that next.
      return siblingFiber;
    } // Otherwise, return to the parent


    workInProgress = returnFiber;
  } while (workInProgress !== null); // We've reached the root.


  if (workInProgressRootExitStatus === RootIncomplete) {
    workInProgressRootExitStatus = RootCompleted;
  }

  return null;
}

function getRemainingExpirationTime(fiber) {
  var updateExpirationTime = fiber.expirationTime;
  var childExpirationTime = fiber.childExpirationTime;
  return updateExpirationTime > childExpirationTime ? updateExpirationTime : childExpirationTime;
}

function resetChildExpirationTime(completedWork) {
  if (renderExpirationTime$1 !== Never && completedWork.childExpirationTime === Never) {
    // The children of this component are hidden. Don't bubble their
    // expiration times.
    return;
  }

  var newChildExpirationTime = NoWork; // Bubble up the earliest expiration time.

  if ( (completedWork.mode & ProfileMode) !== NoMode) {
    // In profiling mode, resetChildExpirationTime is also used to reset
    // profiler durations.
    var actualDuration = completedWork.actualDuration;
    var treeBaseDuration = completedWork.selfBaseDuration; // When a fiber is cloned, its actualDuration is reset to 0. This value will
    // only be updated if work is done on the fiber (i.e. it doesn't bailout).
    // When work is done, it should bubble to the parent's actualDuration. If
    // the fiber has not been cloned though, (meaning no work was done), then
    // this value will reflect the amount of time spent working on a previous
    // render. In that case it should not bubble. We determine whether it was
    // cloned by comparing the child pointer.

    var shouldBubbleActualDurations = completedWork.alternate === null || completedWork.child !== completedWork.alternate.child;
    var child = completedWork.child;

    while (child !== null) {
      var childUpdateExpirationTime = child.expirationTime;
      var childChildExpirationTime = child.childExpirationTime;

      if (childUpdateExpirationTime > newChildExpirationTime) {
        newChildExpirationTime = childUpdateExpirationTime;
      }

      if (childChildExpirationTime > newChildExpirationTime) {
        newChildExpirationTime = childChildExpirationTime;
      }

      if (shouldBubbleActualDurations) {
        actualDuration += child.actualDuration;
      }

      treeBaseDuration += child.treeBaseDuration;
      child = child.sibling;
    }

    completedWork.actualDuration = actualDuration;
    completedWork.treeBaseDuration = treeBaseDuration;
  } else {
    var _child = completedWork.child;

    while (_child !== null) {
      var _childUpdateExpirationTime = _child.expirationTime;
      var _childChildExpirationTime = _child.childExpirationTime;

      if (_childUpdateExpirationTime > newChildExpirationTime) {
        newChildExpirationTime = _childUpdateExpirationTime;
      }

      if (_childChildExpirationTime > newChildExpirationTime) {
        newChildExpirationTime = _childChildExpirationTime;
      }

      _child = _child.sibling;
    }
  }

  completedWork.childExpirationTime = newChildExpirationTime;
}

function commitRoot(root) {
  var renderPriorityLevel = getCurrentPriorityLevel();
  runWithPriority$1(ImmediatePriority, commitRootImpl.bind(null, root, renderPriorityLevel));
  return null;
}

function commitRootImpl(root, renderPriorityLevel) {
  do {
    // `flushPassiveEffects` will call `flushSyncUpdateQueue` at the end, which
    // means `flushPassiveEffects` will sometimes result in additional
    // passive effects. So we need to keep flushing in a loop until there are
    // no more pending effects.
    // TODO: Might be better if `flushPassiveEffects` did not automatically
    // flush synchronous work at the end, to avoid factoring hazards like this.
    flushPassiveEffects();
  } while (rootWithPendingPassiveEffects !== null);

  flushRenderPhaseStrictModeWarningsInDEV();

  if (!((executionContext & (RenderContext | CommitContext)) === NoContext)) {
    {
      throw Error( "Should not already be working." );
    }
  }

  var finishedWork = root.finishedWork;
  var expirationTime = root.finishedExpirationTime;

  if (finishedWork === null) {
    return null;
  }

  root.finishedWork = null;
  root.finishedExpirationTime = NoWork;

  if (!(finishedWork !== root.current)) {
    {
      throw Error( "Cannot commit the same tree as before. This error is likely caused by a bug in React. Please file an issue." );
    }
  } // commitRoot never returns a continuation; it always finishes synchronously.
  // So we can clear these now to allow a new callback to be scheduled.


  root.callbackNode = null;
  root.callbackExpirationTime = NoWork;
  root.callbackPriority = NoPriority;
  root.nextKnownPendingLevel = NoWork;
  startCommitTimer(); // Update the first and last pending times on this root. The new first
  // pending time is whatever is left on the root fiber.

  var remainingExpirationTimeBeforeCommit = getRemainingExpirationTime(finishedWork);
  markRootFinishedAtTime(root, expirationTime, remainingExpirationTimeBeforeCommit);

  if (root === workInProgressRoot) {
    // We can reset these now that they are finished.
    workInProgressRoot = null;
    workInProgress = null;
    renderExpirationTime$1 = NoWork;
  } // This indicates that the last root we worked on is not the same one that
  // we're committing now. This most commonly happens when a suspended root
  // times out.
  // Get the list of effects.


  var firstEffect;

  if (finishedWork.effectTag > PerformedWork) {
    // A fiber's effect list consists only of its children, not itself. So if
    // the root has an effect, we need to add it to the end of the list. The
    // resulting list is the set that would belong to the root's parent, if it
    // had one; that is, all the effects in the tree including the root.
    if (finishedWork.lastEffect !== null) {
      finishedWork.lastEffect.nextEffect = finishedWork;
      firstEffect = finishedWork.firstEffect;
    } else {
      firstEffect = finishedWork;
    }
  } else {
    // There is no effect on the root.
    firstEffect = finishedWork.firstEffect;
  }

  if (firstEffect !== null) {
    var prevExecutionContext = executionContext;
    executionContext |= CommitContext;
    var prevInteractions = pushInteractions(root); // Reset this to null before calling lifecycles

    ReactCurrentOwner$2.current = null; // The commit phase is broken into several sub-phases. We do a separate pass
    // of the effect list for each phase: all mutation effects come before all
    // layout effects, and so on.
    // The first phase a "before mutation" phase. We use this phase to read the
    // state of the host tree right before we mutate it. This is where
    // getSnapshotBeforeUpdate is called.

    startCommitSnapshotEffectsTimer();
    prepareForCommit(root.containerInfo);
    nextEffect = firstEffect;

    do {
      {
        invokeGuardedCallback(null, commitBeforeMutationEffects, null);

        if (hasCaughtError()) {
          if (!(nextEffect !== null)) {
            {
              throw Error( "Should be working on an effect." );
            }
          }

          var error = clearCaughtError();
          captureCommitPhaseError(nextEffect, error);
          nextEffect = nextEffect.nextEffect;
        }
      }
    } while (nextEffect !== null);

    stopCommitSnapshotEffectsTimer();

    {
      // Mark the current commit time to be shared by all Profilers in this
      // batch. This enables them to be grouped later.
      recordCommitTime();
    } // The next phase is the mutation phase, where we mutate the host tree.


    startCommitHostEffectsTimer();
    nextEffect = firstEffect;

    do {
      {
        invokeGuardedCallback(null, commitMutationEffects, null, root, renderPriorityLevel);

        if (hasCaughtError()) {
          if (!(nextEffect !== null)) {
            {
              throw Error( "Should be working on an effect." );
            }
          }

          var _error = clearCaughtError();

          captureCommitPhaseError(nextEffect, _error);
          nextEffect = nextEffect.nextEffect;
        }
      }
    } while (nextEffect !== null);

    stopCommitHostEffectsTimer();
    resetAfterCommit(root.containerInfo); // The work-in-progress tree is now the current tree. This must come after
    // the mutation phase, so that the previous tree is still current during
    // componentWillUnmount, but before the layout phase, so that the finished
    // work is current during componentDidMount/Update.

    root.current = finishedWork; // The next phase is the layout phase, where we call effects that read
    // the host tree after it's been mutated. The idiomatic use case for this is
    // layout, but class component lifecycles also fire here for legacy reasons.

    startCommitLifeCyclesTimer();
    nextEffect = firstEffect;

    do {
      {
        invokeGuardedCallback(null, commitLayoutEffects, null, root, expirationTime);

        if (hasCaughtError()) {
          if (!(nextEffect !== null)) {
            {
              throw Error( "Should be working on an effect." );
            }
          }

          var _error2 = clearCaughtError();

          captureCommitPhaseError(nextEffect, _error2);
          nextEffect = nextEffect.nextEffect;
        }
      }
    } while (nextEffect !== null);

    stopCommitLifeCyclesTimer();
    nextEffect = null; // Tell Scheduler to yield at the end of the frame, so the browser has an
    // opportunity to paint.

    requestPaint();

    {
      popInteractions(prevInteractions);
    }

    executionContext = prevExecutionContext;
  } else {
    // No effects.
    root.current = finishedWork; // Measure these anyway so the flamegraph explicitly shows that there were
    // no effects.
    // TODO: Maybe there's a better way to report this.

    startCommitSnapshotEffectsTimer();
    stopCommitSnapshotEffectsTimer();

    {
      recordCommitTime();
    }

    startCommitHostEffectsTimer();
    stopCommitHostEffectsTimer();
    startCommitLifeCyclesTimer();
    stopCommitLifeCyclesTimer();
  }

  stopCommitTimer();
  var rootDidHavePassiveEffects = rootDoesHavePassiveEffects;

  if (rootDoesHavePassiveEffects) {
    // This commit has passive effects. Stash a reference to them. But don't
    // schedule a callback until after flushing layout work.
    rootDoesHavePassiveEffects = false;
    rootWithPendingPassiveEffects = root;
    pendingPassiveEffectsExpirationTime = expirationTime;
    pendingPassiveEffectsRenderPriority = renderPriorityLevel;
  } else {
    // We are done with the effect chain at this point so let's clear the
    // nextEffect pointers to assist with GC. If we have passive effects, we'll
    // clear this in flushPassiveEffects.
    nextEffect = firstEffect;

    while (nextEffect !== null) {
      var nextNextEffect = nextEffect.nextEffect;
      nextEffect.nextEffect = null;
      nextEffect = nextNextEffect;
    }
  } // Check if there's remaining work on this root


  var remainingExpirationTime = root.firstPendingTime;

  if (remainingExpirationTime !== NoWork) {
    {
      if (spawnedWorkDuringRender !== null) {
        var expirationTimes = spawnedWorkDuringRender;
        spawnedWorkDuringRender = null;

        for (var i = 0; i < expirationTimes.length; i++) {
          scheduleInteractions(root, expirationTimes[i], root.memoizedInteractions);
        }
      }

      schedulePendingInteractions(root, remainingExpirationTime);
    }
  } else {
    // If there's no remaining work, we can clear the set of already failed
    // error boundaries.
    legacyErrorBoundariesThatAlreadyFailed = null;
  }

  {
    if (!rootDidHavePassiveEffects) {
      // If there are no passive effects, then we can complete the pending interactions.
      // Otherwise, we'll wait until after the passive effects are flushed.
      // Wait to do this until after remaining work has been scheduled,
      // so that we don't prematurely signal complete for interactions when there's e.g. hidden work.
      finishPendingInteractions(root, expirationTime);
    }
  }

  if (remainingExpirationTime === Sync) {
    // Count the number of times the root synchronously re-renders without
    // finishing. If there are too many, it indicates an infinite update loop.
    if (root === rootWithNestedUpdates) {
      nestedUpdateCount++;
    } else {
      nestedUpdateCount = 0;
      rootWithNestedUpdates = root;
    }
  } else {
    nestedUpdateCount = 0;
  }

  onCommitRoot(finishedWork.stateNode, expirationTime); // Always call this before exiting `commitRoot`, to ensure that any
  // additional work on this root is scheduled.

  ensureRootIsScheduled(root);

  if (hasUncaughtError) {
    hasUncaughtError = false;
    var _error3 = firstUncaughtError;
    firstUncaughtError = null;
    throw _error3;
  }

  if ((executionContext & LegacyUnbatchedContext) !== NoContext) {
    // This is a legacy edge case. We just committed the initial mount of
    // a ReactDOM.render-ed root inside of batchedUpdates. The commit fired
    // synchronously, but layout updates should be deferred until the end
    // of the batch.
    return null;
  } // If layout work was scheduled, flush it now.


  flushSyncCallbackQueue();
  return null;
}

function commitBeforeMutationEffects() {
  while (nextEffect !== null) {
    var effectTag = nextEffect.effectTag;

    if ((effectTag & Snapshot) !== NoEffect) {
      setCurrentFiber(nextEffect);
      recordEffect();
      var current = nextEffect.alternate;
      commitBeforeMutationLifeCycles(current, nextEffect);
      resetCurrentFiber();
    }

    if ((effectTag & Passive) !== NoEffect) {
      // If there are passive effects, schedule a callback to flush at
      // the earliest opportunity.
      if (!rootDoesHavePassiveEffects) {
        rootDoesHavePassiveEffects = true;
        scheduleCallback(NormalPriority, function () {
          flushPassiveEffects();
          return null;
        });
      }
    }

    nextEffect = nextEffect.nextEffect;
  }
}

function commitMutationEffects(root, renderPriorityLevel) {
  // TODO: Should probably move the bulk of this function to commitWork.
  while (nextEffect !== null) {
    setCurrentFiber(nextEffect);
    var effectTag = nextEffect.effectTag;

    if (effectTag & ContentReset) {
      commitResetTextContent(nextEffect);
    }

    if (effectTag & Ref) {
      var current = nextEffect.alternate;

      if (current !== null) {
        commitDetachRef(current);
      }
    } // The following switch statement is only concerned about placement,
    // updates, and deletions. To avoid needing to add a case for every possible
    // bitmap value, we remove the secondary effects from the effect tag and
    // switch on that value.


    var primaryEffectTag = effectTag & (Placement | Update | Deletion | Hydrating);

    switch (primaryEffectTag) {
      case Placement:
        {
          commitPlacement(nextEffect); // Clear the "placement" from effect tag so that we know that this is
          // inserted, before any life-cycles like componentDidMount gets called.
          // TODO: findDOMNode doesn't rely on this any more but isMounted does
          // and isMounted is deprecated anyway so we should be able to kill this.

          nextEffect.effectTag &= ~Placement;
          break;
        }

      case PlacementAndUpdate:
        {
          // Placement
          commitPlacement(nextEffect); // Clear the "placement" from effect tag so that we know that this is
          // inserted, before any life-cycles like componentDidMount gets called.

          nextEffect.effectTag &= ~Placement; // Update

          var _current = nextEffect.alternate;
          commitWork(_current, nextEffect);
          break;
        }

      case Hydrating:
        {
          nextEffect.effectTag &= ~Hydrating;
          break;
        }

      case HydratingAndUpdate:
        {
          nextEffect.effectTag &= ~Hydrating; // Update

          var _current2 = nextEffect.alternate;
          commitWork(_current2, nextEffect);
          break;
        }

      case Update:
        {
          var _current3 = nextEffect.alternate;
          commitWork(_current3, nextEffect);
          break;
        }

      case Deletion:
        {
          commitDeletion(root, nextEffect, renderPriorityLevel);
          break;
        }
    } // TODO: Only record a mutation effect if primaryEffectTag is non-zero.


    recordEffect();
    resetCurrentFiber();
    nextEffect = nextEffect.nextEffect;
  }
}

function commitLayoutEffects(root, committedExpirationTime) {
  // TODO: Should probably move the bulk of this function to commitWork.
  while (nextEffect !== null) {
    setCurrentFiber(nextEffect);
    var effectTag = nextEffect.effectTag;

    if (effectTag & (Update | Callback)) {
      recordEffect();
      var current = nextEffect.alternate;
      commitLifeCycles(root, current, nextEffect);
    }

    if (effectTag & Ref) {
      recordEffect();
      commitAttachRef(nextEffect);
    }

    resetCurrentFiber();
    nextEffect = nextEffect.nextEffect;
  }
}

function flushPassiveEffects() {
  if (pendingPassiveEffectsRenderPriority !== NoPriority) {
    var priorityLevel = pendingPassiveEffectsRenderPriority > NormalPriority ? NormalPriority : pendingPassiveEffectsRenderPriority;
    pendingPassiveEffectsRenderPriority = NoPriority;
    return runWithPriority$1(priorityLevel, flushPassiveEffectsImpl);
  }
}

function flushPassiveEffectsImpl() {
  if (rootWithPendingPassiveEffects === null) {
    return false;
  }

  var root = rootWithPendingPassiveEffects;
  var expirationTime = pendingPassiveEffectsExpirationTime;
  rootWithPendingPassiveEffects = null;
  pendingPassiveEffectsExpirationTime = NoWork;

  if (!((executionContext & (RenderContext | CommitContext)) === NoContext)) {
    {
      throw Error( "Cannot flush passive effects while already rendering." );
    }
  }

  var prevExecutionContext = executionContext;
  executionContext |= CommitContext;
  var prevInteractions = pushInteractions(root);

  {
    // Note: This currently assumes there are no passive effects on the root fiber
    // because the root is not part of its own effect list.
    // This could change in the future.
    var _effect2 = root.current.firstEffect;

    while (_effect2 !== null) {
      {
        setCurrentFiber(_effect2);
        invokeGuardedCallback(null, commitPassiveHookEffects, null, _effect2);

        if (hasCaughtError()) {
          if (!(_effect2 !== null)) {
            {
              throw Error( "Should be working on an effect." );
            }
          }

          var _error5 = clearCaughtError();

          captureCommitPhaseError(_effect2, _error5);
        }

        resetCurrentFiber();
      }

      var nextNextEffect = _effect2.nextEffect; // Remove nextEffect pointer to assist GC

      _effect2.nextEffect = null;
      _effect2 = nextNextEffect;
    }
  }

  {
    popInteractions(prevInteractions);
    finishPendingInteractions(root, expirationTime);
  }

  executionContext = prevExecutionContext;
  flushSyncCallbackQueue(); // If additional passive effects were scheduled, increment a counter. If this
  // exceeds the limit, we'll fire a warning.

  nestedPassiveUpdateCount = rootWithPendingPassiveEffects === null ? 0 : nestedPassiveUpdateCount + 1;
  return true;
}

function isAlreadyFailedLegacyErrorBoundary(instance) {
  return legacyErrorBoundariesThatAlreadyFailed !== null && legacyErrorBoundariesThatAlreadyFailed.has(instance);
}
function markLegacyErrorBoundaryAsFailed(instance) {
  if (legacyErrorBoundariesThatAlreadyFailed === null) {
    legacyErrorBoundariesThatAlreadyFailed = new Set([instance]);
  } else {
    legacyErrorBoundariesThatAlreadyFailed.add(instance);
  }
}

function prepareToThrowUncaughtError(error) {
  if (!hasUncaughtError) {
    hasUncaughtError = true;
    firstUncaughtError = error;
  }
}

var onUncaughtError = prepareToThrowUncaughtError;

function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error) {
  var errorInfo = createCapturedValue(error, sourceFiber);
  var update = createRootErrorUpdate(rootFiber, errorInfo, Sync);
  enqueueUpdate(rootFiber, update);
  var root = markUpdateTimeFromFiberToRoot(rootFiber, Sync);

  if (root !== null) {
    ensureRootIsScheduled(root);
    schedulePendingInteractions(root, Sync);
  }
}

function captureCommitPhaseError(sourceFiber, error) {
  if (sourceFiber.tag === HostRoot) {
    // Error was thrown at the root. There is no parent, so the root
    // itself should capture it.
    captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error);
    return;
  }

  var fiber = sourceFiber.return;

  while (fiber !== null) {
    if (fiber.tag === HostRoot) {
      captureCommitPhaseErrorOnRoot(fiber, sourceFiber, error);
      return;
    } else if (fiber.tag === ClassComponent) {
      var ctor = fiber.type;
      var instance = fiber.stateNode;

      if (typeof ctor.getDerivedStateFromError === 'function' || typeof instance.componentDidCatch === 'function' && !isAlreadyFailedLegacyErrorBoundary(instance)) {
        var errorInfo = createCapturedValue(error, sourceFiber);
        var update = createClassErrorUpdate(fiber, errorInfo, // TODO: This is always sync
        Sync);
        enqueueUpdate(fiber, update);
        var root = markUpdateTimeFromFiberToRoot(fiber, Sync);

        if (root !== null) {
          ensureRootIsScheduled(root);
          schedulePendingInteractions(root, Sync);
        }

        return;
      }
    }

    fiber = fiber.return;
  }
}
function pingSuspendedRoot(root, thenable, suspendedTime) {
  var pingCache = root.pingCache;

  if (pingCache !== null) {
    // The thenable resolved, so we no longer need to memoize, because it will
    // never be thrown again.
    pingCache.delete(thenable);
  }

  if (workInProgressRoot === root && renderExpirationTime$1 === suspendedTime) {
    // Received a ping at the same priority level at which we're currently
    // rendering. We might want to restart this render. This should mirror
    // the logic of whether or not a root suspends once it completes.
    // TODO: If we're rendering sync either due to Sync, Batched or expired,
    // we should probably never restart.
    // If we're suspended with delay, we'll always suspend so we can always
    // restart. If we're suspended without any updates, it might be a retry.
    // If it's early in the retry we can restart. We can't know for sure
    // whether we'll eventually process an update during this render pass,
    // but it's somewhat unlikely that we get to a ping before that, since
    // getting to the root most update is usually very fast.
    if (workInProgressRootExitStatus === RootSuspendedWithDelay || workInProgressRootExitStatus === RootSuspended && workInProgressRootLatestProcessedExpirationTime === Sync && now() - globalMostRecentFallbackTime < FALLBACK_THROTTLE_MS) {
      // Restart from the root. Don't need to schedule a ping because
      // we're already working on this tree.
      prepareFreshStack(root, renderExpirationTime$1);
    } else {
      // Even though we can't restart right now, we might get an
      // opportunity later. So we mark this render as having a ping.
      workInProgressRootHasPendingPing = true;
    }

    return;
  }

  if (!isRootSuspendedAtTime(root, suspendedTime)) {
    // The root is no longer suspended at this time.
    return;
  }

  var lastPingedTime = root.lastPingedTime;

  if (lastPingedTime !== NoWork && lastPingedTime < suspendedTime) {
    // There's already a lower priority ping scheduled.
    return;
  } // Mark the time at which this ping was scheduled.


  root.lastPingedTime = suspendedTime;

  ensureRootIsScheduled(root);
  schedulePendingInteractions(root, suspendedTime);
}

function retryTimedOutBoundary(boundaryFiber, retryTime) {
  // The boundary fiber (a Suspense component or SuspenseList component)
  // previously was rendered in its fallback state. One of the promises that
  // suspended it has resolved, which means at least part of the tree was
  // likely unblocked. Try rendering again, at a new expiration time.
  if (retryTime === NoWork) {
    var suspenseConfig = null; // Retries don't carry over the already committed update.

    var currentTime = requestCurrentTimeForUpdate();
    retryTime = computeExpirationForFiber(currentTime, boundaryFiber, suspenseConfig);
  } // TODO: Special case idle priority?


  var root = markUpdateTimeFromFiberToRoot(boundaryFiber, retryTime);

  if (root !== null) {
    ensureRootIsScheduled(root);
    schedulePendingInteractions(root, retryTime);
  }
}
function resolveRetryThenable(boundaryFiber, thenable) {
  var retryTime = NoWork; // Default

  var retryCache;

  {
    retryCache = boundaryFiber.stateNode;
  }

  if (retryCache !== null) {
    // The thenable resolved, so we no longer need to memoize, because it will
    // never be thrown again.
    retryCache.delete(thenable);
  }

  retryTimedOutBoundary(boundaryFiber, retryTime);
} // Computes the next Just Noticeable Difference (JND) boundary.
// The theory is that a person can't tell the difference between small differences in time.
// Therefore, if we wait a bit longer than necessary that won't translate to a noticeable
// difference in the experience. However, waiting for longer might mean that we can avoid
// showing an intermediate loading state. The longer we have already waited, the harder it
// is to tell small differences in time. Therefore, the longer we've already waited,
// the longer we can wait additionally. At some point we have to give up though.
// We pick a train model where the next boundary commits at a consistent schedule.
// These particular numbers are vague estimates. We expect to adjust them based on research.

function jnd(timeElapsed) {
  return timeElapsed < 120 ? 120 : timeElapsed < 480 ? 480 : timeElapsed < 1080 ? 1080 : timeElapsed < 1920 ? 1920 : timeElapsed < 3000 ? 3000 : timeElapsed < 4320 ? 4320 : ceil(timeElapsed / 1960) * 1960;
}

function computeMsUntilSuspenseLoadingDelay(mostRecentEventTime, committedExpirationTime, suspenseConfig) {
  var busyMinDurationMs = suspenseConfig.busyMinDurationMs | 0;

  if (busyMinDurationMs <= 0) {
    return 0;
  }

  var busyDelayMs = suspenseConfig.busyDelayMs | 0; // Compute the time until this render pass would expire.

  var currentTimeMs = now();
  var eventTimeMs = inferTimeFromExpirationTimeWithSuspenseConfig(mostRecentEventTime, suspenseConfig);
  var timeElapsed = currentTimeMs - eventTimeMs;

  if (timeElapsed <= busyDelayMs) {
    // If we haven't yet waited longer than the initial delay, we don't
    // have to wait any additional time.
    return 0;
  }

  var msUntilTimeout = busyDelayMs + busyMinDurationMs - timeElapsed; // This is the value that is passed to `setTimeout`.

  return msUntilTimeout;
}

function checkForNestedUpdates() {
  if (nestedUpdateCount > NESTED_UPDATE_LIMIT) {
    nestedUpdateCount = 0;
    rootWithNestedUpdates = null;

    {
      {
        throw Error( "Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops." );
      }
    }
  }

  {
    if (nestedPassiveUpdateCount > NESTED_PASSIVE_UPDATE_LIMIT) {
      nestedPassiveUpdateCount = 0;

      error('Maximum update depth exceeded. This can happen when a component ' + "calls setState inside useEffect, but useEffect either doesn't " + 'have a dependency array, or one of the dependencies changes on ' + 'every render.');
    }
  }
}

function flushRenderPhaseStrictModeWarningsInDEV() {
  {
    ReactStrictModeWarnings.flushLegacyContextWarning();

    {
      ReactStrictModeWarnings.flushPendingUnsafeLifecycleWarnings();
    }
  }
}

function stopFinishedWorkLoopTimer() {
  var didCompleteRoot = true;
  stopWorkLoopTimer(interruptedBy, didCompleteRoot);
  interruptedBy = null;
}

function stopInterruptedWorkLoopTimer() {
  // TODO: Track which fiber caused the interruption.
  var didCompleteRoot = false;
  stopWorkLoopTimer(interruptedBy, didCompleteRoot);
  interruptedBy = null;
}

function checkForInterruption(fiberThatReceivedUpdate, updateExpirationTime) {
  if ( workInProgressRoot !== null && updateExpirationTime > renderExpirationTime$1) {
    interruptedBy = fiberThatReceivedUpdate;
  }
}

var didWarnStateUpdateForUnmountedComponent = null;

function warnAboutUpdateOnUnmountedFiberInDEV(fiber) {
  {
    var tag = fiber.tag;

    if (tag !== HostRoot && tag !== ClassComponent && tag !== FunctionComponent && tag !== ForwardRef && tag !== MemoComponent && tag !== SimpleMemoComponent && tag !== Block) {
      // Only warn for user-defined components, not internal ones like Suspense.
      return;
    }
    // the problematic code almost always lies inside that component.


    var componentName = getComponentName(fiber.type) || 'ReactComponent';

    if (didWarnStateUpdateForUnmountedComponent !== null) {
      if (didWarnStateUpdateForUnmountedComponent.has(componentName)) {
        return;
      }

      didWarnStateUpdateForUnmountedComponent.add(componentName);
    } else {
      didWarnStateUpdateForUnmountedComponent = new Set([componentName]);
    }

    error("Can't perform a React state update on an unmounted component. This " + 'is a no-op, but it indicates a memory leak in your application. To ' + 'fix, cancel all subscriptions and asynchronous tasks in %s.%s', tag === ClassComponent ? 'the componentWillUnmount method' : 'a useEffect cleanup function', getStackByFiberInDevAndProd(fiber));
  }
}

var beginWork$1;

{
  var dummyFiber = null;

  beginWork$1 = function (current, unitOfWork, expirationTime) {
    // If a component throws an error, we replay it again in a synchronously
    // dispatched event, so that the debugger will treat it as an uncaught
    // error See ReactErrorUtils for more information.
    // Before entering the begin phase, copy the work-in-progress onto a dummy
    // fiber. If beginWork throws, we'll use this to reset the state.
    var originalWorkInProgressCopy = assignFiberPropertiesInDEV(dummyFiber, unitOfWork);

    try {
      return beginWork(current, unitOfWork, expirationTime);
    } catch (originalError) {
      if (originalError !== null && typeof originalError === 'object' && typeof originalError.then === 'function') {
        // Don't replay promises. Treat everything else like an error.
        throw originalError;
      } // Keep this code in sync with handleError; any changes here must have
      // corresponding changes there.


      resetContextDependencies();
      resetHooksAfterThrow(); // Don't reset current debug fiber, since we're about to work on the
      // same fiber again.
      // Unwind the failed stack frame

      unwindInterruptedWork(unitOfWork); // Restore the original properties of the fiber.

      assignFiberPropertiesInDEV(unitOfWork, originalWorkInProgressCopy);

      if ( unitOfWork.mode & ProfileMode) {
        // Reset the profiler timer.
        startProfilerTimer(unitOfWork);
      } // Run beginWork again.


      invokeGuardedCallback(null, beginWork, null, current, unitOfWork, expirationTime);

      if (hasCaughtError()) {
        var replayError = clearCaughtError(); // `invokeGuardedCallback` sometimes sets an expando `_suppressLogging`.
        // Rethrow this error instead of the original one.

        throw replayError;
      } else {
        // This branch is reachable if the render phase is impure.
        throw originalError;
      }
    }
  };
}

var didWarnAboutUpdateInRender = false;
var didWarnAboutUpdateInGetChildContext = false;

function warnAboutRenderPhaseUpdatesInDEV(fiber) {
  {
    if ((executionContext & RenderContext) !== NoContext) {
      switch (fiber.tag) {
        case FunctionComponent:
        case ForwardRef:
        case SimpleMemoComponent:
          {
            error('Cannot update a component from inside the function body of a ' + 'different component.');

            break;
          }

        case ClassComponent:
          {
            switch (phase) {
              case 'getChildContext':
                if (didWarnAboutUpdateInGetChildContext) {
                  return;
                }

                error('setState(...): Cannot call setState() inside getChildContext()');

                didWarnAboutUpdateInGetChildContext = true;
                break;

              case 'render':
                if (didWarnAboutUpdateInRender) {
                  return;
                }

                error('Cannot update during an existing state transition (such as ' + 'within `render`). Render methods should be a pure ' + 'function of props and state.');

                didWarnAboutUpdateInRender = true;
                break;
            }

            break;
          }
      }
    }
  }
} // a 'shared' variable that changes when act() opens/closes in tests.


var IsThisRendererActing = {
  current: false
};
function warnIfNotScopedWithMatchingAct(fiber) {
  {
    if ( IsSomeRendererActing.current === true && IsThisRendererActing.current !== true) {
      error("It looks like you're using the wrong act() around your test interactions.\n" + 'Be sure to use the matching version of act() corresponding to your renderer:\n\n' + '// for react-dom:\n' + "import {act} from 'react-dom/test-utils';\n" + '// ...\n' + 'act(() => ...);\n\n' + '// for react-test-renderer:\n' + "import TestRenderer from 'react-test-renderer';\n" + 'const {act} = TestRenderer;\n' + '// ...\n' + 'act(() => ...);' + '%s', getStackByFiberInDevAndProd(fiber));
    }
  }
}
function warnIfNotCurrentlyActingEffectsInDEV(fiber) {
  {
    if ( (fiber.mode & StrictMode) !== NoMode && IsSomeRendererActing.current === false && IsThisRendererActing.current === false) {
      error('An update to %s ran an effect, but was not wrapped in act(...).\n\n' + 'When testing, code that causes React state updates should be ' + 'wrapped into act(...):\n\n' + 'act(() => {\n' + '  /* fire events that update state */\n' + '});\n' + '/* assert on the output */\n\n' + "This ensures that you're testing the behavior the user would see " + 'in the browser.' + ' Learn more at https://fb.me/react-wrap-tests-with-act' + '%s', getComponentName(fiber.type), getStackByFiberInDevAndProd(fiber));
    }
  }
}

function warnIfNotCurrentlyActingUpdatesInDEV(fiber) {
  {
    if ( executionContext === NoContext && IsSomeRendererActing.current === false && IsThisRendererActing.current === false) {
      error('An update to %s inside a test was not wrapped in act(...).\n\n' + 'When testing, code that causes React state updates should be ' + 'wrapped into act(...):\n\n' + 'act(() => {\n' + '  /* fire events that update state */\n' + '});\n' + '/* assert on the output */\n\n' + "This ensures that you're testing the behavior the user would see " + 'in the browser.' + ' Learn more at https://fb.me/react-wrap-tests-with-act' + '%s', getComponentName(fiber.type), getStackByFiberInDevAndProd(fiber));
    }
  }
}

var warnIfNotCurrentlyActingUpdatesInDev = warnIfNotCurrentlyActingUpdatesInDEV; // In tests, we want to enforce a mocked scheduler.

var didWarnAboutUnmockedScheduler = false; // TODO Before we release concurrent mode, revisit this and decide whether a mocked
// scheduler is the actual recommendation. The alternative could be a testing build,
// a new lib, or whatever; we dunno just yet. This message is for early adopters
// to get their tests right.

function warnIfUnmockedScheduler(fiber) {
  {
    if (didWarnAboutUnmockedScheduler === false && Scheduler.unstable_flushAllWithoutAsserting === undefined) {
      if (fiber.mode & BlockingMode || fiber.mode & ConcurrentMode) {
        didWarnAboutUnmockedScheduler = true;

        error('In Concurrent or Sync modes, the "scheduler" module needs to be mocked ' + 'to guarantee consistent behaviour across tests and browsers. ' + 'For example, with jest: \n' + "jest.mock('scheduler', () => require('scheduler/unstable_mock'));\n\n" + 'For more info, visit https://fb.me/react-mock-scheduler');
      }
    }
  }
}

function computeThreadID(root, expirationTime) {
  // Interaction threads are unique per root and expiration time.
  return expirationTime * 1000 + root.interactionThreadID;
}

function markSpawnedWork(expirationTime) {

  if (spawnedWorkDuringRender === null) {
    spawnedWorkDuringRender = [expirationTime];
  } else {
    spawnedWorkDuringRender.push(expirationTime);
  }
}

function scheduleInteractions(root, expirationTime, interactions) {

  if (interactions.size > 0) {
    var pendingInteractionMap = root.pendingInteractionMap;
    var pendingInteractions = pendingInteractionMap.get(expirationTime);

    if (pendingInteractions != null) {
      interactions.forEach(function (interaction) {
        if (!pendingInteractions.has(interaction)) {
          // Update the pending async work count for previously unscheduled interaction.
          interaction.__count++;
        }

        pendingInteractions.add(interaction);
      });
    } else {
      pendingInteractionMap.set(expirationTime, new Set(interactions)); // Update the pending async work count for the current interactions.

      interactions.forEach(function (interaction) {
        interaction.__count++;
      });
    }

    var subscriber = tracing.__subscriberRef.current;

    if (subscriber !== null) {
      var threadID = computeThreadID(root, expirationTime);
      subscriber.onWorkScheduled(interactions, threadID);
    }
  }
}

function schedulePendingInteractions(root, expirationTime) {

  scheduleInteractions(root, expirationTime, tracing.__interactionsRef.current);
}

function startWorkOnPendingInteractions(root, expirationTime) {
  // we can accurately attribute time spent working on it, And so that cascading
  // work triggered during the render phase will be associated with it.


  var interactions = new Set();
  root.pendingInteractionMap.forEach(function (scheduledInteractions, scheduledExpirationTime) {
    if (scheduledExpirationTime >= expirationTime) {
      scheduledInteractions.forEach(function (interaction) {
        return interactions.add(interaction);
      });
    }
  }); // Store the current set of interactions on the FiberRoot for a few reasons:
  // We can re-use it in hot functions like performConcurrentWorkOnRoot()
  // without having to recalculate it. We will also use it in commitWork() to
  // pass to any Profiler onRender() hooks. This also provides DevTools with a
  // way to access it when the onCommitRoot() hook is called.

  root.memoizedInteractions = interactions;

  if (interactions.size > 0) {
    var subscriber = tracing.__subscriberRef.current;

    if (subscriber !== null) {
      var threadID = computeThreadID(root, expirationTime);

      try {
        subscriber.onWorkStarted(interactions, threadID);
      } catch (error) {
        // If the subscriber throws, rethrow it in a separate task
        scheduleCallback(ImmediatePriority, function () {
          throw error;
        });
      }
    }
  }
}

function finishPendingInteractions(root, committedExpirationTime) {

  var earliestRemainingTimeAfterCommit = root.firstPendingTime;
  var subscriber;

  try {
    subscriber = tracing.__subscriberRef.current;

    if (subscriber !== null && root.memoizedInteractions.size > 0) {
      var threadID = computeThreadID(root, committedExpirationTime);
      subscriber.onWorkStopped(root.memoizedInteractions, threadID);
    }
  } catch (error) {
    // If the subscriber throws, rethrow it in a separate task
    scheduleCallback(ImmediatePriority, function () {
      throw error;
    });
  } finally {
    // Clear completed interactions from the pending Map.
    // Unless the render was suspended or cascading work was scheduled,
    // In which case– leave pending interactions until the subsequent render.
    var pendingInteractionMap = root.pendingInteractionMap;
    pendingInteractionMap.forEach(function (scheduledInteractions, scheduledExpirationTime) {
      // Only decrement the pending interaction count if we're done.
      // If there's still work at the current priority,
      // That indicates that we are waiting for suspense data.
      if (scheduledExpirationTime > earliestRemainingTimeAfterCommit) {
        pendingInteractionMap.delete(scheduledExpirationTime);
        scheduledInteractions.forEach(function (interaction) {
          interaction.__count--;

          if (subscriber !== null && interaction.__count === 0) {
            try {
              subscriber.onInteractionScheduledWorkCompleted(interaction);
            } catch (error) {
              // If the subscriber throws, rethrow it in a separate task
              scheduleCallback(ImmediatePriority, function () {
                throw error;
              });
            }
          }
        });
      }
    });
  }
}

var onScheduleFiberRoot = null;
var onCommitFiberRoot = null;
var onCommitFiberUnmount = null;
var hasLoggedError = false;
var isDevToolsPresent = typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined';
function injectInternals(internals) {
  if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
    // No DevTools
    return false;
  }

  var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;

  if (hook.isDisabled) {
    // This isn't a real property on the hook, but it can be set to opt out
    // of DevTools integration and associated warnings and logs.
    // https://github.com/facebook/react/issues/3877
    return true;
  }

  if (!hook.supportsFiber) {
    {
      error('The installed version of React DevTools is too old and will not work ' + 'with the current version of React. Please update React DevTools. ' + 'https://fb.me/react-devtools');
    } // DevTools exists, even though it doesn't support Fiber.


    return true;
  }

  try {
    var rendererID = hook.inject(internals); // We have successfully injected, so now it is safe to set up hooks.

    if (true) {
      // Only used by Fast Refresh
      if (typeof hook.onScheduleFiberRoot === 'function') {
        onScheduleFiberRoot = function (root, children) {
          try {
            hook.onScheduleFiberRoot(rendererID, root, children);
          } catch (err) {
            if ( true && !hasLoggedError) {
              hasLoggedError = true;

              error('React instrumentation encountered an error: %s', err);
            }
          }
        };
      }
    }

    onCommitFiberRoot = function (root, expirationTime) {
      try {
        var didError = (root.current.effectTag & DidCapture) === DidCapture;

        if (enableProfilerTimer) {
          var currentTime = getCurrentTime();
          var priorityLevel = inferPriorityFromExpirationTime(currentTime, expirationTime);
          hook.onCommitFiberRoot(rendererID, root, priorityLevel, didError);
        } else {
          hook.onCommitFiberRoot(rendererID, root, undefined, didError);
        }
      } catch (err) {
        if (true) {
          if (!hasLoggedError) {
            hasLoggedError = true;

            error('React instrumentation encountered an error: %s', err);
          }
        }
      }
    };

    onCommitFiberUnmount = function (fiber) {
      try {
        hook.onCommitFiberUnmount(rendererID, fiber);
      } catch (err) {
        if (true) {
          if (!hasLoggedError) {
            hasLoggedError = true;

            error('React instrumentation encountered an error: %s', err);
          }
        }
      }
    };
  } catch (err) {
    // Catch all errors because it is unsafe to throw during initialization.
    {
      error('React instrumentation encountered an error: %s.', err);
    }
  } // DevTools exists


  return true;
}
function onScheduleRoot(root, children) {
  if (typeof onScheduleFiberRoot === 'function') {
    onScheduleFiberRoot(root, children);
  }
}
function onCommitRoot(root, expirationTime) {
  if (typeof onCommitFiberRoot === 'function') {
    onCommitFiberRoot(root, expirationTime);
  }
}
function onCommitUnmount(fiber) {
  if (typeof onCommitFiberUnmount === 'function') {
    onCommitFiberUnmount(fiber);
  }
}

var hasBadMapPolyfill;

{
  hasBadMapPolyfill = false;

  try {
    var nonExtensibleObject = Object.preventExtensions({});
    var testMap = new Map([[nonExtensibleObject, null]]);
    var testSet = new Set([nonExtensibleObject]); // This is necessary for Rollup to not consider these unused.
    // https://github.com/rollup/rollup/issues/1771
    // TODO: we can remove these if Rollup fixes the bug.

    testMap.set(0, 0);
    testSet.add(0);
  } catch (e) {
    // TODO: Consider warning about bad polyfills
    hasBadMapPolyfill = true;
  }
}

var debugCounter = 1;

function FiberNode(tag, pendingProps, key, mode) {
  // Instance
  this.tag = tag;
  this.key = key;
  this.elementType = null;
  this.type = null;
  this.stateNode = null; // Fiber

  this.return = null;
  this.child = null;
  this.sibling = null;
  this.index = 0;
  this.ref = null;
  this.pendingProps = pendingProps;
  this.memoizedProps = null;
  this.updateQueue = null;
  this.memoizedState = null;
  this.dependencies = null;
  this.mode = mode; // Effects

  this.effectTag = NoEffect;
  this.nextEffect = null;
  this.firstEffect = null;
  this.lastEffect = null;
  this.expirationTime = NoWork;
  this.childExpirationTime = NoWork;
  this.alternate = null;

  {
    // Note: The following is done to avoid a v8 performance cliff.
    //
    // Initializing the fields below to smis and later updating them with
    // double values will cause Fibers to end up having separate shapes.
    // This behavior/bug has something to do with Object.preventExtension().
    // Fortunately this only impacts DEV builds.
    // Unfortunately it makes React unusably slow for some applications.
    // To work around this, initialize the fields below with doubles.
    //
    // Learn more about this here:
    // https://github.com/facebook/react/issues/14365
    // https://bugs.chromium.org/p/v8/issues/detail?id=8538
    this.actualDuration = Number.NaN;
    this.actualStartTime = Number.NaN;
    this.selfBaseDuration = Number.NaN;
    this.treeBaseDuration = Number.NaN; // It's okay to replace the initial doubles with smis after initialization.
    // This won't trigger the performance cliff mentioned above,
    // and it simplifies other profiler code (including DevTools).

    this.actualDuration = 0;
    this.actualStartTime = -1;
    this.selfBaseDuration = 0;
    this.treeBaseDuration = 0;
  } // This is normally DEV-only except www when it adds listeners.
  // TODO: remove the User Timing integration in favor of Root Events.


  {
    this._debugID = debugCounter++;
    this._debugIsCurrentlyTiming = false;
  }

  {
    this._debugSource = null;
    this._debugOwner = null;
    this._debugNeedsRemount = false;
    this._debugHookTypes = null;

    if (!hasBadMapPolyfill && typeof Object.preventExtensions === 'function') {
      Object.preventExtensions(this);
    }
  }
} // This is a constructor function, rather than a POJO constructor, still
// please ensure we do the following:
// 1) Nobody should add any instance methods on this. Instance methods can be
//    more difficult to predict when they get optimized and they are almost
//    never inlined properly in static compilers.
// 2) Nobody should rely on `instanceof Fiber` for type testing. We should
//    always know when it is a fiber.
// 3) We might want to experiment with using numeric keys since they are easier
//    to optimize in a non-JIT environment.
// 4) We can easily go from a constructor to a createFiber object literal if that
//    is faster.
// 5) It should be easy to port this to a C struct and keep a C implementation
//    compatible.


var createFiber = function (tag, pendingProps, key, mode) {
  // $FlowFixMe: the shapes are exact here but Flow doesn't like constructors
  return new FiberNode(tag, pendingProps, key, mode);
};

function shouldConstruct(Component) {
  var prototype = Component.prototype;
  return !!(prototype && prototype.isReactComponent);
}

function isSimpleFunctionComponent(type) {
  return typeof type === 'function' && !shouldConstruct(type) && type.defaultProps === undefined;
}
function resolveLazyComponentTag(Component) {
  if (typeof Component === 'function') {
    return shouldConstruct(Component) ? ClassComponent : FunctionComponent;
  } else if (Component !== undefined && Component !== null) {
    var $$typeof = Component.$$typeof;

    if ($$typeof === REACT_FORWARD_REF_TYPE) {
      return ForwardRef;
    }

    if ($$typeof === REACT_MEMO_TYPE) {
      return MemoComponent;
    }
  }

  return IndeterminateComponent;
} // This is used to create an alternate fiber to do work on.

function createWorkInProgress(current, pendingProps) {
  var workInProgress = current.alternate;

  if (workInProgress === null) {
    // We use a double buffering pooling technique because we know that we'll
    // only ever need at most two versions of a tree. We pool the "other" unused
    // node that we're free to reuse. This is lazily created to avoid allocating
    // extra objects for things that are never updated. It also allow us to
    // reclaim the extra memory if needed.
    workInProgress = createFiber(current.tag, pendingProps, current.key, current.mode);
    workInProgress.elementType = current.elementType;
    workInProgress.type = current.type;
    workInProgress.stateNode = current.stateNode;

    {
      // DEV-only fields
      {
        workInProgress._debugID = current._debugID;
      }

      workInProgress._debugSource = current._debugSource;
      workInProgress._debugOwner = current._debugOwner;
      workInProgress._debugHookTypes = current._debugHookTypes;
    }

    workInProgress.alternate = current;
    current.alternate = workInProgress;
  } else {
    workInProgress.pendingProps = pendingProps; // We already have an alternate.
    // Reset the effect tag.

    workInProgress.effectTag = NoEffect; // The effect list is no longer valid.

    workInProgress.nextEffect = null;
    workInProgress.firstEffect = null;
    workInProgress.lastEffect = null;

    {
      // We intentionally reset, rather than copy, actualDuration & actualStartTime.
      // This prevents time from endlessly accumulating in new commits.
      // This has the downside of resetting values for different priority renders,
      // But works for yielding (the common case) and should support resuming.
      workInProgress.actualDuration = 0;
      workInProgress.actualStartTime = -1;
    }
  }

  workInProgress.childExpirationTime = current.childExpirationTime;
  workInProgress.expirationTime = current.expirationTime;
  workInProgress.child = current.child;
  workInProgress.memoizedProps = current.memoizedProps;
  workInProgress.memoizedState = current.memoizedState;
  workInProgress.updateQueue = current.updateQueue; // Clone the dependencies object. This is mutated during the render phase, so
  // it cannot be shared with the current fiber.

  var currentDependencies = current.dependencies;
  workInProgress.dependencies = currentDependencies === null ? null : {
    expirationTime: currentDependencies.expirationTime,
    firstContext: currentDependencies.firstContext,
    responders: currentDependencies.responders
  }; // These will be overridden during the parent's reconciliation

  workInProgress.sibling = current.sibling;
  workInProgress.index = current.index;
  workInProgress.ref = current.ref;

  {
    workInProgress.selfBaseDuration = current.selfBaseDuration;
    workInProgress.treeBaseDuration = current.treeBaseDuration;
  }

  {
    workInProgress._debugNeedsRemount = current._debugNeedsRemount;

    switch (workInProgress.tag) {
      case IndeterminateComponent:
      case FunctionComponent:
      case SimpleMemoComponent:
        workInProgress.type = resolveFunctionForHotReloading(current.type);
        break;

      case ClassComponent:
        workInProgress.type = resolveClassForHotReloading(current.type);
        break;

      case ForwardRef:
        workInProgress.type = resolveForwardRefForHotReloading(current.type);
        break;
    }
  }

  return workInProgress;
} // Used to reuse a Fiber for a second pass.

function resetWorkInProgress(workInProgress, renderExpirationTime) {
  // This resets the Fiber to what createFiber or createWorkInProgress would
  // have set the values to before during the first pass. Ideally this wouldn't
  // be necessary but unfortunately many code paths reads from the workInProgress
  // when they should be reading from current and writing to workInProgress.
  // We assume pendingProps, index, key, ref, return are still untouched to
  // avoid doing another reconciliation.
  // Reset the effect tag but keep any Placement tags, since that's something
  // that child fiber is setting, not the reconciliation.
  workInProgress.effectTag &= Placement; // The effect list is no longer valid.

  workInProgress.nextEffect = null;
  workInProgress.firstEffect = null;
  workInProgress.lastEffect = null;
  var current = workInProgress.alternate;

  if (current === null) {
    // Reset to createFiber's initial values.
    workInProgress.childExpirationTime = NoWork;
    workInProgress.expirationTime = renderExpirationTime;
    workInProgress.child = null;
    workInProgress.memoizedProps = null;
    workInProgress.memoizedState = null;
    workInProgress.updateQueue = null;
    workInProgress.dependencies = null;

    {
      // Note: We don't reset the actualTime counts. It's useful to accumulate
      // actual time across multiple render passes.
      workInProgress.selfBaseDuration = 0;
      workInProgress.treeBaseDuration = 0;
    }
  } else {
    // Reset to the cloned values that createWorkInProgress would've.
    workInProgress.childExpirationTime = current.childExpirationTime;
    workInProgress.expirationTime = current.expirationTime;
    workInProgress.child = current.child;
    workInProgress.memoizedProps = current.memoizedProps;
    workInProgress.memoizedState = current.memoizedState;
    workInProgress.updateQueue = current.updateQueue; // Clone the dependencies object. This is mutated during the render phase, so
    // it cannot be shared with the current fiber.

    var currentDependencies = current.dependencies;
    workInProgress.dependencies = currentDependencies === null ? null : {
      expirationTime: currentDependencies.expirationTime,
      firstContext: currentDependencies.firstContext,
      responders: currentDependencies.responders
    };

    {
      // Note: We don't reset the actualTime counts. It's useful to accumulate
      // actual time across multiple render passes.
      workInProgress.selfBaseDuration = current.selfBaseDuration;
      workInProgress.treeBaseDuration = current.treeBaseDuration;
    }
  }

  return workInProgress;
}
function createHostRootFiber(tag) {
  var mode;

  if (tag === ConcurrentRoot) {
    mode = ConcurrentMode | BlockingMode | StrictMode;
  } else if (tag === BlockingRoot) {
    mode = BlockingMode | StrictMode;
  } else {
    mode = NoMode;
  }

  if ( isDevToolsPresent) {
    // Always collect profile timings when DevTools are present.
    // This enables DevTools to start capturing timing at any point–
    // Without some nodes in the tree having empty base times.
    mode |= ProfileMode;
  }

  return createFiber(HostRoot, null, null, mode);
}
function createFiberFromTypeAndProps(type, // React$ElementType
key, pendingProps, owner, mode, expirationTime) {
  var fiber;
  var fiberTag = IndeterminateComponent; // The resolved type is set if we know what the final type will be. I.e. it's not lazy.

  var resolvedType = type;

  if (typeof type === 'function') {
    if (shouldConstruct(type)) {
      fiberTag = ClassComponent;

      {
        resolvedType = resolveClassForHotReloading(resolvedType);
      }
    } else {
      {
        resolvedType = resolveFunctionForHotReloading(resolvedType);
      }
    }
  } else if (typeof type === 'string') {
    fiberTag = HostComponent;
  } else {
    getTag: switch (type) {
      case REACT_FRAGMENT_TYPE:
        return createFiberFromFragment(pendingProps.children, mode, expirationTime, key);

      case REACT_CONCURRENT_MODE_TYPE:
        fiberTag = Mode;
        mode |= ConcurrentMode | BlockingMode | StrictMode;
        break;

      case REACT_STRICT_MODE_TYPE:
        fiberTag = Mode;
        mode |= StrictMode;
        break;

      case REACT_PROFILER_TYPE:
        return createFiberFromProfiler(pendingProps, mode, expirationTime, key);

      case REACT_SUSPENSE_TYPE:
        return createFiberFromSuspense(pendingProps, mode, expirationTime, key);

      case REACT_SUSPENSE_LIST_TYPE:
        return createFiberFromSuspenseList(pendingProps, mode, expirationTime, key);

      default:
        {
          if (typeof type === 'object' && type !== null) {
            switch (type.$$typeof) {
              case REACT_PROVIDER_TYPE:
                fiberTag = ContextProvider;
                break getTag;

              case REACT_CONTEXT_TYPE:
                // This is a consumer
                fiberTag = ContextConsumer;
                break getTag;

              case REACT_FORWARD_REF_TYPE:
                fiberTag = ForwardRef;

                {
                  resolvedType = resolveForwardRefForHotReloading(resolvedType);
                }

                break getTag;

              case REACT_MEMO_TYPE:
                fiberTag = MemoComponent;
                break getTag;

              case REACT_LAZY_TYPE:
                fiberTag = LazyComponent;
                resolvedType = null;
                break getTag;

              case REACT_BLOCK_TYPE:
                fiberTag = Block;
                break getTag;

            }
          }

          var info = '';

          {
            if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
              info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and " + 'named imports.';
            }

            var ownerName = owner ? getComponentName(owner.type) : null;

            if (ownerName) {
              info += '\n\nCheck the render method of `' + ownerName + '`.';
            }
          }

          {
            {
              throw Error( "Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: " + (type == null ? type : typeof type) + "." + info );
            }
          }
        }
    }
  }

  fiber = createFiber(fiberTag, pendingProps, key, mode);
  fiber.elementType = type;
  fiber.type = resolvedType;
  fiber.expirationTime = expirationTime;
  return fiber;
}
function createFiberFromElement(element, mode, expirationTime) {
  var owner = null;

  {
    owner = element._owner;
  }

  var type = element.type;
  var key = element.key;
  var pendingProps = element.props;
  var fiber = createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, expirationTime);

  {
    fiber._debugSource = element._source;
    fiber._debugOwner = element._owner;
  }

  return fiber;
}
function createFiberFromFragment(elements, mode, expirationTime, key) {
  var fiber = createFiber(Fragment, elements, key, mode);
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromProfiler(pendingProps, mode, expirationTime, key) {
  {
    if (typeof pendingProps.id !== 'string' || typeof pendingProps.onRender !== 'function') {
      error('Profiler must specify an "id" string and "onRender" function as props');
    }
  }

  var fiber = createFiber(Profiler, pendingProps, key, mode | ProfileMode); // TODO: The Profiler fiber shouldn't have a type. It has a tag.

  fiber.elementType = REACT_PROFILER_TYPE;
  fiber.type = REACT_PROFILER_TYPE;
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromSuspense(pendingProps, mode, expirationTime, key) {
  var fiber = createFiber(SuspenseComponent, pendingProps, key, mode); // TODO: The SuspenseComponent fiber shouldn't have a type. It has a tag.
  // This needs to be fixed in getComponentName so that it relies on the tag
  // instead.

  fiber.type = REACT_SUSPENSE_TYPE;
  fiber.elementType = REACT_SUSPENSE_TYPE;
  fiber.expirationTime = expirationTime;
  return fiber;
}
function createFiberFromSuspenseList(pendingProps, mode, expirationTime, key) {
  var fiber = createFiber(SuspenseListComponent, pendingProps, key, mode);

  {
    // TODO: The SuspenseListComponent fiber shouldn't have a type. It has a tag.
    // This needs to be fixed in getComponentName so that it relies on the tag
    // instead.
    fiber.type = REACT_SUSPENSE_LIST_TYPE;
  }

  fiber.elementType = REACT_SUSPENSE_LIST_TYPE;
  fiber.expirationTime = expirationTime;
  return fiber;
}
function createFiberFromText(content, mode, expirationTime) {
  var fiber = createFiber(HostText, content, null, mode);
  fiber.expirationTime = expirationTime;
  return fiber;
}
function createFiberFromHostInstanceForDeletion() {
  var fiber = createFiber(HostComponent, null, null, NoMode); // TODO: These should not need a type.

  fiber.elementType = 'DELETED';
  fiber.type = 'DELETED';
  return fiber;
}
function createFiberFromPortal(portal, mode, expirationTime) {
  var pendingProps = portal.children !== null ? portal.children : [];
  var fiber = createFiber(HostPortal, pendingProps, portal.key, mode);
  fiber.expirationTime = expirationTime;
  fiber.stateNode = {
    containerInfo: portal.containerInfo,
    pendingChildren: null,
    // Used by persistent updates
    implementation: portal.implementation
  };
  return fiber;
} // Used for stashing WIP properties to replay failed work in DEV.

function assignFiberPropertiesInDEV(target, source) {
  if (target === null) {
    // This Fiber's initial properties will always be overwritten.
    // We only use a Fiber to ensure the same hidden class so DEV isn't slow.
    target = createFiber(IndeterminateComponent, null, null, NoMode);
  } // This is intentionally written as a list of all properties.
  // We tried to use Object.assign() instead but this is called in
  // the hottest path, and Object.assign() was too slow:
  // https://github.com/facebook/react/issues/12502
  // This code is DEV-only so size is not a concern.


  target.tag = source.tag;
  target.key = source.key;
  target.elementType = source.elementType;
  target.type = source.type;
  target.stateNode = source.stateNode;
  target.return = source.return;
  target.child = source.child;
  target.sibling = source.sibling;
  target.index = source.index;
  target.ref = source.ref;
  target.pendingProps = source.pendingProps;
  target.memoizedProps = source.memoizedProps;
  target.updateQueue = source.updateQueue;
  target.memoizedState = source.memoizedState;
  target.dependencies = source.dependencies;
  target.mode = source.mode;
  target.effectTag = source.effectTag;
  target.nextEffect = source.nextEffect;
  target.firstEffect = source.firstEffect;
  target.lastEffect = source.lastEffect;
  target.expirationTime = source.expirationTime;
  target.childExpirationTime = source.childExpirationTime;
  target.alternate = source.alternate;

  {
    target.actualDuration = source.actualDuration;
    target.actualStartTime = source.actualStartTime;
    target.selfBaseDuration = source.selfBaseDuration;
    target.treeBaseDuration = source.treeBaseDuration;
  }

  {
    target._debugID = source._debugID;
  }

  target._debugSource = source._debugSource;
  target._debugOwner = source._debugOwner;
  target._debugIsCurrentlyTiming = source._debugIsCurrentlyTiming;
  target._debugNeedsRemount = source._debugNeedsRemount;
  target._debugHookTypes = source._debugHookTypes;
  return target;
}

function FiberRootNode(containerInfo, tag, hydrate) {
  this.tag = tag;
  this.current = null;
  this.containerInfo = containerInfo;
  this.pendingChildren = null;
  this.pingCache = null;
  this.finishedExpirationTime = NoWork;
  this.finishedWork = null;
  this.timeoutHandle = noTimeout;
  this.context = null;
  this.pendingContext = null;
  this.hydrate = hydrate;
  this.callbackNode = null;
  this.callbackPriority = NoPriority;
  this.firstPendingTime = NoWork;
  this.firstSuspendedTime = NoWork;
  this.lastSuspendedTime = NoWork;
  this.nextKnownPendingLevel = NoWork;
  this.lastPingedTime = NoWork;
  this.lastExpiredTime = NoWork;

  {
    this.interactionThreadID = tracing.unstable_getThreadID();
    this.memoizedInteractions = new Set();
    this.pendingInteractionMap = new Map();
  }
}

function createFiberRoot(containerInfo, tag, hydrate, hydrationCallbacks) {
  var root = new FiberRootNode(containerInfo, tag, hydrate);
  // stateNode is any.


  var uninitializedFiber = createHostRootFiber(tag);
  root.current = uninitializedFiber;
  uninitializedFiber.stateNode = root;
  initializeUpdateQueue(uninitializedFiber);
  return root;
}
function isRootSuspendedAtTime(root, expirationTime) {
  var firstSuspendedTime = root.firstSuspendedTime;
  var lastSuspendedTime = root.lastSuspendedTime;
  return firstSuspendedTime !== NoWork && firstSuspendedTime >= expirationTime && lastSuspendedTime <= expirationTime;
}
function markRootSuspendedAtTime(root, expirationTime) {
  var firstSuspendedTime = root.firstSuspendedTime;
  var lastSuspendedTime = root.lastSuspendedTime;

  if (firstSuspendedTime < expirationTime) {
    root.firstSuspendedTime = expirationTime;
  }

  if (lastSuspendedTime > expirationTime || firstSuspendedTime === NoWork) {
    root.lastSuspendedTime = expirationTime;
  }

  if (expirationTime <= root.lastPingedTime) {
    root.lastPingedTime = NoWork;
  }

  if (expirationTime <= root.lastExpiredTime) {
    root.lastExpiredTime = NoWork;
  }
}
function markRootUpdatedAtTime(root, expirationTime) {
  // Update the range of pending times
  var firstPendingTime = root.firstPendingTime;

  if (expirationTime > firstPendingTime) {
    root.firstPendingTime = expirationTime;
  } // Update the range of suspended times. Treat everything lower priority or
  // equal to this update as unsuspended.


  var firstSuspendedTime = root.firstSuspendedTime;

  if (firstSuspendedTime !== NoWork) {
    if (expirationTime >= firstSuspendedTime) {
      // The entire suspended range is now unsuspended.
      root.firstSuspendedTime = root.lastSuspendedTime = root.nextKnownPendingLevel = NoWork;
    } else if (expirationTime >= root.lastSuspendedTime) {
      root.lastSuspendedTime = expirationTime + 1;
    } // This is a pending level. Check if it's higher priority than the next
    // known pending level.


    if (expirationTime > root.nextKnownPendingLevel) {
      root.nextKnownPendingLevel = expirationTime;
    }
  }
}
function markRootFinishedAtTime(root, finishedExpirationTime, remainingExpirationTime) {
  // Update the range of pending times
  root.firstPendingTime = remainingExpirationTime; // Update the range of suspended times. Treat everything higher priority or
  // equal to this update as unsuspended.

  if (finishedExpirationTime <= root.lastSuspendedTime) {
    // The entire suspended range is now unsuspended.
    root.firstSuspendedTime = root.lastSuspendedTime = root.nextKnownPendingLevel = NoWork;
  } else if (finishedExpirationTime <= root.firstSuspendedTime) {
    // Part of the suspended range is now unsuspended. Narrow the range to
    // include everything between the unsuspended time (non-inclusive) and the
    // last suspended time.
    root.firstSuspendedTime = finishedExpirationTime - 1;
  }

  if (finishedExpirationTime <= root.lastPingedTime) {
    // Clear the pinged time
    root.lastPingedTime = NoWork;
  }

  if (finishedExpirationTime <= root.lastExpiredTime) {
    // Clear the expired time
    root.lastExpiredTime = NoWork;
  }
}
function markRootExpiredAtTime(root, expirationTime) {
  var lastExpiredTime = root.lastExpiredTime;

  if (lastExpiredTime === NoWork || lastExpiredTime > expirationTime) {
    root.lastExpiredTime = expirationTime;
  }
}

var didWarnAboutNestedUpdates;
var didWarnAboutFindNodeInStrictMode;

{
  didWarnAboutNestedUpdates = false;
  didWarnAboutFindNodeInStrictMode = {};
}

function getContextForSubtree(parentComponent) {
  if (!parentComponent) {
    return emptyContextObject;
  }

  var fiber = get(parentComponent);
  var parentContext = findCurrentUnmaskedContext(fiber);

  if (fiber.tag === ClassComponent) {
    var Component = fiber.type;

    if (isContextProvider(Component)) {
      return processChildContext(fiber, Component, parentContext);
    }
  }

  return parentContext;
}

function findHostInstanceWithWarning(component, methodName) {
  {
    var fiber = get(component);

    if (fiber === undefined) {
      if (typeof component.render === 'function') {
        {
          {
            throw Error( "Unable to find node on an unmounted component." );
          }
        }
      } else {
        {
          {
            throw Error( "Argument appears to not be a ReactComponent. Keys: " + Object.keys(component) );
          }
        }
      }
    }

    var hostFiber = findCurrentHostFiber(fiber);

    if (hostFiber === null) {
      return null;
    }

    if (hostFiber.mode & StrictMode) {
      var componentName = getComponentName(fiber.type) || 'Component';

      if (!didWarnAboutFindNodeInStrictMode[componentName]) {
        didWarnAboutFindNodeInStrictMode[componentName] = true;

        if (fiber.mode & StrictMode) {
          error('%s is deprecated in StrictMode. ' + '%s was passed an instance of %s which is inside StrictMode. ' + 'Instead, add a ref directly to the element you want to reference. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-find-node%s', methodName, methodName, componentName, getStackByFiberInDevAndProd(hostFiber));
        } else {
          error('%s is deprecated in StrictMode. ' + '%s was passed an instance of %s which renders StrictMode children. ' + 'Instead, add a ref directly to the element you want to reference. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-find-node%s', methodName, methodName, componentName, getStackByFiberInDevAndProd(hostFiber));
        }
      }
    }

    return hostFiber.stateNode;
  }
}

function createContainer(containerInfo, tag, hydrate, hydrationCallbacks) {
  return createFiberRoot(containerInfo, tag, hydrate);
}
function updateContainer(element, container, parentComponent, callback) {
  {
    onScheduleRoot(container, element);
  }

  var current$1 = container.current;
  var currentTime = requestCurrentTimeForUpdate();

  {
    // $FlowExpectedError - jest isn't a global, and isn't recognized outside of tests
    if ('undefined' !== typeof jest) {
      warnIfUnmockedScheduler(current$1);
      warnIfNotScopedWithMatchingAct(current$1);
    }
  }

  var suspenseConfig = requestCurrentSuspenseConfig();
  var expirationTime = computeExpirationForFiber(currentTime, current$1, suspenseConfig);
  var context = getContextForSubtree(parentComponent);

  if (container.context === null) {
    container.context = context;
  } else {
    container.pendingContext = context;
  }

  {
    if (phase === 'render' && current !== null && !didWarnAboutNestedUpdates) {
      didWarnAboutNestedUpdates = true;

      error('Render methods should be a pure function of props and state; ' + 'triggering nested component updates from render is not allowed. ' + 'If necessary, trigger nested updates in componentDidUpdate.\n\n' + 'Check the render method of %s.', getComponentName(current.type) || 'Unknown');
    }
  }

  var update = createUpdate(expirationTime, suspenseConfig); // Caution: React DevTools currently depends on this property
  // being called "element".

  update.payload = {
    element: element
  };
  callback = callback === undefined ? null : callback;

  if (callback !== null) {
    {
      if (typeof callback !== 'function') {
        error('render(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callback);
      }
    }

    update.callback = callback;
  }

  enqueueUpdate(current$1, update);
  scheduleWork(current$1, expirationTime);
  return expirationTime;
}
function getPublicRootInstance(container) {
  var containerFiber = container.current;

  if (!containerFiber.child) {
    return null;
  }

  switch (containerFiber.child.tag) {
    case HostComponent:
      return getPublicInstance(containerFiber.child.stateNode);

    default:
      return containerFiber.child.stateNode;
  }
}

function markRetryTimeImpl(fiber, retryTime) {
  var suspenseState = fiber.memoizedState;

  if (suspenseState !== null && suspenseState.dehydrated !== null) {
    if (suspenseState.retryTime < retryTime) {
      suspenseState.retryTime = retryTime;
    }
  }
} // Increases the priority of thennables when they resolve within this boundary.


function markRetryTimeIfNotHydrated(fiber, retryTime) {
  markRetryTimeImpl(fiber, retryTime);
  var alternate = fiber.alternate;

  if (alternate) {
    markRetryTimeImpl(alternate, retryTime);
  }
}

function attemptUserBlockingHydration$1(fiber) {
  if (fiber.tag !== SuspenseComponent) {
    // We ignore HostRoots here because we can't increase
    // their priority and they should not suspend on I/O,
    // since you have to wrap anything that might suspend in
    // Suspense.
    return;
  }

  var expTime = computeInteractiveExpiration(requestCurrentTimeForUpdate());
  scheduleWork(fiber, expTime);
  markRetryTimeIfNotHydrated(fiber, expTime);
}
function attemptContinuousHydration$1(fiber) {
  if (fiber.tag !== SuspenseComponent) {
    // We ignore HostRoots here because we can't increase
    // their priority and they should not suspend on I/O,
    // since you have to wrap anything that might suspend in
    // Suspense.
    return;
  }

  scheduleWork(fiber, ContinuousHydration);
  markRetryTimeIfNotHydrated(fiber, ContinuousHydration);
}
function attemptHydrationAtCurrentPriority$1(fiber) {
  if (fiber.tag !== SuspenseComponent) {
    // We ignore HostRoots here because we can't increase
    // their priority other than synchronously flush it.
    return;
  }

  var currentTime = requestCurrentTimeForUpdate();
  var expTime = computeExpirationForFiber(currentTime, fiber, null);
  scheduleWork(fiber, expTime);
  markRetryTimeIfNotHydrated(fiber, expTime);
}
function findHostInstanceWithNoPortals(fiber) {
  var hostFiber = findCurrentHostFiberWithNoPortals(fiber);

  if (hostFiber === null) {
    return null;
  }

  if (hostFiber.tag === FundamentalComponent) {
    return hostFiber.stateNode.instance;
  }

  return hostFiber.stateNode;
}

var shouldSuspendImpl = function (fiber) {
  return false;
};

function shouldSuspend(fiber) {
  return shouldSuspendImpl(fiber);
}
var overrideHookState = null;
var overrideProps = null;
var scheduleUpdate = null;
var setSuspenseHandler = null;

{
  var copyWithSetImpl = function (obj, path, idx, value) {
    if (idx >= path.length) {
      return value;
    }

    var key = path[idx];
    var updated = Array.isArray(obj) ? obj.slice() : _assign({}, obj); // $FlowFixMe number or string is fine here

    updated[key] = copyWithSetImpl(obj[key], path, idx + 1, value);
    return updated;
  };

  var copyWithSet = function (obj, path, value) {
    return copyWithSetImpl(obj, path, 0, value);
  }; // Support DevTools editable values for useState and useReducer.


  overrideHookState = function (fiber, id, path, value) {
    // For now, the "id" of stateful hooks is just the stateful hook index.
    // This may change in the future with e.g. nested hooks.
    var currentHook = fiber.memoizedState;

    while (currentHook !== null && id > 0) {
      currentHook = currentHook.next;
      id--;
    }

    if (currentHook !== null) {
      var newState = copyWithSet(currentHook.memoizedState, path, value);
      currentHook.memoizedState = newState;
      currentHook.baseState = newState; // We aren't actually adding an update to the queue,
      // because there is no update we can add for useReducer hooks that won't trigger an error.
      // (There's no appropriate action type for DevTools overrides.)
      // As a result though, React will see the scheduled update as a noop and bailout.
      // Shallow cloning props works as a workaround for now to bypass the bailout check.

      fiber.memoizedProps = _assign({}, fiber.memoizedProps);
      scheduleWork(fiber, Sync);
    }
  }; // Support DevTools props for function components, forwardRef, memo, host components, etc.


  overrideProps = function (fiber, path, value) {
    fiber.pendingProps = copyWithSet(fiber.memoizedProps, path, value);

    if (fiber.alternate) {
      fiber.alternate.pendingProps = fiber.pendingProps;
    }

    scheduleWork(fiber, Sync);
  };

  scheduleUpdate = function (fiber) {
    scheduleWork(fiber, Sync);
  };

  setSuspenseHandler = function (newShouldSuspendImpl) {
    shouldSuspendImpl = newShouldSuspendImpl;
  };
}

function injectIntoDevTools(devToolsConfig) {
  var findFiberByHostInstance = devToolsConfig.findFiberByHostInstance;
  var ReactCurrentDispatcher = ReactSharedInternals.ReactCurrentDispatcher;
  return injectInternals(_assign({}, devToolsConfig, {
    overrideHookState: overrideHookState,
    overrideProps: overrideProps,
    setSuspenseHandler: setSuspenseHandler,
    scheduleUpdate: scheduleUpdate,
    currentDispatcherRef: ReactCurrentDispatcher,
    findHostInstanceByFiber: function (fiber) {
      var hostFiber = findCurrentHostFiber(fiber);

      if (hostFiber === null) {
        return null;
      }

      return hostFiber.stateNode;
    },
    findFiberByHostInstance: function (instance) {
      if (!findFiberByHostInstance) {
        // Might not be implemented by the renderer.
        return null;
      }

      return findFiberByHostInstance(instance);
    },
    // React Refresh
    findHostInstancesForRefresh:  findHostInstancesForRefresh ,
    scheduleRefresh:  scheduleRefresh ,
    scheduleRoot:  scheduleRoot ,
    setRefreshHandler:  setRefreshHandler ,
    // Enables DevTools to append owner stacks to error messages in DEV mode.
    getCurrentFiber:  function () {
      return current;
    } 
  }));
}
var IsSomeRendererActing$1 = ReactSharedInternals.IsSomeRendererActing;

function ReactDOMRoot(container, options) {
  this._internalRoot = createRootImpl(container, ConcurrentRoot, options);
}

function ReactDOMBlockingRoot(container, tag, options) {
  this._internalRoot = createRootImpl(container, tag, options);
}

ReactDOMRoot.prototype.render = ReactDOMBlockingRoot.prototype.render = function (children) {
  var root = this._internalRoot;

  {
    if (typeof arguments[1] === 'function') {
      error('render(...): does not support the second callback argument. ' + 'To execute a side effect after rendering, declare it in a component body with useEffect().');
    }

    var container = root.containerInfo;

    if (container.nodeType !== COMMENT_NODE) {
      var hostInstance = findHostInstanceWithNoPortals(root.current);

      if (hostInstance) {
        if (hostInstance.parentNode !== container) {
          error('render(...): It looks like the React-rendered content of the ' + 'root container was removed without using React. This is not ' + 'supported and will cause errors. Instead, call ' + "root.unmount() to empty a root's container.");
        }
      }
    }
  }

  updateContainer(children, root, null, null);
};

ReactDOMRoot.prototype.unmount = ReactDOMBlockingRoot.prototype.unmount = function () {
  {
    if (typeof arguments[0] === 'function') {
      error('unmount(...): does not support a callback argument. ' + 'To execute a side effect after rendering, declare it in a component body with useEffect().');
    }
  }

  var root = this._internalRoot;
  var container = root.containerInfo;
  updateContainer(null, root, null, function () {
    unmarkContainerAsRoot(container);
  });
};

function createRootImpl(container, tag, options) {
  // Tag is either LegacyRoot or Concurrent Root
  var hydrate = options != null && options.hydrate === true;
  var hydrationCallbacks = options != null && options.hydrationOptions || null;
  var root = createContainer(container, tag, hydrate);
  markContainerAsRoot(root.current, container);

  if (hydrate && tag !== LegacyRoot) {
    var doc = container.nodeType === DOCUMENT_NODE ? container : container.ownerDocument;
    eagerlyTrapReplayableEvents(container, doc);
  }

  return root;
}
function createLegacyRoot(container, options) {
  return new ReactDOMBlockingRoot(container, LegacyRoot, options);
}
function isValidContainer(node) {
  return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || node.nodeType === COMMENT_NODE && node.nodeValue === ' react-mount-point-unstable '));
}

var ReactCurrentOwner$3 = ReactSharedInternals.ReactCurrentOwner;
var topLevelUpdateWarnings;
var warnedAboutHydrateAPI = false;

{
  topLevelUpdateWarnings = function (container) {
    if (container._reactRootContainer && container.nodeType !== COMMENT_NODE) {
      var hostInstance = findHostInstanceWithNoPortals(container._reactRootContainer._internalRoot.current);

      if (hostInstance) {
        if (hostInstance.parentNode !== container) {
          error('render(...): It looks like the React-rendered content of this ' + 'container was removed without using React. This is not ' + 'supported and will cause errors. Instead, call ' + 'ReactDOM.unmountComponentAtNode to empty a container.');
        }
      }
    }

    var isRootRenderedBySomeReact = !!container._reactRootContainer;
    var rootEl = getReactRootElementInContainer(container);
    var hasNonRootReactChild = !!(rootEl && getInstanceFromNode$1(rootEl));

    if (hasNonRootReactChild && !isRootRenderedBySomeReact) {
      error('render(...): Replacing React-rendered children with a new root ' + 'component. If you intended to update the children of this node, ' + 'you should instead have the existing children update their state ' + 'and render the new components instead of calling ReactDOM.render.');
    }

    if (container.nodeType === ELEMENT_NODE && container.tagName && container.tagName.toUpperCase() === 'BODY') {
      error('render(): Rendering components directly into document.body is ' + 'discouraged, since its children are often manipulated by third-party ' + 'scripts and browser extensions. This may lead to subtle ' + 'reconciliation issues. Try rendering into a container element created ' + 'for your app.');
    }
  };
}

function getReactRootElementInContainer(container) {
  if (!container) {
    return null;
  }

  if (container.nodeType === DOCUMENT_NODE) {
    return container.documentElement;
  } else {
    return container.firstChild;
  }
}

function shouldHydrateDueToLegacyHeuristic(container) {
  var rootElement = getReactRootElementInContainer(container);
  return !!(rootElement && rootElement.nodeType === ELEMENT_NODE && rootElement.hasAttribute(ROOT_ATTRIBUTE_NAME));
}

function legacyCreateRootFromDOMContainer(container, forceHydrate) {
  var shouldHydrate = forceHydrate || shouldHydrateDueToLegacyHeuristic(container); // First clear any existing content.

  if (!shouldHydrate) {
    var warned = false;
    var rootSibling;

    while (rootSibling = container.lastChild) {
      {
        if (!warned && rootSibling.nodeType === ELEMENT_NODE && rootSibling.hasAttribute(ROOT_ATTRIBUTE_NAME)) {
          warned = true;

          error('render(): Target node has markup rendered by React, but there ' + 'are unrelated nodes as well. This is most commonly caused by ' + 'white-space inserted around server-rendered markup.');
        }
      }

      container.removeChild(rootSibling);
    }
  }

  {
    if (shouldHydrate && !forceHydrate && !warnedAboutHydrateAPI) {
      warnedAboutHydrateAPI = true;

      warn('render(): Calling ReactDOM.render() to hydrate server-rendered markup ' + 'will stop working in React v17. Replace the ReactDOM.render() call ' + 'with ReactDOM.hydrate() if you want React to attach to the server HTML.');
    }
  }

  return createLegacyRoot(container, shouldHydrate ? {
    hydrate: true
  } : undefined);
}

function warnOnInvalidCallback$1(callback, callerName) {
  {
    if (callback !== null && typeof callback !== 'function') {
      error('%s(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callerName, callback);
    }
  }
}

function legacyRenderSubtreeIntoContainer(parentComponent, children, container, forceHydrate, callback) {
  {
    topLevelUpdateWarnings(container);
    warnOnInvalidCallback$1(callback === undefined ? null : callback, 'render');
  } // TODO: Without `any` type, Flow says "Property cannot be accessed on any
  // member of intersection type." Whyyyyyy.


  var root = container._reactRootContainer;
  var fiberRoot;

  if (!root) {
    // Initial mount
    root = container._reactRootContainer = legacyCreateRootFromDOMContainer(container, forceHydrate);
    fiberRoot = root._internalRoot;

    if (typeof callback === 'function') {
      var originalCallback = callback;

      callback = function () {
        var instance = getPublicRootInstance(fiberRoot);
        originalCallback.call(instance);
      };
    } // Initial mount should not be batched.


    unbatchedUpdates(function () {
      updateContainer(children, fiberRoot, parentComponent, callback);
    });
  } else {
    fiberRoot = root._internalRoot;

    if (typeof callback === 'function') {
      var _originalCallback = callback;

      callback = function () {
        var instance = getPublicRootInstance(fiberRoot);

        _originalCallback.call(instance);
      };
    } // Update


    updateContainer(children, fiberRoot, parentComponent, callback);
  }

  return getPublicRootInstance(fiberRoot);
}

function findDOMNode(componentOrElement) {
  {
    var owner = ReactCurrentOwner$3.current;

    if (owner !== null && owner.stateNode !== null) {
      var warnedAboutRefsInRender = owner.stateNode._warnedAboutRefsInRender;

      if (!warnedAboutRefsInRender) {
        error('%s is accessing findDOMNode inside its render(). ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(owner.type) || 'A component');
      }

      owner.stateNode._warnedAboutRefsInRender = true;
    }
  }

  if (componentOrElement == null) {
    return null;
  }

  if (componentOrElement.nodeType === ELEMENT_NODE) {
    return componentOrElement;
  }

  {
    return findHostInstanceWithWarning(componentOrElement, 'findDOMNode');
  }
}
function hydrate(element, container, callback) {
  if (!isValidContainer(container)) {
    {
      throw Error( "Target container is not a DOM element." );
    }
  }

  {
    var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === undefined;

    if (isModernRoot) {
      error('You are calling ReactDOM.hydrate() on a container that was previously ' + 'passed to ReactDOM.createRoot(). This is not supported. ' + 'Did you mean to call createRoot(container, {hydrate: true}).render(element)?');
    }
  } // TODO: throw or warn if we couldn't hydrate?


  return legacyRenderSubtreeIntoContainer(null, element, container, true, callback);
}
function render(element, container, callback) {
  if (!isValidContainer(container)) {
    {
      throw Error( "Target container is not a DOM element." );
    }
  }

  {
    var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === undefined;

    if (isModernRoot) {
      error('You are calling ReactDOM.render() on a container that was previously ' + 'passed to ReactDOM.createRoot(). This is not supported. ' + 'Did you mean to call root.render(element)?');
    }
  }

  return legacyRenderSubtreeIntoContainer(null, element, container, false, callback);
}
function unstable_renderSubtreeIntoContainer(parentComponent, element, containerNode, callback) {
  if (!isValidContainer(containerNode)) {
    {
      throw Error( "Target container is not a DOM element." );
    }
  }

  if (!(parentComponent != null && has(parentComponent))) {
    {
      throw Error( "parentComponent must be a valid React Component" );
    }
  }

  return legacyRenderSubtreeIntoContainer(parentComponent, element, containerNode, false, callback);
}
function unmountComponentAtNode(container) {
  if (!isValidContainer(container)) {
    {
      throw Error( "unmountComponentAtNode(...): Target container is not a DOM element." );
    }
  }

  {
    var isModernRoot = isContainerMarkedAsRoot(container) && container._reactRootContainer === undefined;

    if (isModernRoot) {
      error('You are calling ReactDOM.unmountComponentAtNode() on a container that was previously ' + 'passed to ReactDOM.createRoot(). This is not supported. Did you mean to call root.unmount()?');
    }
  }

  if (container._reactRootContainer) {
    {
      var rootEl = getReactRootElementInContainer(container);
      var renderedByDifferentReact = rootEl && !getInstanceFromNode$1(rootEl);

      if (renderedByDifferentReact) {
        error("unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by another copy of React.');
      }
    } // Unmount should not be batched.


    unbatchedUpdates(function () {
      legacyRenderSubtreeIntoContainer(null, null, container, false, function () {
        // $FlowFixMe This should probably use `delete container._reactRootContainer`
        container._reactRootContainer = null;
        unmarkContainerAsRoot(container);
      });
    }); // If you call unmountComponentAtNode twice in quick succession, you'll
    // get `true` twice. That's probably fine?

    return true;
  } else {
    {
      var _rootEl = getReactRootElementInContainer(container);

      var hasNonRootReactChild = !!(_rootEl && getInstanceFromNode$1(_rootEl)); // Check if the container itself is a React root node.

      var isContainerReactRoot = container.nodeType === ELEMENT_NODE && isValidContainer(container.parentNode) && !!container.parentNode._reactRootContainer;

      if (hasNonRootReactChild) {
        error("unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by React and is not a top-level container. %s', isContainerReactRoot ? 'You may have accidentally passed in a React root node instead ' + 'of its container.' : 'Instead, have the parent component update its state and ' + 'rerender in order to remove this component.');
      }
    }

    return false;
  }
}

function createPortal(children, containerInfo, // TODO: figure out the API for cross-renderer implementation.
implementation) {
  var key = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;
  return {
    // This tag allow us to uniquely identify this as a React Portal
    $$typeof: REACT_PORTAL_TYPE,
    key: key == null ? null : '' + key,
    children: children,
    containerInfo: containerInfo,
    implementation: implementation
  };
}

var ReactVersion = '16.13.0';

setAttemptUserBlockingHydration(attemptUserBlockingHydration$1);
setAttemptContinuousHydration(attemptContinuousHydration$1);
setAttemptHydrationAtCurrentPriority(attemptHydrationAtCurrentPriority$1);
var didWarnAboutUnstableCreatePortal = false;

{
  if (typeof Map !== 'function' || // $FlowIssue Flow incorrectly thinks Map has no prototype
  Map.prototype == null || typeof Map.prototype.forEach !== 'function' || typeof Set !== 'function' || // $FlowIssue Flow incorrectly thinks Set has no prototype
  Set.prototype == null || typeof Set.prototype.clear !== 'function' || typeof Set.prototype.forEach !== 'function') {
    error('React depends on Map and Set built-in types. Make sure that you load a ' + 'polyfill in older browsers. https://fb.me/react-polyfills');
  }
}

setRestoreImplementation(restoreControlledState$3);
setBatchingImplementation(batchedUpdates$1, discreteUpdates$1, flushDiscreteUpdates, batchedEventUpdates$1);

function createPortal$1(children, container) {
  var key = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;

  if (!isValidContainer(container)) {
    {
      throw Error( "Target container is not a DOM element." );
    }
  } // TODO: pass ReactDOM portal implementation as third argument
  // $FlowFixMe The Flow type is opaque but there's no way to actually create it.


  return createPortal(children, container, null, key);
}

function renderSubtreeIntoContainer(parentComponent, element, containerNode, callback) {

  return unstable_renderSubtreeIntoContainer(parentComponent, element, containerNode, callback);
}

function unstable_createPortal(children, container) {
  var key = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;

  {
    if (!didWarnAboutUnstableCreatePortal) {
      didWarnAboutUnstableCreatePortal = true;

      warn('The ReactDOM.unstable_createPortal() alias has been deprecated, ' + 'and will be removed in React 17+. Update your code to use ' + 'ReactDOM.createPortal() instead. It has the exact same API, ' + 'but without the "unstable_" prefix.');
    }
  }

  return createPortal$1(children, container, key);
}

var Internals = {
  // Keep in sync with ReactDOMUnstableNativeDependencies.js
  // ReactTestUtils.js, and ReactTestUtilsAct.js. This is an array for better minification.
  Events: [getInstanceFromNode$1, getNodeFromInstance$1, getFiberCurrentPropsFromNode$1, injectEventPluginsByName, eventNameDispatchConfigs, accumulateTwoPhaseDispatches, accumulateDirectDispatches, enqueueStateRestore, restoreStateIfNeeded, dispatchEvent, runEventsInBatch, flushPassiveEffects, IsThisRendererActing]
};
var foundDevTools = injectIntoDevTools({
  findFiberByHostInstance: getClosestInstanceFromNode,
  bundleType:  1 ,
  version: ReactVersion,
  rendererPackageName: 'react-dom'
});

{
  if (!foundDevTools && canUseDOM && window.top === window.self) {
    // If we're in Chrome or Firefox, provide a download link if not installed.
    if (navigator.userAgent.indexOf('Chrome') > -1 && navigator.userAgent.indexOf('Edge') === -1 || navigator.userAgent.indexOf('Firefox') > -1) {
      var protocol = window.location.protocol; // Don't warn in exotic cases like chrome-extension://.

      if (/^(https?|file):$/.test(protocol)) {
        // eslint-disable-next-line react-internal/no-production-logging
        console.info('%cDownload the React DevTools ' + 'for a better development experience: ' + 'https://fb.me/react-devtools' + (protocol === 'file:' ? '\nYou might need to use a local HTTP server (instead of file://): ' + 'https://fb.me/react-devtools-faq' : ''), 'font-weight:bold');
      }
    }
  }
}

exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = Internals;
exports.createPortal = createPortal$1;
exports.findDOMNode = findDOMNode;
exports.flushSync = flushSync;
exports.hydrate = hydrate;
exports.render = render;
exports.unmountComponentAtNode = unmountComponentAtNode;
exports.unstable_batchedUpdates = batchedUpdates$1;
exports.unstable_createPortal = unstable_createPortal;
exports.unstable_renderSubtreeIntoContainer = renderSubtreeIntoContainer;
exports.version = ReactVersion;
  })();
}


/***/ }),

/***/ "./node_modules/react-dom/index.js":
/*!*****************************************!*\
  !*** ./node_modules/react-dom/index.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


function checkDCE() {
  /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */
  if (
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined' ||
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== 'function'
  ) {
    return;
  }
  if (true) {
    // This branch is unreachable because this function is only called
    // in production, but the condition is true only in development.
    // Therefore if the branch is still here, dead code elimination wasn't
    // properly applied.
    // Don't change the message. React DevTools relies on it. Also make sure
    // this message doesn't occur elsewhere in this function, or it will cause
    // a false positive.
    throw new Error('^_^');
  }
  try {
    // Verify that the code above has been dead code eliminated (DCE'd).
    __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
  } catch (err) {
    // DevTools shouldn't crash React, no matter what.
    // We should still report in case we break this code.
    console.error(err);
  }
}

if (false) {} else {
  module.exports = __webpack_require__(/*! ./cjs/react-dom.development.js */ "./node_modules/react-dom/cjs/react-dom.development.js");
}


/***/ }),

/***/ "./node_modules/react-is/cjs/react-is.development.js":
/*!***********************************************************!*\
  !*** ./node_modules/react-is/cjs/react-is.development.js ***!
  \***********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.13.0
 * react-is.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (true) {
  (function() {
'use strict';

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol.for;
var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;
var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;
var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;
var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;
var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary
// (unstable) APIs that have been removed. Can we remove the symbols?

var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for('react.async_mode') : 0xeacf;
var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;
var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;
var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;
var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;
var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;
var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;
var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for('react.block') : 0xead9;
var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for('react.fundamental') : 0xead5;
var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for('react.responder') : 0xead6;
var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for('react.scope') : 0xead7;

function isValidElementType(type) {
  return typeof type === 'string' || typeof type === 'function' || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
  type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === 'object' && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
}

function typeOf(object) {
  if (typeof object === 'object' && object !== null) {
    var $$typeof = object.$$typeof;

    switch ($$typeof) {
      case REACT_ELEMENT_TYPE:
        var type = object.type;

        switch (type) {
          case REACT_ASYNC_MODE_TYPE:
          case REACT_CONCURRENT_MODE_TYPE:
          case REACT_FRAGMENT_TYPE:
          case REACT_PROFILER_TYPE:
          case REACT_STRICT_MODE_TYPE:
          case REACT_SUSPENSE_TYPE:
            return type;

          default:
            var $$typeofType = type && type.$$typeof;

            switch ($$typeofType) {
              case REACT_CONTEXT_TYPE:
              case REACT_FORWARD_REF_TYPE:
              case REACT_LAZY_TYPE:
              case REACT_MEMO_TYPE:
              case REACT_PROVIDER_TYPE:
                return $$typeofType;

              default:
                return $$typeof;
            }

        }

      case REACT_PORTAL_TYPE:
        return $$typeof;
    }
  }

  return undefined;
} // AsyncMode is deprecated along with isAsyncMode

var AsyncMode = REACT_ASYNC_MODE_TYPE;
var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
var ContextConsumer = REACT_CONTEXT_TYPE;
var ContextProvider = REACT_PROVIDER_TYPE;
var Element = REACT_ELEMENT_TYPE;
var ForwardRef = REACT_FORWARD_REF_TYPE;
var Fragment = REACT_FRAGMENT_TYPE;
var Lazy = REACT_LAZY_TYPE;
var Memo = REACT_MEMO_TYPE;
var Portal = REACT_PORTAL_TYPE;
var Profiler = REACT_PROFILER_TYPE;
var StrictMode = REACT_STRICT_MODE_TYPE;
var Suspense = REACT_SUSPENSE_TYPE;
var hasWarnedAboutDeprecatedIsAsyncMode = false; // AsyncMode should be deprecated

function isAsyncMode(object) {
  {
    if (!hasWarnedAboutDeprecatedIsAsyncMode) {
      hasWarnedAboutDeprecatedIsAsyncMode = true; // Using console['warn'] to evade Babel and ESLint

      console['warn']('The ReactIs.isAsyncMode() alias has been deprecated, ' + 'and will be removed in React 17+. Update your code to use ' + 'ReactIs.isConcurrentMode() instead. It has the exact same API.');
    }
  }

  return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
}
function isConcurrentMode(object) {
  return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
}
function isContextConsumer(object) {
  return typeOf(object) === REACT_CONTEXT_TYPE;
}
function isContextProvider(object) {
  return typeOf(object) === REACT_PROVIDER_TYPE;
}
function isElement(object) {
  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
}
function isForwardRef(object) {
  return typeOf(object) === REACT_FORWARD_REF_TYPE;
}
function isFragment(object) {
  return typeOf(object) === REACT_FRAGMENT_TYPE;
}
function isLazy(object) {
  return typeOf(object) === REACT_LAZY_TYPE;
}
function isMemo(object) {
  return typeOf(object) === REACT_MEMO_TYPE;
}
function isPortal(object) {
  return typeOf(object) === REACT_PORTAL_TYPE;
}
function isProfiler(object) {
  return typeOf(object) === REACT_PROFILER_TYPE;
}
function isStrictMode(object) {
  return typeOf(object) === REACT_STRICT_MODE_TYPE;
}
function isSuspense(object) {
  return typeOf(object) === REACT_SUSPENSE_TYPE;
}

exports.AsyncMode = AsyncMode;
exports.ConcurrentMode = ConcurrentMode;
exports.ContextConsumer = ContextConsumer;
exports.ContextProvider = ContextProvider;
exports.Element = Element;
exports.ForwardRef = ForwardRef;
exports.Fragment = Fragment;
exports.Lazy = Lazy;
exports.Memo = Memo;
exports.Portal = Portal;
exports.Profiler = Profiler;
exports.StrictMode = StrictMode;
exports.Suspense = Suspense;
exports.isAsyncMode = isAsyncMode;
exports.isConcurrentMode = isConcurrentMode;
exports.isContextConsumer = isContextConsumer;
exports.isContextProvider = isContextProvider;
exports.isElement = isElement;
exports.isForwardRef = isForwardRef;
exports.isFragment = isFragment;
exports.isLazy = isLazy;
exports.isMemo = isMemo;
exports.isPortal = isPortal;
exports.isProfiler = isProfiler;
exports.isStrictMode = isStrictMode;
exports.isSuspense = isSuspense;
exports.isValidElementType = isValidElementType;
exports.typeOf = typeOf;
  })();
}


/***/ }),

/***/ "./node_modules/react-is/index.js":
/*!****************************************!*\
  !*** ./node_modules/react-is/index.js ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


if (false) {} else {
  module.exports = __webpack_require__(/*! ./cjs/react-is.development.js */ "./node_modules/react-is/cjs/react-is.development.js");
}


/***/ }),

/***/ "./node_modules/react-redux/es/components/Context.js":
/*!***********************************************************!*\
  !*** ./node_modules/react-redux/es/components/Context.js ***!
  \***********************************************************/
/*! exports provided: ReactReduxContext, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ReactReduxContext", function() { return ReactReduxContext; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);

var ReactReduxContext =
/*#__PURE__*/
react__WEBPACK_IMPORTED_MODULE_0___default.a.createContext(null);

if (true) {
  ReactReduxContext.displayName = 'ReactRedux';
}

/* harmony default export */ __webpack_exports__["default"] = (ReactReduxContext);

/***/ }),

/***/ "./node_modules/react-redux/es/components/Provider.js":
/*!************************************************************!*\
  !*** ./node_modules/react-redux/es/components/Provider.js ***!
  \************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! prop-types */ "./node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _Context__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Context */ "./node_modules/react-redux/es/components/Context.js");
/* harmony import */ var _utils_Subscription__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../utils/Subscription */ "./node_modules/react-redux/es/utils/Subscription.js");





function Provider(_ref) {
  var store = _ref.store,
      context = _ref.context,
      children = _ref.children;
  var contextValue = Object(react__WEBPACK_IMPORTED_MODULE_0__["useMemo"])(function () {
    var subscription = new _utils_Subscription__WEBPACK_IMPORTED_MODULE_3__["default"](store);
    subscription.onStateChange = subscription.notifyNestedSubs;
    return {
      store: store,
      subscription: subscription
    };
  }, [store]);
  var previousState = Object(react__WEBPACK_IMPORTED_MODULE_0__["useMemo"])(function () {
    return store.getState();
  }, [store]);
  Object(react__WEBPACK_IMPORTED_MODULE_0__["useEffect"])(function () {
    var subscription = contextValue.subscription;
    subscription.trySubscribe();

    if (previousState !== store.getState()) {
      subscription.notifyNestedSubs();
    }

    return function () {
      subscription.tryUnsubscribe();
      subscription.onStateChange = null;
    };
  }, [contextValue, previousState]);
  var Context = context || _Context__WEBPACK_IMPORTED_MODULE_2__["ReactReduxContext"];
  return react__WEBPACK_IMPORTED_MODULE_0___default.a.createElement(Context.Provider, {
    value: contextValue
  }, children);
}

if (true) {
  Provider.propTypes = {
    store: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.shape({
      subscribe: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func.isRequired,
      dispatch: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func.isRequired,
      getState: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.func.isRequired
    }),
    context: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.object,
    children: prop_types__WEBPACK_IMPORTED_MODULE_1___default.a.any
  };
}

/* harmony default export */ __webpack_exports__["default"] = (Provider);

/***/ }),

/***/ "./node_modules/react-redux/es/components/connectAdvanced.js":
/*!*******************************************************************!*\
  !*** ./node_modules/react-redux/es/components/connectAdvanced.js ***!
  \*******************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return connectAdvanced; });
/* harmony import */ var _babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/helpers/esm/extends */ "./node_modules/@babel/runtime/helpers/esm/extends.js");
/* harmony import */ var _babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @babel/runtime/helpers/esm/objectWithoutPropertiesLoose */ "./node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js");
/* harmony import */ var hoist_non_react_statics__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! hoist-non-react-statics */ "./node_modules/hoist-non-react-statics/dist/hoist-non-react-statics.cjs.js");
/* harmony import */ var hoist_non_react_statics__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(hoist_non_react_statics__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var react_is__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react-is */ "./node_modules/react-is/index.js");
/* harmony import */ var react_is__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react_is__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _utils_Subscription__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../utils/Subscription */ "./node_modules/react-redux/es/utils/Subscription.js");
/* harmony import */ var _utils_useIsomorphicLayoutEffect__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../utils/useIsomorphicLayoutEffect */ "./node_modules/react-redux/es/utils/useIsomorphicLayoutEffect.js");
/* harmony import */ var _Context__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./Context */ "./node_modules/react-redux/es/components/Context.js");







 // Define some constant arrays just to avoid re-creating these

var EMPTY_ARRAY = [];
var NO_SUBSCRIPTION_ARRAY = [null, null];

var stringifyComponent = function stringifyComponent(Comp) {
  try {
    return JSON.stringify(Comp);
  } catch (err) {
    return String(Comp);
  }
};

function storeStateUpdatesReducer(state, action) {
  var updateCount = state[1];
  return [action.payload, updateCount + 1];
}

function useIsomorphicLayoutEffectWithArgs(effectFunc, effectArgs, dependencies) {
  Object(_utils_useIsomorphicLayoutEffect__WEBPACK_IMPORTED_MODULE_6__["useIsomorphicLayoutEffect"])(function () {
    return effectFunc.apply(void 0, effectArgs);
  }, dependencies);
}

function captureWrapperProps(lastWrapperProps, lastChildProps, renderIsScheduled, wrapperProps, actualChildProps, childPropsFromStoreUpdate, notifyNestedSubs) {
  // We want to capture the wrapper props and child props we used for later comparisons
  lastWrapperProps.current = wrapperProps;
  lastChildProps.current = actualChildProps;
  renderIsScheduled.current = false; // If the render was from a store update, clear out that reference and cascade the subscriber update

  if (childPropsFromStoreUpdate.current) {
    childPropsFromStoreUpdate.current = null;
    notifyNestedSubs();
  }
}

function subscribeUpdates(shouldHandleStateChanges, store, subscription, childPropsSelector, lastWrapperProps, lastChildProps, renderIsScheduled, childPropsFromStoreUpdate, notifyNestedSubs, forceComponentUpdateDispatch) {
  // If we're not subscribed to the store, nothing to do here
  if (!shouldHandleStateChanges) return; // Capture values for checking if and when this component unmounts

  var didUnsubscribe = false;
  var lastThrownError = null; // We'll run this callback every time a store subscription update propagates to this component

  var checkForUpdates = function checkForUpdates() {
    if (didUnsubscribe) {
      // Don't run stale listeners.
      // Redux doesn't guarantee unsubscriptions happen until next dispatch.
      return;
    }

    var latestStoreState = store.getState();
    var newChildProps, error;

    try {
      // Actually run the selector with the most recent store state and wrapper props
      // to determine what the child props should be
      newChildProps = childPropsSelector(latestStoreState, lastWrapperProps.current);
    } catch (e) {
      error = e;
      lastThrownError = e;
    }

    if (!error) {
      lastThrownError = null;
    } // If the child props haven't changed, nothing to do here - cascade the subscription update


    if (newChildProps === lastChildProps.current) {
      if (!renderIsScheduled.current) {
        notifyNestedSubs();
      }
    } else {
      // Save references to the new child props.  Note that we track the "child props from store update"
      // as a ref instead of a useState/useReducer because we need a way to determine if that value has
      // been processed.  If this went into useState/useReducer, we couldn't clear out the value without
      // forcing another re-render, which we don't want.
      lastChildProps.current = newChildProps;
      childPropsFromStoreUpdate.current = newChildProps;
      renderIsScheduled.current = true; // If the child props _did_ change (or we caught an error), this wrapper component needs to re-render

      forceComponentUpdateDispatch({
        type: 'STORE_UPDATED',
        payload: {
          error: error
        }
      });
    }
  }; // Actually subscribe to the nearest connected ancestor (or store)


  subscription.onStateChange = checkForUpdates;
  subscription.trySubscribe(); // Pull data from the store after first render in case the store has
  // changed since we began.

  checkForUpdates();

  var unsubscribeWrapper = function unsubscribeWrapper() {
    didUnsubscribe = true;
    subscription.tryUnsubscribe();
    subscription.onStateChange = null;

    if (lastThrownError) {
      // It's possible that we caught an error due to a bad mapState function, but the
      // parent re-rendered without this component and we're about to unmount.
      // This shouldn't happen as long as we do top-down subscriptions correctly, but
      // if we ever do those wrong, this throw will surface the error in our tests.
      // In that case, throw the error from here so it doesn't get lost.
      throw lastThrownError;
    }
  };

  return unsubscribeWrapper;
}

var initStateUpdates = function initStateUpdates() {
  return [null, 0];
};

function connectAdvanced(
/*
  selectorFactory is a func that is responsible for returning the selector function used to
  compute new props from state, props, and dispatch. For example:
      export default connectAdvanced((dispatch, options) => (state, props) => ({
      thing: state.things[props.thingId],
      saveThing: fields => dispatch(actionCreators.saveThing(props.thingId, fields)),
    }))(YourComponent)
    Access to dispatch is provided to the factory so selectorFactories can bind actionCreators
  outside of their selector as an optimization. Options passed to connectAdvanced are passed to
  the selectorFactory, along with displayName and WrappedComponent, as the second argument.
    Note that selectorFactory is responsible for all caching/memoization of inbound and outbound
  props. Do not use connectAdvanced directly without memoizing results between calls to your
  selector, otherwise the Connect component will re-render on every state or props change.
*/
selectorFactory, // options object:
_ref) {
  if (_ref === void 0) {
    _ref = {};
  }

  var _ref2 = _ref,
      _ref2$getDisplayName = _ref2.getDisplayName,
      getDisplayName = _ref2$getDisplayName === void 0 ? function (name) {
    return "ConnectAdvanced(" + name + ")";
  } : _ref2$getDisplayName,
      _ref2$methodName = _ref2.methodName,
      methodName = _ref2$methodName === void 0 ? 'connectAdvanced' : _ref2$methodName,
      _ref2$renderCountProp = _ref2.renderCountProp,
      renderCountProp = _ref2$renderCountProp === void 0 ? undefined : _ref2$renderCountProp,
      _ref2$shouldHandleSta = _ref2.shouldHandleStateChanges,
      shouldHandleStateChanges = _ref2$shouldHandleSta === void 0 ? true : _ref2$shouldHandleSta,
      _ref2$storeKey = _ref2.storeKey,
      storeKey = _ref2$storeKey === void 0 ? 'store' : _ref2$storeKey,
      _ref2$withRef = _ref2.withRef,
      withRef = _ref2$withRef === void 0 ? false : _ref2$withRef,
      _ref2$forwardRef = _ref2.forwardRef,
      forwardRef = _ref2$forwardRef === void 0 ? false : _ref2$forwardRef,
      _ref2$context = _ref2.context,
      context = _ref2$context === void 0 ? _Context__WEBPACK_IMPORTED_MODULE_7__["ReactReduxContext"] : _ref2$context,
      connectOptions = Object(_babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_1__["default"])(_ref2, ["getDisplayName", "methodName", "renderCountProp", "shouldHandleStateChanges", "storeKey", "withRef", "forwardRef", "context"]);

  if (true) {
    if (renderCountProp !== undefined) {
      throw new Error("renderCountProp is removed. render counting is built into the latest React Dev Tools profiling extension");
    }

    if (withRef) {
      throw new Error('withRef is removed. To access the wrapped instance, use a ref on the connected component');
    }

    var customStoreWarningMessage = 'To use a custom Redux store for specific components, create a custom React context with ' + "React.createContext(), and pass the context object to React Redux's Provider and specific components" + ' like: <Provider context={MyContext}><ConnectedComponent context={MyContext} /></Provider>. ' + 'You may also pass a {context : MyContext} option to connect';

    if (storeKey !== 'store') {
      throw new Error('storeKey has been removed and does not do anything. ' + customStoreWarningMessage);
    }
  }

  var Context = context;
  return function wrapWithConnect(WrappedComponent) {
    if ( true && !Object(react_is__WEBPACK_IMPORTED_MODULE_4__["isValidElementType"])(WrappedComponent)) {
      throw new Error("You must pass a component to the function returned by " + (methodName + ". Instead received " + stringifyComponent(WrappedComponent)));
    }

    var wrappedComponentName = WrappedComponent.displayName || WrappedComponent.name || 'Component';
    var displayName = getDisplayName(wrappedComponentName);

    var selectorFactoryOptions = Object(_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__["default"])({}, connectOptions, {
      getDisplayName: getDisplayName,
      methodName: methodName,
      renderCountProp: renderCountProp,
      shouldHandleStateChanges: shouldHandleStateChanges,
      storeKey: storeKey,
      displayName: displayName,
      wrappedComponentName: wrappedComponentName,
      WrappedComponent: WrappedComponent
    });

    var pure = connectOptions.pure;

    function createChildSelector(store) {
      return selectorFactory(store.dispatch, selectorFactoryOptions);
    } // If we aren't running in "pure" mode, we don't want to memoize values.
    // To avoid conditionally calling hooks, we fall back to a tiny wrapper
    // that just executes the given callback immediately.


    var usePureOnlyMemo = pure ? react__WEBPACK_IMPORTED_MODULE_3__["useMemo"] : function (callback) {
      return callback();
    };

    function ConnectFunction(props) {
      var _useMemo = Object(react__WEBPACK_IMPORTED_MODULE_3__["useMemo"])(function () {
        // Distinguish between actual "data" props that were passed to the wrapper component,
        // and values needed to control behavior (forwarded refs, alternate context instances).
        // To maintain the wrapperProps object reference, memoize this destructuring.
        var forwardedRef = props.forwardedRef,
            wrapperProps = Object(_babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_1__["default"])(props, ["forwardedRef"]);

        return [props.context, forwardedRef, wrapperProps];
      }, [props]),
          propsContext = _useMemo[0],
          forwardedRef = _useMemo[1],
          wrapperProps = _useMemo[2];

      var ContextToUse = Object(react__WEBPACK_IMPORTED_MODULE_3__["useMemo"])(function () {
        // Users may optionally pass in a custom context instance to use instead of our ReactReduxContext.
        // Memoize the check that determines which context instance we should use.
        return propsContext && propsContext.Consumer && Object(react_is__WEBPACK_IMPORTED_MODULE_4__["isContextConsumer"])(react__WEBPACK_IMPORTED_MODULE_3___default.a.createElement(propsContext.Consumer, null)) ? propsContext : Context;
      }, [propsContext, Context]); // Retrieve the store and ancestor subscription via context, if available

      var contextValue = Object(react__WEBPACK_IMPORTED_MODULE_3__["useContext"])(ContextToUse); // The store _must_ exist as either a prop or in context.
      // We'll check to see if it _looks_ like a Redux store first.
      // This allows us to pass through a `store` prop that is just a plain value.

      var didStoreComeFromProps = Boolean(props.store) && Boolean(props.store.getState) && Boolean(props.store.dispatch);
      var didStoreComeFromContext = Boolean(contextValue) && Boolean(contextValue.store);

      if ( true && !didStoreComeFromProps && !didStoreComeFromContext) {
        throw new Error("Could not find \"store\" in the context of " + ("\"" + displayName + "\". Either wrap the root component in a <Provider>, ") + "or pass a custom React context provider to <Provider> and the corresponding " + ("React context consumer to " + displayName + " in connect options."));
      } // Based on the previous check, one of these must be true


      var store = didStoreComeFromProps ? props.store : contextValue.store;
      var childPropsSelector = Object(react__WEBPACK_IMPORTED_MODULE_3__["useMemo"])(function () {
        // The child props selector needs the store reference as an input.
        // Re-create this selector whenever the store changes.
        return createChildSelector(store);
      }, [store]);

      var _useMemo2 = Object(react__WEBPACK_IMPORTED_MODULE_3__["useMemo"])(function () {
        if (!shouldHandleStateChanges) return NO_SUBSCRIPTION_ARRAY; // This Subscription's source should match where store came from: props vs. context. A component
        // connected to the store via props shouldn't use subscription from context, or vice versa.

        var subscription = new _utils_Subscription__WEBPACK_IMPORTED_MODULE_5__["default"](store, didStoreComeFromProps ? null : contextValue.subscription); // `notifyNestedSubs` is duplicated to handle the case where the component is unmounted in
        // the middle of the notification loop, where `subscription` will then be null. This can
        // probably be avoided if Subscription's listeners logic is changed to not call listeners
        // that have been unsubscribed in the  middle of the notification loop.

        var notifyNestedSubs = subscription.notifyNestedSubs.bind(subscription);
        return [subscription, notifyNestedSubs];
      }, [store, didStoreComeFromProps, contextValue]),
          subscription = _useMemo2[0],
          notifyNestedSubs = _useMemo2[1]; // Determine what {store, subscription} value should be put into nested context, if necessary,
      // and memoize that value to avoid unnecessary context updates.


      var overriddenContextValue = Object(react__WEBPACK_IMPORTED_MODULE_3__["useMemo"])(function () {
        if (didStoreComeFromProps) {
          // This component is directly subscribed to a store from props.
          // We don't want descendants reading from this store - pass down whatever
          // the existing context value is from the nearest connected ancestor.
          return contextValue;
        } // Otherwise, put this component's subscription instance into context, so that
        // connected descendants won't update until after this component is done


        return Object(_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__["default"])({}, contextValue, {
          subscription: subscription
        });
      }, [didStoreComeFromProps, contextValue, subscription]); // We need to force this wrapper component to re-render whenever a Redux store update
      // causes a change to the calculated child component props (or we caught an error in mapState)

      var _useReducer = Object(react__WEBPACK_IMPORTED_MODULE_3__["useReducer"])(storeStateUpdatesReducer, EMPTY_ARRAY, initStateUpdates),
          _useReducer$ = _useReducer[0],
          previousStateUpdateResult = _useReducer$[0],
          forceComponentUpdateDispatch = _useReducer[1]; // Propagate any mapState/mapDispatch errors upwards


      if (previousStateUpdateResult && previousStateUpdateResult.error) {
        throw previousStateUpdateResult.error;
      } // Set up refs to coordinate values between the subscription effect and the render logic


      var lastChildProps = Object(react__WEBPACK_IMPORTED_MODULE_3__["useRef"])();
      var lastWrapperProps = Object(react__WEBPACK_IMPORTED_MODULE_3__["useRef"])(wrapperProps);
      var childPropsFromStoreUpdate = Object(react__WEBPACK_IMPORTED_MODULE_3__["useRef"])();
      var renderIsScheduled = Object(react__WEBPACK_IMPORTED_MODULE_3__["useRef"])(false);
      var actualChildProps = usePureOnlyMemo(function () {
        // Tricky logic here:
        // - This render may have been triggered by a Redux store update that produced new child props
        // - However, we may have gotten new wrapper props after that
        // If we have new child props, and the same wrapper props, we know we should use the new child props as-is.
        // But, if we have new wrapper props, those might change the child props, so we have to recalculate things.
        // So, we'll use the child props from store update only if the wrapper props are the same as last time.
        if (childPropsFromStoreUpdate.current && wrapperProps === lastWrapperProps.current) {
          return childPropsFromStoreUpdate.current;
        } // TODO We're reading the store directly in render() here. Bad idea?
        // This will likely cause Bad Things (TM) to happen in Concurrent Mode.
        // Note that we do this because on renders _not_ caused by store updates, we need the latest store state
        // to determine what the child props should be.


        return childPropsSelector(store.getState(), wrapperProps);
      }, [store, previousStateUpdateResult, wrapperProps]); // We need this to execute synchronously every time we re-render. However, React warns
      // about useLayoutEffect in SSR, so we try to detect environment and fall back to
      // just useEffect instead to avoid the warning, since neither will run anyway.

      useIsomorphicLayoutEffectWithArgs(captureWrapperProps, [lastWrapperProps, lastChildProps, renderIsScheduled, wrapperProps, actualChildProps, childPropsFromStoreUpdate, notifyNestedSubs]); // Our re-subscribe logic only runs when the store/subscription setup changes

      useIsomorphicLayoutEffectWithArgs(subscribeUpdates, [shouldHandleStateChanges, store, subscription, childPropsSelector, lastWrapperProps, lastChildProps, renderIsScheduled, childPropsFromStoreUpdate, notifyNestedSubs, forceComponentUpdateDispatch], [store, subscription, childPropsSelector]); // Now that all that's done, we can finally try to actually render the child component.
      // We memoize the elements for the rendered child component as an optimization.

      var renderedWrappedComponent = Object(react__WEBPACK_IMPORTED_MODULE_3__["useMemo"])(function () {
        return react__WEBPACK_IMPORTED_MODULE_3___default.a.createElement(WrappedComponent, Object(_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__["default"])({}, actualChildProps, {
          ref: forwardedRef
        }));
      }, [forwardedRef, WrappedComponent, actualChildProps]); // If React sees the exact same element reference as last time, it bails out of re-rendering
      // that child, same as if it was wrapped in React.memo() or returned false from shouldComponentUpdate.

      var renderedChild = Object(react__WEBPACK_IMPORTED_MODULE_3__["useMemo"])(function () {
        if (shouldHandleStateChanges) {
          // If this component is subscribed to store updates, we need to pass its own
          // subscription instance down to our descendants. That means rendering the same
          // Context instance, and putting a different value into the context.
          return react__WEBPACK_IMPORTED_MODULE_3___default.a.createElement(ContextToUse.Provider, {
            value: overriddenContextValue
          }, renderedWrappedComponent);
        }

        return renderedWrappedComponent;
      }, [ContextToUse, renderedWrappedComponent, overriddenContextValue]);
      return renderedChild;
    } // If we're in "pure" mode, ensure our wrapper component only re-renders when incoming props have changed.


    var Connect = pure ? react__WEBPACK_IMPORTED_MODULE_3___default.a.memo(ConnectFunction) : ConnectFunction;
    Connect.WrappedComponent = WrappedComponent;
    Connect.displayName = displayName;

    if (forwardRef) {
      var forwarded = react__WEBPACK_IMPORTED_MODULE_3___default.a.forwardRef(function forwardConnectRef(props, ref) {
        return react__WEBPACK_IMPORTED_MODULE_3___default.a.createElement(Connect, Object(_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__["default"])({}, props, {
          forwardedRef: ref
        }));
      });
      forwarded.displayName = displayName;
      forwarded.WrappedComponent = WrappedComponent;
      return hoist_non_react_statics__WEBPACK_IMPORTED_MODULE_2___default()(forwarded, WrappedComponent);
    }

    return hoist_non_react_statics__WEBPACK_IMPORTED_MODULE_2___default()(Connect, WrappedComponent);
  };
}

/***/ }),

/***/ "./node_modules/react-redux/es/connect/connect.js":
/*!********************************************************!*\
  !*** ./node_modules/react-redux/es/connect/connect.js ***!
  \********************************************************/
/*! exports provided: createConnect, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createConnect", function() { return createConnect; });
/* harmony import */ var _babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/helpers/esm/extends */ "./node_modules/@babel/runtime/helpers/esm/extends.js");
/* harmony import */ var _babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @babel/runtime/helpers/esm/objectWithoutPropertiesLoose */ "./node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js");
/* harmony import */ var _components_connectAdvanced__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../components/connectAdvanced */ "./node_modules/react-redux/es/components/connectAdvanced.js");
/* harmony import */ var _utils_shallowEqual__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../utils/shallowEqual */ "./node_modules/react-redux/es/utils/shallowEqual.js");
/* harmony import */ var _mapDispatchToProps__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./mapDispatchToProps */ "./node_modules/react-redux/es/connect/mapDispatchToProps.js");
/* harmony import */ var _mapStateToProps__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./mapStateToProps */ "./node_modules/react-redux/es/connect/mapStateToProps.js");
/* harmony import */ var _mergeProps__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./mergeProps */ "./node_modules/react-redux/es/connect/mergeProps.js");
/* harmony import */ var _selectorFactory__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./selectorFactory */ "./node_modules/react-redux/es/connect/selectorFactory.js");








/*
  connect is a facade over connectAdvanced. It turns its args into a compatible
  selectorFactory, which has the signature:

    (dispatch, options) => (nextState, nextOwnProps) => nextFinalProps
  
  connect passes its args to connectAdvanced as options, which will in turn pass them to
  selectorFactory each time a Connect component instance is instantiated or hot reloaded.

  selectorFactory returns a final props selector from its mapStateToProps,
  mapStateToPropsFactories, mapDispatchToProps, mapDispatchToPropsFactories, mergeProps,
  mergePropsFactories, and pure args.

  The resulting final props selector is called by the Connect component instance whenever
  it receives new props or store state.
 */

function match(arg, factories, name) {
  for (var i = factories.length - 1; i >= 0; i--) {
    var result = factories[i](arg);
    if (result) return result;
  }

  return function (dispatch, options) {
    throw new Error("Invalid value of type " + typeof arg + " for " + name + " argument when connecting component " + options.wrappedComponentName + ".");
  };
}

function strictEqual(a, b) {
  return a === b;
} // createConnect with default args builds the 'official' connect behavior. Calling it with
// different options opens up some testing and extensibility scenarios


function createConnect(_temp) {
  var _ref = _temp === void 0 ? {} : _temp,
      _ref$connectHOC = _ref.connectHOC,
      connectHOC = _ref$connectHOC === void 0 ? _components_connectAdvanced__WEBPACK_IMPORTED_MODULE_2__["default"] : _ref$connectHOC,
      _ref$mapStateToPropsF = _ref.mapStateToPropsFactories,
      mapStateToPropsFactories = _ref$mapStateToPropsF === void 0 ? _mapStateToProps__WEBPACK_IMPORTED_MODULE_5__["default"] : _ref$mapStateToPropsF,
      _ref$mapDispatchToPro = _ref.mapDispatchToPropsFactories,
      mapDispatchToPropsFactories = _ref$mapDispatchToPro === void 0 ? _mapDispatchToProps__WEBPACK_IMPORTED_MODULE_4__["default"] : _ref$mapDispatchToPro,
      _ref$mergePropsFactor = _ref.mergePropsFactories,
      mergePropsFactories = _ref$mergePropsFactor === void 0 ? _mergeProps__WEBPACK_IMPORTED_MODULE_6__["default"] : _ref$mergePropsFactor,
      _ref$selectorFactory = _ref.selectorFactory,
      selectorFactory = _ref$selectorFactory === void 0 ? _selectorFactory__WEBPACK_IMPORTED_MODULE_7__["default"] : _ref$selectorFactory;

  return function connect(mapStateToProps, mapDispatchToProps, mergeProps, _ref2) {
    if (_ref2 === void 0) {
      _ref2 = {};
    }

    var _ref3 = _ref2,
        _ref3$pure = _ref3.pure,
        pure = _ref3$pure === void 0 ? true : _ref3$pure,
        _ref3$areStatesEqual = _ref3.areStatesEqual,
        areStatesEqual = _ref3$areStatesEqual === void 0 ? strictEqual : _ref3$areStatesEqual,
        _ref3$areOwnPropsEqua = _ref3.areOwnPropsEqual,
        areOwnPropsEqual = _ref3$areOwnPropsEqua === void 0 ? _utils_shallowEqual__WEBPACK_IMPORTED_MODULE_3__["default"] : _ref3$areOwnPropsEqua,
        _ref3$areStatePropsEq = _ref3.areStatePropsEqual,
        areStatePropsEqual = _ref3$areStatePropsEq === void 0 ? _utils_shallowEqual__WEBPACK_IMPORTED_MODULE_3__["default"] : _ref3$areStatePropsEq,
        _ref3$areMergedPropsE = _ref3.areMergedPropsEqual,
        areMergedPropsEqual = _ref3$areMergedPropsE === void 0 ? _utils_shallowEqual__WEBPACK_IMPORTED_MODULE_3__["default"] : _ref3$areMergedPropsE,
        extraOptions = Object(_babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_1__["default"])(_ref3, ["pure", "areStatesEqual", "areOwnPropsEqual", "areStatePropsEqual", "areMergedPropsEqual"]);

    var initMapStateToProps = match(mapStateToProps, mapStateToPropsFactories, 'mapStateToProps');
    var initMapDispatchToProps = match(mapDispatchToProps, mapDispatchToPropsFactories, 'mapDispatchToProps');
    var initMergeProps = match(mergeProps, mergePropsFactories, 'mergeProps');
    return connectHOC(selectorFactory, Object(_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__["default"])({
      // used in error messages
      methodName: 'connect',
      // used to compute Connect's displayName from the wrapped component's displayName.
      getDisplayName: function getDisplayName(name) {
        return "Connect(" + name + ")";
      },
      // if mapStateToProps is falsy, the Connect component doesn't subscribe to store state changes
      shouldHandleStateChanges: Boolean(mapStateToProps),
      // passed through to selectorFactory
      initMapStateToProps: initMapStateToProps,
      initMapDispatchToProps: initMapDispatchToProps,
      initMergeProps: initMergeProps,
      pure: pure,
      areStatesEqual: areStatesEqual,
      areOwnPropsEqual: areOwnPropsEqual,
      areStatePropsEqual: areStatePropsEqual,
      areMergedPropsEqual: areMergedPropsEqual
    }, extraOptions));
  };
}
/* harmony default export */ __webpack_exports__["default"] = (/*#__PURE__*/createConnect());

/***/ }),

/***/ "./node_modules/react-redux/es/connect/mapDispatchToProps.js":
/*!*******************************************************************!*\
  !*** ./node_modules/react-redux/es/connect/mapDispatchToProps.js ***!
  \*******************************************************************/
/*! exports provided: whenMapDispatchToPropsIsFunction, whenMapDispatchToPropsIsMissing, whenMapDispatchToPropsIsObject, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "whenMapDispatchToPropsIsFunction", function() { return whenMapDispatchToPropsIsFunction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "whenMapDispatchToPropsIsMissing", function() { return whenMapDispatchToPropsIsMissing; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "whenMapDispatchToPropsIsObject", function() { return whenMapDispatchToPropsIsObject; });
/* harmony import */ var redux__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! redux */ "./node_modules/redux/es/redux.js");
/* harmony import */ var _wrapMapToProps__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./wrapMapToProps */ "./node_modules/react-redux/es/connect/wrapMapToProps.js");


function whenMapDispatchToPropsIsFunction(mapDispatchToProps) {
  return typeof mapDispatchToProps === 'function' ? Object(_wrapMapToProps__WEBPACK_IMPORTED_MODULE_1__["wrapMapToPropsFunc"])(mapDispatchToProps, 'mapDispatchToProps') : undefined;
}
function whenMapDispatchToPropsIsMissing(mapDispatchToProps) {
  return !mapDispatchToProps ? Object(_wrapMapToProps__WEBPACK_IMPORTED_MODULE_1__["wrapMapToPropsConstant"])(function (dispatch) {
    return {
      dispatch: dispatch
    };
  }) : undefined;
}
function whenMapDispatchToPropsIsObject(mapDispatchToProps) {
  return mapDispatchToProps && typeof mapDispatchToProps === 'object' ? Object(_wrapMapToProps__WEBPACK_IMPORTED_MODULE_1__["wrapMapToPropsConstant"])(function (dispatch) {
    return Object(redux__WEBPACK_IMPORTED_MODULE_0__["bindActionCreators"])(mapDispatchToProps, dispatch);
  }) : undefined;
}
/* harmony default export */ __webpack_exports__["default"] = ([whenMapDispatchToPropsIsFunction, whenMapDispatchToPropsIsMissing, whenMapDispatchToPropsIsObject]);

/***/ }),

/***/ "./node_modules/react-redux/es/connect/mapStateToProps.js":
/*!****************************************************************!*\
  !*** ./node_modules/react-redux/es/connect/mapStateToProps.js ***!
  \****************************************************************/
/*! exports provided: whenMapStateToPropsIsFunction, whenMapStateToPropsIsMissing, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "whenMapStateToPropsIsFunction", function() { return whenMapStateToPropsIsFunction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "whenMapStateToPropsIsMissing", function() { return whenMapStateToPropsIsMissing; });
/* harmony import */ var _wrapMapToProps__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./wrapMapToProps */ "./node_modules/react-redux/es/connect/wrapMapToProps.js");

function whenMapStateToPropsIsFunction(mapStateToProps) {
  return typeof mapStateToProps === 'function' ? Object(_wrapMapToProps__WEBPACK_IMPORTED_MODULE_0__["wrapMapToPropsFunc"])(mapStateToProps, 'mapStateToProps') : undefined;
}
function whenMapStateToPropsIsMissing(mapStateToProps) {
  return !mapStateToProps ? Object(_wrapMapToProps__WEBPACK_IMPORTED_MODULE_0__["wrapMapToPropsConstant"])(function () {
    return {};
  }) : undefined;
}
/* harmony default export */ __webpack_exports__["default"] = ([whenMapStateToPropsIsFunction, whenMapStateToPropsIsMissing]);

/***/ }),

/***/ "./node_modules/react-redux/es/connect/mergeProps.js":
/*!***********************************************************!*\
  !*** ./node_modules/react-redux/es/connect/mergeProps.js ***!
  \***********************************************************/
/*! exports provided: defaultMergeProps, wrapMergePropsFunc, whenMergePropsIsFunction, whenMergePropsIsOmitted, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "defaultMergeProps", function() { return defaultMergeProps; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "wrapMergePropsFunc", function() { return wrapMergePropsFunc; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "whenMergePropsIsFunction", function() { return whenMergePropsIsFunction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "whenMergePropsIsOmitted", function() { return whenMergePropsIsOmitted; });
/* harmony import */ var _babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/helpers/esm/extends */ "./node_modules/@babel/runtime/helpers/esm/extends.js");
/* harmony import */ var _utils_verifyPlainObject__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../utils/verifyPlainObject */ "./node_modules/react-redux/es/utils/verifyPlainObject.js");


function defaultMergeProps(stateProps, dispatchProps, ownProps) {
  return Object(_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_0__["default"])({}, ownProps, {}, stateProps, {}, dispatchProps);
}
function wrapMergePropsFunc(mergeProps) {
  return function initMergePropsProxy(dispatch, _ref) {
    var displayName = _ref.displayName,
        pure = _ref.pure,
        areMergedPropsEqual = _ref.areMergedPropsEqual;
    var hasRunOnce = false;
    var mergedProps;
    return function mergePropsProxy(stateProps, dispatchProps, ownProps) {
      var nextMergedProps = mergeProps(stateProps, dispatchProps, ownProps);

      if (hasRunOnce) {
        if (!pure || !areMergedPropsEqual(nextMergedProps, mergedProps)) mergedProps = nextMergedProps;
      } else {
        hasRunOnce = true;
        mergedProps = nextMergedProps;
        if (true) Object(_utils_verifyPlainObject__WEBPACK_IMPORTED_MODULE_1__["default"])(mergedProps, displayName, 'mergeProps');
      }

      return mergedProps;
    };
  };
}
function whenMergePropsIsFunction(mergeProps) {
  return typeof mergeProps === 'function' ? wrapMergePropsFunc(mergeProps) : undefined;
}
function whenMergePropsIsOmitted(mergeProps) {
  return !mergeProps ? function () {
    return defaultMergeProps;
  } : undefined;
}
/* harmony default export */ __webpack_exports__["default"] = ([whenMergePropsIsFunction, whenMergePropsIsOmitted]);

/***/ }),

/***/ "./node_modules/react-redux/es/connect/selectorFactory.js":
/*!****************************************************************!*\
  !*** ./node_modules/react-redux/es/connect/selectorFactory.js ***!
  \****************************************************************/
/*! exports provided: impureFinalPropsSelectorFactory, pureFinalPropsSelectorFactory, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "impureFinalPropsSelectorFactory", function() { return impureFinalPropsSelectorFactory; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "pureFinalPropsSelectorFactory", function() { return pureFinalPropsSelectorFactory; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return finalPropsSelectorFactory; });
/* harmony import */ var _babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/helpers/esm/objectWithoutPropertiesLoose */ "./node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js");
/* harmony import */ var _verifySubselectors__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./verifySubselectors */ "./node_modules/react-redux/es/connect/verifySubselectors.js");


function impureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch) {
  return function impureFinalPropsSelector(state, ownProps) {
    return mergeProps(mapStateToProps(state, ownProps), mapDispatchToProps(dispatch, ownProps), ownProps);
  };
}
function pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, _ref) {
  var areStatesEqual = _ref.areStatesEqual,
      areOwnPropsEqual = _ref.areOwnPropsEqual,
      areStatePropsEqual = _ref.areStatePropsEqual;
  var hasRunAtLeastOnce = false;
  var state;
  var ownProps;
  var stateProps;
  var dispatchProps;
  var mergedProps;

  function handleFirstCall(firstState, firstOwnProps) {
    state = firstState;
    ownProps = firstOwnProps;
    stateProps = mapStateToProps(state, ownProps);
    dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    hasRunAtLeastOnce = true;
    return mergedProps;
  }

  function handleNewPropsAndNewState() {
    stateProps = mapStateToProps(state, ownProps);
    if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleNewProps() {
    if (mapStateToProps.dependsOnOwnProps) stateProps = mapStateToProps(state, ownProps);
    if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleNewState() {
    var nextStateProps = mapStateToProps(state, ownProps);
    var statePropsChanged = !areStatePropsEqual(nextStateProps, stateProps);
    stateProps = nextStateProps;
    if (statePropsChanged) mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleSubsequentCalls(nextState, nextOwnProps) {
    var propsChanged = !areOwnPropsEqual(nextOwnProps, ownProps);
    var stateChanged = !areStatesEqual(nextState, state);
    state = nextState;
    ownProps = nextOwnProps;
    if (propsChanged && stateChanged) return handleNewPropsAndNewState();
    if (propsChanged) return handleNewProps();
    if (stateChanged) return handleNewState();
    return mergedProps;
  }

  return function pureFinalPropsSelector(nextState, nextOwnProps) {
    return hasRunAtLeastOnce ? handleSubsequentCalls(nextState, nextOwnProps) : handleFirstCall(nextState, nextOwnProps);
  };
} // TODO: Add more comments
// If pure is true, the selector returned by selectorFactory will memoize its results,
// allowing connectAdvanced's shouldComponentUpdate to return false if final
// props have not changed. If false, the selector will always return a new
// object and shouldComponentUpdate will always return true.

function finalPropsSelectorFactory(dispatch, _ref2) {
  var initMapStateToProps = _ref2.initMapStateToProps,
      initMapDispatchToProps = _ref2.initMapDispatchToProps,
      initMergeProps = _ref2.initMergeProps,
      options = Object(_babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_0__["default"])(_ref2, ["initMapStateToProps", "initMapDispatchToProps", "initMergeProps"]);

  var mapStateToProps = initMapStateToProps(dispatch, options);
  var mapDispatchToProps = initMapDispatchToProps(dispatch, options);
  var mergeProps = initMergeProps(dispatch, options);

  if (true) {
    Object(_verifySubselectors__WEBPACK_IMPORTED_MODULE_1__["default"])(mapStateToProps, mapDispatchToProps, mergeProps, options.displayName);
  }

  var selectorFactory = options.pure ? pureFinalPropsSelectorFactory : impureFinalPropsSelectorFactory;
  return selectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, options);
}

/***/ }),

/***/ "./node_modules/react-redux/es/connect/verifySubselectors.js":
/*!*******************************************************************!*\
  !*** ./node_modules/react-redux/es/connect/verifySubselectors.js ***!
  \*******************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return verifySubselectors; });
/* harmony import */ var _utils_warning__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../utils/warning */ "./node_modules/react-redux/es/utils/warning.js");


function verify(selector, methodName, displayName) {
  if (!selector) {
    throw new Error("Unexpected value for " + methodName + " in " + displayName + ".");
  } else if (methodName === 'mapStateToProps' || methodName === 'mapDispatchToProps') {
    if (!Object.prototype.hasOwnProperty.call(selector, 'dependsOnOwnProps')) {
      Object(_utils_warning__WEBPACK_IMPORTED_MODULE_0__["default"])("The selector for " + methodName + " of " + displayName + " did not specify a value for dependsOnOwnProps.");
    }
  }
}

function verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps, displayName) {
  verify(mapStateToProps, 'mapStateToProps', displayName);
  verify(mapDispatchToProps, 'mapDispatchToProps', displayName);
  verify(mergeProps, 'mergeProps', displayName);
}

/***/ }),

/***/ "./node_modules/react-redux/es/connect/wrapMapToProps.js":
/*!***************************************************************!*\
  !*** ./node_modules/react-redux/es/connect/wrapMapToProps.js ***!
  \***************************************************************/
/*! exports provided: wrapMapToPropsConstant, getDependsOnOwnProps, wrapMapToPropsFunc */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "wrapMapToPropsConstant", function() { return wrapMapToPropsConstant; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getDependsOnOwnProps", function() { return getDependsOnOwnProps; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "wrapMapToPropsFunc", function() { return wrapMapToPropsFunc; });
/* harmony import */ var _utils_verifyPlainObject__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../utils/verifyPlainObject */ "./node_modules/react-redux/es/utils/verifyPlainObject.js");

function wrapMapToPropsConstant(getConstant) {
  return function initConstantSelector(dispatch, options) {
    var constant = getConstant(dispatch, options);

    function constantSelector() {
      return constant;
    }

    constantSelector.dependsOnOwnProps = false;
    return constantSelector;
  };
} // dependsOnOwnProps is used by createMapToPropsProxy to determine whether to pass props as args
// to the mapToProps function being wrapped. It is also used by makePurePropsSelector to determine
// whether mapToProps needs to be invoked when props have changed.
//
// A length of one signals that mapToProps does not depend on props from the parent component.
// A length of zero is assumed to mean mapToProps is getting args via arguments or ...args and
// therefore not reporting its length accurately..

function getDependsOnOwnProps(mapToProps) {
  return mapToProps.dependsOnOwnProps !== null && mapToProps.dependsOnOwnProps !== undefined ? Boolean(mapToProps.dependsOnOwnProps) : mapToProps.length !== 1;
} // Used by whenMapStateToPropsIsFunction and whenMapDispatchToPropsIsFunction,
// this function wraps mapToProps in a proxy function which does several things:
//
//  * Detects whether the mapToProps function being called depends on props, which
//    is used by selectorFactory to decide if it should reinvoke on props changes.
//
//  * On first call, handles mapToProps if returns another function, and treats that
//    new function as the true mapToProps for subsequent calls.
//
//  * On first call, verifies the first result is a plain object, in order to warn
//    the developer that their mapToProps function is not returning a valid result.
//

function wrapMapToPropsFunc(mapToProps, methodName) {
  return function initProxySelector(dispatch, _ref) {
    var displayName = _ref.displayName;

    var proxy = function mapToPropsProxy(stateOrDispatch, ownProps) {
      return proxy.dependsOnOwnProps ? proxy.mapToProps(stateOrDispatch, ownProps) : proxy.mapToProps(stateOrDispatch);
    }; // allow detectFactoryAndVerify to get ownProps


    proxy.dependsOnOwnProps = true;

    proxy.mapToProps = function detectFactoryAndVerify(stateOrDispatch, ownProps) {
      proxy.mapToProps = mapToProps;
      proxy.dependsOnOwnProps = getDependsOnOwnProps(mapToProps);
      var props = proxy(stateOrDispatch, ownProps);

      if (typeof props === 'function') {
        proxy.mapToProps = props;
        proxy.dependsOnOwnProps = getDependsOnOwnProps(props);
        props = proxy(stateOrDispatch, ownProps);
      }

      if (true) Object(_utils_verifyPlainObject__WEBPACK_IMPORTED_MODULE_0__["default"])(props, displayName, methodName);
      return props;
    };

    return proxy;
  };
}

/***/ }),

/***/ "./node_modules/react-redux/es/hooks/useDispatch.js":
/*!**********************************************************!*\
  !*** ./node_modules/react-redux/es/hooks/useDispatch.js ***!
  \**********************************************************/
/*! exports provided: createDispatchHook, useDispatch */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createDispatchHook", function() { return createDispatchHook; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "useDispatch", function() { return useDispatch; });
/* harmony import */ var _components_Context__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../components/Context */ "./node_modules/react-redux/es/components/Context.js");
/* harmony import */ var _useStore__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./useStore */ "./node_modules/react-redux/es/hooks/useStore.js");


/**
 * Hook factory, which creates a `useDispatch` hook bound to a given context.
 *
 * @param {React.Context} [context=ReactReduxContext] Context passed to your `<Provider>`.
 * @returns {Function} A `useDispatch` hook bound to the specified context.
 */

function createDispatchHook(context) {
  if (context === void 0) {
    context = _components_Context__WEBPACK_IMPORTED_MODULE_0__["ReactReduxContext"];
  }

  var useStore = context === _components_Context__WEBPACK_IMPORTED_MODULE_0__["ReactReduxContext"] ? _useStore__WEBPACK_IMPORTED_MODULE_1__["useStore"] : Object(_useStore__WEBPACK_IMPORTED_MODULE_1__["createStoreHook"])(context);
  return function useDispatch() {
    var store = useStore();
    return store.dispatch;
  };
}
/**
 * A hook to access the redux `dispatch` function.
 *
 * @returns {any|function} redux store's `dispatch` function
 *
 * @example
 *
 * import React, { useCallback } from 'react'
 * import { useDispatch } from 'react-redux'
 *
 * export const CounterComponent = ({ value }) => {
 *   const dispatch = useDispatch()
 *   const increaseCounter = useCallback(() => dispatch({ type: 'increase-counter' }), [])
 *   return (
 *     <div>
 *       <span>{value}</span>
 *       <button onClick={increaseCounter}>Increase counter</button>
 *     </div>
 *   )
 * }
 */

var useDispatch =
/*#__PURE__*/
createDispatchHook();

/***/ }),

/***/ "./node_modules/react-redux/es/hooks/useReduxContext.js":
/*!**************************************************************!*\
  !*** ./node_modules/react-redux/es/hooks/useReduxContext.js ***!
  \**************************************************************/
/*! exports provided: useReduxContext */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "useReduxContext", function() { return useReduxContext; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _components_Context__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../components/Context */ "./node_modules/react-redux/es/components/Context.js");


/**
 * A hook to access the value of the `ReactReduxContext`. This is a low-level
 * hook that you should usually not need to call directly.
 *
 * @returns {any} the value of the `ReactReduxContext`
 *
 * @example
 *
 * import React from 'react'
 * import { useReduxContext } from 'react-redux'
 *
 * export const CounterComponent = ({ value }) => {
 *   const { store } = useReduxContext()
 *   return <div>{store.getState()}</div>
 * }
 */

function useReduxContext() {
  var contextValue = Object(react__WEBPACK_IMPORTED_MODULE_0__["useContext"])(_components_Context__WEBPACK_IMPORTED_MODULE_1__["ReactReduxContext"]);

  if ( true && !contextValue) {
    throw new Error('could not find react-redux context value; please ensure the component is wrapped in a <Provider>');
  }

  return contextValue;
}

/***/ }),

/***/ "./node_modules/react-redux/es/hooks/useSelector.js":
/*!**********************************************************!*\
  !*** ./node_modules/react-redux/es/hooks/useSelector.js ***!
  \**********************************************************/
/*! exports provided: createSelectorHook, useSelector */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createSelectorHook", function() { return createSelectorHook; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "useSelector", function() { return useSelector; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _useReduxContext__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./useReduxContext */ "./node_modules/react-redux/es/hooks/useReduxContext.js");
/* harmony import */ var _utils_Subscription__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../utils/Subscription */ "./node_modules/react-redux/es/utils/Subscription.js");
/* harmony import */ var _utils_useIsomorphicLayoutEffect__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../utils/useIsomorphicLayoutEffect */ "./node_modules/react-redux/es/utils/useIsomorphicLayoutEffect.js");
/* harmony import */ var _components_Context__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../components/Context */ "./node_modules/react-redux/es/components/Context.js");






var refEquality = function refEquality(a, b) {
  return a === b;
};

function useSelectorWithStoreAndSubscription(selector, equalityFn, store, contextSub) {
  var _useReducer = Object(react__WEBPACK_IMPORTED_MODULE_0__["useReducer"])(function (s) {
    return s + 1;
  }, 0),
      forceRender = _useReducer[1];

  var subscription = Object(react__WEBPACK_IMPORTED_MODULE_0__["useMemo"])(function () {
    return new _utils_Subscription__WEBPACK_IMPORTED_MODULE_2__["default"](store, contextSub);
  }, [store, contextSub]);
  var latestSubscriptionCallbackError = Object(react__WEBPACK_IMPORTED_MODULE_0__["useRef"])();
  var latestSelector = Object(react__WEBPACK_IMPORTED_MODULE_0__["useRef"])();
  var latestSelectedState = Object(react__WEBPACK_IMPORTED_MODULE_0__["useRef"])();
  var selectedState;

  try {
    if (selector !== latestSelector.current || latestSubscriptionCallbackError.current) {
      selectedState = selector(store.getState());
    } else {
      selectedState = latestSelectedState.current;
    }
  } catch (err) {
    if (latestSubscriptionCallbackError.current) {
      err.message += "\nThe error may be correlated with this previous error:\n" + latestSubscriptionCallbackError.current.stack + "\n\n";
    }

    throw err;
  }

  Object(_utils_useIsomorphicLayoutEffect__WEBPACK_IMPORTED_MODULE_3__["useIsomorphicLayoutEffect"])(function () {
    latestSelector.current = selector;
    latestSelectedState.current = selectedState;
    latestSubscriptionCallbackError.current = undefined;
  });
  Object(_utils_useIsomorphicLayoutEffect__WEBPACK_IMPORTED_MODULE_3__["useIsomorphicLayoutEffect"])(function () {
    function checkForUpdates() {
      try {
        var newSelectedState = latestSelector.current(store.getState());

        if (equalityFn(newSelectedState, latestSelectedState.current)) {
          return;
        }

        latestSelectedState.current = newSelectedState;
      } catch (err) {
        // we ignore all errors here, since when the component
        // is re-rendered, the selectors are called again, and
        // will throw again, if neither props nor store state
        // changed
        latestSubscriptionCallbackError.current = err;
      }

      forceRender({});
    }

    subscription.onStateChange = checkForUpdates;
    subscription.trySubscribe();
    checkForUpdates();
    return function () {
      return subscription.tryUnsubscribe();
    };
  }, [store, subscription]);
  return selectedState;
}
/**
 * Hook factory, which creates a `useSelector` hook bound to a given context.
 *
 * @param {React.Context} [context=ReactReduxContext] Context passed to your `<Provider>`.
 * @returns {Function} A `useSelector` hook bound to the specified context.
 */


function createSelectorHook(context) {
  if (context === void 0) {
    context = _components_Context__WEBPACK_IMPORTED_MODULE_4__["ReactReduxContext"];
  }

  var useReduxContext = context === _components_Context__WEBPACK_IMPORTED_MODULE_4__["ReactReduxContext"] ? _useReduxContext__WEBPACK_IMPORTED_MODULE_1__["useReduxContext"] : function () {
    return Object(react__WEBPACK_IMPORTED_MODULE_0__["useContext"])(context);
  };
  return function useSelector(selector, equalityFn) {
    if (equalityFn === void 0) {
      equalityFn = refEquality;
    }

    if ( true && !selector) {
      throw new Error("You must pass a selector to useSelectors");
    }

    var _useReduxContext = useReduxContext(),
        store = _useReduxContext.store,
        contextSub = _useReduxContext.subscription;

    return useSelectorWithStoreAndSubscription(selector, equalityFn, store, contextSub);
  };
}
/**
 * A hook to access the redux store's state. This hook takes a selector function
 * as an argument. The selector is called with the store state.
 *
 * This hook takes an optional equality comparison function as the second parameter
 * that allows you to customize the way the selected state is compared to determine
 * whether the component needs to be re-rendered.
 *
 * @param {Function} selector the selector function
 * @param {Function=} equalityFn the function that will be used to determine equality
 *
 * @returns {any} the selected state
 *
 * @example
 *
 * import React from 'react'
 * import { useSelector } from 'react-redux'
 *
 * export const CounterComponent = () => {
 *   const counter = useSelector(state => state.counter)
 *   return <div>{counter}</div>
 * }
 */

var useSelector =
/*#__PURE__*/
createSelectorHook();

/***/ }),

/***/ "./node_modules/react-redux/es/hooks/useStore.js":
/*!*******************************************************!*\
  !*** ./node_modules/react-redux/es/hooks/useStore.js ***!
  \*******************************************************/
/*! exports provided: createStoreHook, useStore */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createStoreHook", function() { return createStoreHook; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "useStore", function() { return useStore; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _components_Context__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../components/Context */ "./node_modules/react-redux/es/components/Context.js");
/* harmony import */ var _useReduxContext__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./useReduxContext */ "./node_modules/react-redux/es/hooks/useReduxContext.js");



/**
 * Hook factory, which creates a `useStore` hook bound to a given context.
 *
 * @param {React.Context} [context=ReactReduxContext] Context passed to your `<Provider>`.
 * @returns {Function} A `useStore` hook bound to the specified context.
 */

function createStoreHook(context) {
  if (context === void 0) {
    context = _components_Context__WEBPACK_IMPORTED_MODULE_1__["ReactReduxContext"];
  }

  var useReduxContext = context === _components_Context__WEBPACK_IMPORTED_MODULE_1__["ReactReduxContext"] ? _useReduxContext__WEBPACK_IMPORTED_MODULE_2__["useReduxContext"] : function () {
    return Object(react__WEBPACK_IMPORTED_MODULE_0__["useContext"])(context);
  };
  return function useStore() {
    var _useReduxContext = useReduxContext(),
        store = _useReduxContext.store;

    return store;
  };
}
/**
 * A hook to access the redux store.
 *
 * @returns {any} the redux store
 *
 * @example
 *
 * import React from 'react'
 * import { useStore } from 'react-redux'
 *
 * export const ExampleComponent = () => {
 *   const store = useStore()
 *   return <div>{store.getState()}</div>
 * }
 */

var useStore =
/*#__PURE__*/
createStoreHook();

/***/ }),

/***/ "./node_modules/react-redux/es/index.js":
/*!**********************************************!*\
  !*** ./node_modules/react-redux/es/index.js ***!
  \**********************************************/
/*! exports provided: Provider, connectAdvanced, ReactReduxContext, connect, batch, useDispatch, createDispatchHook, useSelector, createSelectorHook, useStore, createStoreHook, shallowEqual */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _components_Provider__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./components/Provider */ "./node_modules/react-redux/es/components/Provider.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "Provider", function() { return _components_Provider__WEBPACK_IMPORTED_MODULE_0__["default"]; });

/* harmony import */ var _components_connectAdvanced__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./components/connectAdvanced */ "./node_modules/react-redux/es/components/connectAdvanced.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "connectAdvanced", function() { return _components_connectAdvanced__WEBPACK_IMPORTED_MODULE_1__["default"]; });

/* harmony import */ var _components_Context__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./components/Context */ "./node_modules/react-redux/es/components/Context.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "ReactReduxContext", function() { return _components_Context__WEBPACK_IMPORTED_MODULE_2__["ReactReduxContext"]; });

/* harmony import */ var _connect_connect__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./connect/connect */ "./node_modules/react-redux/es/connect/connect.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "connect", function() { return _connect_connect__WEBPACK_IMPORTED_MODULE_3__["default"]; });

/* harmony import */ var _hooks_useDispatch__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./hooks/useDispatch */ "./node_modules/react-redux/es/hooks/useDispatch.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "useDispatch", function() { return _hooks_useDispatch__WEBPACK_IMPORTED_MODULE_4__["useDispatch"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "createDispatchHook", function() { return _hooks_useDispatch__WEBPACK_IMPORTED_MODULE_4__["createDispatchHook"]; });

/* harmony import */ var _hooks_useSelector__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./hooks/useSelector */ "./node_modules/react-redux/es/hooks/useSelector.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "useSelector", function() { return _hooks_useSelector__WEBPACK_IMPORTED_MODULE_5__["useSelector"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "createSelectorHook", function() { return _hooks_useSelector__WEBPACK_IMPORTED_MODULE_5__["createSelectorHook"]; });

/* harmony import */ var _hooks_useStore__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./hooks/useStore */ "./node_modules/react-redux/es/hooks/useStore.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "useStore", function() { return _hooks_useStore__WEBPACK_IMPORTED_MODULE_6__["useStore"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "createStoreHook", function() { return _hooks_useStore__WEBPACK_IMPORTED_MODULE_6__["createStoreHook"]; });

/* harmony import */ var _utils_batch__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./utils/batch */ "./node_modules/react-redux/es/utils/batch.js");
/* harmony import */ var _utils_reactBatchedUpdates__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./utils/reactBatchedUpdates */ "./node_modules/react-redux/es/utils/reactBatchedUpdates.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "batch", function() { return _utils_reactBatchedUpdates__WEBPACK_IMPORTED_MODULE_8__["unstable_batchedUpdates"]; });

/* harmony import */ var _utils_shallowEqual__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./utils/shallowEqual */ "./node_modules/react-redux/es/utils/shallowEqual.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "shallowEqual", function() { return _utils_shallowEqual__WEBPACK_IMPORTED_MODULE_9__["default"]; });











Object(_utils_batch__WEBPACK_IMPORTED_MODULE_7__["setBatch"])(_utils_reactBatchedUpdates__WEBPACK_IMPORTED_MODULE_8__["unstable_batchedUpdates"]);


/***/ }),

/***/ "./node_modules/react-redux/es/utils/Subscription.js":
/*!***********************************************************!*\
  !*** ./node_modules/react-redux/es/utils/Subscription.js ***!
  \***********************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return Subscription; });
/* harmony import */ var _batch__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./batch */ "./node_modules/react-redux/es/utils/batch.js");
 // encapsulates the subscription logic for connecting a component to the redux store, as
// well as nesting subscriptions of descendant components, so that we can ensure the
// ancestor components re-render before descendants

var nullListeners = {
  notify: function notify() {}
};

function createListenerCollection() {
  var batch = Object(_batch__WEBPACK_IMPORTED_MODULE_0__["getBatch"])();
  var first = null;
  var last = null;
  return {
    clear: function clear() {
      first = null;
      last = null;
    },
    notify: function notify() {
      batch(function () {
        var listener = first;

        while (listener) {
          listener.callback();
          listener = listener.next;
        }
      });
    },
    get: function get() {
      var listeners = [];
      var listener = first;

      while (listener) {
        listeners.push(listener);
        listener = listener.next;
      }

      return listeners;
    },
    subscribe: function subscribe(callback) {
      var isSubscribed = true;
      var listener = last = {
        callback: callback,
        next: null,
        prev: last
      };

      if (listener.prev) {
        listener.prev.next = listener;
      } else {
        first = listener;
      }

      return function unsubscribe() {
        if (!isSubscribed || first === null) return;
        isSubscribed = false;

        if (listener.next) {
          listener.next.prev = listener.prev;
        } else {
          last = listener.prev;
        }

        if (listener.prev) {
          listener.prev.next = listener.next;
        } else {
          first = listener.next;
        }
      };
    }
  };
}

var Subscription =
/*#__PURE__*/
function () {
  function Subscription(store, parentSub) {
    this.store = store;
    this.parentSub = parentSub;
    this.unsubscribe = null;
    this.listeners = nullListeners;
    this.handleChangeWrapper = this.handleChangeWrapper.bind(this);
  }

  var _proto = Subscription.prototype;

  _proto.addNestedSub = function addNestedSub(listener) {
    this.trySubscribe();
    return this.listeners.subscribe(listener);
  };

  _proto.notifyNestedSubs = function notifyNestedSubs() {
    this.listeners.notify();
  };

  _proto.handleChangeWrapper = function handleChangeWrapper() {
    if (this.onStateChange) {
      this.onStateChange();
    }
  };

  _proto.isSubscribed = function isSubscribed() {
    return Boolean(this.unsubscribe);
  };

  _proto.trySubscribe = function trySubscribe() {
    if (!this.unsubscribe) {
      this.unsubscribe = this.parentSub ? this.parentSub.addNestedSub(this.handleChangeWrapper) : this.store.subscribe(this.handleChangeWrapper);
      this.listeners = createListenerCollection();
    }
  };

  _proto.tryUnsubscribe = function tryUnsubscribe() {
    if (this.unsubscribe) {
      this.unsubscribe();
      this.unsubscribe = null;
      this.listeners.clear();
      this.listeners = nullListeners;
    }
  };

  return Subscription;
}();



/***/ }),

/***/ "./node_modules/react-redux/es/utils/batch.js":
/*!****************************************************!*\
  !*** ./node_modules/react-redux/es/utils/batch.js ***!
  \****************************************************/
/*! exports provided: setBatch, getBatch */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "setBatch", function() { return setBatch; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getBatch", function() { return getBatch; });
// Default to a dummy "batch" implementation that just runs the callback
function defaultNoopBatch(callback) {
  callback();
}

var batch = defaultNoopBatch; // Allow injecting another batching function later

var setBatch = function setBatch(newBatch) {
  return batch = newBatch;
}; // Supply a getter just to skip dealing with ESM bindings

var getBatch = function getBatch() {
  return batch;
};

/***/ }),

/***/ "./node_modules/react-redux/es/utils/isPlainObject.js":
/*!************************************************************!*\
  !*** ./node_modules/react-redux/es/utils/isPlainObject.js ***!
  \************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return isPlainObject; });
/**
 * @param {any} obj The object to inspect.
 * @returns {boolean} True if the argument appears to be a plain object.
 */
function isPlainObject(obj) {
  if (typeof obj !== 'object' || obj === null) return false;
  var proto = Object.getPrototypeOf(obj);
  if (proto === null) return true;
  var baseProto = proto;

  while (Object.getPrototypeOf(baseProto) !== null) {
    baseProto = Object.getPrototypeOf(baseProto);
  }

  return proto === baseProto;
}

/***/ }),

/***/ "./node_modules/react-redux/es/utils/reactBatchedUpdates.js":
/*!******************************************************************!*\
  !*** ./node_modules/react-redux/es/utils/reactBatchedUpdates.js ***!
  \******************************************************************/
/*! exports provided: unstable_batchedUpdates */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-dom */ "./node_modules/react-dom/index.js");
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react_dom__WEBPACK_IMPORTED_MODULE_0__);
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "unstable_batchedUpdates", function() { return react_dom__WEBPACK_IMPORTED_MODULE_0__["unstable_batchedUpdates"]; });

/* eslint-disable import/no-unresolved */


/***/ }),

/***/ "./node_modules/react-redux/es/utils/shallowEqual.js":
/*!***********************************************************!*\
  !*** ./node_modules/react-redux/es/utils/shallowEqual.js ***!
  \***********************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return shallowEqual; });
function is(x, y) {
  if (x === y) {
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}

function shallowEqual(objA, objB) {
  if (is(objA, objB)) return true;

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);
  if (keysA.length !== keysB.length) return false;

  for (var i = 0; i < keysA.length; i++) {
    if (!Object.prototype.hasOwnProperty.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
}

/***/ }),

/***/ "./node_modules/react-redux/es/utils/useIsomorphicLayoutEffect.js":
/*!************************************************************************!*\
  !*** ./node_modules/react-redux/es/utils/useIsomorphicLayoutEffect.js ***!
  \************************************************************************/
/*! exports provided: useIsomorphicLayoutEffect */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "useIsomorphicLayoutEffect", function() { return useIsomorphicLayoutEffect; });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
 // React currently throws a warning when using useLayoutEffect on the server.
// To get around it, we can conditionally useEffect on the server (no-op) and
// useLayoutEffect in the browser. We need useLayoutEffect to ensure the store
// subscription callback always has the selector from the latest render commit
// available, otherwise a store update may happen between render and the effect,
// which may cause missed updates; we also must ensure the store subscription
// is created synchronously, otherwise a store update may occur before the
// subscription is created and an inconsistent state may be observed

var useIsomorphicLayoutEffect = typeof window !== 'undefined' && typeof window.document !== 'undefined' && typeof window.document.createElement !== 'undefined' ? react__WEBPACK_IMPORTED_MODULE_0__["useLayoutEffect"] : react__WEBPACK_IMPORTED_MODULE_0__["useEffect"];

/***/ }),

/***/ "./node_modules/react-redux/es/utils/verifyPlainObject.js":
/*!****************************************************************!*\
  !*** ./node_modules/react-redux/es/utils/verifyPlainObject.js ***!
  \****************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return verifyPlainObject; });
/* harmony import */ var _isPlainObject__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./isPlainObject */ "./node_modules/react-redux/es/utils/isPlainObject.js");
/* harmony import */ var _warning__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./warning */ "./node_modules/react-redux/es/utils/warning.js");


function verifyPlainObject(value, displayName, methodName) {
  if (!Object(_isPlainObject__WEBPACK_IMPORTED_MODULE_0__["default"])(value)) {
    Object(_warning__WEBPACK_IMPORTED_MODULE_1__["default"])(methodName + "() in " + displayName + " must return a plain object. Instead received " + value + ".");
  }
}

/***/ }),

/***/ "./node_modules/react-redux/es/utils/warning.js":
/*!******************************************************!*\
  !*** ./node_modules/react-redux/es/utils/warning.js ***!
  \******************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return warning; });
/**
 * Prints a warning in the console if it exists.
 *
 * @param {String} message The warning message.
 * @returns {void}
 */
function warning(message) {
  /* eslint-disable no-console */
  if (typeof console !== 'undefined' && typeof console.error === 'function') {
    console.error(message);
  }
  /* eslint-enable no-console */


  try {
    // This error was thrown as a convenience so that if you enable
    // "break on all exceptions" in your console,
    // it would pause the execution at this line.
    throw new Error(message);
    /* eslint-disable no-empty */
  } catch (e) {}
  /* eslint-enable no-empty */

}

/***/ }),

/***/ "./node_modules/react/cjs/react.development.js":
/*!*****************************************************!*\
  !*** ./node_modules/react/cjs/react.development.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.13.0
 * react.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (true) {
  (function() {
'use strict';

var _assign = __webpack_require__(/*! object-assign */ "./node_modules/object-assign/index.js");
var checkPropTypes = __webpack_require__(/*! prop-types/checkPropTypes */ "./node_modules/prop-types/checkPropTypes.js");

var ReactVersion = '16.13.0';

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol.for;
var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;
var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;
var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;
var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;
var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary
var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;
var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;
var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;
var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;
var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;
var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;
var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for('react.block') : 0xead9;
var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for('react.fundamental') : 0xead5;
var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for('react.responder') : 0xead6;
var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for('react.scope') : 0xead7;
var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator';
function getIteratorFn(maybeIterable) {
  if (maybeIterable === null || typeof maybeIterable !== 'object') {
    return null;
  }

  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];

  if (typeof maybeIterator === 'function') {
    return maybeIterator;
  }

  return null;
}

/**
 * Keeps track of the current dispatcher.
 */
var ReactCurrentDispatcher = {
  /**
   * @internal
   * @type {ReactComponent}
   */
  current: null
};

/**
 * Keeps track of the current batch's configuration such as how long an update
 * should suspend for if it needs to.
 */
var ReactCurrentBatchConfig = {
  suspense: null
};

/**
 * Keeps track of the current owner.
 *
 * The current owner is the component who should own any components that are
 * currently being constructed.
 */
var ReactCurrentOwner = {
  /**
   * @internal
   * @type {ReactComponent}
   */
  current: null
};

var BEFORE_SLASH_RE = /^(.*)[\\\/]/;
function describeComponentFrame (name, source, ownerName) {
  var sourceInfo = '';

  if (source) {
    var path = source.fileName;
    var fileName = path.replace(BEFORE_SLASH_RE, '');

    {
      // In DEV, include code for a common special case:
      // prefer "folder/index.js" instead of just "index.js".
      if (/^index\./.test(fileName)) {
        var match = path.match(BEFORE_SLASH_RE);

        if (match) {
          var pathBeforeSlash = match[1];

          if (pathBeforeSlash) {
            var folderName = pathBeforeSlash.replace(BEFORE_SLASH_RE, '');
            fileName = folderName + '/' + fileName;
          }
        }
      }
    }

    sourceInfo = ' (at ' + fileName + ':' + source.lineNumber + ')';
  } else if (ownerName) {
    sourceInfo = ' (created by ' + ownerName + ')';
  }

  return '\n    in ' + (name || 'Unknown') + sourceInfo;
}

var Resolved = 1;
function refineResolvedLazyComponent(lazyComponent) {
  return lazyComponent._status === Resolved ? lazyComponent._result : null;
}

function getWrappedName(outerType, innerType, wrapperName) {
  var functionName = innerType.displayName || innerType.name || '';
  return outerType.displayName || (functionName !== '' ? wrapperName + "(" + functionName + ")" : wrapperName);
}

function getComponentName(type) {
  if (type == null) {
    // Host root, text node or just invalid type.
    return null;
  }

  {
    if (typeof type.tag === 'number') {
      error('Received an unexpected object in getComponentName(). ' + 'This is likely a bug in React. Please file an issue.');
    }
  }

  if (typeof type === 'function') {
    return type.displayName || type.name || null;
  }

  if (typeof type === 'string') {
    return type;
  }

  switch (type) {
    case REACT_FRAGMENT_TYPE:
      return 'Fragment';

    case REACT_PORTAL_TYPE:
      return 'Portal';

    case REACT_PROFILER_TYPE:
      return "Profiler";

    case REACT_STRICT_MODE_TYPE:
      return 'StrictMode';

    case REACT_SUSPENSE_TYPE:
      return 'Suspense';

    case REACT_SUSPENSE_LIST_TYPE:
      return 'SuspenseList';
  }

  if (typeof type === 'object') {
    switch (type.$$typeof) {
      case REACT_CONTEXT_TYPE:
        return 'Context.Consumer';

      case REACT_PROVIDER_TYPE:
        return 'Context.Provider';

      case REACT_FORWARD_REF_TYPE:
        return getWrappedName(type, type.render, 'ForwardRef');

      case REACT_MEMO_TYPE:
        return getComponentName(type.type);

      case REACT_BLOCK_TYPE:
        return getComponentName(type.render);

      case REACT_LAZY_TYPE:
        {
          var thenable = type;
          var resolvedThenable = refineResolvedLazyComponent(thenable);

          if (resolvedThenable) {
            return getComponentName(resolvedThenable);
          }

          break;
        }
    }
  }

  return null;
}

var ReactDebugCurrentFrame = {};
var currentlyValidatingElement = null;
function setCurrentlyValidatingElement(element) {
  {
    currentlyValidatingElement = element;
  }
}

{
  // Stack implementation injected by the current renderer.
  ReactDebugCurrentFrame.getCurrentStack = null;

  ReactDebugCurrentFrame.getStackAddendum = function () {
    var stack = ''; // Add an extra top frame while an element is being validated

    if (currentlyValidatingElement) {
      var name = getComponentName(currentlyValidatingElement.type);
      var owner = currentlyValidatingElement._owner;
      stack += describeComponentFrame(name, currentlyValidatingElement._source, owner && getComponentName(owner.type));
    } // Delegate to the injected renderer-specific implementation


    var impl = ReactDebugCurrentFrame.getCurrentStack;

    if (impl) {
      stack += impl() || '';
    }

    return stack;
  };
}

/**
 * Used by act() to track whether you're inside an act() scope.
 */
var IsSomeRendererActing = {
  current: false
};

var ReactSharedInternals = {
  ReactCurrentDispatcher: ReactCurrentDispatcher,
  ReactCurrentBatchConfig: ReactCurrentBatchConfig,
  ReactCurrentOwner: ReactCurrentOwner,
  IsSomeRendererActing: IsSomeRendererActing,
  // Used by renderers to avoid bundling object-assign twice in UMD bundles:
  assign: _assign
};

{
  _assign(ReactSharedInternals, {
    // These should not be included in production.
    ReactDebugCurrentFrame: ReactDebugCurrentFrame,
    // Shim for React DOM 16.0.0 which still destructured (but not used) this.
    // TODO: remove in React 17.0.
    ReactComponentTreeHook: {}
  });
}

// by calls to these methods by a Babel plugin.
//
// In PROD (or in packages without access to React internals),
// they are left as they are instead.

function warn(format) {
  {
    for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    printWarning('warn', format, args);
  }
}
function error(format) {
  {
    for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
      args[_key2 - 1] = arguments[_key2];
    }

    printWarning('error', format, args);
  }
}

function printWarning(level, format, args) {
  // When changing this logic, you might want to also
  // update consoleWithStackDev.www.js as well.
  {
    var hasExistingStack = args.length > 0 && typeof args[args.length - 1] === 'string' && args[args.length - 1].indexOf('\n    in') === 0;

    if (!hasExistingStack) {
      var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
      var stack = ReactDebugCurrentFrame.getStackAddendum();

      if (stack !== '') {
        format += '%s';
        args = args.concat([stack]);
      }
    }

    var argsWithFormat = args.map(function (item) {
      return '' + item;
    }); // Careful: RN currently depends on this prefix

    argsWithFormat.unshift('Warning: ' + format); // We intentionally don't use spread (or .apply) directly because it
    // breaks IE9: https://github.com/facebook/react/issues/13610
    // eslint-disable-next-line react-internal/no-production-logging

    Function.prototype.apply.call(console[level], console, argsWithFormat);

    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      var argIndex = 0;
      var message = 'Warning: ' + format.replace(/%s/g, function () {
        return args[argIndex++];
      });
      throw new Error(message);
    } catch (x) {}
  }
}

var didWarnStateUpdateForUnmountedComponent = {};

function warnNoop(publicInstance, callerName) {
  {
    var _constructor = publicInstance.constructor;
    var componentName = _constructor && (_constructor.displayName || _constructor.name) || 'ReactClass';
    var warningKey = componentName + "." + callerName;

    if (didWarnStateUpdateForUnmountedComponent[warningKey]) {
      return;
    }

    error("Can't call %s on a component that is not yet mounted. " + 'This is a no-op, but it might indicate a bug in your application. ' + 'Instead, assign to `this.state` directly or define a `state = {};` ' + 'class property with the desired state in the %s component.', callerName, componentName);

    didWarnStateUpdateForUnmountedComponent[warningKey] = true;
  }
}
/**
 * This is the abstract API for an update queue.
 */


var ReactNoopUpdateQueue = {
  /**
   * Checks whether or not this composite component is mounted.
   * @param {ReactClass} publicInstance The instance we want to test.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */
  isMounted: function (publicInstance) {
    return false;
  },

  /**
   * Forces an update. This should only be invoked when it is known with
   * certainty that we are **not** in a DOM transaction.
   *
   * You may want to call this when you know that some deeper aspect of the
   * component's state has changed but `setState` was not called.
   *
   * This will not invoke `shouldComponentUpdate`, but it will invoke
   * `componentWillUpdate` and `componentDidUpdate`.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */
  enqueueForceUpdate: function (publicInstance, callback, callerName) {
    warnNoop(publicInstance, 'forceUpdate');
  },

  /**
   * Replaces all of the state. Always use this or `setState` to mutate state.
   * You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} completeState Next state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */
  enqueueReplaceState: function (publicInstance, completeState, callback, callerName) {
    warnNoop(publicInstance, 'replaceState');
  },

  /**
   * Sets a subset of the state. This only exists because _pendingState is
   * internal. This provides a merging strategy that is not available to deep
   * properties which is confusing. TODO: Expose pendingState or don't use it
   * during the merge.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialState Next partial state to be merged with state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} Name of the calling function in the public API.
   * @internal
   */
  enqueueSetState: function (publicInstance, partialState, callback, callerName) {
    warnNoop(publicInstance, 'setState');
  }
};

var emptyObject = {};

{
  Object.freeze(emptyObject);
}
/**
 * Base class helpers for the updating state of a component.
 */


function Component(props, context, updater) {
  this.props = props;
  this.context = context; // If a component has string refs, we will assign a different object later.

  this.refs = emptyObject; // We initialize the default updater but the real one gets injected by the
  // renderer.

  this.updater = updater || ReactNoopUpdateQueue;
}

Component.prototype.isReactComponent = {};
/**
 * Sets a subset of the state. Always use this to mutate
 * state. You should treat `this.state` as immutable.
 *
 * There is no guarantee that `this.state` will be immediately updated, so
 * accessing `this.state` after calling this method may return the old value.
 *
 * There is no guarantee that calls to `setState` will run synchronously,
 * as they may eventually be batched together.  You can provide an optional
 * callback that will be executed when the call to setState is actually
 * completed.
 *
 * When a function is provided to setState, it will be called at some point in
 * the future (not synchronously). It will be called with the up to date
 * component arguments (state, props, context). These values can be different
 * from this.* because your function may be called after receiveProps but before
 * shouldComponentUpdate, and this new state, props, and context will not yet be
 * assigned to this.
 *
 * @param {object|function} partialState Next partial state or function to
 *        produce next partial state to be merged with current state.
 * @param {?function} callback Called after state is updated.
 * @final
 * @protected
 */

Component.prototype.setState = function (partialState, callback) {
  if (!(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null)) {
    {
      throw Error( "setState(...): takes an object of state variables to update or a function which returns an object of state variables." );
    }
  }

  this.updater.enqueueSetState(this, partialState, callback, 'setState');
};
/**
 * Forces an update. This should only be invoked when it is known with
 * certainty that we are **not** in a DOM transaction.
 *
 * You may want to call this when you know that some deeper aspect of the
 * component's state has changed but `setState` was not called.
 *
 * This will not invoke `shouldComponentUpdate`, but it will invoke
 * `componentWillUpdate` and `componentDidUpdate`.
 *
 * @param {?function} callback Called after update is complete.
 * @final
 * @protected
 */


Component.prototype.forceUpdate = function (callback) {
  this.updater.enqueueForceUpdate(this, callback, 'forceUpdate');
};
/**
 * Deprecated APIs. These APIs used to exist on classic React classes but since
 * we would like to deprecate them, we're not going to move them over to this
 * modern base class. Instead, we define a getter that warns if it's accessed.
 */


{
  var deprecatedAPIs = {
    isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],
    replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).']
  };

  var defineDeprecationWarning = function (methodName, info) {
    Object.defineProperty(Component.prototype, methodName, {
      get: function () {
        warn('%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]);

        return undefined;
      }
    });
  };

  for (var fnName in deprecatedAPIs) {
    if (deprecatedAPIs.hasOwnProperty(fnName)) {
      defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
    }
  }
}

function ComponentDummy() {}

ComponentDummy.prototype = Component.prototype;
/**
 * Convenience component with default shallow equality check for sCU.
 */

function PureComponent(props, context, updater) {
  this.props = props;
  this.context = context; // If a component has string refs, we will assign a different object later.

  this.refs = emptyObject;
  this.updater = updater || ReactNoopUpdateQueue;
}

var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
pureComponentPrototype.constructor = PureComponent; // Avoid an extra prototype jump for these methods.

_assign(pureComponentPrototype, Component.prototype);

pureComponentPrototype.isPureReactComponent = true;

// an immutable object with a single mutable value
function createRef() {
  var refObject = {
    current: null
  };

  {
    Object.seal(refObject);
  }

  return refObject;
}

var hasOwnProperty = Object.prototype.hasOwnProperty;
var RESERVED_PROPS = {
  key: true,
  ref: true,
  __self: true,
  __source: true
};
var specialPropKeyWarningShown, specialPropRefWarningShown, didWarnAboutStringRefs;

{
  didWarnAboutStringRefs = {};
}

function hasValidRef(config) {
  {
    if (hasOwnProperty.call(config, 'ref')) {
      var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;

      if (getter && getter.isReactWarning) {
        return false;
      }
    }
  }

  return config.ref !== undefined;
}

function hasValidKey(config) {
  {
    if (hasOwnProperty.call(config, 'key')) {
      var getter = Object.getOwnPropertyDescriptor(config, 'key').get;

      if (getter && getter.isReactWarning) {
        return false;
      }
    }
  }

  return config.key !== undefined;
}

function defineKeyPropWarningGetter(props, displayName) {
  var warnAboutAccessingKey = function () {
    {
      if (!specialPropKeyWarningShown) {
        specialPropKeyWarningShown = true;

        error('%s: `key` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
      }
    }
  };

  warnAboutAccessingKey.isReactWarning = true;
  Object.defineProperty(props, 'key', {
    get: warnAboutAccessingKey,
    configurable: true
  });
}

function defineRefPropWarningGetter(props, displayName) {
  var warnAboutAccessingRef = function () {
    {
      if (!specialPropRefWarningShown) {
        specialPropRefWarningShown = true;

        error('%s: `ref` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
      }
    }
  };

  warnAboutAccessingRef.isReactWarning = true;
  Object.defineProperty(props, 'ref', {
    get: warnAboutAccessingRef,
    configurable: true
  });
}

function warnIfStringRefCannotBeAutoConverted(config) {
  {
    if (typeof config.ref === 'string' && ReactCurrentOwner.current && config.__self && ReactCurrentOwner.current.stateNode !== config.__self) {
      var componentName = getComponentName(ReactCurrentOwner.current.type);

      if (!didWarnAboutStringRefs[componentName]) {
        error('Component "%s" contains the string ref "%s". ' + 'Support for string refs will be removed in a future major release. ' + 'This case cannot be automatically converted to an arrow function. ' + 'We ask you to manually fix this case by using useRef() or createRef() instead. ' + 'Learn more about using refs safely here: ' + 'https://fb.me/react-strict-mode-string-ref', getComponentName(ReactCurrentOwner.current.type), config.ref);

        didWarnAboutStringRefs[componentName] = true;
      }
    }
  }
}
/**
 * Factory method to create a new React element. This no longer adheres to
 * the class pattern, so do not use new to call it. Also, instanceof check
 * will not work. Instead test $$typeof field against Symbol.for('react.element') to check
 * if something is a React Element.
 *
 * @param {*} type
 * @param {*} props
 * @param {*} key
 * @param {string|object} ref
 * @param {*} owner
 * @param {*} self A *temporary* helper to detect places where `this` is
 * different from the `owner` when React.createElement is called, so that we
 * can warn. We want to get rid of owner and replace string `ref`s with arrow
 * functions, and as long as `this` and owner are the same, there will be no
 * change in behavior.
 * @param {*} source An annotation object (added by a transpiler or otherwise)
 * indicating filename, line number, and/or other information.
 * @internal
 */


var ReactElement = function (type, key, ref, self, source, owner, props) {
  var element = {
    // This tag allows us to uniquely identify this as a React Element
    $$typeof: REACT_ELEMENT_TYPE,
    // Built-in properties that belong on the element
    type: type,
    key: key,
    ref: ref,
    props: props,
    // Record the component responsible for creating this element.
    _owner: owner
  };

  {
    // The validation flag is currently mutative. We put it on
    // an external backing store so that we can freeze the whole object.
    // This can be replaced with a WeakMap once they are implemented in
    // commonly used development environments.
    element._store = {}; // To make comparing ReactElements easier for testing purposes, we make
    // the validation flag non-enumerable (where possible, which should
    // include every environment we run tests in), so the test framework
    // ignores it.

    Object.defineProperty(element._store, 'validated', {
      configurable: false,
      enumerable: false,
      writable: true,
      value: false
    }); // self and source are DEV only properties.

    Object.defineProperty(element, '_self', {
      configurable: false,
      enumerable: false,
      writable: false,
      value: self
    }); // Two elements created in two different places should be considered
    // equal for testing purposes and therefore we hide it from enumeration.

    Object.defineProperty(element, '_source', {
      configurable: false,
      enumerable: false,
      writable: false,
      value: source
    });

    if (Object.freeze) {
      Object.freeze(element.props);
      Object.freeze(element);
    }
  }

  return element;
};
/**
 * Create and return a new ReactElement of the given type.
 * See https://reactjs.org/docs/react-api.html#createelement
 */

function createElement(type, config, children) {
  var propName; // Reserved names are extracted

  var props = {};
  var key = null;
  var ref = null;
  var self = null;
  var source = null;

  if (config != null) {
    if (hasValidRef(config)) {
      ref = config.ref;

      {
        warnIfStringRefCannotBeAutoConverted(config);
      }
    }

    if (hasValidKey(config)) {
      key = '' + config.key;
    }

    self = config.__self === undefined ? null : config.__self;
    source = config.__source === undefined ? null : config.__source; // Remaining properties are added to a new props object

    for (propName in config) {
      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        props[propName] = config[propName];
      }
    }
  } // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.


  var childrenLength = arguments.length - 2;

  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);

    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }

    {
      if (Object.freeze) {
        Object.freeze(childArray);
      }
    }

    props.children = childArray;
  } // Resolve default props


  if (type && type.defaultProps) {
    var defaultProps = type.defaultProps;

    for (propName in defaultProps) {
      if (props[propName] === undefined) {
        props[propName] = defaultProps[propName];
      }
    }
  }

  {
    if (key || ref) {
      var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;

      if (key) {
        defineKeyPropWarningGetter(props, displayName);
      }

      if (ref) {
        defineRefPropWarningGetter(props, displayName);
      }
    }
  }

  return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
}
function cloneAndReplaceKey(oldElement, newKey) {
  var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);
  return newElement;
}
/**
 * Clone and return a new ReactElement using element as the starting point.
 * See https://reactjs.org/docs/react-api.html#cloneelement
 */

function cloneElement(element, config, children) {
  if (!!(element === null || element === undefined)) {
    {
      throw Error( "React.cloneElement(...): The argument must be a React element, but you passed " + element + "." );
    }
  }

  var propName; // Original props are copied

  var props = _assign({}, element.props); // Reserved names are extracted


  var key = element.key;
  var ref = element.ref; // Self is preserved since the owner is preserved.

  var self = element._self; // Source is preserved since cloneElement is unlikely to be targeted by a
  // transpiler, and the original source is probably a better indicator of the
  // true owner.

  var source = element._source; // Owner will be preserved, unless ref is overridden

  var owner = element._owner;

  if (config != null) {
    if (hasValidRef(config)) {
      // Silently steal the ref from the parent.
      ref = config.ref;
      owner = ReactCurrentOwner.current;
    }

    if (hasValidKey(config)) {
      key = '' + config.key;
    } // Remaining properties override existing props


    var defaultProps;

    if (element.type && element.type.defaultProps) {
      defaultProps = element.type.defaultProps;
    }

    for (propName in config) {
      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        if (config[propName] === undefined && defaultProps !== undefined) {
          // Resolve default props
          props[propName] = defaultProps[propName];
        } else {
          props[propName] = config[propName];
        }
      }
    }
  } // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.


  var childrenLength = arguments.length - 2;

  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);

    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }

    props.children = childArray;
  }

  return ReactElement(element.type, key, ref, self, source, owner, props);
}
/**
 * Verifies the object is a ReactElement.
 * See https://reactjs.org/docs/react-api.html#isvalidelement
 * @param {?object} object
 * @return {boolean} True if `object` is a ReactElement.
 * @final
 */

function isValidElement(object) {
  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
}

var SEPARATOR = '.';
var SUBSEPARATOR = ':';
/**
 * Escape and wrap key so it is safe to use as a reactid
 *
 * @param {string} key to be escaped.
 * @return {string} the escaped key.
 */

function escape(key) {
  var escapeRegex = /[=:]/g;
  var escaperLookup = {
    '=': '=0',
    ':': '=2'
  };
  var escapedString = ('' + key).replace(escapeRegex, function (match) {
    return escaperLookup[match];
  });
  return '$' + escapedString;
}
/**
 * TODO: Test that a single child and an array with one item have the same key
 * pattern.
 */


var didWarnAboutMaps = false;
var userProvidedKeyEscapeRegex = /\/+/g;

function escapeUserProvidedKey(text) {
  return ('' + text).replace(userProvidedKeyEscapeRegex, '$&/');
}

var POOL_SIZE = 10;
var traverseContextPool = [];

function getPooledTraverseContext(mapResult, keyPrefix, mapFunction, mapContext) {
  if (traverseContextPool.length) {
    var traverseContext = traverseContextPool.pop();
    traverseContext.result = mapResult;
    traverseContext.keyPrefix = keyPrefix;
    traverseContext.func = mapFunction;
    traverseContext.context = mapContext;
    traverseContext.count = 0;
    return traverseContext;
  } else {
    return {
      result: mapResult,
      keyPrefix: keyPrefix,
      func: mapFunction,
      context: mapContext,
      count: 0
    };
  }
}

function releaseTraverseContext(traverseContext) {
  traverseContext.result = null;
  traverseContext.keyPrefix = null;
  traverseContext.func = null;
  traverseContext.context = null;
  traverseContext.count = 0;

  if (traverseContextPool.length < POOL_SIZE) {
    traverseContextPool.push(traverseContext);
  }
}
/**
 * @param {?*} children Children tree container.
 * @param {!string} nameSoFar Name of the key path so far.
 * @param {!function} callback Callback to invoke with each child found.
 * @param {?*} traverseContext Used to pass information throughout the traversal
 * process.
 * @return {!number} The number of children in this subtree.
 */


function traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {
  var type = typeof children;

  if (type === 'undefined' || type === 'boolean') {
    // All of the above are perceived as null.
    children = null;
  }

  var invokeCallback = false;

  if (children === null) {
    invokeCallback = true;
  } else {
    switch (type) {
      case 'string':
      case 'number':
        invokeCallback = true;
        break;

      case 'object':
        switch (children.$$typeof) {
          case REACT_ELEMENT_TYPE:
          case REACT_PORTAL_TYPE:
            invokeCallback = true;
        }

    }
  }

  if (invokeCallback) {
    callback(traverseContext, children, // If it's the only child, treat the name as if it was wrapped in an array
    // so that it's consistent if the number of children grows.
    nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);
    return 1;
  }

  var child;
  var nextName;
  var subtreeCount = 0; // Count of children found in the current subtree.

  var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;

  if (Array.isArray(children)) {
    for (var i = 0; i < children.length; i++) {
      child = children[i];
      nextName = nextNamePrefix + getComponentKey(child, i);
      subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
    }
  } else {
    var iteratorFn = getIteratorFn(children);

    if (typeof iteratorFn === 'function') {

      {
        // Warn about using Maps as children
        if (iteratorFn === children.entries) {
          if (!didWarnAboutMaps) {
            warn('Using Maps as children is deprecated and will be removed in ' + 'a future major release. Consider converting children to ' + 'an array of keyed ReactElements instead.');
          }

          didWarnAboutMaps = true;
        }
      }

      var iterator = iteratorFn.call(children);
      var step;
      var ii = 0;

      while (!(step = iterator.next()).done) {
        child = step.value;
        nextName = nextNamePrefix + getComponentKey(child, ii++);
        subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
      }
    } else if (type === 'object') {
      var addendum = '';

      {
        addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + ReactDebugCurrentFrame.getStackAddendum();
      }

      var childrenString = '' + children;

      {
        {
          throw Error( "Objects are not valid as a React child (found: " + (childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString) + ")." + addendum );
        }
      }
    }
  }

  return subtreeCount;
}
/**
 * Traverses children that are typically specified as `props.children`, but
 * might also be specified through attributes:
 *
 * - `traverseAllChildren(this.props.children, ...)`
 * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
 *
 * The `traverseContext` is an optional argument that is passed through the
 * entire traversal. It can be used to store accumulations or anything else that
 * the callback might find relevant.
 *
 * @param {?*} children Children tree object.
 * @param {!function} callback To invoke upon traversing each child.
 * @param {?*} traverseContext Context for traversal.
 * @return {!number} The number of children in this subtree.
 */


function traverseAllChildren(children, callback, traverseContext) {
  if (children == null) {
    return 0;
  }

  return traverseAllChildrenImpl(children, '', callback, traverseContext);
}
/**
 * Generate a key string that identifies a component within a set.
 *
 * @param {*} component A component that could contain a manual key.
 * @param {number} index Index that is used if a manual key is not provided.
 * @return {string}
 */


function getComponentKey(component, index) {
  // Do some typechecking here since we call this blindly. We want to ensure
  // that we don't block potential future ES APIs.
  if (typeof component === 'object' && component !== null && component.key != null) {
    // Explicit key
    return escape(component.key);
  } // Implicit key determined by the index in the set


  return index.toString(36);
}

function forEachSingleChild(bookKeeping, child, name) {
  var func = bookKeeping.func,
      context = bookKeeping.context;
  func.call(context, child, bookKeeping.count++);
}
/**
 * Iterates through children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrenforeach
 *
 * The provided forEachFunc(child, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} forEachFunc
 * @param {*} forEachContext Context for forEachContext.
 */


function forEachChildren(children, forEachFunc, forEachContext) {
  if (children == null) {
    return children;
  }

  var traverseContext = getPooledTraverseContext(null, null, forEachFunc, forEachContext);
  traverseAllChildren(children, forEachSingleChild, traverseContext);
  releaseTraverseContext(traverseContext);
}

function mapSingleChildIntoContext(bookKeeping, child, childKey) {
  var result = bookKeeping.result,
      keyPrefix = bookKeeping.keyPrefix,
      func = bookKeeping.func,
      context = bookKeeping.context;
  var mappedChild = func.call(context, child, bookKeeping.count++);

  if (Array.isArray(mappedChild)) {
    mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, function (c) {
      return c;
    });
  } else if (mappedChild != null) {
    if (isValidElement(mappedChild)) {
      mappedChild = cloneAndReplaceKey(mappedChild, // Keep both the (mapped) and old keys if they differ, just as
      // traverseAllChildren used to do for objects as children
      keyPrefix + (mappedChild.key && (!child || child.key !== mappedChild.key) ? escapeUserProvidedKey(mappedChild.key) + '/' : '') + childKey);
    }

    result.push(mappedChild);
  }
}

function mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {
  var escapedPrefix = '';

  if (prefix != null) {
    escapedPrefix = escapeUserProvidedKey(prefix) + '/';
  }

  var traverseContext = getPooledTraverseContext(array, escapedPrefix, func, context);
  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
  releaseTraverseContext(traverseContext);
}
/**
 * Maps children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrenmap
 *
 * The provided mapFunction(child, key, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} func The map function.
 * @param {*} context Context for mapFunction.
 * @return {object} Object containing the ordered map of results.
 */


function mapChildren(children, func, context) {
  if (children == null) {
    return children;
  }

  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, func, context);
  return result;
}
/**
 * Count the number of children that are typically specified as
 * `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrencount
 *
 * @param {?*} children Children tree container.
 * @return {number} The number of children.
 */


function countChildren(children) {
  return traverseAllChildren(children, function () {
    return null;
  }, null);
}
/**
 * Flatten a children object (typically specified as `props.children`) and
 * return an array with appropriately re-keyed children.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrentoarray
 */


function toArray(children) {
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, function (child) {
    return child;
  });
  return result;
}
/**
 * Returns the first child in a collection of children and verifies that there
 * is only one child in the collection.
 *
 * See https://reactjs.org/docs/react-api.html#reactchildrenonly
 *
 * The current implementation of this function assumes that a single child gets
 * passed without a wrapper, but the purpose of this helper function is to
 * abstract away the particular structure of children.
 *
 * @param {?object} children Child collection structure.
 * @return {ReactElement} The first and only `ReactElement` contained in the
 * structure.
 */


function onlyChild(children) {
  if (!isValidElement(children)) {
    {
      throw Error( "React.Children.only expected to receive a single React element child." );
    }
  }

  return children;
}

function createContext(defaultValue, calculateChangedBits) {
  if (calculateChangedBits === undefined) {
    calculateChangedBits = null;
  } else {
    {
      if (calculateChangedBits !== null && typeof calculateChangedBits !== 'function') {
        error('createContext: Expected the optional second argument to be a ' + 'function. Instead received: %s', calculateChangedBits);
      }
    }
  }

  var context = {
    $$typeof: REACT_CONTEXT_TYPE,
    _calculateChangedBits: calculateChangedBits,
    // As a workaround to support multiple concurrent renderers, we categorize
    // some renderers as primary and others as secondary. We only expect
    // there to be two concurrent renderers at most: React Native (primary) and
    // Fabric (secondary); React DOM (primary) and React ART (secondary).
    // Secondary renderers store their context values on separate fields.
    _currentValue: defaultValue,
    _currentValue2: defaultValue,
    // Used to track how many concurrent renderers this context currently
    // supports within in a single renderer. Such as parallel server rendering.
    _threadCount: 0,
    // These are circular
    Provider: null,
    Consumer: null
  };
  context.Provider = {
    $$typeof: REACT_PROVIDER_TYPE,
    _context: context
  };
  var hasWarnedAboutUsingNestedContextConsumers = false;
  var hasWarnedAboutUsingConsumerProvider = false;

  {
    // A separate object, but proxies back to the original context object for
    // backwards compatibility. It has a different $$typeof, so we can properly
    // warn for the incorrect usage of Context as a Consumer.
    var Consumer = {
      $$typeof: REACT_CONTEXT_TYPE,
      _context: context,
      _calculateChangedBits: context._calculateChangedBits
    }; // $FlowFixMe: Flow complains about not setting a value, which is intentional here

    Object.defineProperties(Consumer, {
      Provider: {
        get: function () {
          if (!hasWarnedAboutUsingConsumerProvider) {
            hasWarnedAboutUsingConsumerProvider = true;

            error('Rendering <Context.Consumer.Provider> is not supported and will be removed in ' + 'a future major release. Did you mean to render <Context.Provider> instead?');
          }

          return context.Provider;
        },
        set: function (_Provider) {
          context.Provider = _Provider;
        }
      },
      _currentValue: {
        get: function () {
          return context._currentValue;
        },
        set: function (_currentValue) {
          context._currentValue = _currentValue;
        }
      },
      _currentValue2: {
        get: function () {
          return context._currentValue2;
        },
        set: function (_currentValue2) {
          context._currentValue2 = _currentValue2;
        }
      },
      _threadCount: {
        get: function () {
          return context._threadCount;
        },
        set: function (_threadCount) {
          context._threadCount = _threadCount;
        }
      },
      Consumer: {
        get: function () {
          if (!hasWarnedAboutUsingNestedContextConsumers) {
            hasWarnedAboutUsingNestedContextConsumers = true;

            error('Rendering <Context.Consumer.Consumer> is not supported and will be removed in ' + 'a future major release. Did you mean to render <Context.Consumer> instead?');
          }

          return context.Consumer;
        }
      }
    }); // $FlowFixMe: Flow complains about missing properties because it doesn't understand defineProperty

    context.Consumer = Consumer;
  }

  {
    context._currentRenderer = null;
    context._currentRenderer2 = null;
  }

  return context;
}

function lazy(ctor) {
  var lazyType = {
    $$typeof: REACT_LAZY_TYPE,
    _ctor: ctor,
    // React uses these fields to store the result.
    _status: -1,
    _result: null
  };

  {
    // In production, this would just set it on the object.
    var defaultProps;
    var propTypes;
    Object.defineProperties(lazyType, {
      defaultProps: {
        configurable: true,
        get: function () {
          return defaultProps;
        },
        set: function (newDefaultProps) {
          error('React.lazy(...): It is not supported to assign `defaultProps` to ' + 'a lazy component import. Either specify them where the component ' + 'is defined, or create a wrapping component around it.');

          defaultProps = newDefaultProps; // Match production behavior more closely:

          Object.defineProperty(lazyType, 'defaultProps', {
            enumerable: true
          });
        }
      },
      propTypes: {
        configurable: true,
        get: function () {
          return propTypes;
        },
        set: function (newPropTypes) {
          error('React.lazy(...): It is not supported to assign `propTypes` to ' + 'a lazy component import. Either specify them where the component ' + 'is defined, or create a wrapping component around it.');

          propTypes = newPropTypes; // Match production behavior more closely:

          Object.defineProperty(lazyType, 'propTypes', {
            enumerable: true
          });
        }
      }
    });
  }

  return lazyType;
}

function forwardRef(render) {
  {
    if (render != null && render.$$typeof === REACT_MEMO_TYPE) {
      error('forwardRef requires a render function but received a `memo` ' + 'component. Instead of forwardRef(memo(...)), use ' + 'memo(forwardRef(...)).');
    } else if (typeof render !== 'function') {
      error('forwardRef requires a render function but was given %s.', render === null ? 'null' : typeof render);
    } else {
      if (render.length !== 0 && render.length !== 2) {
        error('forwardRef render functions accept exactly two parameters: props and ref. %s', render.length === 1 ? 'Did you forget to use the ref parameter?' : 'Any additional parameter will be undefined.');
      }
    }

    if (render != null) {
      if (render.defaultProps != null || render.propTypes != null) {
        error('forwardRef render functions do not support propTypes or defaultProps. ' + 'Did you accidentally pass a React component?');
      }
    }
  }

  return {
    $$typeof: REACT_FORWARD_REF_TYPE,
    render: render
  };
}

function isValidElementType(type) {
  return typeof type === 'string' || typeof type === 'function' || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
  type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === 'object' && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
}

function memo(type, compare) {
  {
    if (!isValidElementType(type)) {
      error('memo: The first argument must be a component. Instead ' + 'received: %s', type === null ? 'null' : typeof type);
    }
  }

  return {
    $$typeof: REACT_MEMO_TYPE,
    type: type,
    compare: compare === undefined ? null : compare
  };
}

function resolveDispatcher() {
  var dispatcher = ReactCurrentDispatcher.current;

  if (!(dispatcher !== null)) {
    {
      throw Error( "Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://fb.me/react-invalid-hook-call for tips about how to debug and fix this problem." );
    }
  }

  return dispatcher;
}

function useContext(Context, unstable_observedBits) {
  var dispatcher = resolveDispatcher();

  {
    if (unstable_observedBits !== undefined) {
      error('useContext() second argument is reserved for future ' + 'use in React. Passing it is not supported. ' + 'You passed: %s.%s', unstable_observedBits, typeof unstable_observedBits === 'number' && Array.isArray(arguments[2]) ? '\n\nDid you call array.map(useContext)? ' + 'Calling Hooks inside a loop is not supported. ' + 'Learn more at https://fb.me/rules-of-hooks' : '');
    } // TODO: add a more generic warning for invalid values.


    if (Context._context !== undefined) {
      var realContext = Context._context; // Don't deduplicate because this legitimately causes bugs
      // and nobody should be using this in existing code.

      if (realContext.Consumer === Context) {
        error('Calling useContext(Context.Consumer) is not supported, may cause bugs, and will be ' + 'removed in a future major release. Did you mean to call useContext(Context) instead?');
      } else if (realContext.Provider === Context) {
        error('Calling useContext(Context.Provider) is not supported. ' + 'Did you mean to call useContext(Context) instead?');
      }
    }
  }

  return dispatcher.useContext(Context, unstable_observedBits);
}
function useState(initialState) {
  var dispatcher = resolveDispatcher();
  return dispatcher.useState(initialState);
}
function useReducer(reducer, initialArg, init) {
  var dispatcher = resolveDispatcher();
  return dispatcher.useReducer(reducer, initialArg, init);
}
function useRef(initialValue) {
  var dispatcher = resolveDispatcher();
  return dispatcher.useRef(initialValue);
}
function useEffect(create, deps) {
  var dispatcher = resolveDispatcher();
  return dispatcher.useEffect(create, deps);
}
function useLayoutEffect(create, deps) {
  var dispatcher = resolveDispatcher();
  return dispatcher.useLayoutEffect(create, deps);
}
function useCallback(callback, deps) {
  var dispatcher = resolveDispatcher();
  return dispatcher.useCallback(callback, deps);
}
function useMemo(create, deps) {
  var dispatcher = resolveDispatcher();
  return dispatcher.useMemo(create, deps);
}
function useImperativeHandle(ref, create, deps) {
  var dispatcher = resolveDispatcher();
  return dispatcher.useImperativeHandle(ref, create, deps);
}
function useDebugValue(value, formatterFn) {
  {
    var dispatcher = resolveDispatcher();
    return dispatcher.useDebugValue(value, formatterFn);
  }
}

var propTypesMisspellWarningShown;

{
  propTypesMisspellWarningShown = false;
}

function getDeclarationErrorAddendum() {
  if (ReactCurrentOwner.current) {
    var name = getComponentName(ReactCurrentOwner.current.type);

    if (name) {
      return '\n\nCheck the render method of `' + name + '`.';
    }
  }

  return '';
}

function getSourceInfoErrorAddendum(source) {
  if (source !== undefined) {
    var fileName = source.fileName.replace(/^.*[\\\/]/, '');
    var lineNumber = source.lineNumber;
    return '\n\nCheck your code at ' + fileName + ':' + lineNumber + '.';
  }

  return '';
}

function getSourceInfoErrorAddendumForProps(elementProps) {
  if (elementProps !== null && elementProps !== undefined) {
    return getSourceInfoErrorAddendum(elementProps.__source);
  }

  return '';
}
/**
 * Warn if there's no key explicitly set on dynamic arrays of children or
 * object keys are not valid. This allows us to keep track of children between
 * updates.
 */


var ownerHasKeyUseWarning = {};

function getCurrentComponentErrorInfo(parentType) {
  var info = getDeclarationErrorAddendum();

  if (!info) {
    var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;

    if (parentName) {
      info = "\n\nCheck the top-level render call using <" + parentName + ">.";
    }
  }

  return info;
}
/**
 * Warn if the element doesn't have an explicit key assigned to it.
 * This element is in an array. The array could grow and shrink or be
 * reordered. All children that haven't already been validated are required to
 * have a "key" property assigned to it. Error statuses are cached so a warning
 * will only be shown once.
 *
 * @internal
 * @param {ReactElement} element Element that requires a key.
 * @param {*} parentType element's parent's type.
 */


function validateExplicitKey(element, parentType) {
  if (!element._store || element._store.validated || element.key != null) {
    return;
  }

  element._store.validated = true;
  var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);

  if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
    return;
  }

  ownerHasKeyUseWarning[currentComponentErrorInfo] = true; // Usually the current owner is the offender, but if it accepts children as a
  // property, it may be the creator of the child that's responsible for
  // assigning it a key.

  var childOwner = '';

  if (element && element._owner && element._owner !== ReactCurrentOwner.current) {
    // Give the component that originally created this child.
    childOwner = " It was passed a child from " + getComponentName(element._owner.type) + ".";
  }

  setCurrentlyValidatingElement(element);

  {
    error('Each child in a list should have a unique "key" prop.' + '%s%s See https://fb.me/react-warning-keys for more information.', currentComponentErrorInfo, childOwner);
  }

  setCurrentlyValidatingElement(null);
}
/**
 * Ensure that every element either is passed in a static location, in an
 * array with an explicit keys property defined, or in an object literal
 * with valid key property.
 *
 * @internal
 * @param {ReactNode} node Statically passed child of any type.
 * @param {*} parentType node's parent's type.
 */


function validateChildKeys(node, parentType) {
  if (typeof node !== 'object') {
    return;
  }

  if (Array.isArray(node)) {
    for (var i = 0; i < node.length; i++) {
      var child = node[i];

      if (isValidElement(child)) {
        validateExplicitKey(child, parentType);
      }
    }
  } else if (isValidElement(node)) {
    // This element was passed in a valid location.
    if (node._store) {
      node._store.validated = true;
    }
  } else if (node) {
    var iteratorFn = getIteratorFn(node);

    if (typeof iteratorFn === 'function') {
      // Entry iterators used to provide implicit keys,
      // but now we print a separate warning for them later.
      if (iteratorFn !== node.entries) {
        var iterator = iteratorFn.call(node);
        var step;

        while (!(step = iterator.next()).done) {
          if (isValidElement(step.value)) {
            validateExplicitKey(step.value, parentType);
          }
        }
      }
    }
  }
}
/**
 * Given an element, validate that its props follow the propTypes definition,
 * provided by the type.
 *
 * @param {ReactElement} element
 */


function validatePropTypes(element) {
  {
    var type = element.type;

    if (type === null || type === undefined || typeof type === 'string') {
      return;
    }

    var name = getComponentName(type);
    var propTypes;

    if (typeof type === 'function') {
      propTypes = type.propTypes;
    } else if (typeof type === 'object' && (type.$$typeof === REACT_FORWARD_REF_TYPE || // Note: Memo only checks outer props here.
    // Inner props are checked in the reconciler.
    type.$$typeof === REACT_MEMO_TYPE)) {
      propTypes = type.propTypes;
    } else {
      return;
    }

    if (propTypes) {
      setCurrentlyValidatingElement(element);
      checkPropTypes(propTypes, element.props, 'prop', name, ReactDebugCurrentFrame.getStackAddendum);
      setCurrentlyValidatingElement(null);
    } else if (type.PropTypes !== undefined && !propTypesMisspellWarningShown) {
      propTypesMisspellWarningShown = true;

      error('Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', name || 'Unknown');
    }

    if (typeof type.getDefaultProps === 'function' && !type.getDefaultProps.isReactClassApproved) {
      error('getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.');
    }
  }
}
/**
 * Given a fragment, validate that it can only be provided with fragment props
 * @param {ReactElement} fragment
 */


function validateFragmentProps(fragment) {
  {
    setCurrentlyValidatingElement(fragment);
    var keys = Object.keys(fragment.props);

    for (var i = 0; i < keys.length; i++) {
      var key = keys[i];

      if (key !== 'children' && key !== 'key') {
        error('Invalid prop `%s` supplied to `React.Fragment`. ' + 'React.Fragment can only have `key` and `children` props.', key);

        break;
      }
    }

    if (fragment.ref !== null) {
      error('Invalid attribute `ref` supplied to `React.Fragment`.');
    }

    setCurrentlyValidatingElement(null);
  }
}
function createElementWithValidation(type, props, children) {
  var validType = isValidElementType(type); // We warn in this case but don't throw. We expect the element creation to
  // succeed and there will likely be errors in render.

  if (!validType) {
    var info = '';

    if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
      info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
    }

    var sourceInfo = getSourceInfoErrorAddendumForProps(props);

    if (sourceInfo) {
      info += sourceInfo;
    } else {
      info += getDeclarationErrorAddendum();
    }

    var typeString;

    if (type === null) {
      typeString = 'null';
    } else if (Array.isArray(type)) {
      typeString = 'array';
    } else if (type !== undefined && type.$$typeof === REACT_ELEMENT_TYPE) {
      typeString = "<" + (getComponentName(type.type) || 'Unknown') + " />";
      info = ' Did you accidentally export a JSX literal instead of a component?';
    } else {
      typeString = typeof type;
    }

    {
      error('React.createElement: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', typeString, info);
    }
  }

  var element = createElement.apply(this, arguments); // The result can be nullish if a mock or a custom function is used.
  // TODO: Drop this when these are no longer allowed as the type argument.

  if (element == null) {
    return element;
  } // Skip key warning if the type isn't valid since our key validation logic
  // doesn't expect a non-string/function type and can throw confusing errors.
  // We don't want exception behavior to differ between dev and prod.
  // (Rendering will throw with a helpful message and as soon as the type is
  // fixed, the key warnings will appear.)


  if (validType) {
    for (var i = 2; i < arguments.length; i++) {
      validateChildKeys(arguments[i], type);
    }
  }

  if (type === REACT_FRAGMENT_TYPE) {
    validateFragmentProps(element);
  } else {
    validatePropTypes(element);
  }

  return element;
}
var didWarnAboutDeprecatedCreateFactory = false;
function createFactoryWithValidation(type) {
  var validatedFactory = createElementWithValidation.bind(null, type);
  validatedFactory.type = type;

  {
    if (!didWarnAboutDeprecatedCreateFactory) {
      didWarnAboutDeprecatedCreateFactory = true;

      warn('React.createFactory() is deprecated and will be removed in ' + 'a future major release. Consider using JSX ' + 'or use React.createElement() directly instead.');
    } // Legacy hook: remove it


    Object.defineProperty(validatedFactory, 'type', {
      enumerable: false,
      get: function () {
        warn('Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.');

        Object.defineProperty(this, 'type', {
          value: type
        });
        return type;
      }
    });
  }

  return validatedFactory;
}
function cloneElementWithValidation(element, props, children) {
  var newElement = cloneElement.apply(this, arguments);

  for (var i = 2; i < arguments.length; i++) {
    validateChildKeys(arguments[i], newElement.type);
  }

  validatePropTypes(newElement);
  return newElement;
}

{

  try {
    var frozenObject = Object.freeze({});
    var testMap = new Map([[frozenObject, null]]);
    var testSet = new Set([frozenObject]); // This is necessary for Rollup to not consider these unused.
    // https://github.com/rollup/rollup/issues/1771
    // TODO: we can remove these if Rollup fixes the bug.

    testMap.set(0, 0);
    testSet.add(0);
  } catch (e) {
  }
}

var createElement$1 =  createElementWithValidation ;
var cloneElement$1 =  cloneElementWithValidation ;
var createFactory =  createFactoryWithValidation ;
var Children = {
  map: mapChildren,
  forEach: forEachChildren,
  count: countChildren,
  toArray: toArray,
  only: onlyChild
};

exports.Children = Children;
exports.Component = Component;
exports.Fragment = REACT_FRAGMENT_TYPE;
exports.Profiler = REACT_PROFILER_TYPE;
exports.PureComponent = PureComponent;
exports.StrictMode = REACT_STRICT_MODE_TYPE;
exports.Suspense = REACT_SUSPENSE_TYPE;
exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactSharedInternals;
exports.cloneElement = cloneElement$1;
exports.createContext = createContext;
exports.createElement = createElement$1;
exports.createFactory = createFactory;
exports.createRef = createRef;
exports.forwardRef = forwardRef;
exports.isValidElement = isValidElement;
exports.lazy = lazy;
exports.memo = memo;
exports.useCallback = useCallback;
exports.useContext = useContext;
exports.useDebugValue = useDebugValue;
exports.useEffect = useEffect;
exports.useImperativeHandle = useImperativeHandle;
exports.useLayoutEffect = useLayoutEffect;
exports.useMemo = useMemo;
exports.useReducer = useReducer;
exports.useRef = useRef;
exports.useState = useState;
exports.version = ReactVersion;
  })();
}


/***/ }),

/***/ "./node_modules/react/index.js":
/*!*************************************!*\
  !*** ./node_modules/react/index.js ***!
  \*************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


if (false) {} else {
  module.exports = __webpack_require__(/*! ./cjs/react.development.js */ "./node_modules/react/cjs/react.development.js");
}


/***/ }),

/***/ "./node_modules/redux/es/redux.js":
/*!****************************************!*\
  !*** ./node_modules/redux/es/redux.js ***!
  \****************************************/
/*! exports provided: __DO_NOT_USE__ActionTypes, applyMiddleware, bindActionCreators, combineReducers, compose, createStore */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "__DO_NOT_USE__ActionTypes", function() { return ActionTypes; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "applyMiddleware", function() { return applyMiddleware; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "bindActionCreators", function() { return bindActionCreators; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "combineReducers", function() { return combineReducers; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "compose", function() { return compose; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createStore", function() { return createStore; });
/* harmony import */ var symbol_observable__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! symbol-observable */ "./node_modules/symbol-observable/es/index.js");


/**
 * These are private action types reserved by Redux.
 * For any unknown actions, you must return the current state.
 * If the current state is undefined, you must return the initial state.
 * Do not reference these action types directly in your code.
 */
var randomString = function randomString() {
  return Math.random().toString(36).substring(7).split('').join('.');
};

var ActionTypes = {
  INIT: "@@redux/INIT" + randomString(),
  REPLACE: "@@redux/REPLACE" + randomString(),
  PROBE_UNKNOWN_ACTION: function PROBE_UNKNOWN_ACTION() {
    return "@@redux/PROBE_UNKNOWN_ACTION" + randomString();
  }
};

/**
 * @param {any} obj The object to inspect.
 * @returns {boolean} True if the argument appears to be a plain object.
 */
function isPlainObject(obj) {
  if (typeof obj !== 'object' || obj === null) return false;
  var proto = obj;

  while (Object.getPrototypeOf(proto) !== null) {
    proto = Object.getPrototypeOf(proto);
  }

  return Object.getPrototypeOf(obj) === proto;
}

/**
 * Creates a Redux store that holds the state tree.
 * The only way to change the data in the store is to call `dispatch()` on it.
 *
 * There should only be a single store in your app. To specify how different
 * parts of the state tree respond to actions, you may combine several reducers
 * into a single reducer function by using `combineReducers`.
 *
 * @param {Function} reducer A function that returns the next state tree, given
 * the current state tree and the action to handle.
 *
 * @param {any} [preloadedState] The initial state. You may optionally specify it
 * to hydrate the state from the server in universal apps, or to restore a
 * previously serialized user session.
 * If you use `combineReducers` to produce the root reducer function, this must be
 * an object with the same shape as `combineReducers` keys.
 *
 * @param {Function} [enhancer] The store enhancer. You may optionally specify it
 * to enhance the store with third-party capabilities such as middleware,
 * time travel, persistence, etc. The only store enhancer that ships with Redux
 * is `applyMiddleware()`.
 *
 * @returns {Store} A Redux store that lets you read the state, dispatch actions
 * and subscribe to changes.
 */

function createStore(reducer, preloadedState, enhancer) {
  var _ref2;

  if (typeof preloadedState === 'function' && typeof enhancer === 'function' || typeof enhancer === 'function' && typeof arguments[3] === 'function') {
    throw new Error('It looks like you are passing several store enhancers to ' + 'createStore(). This is not supported. Instead, compose them ' + 'together to a single function.');
  }

  if (typeof preloadedState === 'function' && typeof enhancer === 'undefined') {
    enhancer = preloadedState;
    preloadedState = undefined;
  }

  if (typeof enhancer !== 'undefined') {
    if (typeof enhancer !== 'function') {
      throw new Error('Expected the enhancer to be a function.');
    }

    return enhancer(createStore)(reducer, preloadedState);
  }

  if (typeof reducer !== 'function') {
    throw new Error('Expected the reducer to be a function.');
  }

  var currentReducer = reducer;
  var currentState = preloadedState;
  var currentListeners = [];
  var nextListeners = currentListeners;
  var isDispatching = false;
  /**
   * This makes a shallow copy of currentListeners so we can use
   * nextListeners as a temporary list while dispatching.
   *
   * This prevents any bugs around consumers calling
   * subscribe/unsubscribe in the middle of a dispatch.
   */

  function ensureCanMutateNextListeners() {
    if (nextListeners === currentListeners) {
      nextListeners = currentListeners.slice();
    }
  }
  /**
   * Reads the state tree managed by the store.
   *
   * @returns {any} The current state tree of your application.
   */


  function getState() {
    if (isDispatching) {
      throw new Error('You may not call store.getState() while the reducer is executing. ' + 'The reducer has already received the state as an argument. ' + 'Pass it down from the top reducer instead of reading it from the store.');
    }

    return currentState;
  }
  /**
   * Adds a change listener. It will be called any time an action is dispatched,
   * and some part of the state tree may potentially have changed. You may then
   * call `getState()` to read the current state tree inside the callback.
   *
   * You may call `dispatch()` from a change listener, with the following
   * caveats:
   *
   * 1. The subscriptions are snapshotted just before every `dispatch()` call.
   * If you subscribe or unsubscribe while the listeners are being invoked, this
   * will not have any effect on the `dispatch()` that is currently in progress.
   * However, the next `dispatch()` call, whether nested or not, will use a more
   * recent snapshot of the subscription list.
   *
   * 2. The listener should not expect to see all state changes, as the state
   * might have been updated multiple times during a nested `dispatch()` before
   * the listener is called. It is, however, guaranteed that all subscribers
   * registered before the `dispatch()` started will be called with the latest
   * state by the time it exits.
   *
   * @param {Function} listener A callback to be invoked on every dispatch.
   * @returns {Function} A function to remove this change listener.
   */


  function subscribe(listener) {
    if (typeof listener !== 'function') {
      throw new Error('Expected the listener to be a function.');
    }

    if (isDispatching) {
      throw new Error('You may not call store.subscribe() while the reducer is executing. ' + 'If you would like to be notified after the store has been updated, subscribe from a ' + 'component and invoke store.getState() in the callback to access the latest state. ' + 'See https://redux.js.org/api-reference/store#subscribelistener for more details.');
    }

    var isSubscribed = true;
    ensureCanMutateNextListeners();
    nextListeners.push(listener);
    return function unsubscribe() {
      if (!isSubscribed) {
        return;
      }

      if (isDispatching) {
        throw new Error('You may not unsubscribe from a store listener while the reducer is executing. ' + 'See https://redux.js.org/api-reference/store#subscribelistener for more details.');
      }

      isSubscribed = false;
      ensureCanMutateNextListeners();
      var index = nextListeners.indexOf(listener);
      nextListeners.splice(index, 1);
      currentListeners = null;
    };
  }
  /**
   * Dispatches an action. It is the only way to trigger a state change.
   *
   * The `reducer` function, used to create the store, will be called with the
   * current state tree and the given `action`. Its return value will
   * be considered the **next** state of the tree, and the change listeners
   * will be notified.
   *
   * The base implementation only supports plain object actions. If you want to
   * dispatch a Promise, an Observable, a thunk, or something else, you need to
   * wrap your store creating function into the corresponding middleware. For
   * example, see the documentation for the `redux-thunk` package. Even the
   * middleware will eventually dispatch plain object actions using this method.
   *
   * @param {Object} action A plain object representing “what changed”. It is
   * a good idea to keep actions serializable so you can record and replay user
   * sessions, or use the time travelling `redux-devtools`. An action must have
   * a `type` property which may not be `undefined`. It is a good idea to use
   * string constants for action types.
   *
   * @returns {Object} For convenience, the same action object you dispatched.
   *
   * Note that, if you use a custom middleware, it may wrap `dispatch()` to
   * return something else (for example, a Promise you can await).
   */


  function dispatch(action) {
    if (!isPlainObject(action)) {
      throw new Error('Actions must be plain objects. ' + 'Use custom middleware for async actions.');
    }

    if (typeof action.type === 'undefined') {
      throw new Error('Actions may not have an undefined "type" property. ' + 'Have you misspelled a constant?');
    }

    if (isDispatching) {
      throw new Error('Reducers may not dispatch actions.');
    }

    try {
      isDispatching = true;
      currentState = currentReducer(currentState, action);
    } finally {
      isDispatching = false;
    }

    var listeners = currentListeners = nextListeners;

    for (var i = 0; i < listeners.length; i++) {
      var listener = listeners[i];
      listener();
    }

    return action;
  }
  /**
   * Replaces the reducer currently used by the store to calculate the state.
   *
   * You might need this if your app implements code splitting and you want to
   * load some of the reducers dynamically. You might also need this if you
   * implement a hot reloading mechanism for Redux.
   *
   * @param {Function} nextReducer The reducer for the store to use instead.
   * @returns {void}
   */


  function replaceReducer(nextReducer) {
    if (typeof nextReducer !== 'function') {
      throw new Error('Expected the nextReducer to be a function.');
    }

    currentReducer = nextReducer; // This action has a similiar effect to ActionTypes.INIT.
    // Any reducers that existed in both the new and old rootReducer
    // will receive the previous state. This effectively populates
    // the new state tree with any relevant data from the old one.

    dispatch({
      type: ActionTypes.REPLACE
    });
  }
  /**
   * Interoperability point for observable/reactive libraries.
   * @returns {observable} A minimal observable of state changes.
   * For more information, see the observable proposal:
   * https://github.com/tc39/proposal-observable
   */


  function observable() {
    var _ref;

    var outerSubscribe = subscribe;
    return _ref = {
      /**
       * The minimal observable subscription method.
       * @param {Object} observer Any object that can be used as an observer.
       * The observer object should have a `next` method.
       * @returns {subscription} An object with an `unsubscribe` method that can
       * be used to unsubscribe the observable from the store, and prevent further
       * emission of values from the observable.
       */
      subscribe: function subscribe(observer) {
        if (typeof observer !== 'object' || observer === null) {
          throw new TypeError('Expected the observer to be an object.');
        }

        function observeState() {
          if (observer.next) {
            observer.next(getState());
          }
        }

        observeState();
        var unsubscribe = outerSubscribe(observeState);
        return {
          unsubscribe: unsubscribe
        };
      }
    }, _ref[symbol_observable__WEBPACK_IMPORTED_MODULE_0__["default"]] = function () {
      return this;
    }, _ref;
  } // When a store is created, an "INIT" action is dispatched so that every
  // reducer returns their initial state. This effectively populates
  // the initial state tree.


  dispatch({
    type: ActionTypes.INIT
  });
  return _ref2 = {
    dispatch: dispatch,
    subscribe: subscribe,
    getState: getState,
    replaceReducer: replaceReducer
  }, _ref2[symbol_observable__WEBPACK_IMPORTED_MODULE_0__["default"]] = observable, _ref2;
}

/**
 * Prints a warning in the console if it exists.
 *
 * @param {String} message The warning message.
 * @returns {void}
 */
function warning(message) {
  /* eslint-disable no-console */
  if (typeof console !== 'undefined' && typeof console.error === 'function') {
    console.error(message);
  }
  /* eslint-enable no-console */


  try {
    // This error was thrown as a convenience so that if you enable
    // "break on all exceptions" in your console,
    // it would pause the execution at this line.
    throw new Error(message);
  } catch (e) {} // eslint-disable-line no-empty

}

function getUndefinedStateErrorMessage(key, action) {
  var actionType = action && action.type;
  var actionDescription = actionType && "action \"" + String(actionType) + "\"" || 'an action';
  return "Given " + actionDescription + ", reducer \"" + key + "\" returned undefined. " + "To ignore an action, you must explicitly return the previous state. " + "If you want this reducer to hold no value, you can return null instead of undefined.";
}

function getUnexpectedStateShapeWarningMessage(inputState, reducers, action, unexpectedKeyCache) {
  var reducerKeys = Object.keys(reducers);
  var argumentName = action && action.type === ActionTypes.INIT ? 'preloadedState argument passed to createStore' : 'previous state received by the reducer';

  if (reducerKeys.length === 0) {
    return 'Store does not have a valid reducer. Make sure the argument passed ' + 'to combineReducers is an object whose values are reducers.';
  }

  if (!isPlainObject(inputState)) {
    return "The " + argumentName + " has unexpected type of \"" + {}.toString.call(inputState).match(/\s([a-z|A-Z]+)/)[1] + "\". Expected argument to be an object with the following " + ("keys: \"" + reducerKeys.join('", "') + "\"");
  }

  var unexpectedKeys = Object.keys(inputState).filter(function (key) {
    return !reducers.hasOwnProperty(key) && !unexpectedKeyCache[key];
  });
  unexpectedKeys.forEach(function (key) {
    unexpectedKeyCache[key] = true;
  });
  if (action && action.type === ActionTypes.REPLACE) return;

  if (unexpectedKeys.length > 0) {
    return "Unexpected " + (unexpectedKeys.length > 1 ? 'keys' : 'key') + " " + ("\"" + unexpectedKeys.join('", "') + "\" found in " + argumentName + ". ") + "Expected to find one of the known reducer keys instead: " + ("\"" + reducerKeys.join('", "') + "\". Unexpected keys will be ignored.");
  }
}

function assertReducerShape(reducers) {
  Object.keys(reducers).forEach(function (key) {
    var reducer = reducers[key];
    var initialState = reducer(undefined, {
      type: ActionTypes.INIT
    });

    if (typeof initialState === 'undefined') {
      throw new Error("Reducer \"" + key + "\" returned undefined during initialization. " + "If the state passed to the reducer is undefined, you must " + "explicitly return the initial state. The initial state may " + "not be undefined. If you don't want to set a value for this reducer, " + "you can use null instead of undefined.");
    }

    if (typeof reducer(undefined, {
      type: ActionTypes.PROBE_UNKNOWN_ACTION()
    }) === 'undefined') {
      throw new Error("Reducer \"" + key + "\" returned undefined when probed with a random type. " + ("Don't try to handle " + ActionTypes.INIT + " or other actions in \"redux/*\" ") + "namespace. They are considered private. Instead, you must return the " + "current state for any unknown actions, unless it is undefined, " + "in which case you must return the initial state, regardless of the " + "action type. The initial state may not be undefined, but can be null.");
    }
  });
}
/**
 * Turns an object whose values are different reducer functions, into a single
 * reducer function. It will call every child reducer, and gather their results
 * into a single state object, whose keys correspond to the keys of the passed
 * reducer functions.
 *
 * @param {Object} reducers An object whose values correspond to different
 * reducer functions that need to be combined into one. One handy way to obtain
 * it is to use ES6 `import * as reducers` syntax. The reducers may never return
 * undefined for any action. Instead, they should return their initial state
 * if the state passed to them was undefined, and the current state for any
 * unrecognized action.
 *
 * @returns {Function} A reducer function that invokes every reducer inside the
 * passed object, and builds a state object with the same shape.
 */


function combineReducers(reducers) {
  var reducerKeys = Object.keys(reducers);
  var finalReducers = {};

  for (var i = 0; i < reducerKeys.length; i++) {
    var key = reducerKeys[i];

    if (true) {
      if (typeof reducers[key] === 'undefined') {
        warning("No reducer provided for key \"" + key + "\"");
      }
    }

    if (typeof reducers[key] === 'function') {
      finalReducers[key] = reducers[key];
    }
  }

  var finalReducerKeys = Object.keys(finalReducers); // This is used to make sure we don't warn about the same
  // keys multiple times.

  var unexpectedKeyCache;

  if (true) {
    unexpectedKeyCache = {};
  }

  var shapeAssertionError;

  try {
    assertReducerShape(finalReducers);
  } catch (e) {
    shapeAssertionError = e;
  }

  return function combination(state, action) {
    if (state === void 0) {
      state = {};
    }

    if (shapeAssertionError) {
      throw shapeAssertionError;
    }

    if (true) {
      var warningMessage = getUnexpectedStateShapeWarningMessage(state, finalReducers, action, unexpectedKeyCache);

      if (warningMessage) {
        warning(warningMessage);
      }
    }

    var hasChanged = false;
    var nextState = {};

    for (var _i = 0; _i < finalReducerKeys.length; _i++) {
      var _key = finalReducerKeys[_i];
      var reducer = finalReducers[_key];
      var previousStateForKey = state[_key];
      var nextStateForKey = reducer(previousStateForKey, action);

      if (typeof nextStateForKey === 'undefined') {
        var errorMessage = getUndefinedStateErrorMessage(_key, action);
        throw new Error(errorMessage);
      }

      nextState[_key] = nextStateForKey;
      hasChanged = hasChanged || nextStateForKey !== previousStateForKey;
    }

    hasChanged = hasChanged || finalReducerKeys.length !== Object.keys(state).length;
    return hasChanged ? nextState : state;
  };
}

function bindActionCreator(actionCreator, dispatch) {
  return function () {
    return dispatch(actionCreator.apply(this, arguments));
  };
}
/**
 * Turns an object whose values are action creators, into an object with the
 * same keys, but with every function wrapped into a `dispatch` call so they
 * may be invoked directly. This is just a convenience method, as you can call
 * `store.dispatch(MyActionCreators.doSomething())` yourself just fine.
 *
 * For convenience, you can also pass an action creator as the first argument,
 * and get a dispatch wrapped function in return.
 *
 * @param {Function|Object} actionCreators An object whose values are action
 * creator functions. One handy way to obtain it is to use ES6 `import * as`
 * syntax. You may also pass a single function.
 *
 * @param {Function} dispatch The `dispatch` function available on your Redux
 * store.
 *
 * @returns {Function|Object} The object mimicking the original object, but with
 * every action creator wrapped into the `dispatch` call. If you passed a
 * function as `actionCreators`, the return value will also be a single
 * function.
 */


function bindActionCreators(actionCreators, dispatch) {
  if (typeof actionCreators === 'function') {
    return bindActionCreator(actionCreators, dispatch);
  }

  if (typeof actionCreators !== 'object' || actionCreators === null) {
    throw new Error("bindActionCreators expected an object or a function, instead received " + (actionCreators === null ? 'null' : typeof actionCreators) + ". " + "Did you write \"import ActionCreators from\" instead of \"import * as ActionCreators from\"?");
  }

  var boundActionCreators = {};

  for (var key in actionCreators) {
    var actionCreator = actionCreators[key];

    if (typeof actionCreator === 'function') {
      boundActionCreators[key] = bindActionCreator(actionCreator, dispatch);
    }
  }

  return boundActionCreators;
}

function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }

  return obj;
}

function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);

  if (Object.getOwnPropertySymbols) {
    keys.push.apply(keys, Object.getOwnPropertySymbols(object));
  }

  if (enumerableOnly) keys = keys.filter(function (sym) {
    return Object.getOwnPropertyDescriptor(object, sym).enumerable;
  });
  return keys;
}

function _objectSpread2(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};

    if (i % 2) {
      ownKeys(source, true).forEach(function (key) {
        _defineProperty(target, key, source[key]);
      });
    } else if (Object.getOwnPropertyDescriptors) {
      Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
      ownKeys(source).forEach(function (key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
  }

  return target;
}

/**
 * Composes single-argument functions from right to left. The rightmost
 * function can take multiple arguments as it provides the signature for
 * the resulting composite function.
 *
 * @param {...Function} funcs The functions to compose.
 * @returns {Function} A function obtained by composing the argument functions
 * from right to left. For example, compose(f, g, h) is identical to doing
 * (...args) => f(g(h(...args))).
 */
function compose() {
  for (var _len = arguments.length, funcs = new Array(_len), _key = 0; _key < _len; _key++) {
    funcs[_key] = arguments[_key];
  }

  if (funcs.length === 0) {
    return function (arg) {
      return arg;
    };
  }

  if (funcs.length === 1) {
    return funcs[0];
  }

  return funcs.reduce(function (a, b) {
    return function () {
      return a(b.apply(void 0, arguments));
    };
  });
}

/**
 * Creates a store enhancer that applies middleware to the dispatch method
 * of the Redux store. This is handy for a variety of tasks, such as expressing
 * asynchronous actions in a concise manner, or logging every action payload.
 *
 * See `redux-thunk` package as an example of the Redux middleware.
 *
 * Because middleware is potentially asynchronous, this should be the first
 * store enhancer in the composition chain.
 *
 * Note that each middleware will be given the `dispatch` and `getState` functions
 * as named arguments.
 *
 * @param {...Function} middlewares The middleware chain to be applied.
 * @returns {Function} A store enhancer applying the middleware.
 */

function applyMiddleware() {
  for (var _len = arguments.length, middlewares = new Array(_len), _key = 0; _key < _len; _key++) {
    middlewares[_key] = arguments[_key];
  }

  return function (createStore) {
    return function () {
      var store = createStore.apply(void 0, arguments);

      var _dispatch = function dispatch() {
        throw new Error('Dispatching while constructing your middleware is not allowed. ' + 'Other middleware would not be applied to this dispatch.');
      };

      var middlewareAPI = {
        getState: store.getState,
        dispatch: function dispatch() {
          return _dispatch.apply(void 0, arguments);
        }
      };
      var chain = middlewares.map(function (middleware) {
        return middleware(middlewareAPI);
      });
      _dispatch = compose.apply(void 0, chain)(store.dispatch);
      return _objectSpread2({}, store, {
        dispatch: _dispatch
      });
    };
  };
}

/*
 * This is a dummy function to check if the function name has been altered by minification.
 * If the function has been minified and NODE_ENV !== 'production', warn the user.
 */

function isCrushed() {}

if ( true && typeof isCrushed.name === 'string' && isCrushed.name !== 'isCrushed') {
  warning('You are currently using minified code outside of NODE_ENV === "production". ' + 'This means that you are running a slower development build of Redux. ' + 'You can use loose-envify (https://github.com/zertosh/loose-envify) for browserify ' + 'or setting mode to production in webpack (https://webpack.js.org/concepts/mode/) ' + 'to ensure you have the correct code for your production build.');
}




/***/ }),

/***/ "./node_modules/regenerator-runtime/runtime.js":
/*!*****************************************************!*\
  !*** ./node_modules/regenerator-runtime/runtime.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

var runtime = (function (exports) {
  "use strict";

  var Op = Object.prototype;
  var hasOwn = Op.hasOwnProperty;
  var undefined; // More compressible than void 0.
  var $Symbol = typeof Symbol === "function" ? Symbol : {};
  var iteratorSymbol = $Symbol.iterator || "@@iterator";
  var asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator";
  var toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag";

  function wrap(innerFn, outerFn, self, tryLocsList) {
    // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.
    var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;
    var generator = Object.create(protoGenerator.prototype);
    var context = new Context(tryLocsList || []);

    // The ._invoke method unifies the implementations of the .next,
    // .throw, and .return methods.
    generator._invoke = makeInvokeMethod(innerFn, self, context);

    return generator;
  }
  exports.wrap = wrap;

  // Try/catch helper to minimize deoptimizations. Returns a completion
  // record like context.tryEntries[i].completion. This interface could
  // have been (and was previously) designed to take a closure to be
  // invoked without arguments, but in all the cases we care about we
  // already have an existing method we want to call, so there's no need
  // to create a new function object. We can even get away with assuming
  // the method takes exactly one argument, since that happens to be true
  // in every case, so we don't have to touch the arguments object. The
  // only additional allocation required is the completion record, which
  // has a stable shape and so hopefully should be cheap to allocate.
  function tryCatch(fn, obj, arg) {
    try {
      return { type: "normal", arg: fn.call(obj, arg) };
    } catch (err) {
      return { type: "throw", arg: err };
    }
  }

  var GenStateSuspendedStart = "suspendedStart";
  var GenStateSuspendedYield = "suspendedYield";
  var GenStateExecuting = "executing";
  var GenStateCompleted = "completed";

  // Returning this object from the innerFn has the same effect as
  // breaking out of the dispatch switch statement.
  var ContinueSentinel = {};

  // Dummy constructor functions that we use as the .constructor and
  // .constructor.prototype properties for functions that return Generator
  // objects. For full spec compliance, you may wish to configure your
  // minifier not to mangle the names of these two functions.
  function Generator() {}
  function GeneratorFunction() {}
  function GeneratorFunctionPrototype() {}

  // This is a polyfill for %IteratorPrototype% for environments that
  // don't natively support it.
  var IteratorPrototype = {};
  IteratorPrototype[iteratorSymbol] = function () {
    return this;
  };

  var getProto = Object.getPrototypeOf;
  var NativeIteratorPrototype = getProto && getProto(getProto(values([])));
  if (NativeIteratorPrototype &&
      NativeIteratorPrototype !== Op &&
      hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) {
    // This environment has a native %IteratorPrototype%; use it instead
    // of the polyfill.
    IteratorPrototype = NativeIteratorPrototype;
  }

  var Gp = GeneratorFunctionPrototype.prototype =
    Generator.prototype = Object.create(IteratorPrototype);
  GeneratorFunction.prototype = Gp.constructor = GeneratorFunctionPrototype;
  GeneratorFunctionPrototype.constructor = GeneratorFunction;
  GeneratorFunctionPrototype[toStringTagSymbol] =
    GeneratorFunction.displayName = "GeneratorFunction";

  // Helper for defining the .next, .throw, and .return methods of the
  // Iterator interface in terms of a single ._invoke method.
  function defineIteratorMethods(prototype) {
    ["next", "throw", "return"].forEach(function(method) {
      prototype[method] = function(arg) {
        return this._invoke(method, arg);
      };
    });
  }

  exports.isGeneratorFunction = function(genFun) {
    var ctor = typeof genFun === "function" && genFun.constructor;
    return ctor
      ? ctor === GeneratorFunction ||
        // For the native GeneratorFunction constructor, the best we can
        // do is to check its .name property.
        (ctor.displayName || ctor.name) === "GeneratorFunction"
      : false;
  };

  exports.mark = function(genFun) {
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);
    } else {
      genFun.__proto__ = GeneratorFunctionPrototype;
      if (!(toStringTagSymbol in genFun)) {
        genFun[toStringTagSymbol] = "GeneratorFunction";
      }
    }
    genFun.prototype = Object.create(Gp);
    return genFun;
  };

  // Within the body of any async function, `await x` is transformed to
  // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test
  // `hasOwn.call(value, "__await")` to determine if the yielded value is
  // meant to be awaited.
  exports.awrap = function(arg) {
    return { __await: arg };
  };

  function AsyncIterator(generator, PromiseImpl) {
    function invoke(method, arg, resolve, reject) {
      var record = tryCatch(generator[method], generator, arg);
      if (record.type === "throw") {
        reject(record.arg);
      } else {
        var result = record.arg;
        var value = result.value;
        if (value &&
            typeof value === "object" &&
            hasOwn.call(value, "__await")) {
          return PromiseImpl.resolve(value.__await).then(function(value) {
            invoke("next", value, resolve, reject);
          }, function(err) {
            invoke("throw", err, resolve, reject);
          });
        }

        return PromiseImpl.resolve(value).then(function(unwrapped) {
          // When a yielded Promise is resolved, its final value becomes
          // the .value of the Promise<{value,done}> result for the
          // current iteration.
          result.value = unwrapped;
          resolve(result);
        }, function(error) {
          // If a rejected Promise was yielded, throw the rejection back
          // into the async generator function so it can be handled there.
          return invoke("throw", error, resolve, reject);
        });
      }
    }

    var previousPromise;

    function enqueue(method, arg) {
      function callInvokeWithMethodAndArg() {
        return new PromiseImpl(function(resolve, reject) {
          invoke(method, arg, resolve, reject);
        });
      }

      return previousPromise =
        // If enqueue has been called before, then we want to wait until
        // all previous Promises have been resolved before calling invoke,
        // so that results are always delivered in the correct order. If
        // enqueue has not been called before, then it is important to
        // call invoke immediately, without waiting on a callback to fire,
        // so that the async generator function has the opportunity to do
        // any necessary setup in a predictable way. This predictability
        // is why the Promise constructor synchronously invokes its
        // executor callback, and why async functions synchronously
        // execute code before the first await. Since we implement simple
        // async functions in terms of async generators, it is especially
        // important to get this right, even though it requires care.
        previousPromise ? previousPromise.then(
          callInvokeWithMethodAndArg,
          // Avoid propagating failures to Promises returned by later
          // invocations of the iterator.
          callInvokeWithMethodAndArg
        ) : callInvokeWithMethodAndArg();
    }

    // Define the unified helper method that is used to implement .next,
    // .throw, and .return (see defineIteratorMethods).
    this._invoke = enqueue;
  }

  defineIteratorMethods(AsyncIterator.prototype);
  AsyncIterator.prototype[asyncIteratorSymbol] = function () {
    return this;
  };
  exports.AsyncIterator = AsyncIterator;

  // Note that simple async functions are implemented on top of
  // AsyncIterator objects; they just return a Promise for the value of
  // the final result produced by the iterator.
  exports.async = function(innerFn, outerFn, self, tryLocsList, PromiseImpl) {
    if (PromiseImpl === void 0) PromiseImpl = Promise;

    var iter = new AsyncIterator(
      wrap(innerFn, outerFn, self, tryLocsList),
      PromiseImpl
    );

    return exports.isGeneratorFunction(outerFn)
      ? iter // If outerFn is a generator, return the full iterator.
      : iter.next().then(function(result) {
          return result.done ? result.value : iter.next();
        });
  };

  function makeInvokeMethod(innerFn, self, context) {
    var state = GenStateSuspendedStart;

    return function invoke(method, arg) {
      if (state === GenStateExecuting) {
        throw new Error("Generator is already running");
      }

      if (state === GenStateCompleted) {
        if (method === "throw") {
          throw arg;
        }

        // Be forgiving, per 25.3.3.3.3 of the spec:
        // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume
        return doneResult();
      }

      context.method = method;
      context.arg = arg;

      while (true) {
        var delegate = context.delegate;
        if (delegate) {
          var delegateResult = maybeInvokeDelegate(delegate, context);
          if (delegateResult) {
            if (delegateResult === ContinueSentinel) continue;
            return delegateResult;
          }
        }

        if (context.method === "next") {
          // Setting context._sent for legacy support of Babel's
          // function.sent implementation.
          context.sent = context._sent = context.arg;

        } else if (context.method === "throw") {
          if (state === GenStateSuspendedStart) {
            state = GenStateCompleted;
            throw context.arg;
          }

          context.dispatchException(context.arg);

        } else if (context.method === "return") {
          context.abrupt("return", context.arg);
        }

        state = GenStateExecuting;

        var record = tryCatch(innerFn, self, context);
        if (record.type === "normal") {
          // If an exception is thrown from innerFn, we leave state ===
          // GenStateExecuting and loop back for another invocation.
          state = context.done
            ? GenStateCompleted
            : GenStateSuspendedYield;

          if (record.arg === ContinueSentinel) {
            continue;
          }

          return {
            value: record.arg,
            done: context.done
          };

        } else if (record.type === "throw") {
          state = GenStateCompleted;
          // Dispatch the exception by looping back around to the
          // context.dispatchException(context.arg) call above.
          context.method = "throw";
          context.arg = record.arg;
        }
      }
    };
  }

  // Call delegate.iterator[context.method](context.arg) and handle the
  // result, either by returning a { value, done } result from the
  // delegate iterator, or by modifying context.method and context.arg,
  // setting context.delegate to null, and returning the ContinueSentinel.
  function maybeInvokeDelegate(delegate, context) {
    var method = delegate.iterator[context.method];
    if (method === undefined) {
      // A .throw or .return when the delegate iterator has no .throw
      // method always terminates the yield* loop.
      context.delegate = null;

      if (context.method === "throw") {
        // Note: ["return"] must be used for ES3 parsing compatibility.
        if (delegate.iterator["return"]) {
          // If the delegate iterator has a return method, give it a
          // chance to clean up.
          context.method = "return";
          context.arg = undefined;
          maybeInvokeDelegate(delegate, context);

          if (context.method === "throw") {
            // If maybeInvokeDelegate(context) changed context.method from
            // "return" to "throw", let that override the TypeError below.
            return ContinueSentinel;
          }
        }

        context.method = "throw";
        context.arg = new TypeError(
          "The iterator does not provide a 'throw' method");
      }

      return ContinueSentinel;
    }

    var record = tryCatch(method, delegate.iterator, context.arg);

    if (record.type === "throw") {
      context.method = "throw";
      context.arg = record.arg;
      context.delegate = null;
      return ContinueSentinel;
    }

    var info = record.arg;

    if (! info) {
      context.method = "throw";
      context.arg = new TypeError("iterator result is not an object");
      context.delegate = null;
      return ContinueSentinel;
    }

    if (info.done) {
      // Assign the result of the finished delegate to the temporary
      // variable specified by delegate.resultName (see delegateYield).
      context[delegate.resultName] = info.value;

      // Resume execution at the desired location (see delegateYield).
      context.next = delegate.nextLoc;

      // If context.method was "throw" but the delegate handled the
      // exception, let the outer generator proceed normally. If
      // context.method was "next", forget context.arg since it has been
      // "consumed" by the delegate iterator. If context.method was
      // "return", allow the original .return call to continue in the
      // outer generator.
      if (context.method !== "return") {
        context.method = "next";
        context.arg = undefined;
      }

    } else {
      // Re-yield the result returned by the delegate method.
      return info;
    }

    // The delegate iterator is finished, so forget it and continue with
    // the outer generator.
    context.delegate = null;
    return ContinueSentinel;
  }

  // Define Generator.prototype.{next,throw,return} in terms of the
  // unified ._invoke helper method.
  defineIteratorMethods(Gp);

  Gp[toStringTagSymbol] = "Generator";

  // A Generator should always return itself as the iterator object when the
  // @@iterator function is called on it. Some browsers' implementations of the
  // iterator prototype chain incorrectly implement this, causing the Generator
  // object to not be returned from this call. This ensures that doesn't happen.
  // See https://github.com/facebook/regenerator/issues/274 for more details.
  Gp[iteratorSymbol] = function() {
    return this;
  };

  Gp.toString = function() {
    return "[object Generator]";
  };

  function pushTryEntry(locs) {
    var entry = { tryLoc: locs[0] };

    if (1 in locs) {
      entry.catchLoc = locs[1];
    }

    if (2 in locs) {
      entry.finallyLoc = locs[2];
      entry.afterLoc = locs[3];
    }

    this.tryEntries.push(entry);
  }

  function resetTryEntry(entry) {
    var record = entry.completion || {};
    record.type = "normal";
    delete record.arg;
    entry.completion = record;
  }

  function Context(tryLocsList) {
    // The root entry object (effectively a try statement without a catch
    // or a finally block) gives us a place to store values thrown from
    // locations where there is no enclosing try statement.
    this.tryEntries = [{ tryLoc: "root" }];
    tryLocsList.forEach(pushTryEntry, this);
    this.reset(true);
  }

  exports.keys = function(object) {
    var keys = [];
    for (var key in object) {
      keys.push(key);
    }
    keys.reverse();

    // Rather than returning an object with a next method, we keep
    // things simple and return the next function itself.
    return function next() {
      while (keys.length) {
        var key = keys.pop();
        if (key in object) {
          next.value = key;
          next.done = false;
          return next;
        }
      }

      // To avoid creating an additional object, we just hang the .value
      // and .done properties off the next function object itself. This
      // also ensures that the minifier will not anonymize the function.
      next.done = true;
      return next;
    };
  };

  function values(iterable) {
    if (iterable) {
      var iteratorMethod = iterable[iteratorSymbol];
      if (iteratorMethod) {
        return iteratorMethod.call(iterable);
      }

      if (typeof iterable.next === "function") {
        return iterable;
      }

      if (!isNaN(iterable.length)) {
        var i = -1, next = function next() {
          while (++i < iterable.length) {
            if (hasOwn.call(iterable, i)) {
              next.value = iterable[i];
              next.done = false;
              return next;
            }
          }

          next.value = undefined;
          next.done = true;

          return next;
        };

        return next.next = next;
      }
    }

    // Return an iterator with no values.
    return { next: doneResult };
  }
  exports.values = values;

  function doneResult() {
    return { value: undefined, done: true };
  }

  Context.prototype = {
    constructor: Context,

    reset: function(skipTempReset) {
      this.prev = 0;
      this.next = 0;
      // Resetting context._sent for legacy support of Babel's
      // function.sent implementation.
      this.sent = this._sent = undefined;
      this.done = false;
      this.delegate = null;

      this.method = "next";
      this.arg = undefined;

      this.tryEntries.forEach(resetTryEntry);

      if (!skipTempReset) {
        for (var name in this) {
          // Not sure about the optimal order of these conditions:
          if (name.charAt(0) === "t" &&
              hasOwn.call(this, name) &&
              !isNaN(+name.slice(1))) {
            this[name] = undefined;
          }
        }
      }
    },

    stop: function() {
      this.done = true;

      var rootEntry = this.tryEntries[0];
      var rootRecord = rootEntry.completion;
      if (rootRecord.type === "throw") {
        throw rootRecord.arg;
      }

      return this.rval;
    },

    dispatchException: function(exception) {
      if (this.done) {
        throw exception;
      }

      var context = this;
      function handle(loc, caught) {
        record.type = "throw";
        record.arg = exception;
        context.next = loc;

        if (caught) {
          // If the dispatched exception was caught by a catch block,
          // then let that catch block handle the exception normally.
          context.method = "next";
          context.arg = undefined;
        }

        return !! caught;
      }

      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        var record = entry.completion;

        if (entry.tryLoc === "root") {
          // Exception thrown outside of any try block that could handle
          // it, so set the completion value of the entire function to
          // throw the exception.
          return handle("end");
        }

        if (entry.tryLoc <= this.prev) {
          var hasCatch = hasOwn.call(entry, "catchLoc");
          var hasFinally = hasOwn.call(entry, "finallyLoc");

          if (hasCatch && hasFinally) {
            if (this.prev < entry.catchLoc) {
              return handle(entry.catchLoc, true);
            } else if (this.prev < entry.finallyLoc) {
              return handle(entry.finallyLoc);
            }

          } else if (hasCatch) {
            if (this.prev < entry.catchLoc) {
              return handle(entry.catchLoc, true);
            }

          } else if (hasFinally) {
            if (this.prev < entry.finallyLoc) {
              return handle(entry.finallyLoc);
            }

          } else {
            throw new Error("try statement without catch or finally");
          }
        }
      }
    },

    abrupt: function(type, arg) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.tryLoc <= this.prev &&
            hasOwn.call(entry, "finallyLoc") &&
            this.prev < entry.finallyLoc) {
          var finallyEntry = entry;
          break;
        }
      }

      if (finallyEntry &&
          (type === "break" ||
           type === "continue") &&
          finallyEntry.tryLoc <= arg &&
          arg <= finallyEntry.finallyLoc) {
        // Ignore the finally entry if control is not jumping to a
        // location outside the try/catch block.
        finallyEntry = null;
      }

      var record = finallyEntry ? finallyEntry.completion : {};
      record.type = type;
      record.arg = arg;

      if (finallyEntry) {
        this.method = "next";
        this.next = finallyEntry.finallyLoc;
        return ContinueSentinel;
      }

      return this.complete(record);
    },

    complete: function(record, afterLoc) {
      if (record.type === "throw") {
        throw record.arg;
      }

      if (record.type === "break" ||
          record.type === "continue") {
        this.next = record.arg;
      } else if (record.type === "return") {
        this.rval = this.arg = record.arg;
        this.method = "return";
        this.next = "end";
      } else if (record.type === "normal" && afterLoc) {
        this.next = afterLoc;
      }

      return ContinueSentinel;
    },

    finish: function(finallyLoc) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.finallyLoc === finallyLoc) {
          this.complete(entry.completion, entry.afterLoc);
          resetTryEntry(entry);
          return ContinueSentinel;
        }
      }
    },

    "catch": function(tryLoc) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.tryLoc === tryLoc) {
          var record = entry.completion;
          if (record.type === "throw") {
            var thrown = record.arg;
            resetTryEntry(entry);
          }
          return thrown;
        }
      }

      // The context.catch method must only be called with a location
      // argument that corresponds to a known catch block.
      throw new Error("illegal catch attempt");
    },

    delegateYield: function(iterable, resultName, nextLoc) {
      this.delegate = {
        iterator: values(iterable),
        resultName: resultName,
        nextLoc: nextLoc
      };

      if (this.method === "next") {
        // Deliberately forget the last sent value so that we don't
        // accidentally pass it on to the delegate.
        this.arg = undefined;
      }

      return ContinueSentinel;
    }
  };

  // Regardless of whether this script is executing as a CommonJS module
  // or not, return the runtime object so that we can declare the variable
  // regeneratorRuntime in the outer scope, which allows this module to be
  // injected easily by `bin/regenerator --include-runtime script.js`.
  return exports;

}(
  // If this script is executing as a CommonJS module, use module.exports
  // as the regeneratorRuntime namespace. Otherwise create a new empty
  // object. Either way, the resulting object will be used to initialize
  // the regeneratorRuntime variable at the top of this file.
   true ? module.exports : undefined
));

try {
  regeneratorRuntime = runtime;
} catch (accidentalStrictMode) {
  // This module should not be running in strict mode, so the above
  // assignment should always work unless something is misconfigured. Just
  // in case runtime.js accidentally runs in strict mode, we can escape
  // strict mode using a global Function call. This could conceivably fail
  // if a Content Security Policy forbids using Function, but in that case
  // the proper solution is to fix the accidental strict mode problem. If
  // you've misconfigured your bundler to force strict mode and applied a
  // CSP to forbid Function, and you're not willing to fix either of those
  // problems, please detail your unique predicament in a GitHub issue.
  Function("r", "regeneratorRuntime = r")(runtime);
}


/***/ }),

/***/ "./node_modules/scheduler/cjs/scheduler-tracing.development.js":
/*!*********************************************************************!*\
  !*** ./node_modules/scheduler/cjs/scheduler-tracing.development.js ***!
  \*********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v0.19.0
 * scheduler-tracing.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (true) {
  (function() {
'use strict';

var DEFAULT_THREAD_ID = 0; // Counters used to generate unique IDs.

var interactionIDCounter = 0;
var threadIDCounter = 0; // Set of currently traced interactions.
// Interactions "stack"–
// Meaning that newly traced interactions are appended to the previously active set.
// When an interaction goes out of scope, the previous set (if any) is restored.

exports.__interactionsRef = null; // Listener(s) to notify when interactions begin and end.

exports.__subscriberRef = null;

{
  exports.__interactionsRef = {
    current: new Set()
  };
  exports.__subscriberRef = {
    current: null
  };
}
function unstable_clear(callback) {

  var prevInteractions = exports.__interactionsRef.current;
  exports.__interactionsRef.current = new Set();

  try {
    return callback();
  } finally {
    exports.__interactionsRef.current = prevInteractions;
  }
}
function unstable_getCurrent() {
  {
    return exports.__interactionsRef.current;
  }
}
function unstable_getThreadID() {
  return ++threadIDCounter;
}
function unstable_trace(name, timestamp, callback) {
  var threadID = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : DEFAULT_THREAD_ID;

  var interaction = {
    __count: 1,
    id: interactionIDCounter++,
    name: name,
    timestamp: timestamp
  };
  var prevInteractions = exports.__interactionsRef.current; // Traced interactions should stack/accumulate.
  // To do that, clone the current interactions.
  // The previous set will be restored upon completion.

  var interactions = new Set(prevInteractions);
  interactions.add(interaction);
  exports.__interactionsRef.current = interactions;
  var subscriber = exports.__subscriberRef.current;
  var returnValue;

  try {
    if (subscriber !== null) {
      subscriber.onInteractionTraced(interaction);
    }
  } finally {
    try {
      if (subscriber !== null) {
        subscriber.onWorkStarted(interactions, threadID);
      }
    } finally {
      try {
        returnValue = callback();
      } finally {
        exports.__interactionsRef.current = prevInteractions;

        try {
          if (subscriber !== null) {
            subscriber.onWorkStopped(interactions, threadID);
          }
        } finally {
          interaction.__count--; // If no async work was scheduled for this interaction,
          // Notify subscribers that it's completed.

          if (subscriber !== null && interaction.__count === 0) {
            subscriber.onInteractionScheduledWorkCompleted(interaction);
          }
        }
      }
    }
  }

  return returnValue;
}
function unstable_wrap(callback) {
  var threadID = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : DEFAULT_THREAD_ID;

  var wrappedInteractions = exports.__interactionsRef.current;
  var subscriber = exports.__subscriberRef.current;

  if (subscriber !== null) {
    subscriber.onWorkScheduled(wrappedInteractions, threadID);
  } // Update the pending async work count for the current interactions.
  // Update after calling subscribers in case of error.


  wrappedInteractions.forEach(function (interaction) {
    interaction.__count++;
  });
  var hasRun = false;

  function wrapped() {
    var prevInteractions = exports.__interactionsRef.current;
    exports.__interactionsRef.current = wrappedInteractions;
    subscriber = exports.__subscriberRef.current;

    try {
      var returnValue;

      try {
        if (subscriber !== null) {
          subscriber.onWorkStarted(wrappedInteractions, threadID);
        }
      } finally {
        try {
          returnValue = callback.apply(undefined, arguments);
        } finally {
          exports.__interactionsRef.current = prevInteractions;

          if (subscriber !== null) {
            subscriber.onWorkStopped(wrappedInteractions, threadID);
          }
        }
      }

      return returnValue;
    } finally {
      if (!hasRun) {
        // We only expect a wrapped function to be executed once,
        // But in the event that it's executed more than once–
        // Only decrement the outstanding interaction counts once.
        hasRun = true; // Update pending async counts for all wrapped interactions.
        // If this was the last scheduled async work for any of them,
        // Mark them as completed.

        wrappedInteractions.forEach(function (interaction) {
          interaction.__count--;

          if (subscriber !== null && interaction.__count === 0) {
            subscriber.onInteractionScheduledWorkCompleted(interaction);
          }
        });
      }
    }
  }

  wrapped.cancel = function cancel() {
    subscriber = exports.__subscriberRef.current;

    try {
      if (subscriber !== null) {
        subscriber.onWorkCanceled(wrappedInteractions, threadID);
      }
    } finally {
      // Update pending async counts for all wrapped interactions.
      // If this was the last scheduled async work for any of them,
      // Mark them as completed.
      wrappedInteractions.forEach(function (interaction) {
        interaction.__count--;

        if (subscriber && interaction.__count === 0) {
          subscriber.onInteractionScheduledWorkCompleted(interaction);
        }
      });
    }
  };

  return wrapped;
}

var subscribers = null;

{
  subscribers = new Set();
}

function unstable_subscribe(subscriber) {
  {
    subscribers.add(subscriber);

    if (subscribers.size === 1) {
      exports.__subscriberRef.current = {
        onInteractionScheduledWorkCompleted: onInteractionScheduledWorkCompleted,
        onInteractionTraced: onInteractionTraced,
        onWorkCanceled: onWorkCanceled,
        onWorkScheduled: onWorkScheduled,
        onWorkStarted: onWorkStarted,
        onWorkStopped: onWorkStopped
      };
    }
  }
}
function unstable_unsubscribe(subscriber) {
  {
    subscribers.delete(subscriber);

    if (subscribers.size === 0) {
      exports.__subscriberRef.current = null;
    }
  }
}

function onInteractionTraced(interaction) {
  var didCatchError = false;
  var caughtError = null;
  subscribers.forEach(function (subscriber) {
    try {
      subscriber.onInteractionTraced(interaction);
    } catch (error) {
      if (!didCatchError) {
        didCatchError = true;
        caughtError = error;
      }
    }
  });

  if (didCatchError) {
    throw caughtError;
  }
}

function onInteractionScheduledWorkCompleted(interaction) {
  var didCatchError = false;
  var caughtError = null;
  subscribers.forEach(function (subscriber) {
    try {
      subscriber.onInteractionScheduledWorkCompleted(interaction);
    } catch (error) {
      if (!didCatchError) {
        didCatchError = true;
        caughtError = error;
      }
    }
  });

  if (didCatchError) {
    throw caughtError;
  }
}

function onWorkScheduled(interactions, threadID) {
  var didCatchError = false;
  var caughtError = null;
  subscribers.forEach(function (subscriber) {
    try {
      subscriber.onWorkScheduled(interactions, threadID);
    } catch (error) {
      if (!didCatchError) {
        didCatchError = true;
        caughtError = error;
      }
    }
  });

  if (didCatchError) {
    throw caughtError;
  }
}

function onWorkStarted(interactions, threadID) {
  var didCatchError = false;
  var caughtError = null;
  subscribers.forEach(function (subscriber) {
    try {
      subscriber.onWorkStarted(interactions, threadID);
    } catch (error) {
      if (!didCatchError) {
        didCatchError = true;
        caughtError = error;
      }
    }
  });

  if (didCatchError) {
    throw caughtError;
  }
}

function onWorkStopped(interactions, threadID) {
  var didCatchError = false;
  var caughtError = null;
  subscribers.forEach(function (subscriber) {
    try {
      subscriber.onWorkStopped(interactions, threadID);
    } catch (error) {
      if (!didCatchError) {
        didCatchError = true;
        caughtError = error;
      }
    }
  });

  if (didCatchError) {
    throw caughtError;
  }
}

function onWorkCanceled(interactions, threadID) {
  var didCatchError = false;
  var caughtError = null;
  subscribers.forEach(function (subscriber) {
    try {
      subscriber.onWorkCanceled(interactions, threadID);
    } catch (error) {
      if (!didCatchError) {
        didCatchError = true;
        caughtError = error;
      }
    }
  });

  if (didCatchError) {
    throw caughtError;
  }
}

exports.unstable_clear = unstable_clear;
exports.unstable_getCurrent = unstable_getCurrent;
exports.unstable_getThreadID = unstable_getThreadID;
exports.unstable_subscribe = unstable_subscribe;
exports.unstable_trace = unstable_trace;
exports.unstable_unsubscribe = unstable_unsubscribe;
exports.unstable_wrap = unstable_wrap;
  })();
}


/***/ }),

/***/ "./node_modules/scheduler/cjs/scheduler.development.js":
/*!*************************************************************!*\
  !*** ./node_modules/scheduler/cjs/scheduler.development.js ***!
  \*************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v0.19.0
 * scheduler.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (true) {
  (function() {
'use strict';

var enableSchedulerDebugging = false;
var enableProfiling = true;

var requestHostCallback;
var requestHostTimeout;
var cancelHostTimeout;
var shouldYieldToHost;
var requestPaint;

if ( // If Scheduler runs in a non-DOM environment, it falls back to a naive
// implementation using setTimeout.
typeof window === 'undefined' || // Check if MessageChannel is supported, too.
typeof MessageChannel !== 'function') {
  // If this accidentally gets imported in a non-browser environment, e.g. JavaScriptCore,
  // fallback to a naive implementation.
  var _callback = null;
  var _timeoutID = null;

  var _flushCallback = function () {
    if (_callback !== null) {
      try {
        var currentTime = exports.unstable_now();
        var hasRemainingTime = true;

        _callback(hasRemainingTime, currentTime);

        _callback = null;
      } catch (e) {
        setTimeout(_flushCallback, 0);
        throw e;
      }
    }
  };

  var initialTime = Date.now();

  exports.unstable_now = function () {
    return Date.now() - initialTime;
  };

  requestHostCallback = function (cb) {
    if (_callback !== null) {
      // Protect against re-entrancy.
      setTimeout(requestHostCallback, 0, cb);
    } else {
      _callback = cb;
      setTimeout(_flushCallback, 0);
    }
  };

  requestHostTimeout = function (cb, ms) {
    _timeoutID = setTimeout(cb, ms);
  };

  cancelHostTimeout = function () {
    clearTimeout(_timeoutID);
  };

  shouldYieldToHost = function () {
    return false;
  };

  requestPaint = exports.unstable_forceFrameRate = function () {};
} else {
  // Capture local references to native APIs, in case a polyfill overrides them.
  var performance = window.performance;
  var _Date = window.Date;
  var _setTimeout = window.setTimeout;
  var _clearTimeout = window.clearTimeout;

  if (typeof console !== 'undefined') {
    // TODO: Scheduler no longer requires these methods to be polyfilled. But
    // maybe we want to continue warning if they don't exist, to preserve the
    // option to rely on it in the future?
    var requestAnimationFrame = window.requestAnimationFrame;
    var cancelAnimationFrame = window.cancelAnimationFrame; // TODO: Remove fb.me link

    if (typeof requestAnimationFrame !== 'function') {
      // Using console['error'] to evade Babel and ESLint
      console['error']("This browser doesn't support requestAnimationFrame. " + 'Make sure that you load a ' + 'polyfill in older browsers. https://fb.me/react-polyfills');
    }

    if (typeof cancelAnimationFrame !== 'function') {
      // Using console['error'] to evade Babel and ESLint
      console['error']("This browser doesn't support cancelAnimationFrame. " + 'Make sure that you load a ' + 'polyfill in older browsers. https://fb.me/react-polyfills');
    }
  }

  if (typeof performance === 'object' && typeof performance.now === 'function') {
    exports.unstable_now = function () {
      return performance.now();
    };
  } else {
    var _initialTime = _Date.now();

    exports.unstable_now = function () {
      return _Date.now() - _initialTime;
    };
  }

  var isMessageLoopRunning = false;
  var scheduledHostCallback = null;
  var taskTimeoutID = -1; // Scheduler periodically yields in case there is other work on the main
  // thread, like user events. By default, it yields multiple times per frame.
  // It does not attempt to align with frame boundaries, since most tasks don't
  // need to be frame aligned; for those that do, use requestAnimationFrame.

  var yieldInterval = 5;
  var deadline = 0; // TODO: Make this configurable

  {
    // `isInputPending` is not available. Since we have no way of knowing if
    // there's pending input, always yield at the end of the frame.
    shouldYieldToHost = function () {
      return exports.unstable_now() >= deadline;
    }; // Since we yield every frame regardless, `requestPaint` has no effect.


    requestPaint = function () {};
  }

  exports.unstable_forceFrameRate = function (fps) {
    if (fps < 0 || fps > 125) {
      // Using console['error'] to evade Babel and ESLint
      console['error']('forceFrameRate takes a positive int between 0 and 125, ' + 'forcing framerates higher than 125 fps is not unsupported');
      return;
    }

    if (fps > 0) {
      yieldInterval = Math.floor(1000 / fps);
    } else {
      // reset the framerate
      yieldInterval = 5;
    }
  };

  var performWorkUntilDeadline = function () {
    if (scheduledHostCallback !== null) {
      var currentTime = exports.unstable_now(); // Yield after `yieldInterval` ms, regardless of where we are in the vsync
      // cycle. This means there's always time remaining at the beginning of
      // the message event.

      deadline = currentTime + yieldInterval;
      var hasTimeRemaining = true;

      try {
        var hasMoreWork = scheduledHostCallback(hasTimeRemaining, currentTime);

        if (!hasMoreWork) {
          isMessageLoopRunning = false;
          scheduledHostCallback = null;
        } else {
          // If there's more work, schedule the next message event at the end
          // of the preceding one.
          port.postMessage(null);
        }
      } catch (error) {
        // If a scheduler task throws, exit the current browser task so the
        // error can be observed.
        port.postMessage(null);
        throw error;
      }
    } else {
      isMessageLoopRunning = false;
    } // Yielding to the browser will give it a chance to paint, so we can
  };

  var channel = new MessageChannel();
  var port = channel.port2;
  channel.port1.onmessage = performWorkUntilDeadline;

  requestHostCallback = function (callback) {
    scheduledHostCallback = callback;

    if (!isMessageLoopRunning) {
      isMessageLoopRunning = true;
      port.postMessage(null);
    }
  };

  requestHostTimeout = function (callback, ms) {
    taskTimeoutID = _setTimeout(function () {
      callback(exports.unstable_now());
    }, ms);
  };

  cancelHostTimeout = function () {
    _clearTimeout(taskTimeoutID);

    taskTimeoutID = -1;
  };
}

function push(heap, node) {
  var index = heap.length;
  heap.push(node);
  siftUp(heap, node, index);
}
function peek(heap) {
  var first = heap[0];
  return first === undefined ? null : first;
}
function pop(heap) {
  var first = heap[0];

  if (first !== undefined) {
    var last = heap.pop();

    if (last !== first) {
      heap[0] = last;
      siftDown(heap, last, 0);
    }

    return first;
  } else {
    return null;
  }
}

function siftUp(heap, node, i) {
  var index = i;

  while (true) {
    var parentIndex = index - 1 >>> 1;
    var parent = heap[parentIndex];

    if (parent !== undefined && compare(parent, node) > 0) {
      // The parent is larger. Swap positions.
      heap[parentIndex] = node;
      heap[index] = parent;
      index = parentIndex;
    } else {
      // The parent is smaller. Exit.
      return;
    }
  }
}

function siftDown(heap, node, i) {
  var index = i;
  var length = heap.length;

  while (index < length) {
    var leftIndex = (index + 1) * 2 - 1;
    var left = heap[leftIndex];
    var rightIndex = leftIndex + 1;
    var right = heap[rightIndex]; // If the left or right node is smaller, swap with the smaller of those.

    if (left !== undefined && compare(left, node) < 0) {
      if (right !== undefined && compare(right, left) < 0) {
        heap[index] = right;
        heap[rightIndex] = node;
        index = rightIndex;
      } else {
        heap[index] = left;
        heap[leftIndex] = node;
        index = leftIndex;
      }
    } else if (right !== undefined && compare(right, node) < 0) {
      heap[index] = right;
      heap[rightIndex] = node;
      index = rightIndex;
    } else {
      // Neither child is smaller. Exit.
      return;
    }
  }
}

function compare(a, b) {
  // Compare sort index first, then task id.
  var diff = a.sortIndex - b.sortIndex;
  return diff !== 0 ? diff : a.id - b.id;
}

// TODO: Use symbols?
var NoPriority = 0;
var ImmediatePriority = 1;
var UserBlockingPriority = 2;
var NormalPriority = 3;
var LowPriority = 4;
var IdlePriority = 5;

var runIdCounter = 0;
var mainThreadIdCounter = 0;
var profilingStateSize = 4;
var sharedProfilingBuffer =  // $FlowFixMe Flow doesn't know about SharedArrayBuffer
typeof SharedArrayBuffer === 'function' ? new SharedArrayBuffer(profilingStateSize * Int32Array.BYTES_PER_ELEMENT) : // $FlowFixMe Flow doesn't know about ArrayBuffer
typeof ArrayBuffer === 'function' ? new ArrayBuffer(profilingStateSize * Int32Array.BYTES_PER_ELEMENT) : null // Don't crash the init path on IE9
;
var profilingState =  sharedProfilingBuffer !== null ? new Int32Array(sharedProfilingBuffer) : []; // We can't read this but it helps save bytes for null checks

var PRIORITY = 0;
var CURRENT_TASK_ID = 1;
var CURRENT_RUN_ID = 2;
var QUEUE_SIZE = 3;

{
  profilingState[PRIORITY] = NoPriority; // This is maintained with a counter, because the size of the priority queue
  // array might include canceled tasks.

  profilingState[QUEUE_SIZE] = 0;
  profilingState[CURRENT_TASK_ID] = 0;
} // Bytes per element is 4


var INITIAL_EVENT_LOG_SIZE = 131072;
var MAX_EVENT_LOG_SIZE = 524288; // Equivalent to 2 megabytes

var eventLogSize = 0;
var eventLogBuffer = null;
var eventLog = null;
var eventLogIndex = 0;
var TaskStartEvent = 1;
var TaskCompleteEvent = 2;
var TaskErrorEvent = 3;
var TaskCancelEvent = 4;
var TaskRunEvent = 5;
var TaskYieldEvent = 6;
var SchedulerSuspendEvent = 7;
var SchedulerResumeEvent = 8;

function logEvent(entries) {
  if (eventLog !== null) {
    var offset = eventLogIndex;
    eventLogIndex += entries.length;

    if (eventLogIndex + 1 > eventLogSize) {
      eventLogSize *= 2;

      if (eventLogSize > MAX_EVENT_LOG_SIZE) {
        // Using console['error'] to evade Babel and ESLint
        console['error']("Scheduler Profiling: Event log exceeded maximum size. Don't " + 'forget to call `stopLoggingProfilingEvents()`.');
        stopLoggingProfilingEvents();
        return;
      }

      var newEventLog = new Int32Array(eventLogSize * 4);
      newEventLog.set(eventLog);
      eventLogBuffer = newEventLog.buffer;
      eventLog = newEventLog;
    }

    eventLog.set(entries, offset);
  }
}

function startLoggingProfilingEvents() {
  eventLogSize = INITIAL_EVENT_LOG_SIZE;
  eventLogBuffer = new ArrayBuffer(eventLogSize * 4);
  eventLog = new Int32Array(eventLogBuffer);
  eventLogIndex = 0;
}
function stopLoggingProfilingEvents() {
  var buffer = eventLogBuffer;
  eventLogSize = 0;
  eventLogBuffer = null;
  eventLog = null;
  eventLogIndex = 0;
  return buffer;
}
function markTaskStart(task, ms) {
  {
    profilingState[QUEUE_SIZE]++;

    if (eventLog !== null) {
      // performance.now returns a float, representing milliseconds. When the
      // event is logged, it's coerced to an int. Convert to microseconds to
      // maintain extra degrees of precision.
      logEvent([TaskStartEvent, ms * 1000, task.id, task.priorityLevel]);
    }
  }
}
function markTaskCompleted(task, ms) {
  {
    profilingState[PRIORITY] = NoPriority;
    profilingState[CURRENT_TASK_ID] = 0;
    profilingState[QUEUE_SIZE]--;

    if (eventLog !== null) {
      logEvent([TaskCompleteEvent, ms * 1000, task.id]);
    }
  }
}
function markTaskCanceled(task, ms) {
  {
    profilingState[QUEUE_SIZE]--;

    if (eventLog !== null) {
      logEvent([TaskCancelEvent, ms * 1000, task.id]);
    }
  }
}
function markTaskErrored(task, ms) {
  {
    profilingState[PRIORITY] = NoPriority;
    profilingState[CURRENT_TASK_ID] = 0;
    profilingState[QUEUE_SIZE]--;

    if (eventLog !== null) {
      logEvent([TaskErrorEvent, ms * 1000, task.id]);
    }
  }
}
function markTaskRun(task, ms) {
  {
    runIdCounter++;
    profilingState[PRIORITY] = task.priorityLevel;
    profilingState[CURRENT_TASK_ID] = task.id;
    profilingState[CURRENT_RUN_ID] = runIdCounter;

    if (eventLog !== null) {
      logEvent([TaskRunEvent, ms * 1000, task.id, runIdCounter]);
    }
  }
}
function markTaskYield(task, ms) {
  {
    profilingState[PRIORITY] = NoPriority;
    profilingState[CURRENT_TASK_ID] = 0;
    profilingState[CURRENT_RUN_ID] = 0;

    if (eventLog !== null) {
      logEvent([TaskYieldEvent, ms * 1000, task.id, runIdCounter]);
    }
  }
}
function markSchedulerSuspended(ms) {
  {
    mainThreadIdCounter++;

    if (eventLog !== null) {
      logEvent([SchedulerSuspendEvent, ms * 1000, mainThreadIdCounter]);
    }
  }
}
function markSchedulerUnsuspended(ms) {
  {
    if (eventLog !== null) {
      logEvent([SchedulerResumeEvent, ms * 1000, mainThreadIdCounter]);
    }
  }
}

/* eslint-disable no-var */
// Math.pow(2, 30) - 1
// 0b111111111111111111111111111111

var maxSigned31BitInt = 1073741823; // Times out immediately

var IMMEDIATE_PRIORITY_TIMEOUT = -1; // Eventually times out

var USER_BLOCKING_PRIORITY = 250;
var NORMAL_PRIORITY_TIMEOUT = 5000;
var LOW_PRIORITY_TIMEOUT = 10000; // Never times out

var IDLE_PRIORITY = maxSigned31BitInt; // Tasks are stored on a min heap

var taskQueue = [];
var timerQueue = []; // Incrementing id counter. Used to maintain insertion order.

var taskIdCounter = 1; // Pausing the scheduler is useful for debugging.
var currentTask = null;
var currentPriorityLevel = NormalPriority; // This is set while performing work, to prevent re-entrancy.

var isPerformingWork = false;
var isHostCallbackScheduled = false;
var isHostTimeoutScheduled = false;

function advanceTimers(currentTime) {
  // Check for tasks that are no longer delayed and add them to the queue.
  var timer = peek(timerQueue);

  while (timer !== null) {
    if (timer.callback === null) {
      // Timer was cancelled.
      pop(timerQueue);
    } else if (timer.startTime <= currentTime) {
      // Timer fired. Transfer to the task queue.
      pop(timerQueue);
      timer.sortIndex = timer.expirationTime;
      push(taskQueue, timer);

      {
        markTaskStart(timer, currentTime);
        timer.isQueued = true;
      }
    } else {
      // Remaining timers are pending.
      return;
    }

    timer = peek(timerQueue);
  }
}

function handleTimeout(currentTime) {
  isHostTimeoutScheduled = false;
  advanceTimers(currentTime);

  if (!isHostCallbackScheduled) {
    if (peek(taskQueue) !== null) {
      isHostCallbackScheduled = true;
      requestHostCallback(flushWork);
    } else {
      var firstTimer = peek(timerQueue);

      if (firstTimer !== null) {
        requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
      }
    }
  }
}

function flushWork(hasTimeRemaining, initialTime) {
  {
    markSchedulerUnsuspended(initialTime);
  } // We'll need a host callback the next time work is scheduled.


  isHostCallbackScheduled = false;

  if (isHostTimeoutScheduled) {
    // We scheduled a timeout but it's no longer needed. Cancel it.
    isHostTimeoutScheduled = false;
    cancelHostTimeout();
  }

  isPerformingWork = true;
  var previousPriorityLevel = currentPriorityLevel;

  try {
    if (enableProfiling) {
      try {
        return workLoop(hasTimeRemaining, initialTime);
      } catch (error) {
        if (currentTask !== null) {
          var currentTime = exports.unstable_now();
          markTaskErrored(currentTask, currentTime);
          currentTask.isQueued = false;
        }

        throw error;
      }
    } else {
      // No catch in prod codepath.
      return workLoop(hasTimeRemaining, initialTime);
    }
  } finally {
    currentTask = null;
    currentPriorityLevel = previousPriorityLevel;
    isPerformingWork = false;

    {
      var _currentTime = exports.unstable_now();

      markSchedulerSuspended(_currentTime);
    }
  }
}

function workLoop(hasTimeRemaining, initialTime) {
  var currentTime = initialTime;
  advanceTimers(currentTime);
  currentTask = peek(taskQueue);

  while (currentTask !== null && !(enableSchedulerDebugging )) {
    if (currentTask.expirationTime > currentTime && (!hasTimeRemaining || shouldYieldToHost())) {
      // This currentTask hasn't expired, and we've reached the deadline.
      break;
    }

    var callback = currentTask.callback;

    if (callback !== null) {
      currentTask.callback = null;
      currentPriorityLevel = currentTask.priorityLevel;
      var didUserCallbackTimeout = currentTask.expirationTime <= currentTime;
      markTaskRun(currentTask, currentTime);
      var continuationCallback = callback(didUserCallbackTimeout);
      currentTime = exports.unstable_now();

      if (typeof continuationCallback === 'function') {
        currentTask.callback = continuationCallback;
        markTaskYield(currentTask, currentTime);
      } else {
        {
          markTaskCompleted(currentTask, currentTime);
          currentTask.isQueued = false;
        }

        if (currentTask === peek(taskQueue)) {
          pop(taskQueue);
        }
      }

      advanceTimers(currentTime);
    } else {
      pop(taskQueue);
    }

    currentTask = peek(taskQueue);
  } // Return whether there's additional work


  if (currentTask !== null) {
    return true;
  } else {
    var firstTimer = peek(timerQueue);

    if (firstTimer !== null) {
      requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
    }

    return false;
  }
}

function unstable_runWithPriority(priorityLevel, eventHandler) {
  switch (priorityLevel) {
    case ImmediatePriority:
    case UserBlockingPriority:
    case NormalPriority:
    case LowPriority:
    case IdlePriority:
      break;

    default:
      priorityLevel = NormalPriority;
  }

  var previousPriorityLevel = currentPriorityLevel;
  currentPriorityLevel = priorityLevel;

  try {
    return eventHandler();
  } finally {
    currentPriorityLevel = previousPriorityLevel;
  }
}

function unstable_next(eventHandler) {
  var priorityLevel;

  switch (currentPriorityLevel) {
    case ImmediatePriority:
    case UserBlockingPriority:
    case NormalPriority:
      // Shift down to normal priority
      priorityLevel = NormalPriority;
      break;

    default:
      // Anything lower than normal priority should remain at the current level.
      priorityLevel = currentPriorityLevel;
      break;
  }

  var previousPriorityLevel = currentPriorityLevel;
  currentPriorityLevel = priorityLevel;

  try {
    return eventHandler();
  } finally {
    currentPriorityLevel = previousPriorityLevel;
  }
}

function unstable_wrapCallback(callback) {
  var parentPriorityLevel = currentPriorityLevel;
  return function () {
    // This is a fork of runWithPriority, inlined for performance.
    var previousPriorityLevel = currentPriorityLevel;
    currentPriorityLevel = parentPriorityLevel;

    try {
      return callback.apply(this, arguments);
    } finally {
      currentPriorityLevel = previousPriorityLevel;
    }
  };
}

function timeoutForPriorityLevel(priorityLevel) {
  switch (priorityLevel) {
    case ImmediatePriority:
      return IMMEDIATE_PRIORITY_TIMEOUT;

    case UserBlockingPriority:
      return USER_BLOCKING_PRIORITY;

    case IdlePriority:
      return IDLE_PRIORITY;

    case LowPriority:
      return LOW_PRIORITY_TIMEOUT;

    case NormalPriority:
    default:
      return NORMAL_PRIORITY_TIMEOUT;
  }
}

function unstable_scheduleCallback(priorityLevel, callback, options) {
  var currentTime = exports.unstable_now();
  var startTime;
  var timeout;

  if (typeof options === 'object' && options !== null) {
    var delay = options.delay;

    if (typeof delay === 'number' && delay > 0) {
      startTime = currentTime + delay;
    } else {
      startTime = currentTime;
    }

    timeout = typeof options.timeout === 'number' ? options.timeout : timeoutForPriorityLevel(priorityLevel);
  } else {
    timeout = timeoutForPriorityLevel(priorityLevel);
    startTime = currentTime;
  }

  var expirationTime = startTime + timeout;
  var newTask = {
    id: taskIdCounter++,
    callback: callback,
    priorityLevel: priorityLevel,
    startTime: startTime,
    expirationTime: expirationTime,
    sortIndex: -1
  };

  {
    newTask.isQueued = false;
  }

  if (startTime > currentTime) {
    // This is a delayed task.
    newTask.sortIndex = startTime;
    push(timerQueue, newTask);

    if (peek(taskQueue) === null && newTask === peek(timerQueue)) {
      // All tasks are delayed, and this is the task with the earliest delay.
      if (isHostTimeoutScheduled) {
        // Cancel an existing timeout.
        cancelHostTimeout();
      } else {
        isHostTimeoutScheduled = true;
      } // Schedule a timeout.


      requestHostTimeout(handleTimeout, startTime - currentTime);
    }
  } else {
    newTask.sortIndex = expirationTime;
    push(taskQueue, newTask);

    {
      markTaskStart(newTask, currentTime);
      newTask.isQueued = true;
    } // Schedule a host callback, if needed. If we're already performing work,
    // wait until the next time we yield.


    if (!isHostCallbackScheduled && !isPerformingWork) {
      isHostCallbackScheduled = true;
      requestHostCallback(flushWork);
    }
  }

  return newTask;
}

function unstable_pauseExecution() {
}

function unstable_continueExecution() {

  if (!isHostCallbackScheduled && !isPerformingWork) {
    isHostCallbackScheduled = true;
    requestHostCallback(flushWork);
  }
}

function unstable_getFirstCallbackNode() {
  return peek(taskQueue);
}

function unstable_cancelCallback(task) {
  {
    if (task.isQueued) {
      var currentTime = exports.unstable_now();
      markTaskCanceled(task, currentTime);
      task.isQueued = false;
    }
  } // Null out the callback to indicate the task has been canceled. (Can't
  // remove from the queue because you can't remove arbitrary nodes from an
  // array based heap, only the first one.)


  task.callback = null;
}

function unstable_getCurrentPriorityLevel() {
  return currentPriorityLevel;
}

function unstable_shouldYield() {
  var currentTime = exports.unstable_now();
  advanceTimers(currentTime);
  var firstTask = peek(taskQueue);
  return firstTask !== currentTask && currentTask !== null && firstTask !== null && firstTask.callback !== null && firstTask.startTime <= currentTime && firstTask.expirationTime < currentTask.expirationTime || shouldYieldToHost();
}

var unstable_requestPaint = requestPaint;
var unstable_Profiling =  {
  startLoggingProfilingEvents: startLoggingProfilingEvents,
  stopLoggingProfilingEvents: stopLoggingProfilingEvents,
  sharedProfilingBuffer: sharedProfilingBuffer
} ;

exports.unstable_IdlePriority = IdlePriority;
exports.unstable_ImmediatePriority = ImmediatePriority;
exports.unstable_LowPriority = LowPriority;
exports.unstable_NormalPriority = NormalPriority;
exports.unstable_Profiling = unstable_Profiling;
exports.unstable_UserBlockingPriority = UserBlockingPriority;
exports.unstable_cancelCallback = unstable_cancelCallback;
exports.unstable_continueExecution = unstable_continueExecution;
exports.unstable_getCurrentPriorityLevel = unstable_getCurrentPriorityLevel;
exports.unstable_getFirstCallbackNode = unstable_getFirstCallbackNode;
exports.unstable_next = unstable_next;
exports.unstable_pauseExecution = unstable_pauseExecution;
exports.unstable_requestPaint = unstable_requestPaint;
exports.unstable_runWithPriority = unstable_runWithPriority;
exports.unstable_scheduleCallback = unstable_scheduleCallback;
exports.unstable_shouldYield = unstable_shouldYield;
exports.unstable_wrapCallback = unstable_wrapCallback;
  })();
}


/***/ }),

/***/ "./node_modules/scheduler/index.js":
/*!*****************************************!*\
  !*** ./node_modules/scheduler/index.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


if (false) {} else {
  module.exports = __webpack_require__(/*! ./cjs/scheduler.development.js */ "./node_modules/scheduler/cjs/scheduler.development.js");
}


/***/ }),

/***/ "./node_modules/scheduler/tracing.js":
/*!*******************************************!*\
  !*** ./node_modules/scheduler/tracing.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


if (false) {} else {
  module.exports = __webpack_require__(/*! ./cjs/scheduler-tracing.development.js */ "./node_modules/scheduler/cjs/scheduler-tracing.development.js");
}


/***/ }),

/***/ "./node_modules/string-hash/index.js":
/*!*******************************************!*\
  !*** ./node_modules/string-hash/index.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


function hash(str) {
  var hash = 5381,
      i    = str.length;

  while(i) {
    hash = (hash * 33) ^ str.charCodeAt(--i);
  }

  /* JavaScript does bitwise operations (like XOR, above) on 32-bit signed
   * integers. Since we want the results to be always positive, convert the
   * signed int to an unsigned by doing an unsigned bitshift. */
  return hash >>> 0;
}

module.exports = hash;


/***/ }),

/***/ "./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js":
/*!****************************************************************************!*\
  !*** ./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js ***!
  \****************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var isOldIE = function isOldIE() {
  var memo;
  return function memorize() {
    if (typeof memo === 'undefined') {
      // Test for IE <= 9 as proposed by Browserhacks
      // @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
      // Tests for existence of standard globals is to allow style-loader
      // to operate correctly into non-standard environments
      // @see https://github.com/webpack-contrib/style-loader/issues/177
      memo = Boolean(window && document && document.all && !window.atob);
    }

    return memo;
  };
}();

var getTarget = function getTarget() {
  var memo = {};
  return function memorize(target) {
    if (typeof memo[target] === 'undefined') {
      var styleTarget = document.querySelector(target); // Special case to return head of iframe instead of iframe itself

      if (window.HTMLIFrameElement && styleTarget instanceof window.HTMLIFrameElement) {
        try {
          // This will throw an exception if access to iframe is blocked
          // due to cross-origin restrictions
          styleTarget = styleTarget.contentDocument.head;
        } catch (e) {
          // istanbul ignore next
          styleTarget = null;
        }
      }

      memo[target] = styleTarget;
    }

    return memo[target];
  };
}();

var stylesInDom = [];

function getIndexByIdentifier(identifier) {
  var result = -1;

  for (var i = 0; i < stylesInDom.length; i++) {
    if (stylesInDom[i].identifier === identifier) {
      result = i;
      break;
    }
  }

  return result;
}

function modulesToDom(list, options) {
  var idCountMap = {};
  var identifiers = [];

  for (var i = 0; i < list.length; i++) {
    var item = list[i];
    var id = options.base ? item[0] + options.base : item[0];
    var count = idCountMap[id] || 0;
    var identifier = "".concat(id, " ").concat(count);
    idCountMap[id] = count + 1;
    var index = getIndexByIdentifier(identifier);
    var obj = {
      css: item[1],
      media: item[2],
      sourceMap: item[3]
    };

    if (index !== -1) {
      stylesInDom[index].references++;
      stylesInDom[index].updater(obj);
    } else {
      stylesInDom.push({
        identifier: identifier,
        updater: addStyle(obj, options),
        references: 1
      });
    }

    identifiers.push(identifier);
  }

  return identifiers;
}

function insertStyleElement(options) {
  var style = document.createElement('style');
  var attributes = options.attributes || {};

  if (typeof attributes.nonce === 'undefined') {
    var nonce =  true ? __webpack_require__.nc : undefined;

    if (nonce) {
      attributes.nonce = nonce;
    }
  }

  Object.keys(attributes).forEach(function (key) {
    style.setAttribute(key, attributes[key]);
  });

  if (typeof options.insert === 'function') {
    options.insert(style);
  } else {
    var target = getTarget(options.insert || 'head');

    if (!target) {
      throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
    }

    target.appendChild(style);
  }

  return style;
}

function removeStyleElement(style) {
  // istanbul ignore if
  if (style.parentNode === null) {
    return false;
  }

  style.parentNode.removeChild(style);
}
/* istanbul ignore next  */


var replaceText = function replaceText() {
  var textStore = [];
  return function replace(index, replacement) {
    textStore[index] = replacement;
    return textStore.filter(Boolean).join('\n');
  };
}();

function applyToSingletonTag(style, index, remove, obj) {
  var css = remove ? '' : obj.media ? "@media ".concat(obj.media, " {").concat(obj.css, "}") : obj.css; // For old IE

  /* istanbul ignore if  */

  if (style.styleSheet) {
    style.styleSheet.cssText = replaceText(index, css);
  } else {
    var cssNode = document.createTextNode(css);
    var childNodes = style.childNodes;

    if (childNodes[index]) {
      style.removeChild(childNodes[index]);
    }

    if (childNodes.length) {
      style.insertBefore(cssNode, childNodes[index]);
    } else {
      style.appendChild(cssNode);
    }
  }
}

function applyToTag(style, options, obj) {
  var css = obj.css;
  var media = obj.media;
  var sourceMap = obj.sourceMap;

  if (media) {
    style.setAttribute('media', media);
  } else {
    style.removeAttribute('media');
  }

  if (sourceMap && btoa) {
    css += "\n/*# sourceMappingURL=data:application/json;base64,".concat(btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))), " */");
  } // For old IE

  /* istanbul ignore if  */


  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    while (style.firstChild) {
      style.removeChild(style.firstChild);
    }

    style.appendChild(document.createTextNode(css));
  }
}

var singleton = null;
var singletonCounter = 0;

function addStyle(obj, options) {
  var style;
  var update;
  var remove;

  if (options.singleton) {
    var styleIndex = singletonCounter++;
    style = singleton || (singleton = insertStyleElement(options));
    update = applyToSingletonTag.bind(null, style, styleIndex, false);
    remove = applyToSingletonTag.bind(null, style, styleIndex, true);
  } else {
    style = insertStyleElement(options);
    update = applyToTag.bind(null, style, options);

    remove = function remove() {
      removeStyleElement(style);
    };
  }

  update(obj);
  return function updateStyle(newObj) {
    if (newObj) {
      if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap) {
        return;
      }

      update(obj = newObj);
    } else {
      remove();
    }
  };
}

module.exports = function (list, options) {
  options = options || {}; // Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
  // tags it will allow on a page

  if (!options.singleton && typeof options.singleton !== 'boolean') {
    options.singleton = isOldIE();
  }

  list = list || [];
  var lastIdentifiers = modulesToDom(list, options);
  return function update(newList) {
    newList = newList || [];

    if (Object.prototype.toString.call(newList) !== '[object Array]') {
      return;
    }

    for (var i = 0; i < lastIdentifiers.length; i++) {
      var identifier = lastIdentifiers[i];
      var index = getIndexByIdentifier(identifier);
      stylesInDom[index].references--;
    }

    var newLastIdentifiers = modulesToDom(newList, options);

    for (var _i = 0; _i < lastIdentifiers.length; _i++) {
      var _identifier = lastIdentifiers[_i];

      var _index = getIndexByIdentifier(_identifier);

      if (stylesInDom[_index].references === 0) {
        stylesInDom[_index].updater();

        stylesInDom.splice(_index, 1);
      }
    }

    lastIdentifiers = newLastIdentifiers;
  };
};

/***/ }),

/***/ "./node_modules/symbol-observable/es/index.js":
/*!****************************************************!*\
  !*** ./node_modules/symbol-observable/es/index.js ***!
  \****************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* WEBPACK VAR INJECTION */(function(global, module) {/* harmony import */ var _ponyfill_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ponyfill.js */ "./node_modules/symbol-observable/es/ponyfill.js");
/* global window */


var root;

if (typeof self !== 'undefined') {
  root = self;
} else if (typeof window !== 'undefined') {
  root = window;
} else if (typeof global !== 'undefined') {
  root = global;
} else if (true) {
  root = module;
} else {}

var result = Object(_ponyfill_js__WEBPACK_IMPORTED_MODULE_0__["default"])(root);
/* harmony default export */ __webpack_exports__["default"] = (result);

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js"), __webpack_require__(/*! ./../../webpack/buildin/harmony-module.js */ "./node_modules/webpack/buildin/harmony-module.js")(module)))

/***/ }),

/***/ "./node_modules/symbol-observable/es/ponyfill.js":
/*!*******************************************************!*\
  !*** ./node_modules/symbol-observable/es/ponyfill.js ***!
  \*******************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return symbolObservablePonyfill; });
function symbolObservablePonyfill(root) {
	var result;
	var Symbol = root.Symbol;

	if (typeof Symbol === 'function') {
		if (Symbol.observable) {
			result = Symbol.observable;
		} else {
			result = Symbol('observable');
			Symbol.observable = result;
		}
	} else {
		result = '@@observable';
	}

	return result;
};


/***/ }),

/***/ "./node_modules/webpack/buildin/global.js":
/*!***********************************!*\
  !*** (webpack)/buildin/global.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || new Function("return this")();
} catch (e) {
	// This works if the window reference is available
	if (typeof window === "object") g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ }),

/***/ "./node_modules/webpack/buildin/harmony-module.js":
/*!*******************************************!*\
  !*** (webpack)/buildin/harmony-module.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function(originalModule) {
	if (!originalModule.webpackPolyfill) {
		var module = Object.create(originalModule);
		// module.parent = undefined by default
		if (!module.children) module.children = [];
		Object.defineProperty(module, "loaded", {
			enumerable: true,
			get: function() {
				return module.l;
			}
		});
		Object.defineProperty(module, "id", {
			enumerable: true,
			get: function() {
				return module.i;
			}
		});
		Object.defineProperty(module, "exports", {
			enumerable: true
		});
		module.webpackPolyfill = 1;
	}
	return module;
};


/***/ }),

/***/ "./node_modules/webpack/buildin/module.js":
/*!***********************************!*\
  !*** (webpack)/buildin/module.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = function(module) {
	if (!module.webpackPolyfill) {
		module.deprecate = function() {};
		module.paths = [];
		// module.parent = undefined by default
		if (!module.children) module.children = [];
		Object.defineProperty(module, "loaded", {
			enumerable: true,
			get: function() {
				return module.l;
			}
		});
		Object.defineProperty(module, "id", {
			enumerable: true,
			get: function() {
				return module.i;
			}
		});
		module.webpackPolyfill = 1;
	}
	return module;
};


/***/ }),

/***/ "./public/src/actions/index.ts":
/*!*************************************!*\
  !*** ./public/src/actions/index.ts ***!
  \*************************************/
/*! exports provided: onGuideDlgCancel, onGuideDlgNext, onConsentDlgClose, onConsentDlgOK, onConsentDlgCancel */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "onGuideDlgCancel", function() { return onGuideDlgCancel; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "onGuideDlgNext", function() { return onGuideDlgNext; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "onConsentDlgClose", function() { return onConsentDlgClose; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "onConsentDlgOK", function() { return onConsentDlgOK; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "onConsentDlgCancel", function() { return onConsentDlgCancel; });
var onGuideDlgCancel = function onGuideDlgCancel() {
  return {
    type: "GUIDEDLG_CANCEL"
  };
};
var onGuideDlgNext = function onGuideDlgNext() {
  return {
    type: "GUIDEDLG_NEXT"
  };
};
var onConsentDlgClose = function onConsentDlgClose() {
  return {
    type: "CONSENTDLG_CLOSE"
  };
};
var onConsentDlgOK = function onConsentDlgOK() {
  return {
    type: "CONSENTDLG_OK"
  };
};
var onConsentDlgCancel = function onConsentDlgCancel() {
  return {
    type: "CONSENTDLG_CANCEL"
  };
};

/***/ }),

/***/ "./public/src/components/app.tsx":
/*!***************************************!*\
  !*** ./public/src/components/app.tsx ***!
  \***************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es7.symbol.async-iterator */ "./node_modules/core-js/modules/es7.symbol.async-iterator.js");
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.symbol */ "./node_modules/core-js/modules/es6.symbol.js");
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es6.promise */ "./node_modules/core-js/modules/es6.promise.js");
/* harmony import */ var core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es6.object.to-string */ "./node_modules/core-js/modules/es6.object.to-string.js");
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var regenerator_runtime_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! regenerator-runtime/runtime */ "./node_modules/regenerator-runtime/runtime.js");
/* harmony import */ var regenerator_runtime_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(regenerator_runtime_runtime__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var _interop_acadinterop__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../interop/acadinterop */ "./public/src/interop/acadinterop.ts");
/* harmony import */ var _consentDialog__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./consentDialog */ "./public/src/components/consentDialog.tsx");
/* harmony import */ var _guideDialog__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./guideDialog */ "./public/src/components/guideDialog.tsx");
/* harmony import */ var _syncsettingDialog__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./syncsettingDialog */ "./public/src/components/syncsettingDialog.tsx");






function _typeof(obj) { "@babel/helpers - typeof"; if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

function _possibleConstructorReturn(self, call) { if (call && (_typeof(call) === "object" || typeof call === "function")) { return call; } return _assertThisInitialized(self); }

function _getPrototypeOf(o) { _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf(o) { return o.__proto__ || Object.getPrototypeOf(o); }; return _getPrototypeOf(o); }

function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function"); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } }); if (superClass) _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf(o, p) { o.__proto__ = p; return o; }; return _setPrototypeOf(o, p); }







var App = /*#__PURE__*/function (_React$Component) {
  _inherits(App, _React$Component);

  function App(props) {
    var _this;

    _classCallCheck(this, App);

    _this = _possibleConstructorReturn(this, _getPrototypeOf(App).call(this, props));
    _this.onGuideDlgNext = _this.onGuideDlgNext.bind(_assertThisInitialized(_this));
    _this.onCancel = _this.onCancel.bind(_assertThisInitialized(_this));
    _this.onOK = _this.onOK.bind(_assertThisInitialized(_this));
    _this.onLaunchHelp = _this.onLaunchHelp.bind(_assertThisInitialized(_this));
    return _this;
  }

  _createClass(App, [{
    key: "onGuideDlgNext",
    value: function () {
      var _onGuideDlgNext = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function _callee() {
        return regeneratorRuntime.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                this.onOK();

              case 1:
              case "end":
                return _context.stop();
            }
          }
        }, _callee, this);
      }));

      function onGuideDlgNext() {
        return _onGuideDlgNext.apply(this, arguments);
      }

      return onGuideDlgNext;
    }()
  }, {
    key: "onCancel",
    value: function onCancel() {
      _interop_acadinterop__WEBPACK_IMPORTED_MODULE_6__["default"].interop.cancel();
    }
  }, {
    key: "onOK",
    value: function onOK() {
      _interop_acadinterop__WEBPACK_IMPORTED_MODULE_6__["default"].interop.ok();
    }
  }, {
    key: "onLaunchHelp",
    value: function onLaunchHelp(id) {
      _interop_acadinterop__WEBPACK_IMPORTED_MODULE_6__["default"].interop.launchHelp(id);
    }
  }, {
    key: "render",
    value: function render() {
      var dialog = this.props.dialog || "guide";

      if (!dialog || dialog === 'guide') {
        return react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_guideDialog__WEBPACK_IMPORTED_MODULE_8__["default"], {
          onNext: this.onGuideDlgNext,
          onCancel: this.onCancel,
          onHyperlink: this.onLaunchHelp
        }, ">");
      } else if (dialog === 'consent') {
        return react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_consentDialog__WEBPACK_IMPORTED_MODULE_7__["default"], {
          onOK: this.onOK,
          onCancel: this.onCancel
        });
      } else {
        return react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_syncsettingDialog__WEBPACK_IMPORTED_MODULE_9__["default"], {
          onOK: this.onOK,
          onCancel: this.onCancel,
          onHyperlink: this.onLaunchHelp
        });
      }
    }
  }]);

  return App;
}(react__WEBPACK_IMPORTED_MODULE_5__["Component"]);

;
/* harmony default export */ __webpack_exports__["default"] = (App);

/***/ }),

/***/ "./public/src/components/appcontainer.tsx":
/*!************************************************!*\
  !*** ./public/src/components/appcontainer.tsx ***!
  \************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react_redux__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-redux */ "./node_modules/react-redux/es/index.js");
/* harmony import */ var _actions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../actions */ "./public/src/actions/index.ts");
/* harmony import */ var _app__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./app */ "./public/src/components/app.tsx");
/* harmony import */ var _utils_utils__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../utils/utils */ "./public/src/utils/utils.ts");





var mapStateToProps = function mapStateToProps() {
  var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {
    onBoardingState: {}
  };
  var dialog = Object(_utils_utils__WEBPACK_IMPORTED_MODULE_3__["getUrlParameter"])('dialog');

  if (!state.onBoardingState || !state.onBoardingState.dialog) {
    state.onBoardingState = {
      dialog: dialog ? dialog : 'guide'
    };
  }

  return state.onBoardingState;
};

var mapDispatchToProps = function mapDispatchToProps(dispatch) {
  return {
    onNext: function onNext() {
      dispatch(Object(_actions__WEBPACK_IMPORTED_MODULE_1__["onGuideDlgNext"])());
    }
  };
};

/* harmony default export */ __webpack_exports__["default"] = (Object(react_redux__WEBPACK_IMPORTED_MODULE_0__["connect"])(mapStateToProps, mapDispatchToProps)(_app__WEBPACK_IMPORTED_MODULE_2__["default"]));

/***/ }),

/***/ "./public/src/components/consentDialog.tsx":
/*!*************************************************!*\
  !*** ./public/src/components/consentDialog.tsx ***!
  \*************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es7.symbol.async-iterator */ "./node_modules/core-js/modules/es7.symbol.async-iterator.js");
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.symbol */ "./node_modules/core-js/modules/es6.symbol.js");
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _hig_theme_data_build_esm_lightGrayHighDensityTheme__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @hig/theme-data/build/esm/lightGrayHighDensityTheme */ "./node_modules/@hig/theme-data/build/esm/lightGrayHighDensityTheme.js");
/* harmony import */ var _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @hig/theme-context */ "./node_modules/@hig/theme-context/build/index.es.js");
/* harmony import */ var _hig_button__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @hig/button */ "./node_modules/@hig/button/build/index.es.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var lodash_merge__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! lodash.merge */ "./node_modules/lodash.merge/index.js");
/* harmony import */ var lodash_merge__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(lodash_merge__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var aphrodite__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! aphrodite */ "./node_modules/aphrodite/es/index.js");
/* harmony import */ var _utils_lang__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../utils/lang */ "./public/src/utils/lang.ts");
/* harmony import */ var _interop_acadinterop__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../interop/acadinterop */ "./public/src/interop/acadinterop.ts");
/* harmony import */ var _dialogHeader__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./dialogHeader */ "./public/src/components/dialogHeader.tsx");
/* harmony import */ var _utils_utils__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ../utils/utils */ "./public/src/utils/utils.ts");



function _typeof(obj) { "@babel/helpers - typeof"; if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

function _possibleConstructorReturn(self, call) { if (call && (_typeof(call) === "object" || typeof call === "function")) { return call; } return _assertThisInitialized(self); }

function _getPrototypeOf(o) { _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf(o) { return o.__proto__ || Object.getPrototypeOf(o); }; return _getPrototypeOf(o); }

function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function"); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } }); if (superClass) _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf(o, p) { o.__proto__ = p; return o; }; return _setPrototypeOf(o, p); }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }











var default_string = {
  "title": "AutoCAD would like to access to your cloud storage account data",
  "content": "The cloud storage account data refer to the email address used to sign in to your cloud sorage account.",
  "button_yes": "Give permission",
  "button_no": "Don't give permission",
  "hyperlink": "Why should I give permission?",
  "close_tooltip": "close"
};
var styles = aphrodite__WEBPACK_IMPORTED_MODULE_7__["StyleSheet"].create({
  divContainer: {
    display: "flex",
    flexDirection: "column",
    width: "60%",
    maxWidth: "600px",
    height: "100%",
    minWidth: "30rem",
    minHeight: "20rem",
    margin: "0 auto 8px auto"
  },
  header: {
    textAlign: "center",
    fontSize: "1.6em",
    fontWeight: 600,
    lineHeight: "1.666666em",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center"
  },
  divContent: {
    margin: "0 auto",
    fontSize: "1.4rem",
    textAlign: "center"
  },
  divButtonYes: {
    margin: "1rem auto 0.3rem auto",
    alignSelf: "flex-end",
    minWidth: "60%",
    maxWidth: "40rem",
    height: "2.4rem"
  },
  divButtonNo: {
    margin: "0.5rem auto 1rem auto",
    alignSelf: "flex-end",
    minWidth: "60%",
    maxWidth: "40rem",
    height: "2.4rem"
  },
  divHyperlink: {
    margin: "1.4rem auto",
    color: "#006EAF"
  },
  aHyperlink: {
    cursor: "pointer",
    fontSize: "1.4rem"
  }
});

var buttonStyles = function buttonStyles(styles) {
  return lodash_merge__WEBPACK_IMPORTED_MODULE_6___default()(styles, {
    button: {
      fontSize: "1.2rem",
      lineHeight: "1.2rem",
      height: "2.4rem",
      borderRadius: 2
    }
  });
};

;
var CONSENT_YES = 1;
var CONSENT_NO = 2;

var ConsentDialog = /*#__PURE__*/function (_React$Component) {
  _inherits(ConsentDialog, _React$Component);

  function ConsentDialog(props) {
    var _this;

    _classCallCheck(this, ConsentDialog);

    _this = _possibleConstructorReturn(this, _getPrototypeOf(ConsentDialog).call(this, props));

    _defineProperty(_assertThisInitialized(_this), "state", void 0);

    _defineProperty(_assertThisInitialized(_this), "loaded", void 0);

    _this.state = {
      localeString: null
    };
    _this.loaded = false;
    _this.onYes = _this.onYes.bind(_assertThisInitialized(_this));
    _this.onNO = _this.onNO.bind(_assertThisInitialized(_this));
    _this.onEscapePressed = _this.onEscapePressed.bind(_assertThisInitialized(_this));
    return _this;
  }

  _createClass(ConsentDialog, [{
    key: "componentDidMount",
    value: function componentDidMount() {
      var _this2 = this;

      var self = this;
      Object(_utils_lang__WEBPACK_IMPORTED_MODULE_8__["loadLocale"])().then(function (loc) {
        _this2.loaded = true;

        if (!loc || typeof loc.consent_dialog === 'undefined') {
          self.setState({
            localeString: default_string
          });
        } else {
          self.setState({
            localeString: loc.consent_dialog
          });
        }
      });
      document.addEventListener('keydown', this.onKeyDown);
      document.addEventListener('keydown', this.onEscapePressed, false);
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      document.removeEventListener('keydown', this.onKeyDown);
      document.removeEventListener('keydown', this.onEscapePressed);
    }
  }, {
    key: "componentDidUpdate",
    value: function componentDidUpdate() {
      if (!this.loaded) {
        return;
      }

      this.loaded = false;
      Object(_utils_utils__WEBPACK_IMPORTED_MODULE_11__["adjustHeight"])();
    }
  }, {
    key: "onEscapePressed",
    value: function onEscapePressed(e) {
      if (e && (e.key === 'Escape' || e.key === 'Esc' || e.keyCode === 27) && e.target.nodeName === 'BODY') {
        e.preventDefault();
        this.onNO();
      }
    }
  }, {
    key: "onKeyDown",
    value: function onKeyDown(e) {
      var nodes = document.getElementsByClassName('arrow-move'); // 37: left 38: up, 39: right, 40: down

      if (e.keyCode === 40) {
        var ix = 0;

        for (; ix < nodes.length; ix++) {
          if (nodes[ix] === document.activeElement) {
            var next_ix = (ix + 1) % nodes.length;
            nodes[next_ix].focus();
            break;
          }
        }

        if (ix === nodes.length) {
          nodes[0].focus();
        }
      } else if (e.keyCode === 38) {
        for (var ix = 0; ix < nodes.length; ix++) {
          if (nodes[ix] === document.activeElement) {
            var prev_ix = ix - 1 < 0 ? nodes.length - 1 : ix - 1;
            nodes[prev_ix].focus();
            break;
          }
        }

        if (ix === nodes.length) {
          nodes[0].focus();
        }
      }
    }
  }, {
    key: "onYes",
    value: function onYes() {
      _interop_acadinterop__WEBPACK_IMPORTED_MODULE_9__["default"].interop.setConsentStatus(CONSENT_YES);

      if (this.props.onOK) {
        this.props.onOK();
      }
    }
  }, {
    key: "onNO",
    value: function onNO() {
      _interop_acadinterop__WEBPACK_IMPORTED_MODULE_9__["default"].interop.setConsentStatus(CONSENT_NO);

      if (this.props.onOK) {
        this.props.onOK();
      }
    }
  }, {
    key: "render",
    value: function render() {
      var localeString = this.state.localeString;

      if (!localeString) {
        return react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", null);
      }

      return react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_hig_theme_context__WEBPACK_IMPORTED_MODULE_3__["default"].Provider, {
        value: _hig_theme_data_build_esm_lightGrayHighDensityTheme__WEBPACK_IMPORTED_MODULE_2__["default"]
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_dialogHeader__WEBPACK_IMPORTED_MODULE_10__["default"], {
        separateLine: null,
        onClick: this.onNO,
        title: "",
        closeTooltip: localeString.close_tooltip
      }), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divContainer),
        role: "document"
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("header", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.header)
      }, localeString.title), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divContent)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("p", null, localeString.content, " ")), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divButtonYes)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_hig_button__WEBPACK_IMPORTED_MODULE_4__["default"], {
        className: "arrow-move",
        tabIndex: 1,
        stylesheet: buttonStyles,
        onClick: this.onYes,
        size: "standard",
        title: localeString.button_yes,
        type: "primary",
        width: "grow"
      })), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divButtonNo)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_hig_button__WEBPACK_IMPORTED_MODULE_4__["default"], {
        className: "arrow-move",
        tabIndex: 2,
        stylesheet: buttonStyles,
        onClick: this.onNO,
        size: "standard",
        title: localeString.button_no,
        type: "outline",
        width: "grow"
      })), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divHyperlink)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("a", {
        target: "_blank",
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.aHyperlink),
        href: "https://www.autodesk.com/company/legal-notices-trademarks/privacy-statement"
      }, localeString.hyperlink))));
    }
  }]);

  return ConsentDialog;
}(react__WEBPACK_IMPORTED_MODULE_5__["Component"]);

/* harmony default export */ __webpack_exports__["default"] = (ConsentDialog);

/***/ }),

/***/ "./public/src/components/dialogHeader.tsx":
/*!************************************************!*\
  !*** ./public/src/components/dialogHeader.tsx ***!
  \************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es7.symbol.async-iterator */ "./node_modules/core-js/modules/es7.symbol.async-iterator.js");
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.symbol */ "./node_modules/core-js/modules/es6.symbol.js");
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _hig_theme_data_build_esm_lightGrayHighDensityTheme__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @hig/theme-data/build/esm/lightGrayHighDensityTheme */ "./node_modules/@hig/theme-data/build/esm/lightGrayHighDensityTheme.js");
/* harmony import */ var _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @hig/theme-context */ "./node_modules/@hig/theme-context/build/index.es.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var aphrodite__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! aphrodite */ "./node_modules/aphrodite/es/index.js");



function _typeof(obj) { "@babel/helpers - typeof"; if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

function _possibleConstructorReturn(self, call) { if (call && (_typeof(call) === "object" || typeof call === "function")) { return call; } return _assertThisInitialized(self); }

function _getPrototypeOf(o) { _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf(o) { return o.__proto__ || Object.getPrototypeOf(o); }; return _getPrototypeOf(o); }

function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function"); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } }); if (superClass) _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf(o, p) { o.__proto__ = p; return o; }; return _setPrototypeOf(o, p); }





var headStyle = {
  textAlign: "center",
  fontSize: "1.6em",
  fontWeight: 600,
  lineHeight: 1.666666,
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  borderBottom: "1px solid rgba(60,60,60,0.25)"
};
var styles = aphrodite__WEBPACK_IMPORTED_MODULE_5__["StyleSheet"].create({
  header: {
    border: "1px soilid",
    textAlign: "center",
    fontSize: "1.6em",
    fontWeight: 600,
    lineHeight: 1.666666,
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    borderBottom: "1px solid rgba(60,60,60,0.25)"
  },
  headerContainer: {
    marginLeft: "1em",
    marginRight: "1em",
    display: "flex",
    justifyContent: "space-between",
    lineHeight: "calc(36px)"
  },
  parag: {
    margin: "0 5px"
  },
  closeButton: {
    backgroundColor: "transparent",
    borderColor: "transparent",
    borderStyle: "solid",
    borderWidth: 1,
    display: "inline-block",
    position: "relative",
    cursor: "pointer",
    boxSizing: "border-box",
    borderRadius: 2,
    padding: 0,
    height: "calc(36px)",
    lineHeight: "calc(36px)",
    width: "calc(36px)",
    outline: "currentcolor none 0px",
    transitionProperty: "box-shadow, background-color",
    transitionDuration: "0.3s, 0.3s",
    transitionTimingFunction: "cubic-bezier(0.4, 0, 0.2, 1), cubic-bezier(0.4, 0, 0.2, 1)"
  },
  closeSVG: {
    fill: "rgb(128, 128, 128)",
    position: "absolute",
    left: 0,
    right: 0,
    margin: "0px auto",
    top: "50%",
    transform: "translateY(-50%)",
    pointerEvents: "none"
  }
});

var DialogHeader = /*#__PURE__*/function (_React$Component) {
  _inherits(DialogHeader, _React$Component);

  function DialogHeader(props) {
    var _this;

    _classCallCheck(this, DialogHeader);

    _this = _possibleConstructorReturn(this, _getPrototypeOf(DialogHeader).call(this, props));
    _this.onClose = _this.onClose.bind(_assertThisInitialized(_this));
    return _this;
  }

  _createClass(DialogHeader, [{
    key: "onClose",
    value: function onClose() {
      if (this.props.onClick) {
        this.props.onClick();
      }
    }
  }, {
    key: "render",
    value: function render() {
      if (!this.props.separateLine) {
        headStyle.borderBottom = "none";
      }

      return react__WEBPACK_IMPORTED_MODULE_4__["createElement"](_hig_theme_context__WEBPACK_IMPORTED_MODULE_3__["default"].Provider, {
        value: _hig_theme_data_build_esm_lightGrayHighDensityTheme__WEBPACK_IMPORTED_MODULE_2__["default"]
      }, react__WEBPACK_IMPORTED_MODULE_4__["createElement"]("header", {
        style: headStyle
      }, react__WEBPACK_IMPORTED_MODULE_4__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_5__["css"])(styles.headerContainer)
      }, react__WEBPACK_IMPORTED_MODULE_4__["createElement"]("p", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_5__["css"])(styles.parag)
      }, this.props.title), react__WEBPACK_IMPORTED_MODULE_4__["createElement"]("button", {
        tabIndex: -1,
        onClick: this.onClose,
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_5__["css"])(styles.closeButton),
        title: this.props.closeTooltip
      }, react__WEBPACK_IMPORTED_MODULE_4__["createElement"]("svg", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_5__["css"])(styles.closeSVG),
        width: "16px",
        height: "16px",
        viewBox: "0 0 16 16"
      }, react__WEBPACK_IMPORTED_MODULE_4__["createElement"]("title", null, "close-m"), react__WEBPACK_IMPORTED_MODULE_4__["createElement"]("path", {
        d: "M14.4 2.4l-.8-.8L8 7.3 2.4 1.6l-.8.8L7.3 8l-5.7 5.6.8.8L8 8.7l5.6 5.7.8-.8L8.7 8l5.7-5.6z"
      }))))));
    }
  }]);

  return DialogHeader;
}(react__WEBPACK_IMPORTED_MODULE_4__["Component"]);

/* harmony default export */ __webpack_exports__["default"] = (DialogHeader);

/***/ }),

/***/ "./public/src/components/guideDialog.scss":
/*!************************************************!*\
  !*** ./public/src/components/guideDialog.scss ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var api = __webpack_require__(/*! ../../../node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js */ "./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js");
            var content = __webpack_require__(/*! !../../../node_modules/css-loader/dist/cjs.js!../../../node_modules/sass-loader/dist/cjs.js!./guideDialog.scss */ "./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./public/src/components/guideDialog.scss");

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.i, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);

var exported = content.locals ? content.locals : {};



module.exports = exported;

/***/ }),

/***/ "./public/src/components/guideDialog.tsx":
/*!***********************************************!*\
  !*** ./public/src/components/guideDialog.tsx ***!
  \***********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es7.symbol.async-iterator */ "./node_modules/core-js/modules/es7.symbol.async-iterator.js");
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.symbol */ "./node_modules/core-js/modules/es6.symbol.js");
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _hig_theme_data_build_esm_lightGrayHighDensityTheme__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @hig/theme-data/build/esm/lightGrayHighDensityTheme */ "./node_modules/@hig/theme-data/build/esm/lightGrayHighDensityTheme.js");
/* harmony import */ var _hig_theme_context__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @hig/theme-context */ "./node_modules/@hig/theme-context/build/index.es.js");
/* harmony import */ var _hig_button__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @hig/button */ "./node_modules/@hig/button/build/index.es.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var lodash_merge__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! lodash.merge */ "./node_modules/lodash.merge/index.js");
/* harmony import */ var lodash_merge__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(lodash_merge__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var aphrodite__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! aphrodite */ "./node_modules/aphrodite/es/index.js");
/* harmony import */ var _utils_lang__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../utils/lang */ "./public/src/utils/lang.ts");
/* harmony import */ var _dialogHeader__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./dialogHeader */ "./public/src/components/dialogHeader.tsx");
/* harmony import */ var _utils_utils__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ../utils/utils */ "./public/src/utils/utils.ts");
/* harmony import */ var _utils_strings__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ../utils/strings */ "./public/src/utils/strings.ts");
/* harmony import */ var _guideDialog_scss__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./guideDialog.scss */ "./public/src/components/guideDialog.scss");
/* harmony import */ var _guideDialog_scss__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(_guideDialog_scss__WEBPACK_IMPORTED_MODULE_12__);



function _typeof(obj) { "@babel/helpers - typeof"; if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

function _possibleConstructorReturn(self, call) { if (call && (_typeof(call) === "object" || typeof call === "function")) { return call; } return _assertThisInitialized(self); }

function _getPrototypeOf(o) { _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf(o) { return o.__proto__ || Object.getPrototypeOf(o); }; return _getPrototypeOf(o); }

function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function"); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } }); if (superClass) _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf(o, p) { o.__proto__ = p; return o; }; return _setPrototypeOf(o, p); }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }












var default_string = {
  "title": "Sync Your Blocks",
  "step1_title": "1. Sign in to your account.",
  "step1_content": "",
  "step2_title": "2. Select a cloud storage folder.",
  "step2_content": "Blocks will be accessible across devices if you use cloud storage.",
  "step3_title": "3. Access your synced blocks.",
  "step3_content1": "Your blocks can also be accessed in the ",
  "step3_content_ACADWeb": "AutoCAD web app",
  "button_next": "Next",
  "close_tooltip": "close"
};
var styles = aphrodite__WEBPACK_IMPORTED_MODULE_7__["StyleSheet"].create({
  divContainer: {
    display: "flex",
    flexDirection: "column",
    width: "100%",
    /// height: "100%",
    minWidth: "30rem" /// minHeight: "20rem",

  },
  header: {
    fontSize: "1.6em",
    fontWeight: 600,
    margin: "0 auto"
  },
  divListHeader: {
    display: "flex",
    flexDirection: "row"
  },
  img: {
    objectFit: "contain"
  },
  h3: {
    marginTop: 0,
    flexGrow: 2
  },
  paragraph: {
    marginLeft: "5rem",
    fontSize: "1.4rem"
  },
  divButton: {
    /// marginTop: "1rem",
    display: "flex",
    justifyContent: "center"
  },
  aHyperlnk: {
    pointer: "cursor",
    textDecoration: "underline",
    color: "#006EAF"
  }
});

var buttonStyles = function buttonStyles(styles) {
  return lodash_merge__WEBPACK_IMPORTED_MODULE_6___default()(styles, {
    button: {
      fontSize: "1.25em",
      minWidth: "7.2rem",
      borderRadius: 2,
      height: 24
    }
  });
};

;

var GuideDialog = /*#__PURE__*/function (_React$Component) {
  _inherits(GuideDialog, _React$Component);

  function GuideDialog(props) {
    var _this;

    _classCallCheck(this, GuideDialog);

    _this = _possibleConstructorReturn(this, _getPrototypeOf(GuideDialog).call(this, props));

    _defineProperty(_assertThisInitialized(_this), "state", void 0);

    _defineProperty(_assertThisInitialized(_this), "loaded", void 0);

    _this.state = {
      localeString: null
    };
    _this.onNext = _this.onNext.bind(_assertThisInitialized(_this));
    _this.onCancel = _this.onCancel.bind(_assertThisInitialized(_this));
    _this.F1Help = _this.F1Help.bind(_assertThisInitialized(_this));
    _this.onEscapePressed = _this.onEscapePressed.bind(_assertThisInitialized(_this));
    _this.onEnterPressed = _this.onEnterPressed.bind(_assertThisInitialized(_this));
    _this.loaded = false;
    return _this;
  }

  _createClass(GuideDialog, [{
    key: "onEscapePressed",
    value: function onEscapePressed(e) {
      if (e && (e.key === 'Escape' || e.key === 'Esc' || e.keyCode === 27) && e.target.nodeName === 'BODY') {
        e.preventDefault();
        this.onCancel();
      }
    }
  }, {
    key: "onEnterPressed",
    value: function onEnterPressed(e) {
      if (e && (e.key === 'Enter' || e.keyCode === 13 || e.keyCode === 13) && e.target.nodeName === 'BODY') {
        e.preventDefault();
        this.onNext();
      }
    }
  }, {
    key: "onHyperlink",
    value: function onHyperlink() {
      var help_id = "SyncYourBlocksOnboarding";

      if (this.props.onHyperlink) {
        this.props.onHyperlink(help_id);
      }
    }
  }, {
    key: "F1Help",
    value: function F1Help(event) {
      if (event.keyCode === 112) {
        this.onHyperlink();
      }
    }
  }, {
    key: "componentDidMount",
    value: function componentDidMount() {
      var _this2 = this;

      var self = this;
      Object(_utils_lang__WEBPACK_IMPORTED_MODULE_8__["loadLocale"])().then(function (loc) {
        _this2.loaded = true;

        if (!loc || typeof loc.guide_dialog === 'undefined') {
          self.setState({
            localeString: default_string
          });
        } else {
          self.setState({
            localeString: loc.guide_dialog
          });
        }
      });
      document.addEventListener("keydown", this.F1Help, false);
      document.addEventListener("keydown", this.onEscapePressed, false);
      document.addEventListener("keydown", this.onEnterPressed, false);
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      document.removeEventListener("keydown", this.F1Help, false);
      document.removeEventListener("keydown", this.onEscapePressed, false);
      document.removeEventListener("keydown", this.onEnterPressed, false);
    }
  }, {
    key: "componentDidUpdate",
    value: function componentDidUpdate() {
      if (!this.loaded) {
        return;
      }

      this.loaded = false;
      Object(_utils_utils__WEBPACK_IMPORTED_MODULE_10__["adjustHeight"])();
    }
  }, {
    key: "onNext",
    value: function onNext() {
      if (this.props.onNext) {
        this.props.onNext();
      }
    }
  }, {
    key: "onCancel",
    value: function onCancel() {
      if (this.props.onCancel) {
        this.props.onCancel();
      }
    }
  }, {
    key: "render",
    value: function render() {
      var localeString = this.state.localeString;

      if (!localeString) {
        return react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", null);
      }

      return react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_hig_theme_context__WEBPACK_IMPORTED_MODULE_3__["default"].Provider, {
        value: _hig_theme_data_build_esm_lightGrayHighDensityTheme__WEBPACK_IMPORTED_MODULE_2__["default"]
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_dialogHeader__WEBPACK_IMPORTED_MODULE_9__["default"], {
        separateLine: null,
        onClick: this.onCancel,
        title: "",
        closeTooltip: localeString.close_tooltip
      }), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divContainer)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("header", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.header)
      }, localeString.title), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("ul", {
        className: "ul-guide-info"
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("li", {
        className: "li-img-style"
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divListHeader)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("img", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.img),
        srcSet: "\r ./imgs/100/Block_SyncGuideItem1.png 1x,\r ./imgs/150/Block_SyncGuideItem1.png 1.5x,\r ./imgs/200/Block_SyncGuideItem1.png 2x,\r ./imgs/300/Block_SyncGuideItem1.png 3x,\r ./imgs/400/Block_SyncGuideItem1.png 4x\r ",
        style: {
          flexGrow: 0
        }
      }), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("h3", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.h3)
      }, localeString.step1_title)), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", null, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("p", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.paragraph)
      }, localeString.step1_content))), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("li", {
        className: "li-img-style"
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divListHeader)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("img", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.img),
        srcSet: "\r ./imgs/100/Block_SyncGuideItem2.png 1x,\r ./imgs/150/Block_SyncGuideItem2.png 1.5x,\r ./imgs/200/Block_SyncGuideItem2.png 2x,\r ./imgs/300/Block_SyncGuideItem2.png 3x,\r ./imgs/400/Block_SyncGuideItem2.png 4x"
      }), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("h3", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.h3)
      }, localeString.step2_title)), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", null, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("p", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.paragraph)
      }, localeString.step2_content))), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("li", {
        className: "li-img-style"
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divListHeader)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("img", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.img),
        srcSet: "\r ./imgs/100/Block_SyncGuideItem3.png 1x,\r ./imgs/150/Block_SyncGuideItem3.png 1.5x,\r ./imgs/200/Block_SyncGuideItem3.png 2x,\r ./imgs/300/Block_SyncGuideItem3.png 3x,\r ./imgs/400/Block_SyncGuideItem3.png 4x"
      }), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("h3", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.h3)
      }, localeString.step3_title)), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", null, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("p", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.paragraph)
      }, localeString.step3_content1, react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("a", {
        target: "_blank",
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.aHyperlnk),
        href: _utils_strings__WEBPACK_IMPORTED_MODULE_11__["StringResources"].Acad_web_app
      }, localeString.step3_content_ACADWeb), "."))))), react__WEBPACK_IMPORTED_MODULE_5__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_7__["css"])(styles.divButton)
      }, react__WEBPACK_IMPORTED_MODULE_5__["createElement"](_hig_button__WEBPACK_IMPORTED_MODULE_4__["default"], {
        stylesheet: buttonStyles,
        onClick: this.onNext,
        size: "standard",
        title: localeString.button_next,
        type: "primary"
      })));
    }
  }]);

  return GuideDialog;
}(react__WEBPACK_IMPORTED_MODULE_5__["Component"]);

/* harmony default export */ __webpack_exports__["default"] = (GuideDialog);

/***/ }),

/***/ "./public/src/components/syncsettingDialog.scss":
/*!******************************************************!*\
  !*** ./public/src/components/syncsettingDialog.scss ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var api = __webpack_require__(/*! ../../../node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js */ "./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js");
            var content = __webpack_require__(/*! !../../../node_modules/css-loader/dist/cjs.js!../../../node_modules/sass-loader/dist/cjs.js!./syncsettingDialog.scss */ "./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./public/src/components/syncsettingDialog.scss");

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.i, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);

var exported = content.locals ? content.locals : {};



module.exports = exported;

/***/ }),

/***/ "./public/src/components/syncsettingDialog.tsx":
/*!*****************************************************!*\
  !*** ./public/src/components/syncsettingDialog.tsx ***!
  \*****************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es7.symbol.async-iterator */ "./node_modules/core-js/modules/es7.symbol.async-iterator.js");
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.symbol */ "./node_modules/core-js/modules/es6.symbol.js");
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es6.promise */ "./node_modules/core-js/modules/es6.promise.js");
/* harmony import */ var core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es6.object.to-string */ "./node_modules/core-js/modules/es6.object.to-string.js");
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var regenerator_runtime_runtime__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! regenerator-runtime/runtime */ "./node_modules/regenerator-runtime/runtime.js");
/* harmony import */ var regenerator_runtime_runtime__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(regenerator_runtime_runtime__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es6_number_constructor__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es6.number.constructor */ "./node_modules/core-js/modules/es6.number.constructor.js");
/* harmony import */ var core_js_modules_es6_number_constructor__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_number_constructor__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var core_js_modules_es6_number_is_nan__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! core-js/modules/es6.number.is-nan */ "./node_modules/core-js/modules/es6.number.is-nan.js");
/* harmony import */ var core_js_modules_es6_number_is_nan__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_number_is_nan__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var _hig_theme_data_build_esm_lightGrayMediumDensityTheme__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! @hig/theme-data/build/esm/lightGrayMediumDensityTheme */ "./node_modules/@hig/theme-data/build/esm/lightGrayMediumDensityTheme.js");
/* harmony import */ var _hig_theme_context__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! @hig/theme-context */ "./node_modules/@hig/theme-context/build/index.es.js");
/* harmony import */ var _hig_button__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! @hig/button */ "./node_modules/@hig/button/build/index.es.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var lodash_merge__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! lodash.merge */ "./node_modules/lodash.merge/index.js");
/* harmony import */ var lodash_merge__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(lodash_merge__WEBPACK_IMPORTED_MODULE_11__);
/* harmony import */ var aphrodite__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! aphrodite */ "./node_modules/aphrodite/es/index.js");
/* harmony import */ var _utils_lang__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ../utils/lang */ "./public/src/utils/lang.ts");
/* harmony import */ var _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ../interop/acadinterop */ "./public/src/interop/acadinterop.ts");
/* harmony import */ var _hig_input__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! @hig/input */ "./node_modules/@hig/input/build/index.es.js");
/* harmony import */ var _hig_checkbox__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! @hig/checkbox */ "./node_modules/@hig/checkbox/build/index.es.js");
/* harmony import */ var _utils_utils__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ../utils/utils */ "./public/src/utils/utils.ts");
/* harmony import */ var _syncsettingDialog_scss__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./syncsettingDialog.scss */ "./public/src/components/syncsettingDialog.scss");
/* harmony import */ var _syncsettingDialog_scss__WEBPACK_IMPORTED_MODULE_18___default = /*#__PURE__*/__webpack_require__.n(_syncsettingDialog_scss__WEBPACK_IMPORTED_MODULE_18__);








function _typeof(obj) { "@babel/helpers - typeof"; if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

function _possibleConstructorReturn(self, call) { if (call && (_typeof(call) === "object" || typeof call === "function")) { return call; } return _assertThisInitialized(self); }

function _getPrototypeOf(o) { _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf(o) { return o.__proto__ || Object.getPrototypeOf(o); }; return _getPrototypeOf(o); }

function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function"); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } }); if (superClass) _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf(o, p) { o.__proto__ = p; return o; }; return _setPrototypeOf(o, p); }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }














var default_string = {
  title: "Blocks Sync Settings",
  browser_prompt: "Recent blocks and favorites library storage:",
  browser_synced_hint: "Your Blocks are available across devices because you have chosen a cloud storage location.",
  consent_title: "Storage Permission",
  consent_invalid_tooltip: "Make sure you’re connected to the internet and signed in to your Autodesk account to turn on this option.",
  consent_valid_tooltip: "Allows AutoCAD to store your cloud storage user name. Changes made to this setting may not immediately apply.",
  consent_check_string: "Remember my cloud storage usernames across devices.",
  hyperlink: "Learn more",
  OK: "OK",
  Cancel: "Cancel",
  storage_dialog_title: "Select folder for Recent Blocks Location",
  invalid_folder_modal_title: "AutoCAD",
  invalid_folder_modal_content: "Folder does not exist: "
};
var styles = aphrodite__WEBPACK_IMPORTED_MODULE_12__["StyleSheet"].create({
  divContent: {
    margin: "0 25px"
  },
  divBrowser: {
    fontSize: "1.2rem",
    marginBottom: "1.8rem"
  },
  pBrowseTitle: {
    fontWeight: 600,
    marginBlockEnd: "0.5em",
    fontSize: "1.4rem"
  },
  divBrowserRow: {
    height: "2.6rem",
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between"
  },
  divConsent: {
    fontSize: "1.2rem",
    marginBottom: "1.8em"
  },
  pConsentTitle: {
    fontWeight: 600,
    marginBlockEnd: "0.5em",
    fontSize: "1.4rem"
  },
  divConsentCheck: {
    display: "flex",
    flexDirection: "row",
    cursor: "pointer",
    position: "relative"
  },
  pConsentCheck: {
    flexGrow: 2,
    margin: "0 5px"
  },
  pConsentCheckDisable: {
    flexGrow: 2,
    margin: "0 5px",
    color: "#939699"
  },
  divHyperlink: {
    margin: "10px auto",
    fontSize: "1em",
    color: "#006EAF"
  },
  aHyperlink: {
    cursor: "pointer",
    textDecoration: "underline",
    fontWeight: "bold"
  },
  divButtons: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "flex-end",
    height: 24
  },
  divButtonPadding: {
    flexGrow: 6
  },
  iconBtn: {
    height: 24,
    flexGrow: 0
  }
});

var inputStyles = function inputStyles(styles) {
  return lodash_merge__WEBPACK_IMPORTED_MODULE_11___default()(styles, {
    wrapper: {
      flexGrow: 2,
      marginRight: 10
    },
    input: {
      height: "2.4rem",
      paddingTop: 0,
      paddingBottom: 0,
      fontSize: "1.2rem",
      fontWeight: 400
    }
  });
};

var browserBtnStyles = function browserBtnStyles(styles) {
  return lodash_merge__WEBPACK_IMPORTED_MODULE_11___default()(styles, {
    button: {
      flexGrow: 0,
      padding: "2px 8px",
      margin: "1px 0"
    }
  });
};

var okBtnStyles = function okBtnStyles(styles) {
  return lodash_merge__WEBPACK_IMPORTED_MODULE_11___default()(styles, {
    button: {
      flexGrow: 0,
      minWidth: 72,
      fontSize: "1.1em",
      lineHeight: "1.4em",
      borderRadius: 2,
      padding: "4px auto",
      marginLeft: 6,
      marginRight: 6
    }
  });
};

var tooltip_handler = null;

var SyncSettingDialog = /*#__PURE__*/function (_React$Component) {
  _inherits(SyncSettingDialog, _React$Component);

  function SyncSettingDialog(props) {
    var _this;

    _classCallCheck(this, SyncSettingDialog);

    _this = _possibleConstructorReturn(this, _getPrototypeOf(SyncSettingDialog).call(this, props));

    _defineProperty(_assertThisInitialized(_this), "state", void 0);

    _defineProperty(_assertThisInitialized(_this), "inputStr", void 0);

    _defineProperty(_assertThisInitialized(_this), "loaded", void 0);

    _defineProperty(_assertThisInitialized(_this), "tooltips", void 0);

    _defineProperty(_assertThisInitialized(_this), "tooltipsDelay", void 0);

    _this.state = {
      localeString: null,
      recentFolder: null,
      consent: 0,
      inputError: false,
      isLoggedIn: false
    };
    _this.inputStr = null;
    _this.loaded = false;
    _this.tooltips = true;
    _this.tooltipsDelay = 500;
    _this.onOK = _this.onOK.bind(_assertThisInitialized(_this));
    _this.onCancel = _this.onCancel.bind(_assertThisInitialized(_this));
    _this.onInputChange = _this.onInputChange.bind(_assertThisInitialized(_this));
    _this.onHyperlink = _this.onHyperlink.bind(_assertThisInitialized(_this));
    _this.onConsentCheck = _this.onConsentCheck.bind(_assertThisInitialized(_this));
    _this.onBrowse = _this.onBrowse.bind(_assertThisInitialized(_this));
    _this.onEscapePressed = _this.onEscapePressed.bind(_assertThisInitialized(_this));
    _this.showTooltip = _this.showTooltip.bind(_assertThisInitialized(_this));
    _this.hideTooltip = _this.hideTooltip.bind(_assertThisInitialized(_this));
    _this.onMouseMove = _this.onMouseMove.bind(_assertThisInitialized(_this));

    _this.applyTooltips();

    return _this;
  }

  _createClass(SyncSettingDialog, [{
    key: "applyTooltips",
    value: function applyTooltips() {
      var _this2 = this;

      _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.getTooltips().then(function (tooltips) {
        _this2.tooltips = tooltips;

        if (!_this2.tooltips) {
          return;
        }

        _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.getTooltipsDelay().then(function (strDelay) {
          var delay = parseInt(strDelay);

          if (delay && !Number.isNaN(delay) && delay >= 0) {
            _this2.tooltipsDelay = delay;
          }
        });
      });
    }
  }, {
    key: "componentDidMount",
    value: function componentDidMount() {
      var _this3 = this;

      window.addEventListener('keydown', this.onEscapePressed);
      Object(_utils_lang__WEBPACK_IMPORTED_MODULE_13__["loadLocale"])().then(function (loc) {
        window.addEventListener('mousemove', _this3.onMouseMove);

        if (!loc || typeof loc.syncsetting_dialog === 'undefined') {
          _this3.setState({
            localeString: default_string
          });
        } else {
          _this3.setState({
            localeString: loc.syncsetting_dialog
          });
        }

        _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.getBlockSyncFolder().then(function (strFolder) {
          _this3.loaded = true;

          _this3.setState({
            recentFolder: strFolder
          });

          _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.getIsLoggedIn().then(function (isLoggedIn) {
            if (!isLoggedIn) {
              return;
            }

            _this3.setState({
              isLoggedIn: true
            });

            var timeout = 3000;
            _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.getConsentStatus(timeout).then(function (consent) {
              consent = typeof consent === 'undefined' || typeof consent.status === 'undefined' ? 0 : consent.status;

              _this3.setState({
                consent: consent
              });
            });
          });
        });
      });
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      window.removeEventListener('keydown', this.onEscapePressed);
      window.removeEventListener('mousemove', this.onMouseMove);
    }
  }, {
    key: "componentDidUpdate",
    value: function componentDidUpdate() {
      if (!this.loaded) {
        return;
      }

      this.loaded = false;
      Object(_utils_utils__WEBPACK_IMPORTED_MODULE_17__["adjustHeight"])();
    }
  }, {
    key: "showTooltip",
    value: function showTooltip(e) {
      if (!this.tooltips) {
        return;
      }

      if (e) {
        e.preventDefault();
      }

      var elem = document.getElementsByClassName('tooltip');

      if (elem && elem.length > 0) {
        return;
      }

      var _event = e;

      if (!_event || !_event.currentTarget) {
        return;
      }

      var tip = _event.currentTarget.dataset.title;

      if (!tip || tip.length === 0) {
        return;
      } // calculate its absolute position


      var offset = 10;
      var x = _event.clientX + offset;
      var y = _event.clientY + offset;
      var maxTooltipWidth = 350;
      var arr = Object(_utils_utils__WEBPACK_IMPORTED_MODULE_17__["adjustTooltipLocation"])(x, y, maxTooltipWidth, 20);
      x = arr[0];
      y = arr[1];
      tooltip_handler = setTimeout(function () {
        var tooltip_wrapper = document.createElement('div');
        tooltip_wrapper.className = 'tooltip';
        tooltip_wrapper.appendChild(document.createTextNode(tip)); // let's insert tooltip_wrapper to DOM tree

        var first_child = document.body.firstChild;
        first_child.parentNode.insertBefore(tooltip_wrapper, first_child);
        tooltip_wrapper.setAttribute('style', 'top:' + y + 'px;' + 'left:' + x + 'px;');
      }, this.tooltipsDelay);
    }
  }, {
    key: "hideTooltip",
    value: function hideTooltip(e) {
      if (e) {
        e.preventDefault();
      }

      if (tooltip_handler !== null) {
        clearTimeout(tooltip_handler);
        tooltip_handler = null;
      } // remove it from DOM tree


      var elems = document.getElementsByClassName('tooltip');

      if (!elems || elems.length === 0) {
        return;
      }

      for (var ix = 0; ix < elems.length; ix++) {
        elems[ix].remove();
      }
    }
  }, {
    key: "onMouseMove",
    value: function onMouseMove(e) {
      var elem = document.querySelector('.tooltip');

      if (!elem) {
        return;
      }

      var x = e.clientX;
      var y = e.clientY;
      var container = document.getElementById('divConsent');
      var divRect = container === null || container === void 0 ? void 0 : container.getBoundingClientRect();

      if (!divRect) {
        return;
      }

      if (x < divRect.left || x > divRect.right || y < divRect.top || y > divRect.bottom) {
        // Mouse is outside element.
        hideTooltip(e);
      }
    }
  }, {
    key: "onOK",
    value: function () {
      var _onOK = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function _callee() {
        var consent, strFolder, exists, localeString, title, msg;
        return regeneratorRuntime.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                if (!(this.props.onOK != null)) {
                  _context.next = 31;
                  break;
                }

                consent = this.state.consent == 1 ? 1 : 2; // set BlockSyncFolder

                strFolder = this.state.recentFolder;

                if (!(strFolder && strFolder.length > 0)) {
                  _context.next = 30;
                  break;
                }

                _context.prev = 4;
                _context.next = 7;
                return _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.isFolderPathExisted(strFolder);

              case 7:
                exists = _context.sent;

                if (exists) {
                  _context.next = 17;
                  break;
                }

                localeString = this.state.localeString ? this.state.localeString : default_string;
                title = localeString['invalid_folder_modal_title'];
                msg = localeString['invalid_folder_modal_content'];
                _context.next = 14;
                return _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.notifyWarningMessage(title, msg);

              case 14:
                return _context.abrupt("return");

              case 17:
                _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.setBlockSyncFolder(strFolder);
                _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.setConsentStatus(consent);
                _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.setOnBoardingCompleted();
                this.props.onOK();

              case 21:
                _context.next = 28;
                break;

              case 23:
                _context.prev = 23;
                _context.t0 = _context["catch"](4);
                _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.setConsentStatus(consent);
                _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.setOnBoardingCompleted();
                this.props.onOK();

              case 28:
                _context.next = 31;
                break;

              case 30:
                this.props.onOK();

              case 31:
              case "end":
                return _context.stop();
            }
          }
        }, _callee, this, [[4, 23]]);
      }));

      function onOK() {
        return _onOK.apply(this, arguments);
      }

      return onOK;
    }()
  }, {
    key: "onCancel",
    value: function onCancel() {
      if (this.props.onCancel) {
        this.props.onCancel();
      }
    }
  }, {
    key: "onInputChange",
    value: function onInputChange(e) {
      var _this4 = this;

      this.inputStr = "";
      var v = document.getElementById("folder_input").value;
      _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.isFolderPathExisted(v).then(function (existed) {
        if (existed) {
          _this4.setState({
            inputError: false
          });
        } else {
          _this4.setState({
            inputError: true
          });
        }
      });
      this.setState({
        recentFolder: v
      });
    }
  }, {
    key: "onHyperlink",
    value: function onHyperlink() {
      var help_id = "BlockSyncSettingsDlg";

      if (this.props.onHyperlink) {
        this.props.onHyperlink(help_id);
      }
    }
  }, {
    key: "onBrowse",
    value: function () {
      var _onBrowse = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function _callee2() {
        var localeString, title, default_str, res, strFolder;
        return regeneratorRuntime.wrap(function _callee2$(_context2) {
          while (1) {
            switch (_context2.prev = _context2.next) {
              case 0:
                localeString = this.state.localeString;
                title = default_string.storage_dialog_title;

                if (localeString && localeString.storage_dialog_title) {
                  title = localeString.storage_dialog_title;
                }

                default_str = this.state.recentFolder ? this.state.recentFolder : "";
                _context2.next = 6;
                return _interop_acadinterop__WEBPACK_IMPORTED_MODULE_14__["default"].interop.showStorageDialog(title, default_str);

              case 6:
                res = _context2.sent;

                if (res && res.status && res.status === 'Done') {
                  strFolder = res.value;
                  this.setState({
                    recentFolder: strFolder
                  });
                }

              case 8:
              case "end":
                return _context2.stop();
            }
          }
        }, _callee2, this);
      }));

      function onBrowse() {
        return _onBrowse.apply(this, arguments);
      }

      return onBrowse;
    }()
  }, {
    key: "onConsentCheck",
    value: function onConsentCheck(e) {
      if (typeof e === 'boolean') {
        var _value = e ? 1 : 2;

        this.setState({
          consent: _value
        });
      } else {
        var value = 1;

        if (this.state.consent === 1) {
          value = 2;
        }

        this.setState({
          consent: value
        });
      }
    }
  }, {
    key: "onEscapePressed",
    value: function onEscapePressed(e) {
      // check if it is "Esc" pressed
      if (!e) {
        return;
      }

      if ((e.key === 'Escape' || e.key === 'Esc' || e.keyCode === 27) && e.target.nodeName == 'BODY') {
        e.preventDefault();
        this.onCancel();
      }
    }
  }, {
    key: "render",
    value: function render() {
      var consentYes = this.state.consent === 1 ? true : false;
      var localeString = this.state.localeString;
      var strFolder = this.state.recentFolder ? this.state.recentFolder : " ";
      var inputError = this.state.inputError;
      var disableConsent = !this.state.isLoggedIn;

      if (!localeString) {
        return react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", null);
      }

      return react__WEBPACK_IMPORTED_MODULE_10__["createElement"](_hig_theme_context__WEBPACK_IMPORTED_MODULE_8__["default"].Provider, {
        value: _hig_theme_data_build_esm_lightGrayMediumDensityTheme__WEBPACK_IMPORTED_MODULE_7__["default"]
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.divContent)
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.divBrowser)
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", null, react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("p", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.pBrowseTitle)
      }, localeString.browser_prompt)), react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.divBrowserRow)
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"](_hig_input__WEBPACK_IMPORTED_MODULE_15__["default"], {
        id: "folder_input",
        error: inputError,
        stylesheet: inputStyles,
        onChange: this.onInputChange,
        variant: "box",
        value: strFolder
      }), react__WEBPACK_IMPORTED_MODULE_10__["createElement"](_hig_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
        onClick: this.onBrowse,
        stylesheet: browserBtnStyles,
        type: "outline",
        title: "..."
      })), react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", null, react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("p", null, localeString.browser_synced_hint))), react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.divConsent)
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", null, react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("p", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.pConsentTitle)
      }, localeString.consent_title)), disableConsent ? react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", {
        id: "divConsent",
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.divConsentCheck),
        "data-title": localeString.consent_invalid_tooltip,
        onMouseEnter: this.showTooltip,
        onMouseOut: this.hideTooltip
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"](_hig_checkbox__WEBPACK_IMPORTED_MODULE_16__["default"], {
        disabled: true,
        checked: false
      }), react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("p", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.pConsentCheckDisable)
      }, localeString.consent_check_string)) : react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", {
        id: "divConsent",
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.divConsentCheck),
        "data-title": localeString.consent_valid_tooltip,
        onMouseEnter: this.showTooltip,
        onMouseOut: this.hideTooltip
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"](_hig_checkbox__WEBPACK_IMPORTED_MODULE_16__["default"], {
        checked: consentYes,
        onChange: this.onConsentCheck
      }), react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("p", {
        onClick: this.onConsentCheck,
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.pConsentCheck)
      }, localeString.consent_check_string))), react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.divHyperlink)
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("a", {
        tabIndex: -1,
        onClick: this.onHyperlink,
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.aHyperlink)
      }, localeString.hyperlink)), react__WEBPACK_IMPORTED_MODULE_10__["createElement"]("div", {
        className: Object(aphrodite__WEBPACK_IMPORTED_MODULE_12__["css"])(styles.divButtons)
      }, react__WEBPACK_IMPORTED_MODULE_10__["createElement"](_hig_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
        stylesheet: okBtnStyles,
        onClick: this.onOK,
        size: "standard",
        title: localeString.OK,
        type: "primary"
      }), react__WEBPACK_IMPORTED_MODULE_10__["createElement"](_hig_button__WEBPACK_IMPORTED_MODULE_9__["default"], {
        stylesheet: okBtnStyles,
        onClick: this.onCancel,
        size: "standard",
        title: localeString.Cancel,
        type: "outline"
      }))));
    }
  }]);

  return SyncSettingDialog;
}(react__WEBPACK_IMPORTED_MODULE_10__["Component"]);

/* harmony default export */ __webpack_exports__["default"] = (SyncSettingDialog);

/***/ }),

/***/ "./public/src/interop/acadinterop.ts":
/*!*******************************************!*\
  !*** ./public/src/interop/acadinterop.ts ***!
  \*******************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es7.symbol.async-iterator */ "./node_modules/core-js/modules/es7.symbol.async-iterator.js");
/* harmony import */ var core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es7_symbol_async_iterator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.symbol */ "./node_modules/core-js/modules/es6.symbol.js");
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es6.promise */ "./node_modules/core-js/modules/es6.promise.js");
/* harmony import */ var core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es6.object.to-string */ "./node_modules/core-js/modules/es6.object.to-string.js");
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_3__);





function _typeof(obj) { "@babel/helpers - typeof"; if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

var invokeAcadFunction = function invokeAcadFunction(invokeInfo) {
  if (typeof invokeInfo === 'undefined') {
    return null;
  } else if (_typeof(invokeInfo) === 'object') {
    invokeInfo = JSON.stringify(invokeInfo);
  }

  if (typeof execAsync === 'undefined') {
    /// return Promise.reject('Function "execAsync" not available. ');
    return Promise.resolve(true);
  }

  return new Promise(function (resolve, reject) {
    execAsync(invokeInfo, function (data) {
      var result = JSON.parse(data);
      resolve(result.retValue);
    }, function (data) {
      var result = JSON.parse(data);

      try {
        var errorMessage = JSON.parse(result.retErrorString);
        reject(new Error(errorMessage));
      } catch (_unused) {
        reject(result.retErrorString);
      }
    });
  });
};

var onboarding = {
  interop: {
    invokeAcadFunction: invokeAcadFunction,
    ok: function ok() {
      return onboarding.interop.invokeAcadFunction({
        functionName: "HtmlNotificationDialog_Interop.OK",
        invokeAsCommand: false,
        functionParams: {}
      });
    },
    cancel: function cancel() {
      return onboarding.interop.invokeAcadFunction({
        functionName: "HtmlNotificationDialog_Interop.Cancel",
        invokeAsCommand: false,
        functionParams: {}
      });
    },
    signIn: function signIn() {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.signIn",
        invokeAsCommand: false,
        functionParams: {}
      });
    },

    /*
     * return value: {status: 0 - unspecified, 1 - granted, 2 - revoked, 3 - pending revoke}
     */
    getConsentStatus: function getConsentStatus(timeout) {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.getConsentStatus",
        invokeAsCommand: false,
        functionParams: {
          TIMEOUT: timeout
        }
      });
    },
    setConsentStatus: function setConsentStatus(status) {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.setConsentStatus",
        invokeAsCommand: false,
        functionParams: {
          status: status
        }
      });
    },
    launchHelp: function launchHelp(helpId) {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.launchHelp",
        invokeAsCommand: false,
        functionParams: {
          helpId: helpId
        }
      });
    },

    /*
     * return value: sync folder (string)
     */
    getBlockSyncFolder: function getBlockSyncFolder() {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.getBlockSyncFolder",
        invokeAsCommand: false,
        functionParams: {}
      });
    },
    setBlockSyncFolder: function setBlockSyncFolder(v) {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.setBlockSyncFolder",
        invokeAsCommand: false,
        functionParams: {
          "path": v
        }
      });
    },
    showStorageDialog: function showStorageDialog(title, default_str) {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.showStorageDialog",
        invokeAsCommand: false,
        functionParams: {
          title: title,
          default: default_str
        }
      });
    },
    setOnBoardingCompleted: function setOnBoardingCompleted() {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.setOnBoardingCompleted",
        functionParams: {},
        invokeAsCommand: false
      });
    },
    isFolderPathExisted: function isFolderPathExisted(folder) {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.isFolderPathExisted",
        functionParams: {
          folder: folder
        },
        invokeAsCommand: false
      });
    },
    notifyErrorMessage: function notifyErrorMessage(title, msg) {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.notifyErrorMessage",
        functionParams: {
          title: title,
          message: msg
        },
        invokeAsCommand: false
      });
    },
    notifyWarningMessage: function notifyWarningMessage(title, msg) {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.notifyWarningMessage",
        functionParams: {
          title: title,
          message: msg
        },
        invokeAsCommand: false
      });
    },
    getIsLoggedIn: function getIsLoggedIn() {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.getIsLoggedIn",
        functionParams: {},
        invokeAsCommand: false
      });
    },
    adjustHeight: function adjustHeight(heightIncre) {
      onboarding.interop.invokeAcadFunction({
        functionName: "HtmlNotificationDialog_Interop.adjustHeight",
        functionParams: {
          HEIGHTINCRE: heightIncre
        },
        invokeAsCommand: false
      });
    },
    getTooltipsDelay: function getTooltipsDelay() {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.getTooltipsDelay",
        functionParams: {},
        invokeAsCommand: false
      });
    },
    getTooltips: function getTooltips() {
      return onboarding.interop.invokeAcadFunction({
        functionName: "OnBoardingDialog_Interop.getTooltips",
        functionParams: {},
        invokeAsCommand: false
      });
    }
  }
};
/* harmony default export */ __webpack_exports__["default"] = (onboarding);

/***/ }),

/***/ "./public/src/main.tsx":
/*!*****************************!*\
  !*** ./public/src/main.tsx ***!
  \*****************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-dom */ "./node_modules/react-dom/index.js");
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react_dom__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var react_redux__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react-redux */ "./node_modules/react-redux/es/index.js");
/* harmony import */ var redux__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! redux */ "./node_modules/redux/es/redux.js");
/* harmony import */ var _components_appcontainer__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./components/appcontainer */ "./public/src/components/appcontainer.tsx");
/* harmony import */ var _reducer__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./reducer */ "./public/src/reducer/index.ts");






var store = Object(redux__WEBPACK_IMPORTED_MODULE_3__["createStore"])(_reducer__WEBPACK_IMPORTED_MODULE_5__["default"]);
var rootElement = document.getElementById('root');
react_dom__WEBPACK_IMPORTED_MODULE_1___default.a.render(react__WEBPACK_IMPORTED_MODULE_0__["createElement"](react_redux__WEBPACK_IMPORTED_MODULE_2__["Provider"], {
  store: store
}, react__WEBPACK_IMPORTED_MODULE_0__["createElement"](_components_appcontainer__WEBPACK_IMPORTED_MODULE_4__["default"], null)), rootElement);

/***/ }),

/***/ "./public/src/reducer/index.ts":
/*!*************************************!*\
  !*** ./public/src/reducer/index.ts ***!
  \*************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es7_object_get_own_property_descriptors__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es7.object.get-own-property-descriptors */ "./node_modules/core-js/modules/es7.object.get-own-property-descriptors.js");
/* harmony import */ var core_js_modules_es7_object_get_own_property_descriptors__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es7_object_get_own_property_descriptors__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.symbol */ "./node_modules/core-js/modules/es6.symbol.js");
/* harmony import */ var core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_symbol__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_web_dom_iterable__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/web.dom.iterable */ "./node_modules/core-js/modules/web.dom.iterable.js");
/* harmony import */ var core_js_modules_web_dom_iterable__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_iterable__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es6_array_iterator__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es6.array.iterator */ "./node_modules/core-js/modules/es6.array.iterator.js");
/* harmony import */ var core_js_modules_es6_array_iterator__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_array_iterator__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es6.object.to-string */ "./node_modules/core-js/modules/es6.object.to-string.js");
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es6_object_keys__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es6.object.keys */ "./node_modules/core-js/modules/es6.object.keys.js");
/* harmony import */ var core_js_modules_es6_object_keys__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_object_keys__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var redux__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! redux */ "./node_modules/redux/es/redux.js");







function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }



var onBoardingState = function onBoardingState(state, action) {
  state = state || {};

  switch (action.type) {
    case "GUIDEDLG_NEXT":
      return _objectSpread({}, state, {
        dialog: "consent"
      });

    default:
      return state;
  }
};

/* harmony default export */ __webpack_exports__["default"] = (Object(redux__WEBPACK_IMPORTED_MODULE_6__["combineReducers"])({
  onBoardingState: onBoardingState
}));

/***/ }),

/***/ "./public/src/utils/lang.ts":
/*!**********************************!*\
  !*** ./public/src/utils/lang.ts ***!
  \**********************************/
/*! exports provided: loadLocale */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "loadLocale", function() { return loadLocale; });
/* harmony import */ var core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es6.promise */ "./node_modules/core-js/modules/es6.promise.js");
/* harmony import */ var core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_promise__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.object.to-string */ "./node_modules/core-js/modules/es6.object.to-string.js");
/* harmony import */ var core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_object_to_string__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es7_array_includes__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es7.array.includes */ "./node_modules/core-js/modules/es7.array.includes.js");
/* harmony import */ var core_js_modules_es7_array_includes__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es7_array_includes__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./utils */ "./public/src/utils/utils.ts");




var supported_langs = ['en-US', 'en-GB', 'cs-CZ', 'de-DE', 'es-ES', 'fr-FR', 'hu-HU', 'it-IT', 'ja-JP', 'ko-KR', 'pl-PL', 'pt-BR', 'ru-RU', 'zh-CN', 'zh-TW']; // 15 languages

var loadLocale = function loadLocale() {
  var lang = Object(_utils__WEBPACK_IMPORTED_MODULE_3__["getUrlParameter"])('lang');
  lang = lang.length === 0 ? "en-US" : lang;

  if (!supported_langs.includes(lang)) {
    lang = "en-US";
  }

  var lang_file = 'lang/' + lang + "/" + 'index.json';
  var request = new XMLHttpRequest();
  return new Promise(function (resolve, reject) {
    request.open('GET', lang_file);

    request.onload = function () {
      if (request.status === 0 || request.status >= 200 && request.status < 400) {
        var data = JSON.parse(request.responseText);
        resolve(data);
      } else {
        reject(new Error(request.status + " - Can't load locale file."));
      }
    };

    request.onerror = function () {
      reject(new Error("Failed to connect locale file."));
    };

    request.send();
  });
};



/***/ }),

/***/ "./public/src/utils/strings.ts":
/*!*************************************!*\
  !*** ./public/src/utils/strings.ts ***!
  \*************************************/
/*! exports provided: StringResources */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "StringResources", function() { return StringResources; });
var StringResources = {
  "Acad_web_app": "https://web.autocad.com/?ref=desktopblock&utm_source=desktop&utm_medium=block"
};

/***/ }),

/***/ "./public/src/utils/utils.ts":
/*!***********************************!*\
  !*** ./public/src/utils/utils.ts ***!
  \***********************************/
/*! exports provided: getUrlParameter, adjustHeight, adjustTooltipLocation */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getUrlParameter", function() { return getUrlParameter; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "adjustHeight", function() { return adjustHeight; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "adjustTooltipLocation", function() { return adjustTooltipLocation; });
/* harmony import */ var core_js_modules_es6_regexp_search__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es6.regexp.search */ "./node_modules/core-js/modules/es6.regexp.search.js");
/* harmony import */ var core_js_modules_es6_regexp_search__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_regexp_search__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es6_regexp_constructor__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es6.regexp.constructor */ "./node_modules/core-js/modules/es6.regexp.constructor.js");
/* harmony import */ var core_js_modules_es6_regexp_constructor__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_regexp_constructor__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es6_regexp_replace__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es6.regexp.replace */ "./node_modules/core-js/modules/es6.regexp.replace.js");
/* harmony import */ var core_js_modules_es6_regexp_replace__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es6_regexp_replace__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _interop_acadinterop__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../interop/acadinterop */ "./public/src/interop/acadinterop.ts");




var getUrlParameter = function getUrlParameter(name) {
  name = name.replace(/[\[]/, '\\[').replace(/[\]]/, '\\]');
  var regex = new RegExp('[\\?&]' + name + '=([^&#]*)');
  var results = regex.exec(location.search);
  return results === null ? '' : decodeURIComponent(results[1].replace(/\+/g, ' '));
};
var adjustHeight = function adjustHeight() {
  setTimeout(function () {
    window.requestAnimationFrame(function () {
      var bodyHeight = document.body.clientHeight;
      var htmlHeight = document.documentElement.clientHeight;
      var tolerance = 30;

      if (bodyHeight < htmlHeight - tolerance) {
        return;
      }

      var increaseHeight = bodyHeight - htmlHeight;
      var dpi = window.devicePixelRatio;
      _interop_acadinterop__WEBPACK_IMPORTED_MODULE_3__["default"].interop.adjustHeight((increaseHeight + tolerance) * dpi);
    });
  }, 200);
};
var adjustTooltipLocation = function adjustTooltipLocation(x, y, width, height) {
  var htmlWidth = document.documentElement.clientWidth;
  var htmlHeight = document.documentElement.clientHeight;
  var tolerance = 10;

  if (x + width > htmlWidth) {
    x = htmlWidth - width - tolerance;
  }

  if (y + height > htmlHeight) {
    y = htmlHeight - height - tolerance;
  }

  return [x, y];
};

/***/ })

/******/ });
//# sourceMappingURL=bundle.js.map